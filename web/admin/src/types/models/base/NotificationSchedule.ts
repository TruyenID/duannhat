/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Brand } from './Brand';
import type { NotificationAudience } from './NotificationAudience';
import type { Organization } from './Organization';
import type { User } from './User';

/**
 * 通知スケジュール
 */
export interface NotificationSchedule {
  id: string;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  template_key: string;
  audience_id: string;
  audience?: NotificationAudience;
  channels: unknown;
  priority: string;
  params?: unknown | null;
  rrule: string;
  timezone: string;
  starts_at: DateTimeString;
  ends_at?: DateTimeString | null;
  occurrences_remaining?: number | null;
  next_occurrence_at?: DateTimeString | null;
  last_occurrence_at?: DateTimeString | null;
  status: string;
  created_by_id?: string | null;
  createdBy?: User | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationSchedule
 */
export const notificationScheduleI18n = {
  label: {"ja":"通知スケジュール","en":"Notification Schedule","vi":"Lịch thông báo"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    template_key: {
      label: {"ja":"テンプレートキー","en":"Template Key","vi":"Khoá template"},
      placeholder: {"ja":"テンプレートキーを入力","en":"Enter Template Key","vi":"Nhập Khoá template"},
    },
    audience: {
      label: {"ja":"オーディエンス","en":"Audience","vi":"Đối tượng"},
      placeholder: {"ja":"オーディエンスを入力","en":"Enter Audience","vi":"Nhập Đối tượng"},
    },
    channels: {
      label: {"ja":"チャネル","en":"Channels","vi":"Kênh"},
      placeholder: {"ja":"チャネルを入力","en":"Enter Channels","vi":"Nhập Kênh"},
    },
    priority: {
      label: {"ja":"優先度","en":"Priority","vi":"Mức ưu tiên"},
      placeholder: {"ja":"優先度を入力","en":"Enter Priority","vi":"Nhập Mức ưu tiên"},
    },
    params: {
      label: {"ja":"パラメータ","en":"Params","vi":"Tham số"},
      placeholder: {"ja":"パラメータを入力","en":"Enter Params","vi":"Nhập Tham số"},
    },
    rrule: {
      label: {"ja":"RRULE","en":"RRULE","vi":"RRULE"},
      placeholder: {"ja":"RRULEを入力","en":"Enter RRULE","vi":"Nhập RRULE"},
    },
    timezone: {
      label: {"ja":"タイムゾーン","en":"Timezone","vi":"Múi giờ"},
      placeholder: {"ja":"タイムゾーンを入力","en":"Enter Timezone","vi":"Nhập Múi giờ"},
    },
    starts_at: {
      label: {"ja":"開始日時","en":"Starts At","vi":"Bắt đầu"},
      placeholder: {"ja":"開始日時を入力","en":"Enter Starts At","vi":"Nhập Bắt đầu"},
    },
    ends_at: {
      label: {"ja":"終了日時","en":"Ends At","vi":"Kết thúc"},
      placeholder: {"ja":"終了日時を入力","en":"Enter Ends At","vi":"Nhập Kết thúc"},
    },
    occurrences_remaining: {
      label: {"ja":"残オカレンス数","en":"Occurrences Remaining","vi":"Số occurrence còn lại"},
      placeholder: {"ja":"残オカレンス数を入力","en":"Enter Occurrences Remaining","vi":"Nhập Số occurrence còn lại"},
    },
    next_occurrence_at: {
      label: {"ja":"次回発火日時","en":"Next Occurrence At","vi":"Lần sau"},
      placeholder: {"ja":"次回発火日時を入力","en":"Enter Next Occurrence At","vi":"Nhập Lần sau"},
    },
    last_occurrence_at: {
      label: {"ja":"最終発火日時","en":"Last Occurrence At","vi":"Lần trước"},
      placeholder: {"ja":"最終発火日時を入力","en":"Enter Last Occurrence At","vi":"Nhập Lần trước"},
    },
    status: {
      label: {"ja":"ステータス","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"ステータスを入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    created_by: {
      label: {"ja":"作成者","en":"Created By","vi":"Người tạo"},
      placeholder: {"ja":"作成者を入力","en":"Enter Created By","vi":"Nhập Người tạo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationSchedule */
export const baseNotificationScheduleSchemas = {
  template_key: z.string().min(1).max(100),
  channels: z.unknown(),
  priority: z.string().min(1).max(20),
  params: z.unknown().optional().nullable(),
  rrule: z.string().min(1).max(500),
  timezone: z.string().min(1).max(64),
  starts_at: z.string().datetime({ offset: true }),
  ends_at: z.string().datetime({ offset: true }).optional().nullable(),
  occurrences_remaining: z.number().int().optional().nullable(),
  next_occurrence_at: z.string().datetime({ offset: true }).optional().nullable(),
  last_occurrence_at: z.string().datetime({ offset: true }).optional().nullable(),
  status: z.string().min(1).max(20),
} as const;

/** Create schema for NotificationSchedule */
export const baseNotificationScheduleCreateSchema = z.object({
  template_key: baseNotificationScheduleSchemas.template_key,
  channels: baseNotificationScheduleSchemas.channels,
  priority: baseNotificationScheduleSchemas.priority,
  params: baseNotificationScheduleSchemas.params,
  rrule: baseNotificationScheduleSchemas.rrule,
  timezone: baseNotificationScheduleSchemas.timezone,
  starts_at: baseNotificationScheduleSchemas.starts_at,
  ends_at: baseNotificationScheduleSchemas.ends_at,
  occurrences_remaining: baseNotificationScheduleSchemas.occurrences_remaining,
  next_occurrence_at: baseNotificationScheduleSchemas.next_occurrence_at,
  last_occurrence_at: baseNotificationScheduleSchemas.last_occurrence_at,
  status: baseNotificationScheduleSchemas.status,
});

/** Update schema for NotificationSchedule */
export const baseNotificationScheduleUpdateSchema = baseNotificationScheduleCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationScheduleCreate = z.infer<typeof baseNotificationScheduleCreateSchema>;
export type BaseNotificationScheduleUpdate = z.infer<typeof baseNotificationScheduleUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationScheduleLabel(locale: string): string {
  return notificationScheduleI18n.label[locale as keyof typeof notificationScheduleI18n.label] ?? notificationScheduleI18n.label['en'] ?? 'NotificationSchedule';
}

/** Get field label for a specific locale */
export function getNotificationScheduleFieldLabel(field: string, locale: string): string {
  const fields = notificationScheduleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationScheduleFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationScheduleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationSchedule.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationScheduleMetadata = {
  modelName: "NotificationSchedule",
  table: "notification_schedules",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    template_key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "テンプレートキー",
        en: "Template Key",
        vi: "Khoá template",
      },
      placeholder: {
        ja: "テンプレートキーを入力",
        en: "Enter Template Key",
        vi: "Nhập Khoá template",
      },
      description: {
        ja: "`notification_templates.key` へのソフトポインタ（FK ではない — 管理者がテンプレートを削除した場合、スケジュールのルックアップはログ警告 + インボックスのリテラルキーにフォールバック）。例：`stock.alert.low`、`system.weekly_digest`。",
        en: "Soft pointer to `notification_templates.key` (not a FK — admin may delete a template and the schedule's lookup will fall back to a logged warning + literal key in the inbox). Examples: `stock.alert.low`, `system.weekly_digest`.",
        vi: "Soft pointer tới `notification_templates.key` (không phải FK — admin có thể xoá template, lookup của schedule sẽ fall back về warning log + literal key trong inbox). Ví dụ: `stock.alert.low`, `system.weekly_digest`.",
      },
    },
    audience_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "NotificationAudience",
        table: "notification_audiences",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    channels: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "チャネル",
        en: "Channels",
        vi: "Kênh",
      },
      placeholder: {
        ja: "チャネルを入力",
        en: "Enter Channels",
        vi: "Nhập Kênh",
      },
      description: {
        ja: "全てのオカレンスに適用される NotificationChannel enum 値の配列（例：`['in_app', 'email']`）。コンポーザーステップ3でマルチセレクト。作成時に省略された場合、テンプレートの `default_channels` がデフォルト。",
        en: "Array of NotificationChannel enum values applied to every occurrence (e.g. `['in_app', 'email']`). Multi-select on composer step 3. Defaults to the template's `default_channels` if omitted at create time.",
        vi: "Mảng giá trị NotificationChannel enum áp dụng cho mọi occurrence (VD: `['in_app', 'email']`). Multi-select ở composer bước 3. Default về `default_channels` của template nếu bỏ qua khi tạo.",
      },
    },
    priority: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "優先度",
        en: "Priority",
        vi: "Mức ưu tiên",
      },
      placeholder: {
        ja: "優先度を入力",
        en: "Enter Priority",
        vi: "Nhập Mức ưu tiên",
      },
      description: {
        ja: "NotificationPriority enum 値（`low` | `normal` | `high` | `urgent`）。EffectiveChannelService の urgent-bypass ルールを駆動 — urgent オカレンスは master_mute + quiet_hours を無視するが、per-(type × channel) opt-out は引き続き尊重する。",
        en: "NotificationPriority enum value (`low` | `normal` | `high` | `urgent`). Drives the urgent-bypass rule in EffectiveChannelService — urgent occurrences ignore master_mute + quiet_hours but still honour per-(type × channel) opt-out.",
        vi: "Giá trị enum NotificationPriority (`low` | `normal` | `high` | `urgent`). Điều khiển rule urgent-bypass trong EffectiveChannelService — occurrence urgent bỏ qua master_mute + quiet_hours nhưng vẫn tôn trọng per-(type × channel) opt-out.",
      },
    },
    params: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "パラメータ",
        en: "Params",
        vi: "Tham số",
      },
      placeholder: {
        ja: "パラメータを入力",
        en: "Enter Params",
        vi: "Nhập Tham số",
      },
      description: {
        ja: "`TemplateRenderer::render` のトークン置換バッグ（例：`{warehouse_name: 'Tokyo HQ', threshold: 100}`）。Nullable — `{{tokens}}` を持たないテンプレートはパラメータ不要。",
        en: "Token substitution bag for `TemplateRenderer::render` (e.g. `{warehouse_name: 'Tokyo HQ', threshold: 100}`). Nullable — templates without `{{tokens}}` need no params.",
        vi: "Bag thay token cho `TemplateRenderer::render` (VD: `{warehouse_name: 'Tokyo HQ', threshold: 100}`). Nullable — template không có `{{tokens}}` không cần params.",
      },
    },
    rrule: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 500,
      },
      label: {
        ja: "RRULE",
        en: "RRULE",
        vi: "RRULE",
      },
      placeholder: {
        ja: "RRULEを入力",
        en: "Enter RRULE",
        vi: "Nhập RRULE",
      },
      description: {
        ja: "RFC-5545 RRULE サブストリング（`BEGIN:VEVENT`/`END:VEVENT` ラッパーなし）。例：`FREQ=DAILY;BYHOUR=9;BYMINUTE=0`、`FREQ=WEEKLY;BYDAY=MO,WE,FR;BYHOUR=9`。ティック時に `\\Recurr\\Rule` で解析；無効な RRULE → スケジュール作成は 422 を返す。",
        en: "RFC-5545 RRULE substring (no `BEGIN:VEVENT`/`END:VEVENT` wrapper). Examples: `FREQ=DAILY;BYHOUR=9;BYMINUTE=0`, `FREQ=WEEKLY;BYDAY=MO,WE,FR;BYHOUR=9`. Parsed by `\\Recurr\\Rule` at tick time; invalid RRULE → schedule create returns 422.",
        vi: "RRULE substring RFC-5545 (không có wrapper `BEGIN:VEVENT`/`END:VEVENT`). VD: `FREQ=DAILY;BYHOUR=9;BYMINUTE=0`, `FREQ=WEEKLY;BYDAY=MO,WE,FR;BYHOUR=9`. Parse bằng `\\Recurr\\Rule` lúc tick; RRULE không hợp lệ → schedule create trả 422.",
      },
    },
    timezone: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "タイムゾーン",
        en: "Timezone",
        vi: "Múi giờ",
      },
      placeholder: {
        ja: "タイムゾーンを入力",
        en: "Enter Timezone",
        vi: "Nhập Múi giờ",
      },
      description: {
        ja: "IANA tz 名（例：`Asia/Tokyo`、`America/Los_Angeles`）。ティックごとに再読み込みされ、DST 遷移時にオカレンスが正しく進む。作成時に `DateTimeZone::listIdentifiers()` に対して検証。",
        en: "IANA tz name (e.g. `Asia/Tokyo`, `America/Los_Angeles`). Re-read on every tick so DST transitions advance the occurrence correctly. Validated against `DateTimeZone::listIdentifiers()` on create.",
        vi: "Tên tz IANA (VD: `Asia/Tokyo`, `America/Los_Angeles`). Đọc lại mỗi tick để DST chuyển đúng. Validate với `DateTimeZone::listIdentifiers()` khi tạo.",
      },
    },
    starts_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "開始日時",
        en: "Starts At",
        vi: "Bắt đầu",
      },
      placeholder: {
        ja: "開始日時を入力",
        en: "Enter Starts At",
        vi: "Nhập Bắt đầu",
      },
      description: {
        ja: "最初の対象オカレンス。UTC。RRULE イテレータはこの日時から基準を取り；RRULE が一致してもこの日時より前のオカレンスはスキップされる。",
        en: "First eligible occurrence. UTC. The RRULE iterator anchors from this datetime; occurrences before `starts_at` are skipped even if the RRULE would otherwise match.",
        vi: "Occurrence đầu tiên đủ điều kiện. UTC. Iterator RRULE neo từ datetime này; occurrence trước `starts_at` bị skip dù RRULE có match.",
      },
    },
    ends_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "終了日時",
        en: "Ends At",
        vi: "Kết thúc",
      },
      placeholder: {
        ja: "終了日時を入力",
        en: "Enter Ends At",
        vi: "Nhập Kết thúc",
      },
      description: {
        ja: "最後の対象オカレンス（境界を含む）。Nullable → `occurrences_remaining` 枯渇（設定時）または管理者キャンセルまでスケジュールは無期限に実行。ティックが `next_occurrence_at > ends_at` を観測すると `status` を `completed` にフリップ。",
        en: "Last eligible occurrence (inclusive). Nullable → schedule runs indefinitely until `occurrences_remaining` exhausts (if set) or admin cancels. Flips `status` to `completed` once tick observes `next_occurrence_at > ends_at`.",
        vi: "Occurrence cuối cùng (inclusive). Nullable → schedule chạy vô hạn cho tới khi `occurrences_remaining` cạn (nếu set) hoặc admin cancel. Tick quan sát `next_occurrence_at > ends_at` thì flip `status` về `completed`.",
      },
    },
    occurrences_remaining: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "残オカレンス数",
        en: "Occurrences Remaining",
        vi: "Số occurrence còn lại",
      },
      placeholder: {
        ja: "残オカレンス数を入力",
        en: "Enter Occurrences Remaining",
        vi: "Nhập Số occurrence còn lại",
      },
      description: {
        ja: "\"あとN回送信\" カウンタ。Nullable はカウント制限なし（代わりに `ends_at` を使うか、無期限に実行）。ティックは成功的な materialise ごとにデクリメント；0 になると status は `completed` にフリップ、next_occurrence_at は null になる。",
        en: "\"Send N more times\" counter. Nullable means no count limit (use `ends_at` instead, or run indefinitely). Tick decrements on every successful materialise; hits 0 → status flips to `completed`, next_occurrence_at goes null.",
        vi: "Đếm \"gửi thêm N lần\". Nullable nghĩa là không giới hạn số lần (dùng `ends_at` thay thế, hoặc chạy vô hạn). Tick decrement mỗi lần materialise thành công; về 0 → status flip về `completed`, next_occurrence_at thành null.",
      },
    },
    next_occurrence_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "次回発火日時",
        en: "Next Occurrence At",
        vi: "Lần sau",
      },
      placeholder: {
        ja: "次回発火日時を入力",
        en: "Enter Next Occurrence At",
        vi: "Nhập Lần sau",
      },
      description: {
        ja: "次にティックワーカーが materialise する日時（UTC）。作成時 + 毎ティック後に `\\\\Recurr\\\\Transformer\\\\ArrayTransformer` で計算。ステータスがターミナル（`completed`/`cancelled`）の場合 null。",
        en: "Next datetime the tick worker will materialise (UTC). Computed by `\\\\Recurr\\\\Transformer\\\\ArrayTransformer` at create time + after every tick. Null when status is terminal (`completed`/`cancelled`).",
        vi: "Datetime tiếp theo mà tick worker materialise (UTC). Tính bằng `\\\\Recurr\\\\Transformer\\\\ArrayTransformer` lúc tạo + sau mỗi tick. Null khi status đã terminal (`completed`/`cancelled`).",
      },
    },
    last_occurrence_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最終発火日時",
        en: "Last Occurrence At",
        vi: "Lần trước",
      },
      placeholder: {
        ja: "最終発火日時を入力",
        en: "Enter Last Occurrence At",
        vi: "Nhập Lần trước",
      },
      description: {
        ja: "最新の成功オカレンス（UTC）。Notification 行が書き込まれた直後、next_occurrence_at が進む前に `RecurringNotificationDispatcher::tick` が更新。スケジュールのバッジが「3時間前に発火」と表示する場合の管理者診断に有用。",
        en: "Most recent successful occurrence (UTC). Updated by `RecurringNotificationDispatcher::tick` immediately after a Notification row is written + before next_occurrence_at advances. Useful for admin diagnostics when a schedule's badge says \"fired 3h ago\".",
        vi: "Occurrence gần nhất thành công (UTC). Update bởi `RecurringNotificationDispatcher::tick` ngay sau khi Notification row được ghi + trước khi next_occurrence_at tiến. Hữu ích cho diagnostics khi badge schedule hiển thị \"fired 3h ago\".",
      },
    },
    status: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "ステータス",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "ステータスを入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
      description: {
        ja: "ライフサイクル状態 — `active`（ティックが materialise）、`paused`（ティックはスキップ）、`completed`（ターミナル、RRULE 枯渇）、`cancelled`（ターミナル、フリーズウィンドウ前の管理者 DELETE）。状態遷移：active ↔ paused、active → completed（ティック）、active → cancelled（管理者）、paused → cancelled（管理者）。",
        en: "Lifecycle state — `active` (tick materialises), `paused` (tick skips), `completed` (terminal, RRULE exhausted), `cancelled` (terminal, admin DELETE before freeze window). State transitions are: active ↔ paused, active → completed (tick), active → cancelled (admin), paused → cancelled (admin).",
        vi: "Trạng thái lifecycle — `active` (tick materialise), `paused` (tick skip), `completed` (terminal, RRULE cạn), `cancelled` (terminal, admin DELETE trước freeze window). Chuyển trạng thái: active ↔ paused, active → completed (tick), active → cancelled (admin), paused → cancelled (admin).",
      },
    },
    created_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "brand_id", "template_key", "audience_id", "channels", "priority", "rrule", "timezone", "starts_at", "status"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "audience_id", "created_by_id"] as const,
} as const;

export type NotificationScheduleFieldName = keyof typeof notificationScheduleMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationSchedule. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationScheduleCreateFormState {
  organization_id: string;
  brand_id: string;
  template_key: string;
  audience_id: string;
  channels: unknown;
  priority: string;
  params?: unknown | null;
  rrule: string;
  timezone: string;
  starts_at: string;
  ends_at?: string | null;
  occurrences_remaining?: number | null;
  next_occurrence_at?: string | null;
  last_occurrence_at?: string | null;
  status: string;
  created_by_id?: string | null;
}

/** Form-state shape for updating a NotificationSchedule. Every field optional. */
export interface NotificationScheduleUpdateFormState {
  organization_id?: string;
  brand_id?: string;
  template_key?: string;
  audience_id?: string;
  channels?: unknown;
  priority?: string;
  params?: unknown | null;
  rrule?: string;
  timezone?: string;
  starts_at?: string;
  ends_at?: string | null;
  occurrences_remaining?: number | null;
  next_occurrence_at?: string | null;
  last_occurrence_at?: string | null;
  status?: string;
  created_by_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationScheduleCreateForm(): NotificationScheduleCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    template_key: '',
    audience_id: '',
    channels: null,
    priority: "normal",
    params: null,
    rrule: '',
    timezone: "Asia/Tokyo",
    starts_at: '',
    ends_at: '',
    occurrences_remaining: 0,
    next_occurrence_at: '',
    last_occurrence_at: '',
    status: "active",
    created_by_id: '',
  };
}

/** Convert create form state into the payload that baseNotificationScheduleCreateSchema accepts. */
export function buildNotificationScheduleCreatePayload(form: NotificationScheduleCreateFormState): BaseNotificationScheduleCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    template_key: form.template_key.trim(),
    audience_id: form.audience_id.trim(),
    channels: form.channels,
    priority: form.priority.trim(),
    params: form.params,
    rrule: form.rrule.trim(),
    timezone: form.timezone.trim(),
    starts_at: form.starts_at,
    ends_at: form.ends_at,
    occurrences_remaining: form.occurrences_remaining,
    next_occurrence_at: form.next_occurrence_at,
    last_occurrence_at: form.last_occurrence_at,
    status: form.status.trim(),
    created_by_id: form.created_by_id?.toString().trim() || null,
  };

  return payload as BaseNotificationScheduleCreate;
}

/** Convert update form state into the payload that baseNotificationScheduleUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationScheduleUpdatePayload(form: NotificationScheduleUpdateFormState): BaseNotificationScheduleUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.template_key !== undefined) {
    payload.template_key = form.template_key?.toString().trim() ?? null;
  }
  if (form.audience_id !== undefined) {
    payload.audience_id = form.audience_id?.toString().trim() ?? null;
  }
  if (form.channels !== undefined) {
    payload.channels = form.channels;
  }
  if (form.priority !== undefined) {
    payload.priority = form.priority?.toString().trim() ?? null;
  }
  if (form.params !== undefined) {
    payload.params = form.params;
  }
  if (form.rrule !== undefined) {
    payload.rrule = form.rrule?.toString().trim() ?? null;
  }
  if (form.timezone !== undefined) {
    payload.timezone = form.timezone?.toString().trim() ?? null;
  }
  if (form.starts_at !== undefined) {
    payload.starts_at = form.starts_at;
  }
  if (form.ends_at !== undefined) {
    payload.ends_at = form.ends_at;
  }
  if (form.occurrences_remaining !== undefined) {
    payload.occurrences_remaining = form.occurrences_remaining;
  }
  if (form.next_occurrence_at !== undefined) {
    payload.next_occurrence_at = form.next_occurrence_at;
  }
  if (form.last_occurrence_at !== undefined) {
    payload.last_occurrence_at = form.last_occurrence_at;
  }
  if (form.status !== undefined) {
    payload.status = form.status?.toString().trim() ?? null;
  }
  if (form.created_by_id !== undefined) {
    payload.created_by_id = form.created_by_id?.toString().trim() ?? null;
  }
  return payload as BaseNotificationScheduleUpdate;
}
