/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { DenominationKind } from '../enum/DenominationKind';
import type { Organization } from './Organization';

/**
 * 額面
 */
export interface Denomination {
  id: string;
  currency_code: string;
  value: number;
  kind: DenominationKind;
  label?: string | null;
  sort_order: number;
  is_active: boolean;
  organization_id?: string | null;
  organization?: Organization | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Denomination
 */
export const denominationI18n = {
  label: {"ja":"額面","en":"Denomination","vi":"Mệnh giá"},
  fields: {
    currency_code: {
      label: {"ja":"通貨","en":"Currency","vi":"Tiền tệ"},
      placeholder: {"ja":"通貨を入力","en":"Enter Currency","vi":"Nhập Tiền tệ"},
    },
    value: {
      label: {"ja":"額面","en":"Value","vi":"Giá trị"},
      placeholder: {"ja":"額面を入力","en":"Enter Value","vi":"Nhập Giá trị"},
    },
    kind: {
      label: {"ja":"種別","en":"Kind","vi":"Loại"},
      placeholder: {"ja":"種別を入力","en":"Enter Kind","vi":"Nhập Loại"},
    },
    label: {
      label: {"ja":"ラベル","en":"Label","vi":"Nhãn"},
      placeholder: {"ja":"ラベルを入力","en":"Enter Label","vi":"Nhập Nhãn"},
    },
    sort_order: {
      label: {"ja":"表示順","en":"Sort Order","vi":"Thứ tự"},
      placeholder: {"ja":"表示順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Denomination */
export const baseDenominationSchemas = {
  currency_code: z.string().min(1).max(3),
  value: z.number(),
  kind: z.nativeEnum(DenominationKind),
  label: z.string().max(50).optional().nullable(),
  sort_order: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for Denomination */
export const baseDenominationCreateSchema = z.object({
  currency_code: baseDenominationSchemas.currency_code,
  value: baseDenominationSchemas.value,
  kind: baseDenominationSchemas.kind,
  label: baseDenominationSchemas.label,
  sort_order: baseDenominationSchemas.sort_order,
  is_active: baseDenominationSchemas.is_active,
});

/** Update schema for Denomination */
export const baseDenominationUpdateSchema = baseDenominationCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseDenominationCreate = z.infer<typeof baseDenominationCreateSchema>;
export type BaseDenominationUpdate = z.infer<typeof baseDenominationUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getDenominationLabel(locale: string): string {
  return denominationI18n.label[locale as keyof typeof denominationI18n.label] ?? denominationI18n.label['en'] ?? 'Denomination';
}

/** Get field label for a specific locale */
export function getDenominationFieldLabel(field: string, locale: string): string {
  const fields = denominationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getDenominationFieldPlaceholder(field: string, locale: string): string {
  const fields = denominationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Denomination.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const denominationMetadata = {
  modelName: "Denomination",
  table: "denominations",

  fields: {
    currency_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 3,
      },
      label: {
        ja: "通貨",
        en: "Currency",
        vi: "Tiền tệ",
      },
      placeholder: {
        ja: "通貨を入力",
        en: "Enter Currency",
        vi: "Nhập Tiền tệ",
      },
    },
    value: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "額面",
        en: "Value",
        vi: "Giá trị",
      },
      placeholder: {
        ja: "額面を入力",
        en: "Enter Value",
        vi: "Nhập Giá trị",
      },
    },
    kind: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "DenominationKind",
      enumValues: ["note", "coin"] as const,
      label: {
        ja: "種別",
        en: "Kind",
        vi: "Loại",
      },
      placeholder: {
        ja: "種別を入力",
        en: "Enter Kind",
        vi: "Nhập Loại",
      },
    },
    label: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "ラベル",
        en: "Label",
        vi: "Nhãn",
      },
      placeholder: {
        ja: "ラベルを入力",
        en: "Enter Label",
        vi: "Nhập Nhãn",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Sort Order",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    organization_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["currency_code", "value", "kind", "sort_order", "is_active"] as const,
  enumFields: ["kind"] as const,
  relationFields: ["organization_id"] as const,
} as const;

export type DenominationFieldName = keyof typeof denominationMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Denomination. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface DenominationCreateFormState {
  currency_code: string;
  value: number;
  kind: DenominationKind;
  label?: string | null;
  sort_order: number;
  is_active: boolean;
  organization_id?: string | null;
}

/** Form-state shape for updating a Denomination. Every field optional. */
export interface DenominationUpdateFormState {
  currency_code?: string;
  value?: number;
  kind?: DenominationKind;
  label?: string | null;
  sort_order?: number;
  is_active?: boolean;
  organization_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyDenominationCreateForm(): DenominationCreateFormState {
  return {
    currency_code: '',
    value: 0,
    kind: DenominationKind.Note,
    label: '',
    sort_order: 0,
    is_active: true,
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseDenominationCreateSchema accepts. */
export function buildDenominationCreatePayload(form: DenominationCreateFormState): BaseDenominationCreate {
  const payload: Record<string, unknown> = {
    currency_code: form.currency_code.trim(),
    value: form.value,
    kind: form.kind,
    label: form.label?.toString().trim() || null,
    sort_order: form.sort_order,
    is_active: form.is_active,
    organization_id: form.organization_id?.toString().trim() || null,
  };

  return payload as BaseDenominationCreate;
}

/** Convert update form state into the payload that baseDenominationUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildDenominationUpdatePayload(form: DenominationUpdateFormState): BaseDenominationUpdate {
  const payload: Record<string, unknown> = {};
  if (form.currency_code !== undefined) {
    payload.currency_code = form.currency_code?.toString().trim() ?? null;
  }
  if (form.value !== undefined) {
    payload.value = form.value;
  }
  if (form.kind !== undefined) {
    payload.kind = form.kind;
  }
  if (form.label !== undefined) {
    payload.label = form.label?.toString().trim() ?? null;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseDenominationUpdate;
}
