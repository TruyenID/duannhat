/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { StockAlertStatus } from '../enum/StockAlertStatus';
import { StockAlertType } from '../enum/StockAlertType';
import type { Material } from './Material';
import type { Organization } from './Organization';
import type { ProductSku } from './ProductSku';
import type { Warehouse } from './Warehouse';

/**
 * 在庫アラート
 */
export interface StockAlert {
  id: string;
  warehouse_id: string;
  warehouse?: Warehouse;
  product_sku_id?: string | null;
  productSku?: ProductSku | null;
  material_id?: string | null;
  material?: Material | null;
  alert_type: StockAlertType;
  current_quantity: number;
  min_stock?: number | null;
  unit?: string | null;
  status: StockAlertStatus;
  resolved_at?: DateTimeString | null;
  created_at: DateTimeString;
  organization_id: string;
  organization?: Organization;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for StockAlert
 */
export const stockAlertI18n = {
  label: {"ja":"在庫アラート","en":"Stock Alert","vi":"Cảnh báo tồn kho"},
  fields: {
    warehouse: {
      label: {"ja":"倉庫","en":"Warehouse","vi":"Kho hàng"},
      placeholder: {"ja":"倉庫を入力","en":"Enter Warehouse","vi":"Nhập Kho hàng"},
    },
    product_sku: {
      label: {"ja":"商品バリエーション","en":"Product Variant","vi":"Biến thể sản phẩm"},
      placeholder: {"ja":"商品バリエーションを入力","en":"Enter Product Variant","vi":"Nhập Biến thể sản phẩm"},
    },
    material: {
      label: {"ja":"材料","en":"Material","vi":"Nguyên liệu"},
      placeholder: {"ja":"材料を入力","en":"Enter Material","vi":"Nhập Nguyên liệu"},
    },
    alert_type: {
      label: {"ja":"アラートタイプ","en":"Alert Type","vi":"Loại cảnh báo"},
      placeholder: {"ja":"アラートタイプを入力","en":"Enter Alert Type","vi":"Nhập Loại cảnh báo"},
    },
    current_quantity: {
      label: {"ja":"現在数量","en":"Current Quantity","vi":"Tồn kho hiện tại"},
      placeholder: {"ja":"現在数量を入力","en":"Enter Current Quantity","vi":"Nhập Tồn kho hiện tại"},
    },
    min_stock: {
      label: {"ja":"最小在庫しきい値","en":"Min Stock Threshold","vi":"Ngưỡng tối thiểu"},
      placeholder: {"ja":"最小在庫しきい値を入力","en":"Enter Min Stock Threshold","vi":"Nhập Ngưỡng tối thiểu"},
    },
    unit: {
      label: {"ja":"単位","en":"Unit","vi":"Đơn vị"},
      placeholder: {"ja":"単位を入力","en":"Enter Unit","vi":"Nhập Đơn vị"},
    },
    status: {
      label: {"ja":"ステータス","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"ステータスを入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    resolved_at: {
      label: {"ja":"解決日時","en":"Resolved At","vi":"Thời điểm giải quyết"},
      placeholder: {"ja":"解決日時を入力","en":"Enter Resolved At","vi":"Nhập Thời điểm giải quyết"},
    },
    created_at: {
      label: {"ja":"作成日時","en":"Created At","vi":"Ngày tạo"},
      placeholder: {"ja":"作成日時を入力","en":"Enter Created At","vi":"Nhập Ngày tạo"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for StockAlert */
export const baseStockAlertSchemas = {
  alert_type: z.nativeEnum(StockAlertType),
  current_quantity: z.number(),
  min_stock: z.number().optional().nullable(),
  unit: z.string().max(50).optional().nullable(),
  status: z.nativeEnum(StockAlertStatus),
  resolved_at: z.string().datetime({ offset: true }).optional().nullable(),
  created_at: z.string().datetime({ offset: true }),
} as const;

/** Create schema for StockAlert */
export const baseStockAlertCreateSchema = z.object({
  alert_type: baseStockAlertSchemas.alert_type,
  current_quantity: baseStockAlertSchemas.current_quantity,
  min_stock: baseStockAlertSchemas.min_stock,
  unit: baseStockAlertSchemas.unit,
  status: baseStockAlertSchemas.status,
  resolved_at: baseStockAlertSchemas.resolved_at,
  created_at: baseStockAlertSchemas.created_at,
});

/** Update schema for StockAlert */
export const baseStockAlertUpdateSchema = baseStockAlertCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseStockAlertCreate = z.infer<typeof baseStockAlertCreateSchema>;
export type BaseStockAlertUpdate = z.infer<typeof baseStockAlertUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getStockAlertLabel(locale: string): string {
  return stockAlertI18n.label[locale as keyof typeof stockAlertI18n.label] ?? stockAlertI18n.label['en'] ?? 'StockAlert';
}

/** Get field label for a specific locale */
export function getStockAlertFieldLabel(field: string, locale: string): string {
  const fields = stockAlertI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getStockAlertFieldPlaceholder(field: string, locale: string): string {
  const fields = stockAlertI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for StockAlert.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const stockAlertMetadata = {
  modelName: "StockAlert",
  table: "stock_alerts",

  fields: {
    warehouse_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Warehouse",
        table: "warehouses",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    material_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Material",
        table: "materials",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    alert_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "StockAlertType",
      enumValues: ["low_stock", "out_of_stock"] as const,
      label: {
        ja: "アラートタイプ",
        en: "Alert Type",
        vi: "Loại cảnh báo",
      },
      placeholder: {
        ja: "アラートタイプを入力",
        en: "Enter Alert Type",
        vi: "Nhập Loại cảnh báo",
      },
      description: {
        ja: "low_stock (0 < 数量 <= 最小在庫) または out_of_stock (数量 <= 0)",
        en: "low_stock (0 < qty <= min_stock) or out_of_stock (qty <= 0)",
        vi: "low_stock (0 < tồn ≤ min_stock) hoặc out_of_stock (tồn ≤ 0)",
      },
    },
    current_quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "現在数量",
        en: "Current Quantity",
        vi: "Tồn kho hiện tại",
      },
      placeholder: {
        ja: "現在数量を入力",
        en: "Enter Current Quantity",
        vi: "Nhập Tồn kho hiện tại",
      },
      description: {
        ja: "アラート作成/更新時の在庫数量スナップショット",
        en: "Snapshot of stock quantity at alert creation/update time",
        vi: "Snapshot tồn kho tại thời điểm tạo/cập nhật alert",
      },
    },
    min_stock: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最小在庫しきい値",
        en: "Min Stock Threshold",
        vi: "Ngưỡng tối thiểu",
      },
      placeholder: {
        ja: "最小在庫しきい値を入力",
        en: "Enter Min Stock Threshold",
        vi: "Nhập Ngưỡng tối thiểu",
      },
      description: {
        ja: "アラート作成時のstock_levelsの最小在庫しきい値スナップショット。閾値未設定のStockLevelで allow_negative_sales 経由で発火した場合は NULL。",
        en: "Snapshot of min_stock threshold from stock_levels at alert creation. NULL when the alert was fired by allow-negative shortage on a StockLevel with no threshold configured.",
        vi: "Snapshot ngưỡng min_stock từ stock_levels tại thời điểm tạo alert. NULL nếu alert do allow_negative_sales kích hoạt trên StockLevel chưa cấu hình ngưỡng.",
      },
    },
    unit: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "単位",
        en: "Unit",
        vi: "Đơn vị",
      },
      placeholder: {
        ja: "単位を入力",
        en: "Enter Unit",
        vi: "Nhập Đơn vị",
      },
    },
    status: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "StockAlertStatus",
      enumValues: ["active", "resolved"] as const,
      label: {
        ja: "ステータス",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "ステータスを入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    resolved_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "解決日時",
        en: "Resolved At",
        vi: "Thời điểm giải quyết",
      },
      placeholder: {
        ja: "解決日時を入力",
        en: "Enter Resolved At",
        vi: "Nhập Thời điểm giải quyết",
      },
    },
    created_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "作成日時",
        en: "Created At",
        vi: "Ngày tạo",
      },
      placeholder: {
        ja: "作成日時を入力",
        en: "Enter Created At",
        vi: "Nhập Ngày tạo",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["warehouse_id", "alert_type", "current_quantity", "status", "created_at", "organization_id"] as const,
  enumFields: ["alert_type", "status"] as const,
  relationFields: ["warehouse_id", "product_sku_id", "material_id", "organization_id"] as const,
} as const;

export type StockAlertFieldName = keyof typeof stockAlertMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a StockAlert. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface StockAlertCreateFormState {
  warehouse_id: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  alert_type: StockAlertType;
  current_quantity: number;
  min_stock?: number | null;
  unit?: string | null;
  status: StockAlertStatus;
  resolved_at?: string | null;
  created_at: string;
  organization_id: string;
}

/** Form-state shape for updating a StockAlert. Every field optional. */
export interface StockAlertUpdateFormState {
  warehouse_id?: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  alert_type?: StockAlertType;
  current_quantity?: number;
  min_stock?: number | null;
  unit?: string | null;
  status?: StockAlertStatus;
  resolved_at?: string | null;
  created_at?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyStockAlertCreateForm(): StockAlertCreateFormState {
  return {
    warehouse_id: '',
    product_sku_id: '',
    material_id: '',
    alert_type: StockAlertType.LowStock,
    current_quantity: 0,
    min_stock: 0,
    unit: '',
    status: StockAlertStatus.Active,
    resolved_at: '',
    created_at: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseStockAlertCreateSchema accepts. */
export function buildStockAlertCreatePayload(form: StockAlertCreateFormState): BaseStockAlertCreate {
  const payload: Record<string, unknown> = {
    warehouse_id: form.warehouse_id.trim(),
    product_sku_id: form.product_sku_id?.toString().trim() || null,
    material_id: form.material_id?.toString().trim() || null,
    alert_type: form.alert_type,
    current_quantity: form.current_quantity,
    min_stock: form.min_stock,
    unit: form.unit?.toString().trim() || null,
    status: form.status,
    resolved_at: form.resolved_at,
    created_at: form.created_at,
    organization_id: form.organization_id.trim(),
  };

  return payload as BaseStockAlertCreate;
}

/** Convert update form state into the payload that baseStockAlertUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildStockAlertUpdatePayload(form: StockAlertUpdateFormState): BaseStockAlertUpdate {
  const payload: Record<string, unknown> = {};
  if (form.warehouse_id !== undefined) {
    payload.warehouse_id = form.warehouse_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.material_id !== undefined) {
    payload.material_id = form.material_id?.toString().trim() ?? null;
  }
  if (form.alert_type !== undefined) {
    payload.alert_type = form.alert_type;
  }
  if (form.current_quantity !== undefined) {
    payload.current_quantity = form.current_quantity;
  }
  if (form.min_stock !== undefined) {
    payload.min_stock = form.min_stock;
  }
  if (form.unit !== undefined) {
    payload.unit = form.unit?.toString().trim() ?? null;
  }
  if (form.status !== undefined) {
    payload.status = form.status;
  }
  if (form.resolved_at !== undefined) {
    payload.resolved_at = form.resolved_at;
  }
  if (form.created_at !== undefined) {
    payload.created_at = form.created_at;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseStockAlertUpdate;
}
