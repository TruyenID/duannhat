/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * InvoiceCounter
 */
export interface InvoiceCounter {
  branch_id: string;
  year_month: string;
  last_seq: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for InvoiceCounter
 */
export const invoiceCounterI18n = {
  label: {"ja":"InvoiceCounter","en":"InvoiceCounter","vi":"InvoiceCounter"},
  fields: {
    branch_id: {
      label: {"ja":"支店ID","en":"Branch ID","vi":"ID chi nhánh"},
      placeholder: {"ja":"支店IDを入力","en":"Enter Branch ID","vi":"Nhập ID chi nhánh"},
    },
    year_month: {
      label: {"ja":"年月","en":"Year-Month","vi":"Tháng"},
      placeholder: {"ja":"年月を入力","en":"Enter Year-Month","vi":"Nhập Tháng"},
    },
    last_seq: {
      label: {"ja":"最終連番","en":"Last Sequence","vi":"Số cuối"},
      placeholder: {"ja":"最終連番を入力","en":"Enter Last Sequence","vi":"Nhập Số cuối"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for InvoiceCounter */
export const baseInvoiceCounterSchemas = {
  branch_id: z.string(),
  year_month: z.string().min(1).max(6),
  last_seq: z.number().int(),
} as const;

/** Create schema for InvoiceCounter */
export const baseInvoiceCounterCreateSchema = z.object({
  branch_id: baseInvoiceCounterSchemas.branch_id,
  year_month: baseInvoiceCounterSchemas.year_month,
  last_seq: baseInvoiceCounterSchemas.last_seq,
});

/** Update schema for InvoiceCounter */
export const baseInvoiceCounterUpdateSchema = baseInvoiceCounterCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseInvoiceCounterCreate = z.infer<typeof baseInvoiceCounterCreateSchema>;
export type BaseInvoiceCounterUpdate = z.infer<typeof baseInvoiceCounterUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getInvoiceCounterLabel(locale: string): string {
  return invoiceCounterI18n.label[locale as keyof typeof invoiceCounterI18n.label] ?? invoiceCounterI18n.label['en'] ?? 'InvoiceCounter';
}

/** Get field label for a specific locale */
export function getInvoiceCounterFieldLabel(field: string, locale: string): string {
  const fields = invoiceCounterI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getInvoiceCounterFieldPlaceholder(field: string, locale: string): string {
  const fields = invoiceCounterI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for InvoiceCounter.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const invoiceCounterMetadata = {
  modelName: "InvoiceCounter",
  table: "invoice_counters",

  fields: {
    branch_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "支店ID",
        en: "Branch ID",
        vi: "ID chi nhánh",
      },
      placeholder: {
        ja: "支店IDを入力",
        en: "Enter Branch ID",
        vi: "Nhập ID chi nhánh",
      },
    },
    year_month: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 6,
      },
      label: {
        ja: "年月",
        en: "Year-Month",
        vi: "Tháng",
      },
      placeholder: {
        ja: "年月を入力",
        en: "Enter Year-Month",
        vi: "Nhập Tháng",
      },
    },
    last_seq: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "最終連番",
        en: "Last Sequence",
        vi: "Số cuối",
      },
      placeholder: {
        ja: "最終連番を入力",
        en: "Enter Last Sequence",
        vi: "Nhập Số cuối",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["branch_id", "year_month", "last_seq"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type InvoiceCounterFieldName = keyof typeof invoiceCounterMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a InvoiceCounter. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface InvoiceCounterCreateFormState {
  branch_id: string;
  year_month: string;
  last_seq: number;
}

/** Form-state shape for updating a InvoiceCounter. Every field optional. */
export interface InvoiceCounterUpdateFormState {
  branch_id?: string;
  year_month?: string;
  last_seq?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyInvoiceCounterCreateForm(): InvoiceCounterCreateFormState {
  return {
    branch_id: '',
    year_month: '',
    last_seq: 0,
  };
}

/** Convert create form state into the payload that baseInvoiceCounterCreateSchema accepts. */
export function buildInvoiceCounterCreatePayload(form: InvoiceCounterCreateFormState): BaseInvoiceCounterCreate {
  const payload: Record<string, unknown> = {
    branch_id: form.branch_id.trim(),
    year_month: form.year_month.trim(),
    last_seq: form.last_seq,
  };

  return payload as BaseInvoiceCounterCreate;
}

/** Convert update form state into the payload that baseInvoiceCounterUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildInvoiceCounterUpdatePayload(form: InvoiceCounterUpdateFormState): BaseInvoiceCounterUpdate {
  const payload: Record<string, unknown> = {};
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.year_month !== undefined) {
    payload.year_month = form.year_month?.toString().trim() ?? null;
  }
  if (form.last_seq !== undefined) {
    payload.last_seq = form.last_seq;
  }
  return payload as BaseInvoiceCounterUpdate;
}
