/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Menu } from './Menu';
import type { ProductSku } from './ProductSku';

/**
 * メニュー項目
 */
export interface MenuItem {
  id: string;
  menu_id: string;
  menu?: Menu;
  productSku_id: string;
  productSku?: ProductSku;
  selling_price: number;
  availability: 'Available' | 'Unavailable' | 'OutOfStock';
  display_order: number;
  is_price_overridden: boolean;
  master_price?: number;
  masterItem_id?: string;
  masterItem?: MenuItem | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuItem
 */
export const menuItemI18n = {
  label: {"ja":"メニュー項目","en":"Menu Item","vi":"Món trong Menu"},
  fields: {
    menu: {
      label: {"ja":"メニュー","en":"Menu","vi":"Menu"},
    },
    product_sku: {
      label: {"ja":"商品バリエーション","en":"Product Variant","vi":"Biến thể sản phẩm"},
    },
    selling_price: {
      label: {"ja":"販売価格","en":"Selling Price","vi":"Giá bán"},
    },
    availability: {
      label: {"ja":"在庫状況","en":"Availability","vi":"Tình trạng"},
    },
    display_order: {
      label: {"ja":"表示順序","en":"Display Order","vi":"Thứ tự hiển thị"},
    },
    is_price_overridden: {
      label: {"ja":"価格上書き済み","en":"Price Overridden","vi":"Giá đã ghi đè"},
    },
    master_price: {
      label: {"ja":"マスター価格","en":"Master Price","vi":"Giá gốc"},
    },
    master_item: {
      label: {"ja":"マスター項目","en":"Master Item","vi":"Món gốc"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuItem */
export const baseMenuItemSchemas = {
  selling_price: z.number(),
  availability: z.enum(['Available', 'Unavailable', 'OutOfStock']),
  display_order: z.number().int(),
  is_price_overridden: z.boolean(),
  master_price: z.number().optional().nullable(),
} as const;

/** Create schema for MenuItem */
export const baseMenuItemCreateSchema = z.object({
  selling_price: baseMenuItemSchemas.selling_price,
  availability: baseMenuItemSchemas.availability,
  display_order: baseMenuItemSchemas.display_order,
  is_price_overridden: baseMenuItemSchemas.is_price_overridden,
  master_price: baseMenuItemSchemas.master_price,
});

/** Update schema for MenuItem */
export const baseMenuItemUpdateSchema = baseMenuItemCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuItemCreate = z.infer<typeof baseMenuItemCreateSchema>;
export type BaseMenuItemUpdate = z.infer<typeof baseMenuItemUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuItemLabel(locale: string): string {
  return menuItemI18n.label[locale as keyof typeof menuItemI18n.label] ?? menuItemI18n.label['en'] ?? 'MenuItem';
}

/** Get field label for a specific locale */
export function getMenuItemFieldLabel(field: string, locale: string): string {
  const fieldI18n = menuItemI18n.fields[field as keyof typeof menuItemI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuItemFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = menuItemI18n.fields[field as keyof typeof menuItemI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuItem.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuItemMetadata = {
  modelName: "MenuItem",
  table: "menu_items",

  fields: {
    menu_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Menu",
        table: "menus",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "CASCADE",
      },
      label: {},
    },
    selling_price: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "販売価格",
        en: "Selling Price",
        vi: "Giá bán",
      },
    },
    availability: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enumValues: ["Available", "Unavailable", "OutOfStock"] as const,
      label: {
        ja: "在庫状況",
        en: "Availability",
        vi: "Tình trạng",
      },
    },
    display_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順序",
        en: "Display Order",
        vi: "Thứ tự hiển thị",
      },
    },
    is_price_overridden: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "価格上書き済み",
        en: "Price Overridden",
        vi: "Giá đã ghi đè",
      },
    },
    master_price: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "マスター価格",
        en: "Master Price",
        vi: "Giá gốc",
      },
    },
    master_item_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "MenuItem",
        table: "menu_items",
        onDelete: undefined,
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["menu_id", "product_sku_id", "selling_price", "availability", "display_order", "is_price_overridden"] as const,
  enumFields: ["availability"] as const,
  relationFields: ["menu_id", "product_sku_id", "master_item_id"] as const,
} as const;

export type MenuItemFieldName = keyof typeof menuItemMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuItem. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuItemCreateFormState {
  menu_id: string;
  product_sku_id: string;
  selling_price: number;
  availability: 'Available' | 'Unavailable' | 'OutOfStock';
  display_order: number;
  is_price_overridden: boolean;
  master_price?: number | null;
  master_item_id?: string | null;
}

/** Form-state shape for updating a MenuItem. Every field optional. */
export interface MenuItemUpdateFormState {
  menu_id?: string;
  product_sku_id?: string;
  selling_price?: number;
  availability?: 'Available' | 'Unavailable' | 'OutOfStock';
  display_order?: number;
  is_price_overridden?: boolean;
  master_price?: number | null;
  master_item_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuItemCreateForm(): MenuItemCreateFormState {
  return {
    menu_id: '',
    product_sku_id: '',
    selling_price: 0,
    availability: "Available",
    display_order: 0,
    is_price_overridden: false,
    master_price: 0,
    master_item_id: '',
  };
}

/** Convert create form state into the payload that baseMenuItemCreateSchema accepts. */
export function buildMenuItemCreatePayload(form: MenuItemCreateFormState): BaseMenuItemCreate {
  const payload: Record<string, unknown> = {
    menu_id: form.menu_id.trim(),
    product_sku_id: form.product_sku_id.trim(),
    selling_price: form.selling_price,
    availability: form.availability,
    display_order: form.display_order,
    is_price_overridden: form.is_price_overridden,
    master_price: form.master_price,
    master_item_id: form.master_item_id?.toString().trim() || null,
  };

  return payload as BaseMenuItemCreate;
}

/** Convert update form state into the payload that baseMenuItemUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuItemUpdatePayload(form: MenuItemUpdateFormState): BaseMenuItemUpdate {
  const payload: Record<string, unknown> = {};
  if (form.menu_id !== undefined) {
    payload.menu_id = form.menu_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.selling_price !== undefined) {
    payload.selling_price = form.selling_price;
  }
  if (form.availability !== undefined) {
    payload.availability = form.availability;
  }
  if (form.display_order !== undefined) {
    payload.display_order = form.display_order;
  }
  if (form.is_price_overridden !== undefined) {
    payload.is_price_overridden = form.is_price_overridden;
  }
  if (form.master_price !== undefined) {
    payload.master_price = form.master_price;
  }
  if (form.master_item_id !== undefined) {
    payload.master_item_id = form.master_item_id?.toString().trim() ?? null;
  }
  return payload as BaseMenuItemUpdate;
}
