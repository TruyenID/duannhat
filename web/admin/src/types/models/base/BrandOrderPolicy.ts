/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Brand } from './Brand';
import type { Organization } from './Organization';

/**
 * ブランド注文ポリシー
 */
export interface BrandOrderPolicy {
  id: string;
  default_print_label_locale?: string | null;
  default_prep_before_payment: boolean;
  default_prep_minutes_per_item?: number | null;
  allow_shop_edit_hq_tables: boolean;
  cash_variance_tolerance_minor: number;
  brand_id: string;
  brand?: Brand;
  organization_id: string;
  organization?: Organization;
  default_confirmation_timeout_minutes: number;
  default_table_status_after_payment: string;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for BrandOrderPolicy
 */
export const brandOrderPolicyI18n = {
  label: {"ja":"ブランド注文ポリシー","en":"Brand Order Policy","vi":"Chính sách đơn hàng theo Brand"},
  fields: {
    default_print_label_locale: {
      label: {"ja":"デフォルト印刷言語","en":"Default print label language","vi":"Ngôn ngữ in mặc định"},
      placeholder: {"ja":"デフォルト印刷言語を入力","en":"Enter Default print label language","vi":"Nhập Ngôn ngữ in mặc định"},
    },
    default_prep_before_payment: {
      label: {"ja":"デフォルト — 調理前の支払い必須","en":"Default — Require payment before prep","vi":"Mặc định — Phải thanh toán trước khi chuẩn bị"},
      placeholder: {"ja":"デフォルト — 調理前の支払い必須を入力","en":"Enter Default — Require payment before prep","vi":"Nhập Mặc định — Phải thanh toán trước khi chuẩn bị"},
    },
    default_prep_minutes_per_item: {
      label: {"ja":"デフォルト — 1品あたりの調理時間 (分)","en":"Default — prep minutes per item","vi":"Mặc định — Thời gian chuẩn bị mỗi món (phút)"},
      placeholder: {"ja":"デフォルト — 1品あたりの調理時間 (分)を入力","en":"Enter Default — prep minutes per item","vi":"Nhập Mặc định — Thời gian chuẩn bị mỗi món (phút)"},
    },
    allow_shop_edit_hq_tables: {
      label: {"ja":"店舗がHQ標準テーブルを編集可能","en":"Shops may edit HQ default tables","vi":"Shop được sửa bàn mặc định từ HQ"},
      placeholder: {"ja":"店舗がHQ標準テーブルを編集可能を入力","en":"Enter Shops may edit HQ default tables","vi":"Nhập Shop được sửa bàn mặc định từ HQ"},
    },
    cash_variance_tolerance_minor: {
      label: {"ja":"現金差異の許容額","en":"Cash variance tolerance (minor)","vi":"Ngưỡng lệch tiền mặt"},
      placeholder: {"ja":"現金差異の許容額を入力","en":"Enter Cash variance tolerance (minor)","vi":"Nhập Ngưỡng lệch tiền mặt"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Brand"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Brand"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    default_confirmation_timeout_minutes: {
      label: {"ja":"確認期限（分）既定","en":"Default Confirmation Timeout (minutes)","vi":"Hạn xác nhận mặc định (phút)"},
      placeholder: {"ja":"確認期限（分）既定を入力","en":"Enter Default Confirmation Timeout (minutes)","vi":"Nhập Hạn xác nhận mặc định (phút)"},
    },
    default_table_status_after_payment: {
      label: {"ja":"支払後テーブル状態（既定）","en":"Default Table Status After Payment","vi":"Trạng thái bàn sau thanh toán (mặc định)"},
      placeholder: {"ja":"支払後テーブル状態（既定）を入力","en":"Enter Default Table Status After Payment","vi":"Nhập Trạng thái bàn sau thanh toán (mặc định)"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for BrandOrderPolicy */
export const baseBrandOrderPolicySchemas = {
  default_print_label_locale: z.string().max(8).optional().nullable(),
  default_prep_before_payment: z.boolean(),
  default_prep_minutes_per_item: z.number().int().optional().nullable(),
  allow_shop_edit_hq_tables: z.boolean(),
  cash_variance_tolerance_minor: z.number().int(),
  default_confirmation_timeout_minutes: z.number().int(),
  default_table_status_after_payment: z.string().min(1).max(16),
} as const;

/** Create schema for BrandOrderPolicy */
export const baseBrandOrderPolicyCreateSchema = z.object({
  default_print_label_locale: baseBrandOrderPolicySchemas.default_print_label_locale,
  default_prep_before_payment: baseBrandOrderPolicySchemas.default_prep_before_payment,
  default_prep_minutes_per_item: baseBrandOrderPolicySchemas.default_prep_minutes_per_item,
  allow_shop_edit_hq_tables: baseBrandOrderPolicySchemas.allow_shop_edit_hq_tables,
  cash_variance_tolerance_minor: baseBrandOrderPolicySchemas.cash_variance_tolerance_minor,
  default_confirmation_timeout_minutes: baseBrandOrderPolicySchemas.default_confirmation_timeout_minutes,
  default_table_status_after_payment: baseBrandOrderPolicySchemas.default_table_status_after_payment,
});

/** Update schema for BrandOrderPolicy */
export const baseBrandOrderPolicyUpdateSchema = baseBrandOrderPolicyCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseBrandOrderPolicyCreate = z.infer<typeof baseBrandOrderPolicyCreateSchema>;
export type BaseBrandOrderPolicyUpdate = z.infer<typeof baseBrandOrderPolicyUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getBrandOrderPolicyLabel(locale: string): string {
  return brandOrderPolicyI18n.label[locale as keyof typeof brandOrderPolicyI18n.label] ?? brandOrderPolicyI18n.label['en'] ?? 'BrandOrderPolicy';
}

/** Get field label for a specific locale */
export function getBrandOrderPolicyFieldLabel(field: string, locale: string): string {
  const fields = brandOrderPolicyI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getBrandOrderPolicyFieldPlaceholder(field: string, locale: string): string {
  const fields = brandOrderPolicyI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for BrandOrderPolicy.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const brandOrderPolicyMetadata = {
  modelName: "BrandOrderPolicy",
  table: "brand_order_policies",

  fields: {
    default_print_label_locale: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 8,
      },
      label: {
        ja: "デフォルト印刷言語",
        en: "Default print label language",
        vi: "Ngôn ngữ in mặc định",
      },
      placeholder: {
        ja: "デフォルト印刷言語を入力",
        en: "Enter Default print label language",
        vi: "Nhập Ngôn ngữ in mặc định",
      },
    },
    default_prep_before_payment: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "デフォルト — 調理前の支払い必須",
        en: "Default — Require payment before prep",
        vi: "Mặc định — Phải thanh toán trước khi chuẩn bị",
      },
      placeholder: {
        ja: "デフォルト — 調理前の支払い必須を入力",
        en: "Enter Default — Require payment before prep",
        vi: "Nhập Mặc định — Phải thanh toán trước khi chuẩn bị",
      },
    },
    default_prep_minutes_per_item: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "デフォルト — 1品あたりの調理時間 (分)",
        en: "Default — prep minutes per item",
        vi: "Mặc định — Thời gian chuẩn bị mỗi món (phút)",
      },
      placeholder: {
        ja: "デフォルト — 1品あたりの調理時間 (分)を入力",
        en: "Enter Default — prep minutes per item",
        vi: "Nhập Mặc định — Thời gian chuẩn bị mỗi món (phút)",
      },
    },
    allow_shop_edit_hq_tables: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "店舗がHQ標準テーブルを編集可能",
        en: "Shops may edit HQ default tables",
        vi: "Shop được sửa bàn mặc định từ HQ",
      },
      placeholder: {
        ja: "店舗がHQ標準テーブルを編集可能を入力",
        en: "Enter Shops may edit HQ default tables",
        vi: "Nhập Shop được sửa bàn mặc định từ HQ",
      },
    },
    cash_variance_tolerance_minor: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "現金差異の許容額",
        en: "Cash variance tolerance (minor)",
        vi: "Ngưỡng lệch tiền mặt",
      },
      placeholder: {
        ja: "現金差異の許容額を入力",
        en: "Enter Cash variance tolerance (minor)",
        vi: "Nhập Ngưỡng lệch tiền mặt",
      },
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    default_confirmation_timeout_minutes: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "確認期限（分）既定",
        en: "Default Confirmation Timeout (minutes)",
        vi: "Hạn xác nhận mặc định (phút)",
      },
      placeholder: {
        ja: "確認期限（分）既定を入力",
        en: "Enter Default Confirmation Timeout (minutes)",
        vi: "Nhập Hạn xác nhận mặc định (phút)",
      },
    },
    default_table_status_after_payment: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 16,
      },
      label: {
        ja: "支払後テーブル状態（既定）",
        en: "Default Table Status After Payment",
        vi: "Trạng thái bàn sau thanh toán (mặc định)",
      },
      placeholder: {
        ja: "支払後テーブル状態（既定）を入力",
        en: "Enter Default Table Status After Payment",
        vi: "Nhập Trạng thái bàn sau thanh toán (mặc định)",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["default_prep_before_payment", "allow_shop_edit_hq_tables", "cash_variance_tolerance_minor", "brand_id", "organization_id", "default_confirmation_timeout_minutes", "default_table_status_after_payment"] as const,
  enumFields: [] as const,
  relationFields: ["brand_id", "organization_id"] as const,
} as const;

export type BrandOrderPolicyFieldName = keyof typeof brandOrderPolicyMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a BrandOrderPolicy. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface BrandOrderPolicyCreateFormState {
  default_print_label_locale?: string | null;
  default_prep_before_payment: boolean;
  default_prep_minutes_per_item?: number | null;
  allow_shop_edit_hq_tables: boolean;
  cash_variance_tolerance_minor: number;
  brand_id: string;
  organization_id: string;
  default_confirmation_timeout_minutes: number;
  default_table_status_after_payment: string;
}

/** Form-state shape for updating a BrandOrderPolicy. Every field optional. */
export interface BrandOrderPolicyUpdateFormState {
  default_print_label_locale?: string | null;
  default_prep_before_payment?: boolean;
  default_prep_minutes_per_item?: number | null;
  allow_shop_edit_hq_tables?: boolean;
  cash_variance_tolerance_minor?: number;
  brand_id?: string;
  organization_id?: string;
  default_confirmation_timeout_minutes?: number;
  default_table_status_after_payment?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyBrandOrderPolicyCreateForm(): BrandOrderPolicyCreateFormState {
  return {
    default_print_label_locale: '',
    default_prep_before_payment: true,
    default_prep_minutes_per_item: 0,
    allow_shop_edit_hq_tables: false,
    cash_variance_tolerance_minor: 100,
    brand_id: '',
    organization_id: '',
    default_confirmation_timeout_minutes: 3,
    default_table_status_after_payment: "free",
  };
}

/** Convert create form state into the payload that baseBrandOrderPolicyCreateSchema accepts. */
export function buildBrandOrderPolicyCreatePayload(form: BrandOrderPolicyCreateFormState): BaseBrandOrderPolicyCreate {
  const payload: Record<string, unknown> = {
    default_print_label_locale: form.default_print_label_locale?.toString().trim() || null,
    default_prep_before_payment: form.default_prep_before_payment,
    default_prep_minutes_per_item: form.default_prep_minutes_per_item,
    allow_shop_edit_hq_tables: form.allow_shop_edit_hq_tables,
    cash_variance_tolerance_minor: form.cash_variance_tolerance_minor,
    brand_id: form.brand_id.trim(),
    organization_id: form.organization_id.trim(),
    default_confirmation_timeout_minutes: form.default_confirmation_timeout_minutes,
    default_table_status_after_payment: form.default_table_status_after_payment.trim(),
  };

  return payload as BaseBrandOrderPolicyCreate;
}

/** Convert update form state into the payload that baseBrandOrderPolicyUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildBrandOrderPolicyUpdatePayload(form: BrandOrderPolicyUpdateFormState): BaseBrandOrderPolicyUpdate {
  const payload: Record<string, unknown> = {};
  if (form.default_print_label_locale !== undefined) {
    payload.default_print_label_locale = form.default_print_label_locale?.toString().trim() ?? null;
  }
  if (form.default_prep_before_payment !== undefined) {
    payload.default_prep_before_payment = form.default_prep_before_payment;
  }
  if (form.default_prep_minutes_per_item !== undefined) {
    payload.default_prep_minutes_per_item = form.default_prep_minutes_per_item;
  }
  if (form.allow_shop_edit_hq_tables !== undefined) {
    payload.allow_shop_edit_hq_tables = form.allow_shop_edit_hq_tables;
  }
  if (form.cash_variance_tolerance_minor !== undefined) {
    payload.cash_variance_tolerance_minor = form.cash_variance_tolerance_minor;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.default_confirmation_timeout_minutes !== undefined) {
    payload.default_confirmation_timeout_minutes = form.default_confirmation_timeout_minutes;
  }
  if (form.default_table_status_after_payment !== undefined) {
    payload.default_table_status_after_payment = form.default_table_status_after_payment?.toString().trim() ?? null;
  }
  return payload as BaseBrandOrderPolicyUpdate;
}
