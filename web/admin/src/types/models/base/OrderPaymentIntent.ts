/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { CustomerOrder } from './CustomerOrder';
import type { Organization } from './Organization';

/**
 * 注文決済インテント
 */
export interface OrderPaymentIntent {
  id: string;
  organization_id: string;
  organization?: Organization;
  customer_order_id: string;
  customer_order?: CustomerOrder;
  provider: string;
  intent_id: string;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for OrderPaymentIntent
 */
export const orderPaymentIntentI18n = {
  label: {"ja":"注文決済インテント","en":"Order Payment Intent","vi":"Intent thanh toán của đơn"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    customer_order: {
      label: {"ja":"注文","en":"Order","vi":"Đơn hàng"},
      placeholder: {"ja":"注文を入力","en":"Enter Order","vi":"Nhập Đơn hàng"},
    },
    provider: {
      label: {"ja":"決済プロバイダ","en":"Provider","vi":"Cổng thanh toán"},
      placeholder: {"ja":"決済プロバイダを入力","en":"Enter Provider","vi":"Nhập Cổng thanh toán"},
    },
    intent_id: {
      label: {"ja":"インテント ID","en":"Intent ID","vi":"Mã intent"},
      placeholder: {"ja":"インテント IDを入力","en":"Enter Intent ID","vi":"Nhập Mã intent"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for OrderPaymentIntent */
export const baseOrderPaymentIntentSchemas = {
  provider: z.string().min(1).max(32),
  intent_id: z.string().min(1).max(255),
} as const;

/** Create schema for OrderPaymentIntent */
export const baseOrderPaymentIntentCreateSchema = z.object({
  provider: baseOrderPaymentIntentSchemas.provider,
  intent_id: baseOrderPaymentIntentSchemas.intent_id,
});

/** Update schema for OrderPaymentIntent */
export const baseOrderPaymentIntentUpdateSchema = baseOrderPaymentIntentCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseOrderPaymentIntentCreate = z.infer<typeof baseOrderPaymentIntentCreateSchema>;
export type BaseOrderPaymentIntentUpdate = z.infer<typeof baseOrderPaymentIntentUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getOrderPaymentIntentLabel(locale: string): string {
  return orderPaymentIntentI18n.label[locale as keyof typeof orderPaymentIntentI18n.label] ?? orderPaymentIntentI18n.label['en'] ?? 'OrderPaymentIntent';
}

/** Get field label for a specific locale */
export function getOrderPaymentIntentFieldLabel(field: string, locale: string): string {
  const fields = orderPaymentIntentI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getOrderPaymentIntentFieldPlaceholder(field: string, locale: string): string {
  const fields = orderPaymentIntentI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for OrderPaymentIntent.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const orderPaymentIntentMetadata = {
  modelName: "OrderPaymentIntent",
  table: "order_payment_intents",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    customer_order_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrder",
        table: "customer_orders",
        onDelete: "CASCADE",
      },
      label: {},
    },
    provider: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "決済プロバイダ",
        en: "Provider",
        vi: "Cổng thanh toán",
      },
      placeholder: {
        ja: "決済プロバイダを入力",
        en: "Enter Provider",
        vi: "Nhập Cổng thanh toán",
      },
    },
    intent_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "インテント ID",
        en: "Intent ID",
        vi: "Mã intent",
      },
      placeholder: {
        ja: "インテント IDを入力",
        en: "Enter Intent ID",
        vi: "Nhập Mã intent",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "customer_order_id", "provider", "intent_id"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "customer_order_id"] as const,
} as const;

export type OrderPaymentIntentFieldName = keyof typeof orderPaymentIntentMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a OrderPaymentIntent. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface OrderPaymentIntentCreateFormState {
  organization_id: string;
  customer_order_id: string;
  provider: string;
  intent_id: string;
}

/** Form-state shape for updating a OrderPaymentIntent. Every field optional. */
export interface OrderPaymentIntentUpdateFormState {
  organization_id?: string;
  customer_order_id?: string;
  provider?: string;
  intent_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyOrderPaymentIntentCreateForm(): OrderPaymentIntentCreateFormState {
  return {
    organization_id: '',
    customer_order_id: '',
    provider: '',
    intent_id: '',
  };
}

/** Convert create form state into the payload that baseOrderPaymentIntentCreateSchema accepts. */
export function buildOrderPaymentIntentCreatePayload(form: OrderPaymentIntentCreateFormState): BaseOrderPaymentIntentCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    customer_order_id: form.customer_order_id.trim(),
    provider: form.provider.trim(),
    intent_id: form.intent_id.trim(),
  };

  return payload as BaseOrderPaymentIntentCreate;
}

/** Convert update form state into the payload that baseOrderPaymentIntentUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildOrderPaymentIntentUpdatePayload(form: OrderPaymentIntentUpdateFormState): BaseOrderPaymentIntentUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.customer_order_id !== undefined) {
    payload.customer_order_id = form.customer_order_id?.toString().trim() ?? null;
  }
  if (form.provider !== undefined) {
    payload.provider = form.provider?.toString().trim() ?? null;
  }
  if (form.intent_id !== undefined) {
    payload.intent_id = form.intent_id?.toString().trim() ?? null;
  }
  return payload as BaseOrderPaymentIntentUpdate;
}
