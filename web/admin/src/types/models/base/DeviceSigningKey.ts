/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Device } from './Device';
import type { Organization } from './Organization';

/**
 * デバイス署名鍵
 */
export interface DeviceSigningKey {
  id: string;
  public_key: string;
  issued_at: DateTimeString;
  expires_at: DateTimeString;
  revoked_at?: DateTimeString | null;
  revoked_reason?: string | null;
  device_id: string;
  device?: Device;
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for DeviceSigningKey
 */
export const deviceSigningKeyI18n = {
  label: {"ja":"デバイス署名鍵","en":"Device Signing Key","vi":"Khóa ký thiết bị"},
  fields: {
    public_key: {
      label: {"ja":"公開鍵","en":"Public Key","vi":"Khóa công khai"},
      placeholder: {"ja":"公開鍵を入力","en":"Enter Public Key","vi":"Nhập Khóa công khai"},
    },
    issued_at: {
      label: {"ja":"発行日時","en":"Issued At","vi":"Thời điểm cấp"},
      placeholder: {"ja":"発行日時を入力","en":"Enter Issued At","vi":"Nhập Thời điểm cấp"},
    },
    expires_at: {
      label: {"ja":"有効期限","en":"Expires At","vi":"Hết hạn"},
      placeholder: {"ja":"有効期限を入力","en":"Enter Expires At","vi":"Nhập Hết hạn"},
    },
    revoked_at: {
      label: {"ja":"失効日時","en":"Revoked At","vi":"Thời điểm thu hồi"},
      placeholder: {"ja":"失効日時を入力","en":"Enter Revoked At","vi":"Nhập Thời điểm thu hồi"},
    },
    revoked_reason: {
      label: {"ja":"失効理由","en":"Revoked Reason","vi":"Lý do thu hồi"},
      placeholder: {"ja":"失効理由を入力","en":"Enter Revoked Reason","vi":"Nhập Lý do thu hồi"},
    },
    device: {
      label: {"ja":"デバイス","en":"Device","vi":"Thiết bị"},
      placeholder: {"ja":"デバイスを入力","en":"Enter Device","vi":"Nhập Thiết bị"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for DeviceSigningKey */
export const baseDeviceSigningKeySchemas = {
  public_key: z.string().min(1).max(64),
  issued_at: z.string().datetime({ offset: true }),
  expires_at: z.string().datetime({ offset: true }),
  revoked_at: z.string().datetime({ offset: true }).optional().nullable(),
  revoked_reason: z.string().max(255).optional().nullable(),
} as const;

/** Create schema for DeviceSigningKey */
export const baseDeviceSigningKeyCreateSchema = z.object({
  public_key: baseDeviceSigningKeySchemas.public_key,
  issued_at: baseDeviceSigningKeySchemas.issued_at,
  expires_at: baseDeviceSigningKeySchemas.expires_at,
  revoked_at: baseDeviceSigningKeySchemas.revoked_at,
  revoked_reason: baseDeviceSigningKeySchemas.revoked_reason,
});

/** Update schema for DeviceSigningKey */
export const baseDeviceSigningKeyUpdateSchema = baseDeviceSigningKeyCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseDeviceSigningKeyCreate = z.infer<typeof baseDeviceSigningKeyCreateSchema>;
export type BaseDeviceSigningKeyUpdate = z.infer<typeof baseDeviceSigningKeyUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getDeviceSigningKeyLabel(locale: string): string {
  return deviceSigningKeyI18n.label[locale as keyof typeof deviceSigningKeyI18n.label] ?? deviceSigningKeyI18n.label['en'] ?? 'DeviceSigningKey';
}

/** Get field label for a specific locale */
export function getDeviceSigningKeyFieldLabel(field: string, locale: string): string {
  const fields = deviceSigningKeyI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getDeviceSigningKeyFieldPlaceholder(field: string, locale: string): string {
  const fields = deviceSigningKeyI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for DeviceSigningKey.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const deviceSigningKeyMetadata = {
  modelName: "DeviceSigningKey",
  table: "device_signing_keys",

  fields: {
    public_key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "公開鍵",
        en: "Public Key",
        vi: "Khóa công khai",
      },
      placeholder: {
        ja: "公開鍵を入力",
        en: "Enter Public Key",
        vi: "Nhập Khóa công khai",
      },
      description: {
        ja: "Ed25519公開鍵（base64、44文字）。秘密鍵はデバイス側のみ保持。",
        en: "Ed25519 public key, base64 (44 chars). The device keeps the private half.",
        vi: "Khóa công khai Ed25519, base64 (44 ký tự). Khóa bí mật chỉ nằm trên thiết bị.",
      },
    },
    issued_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "発行日時",
        en: "Issued At",
        vi: "Thời điểm cấp",
      },
      placeholder: {
        ja: "発行日時を入力",
        en: "Enter Issued At",
        vi: "Nhập Thời điểm cấp",
      },
    },
    expires_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効期限",
        en: "Expires At",
        vi: "Hết hạn",
      },
      placeholder: {
        ja: "有効期限を入力",
        en: "Enter Expires At",
        vi: "Nhập Hết hạn",
      },
      description: {
        ja: "有効期限。発行から180日が既定。延長ではなくローテーションで新鍵を発行する。",
        en: "Validity ceiling. Default 180 days from issue; rotation issues a fresh key rather than extending.",
        vi: "Trần hiệu lực. Mặc định 180 ngày từ lúc cấp; hết thì rotate khóa mới, không gia hạn.",
      },
    },
    revoked_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "失効日時",
        en: "Revoked At",
        vi: "Thời điểm thu hồi",
      },
      placeholder: {
        ja: "失効日時を入力",
        en: "Enter Revoked At",
        vi: "Nhập Thời điểm thu hồi",
      },
    },
    revoked_reason: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "失効理由",
        en: "Revoked Reason",
        vi: "Lý do thu hồi",
      },
      placeholder: {
        ja: "失効理由を入力",
        en: "Enter Revoked Reason",
        vi: "Nhập Lý do thu hồi",
      },
    },
    device_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Device",
        table: "devices",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["public_key", "issued_at", "expires_at", "device_id", "organization_id"] as const,
  enumFields: [] as const,
  relationFields: ["device_id", "organization_id"] as const,
} as const;

export type DeviceSigningKeyFieldName = keyof typeof deviceSigningKeyMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a DeviceSigningKey. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface DeviceSigningKeyCreateFormState {
  public_key: string;
  issued_at: string;
  expires_at: string;
  revoked_at?: string | null;
  revoked_reason?: string | null;
  device_id: string;
  organization_id: string;
}

/** Form-state shape for updating a DeviceSigningKey. Every field optional. */
export interface DeviceSigningKeyUpdateFormState {
  public_key?: string;
  issued_at?: string;
  expires_at?: string;
  revoked_at?: string | null;
  revoked_reason?: string | null;
  device_id?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyDeviceSigningKeyCreateForm(): DeviceSigningKeyCreateFormState {
  return {
    public_key: '',
    issued_at: '',
    expires_at: '',
    revoked_at: '',
    revoked_reason: '',
    device_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseDeviceSigningKeyCreateSchema accepts. */
export function buildDeviceSigningKeyCreatePayload(form: DeviceSigningKeyCreateFormState): BaseDeviceSigningKeyCreate {
  const payload: Record<string, unknown> = {
    public_key: form.public_key.trim(),
    issued_at: form.issued_at,
    expires_at: form.expires_at,
    revoked_at: form.revoked_at,
    revoked_reason: form.revoked_reason?.toString().trim() || null,
    device_id: form.device_id.trim(),
    organization_id: form.organization_id.trim(),
  };

  return payload as BaseDeviceSigningKeyCreate;
}

/** Convert update form state into the payload that baseDeviceSigningKeyUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildDeviceSigningKeyUpdatePayload(form: DeviceSigningKeyUpdateFormState): BaseDeviceSigningKeyUpdate {
  const payload: Record<string, unknown> = {};
  if (form.public_key !== undefined) {
    payload.public_key = form.public_key?.toString().trim() ?? null;
  }
  if (form.issued_at !== undefined) {
    payload.issued_at = form.issued_at;
  }
  if (form.expires_at !== undefined) {
    payload.expires_at = form.expires_at;
  }
  if (form.revoked_at !== undefined) {
    payload.revoked_at = form.revoked_at;
  }
  if (form.revoked_reason !== undefined) {
    payload.revoked_reason = form.revoked_reason?.toString().trim() ?? null;
  }
  if (form.device_id !== undefined) {
    payload.device_id = form.device_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseDeviceSigningKeyUpdate;
}
