/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { TillCashEventType } from '../enum/TillCashEventType';
import type { TillSession } from './TillSession';

/**
 * 現金イベント
 */
export interface TillCashEvent {
  id: string;
  event_type: TillCashEventType;
  amount: number;
  currency_code: string;
  reason?: string | null;
  reference_no?: string | null;
  occurred_at: DateTimeString;
  performed_by_id?: string | null;
  manual_adjustment: boolean;
  session_id: string;
  session?: TillSession;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TillCashEvent
 */
export const tillCashEventI18n = {
  label: {"ja":"現金イベント","en":"Till Cash Event","vi":"Giao dịch tiền mặt giữa ca"},
  fields: {
    event_type: {
      label: {"ja":"種別","en":"Event Type","vi":"Loại"},
      placeholder: {"ja":"種別を入力","en":"Enter Event Type","vi":"Nhập Loại"},
    },
    amount: {
      label: {"ja":"金額","en":"Amount","vi":"Số tiền"},
      placeholder: {"ja":"金額を入力","en":"Enter Amount","vi":"Nhập Số tiền"},
    },
    currency_code: {
      label: {"ja":"通貨","en":"Currency","vi":"Tiền tệ"},
      placeholder: {"ja":"通貨を入力","en":"Enter Currency","vi":"Nhập Tiền tệ"},
    },
    reason: {
      label: {"ja":"理由","en":"Reason","vi":"Lý do"},
      placeholder: {"ja":"理由を入力","en":"Enter Reason","vi":"Nhập Lý do"},
    },
    reference_no: {
      label: {"ja":"参照番号","en":"Reference No","vi":"Mã tham chiếu"},
      placeholder: {"ja":"参照番号を入力","en":"Enter Reference No","vi":"Nhập Mã tham chiếu"},
    },
    occurred_at: {
      label: {"ja":"発生日時","en":"Occurred At","vi":"Thời điểm"},
      placeholder: {"ja":"発生日時を入力","en":"Enter Occurred At","vi":"Nhập Thời điểm"},
    },
    performed_by_id: {
      label: {"ja":"実行者","en":"Performed By","vi":"Người thực hiện"},
      placeholder: {"ja":"実行者を入力","en":"Enter Performed By","vi":"Nhập Người thực hiện"},
    },
    manual_adjustment: {
      label: {"ja":"手動調整","en":"Manual Adjustment","vi":"Điều chỉnh thủ công"},
      placeholder: {"ja":"手動調整を入力","en":"Enter Manual Adjustment","vi":"Nhập Điều chỉnh thủ công"},
    },
    session: {
      label: {"ja":"セッション","en":"Session","vi":"Ca"},
      placeholder: {"ja":"セッションを入力","en":"Enter Session","vi":"Nhập Ca"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TillCashEvent */
export const baseTillCashEventSchemas = {
  event_type: z.nativeEnum(TillCashEventType),
  amount: z.number(),
  currency_code: z.string().min(1).max(3),
  reason: z.string().optional().nullable(),
  reference_no: z.string().max(100).optional().nullable(),
  occurred_at: z.string().datetime({ offset: true }),
  performed_by_id: z.string().optional().nullable(),
  manual_adjustment: z.boolean(),
} as const;

/** Create schema for TillCashEvent */
export const baseTillCashEventCreateSchema = z.object({
  event_type: baseTillCashEventSchemas.event_type,
  amount: baseTillCashEventSchemas.amount,
  currency_code: baseTillCashEventSchemas.currency_code,
  reason: baseTillCashEventSchemas.reason,
  reference_no: baseTillCashEventSchemas.reference_no,
  occurred_at: baseTillCashEventSchemas.occurred_at,
  performed_by_id: baseTillCashEventSchemas.performed_by_id,
  manual_adjustment: baseTillCashEventSchemas.manual_adjustment,
});

/** Update schema for TillCashEvent */
export const baseTillCashEventUpdateSchema = baseTillCashEventCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTillCashEventCreate = z.infer<typeof baseTillCashEventCreateSchema>;
export type BaseTillCashEventUpdate = z.infer<typeof baseTillCashEventUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTillCashEventLabel(locale: string): string {
  return tillCashEventI18n.label[locale as keyof typeof tillCashEventI18n.label] ?? tillCashEventI18n.label['en'] ?? 'TillCashEvent';
}

/** Get field label for a specific locale */
export function getTillCashEventFieldLabel(field: string, locale: string): string {
  const fields = tillCashEventI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTillCashEventFieldPlaceholder(field: string, locale: string): string {
  const fields = tillCashEventI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TillCashEvent.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tillCashEventMetadata = {
  modelName: "TillCashEvent",
  table: "till_cash_events",

  fields: {
    event_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "TillCashEventType",
      enumValues: ["paid_in", "paid_out", "loan_from_safe", "pickup_to_safe"] as const,
      label: {
        ja: "種別",
        en: "Event Type",
        vi: "Loại",
      },
      placeholder: {
        ja: "種別を入力",
        en: "Enter Event Type",
        vi: "Nhập Loại",
      },
    },
    amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "金額",
        en: "Amount",
        vi: "Số tiền",
      },
      placeholder: {
        ja: "金額を入力",
        en: "Enter Amount",
        vi: "Nhập Số tiền",
      },
    },
    currency_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 3,
      },
      label: {
        ja: "通貨",
        en: "Currency",
        vi: "Tiền tệ",
      },
      placeholder: {
        ja: "通貨を入力",
        en: "Enter Currency",
        vi: "Nhập Tiền tệ",
      },
    },
    reason: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "理由",
        en: "Reason",
        vi: "Lý do",
      },
      placeholder: {
        ja: "理由を入力",
        en: "Enter Reason",
        vi: "Nhập Lý do",
      },
    },
    reference_no: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "参照番号",
        en: "Reference No",
        vi: "Mã tham chiếu",
      },
      placeholder: {
        ja: "参照番号を入力",
        en: "Enter Reference No",
        vi: "Nhập Mã tham chiếu",
      },
    },
    occurred_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "発生日時",
        en: "Occurred At",
        vi: "Thời điểm",
      },
      placeholder: {
        ja: "発生日時を入力",
        en: "Enter Occurred At",
        vi: "Nhập Thời điểm",
      },
    },
    performed_by_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "実行者",
        en: "Performed By",
        vi: "Người thực hiện",
      },
      placeholder: {
        ja: "実行者を入力",
        en: "Enter Performed By",
        vi: "Nhập Người thực hiện",
      },
    },
    manual_adjustment: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "手動調整",
        en: "Manual Adjustment",
        vi: "Điều chỉnh thủ công",
      },
      placeholder: {
        ja: "手動調整を入力",
        en: "Enter Manual Adjustment",
        vi: "Nhập Điều chỉnh thủ công",
      },
    },
    session_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "TillSession",
        table: "till_sessions",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["event_type", "amount", "currency_code", "occurred_at", "manual_adjustment", "session_id"] as const,
  enumFields: ["event_type"] as const,
  relationFields: ["session_id"] as const,
} as const;

export type TillCashEventFieldName = keyof typeof tillCashEventMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TillCashEvent. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TillCashEventCreateFormState {
  event_type: TillCashEventType;
  amount: number;
  currency_code: string;
  reason?: string | null;
  reference_no?: string | null;
  occurred_at: string;
  performed_by_id?: string | null;
  manual_adjustment: boolean;
  session_id: string;
}

/** Form-state shape for updating a TillCashEvent. Every field optional. */
export interface TillCashEventUpdateFormState {
  event_type?: TillCashEventType;
  amount?: number;
  currency_code?: string;
  reason?: string | null;
  reference_no?: string | null;
  occurred_at?: string;
  performed_by_id?: string | null;
  manual_adjustment?: boolean;
  session_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTillCashEventCreateForm(): TillCashEventCreateFormState {
  return {
    event_type: TillCashEventType.PaidIn,
    amount: 0,
    currency_code: "JPY",
    reason: '',
    reference_no: '',
    occurred_at: '',
    performed_by_id: '',
    manual_adjustment: false,
    session_id: '',
  };
}

/** Convert create form state into the payload that baseTillCashEventCreateSchema accepts. */
export function buildTillCashEventCreatePayload(form: TillCashEventCreateFormState): BaseTillCashEventCreate {
  const payload: Record<string, unknown> = {
    event_type: form.event_type,
    amount: form.amount,
    currency_code: form.currency_code.trim(),
    reason: form.reason?.toString().trim() || null,
    reference_no: form.reference_no?.toString().trim() || null,
    occurred_at: form.occurred_at,
    performed_by_id: form.performed_by_id?.toString().trim() || null,
    manual_adjustment: form.manual_adjustment,
    session_id: form.session_id.trim(),
  };

  return payload as BaseTillCashEventCreate;
}

/** Convert update form state into the payload that baseTillCashEventUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTillCashEventUpdatePayload(form: TillCashEventUpdateFormState): BaseTillCashEventUpdate {
  const payload: Record<string, unknown> = {};
  if (form.event_type !== undefined) {
    payload.event_type = form.event_type;
  }
  if (form.amount !== undefined) {
    payload.amount = form.amount;
  }
  if (form.currency_code !== undefined) {
    payload.currency_code = form.currency_code?.toString().trim() ?? null;
  }
  if (form.reason !== undefined) {
    payload.reason = form.reason?.toString().trim() ?? null;
  }
  if (form.reference_no !== undefined) {
    payload.reference_no = form.reference_no?.toString().trim() ?? null;
  }
  if (form.occurred_at !== undefined) {
    payload.occurred_at = form.occurred_at;
  }
  if (form.performed_by_id !== undefined) {
    payload.performed_by_id = form.performed_by_id?.toString().trim() ?? null;
  }
  if (form.manual_adjustment !== undefined) {
    payload.manual_adjustment = form.manual_adjustment;
  }
  if (form.session_id !== undefined) {
    payload.session_id = form.session_id?.toString().trim() ?? null;
  }
  return payload as BaseTillCashEventUpdate;
}
