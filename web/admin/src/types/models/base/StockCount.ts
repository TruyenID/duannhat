/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { StockCountScope } from '../enum/StockCountScope';
import { StockCountStatus } from '../enum/StockCountStatus';
import type { Organization } from './Organization';
import type { StockCountItem } from './StockCountItem';
import type { Warehouse } from './Warehouse';

/**
 * 棚卸
 */
export interface StockCount {
  id: string;
  count_code: string;
  warehouse_id: string;
  warehouse?: Warehouse;
  status: StockCountStatus;
  scope: StockCountScope;
  note?: string | null;
  created_by_id: string;
  approved_by_id?: string | null;
  approved_at?: DateTimeString | null;
  completed_at?: DateTimeString | null;
  items?: StockCountItem[];
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for StockCount
 */
export const stockCountI18n = {
  label: {"ja":"棚卸","en":"Stock Count","vi":"Kiểm kê"},
  fields: {
    count_code: {
      label: {"ja":"棚卸コード","en":"Count Code","vi":"Mã kiểm kê"},
      placeholder: {"ja":"棚卸コードを入力","en":"Enter Count Code","vi":"Nhập Mã kiểm kê"},
    },
    warehouse: {
      label: {"ja":"倉庫","en":"Warehouse","vi":"Kho hàng"},
      placeholder: {"ja":"倉庫を入力","en":"Enter Warehouse","vi":"Nhập Kho hàng"},
    },
    status: {
      label: {"ja":"ステータス","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"ステータスを入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    scope: {
      label: {"ja":"範囲","en":"Scope","vi":"Phạm vi"},
      placeholder: {"ja":"範囲を入力","en":"Enter Scope","vi":"Nhập Phạm vi"},
    },
    note: {
      label: {"ja":"備考","en":"Note","vi":"Ghi chú"},
      placeholder: {"ja":"備考を入力","en":"Enter Note","vi":"Nhập Ghi chú"},
    },
    created_by_id: {
      label: {"ja":"作成者","en":"Created By","vi":"Người tạo"},
      placeholder: {"ja":"作成者を入力","en":"Enter Created By","vi":"Nhập Người tạo"},
    },
    approved_by_id: {
      label: {"ja":"承認者","en":"Approved By","vi":"Người duyệt"},
      placeholder: {"ja":"承認者を入力","en":"Enter Approved By","vi":"Nhập Người duyệt"},
    },
    approved_at: {
      label: {"ja":"承認日時","en":"Approved At","vi":"Ngày duyệt"},
      placeholder: {"ja":"承認日時を入力","en":"Enter Approved At","vi":"Nhập Ngày duyệt"},
    },
    completed_at: {
      label: {"ja":"完了日時","en":"Completed At","vi":"Ngày hoàn thành"},
      placeholder: {"ja":"完了日時を入力","en":"Enter Completed At","vi":"Nhập Ngày hoàn thành"},
    },
    items: {
      label: {"ja":"明細","en":"Items","vi":"Chi tiết"},
      placeholder: {"ja":"明細を入力","en":"Enter Items","vi":"Nhập Chi tiết"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for StockCount */
export const baseStockCountSchemas = {
  count_code: z.string().min(1).max(50),
  status: z.nativeEnum(StockCountStatus),
  scope: z.nativeEnum(StockCountScope),
  note: z.string().optional().nullable(),
  created_by_id: z.string(),
  approved_by_id: z.string().optional().nullable(),
  approved_at: z.string().datetime({ offset: true }).optional().nullable(),
  completed_at: z.string().datetime({ offset: true }).optional().nullable(),
} as const;

/** Create schema for StockCount */
export const baseStockCountCreateSchema = z.object({
  count_code: baseStockCountSchemas.count_code,
  status: baseStockCountSchemas.status,
  scope: baseStockCountSchemas.scope,
  note: baseStockCountSchemas.note,
  created_by_id: baseStockCountSchemas.created_by_id,
  approved_by_id: baseStockCountSchemas.approved_by_id,
  approved_at: baseStockCountSchemas.approved_at,
  completed_at: baseStockCountSchemas.completed_at,
});

/** Update schema for StockCount */
export const baseStockCountUpdateSchema = baseStockCountCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseStockCountCreate = z.infer<typeof baseStockCountCreateSchema>;
export type BaseStockCountUpdate = z.infer<typeof baseStockCountUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getStockCountLabel(locale: string): string {
  return stockCountI18n.label[locale as keyof typeof stockCountI18n.label] ?? stockCountI18n.label['en'] ?? 'StockCount';
}

/** Get field label for a specific locale */
export function getStockCountFieldLabel(field: string, locale: string): string {
  const fields = stockCountI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getStockCountFieldPlaceholder(field: string, locale: string): string {
  const fields = stockCountI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for StockCount.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const stockCountMetadata = {
  modelName: "StockCount",
  table: "stock_counts",

  fields: {
    count_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "棚卸コード",
        en: "Count Code",
        vi: "Mã kiểm kê",
      },
      placeholder: {
        ja: "棚卸コードを入力",
        en: "Enter Count Code",
        vi: "Nhập Mã kiểm kê",
      },
      description: {
        ja: "自動生成コード：SC-YYYYMMDD-XXX",
        en: "Auto-generated code: SC-YYYYMMDD-XXX",
        vi: "Mã tự động: SC-YYYYMMDD-XXX",
      },
    },
    warehouse_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Warehouse",
        table: "warehouses",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    status: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "StockCountStatus",
      enumValues: ["draft", "in_progress", "pending_approval", "approved", "cancelled"] as const,
      label: {
        ja: "ステータス",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "ステータスを入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
      description: {
        ja: "ワークフローステータス：下書き → 実施中 → 承認待ち → 承認済み / キャンセル",
        en: "Workflow status: draft → in_progress → pending_approval → approved / cancelled",
        vi: "Trạng thái workflow: nháp → đang kiểm → chờ duyệt → đã duyệt / đã hủy",
      },
    },
    scope: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "StockCountScope",
      enumValues: ["full", "partial"] as const,
      label: {
        ja: "範囲",
        en: "Scope",
        vi: "Phạm vi",
      },
      placeholder: {
        ja: "範囲を入力",
        en: "Enter Scope",
        vi: "Nhập Phạm vi",
      },
      description: {
        ja: "full：倉庫の全品を棚卸。partial：選択した品目のみ。",
        en: "full: count all items in warehouse. partial: count selected items only.",
        vi: "full: kiểm kê toàn bộ. partial: kiểm kê sản phẩm được chọn.",
      },
    },
    note: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "備考",
        en: "Note",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "備考を入力",
        en: "Enter Note",
        vi: "Nhập Ghi chú",
      },
    },
    created_by_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "作成者",
        en: "Created By",
        vi: "Người tạo",
      },
      placeholder: {
        ja: "作成者を入力",
        en: "Enter Created By",
        vi: "Nhập Người tạo",
      },
      description: {
        ja: "この棚卸を作成したSSOユーザー（user_caches.id）。ハードFKなし。",
        en: "SSO user who created this stock count (user_caches.id). No hard FK.",
        vi: "User SSO tạo phiên kiểm kê (user_caches.id). Không dùng FK cứng.",
      },
    },
    approved_by_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "承認者",
        en: "Approved By",
        vi: "Người duyệt",
      },
      placeholder: {
        ja: "承認者を入力",
        en: "Enter Approved By",
        vi: "Nhập Người duyệt",
      },
      description: {
        ja: "この棚卸を承認したSSOユーザー。未承認の場合はNull。",
        en: "SSO user who approved this stock count. Null if not yet approved.",
        vi: "User SSO duyệt phiên kiểm kê. Null nếu chưa duyệt.",
      },
    },
    approved_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "承認日時",
        en: "Approved At",
        vi: "Ngày duyệt",
      },
      placeholder: {
        ja: "承認日時を入力",
        en: "Enter Approved At",
        vi: "Nhập Ngày duyệt",
      },
    },
    completed_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "完了日時",
        en: "Completed At",
        vi: "Ngày hoàn thành",
      },
      placeholder: {
        ja: "完了日時を入力",
        en: "Enter Completed At",
        vi: "Nhập Ngày hoàn thành",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["count_code", "warehouse_id", "status", "scope", "created_by_id", "organization_id"] as const,
  enumFields: ["status", "scope"] as const,
  relationFields: ["warehouse_id", "organization_id"] as const,
} as const;

export type StockCountFieldName = keyof typeof stockCountMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a StockCount. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface StockCountCreateFormState {
  count_code: string;
  warehouse_id: string;
  status: StockCountStatus;
  scope: StockCountScope;
  note?: string | null;
  created_by_id: string;
  approved_by_id?: string | null;
  approved_at?: string | null;
  completed_at?: string | null;
  organization_id: string;
}

/** Form-state shape for updating a StockCount. Every field optional. */
export interface StockCountUpdateFormState {
  count_code?: string;
  warehouse_id?: string;
  status?: StockCountStatus;
  scope?: StockCountScope;
  note?: string | null;
  created_by_id?: string;
  approved_by_id?: string | null;
  approved_at?: string | null;
  completed_at?: string | null;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyStockCountCreateForm(): StockCountCreateFormState {
  return {
    count_code: '',
    warehouse_id: '',
    status: StockCountStatus.Draft,
    scope: StockCountScope.Full,
    note: '',
    created_by_id: '',
    approved_by_id: '',
    approved_at: '',
    completed_at: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseStockCountCreateSchema accepts. */
export function buildStockCountCreatePayload(form: StockCountCreateFormState): BaseStockCountCreate {
  const payload: Record<string, unknown> = {
    count_code: form.count_code.trim(),
    warehouse_id: form.warehouse_id.trim(),
    status: form.status,
    scope: form.scope,
    note: form.note?.toString().trim() || null,
    created_by_id: form.created_by_id.trim(),
    approved_by_id: form.approved_by_id?.toString().trim() || null,
    approved_at: form.approved_at,
    completed_at: form.completed_at,
    organization_id: form.organization_id.trim(),
  };

  return payload as BaseStockCountCreate;
}

/** Convert update form state into the payload that baseStockCountUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildStockCountUpdatePayload(form: StockCountUpdateFormState): BaseStockCountUpdate {
  const payload: Record<string, unknown> = {};
  if (form.count_code !== undefined) {
    payload.count_code = form.count_code?.toString().trim() ?? null;
  }
  if (form.warehouse_id !== undefined) {
    payload.warehouse_id = form.warehouse_id?.toString().trim() ?? null;
  }
  if (form.status !== undefined) {
    payload.status = form.status;
  }
  if (form.scope !== undefined) {
    payload.scope = form.scope;
  }
  if (form.note !== undefined) {
    payload.note = form.note?.toString().trim() ?? null;
  }
  if (form.created_by_id !== undefined) {
    payload.created_by_id = form.created_by_id?.toString().trim() ?? null;
  }
  if (form.approved_by_id !== undefined) {
    payload.approved_by_id = form.approved_by_id?.toString().trim() ?? null;
  }
  if (form.approved_at !== undefined) {
    payload.approved_at = form.approved_at;
  }
  if (form.completed_at !== undefined) {
    payload.completed_at = form.completed_at;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseStockCountUpdate;
}
