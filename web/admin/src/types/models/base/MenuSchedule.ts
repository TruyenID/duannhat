/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString, DateString } from '../common';
import { MenuScheduleRecurrence } from '../enum/MenuScheduleRecurrence';
import type { Menu } from './Menu';
import type { MenuScheduleDate } from './MenuScheduleDate';

/**
 * メニュースケジュール
 */
export interface MenuSchedule {
  id: string;
  start_time: string;
  end_time: string;
  start_date?: DateString | null;
  end_date?: DateString | null;
  days_of_week: number;
  recurrence_kind: MenuScheduleRecurrence;
  days_of_month?: number | null;
  is_active: boolean;
  priority: number;
  created_by_id?: string | null;
  menu_id: string;
  menu?: Menu;
  scheduleDates?: MenuScheduleDate[];
  master_schedule_id?: string | null;
  masterSchedule?: MenuSchedule | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuSchedule
 */
export const menuScheduleI18n = {
  label: {"ja":"メニュースケジュール","en":"Menu Schedule","vi":"Lịch trình menu"},
  fields: {
    start_time: {
      label: {"ja":"開始時刻","en":"Start Time","vi":"Giờ bắt đầu"},
      placeholder: {"ja":"開始時刻を入力","en":"Enter Start Time","vi":"Nhập Giờ bắt đầu"},
    },
    end_time: {
      label: {"ja":"終了時刻","en":"End Time","vi":"Giờ kết thúc"},
      placeholder: {"ja":"終了時刻を入力","en":"Enter End Time","vi":"Nhập Giờ kết thúc"},
    },
    start_date: {
      label: {"ja":"開始日","en":"Start Date","vi":"Ngày bắt đầu"},
      placeholder: {"ja":"開始日を入力","en":"Enter Start Date","vi":"Nhập Ngày bắt đầu"},
    },
    end_date: {
      label: {"ja":"終了日","en":"End Date","vi":"Ngày kết thúc"},
      placeholder: {"ja":"終了日を入力","en":"Enter End Date","vi":"Nhập Ngày kết thúc"},
    },
    days_of_week: {
      label: {"ja":"曜日","en":"Days of Week","vi":"Ngày trong tuần"},
      placeholder: {"ja":"曜日を入力","en":"Enter Days of Week","vi":"Nhập Ngày trong tuần"},
    },
    recurrence_kind: {
      label: {"ja":"繰り返し種別","en":"Recurrence Kind","vi":"Kiểu lặp"},
      placeholder: {"ja":"繰り返し種別を入力","en":"Enter Recurrence Kind","vi":"Nhập Kiểu lặp"},
    },
    days_of_month: {
      label: {"ja":"日付（毎月）","en":"Days of Month","vi":"Ngày trong tháng"},
      placeholder: {"ja":"日付（毎月）を入力","en":"Enter Days of Month","vi":"Nhập Ngày trong tháng"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    priority: {
      label: {"ja":"優先度","en":"Priority","vi":"Độ ưu tiên"},
      placeholder: {"ja":"優先度を入力","en":"Enter Priority","vi":"Nhập Độ ưu tiên"},
    },
    created_by_id: {
      label: {"ja":"作成者","en":"Created By","vi":"Người tạo"},
      placeholder: {"ja":"作成者を入力","en":"Enter Created By","vi":"Nhập Người tạo"},
    },
    menu: {
      label: {"ja":"メニュー","en":"Menu","vi":"Menu"},
      placeholder: {"ja":"メニューを入力","en":"Enter Menu","vi":"Nhập Menu"},
    },
    schedule_dates: {
      label: {"ja":"特定日","en":"Specific Dates","vi":"Ngày cụ thể"},
      placeholder: {"ja":"特定日を入力","en":"Enter Specific Dates","vi":"Nhập Ngày cụ thể"},
    },
    master_schedule: {
      label: {"ja":"マスタースケジュール","en":"Master Schedule","vi":"Lịch gốc (HQ)"},
      placeholder: {"ja":"マスタースケジュールを入力","en":"Enter Master Schedule","vi":"Nhập Lịch gốc (HQ)"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuSchedule */
export const baseMenuScheduleSchemas = {
  start_time: z.string().time(),
  end_time: z.string().time(),
  start_date: z.string().date().optional().nullable(),
  end_date: z.string().date().optional().nullable(),
  days_of_week: z.number().int(),
  recurrence_kind: z.nativeEnum(MenuScheduleRecurrence),
  days_of_month: z.number().int().optional().nullable(),
  is_active: z.boolean(),
  priority: z.number().int(),
  created_by_id: z.string().optional().nullable(),
} as const;

/** Create schema for MenuSchedule */
export const baseMenuScheduleCreateSchema = z.object({
  start_time: baseMenuScheduleSchemas.start_time,
  end_time: baseMenuScheduleSchemas.end_time,
  start_date: baseMenuScheduleSchemas.start_date,
  end_date: baseMenuScheduleSchemas.end_date,
  days_of_week: baseMenuScheduleSchemas.days_of_week,
  recurrence_kind: baseMenuScheduleSchemas.recurrence_kind,
  days_of_month: baseMenuScheduleSchemas.days_of_month,
  is_active: baseMenuScheduleSchemas.is_active,
  priority: baseMenuScheduleSchemas.priority,
  created_by_id: baseMenuScheduleSchemas.created_by_id,
});

/** Update schema for MenuSchedule */
export const baseMenuScheduleUpdateSchema = baseMenuScheduleCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuScheduleCreate = z.infer<typeof baseMenuScheduleCreateSchema>;
export type BaseMenuScheduleUpdate = z.infer<typeof baseMenuScheduleUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuScheduleLabel(locale: string): string {
  return menuScheduleI18n.label[locale as keyof typeof menuScheduleI18n.label] ?? menuScheduleI18n.label['en'] ?? 'MenuSchedule';
}

/** Get field label for a specific locale */
export function getMenuScheduleFieldLabel(field: string, locale: string): string {
  const fields = menuScheduleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuScheduleFieldPlaceholder(field: string, locale: string): string {
  const fields = menuScheduleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuSchedule.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuScheduleMetadata = {
  modelName: "MenuSchedule",
  table: "menu_schedules",

  fields: {
    start_time: {
      type: "time",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "開始時刻",
        en: "Start Time",
        vi: "Giờ bắt đầu",
      },
      placeholder: {
        ja: "開始時刻を入力",
        en: "Enter Start Time",
        vi: "Nhập Giờ bắt đầu",
      },
    },
    end_time: {
      type: "time",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "終了時刻",
        en: "End Time",
        vi: "Giờ kết thúc",
      },
      placeholder: {
        ja: "終了時刻を入力",
        en: "Enter End Time",
        vi: "Nhập Giờ kết thúc",
      },
    },
    start_date: {
      type: "date",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "開始日",
        en: "Start Date",
        vi: "Ngày bắt đầu",
      },
      placeholder: {
        ja: "開始日を入力",
        en: "Enter Start Date",
        vi: "Nhập Ngày bắt đầu",
      },
    },
    end_date: {
      type: "date",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "終了日",
        en: "End Date",
        vi: "Ngày kết thúc",
      },
      placeholder: {
        ja: "終了日を入力",
        en: "Enter End Date",
        vi: "Nhập Ngày kết thúc",
      },
    },
    days_of_week: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "曜日",
        en: "Days of Week",
        vi: "Ngày trong tuần",
      },
      placeholder: {
        ja: "曜日を入力",
        en: "Enter Days of Week",
        vi: "Nhập Ngày trong tuần",
      },
    },
    recurrence_kind: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "MenuScheduleRecurrence",
      enumValues: ["Weekly", "Monthly", "SpecificDates"] as const,
      label: {
        ja: "繰り返し種別",
        en: "Recurrence Kind",
        vi: "Kiểu lặp",
      },
      placeholder: {
        ja: "繰り返し種別を入力",
        en: "Enter Recurrence Kind",
        vi: "Nhập Kiểu lặp",
      },
    },
    days_of_month: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "日付（毎月）",
        en: "Days of Month",
        vi: "Ngày trong tháng",
      },
      placeholder: {
        ja: "日付（毎月）を入力",
        en: "Enter Days of Month",
        vi: "Nhập Ngày trong tháng",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    priority: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "優先度",
        en: "Priority",
        vi: "Độ ưu tiên",
      },
      placeholder: {
        ja: "優先度を入力",
        en: "Enter Priority",
        vi: "Nhập Độ ưu tiên",
      },
    },
    created_by_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "作成者",
        en: "Created By",
        vi: "Người tạo",
      },
      placeholder: {
        ja: "作成者を入力",
        en: "Enter Created By",
        vi: "Nhập Người tạo",
      },
    },
    menu_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Menu",
        table: "menus",
        onDelete: "CASCADE",
      },
      label: {},
    },
    master_schedule_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "MenuSchedule",
        table: "menu_schedules",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["start_time", "end_time", "days_of_week", "recurrence_kind", "is_active", "priority", "menu_id"] as const,
  enumFields: ["recurrence_kind"] as const,
  relationFields: ["menu_id", "master_schedule_id"] as const,
} as const;

export type MenuScheduleFieldName = keyof typeof menuScheduleMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuSchedule. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuScheduleCreateFormState {
  start_time: string;
  end_time: string;
  start_date?: string | null;
  end_date?: string | null;
  days_of_week: number;
  recurrence_kind: MenuScheduleRecurrence;
  days_of_month?: number | null;
  is_active: boolean;
  priority: number;
  created_by_id?: string | null;
  menu_id: string;
  master_schedule_id?: string | null;
}

/** Form-state shape for updating a MenuSchedule. Every field optional. */
export interface MenuScheduleUpdateFormState {
  start_time?: string;
  end_time?: string;
  start_date?: string | null;
  end_date?: string | null;
  days_of_week?: number;
  recurrence_kind?: MenuScheduleRecurrence;
  days_of_month?: number | null;
  is_active?: boolean;
  priority?: number;
  created_by_id?: string | null;
  menu_id?: string;
  master_schedule_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuScheduleCreateForm(): MenuScheduleCreateFormState {
  return {
    start_time: '',
    end_time: '',
    start_date: '',
    end_date: '',
    days_of_week: 0,
    recurrence_kind: MenuScheduleRecurrence.Weekly,
    days_of_month: 0,
    is_active: true,
    priority: 0,
    created_by_id: '',
    menu_id: '',
    master_schedule_id: '',
  };
}

/** Convert create form state into the payload that baseMenuScheduleCreateSchema accepts. */
export function buildMenuScheduleCreatePayload(form: MenuScheduleCreateFormState): BaseMenuScheduleCreate {
  const payload: Record<string, unknown> = {
    start_time: form.start_time,
    end_time: form.end_time,
    start_date: form.start_date,
    end_date: form.end_date,
    days_of_week: form.days_of_week,
    recurrence_kind: form.recurrence_kind,
    days_of_month: form.days_of_month,
    is_active: form.is_active,
    priority: form.priority,
    created_by_id: form.created_by_id?.toString().trim() || null,
    menu_id: form.menu_id.trim(),
    master_schedule_id: form.master_schedule_id?.toString().trim() || null,
  };

  return payload as BaseMenuScheduleCreate;
}

/** Convert update form state into the payload that baseMenuScheduleUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuScheduleUpdatePayload(form: MenuScheduleUpdateFormState): BaseMenuScheduleUpdate {
  const payload: Record<string, unknown> = {};
  if (form.start_time !== undefined) {
    payload.start_time = form.start_time;
  }
  if (form.end_time !== undefined) {
    payload.end_time = form.end_time;
  }
  if (form.start_date !== undefined) {
    payload.start_date = form.start_date;
  }
  if (form.end_date !== undefined) {
    payload.end_date = form.end_date;
  }
  if (form.days_of_week !== undefined) {
    payload.days_of_week = form.days_of_week;
  }
  if (form.recurrence_kind !== undefined) {
    payload.recurrence_kind = form.recurrence_kind;
  }
  if (form.days_of_month !== undefined) {
    payload.days_of_month = form.days_of_month;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.priority !== undefined) {
    payload.priority = form.priority;
  }
  if (form.created_by_id !== undefined) {
    payload.created_by_id = form.created_by_id?.toString().trim() ?? null;
  }
  if (form.menu_id !== undefined) {
    payload.menu_id = form.menu_id?.toString().trim() ?? null;
  }
  if (form.master_schedule_id !== undefined) {
    payload.master_schedule_id = form.master_schedule_id?.toString().trim() ?? null;
  }
  return payload as BaseMenuScheduleUpdate;
}
