/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { WorkstationLogLevel } from '../enum/WorkstationLogLevel';

/**
 * ワークステーションログ
 */
export interface WorkstationLogRecord {
  id: string;
  device_id: string;
  local_id: number;
  branch_id: string;
  organization_id: string;
  request_id: string;
  logged_at: DateTimeString;
  level: WorkstationLogLevel;
  message: string;
  attrs?: unknown | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for WorkstationLogRecord
 */
export const workstationLogRecordI18n = {
  label: {"ja":"ワークステーションログ","en":"Workstation Log Record","vi":"Bản ghi log máy trạm"},
  fields: {
    device_id: {
      label: {"ja":"デバイスID","en":"Device ID","vi":"ID thiết bị"},
      placeholder: {"ja":"デバイスIDを入力","en":"Enter Device ID","vi":"Nhập ID thiết bị"},
    },
    local_id: {
      label: {"ja":"ローカル行ID","en":"Local Row ID","vi":"ID dòng cục bộ"},
      placeholder: {"ja":"ローカル行IDを入力","en":"Enter Local Row ID","vi":"Nhập ID dòng cục bộ"},
    },
    branch_id: {
      label: {"ja":"店舗ID","en":"Branch ID","vi":"ID chi nhánh"},
      placeholder: {"ja":"店舗IDを入力","en":"Enter Branch ID","vi":"Nhập ID chi nhánh"},
    },
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
    request_id: {
      label: {"ja":"取得要求ID","en":"Request ID","vi":"ID yêu cầu"},
      placeholder: {"ja":"取得要求IDを入力","en":"Enter Request ID","vi":"Nhập ID yêu cầu"},
    },
    logged_at: {
      label: {"ja":"記録日時","en":"Logged At","vi":"Thời điểm ghi log"},
      placeholder: {"ja":"記録日時を入力","en":"Enter Logged At","vi":"Nhập Thời điểm ghi log"},
    },
    level: {
      label: {"ja":"ログレベル","en":"Level","vi":"Mức"},
      placeholder: {"ja":"ログレベルを入力","en":"Enter Level","vi":"Nhập Mức"},
    },
    message: {
      label: {"ja":"メッセージ","en":"Message","vi":"Thông điệp"},
      placeholder: {"ja":"メッセージを入力","en":"Enter Message","vi":"Nhập Thông điệp"},
    },
    attrs: {
      label: {"ja":"属性","en":"Attributes","vi":"Thuộc tính"},
      placeholder: {"ja":"属性を入力","en":"Enter Attributes","vi":"Nhập Thuộc tính"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for WorkstationLogRecord */
export const baseWorkstationLogRecordSchemas = {
  device_id: z.string(),
  local_id: z.number().int(),
  branch_id: z.string(),
  organization_id: z.string(),
  request_id: z.string(),
  logged_at: z.string().datetime({ offset: true }),
  level: z.nativeEnum(WorkstationLogLevel),
  message: z.string().min(1).max(255),
  attrs: z.unknown().optional().nullable(),
} as const;

/** Create schema for WorkstationLogRecord */
export const baseWorkstationLogRecordCreateSchema = z.object({
  device_id: baseWorkstationLogRecordSchemas.device_id,
  local_id: baseWorkstationLogRecordSchemas.local_id,
  branch_id: baseWorkstationLogRecordSchemas.branch_id,
  organization_id: baseWorkstationLogRecordSchemas.organization_id,
  request_id: baseWorkstationLogRecordSchemas.request_id,
  logged_at: baseWorkstationLogRecordSchemas.logged_at,
  level: baseWorkstationLogRecordSchemas.level,
  message: baseWorkstationLogRecordSchemas.message,
  attrs: baseWorkstationLogRecordSchemas.attrs,
});

/** Update schema for WorkstationLogRecord */
export const baseWorkstationLogRecordUpdateSchema = baseWorkstationLogRecordCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseWorkstationLogRecordCreate = z.infer<typeof baseWorkstationLogRecordCreateSchema>;
export type BaseWorkstationLogRecordUpdate = z.infer<typeof baseWorkstationLogRecordUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getWorkstationLogRecordLabel(locale: string): string {
  return workstationLogRecordI18n.label[locale as keyof typeof workstationLogRecordI18n.label] ?? workstationLogRecordI18n.label['en'] ?? 'WorkstationLogRecord';
}

/** Get field label for a specific locale */
export function getWorkstationLogRecordFieldLabel(field: string, locale: string): string {
  const fields = workstationLogRecordI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getWorkstationLogRecordFieldPlaceholder(field: string, locale: string): string {
  const fields = workstationLogRecordI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for WorkstationLogRecord.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const workstationLogRecordMetadata = {
  modelName: "WorkstationLogRecord",
  table: "workstation_log_records",

  fields: {
    device_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "デバイスID",
        en: "Device ID",
        vi: "ID thiết bị",
      },
      placeholder: {
        ja: "デバイスIDを入力",
        en: "Enter Device ID",
        vi: "Nhập ID thiết bị",
      },
      description: {
        ja: "行を出力したワークステーション。冪等キーの片方。FKなし（デバイスより長生きする必要がある）。",
        en: "Workstation that produced the line. Half of the idempotency key. Plain UUID, no FK — logs must outlive the device, because a broken device is exactly when they are read.",
        vi: "Máy trạm sinh ra dòng log. Một nửa khoá idempotency. Uuid trần, không FK — log phải sống lâu hơn thiết bị, vì thiết bị hỏng chính là lúc cần đọc nó.",
      },
    },
    local_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ローカル行ID",
        en: "Local Row ID",
        vi: "ID dòng cục bộ",
      },
      placeholder: {
        ja: "ローカル行IDを入力",
        en: "Enter Local Row ID",
        vi: "Nhập ID dòng cục bộ",
      },
      description: {
        ja: "デバイス側SQLiteバッファの自動採番ID。同一デバイス内で再利用されないため冪等キーに使う。",
        en: "Autoincrement id of the line in the device's own SQLite buffer. Stable and never reused on one device, which is why it is the idempotency key.",
        vi: "Id autoincrement của dòng trong vòng đệm SQLite của chính thiết bị. Ổn định, không tái sinh trên cùng thiết bị — vì thế nó là khoá idempotency.",
      },
    },
    branch_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "店舗ID",
        en: "Branch ID",
        vi: "ID chi nhánh",
      },
      placeholder: {
        ja: "店舗IDを入力",
        en: "Enter Branch ID",
        vi: "Nhập ID chi nhánh",
      },
      description: {
        ja: "デバイストークンから解決した店舗。ペイロードからは取らない。",
        en: "Branch resolved from the device token, never from the payload — a device cannot file logs against another branch.",
        vi: "Chi nhánh suy ra từ device token, KHÔNG lấy từ payload — thiết bị không ghi được log sang chi nhánh khác.",
      },
    },
    organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
      description: {
        ja: "店舗の console_organization_id からミラーした組織（#2847）。",
        en: "Local organization mirrored from console_organization_id of the branch (#2847 — branches has no organization_id column; trusting that it does killed 7,523 alerts).",
        vi: "Tổ chức local suy qua mirror console của chi nhánh (#2847 — bảng branches KHÔNG có cột organization_id; tin nhầm điều đó đã làm chết 7.523 alert).",
      },
    },
    request_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "取得要求ID",
        en: "Request ID",
        vi: "ID yêu cầu",
      },
      placeholder: {
        ja: "取得要求IDを入力",
        en: "Enter Request ID",
        vi: "Nhập ID yêu cầu",
      },
      description: {
        ja: "このバッチが応答した取得要求。FKなし。",
        en: "The pull request this batch answered. Plain UUID, no FK — a record must survive its request row being pruned on a different horizon.",
        vi: "Yêu cầu kéo mà lô này trả lời. Uuid trần, không FK — bản ghi phải sống sót khi hàng yêu cầu bị dọn theo một mốc khác.",
      },
    },
    logged_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "記録日時",
        en: "Logged At",
        vi: "Thời điểm ghi log",
      },
      placeholder: {
        ja: "記録日時を入力",
        en: "Enter Logged At",
        vi: "Nhập Thời điểm ghi log",
      },
      description: {
        ja: "端末で行が生成された時刻（RFC3339 UTC）。保存期限はこの時刻から数える。",
        en: "When the line was produced ON THE DEVICE (RFC3339 UTC). The retention horizon counts from HERE, not from created_at — otherwise a shop that was offline for 10 days silently gets 24 days of retention.",
        vi: "Lúc dòng log ra đời TRÊN THIẾT BỊ (RFC3339 UTC). Hạn 14 ngày đếm từ ĐÂY, không từ created_at — nếu không, quán mất mạng 10 ngày sẽ âm thầm được giữ 24 ngày.",
      },
    },
    level: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "WorkstationLogLevel",
      enumValues: ["info", "warn", "error"] as const,
      label: {
        ja: "ログレベル",
        en: "Level",
        vi: "Mức",
      },
      placeholder: {
        ja: "ログレベルを入力",
        en: "Enter Level",
        vi: "Nhập Mức",
      },
      description: {
        ja: "info | warn | error。`debug` はエンドポイントで422。",
        en: "info | warn | error. `debug` is refused with 422 at the endpoint — a debug line arriving means the source filter is broken, so the whole batch is suspect.",
        vi: "info | warn | error. `debug` bị từ chối 422 ngay ở endpoint — một dòng debug tới nơi nghĩa là bộ lọc ở nguồn đã hỏng, nên cả lô đáng ngờ.",
      },
    },
    message: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "メッセージ",
        en: "Message",
        vi: "Thông điệp",
      },
      placeholder: {
        ja: "メッセージを入力",
        en: "Enter Message",
        vi: "Nhập Thông điệp",
      },
      description: {
        ja: "slog のメッセージ原文。allowlist のキーなので正規化しない。",
        en: "The slog message VERBATIM. It is the allowlist key, so it is stored exactly as declared in docs/reference/workstation-log-allowlist.md — never normalised, or the key stops matching.",
        vi: "Thông điệp slog NGUYÊN VĂN. Nó là khoá allowlist nên lưu đúng như khai trong docs/reference/workstation-log-allowlist.md — không chuẩn hoá, nếu không khoá sẽ thôi khớp.",
      },
    },
    attrs: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "属性",
        en: "Attributes",
        vi: "Thuộc tính",
      },
      placeholder: {
        ja: "属性を入力",
        en: "Enter Attributes",
        vi: "Nhập Thuộc tính",
      },
      description: {
        ja: "この message に対して allowlist を通過した属性のみ。未知のキーは破棄し行は残す。",
        en: "Attributes that SURVIVED the allowlist for this message. Unknown keys are dropped and the line is kept; unknown messages drop the whole line instead. Null when the message declares no attributes.",
        vi: "Các attr ĐÃ QUA allowlist của đúng message này. Attr lạ bị bỏ và dòng vẫn giữ; message lạ thì bỏ cả dòng. Null khi message không khai attr nào.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["device_id", "local_id", "branch_id", "organization_id", "request_id", "logged_at", "level", "message"] as const,
  enumFields: ["level"] as const,
  relationFields: [] as const,
} as const;

export type WorkstationLogRecordFieldName = keyof typeof workstationLogRecordMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a WorkstationLogRecord. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface WorkstationLogRecordCreateFormState {
  device_id: string;
  local_id: number;
  branch_id: string;
  organization_id: string;
  request_id: string;
  logged_at: string;
  level: WorkstationLogLevel;
  message: string;
  attrs?: unknown | null;
}

/** Form-state shape for updating a WorkstationLogRecord. Every field optional. */
export interface WorkstationLogRecordUpdateFormState {
  device_id?: string;
  local_id?: number;
  branch_id?: string;
  organization_id?: string;
  request_id?: string;
  logged_at?: string;
  level?: WorkstationLogLevel;
  message?: string;
  attrs?: unknown | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyWorkstationLogRecordCreateForm(): WorkstationLogRecordCreateFormState {
  return {
    device_id: '',
    local_id: 0,
    branch_id: '',
    organization_id: '',
    request_id: '',
    logged_at: '',
    level: WorkstationLogLevel.Info,
    message: '',
    attrs: null,
  };
}

/** Convert create form state into the payload that baseWorkstationLogRecordCreateSchema accepts. */
export function buildWorkstationLogRecordCreatePayload(form: WorkstationLogRecordCreateFormState): BaseWorkstationLogRecordCreate {
  const payload: Record<string, unknown> = {
    device_id: form.device_id.trim(),
    local_id: form.local_id,
    branch_id: form.branch_id.trim(),
    organization_id: form.organization_id.trim(),
    request_id: form.request_id.trim(),
    logged_at: form.logged_at,
    level: form.level,
    message: form.message.trim(),
    attrs: form.attrs,
  };

  return payload as BaseWorkstationLogRecordCreate;
}

/** Convert update form state into the payload that baseWorkstationLogRecordUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildWorkstationLogRecordUpdatePayload(form: WorkstationLogRecordUpdateFormState): BaseWorkstationLogRecordUpdate {
  const payload: Record<string, unknown> = {};
  if (form.device_id !== undefined) {
    payload.device_id = form.device_id?.toString().trim() ?? null;
  }
  if (form.local_id !== undefined) {
    payload.local_id = form.local_id;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.request_id !== undefined) {
    payload.request_id = form.request_id?.toString().trim() ?? null;
  }
  if (form.logged_at !== undefined) {
    payload.logged_at = form.logged_at;
  }
  if (form.level !== undefined) {
    payload.level = form.level;
  }
  if (form.message !== undefined) {
    payload.message = form.message?.toString().trim() ?? null;
  }
  if (form.attrs !== undefined) {
    payload.attrs = form.attrs;
  }
  return payload as BaseWorkstationLogRecordUpdate;
}
