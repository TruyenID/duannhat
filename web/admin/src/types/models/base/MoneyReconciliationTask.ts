/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * 入金差異調整タスク
 */
export interface MoneyReconciliationTask {
  id: string;
  task_type: string;
  subject_type: string;
  subject_id: string;
  branch_id?: string | null;
  organization_id: string;
  payload: unknown;
  status: string;
  attempts: number;
  last_error?: string | null;
  resolution?: string | null;
  claimed_at?: DateTimeString | null;
  resolved_at?: DateTimeString | null;
  next_retry_at?: DateTimeString | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MoneyReconciliationTask
 */
export const moneyReconciliationTaskI18n = {
  label: {"ja":"入金差異調整タスク","en":"Money Reconciliation Task","vi":"Tác vụ đối soát tiền"},
  fields: {
    task_type: {
      label: {"ja":"タスク種別","en":"Task Type","vi":"Loại tác vụ"},
      placeholder: {"ja":"タスク種別を入力","en":"Enter Task Type","vi":"Nhập Loại tác vụ"},
    },
    subject_type: {
      label: {"ja":"対象種別","en":"Subject Type","vi":"Loại đối tượng"},
      placeholder: {"ja":"対象種別を入力","en":"Enter Subject Type","vi":"Nhập Loại đối tượng"},
    },
    subject_id: {
      label: {"ja":"対象ID","en":"Subject ID","vi":"ID đối tượng"},
      placeholder: {"ja":"対象IDを入力","en":"Enter Subject ID","vi":"Nhập ID đối tượng"},
    },
    branch_id: {
      label: {"ja":"店舗ID","en":"Branch ID","vi":"ID chi nhánh"},
      placeholder: {"ja":"店舗IDを入力","en":"Enter Branch ID","vi":"Nhập ID chi nhánh"},
    },
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
    payload: {
      label: {"ja":"確定ペイロード","en":"Settled Payload","vi":"Dữ liệu đã chốt"},
      placeholder: {"ja":"確定ペイロードを入力","en":"Enter Settled Payload","vi":"Nhập Dữ liệu đã chốt"},
    },
    status: {
      label: {"ja":"状態","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"状態を入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    attempts: {
      label: {"ja":"試行回数","en":"Attempts","vi":"Số lần thử"},
      placeholder: {"ja":"試行回数を入力","en":"Enter Attempts","vi":"Nhập Số lần thử"},
    },
    last_error: {
      label: {"ja":"最終エラー","en":"Last Error","vi":"Lỗi gần nhất"},
      placeholder: {"ja":"最終エラーを入力","en":"Enter Last Error","vi":"Nhập Lỗi gần nhất"},
    },
    resolution: {
      label: {"ja":"解決内容","en":"Resolution","vi":"Cách xử lý"},
      placeholder: {"ja":"解決内容を入力","en":"Enter Resolution","vi":"Nhập Cách xử lý"},
    },
    claimed_at: {
      label: {"ja":"取得日時","en":"Claimed At","vi":"Thời điểm nhận xử lý"},
      placeholder: {"ja":"取得日時を入力","en":"Enter Claimed At","vi":"Nhập Thời điểm nhận xử lý"},
    },
    resolved_at: {
      label: {"ja":"解決日時","en":"Resolved At","vi":"Thời điểm xử lý xong"},
      placeholder: {"ja":"解決日時を入力","en":"Enter Resolved At","vi":"Nhập Thời điểm xử lý xong"},
    },
    next_retry_at: {
      label: {"ja":"次回再試行","en":"Next Retry At","vi":"Lần thử tiếp theo"},
      placeholder: {"ja":"次回再試行を入力","en":"Enter Next Retry At","vi":"Nhập Lần thử tiếp theo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MoneyReconciliationTask */
export const baseMoneyReconciliationTaskSchemas = {
  task_type: z.string().min(1).max(40),
  subject_type: z.string().min(1).max(40),
  subject_id: z.string(),
  branch_id: z.string().optional().nullable(),
  organization_id: z.string(),
  payload: z.unknown(),
  status: z.string().min(1).max(20),
  attempts: z.number().int(),
  last_error: z.string().max(1000).optional().nullable(),
  resolution: z.string().max(1000).optional().nullable(),
  claimed_at: z.string().datetime({ offset: true }).optional().nullable(),
  resolved_at: z.string().datetime({ offset: true }).optional().nullable(),
  next_retry_at: z.string().datetime({ offset: true }).optional().nullable(),
} as const;

/** Create schema for MoneyReconciliationTask */
export const baseMoneyReconciliationTaskCreateSchema = z.object({
  task_type: baseMoneyReconciliationTaskSchemas.task_type,
  subject_type: baseMoneyReconciliationTaskSchemas.subject_type,
  subject_id: baseMoneyReconciliationTaskSchemas.subject_id,
  branch_id: baseMoneyReconciliationTaskSchemas.branch_id,
  organization_id: baseMoneyReconciliationTaskSchemas.organization_id,
  payload: baseMoneyReconciliationTaskSchemas.payload,
  status: baseMoneyReconciliationTaskSchemas.status,
  attempts: baseMoneyReconciliationTaskSchemas.attempts,
  last_error: baseMoneyReconciliationTaskSchemas.last_error,
  resolution: baseMoneyReconciliationTaskSchemas.resolution,
  claimed_at: baseMoneyReconciliationTaskSchemas.claimed_at,
  resolved_at: baseMoneyReconciliationTaskSchemas.resolved_at,
  next_retry_at: baseMoneyReconciliationTaskSchemas.next_retry_at,
});

/** Update schema for MoneyReconciliationTask */
export const baseMoneyReconciliationTaskUpdateSchema = baseMoneyReconciliationTaskCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMoneyReconciliationTaskCreate = z.infer<typeof baseMoneyReconciliationTaskCreateSchema>;
export type BaseMoneyReconciliationTaskUpdate = z.infer<typeof baseMoneyReconciliationTaskUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMoneyReconciliationTaskLabel(locale: string): string {
  return moneyReconciliationTaskI18n.label[locale as keyof typeof moneyReconciliationTaskI18n.label] ?? moneyReconciliationTaskI18n.label['en'] ?? 'MoneyReconciliationTask';
}

/** Get field label for a specific locale */
export function getMoneyReconciliationTaskFieldLabel(field: string, locale: string): string {
  const fields = moneyReconciliationTaskI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMoneyReconciliationTaskFieldPlaceholder(field: string, locale: string): string {
  const fields = moneyReconciliationTaskI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MoneyReconciliationTask.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const moneyReconciliationTaskMetadata = {
  modelName: "MoneyReconciliationTask",
  table: "money_reconciliation_tasks",

  fields: {
    task_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 40,
      },
      label: {
        ja: "タスク種別",
        en: "Task Type",
        vi: "Loại tác vụ",
      },
      placeholder: {
        ja: "タスク種別を入力",
        en: "Enter Task Type",
        vi: "Nhập Loại tác vụ",
      },
    },
    subject_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 40,
      },
      label: {
        ja: "対象種別",
        en: "Subject Type",
        vi: "Loại đối tượng",
      },
      placeholder: {
        ja: "対象種別を入力",
        en: "Enter Subject Type",
        vi: "Nhập Loại đối tượng",
      },
    },
    subject_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "対象ID",
        en: "Subject ID",
        vi: "ID đối tượng",
      },
      placeholder: {
        ja: "対象IDを入力",
        en: "Enter Subject ID",
        vi: "Nhập ID đối tượng",
      },
    },
    branch_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "店舗ID",
        en: "Branch ID",
        vi: "ID chi nhánh",
      },
      placeholder: {
        ja: "店舗IDを入力",
        en: "Enter Branch ID",
        vi: "Nhập ID chi nhánh",
      },
    },
    organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
    },
    payload: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "確定ペイロード",
        en: "Settled Payload",
        vi: "Dữ liệu đã chốt",
      },
      placeholder: {
        ja: "確定ペイロードを入力",
        en: "Enter Settled Payload",
        vi: "Nhập Dữ liệu đã chốt",
      },
    },
    status: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "状態",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "状態を入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    attempts: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "試行回数",
        en: "Attempts",
        vi: "Số lần thử",
      },
      placeholder: {
        ja: "試行回数を入力",
        en: "Enter Attempts",
        vi: "Nhập Số lần thử",
      },
    },
    last_error: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 1000,
      },
      label: {
        ja: "最終エラー",
        en: "Last Error",
        vi: "Lỗi gần nhất",
      },
      placeholder: {
        ja: "最終エラーを入力",
        en: "Enter Last Error",
        vi: "Nhập Lỗi gần nhất",
      },
    },
    resolution: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 1000,
      },
      label: {
        ja: "解決内容",
        en: "Resolution",
        vi: "Cách xử lý",
      },
      placeholder: {
        ja: "解決内容を入力",
        en: "Enter Resolution",
        vi: "Nhập Cách xử lý",
      },
    },
    claimed_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "取得日時",
        en: "Claimed At",
        vi: "Thời điểm nhận xử lý",
      },
      placeholder: {
        ja: "取得日時を入力",
        en: "Enter Claimed At",
        vi: "Nhập Thời điểm nhận xử lý",
      },
    },
    resolved_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "解決日時",
        en: "Resolved At",
        vi: "Thời điểm xử lý xong",
      },
      placeholder: {
        ja: "解決日時を入力",
        en: "Enter Resolved At",
        vi: "Nhập Thời điểm xử lý xong",
      },
    },
    next_retry_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "次回再試行",
        en: "Next Retry At",
        vi: "Lần thử tiếp theo",
      },
      placeholder: {
        ja: "次回再試行を入力",
        en: "Enter Next Retry At",
        vi: "Nhập Lần thử tiếp theo",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["task_type", "subject_type", "subject_id", "organization_id", "payload", "status", "attempts"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type MoneyReconciliationTaskFieldName = keyof typeof moneyReconciliationTaskMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MoneyReconciliationTask. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MoneyReconciliationTaskCreateFormState {
  task_type: string;
  subject_type: string;
  subject_id: string;
  branch_id?: string | null;
  organization_id: string;
  payload: unknown;
  status: string;
  attempts: number;
  last_error?: string | null;
  resolution?: string | null;
  claimed_at?: string | null;
  resolved_at?: string | null;
  next_retry_at?: string | null;
}

/** Form-state shape for updating a MoneyReconciliationTask. Every field optional. */
export interface MoneyReconciliationTaskUpdateFormState {
  task_type?: string;
  subject_type?: string;
  subject_id?: string;
  branch_id?: string | null;
  organization_id?: string;
  payload?: unknown;
  status?: string;
  attempts?: number;
  last_error?: string | null;
  resolution?: string | null;
  claimed_at?: string | null;
  resolved_at?: string | null;
  next_retry_at?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMoneyReconciliationTaskCreateForm(): MoneyReconciliationTaskCreateFormState {
  return {
    task_type: '',
    subject_type: '',
    subject_id: '',
    branch_id: '',
    organization_id: '',
    payload: null,
    status: "pending",
    attempts: 0,
    last_error: '',
    resolution: '',
    claimed_at: '',
    resolved_at: '',
    next_retry_at: '',
  };
}

/** Convert create form state into the payload that baseMoneyReconciliationTaskCreateSchema accepts. */
export function buildMoneyReconciliationTaskCreatePayload(form: MoneyReconciliationTaskCreateFormState): BaseMoneyReconciliationTaskCreate {
  const payload: Record<string, unknown> = {
    task_type: form.task_type.trim(),
    subject_type: form.subject_type.trim(),
    subject_id: form.subject_id.trim(),
    branch_id: form.branch_id?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
    payload: form.payload,
    status: form.status.trim(),
    attempts: form.attempts,
    last_error: form.last_error?.toString().trim() || null,
    resolution: form.resolution?.toString().trim() || null,
    claimed_at: form.claimed_at,
    resolved_at: form.resolved_at,
    next_retry_at: form.next_retry_at,
  };

  return payload as BaseMoneyReconciliationTaskCreate;
}

/** Convert update form state into the payload that baseMoneyReconciliationTaskUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMoneyReconciliationTaskUpdatePayload(form: MoneyReconciliationTaskUpdateFormState): BaseMoneyReconciliationTaskUpdate {
  const payload: Record<string, unknown> = {};
  if (form.task_type !== undefined) {
    payload.task_type = form.task_type?.toString().trim() ?? null;
  }
  if (form.subject_type !== undefined) {
    payload.subject_type = form.subject_type?.toString().trim() ?? null;
  }
  if (form.subject_id !== undefined) {
    payload.subject_id = form.subject_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.payload !== undefined) {
    payload.payload = form.payload;
  }
  if (form.status !== undefined) {
    payload.status = form.status?.toString().trim() ?? null;
  }
  if (form.attempts !== undefined) {
    payload.attempts = form.attempts;
  }
  if (form.last_error !== undefined) {
    payload.last_error = form.last_error?.toString().trim() ?? null;
  }
  if (form.resolution !== undefined) {
    payload.resolution = form.resolution?.toString().trim() ?? null;
  }
  if (form.claimed_at !== undefined) {
    payload.claimed_at = form.claimed_at;
  }
  if (form.resolved_at !== undefined) {
    payload.resolved_at = form.resolved_at;
  }
  if (form.next_retry_at !== undefined) {
    payload.next_retry_at = form.next_retry_at;
  }
  return payload as BaseMoneyReconciliationTaskUpdate;
}
