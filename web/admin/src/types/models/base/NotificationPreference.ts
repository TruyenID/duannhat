/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { User } from './User';

/**
 * 通知設定
 */
export interface NotificationPreference {
  id: string;
  user_id: string;
  user?: User;
  type: string;
  channel: string;
  enabled: boolean;
  master_mute: boolean;
  quiet_from?: string | null;
  quiet_to?: string | null;
  quiet_timezone?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationPreference
 */
export const notificationPreferenceI18n = {
  label: {"ja":"通知設定","en":"Notification Preference","vi":"Tuỳ chọn thông báo"},
  fields: {
    user: {
      label: {"ja":"ユーザー","en":"User","vi":"Người dùng"},
      placeholder: {"ja":"ユーザーを入力","en":"Enter User","vi":"Nhập Người dùng"},
    },
    type: {
      label: {"ja":"タイプ","en":"Type","vi":"Loại"},
      placeholder: {"ja":"タイプを入力","en":"Enter Type","vi":"Nhập Loại"},
    },
    channel: {
      label: {"ja":"チャンネル","en":"Channel","vi":"Kênh"},
      placeholder: {"ja":"チャンネルを入力","en":"Enter Channel","vi":"Nhập Kênh"},
    },
    enabled: {
      label: {"ja":"有効","en":"Enabled","vi":"Bật"},
      placeholder: {"ja":"有効を入力","en":"Enter Enabled","vi":"Nhập Bật"},
    },
    master_mute: {
      label: {"ja":"マスターミュート","en":"Master Mute","vi":"Tắt tất cả"},
      placeholder: {"ja":"マスターミュートを入力","en":"Enter Master Mute","vi":"Nhập Tắt tất cả"},
    },
    quiet_from: {
      label: {"ja":"静音開始","en":"Quiet From","vi":"Im lặng từ"},
      placeholder: {"ja":"静音開始を入力","en":"Enter Quiet From","vi":"Nhập Im lặng từ"},
    },
    quiet_to: {
      label: {"ja":"静音終了","en":"Quiet To","vi":"Im lặng đến"},
      placeholder: {"ja":"静音終了を入力","en":"Enter Quiet To","vi":"Nhập Im lặng đến"},
    },
    quiet_timezone: {
      label: {"ja":"静音タイムゾーン","en":"Quiet Timezone","vi":"Múi giờ im lặng"},
      placeholder: {"ja":"静音タイムゾーンを入力","en":"Enter Quiet Timezone","vi":"Nhập Múi giờ im lặng"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationPreference */
export const baseNotificationPreferenceSchemas = {
  type: z.string().min(1).max(100),
  channel: z.string().min(1).max(20),
  enabled: z.boolean(),
  master_mute: z.boolean(),
  quiet_from: z.string().max(5).optional().nullable(),
  quiet_to: z.string().max(5).optional().nullable(),
  quiet_timezone: z.string().max(60).optional().nullable(),
} as const;

/** Create schema for NotificationPreference */
export const baseNotificationPreferenceCreateSchema = z.object({
  type: baseNotificationPreferenceSchemas.type,
  channel: baseNotificationPreferenceSchemas.channel,
  enabled: baseNotificationPreferenceSchemas.enabled,
  master_mute: baseNotificationPreferenceSchemas.master_mute,
  quiet_from: baseNotificationPreferenceSchemas.quiet_from,
  quiet_to: baseNotificationPreferenceSchemas.quiet_to,
  quiet_timezone: baseNotificationPreferenceSchemas.quiet_timezone,
});

/** Update schema for NotificationPreference */
export const baseNotificationPreferenceUpdateSchema = baseNotificationPreferenceCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationPreferenceCreate = z.infer<typeof baseNotificationPreferenceCreateSchema>;
export type BaseNotificationPreferenceUpdate = z.infer<typeof baseNotificationPreferenceUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationPreferenceLabel(locale: string): string {
  return notificationPreferenceI18n.label[locale as keyof typeof notificationPreferenceI18n.label] ?? notificationPreferenceI18n.label['en'] ?? 'NotificationPreference';
}

/** Get field label for a specific locale */
export function getNotificationPreferenceFieldLabel(field: string, locale: string): string {
  const fields = notificationPreferenceI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationPreferenceFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationPreferenceI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationPreference.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationPreferenceMetadata = {
  modelName: "NotificationPreference",
  table: "notification_preferences",

  fields: {
    user_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "CASCADE",
      },
      label: {},
    },
    type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "タイプ",
        en: "Type",
        vi: "Loại",
      },
      placeholder: {
        ja: "タイプを入力",
        en: "Enter Type",
        vi: "Nhập Loại",
      },
      description: {
        ja: "通知タイプ文字列、またはマスター設定行の場合は `*` リテラル。",
        en: "Notification type string, or literal `*` for the master-level preference row.",
        vi: "Chuỗi loại thông báo, hoặc `*` cho row master.",
      },
    },
    channel: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "チャンネル",
        en: "Channel",
        vi: "Kênh",
      },
      placeholder: {
        ja: "チャンネルを入力",
        en: "Enter Channel",
        vi: "Nhập Kênh",
      },
      description: {
        ja: "NotificationChannel 値、またはマスター行の場合は `*` リテラル。",
        en: "NotificationChannel case value, or literal `*` for the master-level row.",
        vi: "Giá trị NotificationChannel, hoặc `*` cho row master.",
      },
    },
    enabled: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Enabled",
        vi: "Bật",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Enabled",
        vi: "Nhập Bật",
      },
    },
    master_mute: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "マスターミュート",
        en: "Master Mute",
        vi: "Tắt tất cả",
      },
      placeholder: {
        ja: "マスターミュートを入力",
        en: "Enter Master Mute",
        vi: "Nhập Tắt tất cả",
      },
    },
    quiet_from: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 5,
      },
      label: {
        ja: "静音開始",
        en: "Quiet From",
        vi: "Im lặng từ",
      },
      placeholder: {
        ja: "静音開始を入力",
        en: "Enter Quiet From",
        vi: "Nhập Im lặng từ",
      },
      description: {
        ja: "HH:MM 24時間表記。マスター行のみ設定される。",
        en: "HH:MM 24h local time. Only set on the master-level row.",
        vi: "HH:MM 24h giờ địa phương. Chỉ set trên row master.",
      },
    },
    quiet_to: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 5,
      },
      label: {
        ja: "静音終了",
        en: "Quiet To",
        vi: "Im lặng đến",
      },
      placeholder: {
        ja: "静音終了を入力",
        en: "Enter Quiet To",
        vi: "Nhập Im lặng đến",
      },
      description: {
        ja: "HH:MM 24時間表記。`quiet_to` < `quiet_from` のとき深夜をまたぐ。",
        en: "HH:MM 24h local time. Wraps across midnight when `quiet_to` < `quiet_from`.",
        vi: "HH:MM 24h. Quấn qua nửa đêm khi `quiet_to` < `quiet_from`.",
      },
    },
    quiet_timezone: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 60,
      },
      label: {
        ja: "静音タイムゾーン",
        en: "Quiet Timezone",
        vi: "Múi giờ im lặng",
      },
      placeholder: {
        ja: "静音タイムゾーンを入力",
        en: "Enter Quiet Timezone",
        vi: "Nhập Múi giờ im lặng",
      },
      description: {
        ja: "IANA タイムゾーン ID（例：Asia/Tokyo）。null の場合は user.timezone → Asia/Tokyo にフォールバック。",
        en: "IANA tz id (e.g. Asia/Tokyo). Falls back to user.timezone then Asia/Tokyo when null.",
        vi: "ID IANA tz (VD: Asia/Tokyo). Null → fallback user.timezone → Asia/Tokyo.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["user_id", "type", "channel", "enabled", "master_mute"] as const,
  enumFields: [] as const,
  relationFields: ["user_id"] as const,
} as const;

export type NotificationPreferenceFieldName = keyof typeof notificationPreferenceMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationPreference. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationPreferenceCreateFormState {
  user_id: string;
  type: string;
  channel: string;
  enabled: boolean;
  master_mute: boolean;
  quiet_from?: string | null;
  quiet_to?: string | null;
  quiet_timezone?: string | null;
}

/** Form-state shape for updating a NotificationPreference. Every field optional. */
export interface NotificationPreferenceUpdateFormState {
  user_id?: string;
  type?: string;
  channel?: string;
  enabled?: boolean;
  master_mute?: boolean;
  quiet_from?: string | null;
  quiet_to?: string | null;
  quiet_timezone?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationPreferenceCreateForm(): NotificationPreferenceCreateFormState {
  return {
    user_id: '',
    type: '',
    channel: '',
    enabled: true,
    master_mute: false,
    quiet_from: '',
    quiet_to: '',
    quiet_timezone: '',
  };
}

/** Convert create form state into the payload that baseNotificationPreferenceCreateSchema accepts. */
export function buildNotificationPreferenceCreatePayload(form: NotificationPreferenceCreateFormState): BaseNotificationPreferenceCreate {
  const payload: Record<string, unknown> = {
    user_id: form.user_id.trim(),
    type: form.type.trim(),
    channel: form.channel.trim(),
    enabled: form.enabled,
    master_mute: form.master_mute,
    quiet_from: form.quiet_from?.toString().trim() || null,
    quiet_to: form.quiet_to?.toString().trim() || null,
    quiet_timezone: form.quiet_timezone?.toString().trim() || null,
  };

  return payload as BaseNotificationPreferenceCreate;
}

/** Convert update form state into the payload that baseNotificationPreferenceUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationPreferenceUpdatePayload(form: NotificationPreferenceUpdateFormState): BaseNotificationPreferenceUpdate {
  const payload: Record<string, unknown> = {};
  if (form.user_id !== undefined) {
    payload.user_id = form.user_id?.toString().trim() ?? null;
  }
  if (form.type !== undefined) {
    payload.type = form.type?.toString().trim() ?? null;
  }
  if (form.channel !== undefined) {
    payload.channel = form.channel?.toString().trim() ?? null;
  }
  if (form.enabled !== undefined) {
    payload.enabled = form.enabled;
  }
  if (form.master_mute !== undefined) {
    payload.master_mute = form.master_mute;
  }
  if (form.quiet_from !== undefined) {
    payload.quiet_from = form.quiet_from?.toString().trim() ?? null;
  }
  if (form.quiet_to !== undefined) {
    payload.quiet_to = form.quiet_to?.toString().trim() ?? null;
  }
  if (form.quiet_timezone !== undefined) {
    payload.quiet_timezone = form.quiet_timezone?.toString().trim() ?? null;
  }
  return payload as BaseNotificationPreferenceUpdate;
}
