/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { GatewayPayout } from './GatewayPayout';
import type { OrderPayment } from './OrderPayment';
import type { PaymentAttempt } from './PaymentAttempt';
import type { PaymentGatewayConnection } from './PaymentGatewayConnection';
import type { SettlementReportBatch } from './SettlementReportBatch';

/**
 * PaymentSettlement
 */
export interface PaymentSettlement {
  id: string;
  connection_id: string;
  connection?: PaymentGatewayConnection;
  provider: string;
  kind: string;
  order_payment_id?: string | null;
  orderPayment?: OrderPayment | null;
  payment_attempt_id?: string | null;
  paymentAttempt?: PaymentAttempt | null;
  gross_minor: number;
  fee_minor: number;
  fee_tax_minor: number;
  net_minor: number;
  currency: string;
  source: string;
  external_ref: string;
  report_batch_id?: string | null;
  reportBatch?: SettlementReportBatch | null;
  payout_id?: string | null;
  payout?: GatewayPayout | null;
  provider_settled_at?: DateTimeString | null;
  status: string;
  metadata?: unknown | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PaymentSettlement
 */
export const paymentSettlementI18n = {
  label: {"ja":"PaymentSettlement","en":"PaymentSettlement","vi":"PaymentSettlement"},
  fields: {
    connection: {
      label: {"ja":"接続","en":"Connection","vi":"Kết nối"},
      placeholder: {"ja":"接続を入力","en":"Enter Connection","vi":"Nhập Kết nối"},
    },
    provider: {
      label: {"ja":"プロバイダ","en":"Provider","vi":"Nhà cung cấp"},
      placeholder: {"ja":"プロバイダを入力","en":"Enter Provider","vi":"Nhập Nhà cung cấp"},
    },
    kind: {
      label: {"ja":"種別","en":"Kind","vi":"Loại"},
      placeholder: {"ja":"種別を入力","en":"Enter Kind","vi":"Nhập Loại"},
    },
    order_payment: {
      label: {"ja":"決済","en":"Order Payment","vi":"Khoản thanh toán"},
      placeholder: {"ja":"決済を入力","en":"Enter Order Payment","vi":"Nhập Khoản thanh toán"},
    },
    payment_attempt: {
      label: {"ja":"決済試行","en":"Payment Attempt","vi":"Lượt thanh toán"},
      placeholder: {"ja":"決済試行を入力","en":"Enter Payment Attempt","vi":"Nhập Lượt thanh toán"},
    },
    gross_minor: {
      label: {"ja":"総額（最小単位）","en":"Gross (minor)","vi":"Tổng (đơn vị nhỏ)"},
      placeholder: {"ja":"総額（最小単位）を入力","en":"Enter Gross (minor)","vi":"Nhập Tổng (đơn vị nhỏ)"},
    },
    fee_minor: {
      label: {"ja":"手数料（最小単位）","en":"Fee (minor)","vi":"Phí (đơn vị nhỏ)"},
      placeholder: {"ja":"手数料（最小単位）を入力","en":"Enter Fee (minor)","vi":"Nhập Phí (đơn vị nhỏ)"},
    },
    fee_tax_minor: {
      label: {"ja":"手数料税（最小単位）","en":"Fee Tax (minor)","vi":"Thuế phí (đơn vị nhỏ)"},
      placeholder: {"ja":"手数料税（最小単位）を入力","en":"Enter Fee Tax (minor)","vi":"Nhập Thuế phí (đơn vị nhỏ)"},
    },
    net_minor: {
      label: {"ja":"純額（最小単位）","en":"Net (minor)","vi":"Thực nhận (đơn vị nhỏ)"},
      placeholder: {"ja":"純額（最小単位）を入力","en":"Enter Net (minor)","vi":"Nhập Thực nhận (đơn vị nhỏ)"},
    },
    currency: {
      label: {"ja":"通貨","en":"Currency","vi":"Tiền tệ"},
      placeholder: {"ja":"通貨を入力","en":"Enter Currency","vi":"Nhập Tiền tệ"},
    },
    source: {
      label: {"ja":"取得元","en":"Source","vi":"Nguồn"},
      placeholder: {"ja":"取得元を入力","en":"Enter Source","vi":"Nhập Nguồn"},
    },
    external_ref: {
      label: {"ja":"外部参照","en":"External Reference","vi":"Tham chiếu bên ngoài"},
      placeholder: {"ja":"外部参照を入力","en":"Enter External Reference","vi":"Nhập Tham chiếu bên ngoài"},
    },
    report_batch: {
      label: {"ja":"取込バッチ","en":"Report Batch","vi":"Lô nhập"},
      placeholder: {"ja":"取込バッチを入力","en":"Enter Report Batch","vi":"Nhập Lô nhập"},
    },
    payout: {
      label: {"ja":"入金","en":"Payout","vi":"Đợt chi trả"},
      placeholder: {"ja":"入金を入力","en":"Enter Payout","vi":"Nhập Đợt chi trả"},
    },
    provider_settled_at: {
      label: {"ja":"精算日時","en":"Provider Settled At","vi":"Cổng chốt lúc"},
      placeholder: {"ja":"精算日時を入力","en":"Enter Provider Settled At","vi":"Nhập Cổng chốt lúc"},
    },
    status: {
      label: {"ja":"状態","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"状態を入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    metadata: {
      label: {"ja":"メタデータ","en":"Metadata","vi":"Dữ liệu kèm"},
      placeholder: {"ja":"メタデータを入力","en":"Enter Metadata","vi":"Nhập Dữ liệu kèm"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PaymentSettlement */
export const basePaymentSettlementSchemas = {
  provider: z.string().min(1).max(50),
  kind: z.string().min(1).max(50),
  gross_minor: z.number().int(),
  fee_minor: z.number().int(),
  fee_tax_minor: z.number().int(),
  net_minor: z.number().int(),
  currency: z.string().min(1).max(3),
  source: z.string().min(1).max(50),
  external_ref: z.string().min(1).max(191),
  provider_settled_at: z.string().datetime({ offset: true }).optional().nullable(),
  status: z.string().min(1).max(50),
  metadata: z.unknown().optional().nullable(),
} as const;

/** Create schema for PaymentSettlement */
export const basePaymentSettlementCreateSchema = z.object({
  provider: basePaymentSettlementSchemas.provider,
  kind: basePaymentSettlementSchemas.kind,
  gross_minor: basePaymentSettlementSchemas.gross_minor,
  fee_minor: basePaymentSettlementSchemas.fee_minor,
  fee_tax_minor: basePaymentSettlementSchemas.fee_tax_minor,
  net_minor: basePaymentSettlementSchemas.net_minor,
  currency: basePaymentSettlementSchemas.currency,
  source: basePaymentSettlementSchemas.source,
  external_ref: basePaymentSettlementSchemas.external_ref,
  provider_settled_at: basePaymentSettlementSchemas.provider_settled_at,
  status: basePaymentSettlementSchemas.status,
  metadata: basePaymentSettlementSchemas.metadata,
});

/** Update schema for PaymentSettlement */
export const basePaymentSettlementUpdateSchema = basePaymentSettlementCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePaymentSettlementCreate = z.infer<typeof basePaymentSettlementCreateSchema>;
export type BasePaymentSettlementUpdate = z.infer<typeof basePaymentSettlementUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPaymentSettlementLabel(locale: string): string {
  return paymentSettlementI18n.label[locale as keyof typeof paymentSettlementI18n.label] ?? paymentSettlementI18n.label['en'] ?? 'PaymentSettlement';
}

/** Get field label for a specific locale */
export function getPaymentSettlementFieldLabel(field: string, locale: string): string {
  const fields = paymentSettlementI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPaymentSettlementFieldPlaceholder(field: string, locale: string): string {
  const fields = paymentSettlementI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PaymentSettlement.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const paymentSettlementMetadata = {
  modelName: "PaymentSettlement",
  table: "payment_settlements",

  fields: {
    connection_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "PaymentGatewayConnection",
        table: "payment_gateway_connections",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    provider: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "プロバイダ",
        en: "Provider",
        vi: "Nhà cung cấp",
      },
      placeholder: {
        ja: "プロバイダを入力",
        en: "Enter Provider",
        vi: "Nhập Nhà cung cấp",
      },
    },
    kind: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "種別",
        en: "Kind",
        vi: "Loại",
      },
      placeholder: {
        ja: "種別を入力",
        en: "Enter Kind",
        vi: "Nhập Loại",
      },
    },
    order_payment_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "OrderPayment",
        table: "order_payments",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    payment_attempt_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "PaymentAttempt",
        table: "payment_attempts",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    gross_minor: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "総額（最小単位）",
        en: "Gross (minor)",
        vi: "Tổng (đơn vị nhỏ)",
      },
      placeholder: {
        ja: "総額（最小単位）を入力",
        en: "Enter Gross (minor)",
        vi: "Nhập Tổng (đơn vị nhỏ)",
      },
    },
    fee_minor: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "手数料（最小単位）",
        en: "Fee (minor)",
        vi: "Phí (đơn vị nhỏ)",
      },
      placeholder: {
        ja: "手数料（最小単位）を入力",
        en: "Enter Fee (minor)",
        vi: "Nhập Phí (đơn vị nhỏ)",
      },
    },
    fee_tax_minor: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "手数料税（最小単位）",
        en: "Fee Tax (minor)",
        vi: "Thuế phí (đơn vị nhỏ)",
      },
      placeholder: {
        ja: "手数料税（最小単位）を入力",
        en: "Enter Fee Tax (minor)",
        vi: "Nhập Thuế phí (đơn vị nhỏ)",
      },
    },
    net_minor: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "純額（最小単位）",
        en: "Net (minor)",
        vi: "Thực nhận (đơn vị nhỏ)",
      },
      placeholder: {
        ja: "純額（最小単位）を入力",
        en: "Enter Net (minor)",
        vi: "Nhập Thực nhận (đơn vị nhỏ)",
      },
    },
    currency: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 3,
      },
      label: {
        ja: "通貨",
        en: "Currency",
        vi: "Tiền tệ",
      },
      placeholder: {
        ja: "通貨を入力",
        en: "Enter Currency",
        vi: "Nhập Tiền tệ",
      },
    },
    source: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "取得元",
        en: "Source",
        vi: "Nguồn",
      },
      placeholder: {
        ja: "取得元を入力",
        en: "Enter Source",
        vi: "Nhập Nguồn",
      },
    },
    external_ref: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 191,
      },
      label: {
        ja: "外部参照",
        en: "External Reference",
        vi: "Tham chiếu bên ngoài",
      },
      placeholder: {
        ja: "外部参照を入力",
        en: "Enter External Reference",
        vi: "Nhập Tham chiếu bên ngoài",
      },
    },
    report_batch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "SettlementReportBatch",
        table: "settlement_report_batches",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    payout_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "GatewayPayout",
        table: "gateway_payouts",
        onDelete: "SET NULL",
      },
      label: {},
    },
    provider_settled_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "精算日時",
        en: "Provider Settled At",
        vi: "Cổng chốt lúc",
      },
      placeholder: {
        ja: "精算日時を入力",
        en: "Enter Provider Settled At",
        vi: "Nhập Cổng chốt lúc",
      },
    },
    status: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "状態",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "状態を入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    metadata: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "メタデータ",
        en: "Metadata",
        vi: "Dữ liệu kèm",
      },
      placeholder: {
        ja: "メタデータを入力",
        en: "Enter Metadata",
        vi: "Nhập Dữ liệu kèm",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["connection_id", "provider", "kind", "gross_minor", "fee_minor", "fee_tax_minor", "net_minor", "currency", "source", "external_ref", "status"] as const,
  enumFields: [] as const,
  relationFields: ["connection_id", "order_payment_id", "payment_attempt_id", "report_batch_id", "payout_id"] as const,
} as const;

export type PaymentSettlementFieldName = keyof typeof paymentSettlementMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PaymentSettlement. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PaymentSettlementCreateFormState {
  connection_id: string;
  provider: string;
  kind: string;
  order_payment_id?: string | null;
  payment_attempt_id?: string | null;
  gross_minor: number;
  fee_minor: number;
  fee_tax_minor: number;
  net_minor: number;
  currency: string;
  source: string;
  external_ref: string;
  report_batch_id?: string | null;
  payout_id?: string | null;
  provider_settled_at?: string | null;
  status: string;
  metadata?: unknown | null;
}

/** Form-state shape for updating a PaymentSettlement. Every field optional. */
export interface PaymentSettlementUpdateFormState {
  connection_id?: string;
  provider?: string;
  kind?: string;
  order_payment_id?: string | null;
  payment_attempt_id?: string | null;
  gross_minor?: number;
  fee_minor?: number;
  fee_tax_minor?: number;
  net_minor?: number;
  currency?: string;
  source?: string;
  external_ref?: string;
  report_batch_id?: string | null;
  payout_id?: string | null;
  provider_settled_at?: string | null;
  status?: string;
  metadata?: unknown | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPaymentSettlementCreateForm(): PaymentSettlementCreateFormState {
  return {
    connection_id: '',
    provider: '',
    kind: '',
    order_payment_id: '',
    payment_attempt_id: '',
    gross_minor: 0,
    fee_minor: 0,
    fee_tax_minor: 0,
    net_minor: 0,
    currency: '',
    source: '',
    external_ref: '',
    report_batch_id: '',
    payout_id: '',
    provider_settled_at: '',
    status: "pending_payout",
    metadata: null,
  };
}

/** Convert create form state into the payload that basePaymentSettlementCreateSchema accepts. */
export function buildPaymentSettlementCreatePayload(form: PaymentSettlementCreateFormState): BasePaymentSettlementCreate {
  const payload: Record<string, unknown> = {
    connection_id: form.connection_id.trim(),
    provider: form.provider.trim(),
    kind: form.kind.trim(),
    order_payment_id: form.order_payment_id?.toString().trim() || null,
    payment_attempt_id: form.payment_attempt_id?.toString().trim() || null,
    gross_minor: form.gross_minor,
    fee_minor: form.fee_minor,
    fee_tax_minor: form.fee_tax_minor,
    net_minor: form.net_minor,
    currency: form.currency.trim(),
    source: form.source.trim(),
    external_ref: form.external_ref.trim(),
    report_batch_id: form.report_batch_id?.toString().trim() || null,
    payout_id: form.payout_id?.toString().trim() || null,
    provider_settled_at: form.provider_settled_at,
    status: form.status.trim(),
    metadata: form.metadata,
  };

  return payload as BasePaymentSettlementCreate;
}

/** Convert update form state into the payload that basePaymentSettlementUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPaymentSettlementUpdatePayload(form: PaymentSettlementUpdateFormState): BasePaymentSettlementUpdate {
  const payload: Record<string, unknown> = {};
  if (form.connection_id !== undefined) {
    payload.connection_id = form.connection_id?.toString().trim() ?? null;
  }
  if (form.provider !== undefined) {
    payload.provider = form.provider?.toString().trim() ?? null;
  }
  if (form.kind !== undefined) {
    payload.kind = form.kind?.toString().trim() ?? null;
  }
  if (form.order_payment_id !== undefined) {
    payload.order_payment_id = form.order_payment_id?.toString().trim() ?? null;
  }
  if (form.payment_attempt_id !== undefined) {
    payload.payment_attempt_id = form.payment_attempt_id?.toString().trim() ?? null;
  }
  if (form.gross_minor !== undefined) {
    payload.gross_minor = form.gross_minor;
  }
  if (form.fee_minor !== undefined) {
    payload.fee_minor = form.fee_minor;
  }
  if (form.fee_tax_minor !== undefined) {
    payload.fee_tax_minor = form.fee_tax_minor;
  }
  if (form.net_minor !== undefined) {
    payload.net_minor = form.net_minor;
  }
  if (form.currency !== undefined) {
    payload.currency = form.currency?.toString().trim() ?? null;
  }
  if (form.source !== undefined) {
    payload.source = form.source?.toString().trim() ?? null;
  }
  if (form.external_ref !== undefined) {
    payload.external_ref = form.external_ref?.toString().trim() ?? null;
  }
  if (form.report_batch_id !== undefined) {
    payload.report_batch_id = form.report_batch_id?.toString().trim() ?? null;
  }
  if (form.payout_id !== undefined) {
    payload.payout_id = form.payout_id?.toString().trim() ?? null;
  }
  if (form.provider_settled_at !== undefined) {
    payload.provider_settled_at = form.provider_settled_at;
  }
  if (form.status !== undefined) {
    payload.status = form.status?.toString().trim() ?? null;
  }
  if (form.metadata !== undefined) {
    payload.metadata = form.metadata;
  }
  return payload as BasePaymentSettlementUpdate;
}
