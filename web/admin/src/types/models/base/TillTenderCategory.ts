/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Organization } from './Organization';

/**
 * TillTenderCategory
 */
export interface TillTenderCategory {
  id: string;
  organization_id: string;
  organization?: Organization;
  branch_id?: string | null;
  branch?: Branch | null;
  key: string;
  name: string;
  sort_order: number;
  is_active: boolean;
  is_system: boolean;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TillTenderCategory
 */
export const tillTenderCategoryI18n = {
  label: {"ja":"TillTenderCategory","en":"TillTenderCategory","vi":"TillTenderCategory"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branch: {
      label: {"ja":"支店","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"支店を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    key: {
      label: {"ja":"キー","en":"Key","vi":"Khoá"},
      placeholder: {"ja":"キーを入力","en":"Enter Key","vi":"Nhập Khoá"},
    },
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    sort_order: {
      label: {"ja":"表示順","en":"Sort Order","vi":"Thứ tự"},
      placeholder: {"ja":"表示順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    is_system: {
      label: {"ja":"システム定義","en":"System Defined","vi":"Do hệ thống tạo"},
      placeholder: {"ja":"システム定義を入力","en":"Enter System Defined","vi":"Nhập Do hệ thống tạo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TillTenderCategory */
export const baseTillTenderCategorySchemas = {
  key: z.string().min(1).max(50),
  name: z.string().min(1).max(255),
  sort_order: z.number().int(),
  is_active: z.boolean(),
  is_system: z.boolean(),
} as const;

/** Create schema for TillTenderCategory */
export const baseTillTenderCategoryCreateSchema = z.object({
  key: baseTillTenderCategorySchemas.key,
  name: baseTillTenderCategorySchemas.name,
  sort_order: baseTillTenderCategorySchemas.sort_order,
  is_active: baseTillTenderCategorySchemas.is_active,
  is_system: baseTillTenderCategorySchemas.is_system,
});

/** Update schema for TillTenderCategory */
export const baseTillTenderCategoryUpdateSchema = baseTillTenderCategoryCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTillTenderCategoryCreate = z.infer<typeof baseTillTenderCategoryCreateSchema>;
export type BaseTillTenderCategoryUpdate = z.infer<typeof baseTillTenderCategoryUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTillTenderCategoryLabel(locale: string): string {
  return tillTenderCategoryI18n.label[locale as keyof typeof tillTenderCategoryI18n.label] ?? tillTenderCategoryI18n.label['en'] ?? 'TillTenderCategory';
}

/** Get field label for a specific locale */
export function getTillTenderCategoryFieldLabel(field: string, locale: string): string {
  const fields = tillTenderCategoryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTillTenderCategoryFieldPlaceholder(field: string, locale: string): string {
  const fields = tillTenderCategoryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TillTenderCategory.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tillTenderCategoryMetadata = {
  modelName: "TillTenderCategory",
  table: "till_tender_categories",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "キー",
        en: "Key",
        vi: "Khoá",
      },
      placeholder: {
        ja: "キーを入力",
        en: "Enter Key",
        vi: "Nhập Khoá",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Sort Order",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    is_system: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "システム定義",
        en: "System Defined",
        vi: "Do hệ thống tạo",
      },
      placeholder: {
        ja: "システム定義を入力",
        en: "Enter System Defined",
        vi: "Nhập Do hệ thống tạo",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "key", "name", "sort_order", "is_active", "is_system"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "branch_id"] as const,
} as const;

export type TillTenderCategoryFieldName = keyof typeof tillTenderCategoryMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TillTenderCategory. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TillTenderCategoryCreateFormState {
  organization_id: string;
  branch_id?: string | null;
  key: string;
  name: string;
  sort_order: number;
  is_active: boolean;
  is_system: boolean;
}

/** Form-state shape for updating a TillTenderCategory. Every field optional. */
export interface TillTenderCategoryUpdateFormState {
  organization_id?: string;
  branch_id?: string | null;
  key?: string;
  name?: string;
  sort_order?: number;
  is_active?: boolean;
  is_system?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTillTenderCategoryCreateForm(): TillTenderCategoryCreateFormState {
  return {
    organization_id: '',
    branch_id: '',
    key: '',
    name: '',
    sort_order: 100,
    is_active: true,
    is_system: false,
  };
}

/** Convert create form state into the payload that baseTillTenderCategoryCreateSchema accepts. */
export function buildTillTenderCategoryCreatePayload(form: TillTenderCategoryCreateFormState): BaseTillTenderCategoryCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    branch_id: form.branch_id?.toString().trim() || null,
    key: form.key.trim(),
    name: form.name.trim(),
    sort_order: form.sort_order,
    is_active: form.is_active,
    is_system: form.is_system,
  };

  return payload as BaseTillTenderCategoryCreate;
}

/** Convert update form state into the payload that baseTillTenderCategoryUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTillTenderCategoryUpdatePayload(form: TillTenderCategoryUpdateFormState): BaseTillTenderCategoryUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.key !== undefined) {
    payload.key = form.key?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.is_system !== undefined) {
    payload.is_system = form.is_system;
  }
  return payload as BaseTillTenderCategoryUpdate;
}
