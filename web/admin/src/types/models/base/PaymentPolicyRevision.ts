/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Organization } from './Organization';

/**
 * 決済ポリシーリビジョン
 */
export interface PaymentPolicyRevision {
  id: string;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  branch_id: string;
  branch?: Branch;
  revision: number;
  ownership_revision: string;
  snapshot_hash: string;
  snapshot: unknown;
  effective_option_count: number;
  source: string;
  published_at: DateTimeString;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PaymentPolicyRevision
 */
export const paymentPolicyRevisionI18n = {
  label: {"ja":"決済ポリシーリビジョン","en":"Payment Policy Revision","vi":"Phiên bản chính sách thanh toán"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"店舗","en":"Shop","vi":"Cửa hàng"},
      placeholder: {"ja":"店舗を入力","en":"Enter Shop","vi":"Nhập Cửa hàng"},
    },
    revision: {
      label: {"ja":"リビジョン","en":"Revision","vi":"Phiên bản"},
      placeholder: {"ja":"リビジョンを入力","en":"Enter Revision","vi":"Nhập Phiên bản"},
    },
    ownership_revision: {
      label: {"ja":"所有権リビジョン","en":"Ownership Revision","vi":"Phiên bản quyền sở hữu"},
      placeholder: {"ja":"所有権リビジョンを入力","en":"Enter Ownership Revision","vi":"Nhập Phiên bản quyền sở hữu"},
    },
    snapshot_hash: {
      label: {"ja":"スナップショットハッシュ","en":"Snapshot Hash","vi":"Hash snapshot"},
      placeholder: {"ja":"スナップショットハッシュを入力","en":"Enter Snapshot Hash","vi":"Nhập Hash snapshot"},
    },
    snapshot: {
      label: {"ja":"有効スナップショット","en":"Effective Snapshot","vi":"Snapshot hiệu lực"},
      placeholder: {"ja":"有効スナップショットを入力","en":"Enter Effective Snapshot","vi":"Nhập Snapshot hiệu lực"},
    },
    effective_option_count: {
      label: {"ja":"有効オプション数","en":"Effective Option Count","vi":"Số tuỳ chọn hiệu lực"},
      placeholder: {"ja":"有効オプション数を入力","en":"Enter Effective Option Count","vi":"Nhập Số tuỳ chọn hiệu lực"},
    },
    source: {
      label: {"ja":"公開ソース","en":"Publication Source","vi":"Nguồn phát hành"},
      placeholder: {"ja":"公開ソースを入力","en":"Enter Publication Source","vi":"Nhập Nguồn phát hành"},
    },
    published_at: {
      label: {"ja":"公開日時","en":"Published At","vi":"Phát hành lúc"},
      placeholder: {"ja":"公開日時を入力","en":"Enter Published At","vi":"Nhập Phát hành lúc"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PaymentPolicyRevision */
export const basePaymentPolicyRevisionSchemas = {
  revision: z.number().int().gte(1),
  ownership_revision: z.string().min(1).max(32).regex(/^[0-9]+$/),
  snapshot_hash: z.string().min(1).max(64).regex(/^[a-zA-Z0-9]+$/).regex(/^[^A-Z]*$/),
  snapshot: z.unknown(),
  effective_option_count: z.number().int(),
  source: z.string().min(1).max(80),
  published_at: z.string().datetime({ offset: true }),
} as const;

/** Create schema for PaymentPolicyRevision */
export const basePaymentPolicyRevisionCreateSchema = z.object({
  revision: basePaymentPolicyRevisionSchemas.revision,
  ownership_revision: basePaymentPolicyRevisionSchemas.ownership_revision,
  snapshot_hash: basePaymentPolicyRevisionSchemas.snapshot_hash,
  snapshot: basePaymentPolicyRevisionSchemas.snapshot,
  effective_option_count: basePaymentPolicyRevisionSchemas.effective_option_count,
  source: basePaymentPolicyRevisionSchemas.source,
  published_at: basePaymentPolicyRevisionSchemas.published_at,
});

/** Update schema for PaymentPolicyRevision */
export const basePaymentPolicyRevisionUpdateSchema = basePaymentPolicyRevisionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePaymentPolicyRevisionCreate = z.infer<typeof basePaymentPolicyRevisionCreateSchema>;
export type BasePaymentPolicyRevisionUpdate = z.infer<typeof basePaymentPolicyRevisionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPaymentPolicyRevisionLabel(locale: string): string {
  return paymentPolicyRevisionI18n.label[locale as keyof typeof paymentPolicyRevisionI18n.label] ?? paymentPolicyRevisionI18n.label['en'] ?? 'PaymentPolicyRevision';
}

/** Get field label for a specific locale */
export function getPaymentPolicyRevisionFieldLabel(field: string, locale: string): string {
  const fields = paymentPolicyRevisionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPaymentPolicyRevisionFieldPlaceholder(field: string, locale: string): string {
  const fields = paymentPolicyRevisionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PaymentPolicyRevision.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const paymentPolicyRevisionMetadata = {
  modelName: "PaymentPolicyRevision",
  table: "payment_policy_revisions",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    revision: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        min: 1,
      },
      label: {
        ja: "リビジョン",
        en: "Revision",
        vi: "Phiên bản",
      },
      placeholder: {
        ja: "リビジョンを入力",
        en: "Enter Revision",
        vi: "Nhập Phiên bản",
      },
    },
    ownership_revision: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "所有権リビジョン",
        en: "Ownership Revision",
        vi: "Phiên bản quyền sở hữu",
      },
      placeholder: {
        ja: "所有権リビジョンを入力",
        en: "Enter Ownership Revision",
        vi: "Nhập Phiên bản quyền sở hữu",
      },
    },
    snapshot_hash: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "スナップショットハッシュ",
        en: "Snapshot Hash",
        vi: "Hash snapshot",
      },
      placeholder: {
        ja: "スナップショットハッシュを入力",
        en: "Enter Snapshot Hash",
        vi: "Nhập Hash snapshot",
      },
    },
    snapshot: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効スナップショット",
        en: "Effective Snapshot",
        vi: "Snapshot hiệu lực",
      },
      placeholder: {
        ja: "有効スナップショットを入力",
        en: "Enter Effective Snapshot",
        vi: "Nhập Snapshot hiệu lực",
      },
    },
    effective_option_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効オプション数",
        en: "Effective Option Count",
        vi: "Số tuỳ chọn hiệu lực",
      },
      placeholder: {
        ja: "有効オプション数を入力",
        en: "Enter Effective Option Count",
        vi: "Nhập Số tuỳ chọn hiệu lực",
      },
    },
    source: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 80,
      },
      label: {
        ja: "公開ソース",
        en: "Publication Source",
        vi: "Nguồn phát hành",
      },
      placeholder: {
        ja: "公開ソースを入力",
        en: "Enter Publication Source",
        vi: "Nhập Nguồn phát hành",
      },
    },
    published_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "公開日時",
        en: "Published At",
        vi: "Phát hành lúc",
      },
      placeholder: {
        ja: "公開日時を入力",
        en: "Enter Published At",
        vi: "Nhập Phát hành lúc",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "brand_id", "branch_id", "revision", "ownership_revision", "snapshot_hash", "snapshot", "effective_option_count", "source", "published_at"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "branch_id"] as const,
} as const;

export type PaymentPolicyRevisionFieldName = keyof typeof paymentPolicyRevisionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PaymentPolicyRevision. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PaymentPolicyRevisionCreateFormState {
  organization_id: string;
  brand_id: string;
  branch_id: string;
  revision: number;
  ownership_revision: string;
  snapshot_hash: string;
  snapshot: unknown;
  effective_option_count: number;
  source: string;
  published_at: string;
}

/** Form-state shape for updating a PaymentPolicyRevision. Every field optional. */
export interface PaymentPolicyRevisionUpdateFormState {
  organization_id?: string;
  brand_id?: string;
  branch_id?: string;
  revision?: number;
  ownership_revision?: string;
  snapshot_hash?: string;
  snapshot?: unknown;
  effective_option_count?: number;
  source?: string;
  published_at?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPaymentPolicyRevisionCreateForm(): PaymentPolicyRevisionCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    branch_id: '',
    revision: 0,
    ownership_revision: '',
    snapshot_hash: '',
    snapshot: null,
    effective_option_count: 0,
    source: '',
    published_at: '',
  };
}

/** Convert create form state into the payload that basePaymentPolicyRevisionCreateSchema accepts. */
export function buildPaymentPolicyRevisionCreatePayload(form: PaymentPolicyRevisionCreateFormState): BasePaymentPolicyRevisionCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    branch_id: form.branch_id.trim(),
    revision: form.revision,
    ownership_revision: form.ownership_revision.trim(),
    snapshot_hash: form.snapshot_hash.trim(),
    snapshot: form.snapshot,
    effective_option_count: form.effective_option_count,
    source: form.source.trim(),
    published_at: form.published_at,
  };

  return payload as BasePaymentPolicyRevisionCreate;
}

/** Convert update form state into the payload that basePaymentPolicyRevisionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPaymentPolicyRevisionUpdatePayload(form: PaymentPolicyRevisionUpdateFormState): BasePaymentPolicyRevisionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.revision !== undefined) {
    payload.revision = form.revision;
  }
  if (form.ownership_revision !== undefined) {
    payload.ownership_revision = form.ownership_revision?.toString().trim() ?? null;
  }
  if (form.snapshot_hash !== undefined) {
    payload.snapshot_hash = form.snapshot_hash?.toString().trim() ?? null;
  }
  if (form.snapshot !== undefined) {
    payload.snapshot = form.snapshot;
  }
  if (form.effective_option_count !== undefined) {
    payload.effective_option_count = form.effective_option_count;
  }
  if (form.source !== undefined) {
    payload.source = form.source?.toString().trim() ?? null;
  }
  if (form.published_at !== undefined) {
    payload.published_at = form.published_at;
  }
  return payload as BasePaymentPolicyRevisionUpdate;
}
