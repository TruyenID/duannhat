/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Coupon } from './Coupon';
import type { Customer } from './Customer';
import type { CustomerOrder } from './CustomerOrder';

/**
 * クーポン利用履歴
 */
export interface CouponRedemption {
  id: string;
  discount_applied_amount: number;
  coupon_snapshot: unknown;
  redeemed_at: DateTimeString;
  released_at?: DateTimeString | null;
  redeemed_via: string;
  redeemed_by_user_id?: string | null;
  coupon_id: string;
  coupon?: Coupon;
  customer_order_id: string;
  customerOrder?: CustomerOrder;
  customer_id?: string | null;
  customer?: Customer | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for CouponRedemption
 */
export const couponRedemptionI18n = {
  label: {"ja":"クーポン利用履歴","en":"Coupon Redemption","vi":"Lịch sử dùng Coupon"},
  fields: {
    discount_applied_amount: {
      label: {"ja":"適用額","en":"Discount Applied","vi":"Đã giảm"},
      placeholder: {"ja":"適用額を入力","en":"Enter Discount Applied","vi":"Nhập Đã giảm"},
    },
    coupon_snapshot: {
      label: {"ja":"クーポンスナップショット","en":"Coupon Snapshot","vi":"Bản sao Coupon"},
      placeholder: {"ja":"クーポンスナップショットを入力","en":"Enter Coupon Snapshot","vi":"Nhập Bản sao Coupon"},
    },
    redeemed_at: {
      label: {"ja":"利用日時","en":"Redeemed At","vi":"Thời điểm dùng"},
      placeholder: {"ja":"利用日時を入力","en":"Enter Redeemed At","vi":"Nhập Thời điểm dùng"},
    },
    released_at: {
      label: {"ja":"解放日時","en":"Released At","vi":"Thời điểm hoàn"},
      placeholder: {"ja":"解放日時を入力","en":"Enter Released At","vi":"Nhập Thời điểm hoàn"},
    },
    redeemed_via: {
      label: {"ja":"利用チャネル","en":"Redeemed Via","vi":"Kênh dùng"},
      placeholder: {"ja":"利用チャネルを入力","en":"Enter Redeemed Via","vi":"Nhập Kênh dùng"},
    },
    redeemed_by_user_id: {
      label: {"ja":"利用者","en":"Redeemed By User","vi":"Người dùng"},
      placeholder: {"ja":"利用者を入力","en":"Enter Redeemed By User","vi":"Nhập Người dùng"},
    },
    coupon: {
      label: {"ja":"クーポン","en":"Coupon","vi":"Coupon"},
      placeholder: {"ja":"クーポンを入力","en":"Enter Coupon","vi":"Nhập Coupon"},
    },
    customer_order: {
      label: {"ja":"注文","en":"Customer Order","vi":"Đơn hàng"},
      placeholder: {"ja":"注文を入力","en":"Enter Customer Order","vi":"Nhập Đơn hàng"},
    },
    customer: {
      label: {"ja":"顧客","en":"Customer","vi":"Khách hàng"},
      placeholder: {"ja":"顧客を入力","en":"Enter Customer","vi":"Nhập Khách hàng"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for CouponRedemption */
export const baseCouponRedemptionSchemas = {
  discount_applied_amount: z.number(),
  coupon_snapshot: z.unknown(),
  redeemed_at: z.string().datetime({ offset: true }),
  released_at: z.string().datetime({ offset: true }).optional().nullable(),
  redeemed_via: z.string().min(1).max(20),
  redeemed_by_user_id: z.string().optional().nullable(),
} as const;

/** Create schema for CouponRedemption */
export const baseCouponRedemptionCreateSchema = z.object({
  discount_applied_amount: baseCouponRedemptionSchemas.discount_applied_amount,
  coupon_snapshot: baseCouponRedemptionSchemas.coupon_snapshot,
  redeemed_at: baseCouponRedemptionSchemas.redeemed_at,
  released_at: baseCouponRedemptionSchemas.released_at,
  redeemed_via: baseCouponRedemptionSchemas.redeemed_via,
  redeemed_by_user_id: baseCouponRedemptionSchemas.redeemed_by_user_id,
});

/** Update schema for CouponRedemption */
export const baseCouponRedemptionUpdateSchema = baseCouponRedemptionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseCouponRedemptionCreate = z.infer<typeof baseCouponRedemptionCreateSchema>;
export type BaseCouponRedemptionUpdate = z.infer<typeof baseCouponRedemptionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getCouponRedemptionLabel(locale: string): string {
  return couponRedemptionI18n.label[locale as keyof typeof couponRedemptionI18n.label] ?? couponRedemptionI18n.label['en'] ?? 'CouponRedemption';
}

/** Get field label for a specific locale */
export function getCouponRedemptionFieldLabel(field: string, locale: string): string {
  const fields = couponRedemptionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getCouponRedemptionFieldPlaceholder(field: string, locale: string): string {
  const fields = couponRedemptionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for CouponRedemption.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const couponRedemptionMetadata = {
  modelName: "CouponRedemption",
  table: "coupon_redemptions",

  fields: {
    discount_applied_amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "適用額",
        en: "Discount Applied",
        vi: "Đã giảm",
      },
      placeholder: {
        ja: "適用額を入力",
        en: "Enter Discount Applied",
        vi: "Nhập Đã giảm",
      },
    },
    coupon_snapshot: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "クーポンスナップショット",
        en: "Coupon Snapshot",
        vi: "Bản sao Coupon",
      },
      placeholder: {
        ja: "クーポンスナップショットを入力",
        en: "Enter Coupon Snapshot",
        vi: "Nhập Bản sao Coupon",
      },
    },
    redeemed_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "利用日時",
        en: "Redeemed At",
        vi: "Thời điểm dùng",
      },
      placeholder: {
        ja: "利用日時を入力",
        en: "Enter Redeemed At",
        vi: "Nhập Thời điểm dùng",
      },
    },
    released_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "解放日時",
        en: "Released At",
        vi: "Thời điểm hoàn",
      },
      placeholder: {
        ja: "解放日時を入力",
        en: "Enter Released At",
        vi: "Nhập Thời điểm hoàn",
      },
    },
    redeemed_via: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "利用チャネル",
        en: "Redeemed Via",
        vi: "Kênh dùng",
      },
      placeholder: {
        ja: "利用チャネルを入力",
        en: "Enter Redeemed Via",
        vi: "Nhập Kênh dùng",
      },
    },
    redeemed_by_user_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "利用者",
        en: "Redeemed By User",
        vi: "Người dùng",
      },
      placeholder: {
        ja: "利用者を入力",
        en: "Enter Redeemed By User",
        vi: "Nhập Người dùng",
      },
    },
    coupon_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Coupon",
        table: "coupons",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    customer_order_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrder",
        table: "customer_orders",
        onDelete: "CASCADE",
      },
      label: {},
    },
    customer_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Customer",
        table: "customers",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["discount_applied_amount", "coupon_snapshot", "redeemed_at", "redeemed_via", "coupon_id", "customer_order_id"] as const,
  enumFields: [] as const,
  relationFields: ["coupon_id", "customer_order_id", "customer_id"] as const,
} as const;

export type CouponRedemptionFieldName = keyof typeof couponRedemptionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a CouponRedemption. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface CouponRedemptionCreateFormState {
  discount_applied_amount: number;
  coupon_snapshot: unknown;
  redeemed_at: string;
  released_at?: string | null;
  redeemed_via: string;
  redeemed_by_user_id?: string | null;
  coupon_id: string;
  customer_order_id: string;
  customer_id?: string | null;
}

/** Form-state shape for updating a CouponRedemption. Every field optional. */
export interface CouponRedemptionUpdateFormState {
  discount_applied_amount?: number;
  coupon_snapshot?: unknown;
  redeemed_at?: string;
  released_at?: string | null;
  redeemed_via?: string;
  redeemed_by_user_id?: string | null;
  coupon_id?: string;
  customer_order_id?: string;
  customer_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyCouponRedemptionCreateForm(): CouponRedemptionCreateFormState {
  return {
    discount_applied_amount: 0,
    coupon_snapshot: null,
    redeemed_at: '',
    released_at: '',
    redeemed_via: '',
    redeemed_by_user_id: '',
    coupon_id: '',
    customer_order_id: '',
    customer_id: '',
  };
}

/** Convert create form state into the payload that baseCouponRedemptionCreateSchema accepts. */
export function buildCouponRedemptionCreatePayload(form: CouponRedemptionCreateFormState): BaseCouponRedemptionCreate {
  const payload: Record<string, unknown> = {
    discount_applied_amount: form.discount_applied_amount,
    coupon_snapshot: form.coupon_snapshot,
    redeemed_at: form.redeemed_at,
    released_at: form.released_at,
    redeemed_via: form.redeemed_via.trim(),
    redeemed_by_user_id: form.redeemed_by_user_id?.toString().trim() || null,
    coupon_id: form.coupon_id.trim(),
    customer_order_id: form.customer_order_id.trim(),
    customer_id: form.customer_id?.toString().trim() || null,
  };

  return payload as BaseCouponRedemptionCreate;
}

/** Convert update form state into the payload that baseCouponRedemptionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildCouponRedemptionUpdatePayload(form: CouponRedemptionUpdateFormState): BaseCouponRedemptionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.discount_applied_amount !== undefined) {
    payload.discount_applied_amount = form.discount_applied_amount;
  }
  if (form.coupon_snapshot !== undefined) {
    payload.coupon_snapshot = form.coupon_snapshot;
  }
  if (form.redeemed_at !== undefined) {
    payload.redeemed_at = form.redeemed_at;
  }
  if (form.released_at !== undefined) {
    payload.released_at = form.released_at;
  }
  if (form.redeemed_via !== undefined) {
    payload.redeemed_via = form.redeemed_via?.toString().trim() ?? null;
  }
  if (form.redeemed_by_user_id !== undefined) {
    payload.redeemed_by_user_id = form.redeemed_by_user_id?.toString().trim() ?? null;
  }
  if (form.coupon_id !== undefined) {
    payload.coupon_id = form.coupon_id?.toString().trim() ?? null;
  }
  if (form.customer_order_id !== undefined) {
    payload.customer_order_id = form.customer_order_id?.toString().trim() ?? null;
  }
  if (form.customer_id !== undefined) {
    payload.customer_id = form.customer_id?.toString().trim() ?? null;
  }
  return payload as BaseCouponRedemptionUpdate;
}
