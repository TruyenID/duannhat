/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { TableTemplate } from './TableTemplate';

/**
 * エリアテンプレート
 */
export interface ZoneTemplate {
  id: string;
  code: string;
  name: string;
  description?: string | null;
  display_order: number;
  is_active: boolean;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  branch_id?: string | null;
  branch?: Branch | null;
  tableTemplates?: TableTemplate[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ZoneTemplate
 */
export const zoneTemplateI18n = {
  label: {"ja":"エリアテンプレート","en":"Zone Template","vi":"Mẫu khu vực"},
  fields: {
    code: {
      label: {"ja":"エリアコード","en":"Zone Code","vi":"Mã khu vực"},
      placeholder: {"ja":"エリアコードを入力","en":"Enter Zone Code","vi":"Nhập Mã khu vực"},
    },
    name: {
      label: {"ja":"エリア名","en":"Zone Name","vi":"Tên khu vực"},
      placeholder: {"ja":"エリア名を入力","en":"Enter Zone Name","vi":"Nhập Tên khu vực"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    display_order: {
      label: {"ja":"表示順","en":"Display Order","vi":"Thứ tự hiển thị"},
      placeholder: {"ja":"表示順を入力","en":"Enter Display Order","vi":"Nhập Thứ tự hiển thị"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"対象店舗","en":"Target Branch","vi":"Chi nhánh áp dụng"},
      placeholder: {"ja":"対象店舗を入力","en":"Enter Target Branch","vi":"Nhập Chi nhánh áp dụng"},
    },
    table_templates: {
      label: {"ja":"テーブルテンプレート","en":"Table Templates","vi":"Mẫu bàn"},
      placeholder: {"ja":"テーブルテンプレートを入力","en":"Enter Table Templates","vi":"Nhập Mẫu bàn"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ZoneTemplate */
export const baseZoneTemplateSchemas = {
  code: z.string().min(1).max(50),
  name: z.string().min(1).max(255),
  description: z.string().optional().nullable(),
  display_order: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for ZoneTemplate */
export const baseZoneTemplateCreateSchema = z.object({
  code: baseZoneTemplateSchemas.code,
  name: baseZoneTemplateSchemas.name,
  description: baseZoneTemplateSchemas.description,
  display_order: baseZoneTemplateSchemas.display_order,
  is_active: baseZoneTemplateSchemas.is_active,
});

/** Update schema for ZoneTemplate */
export const baseZoneTemplateUpdateSchema = baseZoneTemplateCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseZoneTemplateCreate = z.infer<typeof baseZoneTemplateCreateSchema>;
export type BaseZoneTemplateUpdate = z.infer<typeof baseZoneTemplateUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getZoneTemplateLabel(locale: string): string {
  return zoneTemplateI18n.label[locale as keyof typeof zoneTemplateI18n.label] ?? zoneTemplateI18n.label['en'] ?? 'ZoneTemplate';
}

/** Get field label for a specific locale */
export function getZoneTemplateFieldLabel(field: string, locale: string): string {
  const fields = zoneTemplateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getZoneTemplateFieldPlaceholder(field: string, locale: string): string {
  const fields = zoneTemplateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ZoneTemplate.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const zoneTemplateMetadata = {
  modelName: "ZoneTemplate",
  table: "zone_templates",

  fields: {
    code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "エリアコード",
        en: "Zone Code",
        vi: "Mã khu vực",
      },
      placeholder: {
        ja: "エリアコードを入力",
        en: "Enter Zone Code",
        vi: "Nhập Mã khu vực",
      },
      description: {
        ja: "ブランド内で一意のコード（BR-ZT01）。英数字とハイフンのみ。既定適用時に店舗エリアへコピー。",
        en: "Unique code per brand (BR-ZT01). Alphanumeric and hyphens only. Copied to the shop zone on apply-defaults.",
        vi: "Mã unique trong brand (BR-ZT01). Chỉ chữ, số, gạch ngang. Copy sang zone của shop khi nhận mặc định.",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "エリア名",
        en: "Zone Name",
        vi: "Tên khu vực",
      },
      placeholder: {
        ja: "エリア名を入力",
        en: "Enter Zone Name",
        vi: "Nhập Tên khu vực",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    display_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Display Order",
        vi: "Thứ tự hiển thị",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Display Order",
        vi: "Nhập Thứ tự hiển thị",
      },
      description: {
        ja: "数値が小さいほど先頭に表示。",
        en: "Lower number appears first in the template grid.",
        vi: "Số nhỏ hơn hiển thị trước.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
      description: {
        ja: "無効なテンプレートは既定適用時にスキップされます。",
        en: "Inactive templates are skipped when a shop applies brand defaults.",
        vi: "Template vô hiệu sẽ bị bỏ qua khi shop nhận mặc định.",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["code", "name", "display_order", "is_active", "organization_id", "brand_id"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "branch_id"] as const,
} as const;

export type ZoneTemplateFieldName = keyof typeof zoneTemplateMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ZoneTemplate. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ZoneTemplateCreateFormState {
  code: string;
  name: string;
  description?: string | null;
  display_order: number;
  is_active: boolean;
  organization_id: string;
  brand_id: string;
  branch_id?: string | null;
}

/** Form-state shape for updating a ZoneTemplate. Every field optional. */
export interface ZoneTemplateUpdateFormState {
  code?: string;
  name?: string;
  description?: string | null;
  display_order?: number;
  is_active?: boolean;
  organization_id?: string;
  brand_id?: string;
  branch_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyZoneTemplateCreateForm(): ZoneTemplateCreateFormState {
  return {
    code: '',
    name: '',
    description: '',
    display_order: 0,
    is_active: true,
    organization_id: '',
    brand_id: '',
    branch_id: '',
  };
}

/** Convert create form state into the payload that baseZoneTemplateCreateSchema accepts. */
export function buildZoneTemplateCreatePayload(form: ZoneTemplateCreateFormState): BaseZoneTemplateCreate {
  const payload: Record<string, unknown> = {
    code: form.code.trim(),
    name: form.name.trim(),
    description: form.description?.toString().trim() || null,
    display_order: form.display_order,
    is_active: form.is_active,
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    branch_id: form.branch_id?.toString().trim() || null,
  };

  return payload as BaseZoneTemplateCreate;
}

/** Convert update form state into the payload that baseZoneTemplateUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildZoneTemplateUpdatePayload(form: ZoneTemplateUpdateFormState): BaseZoneTemplateUpdate {
  const payload: Record<string, unknown> = {};
  if (form.code !== undefined) {
    payload.code = form.code?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.display_order !== undefined) {
    payload.display_order = form.display_order;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  return payload as BaseZoneTemplateUpdate;
}
