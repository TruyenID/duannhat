/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 繰り返し種別
 */
export enum MenuScheduleRecurrence {
  Weekly = 'Weekly',
  Monthly = 'Monthly',
  Specificdates = 'SpecificDates',
}

/** All MenuScheduleRecurrence values */
export const MenuScheduleRecurrenceValues = Object.values(MenuScheduleRecurrence) as MenuScheduleRecurrence[];

/** Type guard for MenuScheduleRecurrence */
export function isMenuScheduleRecurrence(value: unknown): value is MenuScheduleRecurrence {
  return MenuScheduleRecurrenceValues.includes(value as MenuScheduleRecurrence);
}

const menuScheduleRecurrenceLabels: Partial<Record<MenuScheduleRecurrence, Record<string, string>>> = {
  [MenuScheduleRecurrence.Weekly]: { 'en': 'Weekly (by weekday)', 'ja': '毎週（曜日指定）', 'vi': 'Hàng tuần (theo thứ)' },
  [MenuScheduleRecurrence.Monthly]: { 'en': 'Monthly (by day of month)', 'ja': '毎月（日付指定）', 'vi': 'Hàng tháng (theo ngày)' },
  [MenuScheduleRecurrence.Specificdates]: { 'en': 'Specific dates only', 'ja': '特定日のみ', 'vi': 'Ngày cụ thể' },
};

/** Get label for MenuScheduleRecurrence value with locale support */
export function getMenuScheduleRecurrenceLabel(value: MenuScheduleRecurrence, locale?: string): string {
  const labels = menuScheduleRecurrenceLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for MenuScheduleRecurrence value */
export function getMenuScheduleRecurrenceExtra(_value: MenuScheduleRecurrence): Record<string, unknown> | undefined {
  return undefined;
}
