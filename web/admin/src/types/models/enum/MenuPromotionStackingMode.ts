/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * スタッキングモード
 */
export enum MenuPromotionStackingMode {
  ExclusiveWithCoupons = 'exclusive_with_coupons',
  StackableWithCoupons = 'stackable_with_coupons',
}

/** All MenuPromotionStackingMode values */
export const MenuPromotionStackingModeValues = Object.values(MenuPromotionStackingMode) as MenuPromotionStackingMode[];

/** Type guard for MenuPromotionStackingMode */
export function isMenuPromotionStackingMode(value: unknown): value is MenuPromotionStackingMode {
  return MenuPromotionStackingModeValues.includes(value as MenuPromotionStackingMode);
}

const menuPromotionStackingModeLabels: Partial<Record<MenuPromotionStackingMode, Record<string, string>>> = {
  [MenuPromotionStackingMode.ExclusiveWithCoupons]: { 'en': 'Exclusive with Coupons', 'ja': 'クーポンと併用不可', 'vi': 'Không cộng dồn coupon' },
  [MenuPromotionStackingMode.StackableWithCoupons]: { 'en': 'Stackable with Coupons', 'ja': 'クーポンと併用可能', 'vi': 'Cộng dồn được coupon' },
};

/** Get label for MenuPromotionStackingMode value with locale support */
export function getMenuPromotionStackingModeLabel(value: MenuPromotionStackingMode, locale?: string): string {
  const labels = menuPromotionStackingModeLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for MenuPromotionStackingMode value */
export function getMenuPromotionStackingModeExtra(_value: MenuPromotionStackingMode): Record<string, unknown> | undefined {
  return undefined;
}
