-- Code generated by omnify. DO NOT EDIT.

CREATE TABLE IF NOT EXISTS product_skus (
    id TEXT PRIMARY KEY,
    product_id TEXT NOT NULL,
    sku TEXT,
    name TEXT,
    option_value1_id INTEGER,
    option_value2_id INTEGER,
    option_value3_id INTEGER,
    option_signature TEXT NOT NULL DEFAULT '',
    recipe_id INTEGER,
    recipe_multiplier REAL NOT NULL DEFAULT 1,
    cost_price REAL NOT NULL DEFAULT 0,
    cost_price_auto REAL NOT NULL DEFAULT 0,
    is_cost_override INTEGER NOT NULL DEFAULT 0,
    selling_price REAL NOT NULL DEFAULT 0,
    is_active INTEGER NOT NULL DEFAULT 1,
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at TEXT NOT NULL DEFAULT (datetime('now')),
    deleted_at TEXT
);

CREATE INDEX IF NOT EXISTS idx_product_skus_product_id ON product_skus(product_id);

CREATE INDEX IF NOT EXISTS idx_product_skus_option_value1_id ON product_skus(option_value1_id);

CREATE INDEX IF NOT EXISTS idx_product_skus_option_value2_id ON product_skus(option_value2_id);

CREATE INDEX IF NOT EXISTS idx_product_skus_option_value3_id ON product_skus(option_value3_id);

CREATE INDEX IF NOT EXISTS idx_product_skus_recipe_id ON product_skus(recipe_id);
