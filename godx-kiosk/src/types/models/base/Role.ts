/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Permission } from './Permission';

/**
 * ロール
 */
export interface Role {
  id: string;
  console_organization_id?: string;
  name: string;
  slug: string;
  description?: string;
  level: number;
  permissions?: Permission[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Role
 */
export const roleI18n = {
  label: {"ja":"ロール","en":"Role","vi":"Vai trò"},
  fields: {
    console_organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
    name: {
      label: {"ja":"ロール名","en":"Role Name","vi":"Tên vai trò"},
      placeholder: {"ja":"ロール名を入力","en":"Enter Role Name","vi":"Nhập Tên vai trò"},
    },
    slug: {
      label: {"ja":"スラッグ","en":"Slug","vi":"Slug"},
      placeholder: {"ja":"スラッグを入力","en":"Enter Slug","vi":"Nhập Slug"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    level: {
      label: {"ja":"レベル","en":"Level","vi":"Cấp độ"},
      placeholder: {"ja":"レベルを入力","en":"Enter Level","vi":"Nhập Cấp độ"},
    },
    permissions: {
      label: {"ja":"権限","en":"Permissions","vi":"Quyền hạn"},
      placeholder: {"ja":"権限を入力","en":"Enter Permissions","vi":"Nhập Quyền hạn"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Role */
export const baseRoleSchemas = {
  console_organization_id: z.string().max(36).optional().nullable(),
  name: z.string().min(1).max(100),
  slug: z.string().min(1).max(100),
  description: z.string().optional().nullable(),
  level: z.number().int(),
} as const;

/** Create schema for Role */
export const baseRoleCreateSchema = z.object({
  console_organization_id: baseRoleSchemas.console_organization_id,
  name: baseRoleSchemas.name,
  slug: baseRoleSchemas.slug,
  description: baseRoleSchemas.description,
  level: baseRoleSchemas.level,
});

/** Update schema for Role */
export const baseRoleUpdateSchema = baseRoleCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseRoleCreate = z.infer<typeof baseRoleCreateSchema>;
export type BaseRoleUpdate = z.infer<typeof baseRoleUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getRoleLabel(locale: string): string {
  return roleI18n.label[locale as keyof typeof roleI18n.label] ?? roleI18n.label['en'] ?? 'Role';
}

/** Get field label for a specific locale */
export function getRoleFieldLabel(field: string, locale: string): string {
  const fieldI18n = roleI18n.fields[field as keyof typeof roleI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getRoleFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = roleI18n.fields[field as keyof typeof roleI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Role.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const roleMetadata = {
  modelName: "Role",
  table: "roles",

  fields: {
    console_organization_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 36,
      },
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "ロール名",
        en: "Role Name",
        vi: "Tên vai trò",
      },
      placeholder: {
        ja: "ロール名を入力",
        en: "Enter Role Name",
        vi: "Nhập Tên vai trò",
      },
    },
    slug: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "スラッグ",
        en: "Slug",
        vi: "Slug",
      },
      placeholder: {
        ja: "スラッグを入力",
        en: "Enter Slug",
        vi: "Nhập Slug",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    level: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "レベル",
        en: "Level",
        vi: "Cấp độ",
      },
      placeholder: {
        ja: "レベルを入力",
        en: "Enter Level",
        vi: "Nhập Cấp độ",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["name", "slug", "level"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type RoleFieldName = keyof typeof roleMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Role. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface RoleCreateFormState {
  console_organization_id?: string | null;
  name: string;
  slug: string;
  description?: string | null;
  level: number;
}

/** Form-state shape for updating a Role. Every field optional. */
export interface RoleUpdateFormState {
  console_organization_id?: string | null;
  name?: string;
  slug?: string;
  description?: string | null;
  level?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyRoleCreateForm(): RoleCreateFormState {
  return {
    console_organization_id: '',
    name: '',
    slug: '',
    description: '',
    level: 0,
  };
}

/** Convert create form state into the payload that baseRoleCreateSchema accepts. */
export function buildRoleCreatePayload(form: RoleCreateFormState): BaseRoleCreate {
  const payload: Record<string, unknown> = {
    console_organization_id: form.console_organization_id?.toString().trim() || null,
    name: form.name.trim(),
    slug: form.slug.trim(),
    description: form.description?.toString().trim() || null,
    level: form.level,
  };

  return payload as BaseRoleCreate;
}

/** Convert update form state into the payload that baseRoleUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildRoleUpdatePayload(form: RoleUpdateFormState): BaseRoleUpdate {
  const payload: Record<string, unknown> = {};
  if (form.console_organization_id !== undefined) {
    payload.console_organization_id = form.console_organization_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.slug !== undefined) {
    payload.slug = form.slug?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.level !== undefined) {
    payload.level = form.level;
  }
  return payload as BaseRoleUpdate;
}
