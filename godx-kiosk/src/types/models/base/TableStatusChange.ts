/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { TableStatus } from '../enum/TableStatus';
import type { Table } from './Table';

/**
 * テーブル状態変更
 */
export interface TableStatusChange {
  id: string;
  from_status?: TableStatus;
  to_status: TableStatus;
  changed_by_id: string;
  changed_at: DateTimeString;
  note?: string;
  table_id: string;
  table?: Table;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TableStatusChange
 */
export const tableStatusChangeI18n = {
  label: {"ja":"テーブル状態変更","en":"Table Status Change","vi":"Thay đổi trạng thái bàn"},
  fields: {
    from_status: {
      label: {"ja":"変更前","en":"From Status","vi":"Trạng thái cũ"},
      placeholder: {"ja":"変更前を入力","en":"Enter From Status","vi":"Nhập Trạng thái cũ"},
    },
    to_status: {
      label: {"ja":"変更後","en":"To Status","vi":"Trạng thái mới"},
      placeholder: {"ja":"変更後を入力","en":"Enter To Status","vi":"Nhập Trạng thái mới"},
    },
    changed_by_id: {
      label: {"ja":"変更者ID","en":"Changed By ID","vi":"ID người đổi"},
      placeholder: {"ja":"変更者IDを入力","en":"Enter Changed By ID","vi":"Nhập ID người đổi"},
    },
    changed_at: {
      label: {"ja":"変更日時","en":"Changed At","vi":"Thời điểm đổi"},
      placeholder: {"ja":"変更日時を入力","en":"Enter Changed At","vi":"Nhập Thời điểm đổi"},
    },
    note: {
      label: {"ja":"メモ","en":"Note","vi":"Ghi chú"},
      placeholder: {"ja":"メモを入力","en":"Enter Note","vi":"Nhập Ghi chú"},
    },
    table: {
      label: {"ja":"テーブル","en":"Table","vi":"Bàn"},
      placeholder: {"ja":"テーブルを入力","en":"Enter Table","vi":"Nhập Bàn"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TableStatusChange */
export const baseTableStatusChangeSchemas = {
  from_status: z.nativeEnum(TableStatus).optional().nullable(),
  to_status: z.nativeEnum(TableStatus),
  changed_by_id: z.string(),
  changed_at: z.string().datetime({ offset: true }),
  note: z.string().optional().nullable(),
} as const;

/** Create schema for TableStatusChange */
export const baseTableStatusChangeCreateSchema = z.object({
  from_status: baseTableStatusChangeSchemas.from_status,
  to_status: baseTableStatusChangeSchemas.to_status,
  changed_by_id: baseTableStatusChangeSchemas.changed_by_id,
  changed_at: baseTableStatusChangeSchemas.changed_at,
  note: baseTableStatusChangeSchemas.note,
});

/** Update schema for TableStatusChange */
export const baseTableStatusChangeUpdateSchema = baseTableStatusChangeCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTableStatusChangeCreate = z.infer<typeof baseTableStatusChangeCreateSchema>;
export type BaseTableStatusChangeUpdate = z.infer<typeof baseTableStatusChangeUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTableStatusChangeLabel(locale: string): string {
  return tableStatusChangeI18n.label[locale as keyof typeof tableStatusChangeI18n.label] ?? tableStatusChangeI18n.label['en'] ?? 'TableStatusChange';
}

/** Get field label for a specific locale */
export function getTableStatusChangeFieldLabel(field: string, locale: string): string {
  const fieldI18n = tableStatusChangeI18n.fields[field as keyof typeof tableStatusChangeI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTableStatusChangeFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = tableStatusChangeI18n.fields[field as keyof typeof tableStatusChangeI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TableStatusChange.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tableStatusChangeMetadata = {
  modelName: "TableStatusChange",
  table: "table_status_changes",

  fields: {
    from_status: {
      type: "enum",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      enum: "TableStatus",
      enumValues: ["free", "occupied", "reserved", "cleaning", "out_of_service"] as const,
      label: {
        ja: "変更前",
        en: "From Status",
        vi: "Trạng thái cũ",
      },
      placeholder: {
        ja: "変更前を入力",
        en: "Enter From Status",
        vi: "Nhập Trạng thái cũ",
      },
      description: {
        ja: "前の状態。最初の遷移ではnull可（BR-TSC02）。",
        en: "Previous status. Null on the very first transition (BR-TSC02).",
        vi: "Trạng thái trước. Có thể null ở lần đổi đầu tiên (BR-TSC02).",
      },
    },
    to_status: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "TableStatus",
      enumValues: ["free", "occupied", "reserved", "cleaning", "out_of_service"] as const,
      label: {
        ja: "変更後",
        en: "To Status",
        vi: "Trạng thái mới",
      },
      placeholder: {
        ja: "変更後を入力",
        en: "Enter To Status",
        vi: "Nhập Trạng thái mới",
      },
    },
    changed_by_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "変更者ID",
        en: "Changed By ID",
        vi: "ID người đổi",
      },
      placeholder: {
        ja: "変更者IDを入力",
        en: "Enter Changed By ID",
        vi: "Nhập ID người đổi",
      },
      description: {
        ja: "user_caches.idを参照。user_cachesはSSOキャッシュのためハードFKなし（BR-TSC03）。",
        en: "References user_caches.id. No hard FK because user_caches is an SSO cache (BR-TSC03).",
        vi: "Tham chiếu user_caches.id. Không có FK cứng vì user_caches là cache SSO (BR-TSC03).",
      },
    },
    changed_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "変更日時",
        en: "Changed At",
        vi: "Thời điểm đổi",
      },
      placeholder: {
        ja: "変更日時を入力",
        en: "Enter Changed At",
        vi: "Nhập Thời điểm đổi",
      },
    },
    note: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "メモ",
        en: "Note",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "メモを入力",
        en: "Enter Note",
        vi: "Nhập Ghi chú",
      },
    },
    table_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Table",
        table: "tables",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["to_status", "changed_by_id", "changed_at", "table_id"] as const,
  enumFields: ["from_status", "to_status"] as const,
  relationFields: ["table_id"] as const,
} as const;

export type TableStatusChangeFieldName = keyof typeof tableStatusChangeMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TableStatusChange. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TableStatusChangeCreateFormState {
  from_status?: TableStatus | null;
  to_status: TableStatus;
  changed_by_id: string;
  changed_at: string;
  note?: string | null;
  table_id: string;
}

/** Form-state shape for updating a TableStatusChange. Every field optional. */
export interface TableStatusChangeUpdateFormState {
  from_status?: TableStatus | null;
  to_status?: TableStatus;
  changed_by_id?: string;
  changed_at?: string;
  note?: string | null;
  table_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTableStatusChangeCreateForm(): TableStatusChangeCreateFormState {
  return {
    to_status: TableStatus.Free,
    changed_by_id: '',
    changed_at: '',
    note: '',
    table_id: '',
  };
}

/** Convert create form state into the payload that baseTableStatusChangeCreateSchema accepts. */
export function buildTableStatusChangeCreatePayload(form: TableStatusChangeCreateFormState): BaseTableStatusChangeCreate {
  const payload: Record<string, unknown> = {
    from_status: form.from_status,
    to_status: form.to_status,
    changed_by_id: form.changed_by_id.trim(),
    changed_at: form.changed_at,
    note: form.note?.toString().trim() || null,
    table_id: form.table_id.trim(),
  };

  return payload as BaseTableStatusChangeCreate;
}

/** Convert update form state into the payload that baseTableStatusChangeUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTableStatusChangeUpdatePayload(form: TableStatusChangeUpdateFormState): BaseTableStatusChangeUpdate {
  const payload: Record<string, unknown> = {};
  if (form.from_status !== undefined) {
    payload.from_status = form.from_status;
  }
  if (form.to_status !== undefined) {
    payload.to_status = form.to_status;
  }
  if (form.changed_by_id !== undefined) {
    payload.changed_by_id = form.changed_by_id?.toString().trim() ?? null;
  }
  if (form.changed_at !== undefined) {
    payload.changed_at = form.changed_at;
  }
  if (form.note !== undefined) {
    payload.note = form.note?.toString().trim() ?? null;
  }
  if (form.table_id !== undefined) {
    payload.table_id = form.table_id?.toString().trim() ?? null;
  }
  return payload as BaseTableStatusChangeUpdate;
}
