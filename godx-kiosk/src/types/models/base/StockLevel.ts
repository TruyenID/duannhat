/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Material } from './Material';
import type { ProductSku } from './ProductSku';
import type { Warehouse } from './Warehouse';

/**
 * 在庫レベル
 */
export interface StockLevel {
  id: string;
  warehouse_id: string;
  warehouse?: Warehouse;
  productSku_id?: string;
  productSku?: ProductSku | null;
  material_id?: string;
  material?: Material | null;
  quantity: number;
  unit?: string;
  min_stock?: number;
  max_stock?: number;
  alert_enabled: boolean;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for StockLevel
 */
export const stockLevelI18n = {
  label: {"ja":"在庫レベル","en":"Stock Level","vi":"Tồn kho"},
  fields: {
    warehouse: {
      label: {"ja":"倉庫","en":"Warehouse","vi":"Kho hàng"},
      placeholder: {"ja":"倉庫を入力","en":"Enter Warehouse","vi":"Nhập Kho hàng"},
    },
    product_sku: {
      label: {"ja":"商品バリエーション","en":"Product Variant","vi":"Biến thể sản phẩm"},
      placeholder: {"ja":"商品バリエーションを入力","en":"Enter Product Variant","vi":"Nhập Biến thể sản phẩm"},
    },
    material: {
      label: {"ja":"材料","en":"Material","vi":"Nguyên liệu"},
      placeholder: {"ja":"材料を入力","en":"Enter Material","vi":"Nhập Nguyên liệu"},
    },
    quantity: {
      label: {"ja":"数量","en":"Quantity","vi":"Số lượng"},
      placeholder: {"ja":"数量を入力","en":"Enter Quantity","vi":"Nhập Số lượng"},
    },
    unit: {
      label: {"ja":"単位","en":"Unit","vi":"Đơn vị"},
      placeholder: {"ja":"単位を入力","en":"Enter Unit","vi":"Nhập Đơn vị"},
    },
    min_stock: {
      label: {"ja":"最低在庫","en":"Minimum Stock","vi":"Tồn kho tối thiểu"},
      placeholder: {"ja":"最低在庫を入力","en":"Enter Minimum Stock","vi":"Nhập Tồn kho tối thiểu"},
    },
    max_stock: {
      label: {"ja":"最大在庫","en":"Maximum Stock","vi":"Tồn kho tối đa"},
      placeholder: {"ja":"最大在庫を入力","en":"Enter Maximum Stock","vi":"Nhập Tồn kho tối đa"},
    },
    alert_enabled: {
      label: {"ja":"アラート有効","en":"Alert Enabled","vi":"Bật cảnh báo"},
      placeholder: {"ja":"アラート有効を入力","en":"Enter Alert Enabled","vi":"Nhập Bật cảnh báo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for StockLevel */
export const baseStockLevelSchemas = {
  quantity: z.number(),
  unit: z.string().max(50).optional().nullable(),
  min_stock: z.number().optional().nullable(),
  max_stock: z.number().optional().nullable(),
  alert_enabled: z.boolean(),
} as const;

/** Create schema for StockLevel */
export const baseStockLevelCreateSchema = z.object({
  quantity: baseStockLevelSchemas.quantity,
  unit: baseStockLevelSchemas.unit,
  min_stock: baseStockLevelSchemas.min_stock,
  max_stock: baseStockLevelSchemas.max_stock,
  alert_enabled: baseStockLevelSchemas.alert_enabled,
});

/** Update schema for StockLevel */
export const baseStockLevelUpdateSchema = baseStockLevelCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseStockLevelCreate = z.infer<typeof baseStockLevelCreateSchema>;
export type BaseStockLevelUpdate = z.infer<typeof baseStockLevelUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getStockLevelLabel(locale: string): string {
  return stockLevelI18n.label[locale as keyof typeof stockLevelI18n.label] ?? stockLevelI18n.label['en'] ?? 'StockLevel';
}

/** Get field label for a specific locale */
export function getStockLevelFieldLabel(field: string, locale: string): string {
  const fieldI18n = stockLevelI18n.fields[field as keyof typeof stockLevelI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getStockLevelFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = stockLevelI18n.fields[field as keyof typeof stockLevelI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for StockLevel.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const stockLevelMetadata = {
  modelName: "StockLevel",
  table: "stock_levels",

  fields: {
    warehouse_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Warehouse",
        table: "warehouses",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    material_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Material",
        table: "materials",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "数量",
        en: "Quantity",
        vi: "Số lượng",
      },
      placeholder: {
        ja: "数量を入力",
        en: "Enter Quantity",
        vi: "Nhập Số lượng",
      },
      description: {
        ja: "基本単位での現在の在庫数量。マイナス不可（BR-01）",
        en: "Current stock quantity in base unit. Must not be negative (BR-01)",
        vi: "Số lượng tồn kho hiện tại (đơn vị cơ bản). Không được âm (BR-01)",
      },
    },
    unit: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "単位",
        en: "Unit",
        vi: "Đơn vị",
      },
      placeholder: {
        ja: "単位を入力",
        en: "Enter Unit",
        vi: "Nhập Đơn vị",
      },
      description: {
        ja: "基本計量単位（例：kg、piece、liter）",
        en: "Base unit of measurement (e.g., kg, piece, liter)",
        vi: "Đơn vị đo lường cơ bản (VD: kg, piece, liter)",
      },
    },
    min_stock: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最低在庫",
        en: "Minimum Stock",
        vi: "Tồn kho tối thiểu",
      },
      placeholder: {
        ja: "最低在庫を入力",
        en: "Enter Minimum Stock",
        vi: "Nhập Tồn kho tối thiểu",
      },
      description: {
        ja: "アラート用の最低在庫しきい値（F8）。Null = アラートなし",
        en: "Minimum stock threshold for alerts (F8). Null = no alert",
        vi: "Ngưỡng tồn kho tối thiểu cho cảnh báo (F8). Null = không cảnh báo",
      },
    },
    max_stock: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最大在庫",
        en: "Maximum Stock",
        vi: "Tồn kho tối đa",
      },
      placeholder: {
        ja: "最大在庫を入力",
        en: "Enter Maximum Stock",
        vi: "Nhập Tồn kho tối đa",
      },
      description: {
        ja: "最大在庫しきい値（F8）。Null = 制限なし",
        en: "Maximum stock threshold (F8). Null = no limit",
        vi: "Ngưỡng tồn kho tối đa (F8). Null = không giới hạn",
      },
    },
    alert_enabled: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "アラート有効",
        en: "Alert Enabled",
        vi: "Bật cảnh báo",
      },
      placeholder: {
        ja: "アラート有効を入力",
        en: "Enter Alert Enabled",
        vi: "Nhập Bật cảnh báo",
      },
      description: {
        ja: "このアイテムの在庫レベルアラートを有効にする（F8）",
        en: "Enable stock level alerts for this item (F8)",
        vi: "Bật cảnh báo tồn kho cho item này (F8)",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["warehouse_id", "quantity", "alert_enabled"] as const,
  enumFields: [] as const,
  relationFields: ["warehouse_id", "product_sku_id", "material_id"] as const,
} as const;

export type StockLevelFieldName = keyof typeof stockLevelMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a StockLevel. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface StockLevelCreateFormState {
  warehouse_id: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  quantity: number;
  unit?: string | null;
  min_stock?: number | null;
  max_stock?: number | null;
  alert_enabled: boolean;
}

/** Form-state shape for updating a StockLevel. Every field optional. */
export interface StockLevelUpdateFormState {
  warehouse_id?: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  quantity?: number;
  unit?: string | null;
  min_stock?: number | null;
  max_stock?: number | null;
  alert_enabled?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyStockLevelCreateForm(): StockLevelCreateFormState {
  return {
    warehouse_id: '',
    product_sku_id: '',
    material_id: '',
    quantity: 0,
    unit: '',
    min_stock: 0,
    max_stock: 0,
    alert_enabled: true,
  };
}

/** Convert create form state into the payload that baseStockLevelCreateSchema accepts. */
export function buildStockLevelCreatePayload(form: StockLevelCreateFormState): BaseStockLevelCreate {
  const payload: Record<string, unknown> = {
    warehouse_id: form.warehouse_id.trim(),
    product_sku_id: form.product_sku_id?.toString().trim() || null,
    material_id: form.material_id?.toString().trim() || null,
    quantity: form.quantity,
    unit: form.unit?.toString().trim() || null,
    min_stock: form.min_stock,
    max_stock: form.max_stock,
    alert_enabled: form.alert_enabled,
  };

  return payload as BaseStockLevelCreate;
}

/** Convert update form state into the payload that baseStockLevelUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildStockLevelUpdatePayload(form: StockLevelUpdateFormState): BaseStockLevelUpdate {
  const payload: Record<string, unknown> = {};
  if (form.warehouse_id !== undefined) {
    payload.warehouse_id = form.warehouse_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.material_id !== undefined) {
    payload.material_id = form.material_id?.toString().trim() ?? null;
  }
  if (form.quantity !== undefined) {
    payload.quantity = form.quantity;
  }
  if (form.unit !== undefined) {
    payload.unit = form.unit?.toString().trim() ?? null;
  }
  if (form.min_stock !== undefined) {
    payload.min_stock = form.min_stock;
  }
  if (form.max_stock !== undefined) {
    payload.max_stock = form.max_stock;
  }
  if (form.alert_enabled !== undefined) {
    payload.alert_enabled = form.alert_enabled;
  }
  return payload as BaseStockLevelUpdate;
}
