/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * ブランド
 */
export interface Brand {
  id: string;
  console_brand_id: string;
  console_organization_id: string;
  name: string;
  slug: string;
  description?: string;
  logo_url?: string;
  is_active: boolean;
  primary_color?: string;
  secondary_color?: string;
  accent_color?: string;
  text_color?: string;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Brand
 */
export const brandI18n = {
  label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
  fields: {
    console_brand_id: {
      label: {"ja":"Console Brand ID","en":"Console Brand ID","vi":"Console Brand ID"},
      placeholder: {"ja":"Console Brand IDを入力","en":"Enter Console Brand ID","vi":"Nhập Console Brand ID"},
    },
    console_organization_id: {
      label: {"ja":"Console Organization ID","en":"Console Organization ID","vi":"Console Organization ID"},
      placeholder: {"ja":"Console Organization IDを入力","en":"Enter Console Organization ID","vi":"Nhập Console Organization ID"},
    },
    name: {
      label: {"ja":"ブランド名","en":"Brand Name","vi":"Tên thương hiệu"},
      placeholder: {"ja":"ブランド名を入力","en":"Enter Brand Name","vi":"Nhập Tên thương hiệu"},
    },
    slug: {
      label: {"ja":"スラッグ","en":"Slug","vi":"Slug"},
      placeholder: {"ja":"スラッグを入力","en":"Enter Slug","vi":"Nhập Slug"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    logo_url: {
      label: {"ja":"ロゴURL","en":"Logo URL","vi":"URL Logo"},
      placeholder: {"ja":"ロゴURLを入力","en":"Enter Logo URL","vi":"Nhập URL Logo"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    primary_color: {
      label: {"ja":"プライマリカラー","en":"Primary Color","vi":"Màu chính"},
      placeholder: {"ja":"プライマリカラーを入力","en":"Enter Primary Color","vi":"Nhập Màu chính"},
    },
    secondary_color: {
      label: {"ja":"セカンダリカラー","en":"Secondary Color","vi":"Màu phụ"},
      placeholder: {"ja":"セカンダリカラーを入力","en":"Enter Secondary Color","vi":"Nhập Màu phụ"},
    },
    accent_color: {
      label: {"ja":"アクセントカラー","en":"Accent Color","vi":"Màu nhấn"},
      placeholder: {"ja":"アクセントカラーを入力","en":"Enter Accent Color","vi":"Nhập Màu nhấn"},
    },
    text_color: {
      label: {"ja":"テキストカラー","en":"Text Color","vi":"Màu chữ"},
      placeholder: {"ja":"テキストカラーを入力","en":"Enter Text Color","vi":"Nhập Màu chữ"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Brand */
export const baseBrandSchemas = {
  console_brand_id: z.string(),
  console_organization_id: z.string(),
  name: z.string().min(1).max(255),
  slug: z.string().min(1).max(100),
  description: z.string().optional().nullable(),
  logo_url: z.string().max(500).optional().nullable(),
  is_active: z.boolean(),
  primary_color: z.string().max(7).optional().nullable(),
  secondary_color: z.string().max(7).optional().nullable(),
  accent_color: z.string().max(7).optional().nullable(),
  text_color: z.string().max(7).optional().nullable(),
} as const;

/** Create schema for Brand */
export const baseBrandCreateSchema = z.object({
  console_brand_id: baseBrandSchemas.console_brand_id,
  console_organization_id: baseBrandSchemas.console_organization_id,
  name: baseBrandSchemas.name,
  slug: baseBrandSchemas.slug,
  description: baseBrandSchemas.description,
  logo_url: baseBrandSchemas.logo_url,
  is_active: baseBrandSchemas.is_active,
  primary_color: baseBrandSchemas.primary_color,
  secondary_color: baseBrandSchemas.secondary_color,
  accent_color: baseBrandSchemas.accent_color,
  text_color: baseBrandSchemas.text_color,
});

/** Update schema for Brand */
export const baseBrandUpdateSchema = baseBrandCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseBrandCreate = z.infer<typeof baseBrandCreateSchema>;
export type BaseBrandUpdate = z.infer<typeof baseBrandUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getBrandLabel(locale: string): string {
  return brandI18n.label[locale as keyof typeof brandI18n.label] ?? brandI18n.label['en'] ?? 'Brand';
}

/** Get field label for a specific locale */
export function getBrandFieldLabel(field: string, locale: string): string {
  const fieldI18n = brandI18n.fields[field as keyof typeof brandI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getBrandFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = brandI18n.fields[field as keyof typeof brandI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Brand.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const brandMetadata = {
  modelName: "Brand",
  table: "brands",

  fields: {
    console_brand_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "Console Brand ID",
        en: "Console Brand ID",
        vi: "Console Brand ID",
      },
      placeholder: {
        ja: "Console Brand IDを入力",
        en: "Enter Console Brand ID",
        vi: "Nhập Console Brand ID",
      },
    },
    console_organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "Console Organization ID",
        en: "Console Organization ID",
        vi: "Console Organization ID",
      },
      placeholder: {
        ja: "Console Organization IDを入力",
        en: "Enter Console Organization ID",
        vi: "Nhập Console Organization ID",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "ブランド名",
        en: "Brand Name",
        vi: "Tên thương hiệu",
      },
      placeholder: {
        ja: "ブランド名を入力",
        en: "Enter Brand Name",
        vi: "Nhập Tên thương hiệu",
      },
    },
    slug: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "スラッグ",
        en: "Slug",
        vi: "Slug",
      },
      placeholder: {
        ja: "スラッグを入力",
        en: "Enter Slug",
        vi: "Nhập Slug",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    logo_url: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 500,
      },
      label: {
        ja: "ロゴURL",
        en: "Logo URL",
        vi: "URL Logo",
      },
      placeholder: {
        ja: "ロゴURLを入力",
        en: "Enter Logo URL",
        vi: "Nhập URL Logo",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    primary_color: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 7,
      },
      label: {
        ja: "プライマリカラー",
        en: "Primary Color",
        vi: "Màu chính",
      },
      placeholder: {
        ja: "プライマリカラーを入力",
        en: "Enter Primary Color",
        vi: "Nhập Màu chính",
      },
      description: {
        ja: "7文字の16進数 (例",
        en: "7-char hex (e.g.",
        vi: "7 ký tự hex (vd",
      },
    },
    secondary_color: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 7,
      },
      label: {
        ja: "セカンダリカラー",
        en: "Secondary Color",
        vi: "Màu phụ",
      },
      placeholder: {
        ja: "セカンダリカラーを入力",
        en: "Enter Secondary Color",
        vi: "Nhập Màu phụ",
      },
      description: {
        ja: "ブランドの補助カラー (16進数)",
        en: "Brand secondary color (hex).",
        vi: "Màu phụ của thương hiệu (hex).",
      },
    },
    accent_color: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 7,
      },
      label: {
        ja: "アクセントカラー",
        en: "Accent Color",
        vi: "Màu nhấn",
      },
      placeholder: {
        ja: "アクセントカラーを入力",
        en: "Enter Accent Color",
        vi: "Nhập Màu nhấn",
      },
      description: {
        ja: "アクセント・強調用のサブカラー (16進数)",
        en: "Accent / highlight color (hex).",
        vi: "Màu nhấn / highlight (hex).",
      },
    },
    text_color: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 7,
      },
      label: {
        ja: "テキストカラー",
        en: "Text Color",
        vi: "Màu chữ",
      },
      placeholder: {
        ja: "テキストカラーを入力",
        en: "Enter Text Color",
        vi: "Nhập Màu chữ",
      },
      description: {
        ja: "ブランド固有のテキスト色 (warm off-black 等)",
        en: "Brand-specific text color (e.g. warm off-black). Falls back to the framework neutral when null.",
        vi: "Màu chữ riêng của thương hiệu. Khi null sẽ dùng màu neutral mặc định của framework.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["console_brand_id", "console_organization_id", "name", "slug", "is_active"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type BrandFieldName = keyof typeof brandMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Brand. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface BrandCreateFormState {
  console_brand_id: string;
  console_organization_id: string;
  name: string;
  slug: string;
  description?: string | null;
  logo_url?: string | null;
  is_active: boolean;
  primary_color?: string | null;
  secondary_color?: string | null;
  accent_color?: string | null;
  text_color?: string | null;
}

/** Form-state shape for updating a Brand. Every field optional. */
export interface BrandUpdateFormState {
  console_brand_id?: string;
  console_organization_id?: string;
  name?: string;
  slug?: string;
  description?: string | null;
  logo_url?: string | null;
  is_active?: boolean;
  primary_color?: string | null;
  secondary_color?: string | null;
  accent_color?: string | null;
  text_color?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyBrandCreateForm(): BrandCreateFormState {
  return {
    console_brand_id: '',
    console_organization_id: '',
    name: '',
    slug: '',
    description: '',
    logo_url: '',
    is_active: true,
    primary_color: '',
    secondary_color: '',
    accent_color: '',
    text_color: '',
  };
}

/** Convert create form state into the payload that baseBrandCreateSchema accepts. */
export function buildBrandCreatePayload(form: BrandCreateFormState): BaseBrandCreate {
  const payload: Record<string, unknown> = {
    console_brand_id: form.console_brand_id.trim(),
    console_organization_id: form.console_organization_id.trim(),
    name: form.name.trim(),
    slug: form.slug.trim(),
    description: form.description?.toString().trim() || null,
    logo_url: form.logo_url?.toString().trim() || null,
    is_active: form.is_active,
    primary_color: form.primary_color?.toString().trim() || null,
    secondary_color: form.secondary_color?.toString().trim() || null,
    accent_color: form.accent_color?.toString().trim() || null,
    text_color: form.text_color?.toString().trim() || null,
  };

  return payload as BaseBrandCreate;
}

/** Convert update form state into the payload that baseBrandUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildBrandUpdatePayload(form: BrandUpdateFormState): BaseBrandUpdate {
  const payload: Record<string, unknown> = {};
  if (form.console_brand_id !== undefined) {
    payload.console_brand_id = form.console_brand_id?.toString().trim() ?? null;
  }
  if (form.console_organization_id !== undefined) {
    payload.console_organization_id = form.console_organization_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.slug !== undefined) {
    payload.slug = form.slug?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.logo_url !== undefined) {
    payload.logo_url = form.logo_url?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.primary_color !== undefined) {
    payload.primary_color = form.primary_color?.toString().trim() ?? null;
  }
  if (form.secondary_color !== undefined) {
    payload.secondary_color = form.secondary_color?.toString().trim() ?? null;
  }
  if (form.accent_color !== undefined) {
    payload.accent_color = form.accent_color?.toString().trim() ?? null;
  }
  if (form.text_color !== undefined) {
    payload.text_color = form.text_color?.toString().trim() ?? null;
  }
  return payload as BaseBrandUpdate;
}
