/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Table } from './Table';

/**
 * エリア
 */
export interface Zone {
  id: string;
  code: string;
  name: string;
  description?: string;
  display_order: number;
  is_active: boolean;
  organization_id: number;
  organization?: Organization;
  branch_id: number;
  branch?: Branch;
  tables?: Table[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Zone
 */
export const zoneI18n = {
  label: {"ja":"エリア","en":"Zone","vi":"Khu vực"},
  fields: {
    code: {
      label: {"ja":"エリアコード","en":"Zone Code","vi":"Mã khu vực"},
      placeholder: {"ja":"エリアコードを入力","en":"Enter Zone Code","vi":"Nhập Mã khu vực"},
    },
    name: {
      label: {"ja":"エリア名","en":"Zone Name","vi":"Tên khu vực"},
      placeholder: {"ja":"エリア名を入力","en":"Enter Zone Name","vi":"Nhập Tên khu vực"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    display_order: {
      label: {"ja":"表示順","en":"Display Order","vi":"Thứ tự hiển thị"},
      placeholder: {"ja":"表示順を入力","en":"Enter Display Order","vi":"Nhập Thứ tự hiển thị"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    tables: {
      label: {"ja":"テーブル","en":"Tables","vi":"Bàn"},
      placeholder: {"ja":"テーブルを入力","en":"Enter Tables","vi":"Nhập Bàn"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Zone */
export const baseZoneSchemas = {
  code: z.string().min(1).max(50),
  name: z.string().min(1).max(255),
  description: z.string().optional().nullable(),
  display_order: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for Zone */
export const baseZoneCreateSchema = z.object({
  code: baseZoneSchemas.code,
  name: baseZoneSchemas.name,
  description: baseZoneSchemas.description,
  display_order: baseZoneSchemas.display_order,
  is_active: baseZoneSchemas.is_active,
});

/** Update schema for Zone */
export const baseZoneUpdateSchema = baseZoneCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseZoneCreate = z.infer<typeof baseZoneCreateSchema>;
export type BaseZoneUpdate = z.infer<typeof baseZoneUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getZoneLabel(locale: string): string {
  return zoneI18n.label[locale as keyof typeof zoneI18n.label] ?? zoneI18n.label['en'] ?? 'Zone';
}

/** Get field label for a specific locale */
export function getZoneFieldLabel(field: string, locale: string): string {
  const fieldI18n = zoneI18n.fields[field as keyof typeof zoneI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getZoneFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = zoneI18n.fields[field as keyof typeof zoneI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Zone.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const zoneMetadata = {
  modelName: "Zone",
  table: "zones",

  fields: {
    code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "エリアコード",
        en: "Zone Code",
        vi: "Mã khu vực",
      },
      placeholder: {
        ja: "エリアコードを入力",
        en: "Enter Zone Code",
        vi: "Nhập Mã khu vực",
      },
      description: {
        ja: "店舗内で一意のコード（BR-Z01）。英数字とハイフンのみ。",
        en: "Unique code per branch (BR-Z01). Alphanumeric and hyphens only.",
        vi: "Mã unique trong shop (BR-Z01). Chỉ chữ, số, gạch ngang.",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "エリア名",
        en: "Zone Name",
        vi: "Tên khu vực",
      },
      placeholder: {
        ja: "エリア名を入力",
        en: "Enter Zone Name",
        vi: "Nhập Tên khu vực",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    display_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Display Order",
        vi: "Thứ tự hiển thị",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Display Order",
        vi: "Nhập Thứ tự hiển thị",
      },
      description: {
        ja: "数値が小さいほど先頭に表示。",
        en: "Lower number appears first in the table grid.",
        vi: "Số nhỏ hơn hiển thị trước.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    organization_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branchs",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["code", "name", "display_order", "is_active", "organization_id", "branch_id"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "branch_id"] as const,
} as const;

export type ZoneFieldName = keyof typeof zoneMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Zone. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ZoneCreateFormState {
  code: string;
  name: string;
  description?: string | null;
  display_order: number;
  is_active: boolean;
  organization_id: number;
  branch_id: number;
}

/** Form-state shape for updating a Zone. Every field optional. */
export interface ZoneUpdateFormState {
  code?: string;
  name?: string;
  description?: string | null;
  display_order?: number;
  is_active?: boolean;
  organization_id?: number;
  branch_id?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyZoneCreateForm(): ZoneCreateFormState {
  return {
    code: '',
    name: '',
    description: '',
    display_order: 0,
    is_active: true,
    organization_id: 0,
    branch_id: 0,
  };
}

/** Convert create form state into the payload that baseZoneCreateSchema accepts. */
export function buildZoneCreatePayload(form: ZoneCreateFormState): BaseZoneCreate {
  const payload: Record<string, unknown> = {
    code: form.code.trim(),
    name: form.name.trim(),
    description: form.description?.toString().trim() || null,
    display_order: form.display_order,
    is_active: form.is_active,
    organization_id: form.organization_id,
    branch_id: form.branch_id,
  };

  return payload as BaseZoneCreate;
}

/** Convert update form state into the payload that baseZoneUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildZoneUpdatePayload(form: ZoneUpdateFormState): BaseZoneUpdate {
  const payload: Record<string, unknown> = {};
  if (form.code !== undefined) {
    payload.code = form.code?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.display_order !== undefined) {
    payload.display_order = form.display_order;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id;
  }
  return payload as BaseZoneUpdate;
}
