/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Product } from './Product';

/**
 * 商品タイプ
 */
export interface ProductType {
  id: string;
  code: string;
  name: string;
  description?: string;
  product_form: 'physical' | 'digital';
  has_recipe: boolean;
  is_inventory_tracked: boolean;
  icon?: string;
  is_active: boolean;
  products?: Product[];
  organization_id: number;
  organization?: Organization;
  brand_id: number;
  brand?: Brand;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ProductType
 */
export const productTypeI18n = {
  label: {"ja":"商品タイプ","en":"Product Type","vi":"Loại sản phẩm"},
  fields: {
    code: {
      label: {"ja":"コード","en":"Code","vi":"Mã"},
      placeholder: {"ja":"コードを入力","en":"Enter Code","vi":"Nhập Mã"},
    },
    name: {
      label: {"ja":"名前","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名前を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    product_form: {
      label: {"ja":"商品形態","en":"Product Form","vi":"Hình thức sản phẩm"},
      placeholder: {"ja":"商品形態を入力","en":"Enter Product Form","vi":"Nhập Hình thức sản phẩm"},
    },
    has_recipe: {
      label: {"ja":"レシピあり","en":"Has Recipe","vi":"Có công thức"},
      placeholder: {"ja":"レシピありを入力","en":"Enter Has Recipe","vi":"Nhập Có công thức"},
    },
    is_inventory_tracked: {
      label: {"ja":"在庫追跡","en":"Track Inventory","vi":"Theo dõi tồn kho"},
      placeholder: {"ja":"在庫追跡を入力","en":"Enter Track Inventory","vi":"Nhập Theo dõi tồn kho"},
    },
    icon: {
      label: {"ja":"アイコン","en":"Icon","vi":"Biểu tượng"},
      placeholder: {"ja":"アイコンを入力","en":"Enter Icon","vi":"Nhập Biểu tượng"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    products: {
      label: {"ja":"商品","en":"Products","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Products","vi":"Nhập Sản phẩm"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ProductType */
export const baseProductTypeSchemas = {
  code: z.string().min(1).max(50),
  name: z.string().min(1).max(100),
  description: z.string().optional().nullable(),
  product_form: z.enum(['physical', 'digital']),
  has_recipe: z.boolean(),
  is_inventory_tracked: z.boolean(),
  icon: z.string().max(50).optional().nullable(),
  is_active: z.boolean(),
} as const;

/** Create schema for ProductType */
export const baseProductTypeCreateSchema = z.object({
  code: baseProductTypeSchemas.code,
  name: baseProductTypeSchemas.name,
  description: baseProductTypeSchemas.description,
  product_form: baseProductTypeSchemas.product_form,
  has_recipe: baseProductTypeSchemas.has_recipe,
  is_inventory_tracked: baseProductTypeSchemas.is_inventory_tracked,
  icon: baseProductTypeSchemas.icon,
  is_active: baseProductTypeSchemas.is_active,
});

/** Update schema for ProductType */
export const baseProductTypeUpdateSchema = baseProductTypeCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseProductTypeCreate = z.infer<typeof baseProductTypeCreateSchema>;
export type BaseProductTypeUpdate = z.infer<typeof baseProductTypeUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getProductTypeLabel(locale: string): string {
  return productTypeI18n.label[locale as keyof typeof productTypeI18n.label] ?? productTypeI18n.label['en'] ?? 'ProductType';
}

/** Get field label for a specific locale */
export function getProductTypeFieldLabel(field: string, locale: string): string {
  const fieldI18n = productTypeI18n.fields[field as keyof typeof productTypeI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getProductTypeFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = productTypeI18n.fields[field as keyof typeof productTypeI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ProductType.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const productTypeMetadata = {
  modelName: "ProductType",
  table: "product_types",

  fields: {
    code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "コード",
        en: "Code",
        vi: "Mã",
      },
      placeholder: {
        ja: "コードを入力",
        en: "Enter Code",
        vi: "Nhập Mã",
      },
      description: {
        ja: "組織ごとの一意コード（例：BEVERAGE、FOOD）",
        en: "Unique code per organization (e.g., BEVERAGE, FOOD)",
        vi: "Mã duy nhất theo tổ chức (VD: BEVERAGE, FOOD)",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "名前",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名前を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    product_form: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enumValues: ["physical", "digital"] as const,
      label: {
        ja: "商品形態",
        en: "Product Form",
        vi: "Hình thức sản phẩm",
      },
      placeholder: {
        ja: "商品形態を入力",
        en: "Enter Product Form",
        vi: "Nhập Hình thức sản phẩm",
      },
    },
    has_recipe: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "レシピあり",
        en: "Has Recipe",
        vi: "Có công thức",
      },
      placeholder: {
        ja: "レシピありを入力",
        en: "Enter Has Recipe",
        vi: "Nhập Có công thức",
      },
    },
    is_inventory_tracked: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "在庫追跡",
        en: "Track Inventory",
        vi: "Theo dõi tồn kho",
      },
      placeholder: {
        ja: "在庫追跡を入力",
        en: "Enter Track Inventory",
        vi: "Nhập Theo dõi tồn kho",
      },
    },
    icon: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "アイコン",
        en: "Icon",
        vi: "Biểu tượng",
      },
      placeholder: {
        ja: "アイコンを入力",
        en: "Enter Icon",
        vi: "Nhập Biểu tượng",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    organization_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["code", "name", "product_form", "has_recipe", "is_inventory_tracked", "is_active", "organization_id", "brand_id"] as const,
  enumFields: ["product_form"] as const,
  relationFields: ["organization_id", "brand_id"] as const,
} as const;

export type ProductTypeFieldName = keyof typeof productTypeMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ProductType. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ProductTypeCreateFormState {
  code: string;
  name: string;
  description?: string | null;
  product_form: 'physical' | 'digital';
  has_recipe: boolean;
  is_inventory_tracked: boolean;
  icon?: string | null;
  is_active: boolean;
  organization_id: number;
  brand_id: number;
}

/** Form-state shape for updating a ProductType. Every field optional. */
export interface ProductTypeUpdateFormState {
  code?: string;
  name?: string;
  description?: string | null;
  product_form?: 'physical' | 'digital';
  has_recipe?: boolean;
  is_inventory_tracked?: boolean;
  icon?: string | null;
  is_active?: boolean;
  organization_id?: number;
  brand_id?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyProductTypeCreateForm(): ProductTypeCreateFormState {
  return {
    code: '',
    name: '',
    description: '',
    product_form: "physical",
    has_recipe: true,
    is_inventory_tracked: true,
    icon: '',
    is_active: true,
    organization_id: 0,
    brand_id: 0,
  };
}

/** Convert create form state into the payload that baseProductTypeCreateSchema accepts. */
export function buildProductTypeCreatePayload(form: ProductTypeCreateFormState): BaseProductTypeCreate {
  const payload: Record<string, unknown> = {
    code: form.code.trim(),
    name: form.name.trim(),
    description: form.description?.toString().trim() || null,
    product_form: form.product_form,
    has_recipe: form.has_recipe,
    is_inventory_tracked: form.is_inventory_tracked,
    icon: form.icon?.toString().trim() || null,
    is_active: form.is_active,
    organization_id: form.organization_id,
    brand_id: form.brand_id,
  };

  return payload as BaseProductTypeCreate;
}

/** Convert update form state into the payload that baseProductTypeUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildProductTypeUpdatePayload(form: ProductTypeUpdateFormState): BaseProductTypeUpdate {
  const payload: Record<string, unknown> = {};
  if (form.code !== undefined) {
    payload.code = form.code?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.product_form !== undefined) {
    payload.product_form = form.product_form;
  }
  if (form.has_recipe !== undefined) {
    payload.has_recipe = form.has_recipe;
  }
  if (form.is_inventory_tracked !== undefined) {
    payload.is_inventory_tracked = form.is_inventory_tracked;
  }
  if (form.icon !== undefined) {
    payload.icon = form.icon?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id;
  }
  return payload as BaseProductTypeUpdate;
}
