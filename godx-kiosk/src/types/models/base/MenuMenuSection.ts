/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * メニューセクション関連
 */
export interface MenuMenuSection {
  display_order: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuMenuSection
 */
export const menuMenuSectionI18n = {
  label: {"ja":"メニューセクション関連","en":"Menu Menu Section","vi":"Pivot Menu - Section"},
  fields: {
    display_order: {
      label: {"ja":"表示順序","en":"Display Order","vi":"Thu tu hien thi"},
      placeholder: {"ja":"表示順序を入力","en":"Enter Display Order","vi":"Nhập Thu tu hien thi"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuMenuSection */
export const baseMenuMenuSectionSchemas = {
  display_order: z.number().int(),
} as const;

/** Create schema for MenuMenuSection */
export const baseMenuMenuSectionCreateSchema = z.object({
  display_order: baseMenuMenuSectionSchemas.display_order,
});

/** Update schema for MenuMenuSection */
export const baseMenuMenuSectionUpdateSchema = baseMenuMenuSectionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuMenuSectionCreate = z.infer<typeof baseMenuMenuSectionCreateSchema>;
export type BaseMenuMenuSectionUpdate = z.infer<typeof baseMenuMenuSectionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuMenuSectionLabel(locale: string): string {
  return menuMenuSectionI18n.label[locale as keyof typeof menuMenuSectionI18n.label] ?? menuMenuSectionI18n.label['en'] ?? 'MenuMenuSection';
}

/** Get field label for a specific locale */
export function getMenuMenuSectionFieldLabel(field: string, locale: string): string {
  const fieldI18n = menuMenuSectionI18n.fields[field as keyof typeof menuMenuSectionI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuMenuSectionFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = menuMenuSectionI18n.fields[field as keyof typeof menuMenuSectionI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuMenuSection.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuMenuSectionMetadata = {
  modelName: "MenuMenuSection",
  table: "menu_menu_sections",

  fields: {
    display_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順序",
        en: "Display Order",
        vi: "Thu tu hien thi",
      },
      placeholder: {
        ja: "表示順序を入力",
        en: "Enter Display Order",
        vi: "Nhập Thu tu hien thi",
      },
      description: {
        ja: "メニュー内のセクション表示順序（小さいほど先）",
        en: "Order of this section within the menu (lower = first)",
        vi: "Thu tu hien thi cua section trong menu (nho = truoc)",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["display_order"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type MenuMenuSectionFieldName = keyof typeof menuMenuSectionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuMenuSection. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuMenuSectionCreateFormState {
  display_order: number;
}

/** Form-state shape for updating a MenuMenuSection. Every field optional. */
export interface MenuMenuSectionUpdateFormState {
  display_order?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuMenuSectionCreateForm(): MenuMenuSectionCreateFormState {
  return {
    display_order: 0,
  };
}

/** Convert create form state into the payload that baseMenuMenuSectionCreateSchema accepts. */
export function buildMenuMenuSectionCreatePayload(form: MenuMenuSectionCreateFormState): BaseMenuMenuSectionCreate {
  const payload: Record<string, unknown> = {
    display_order: form.display_order,
  };

  return payload as BaseMenuMenuSectionCreate;
}

/** Convert update form state into the payload that baseMenuMenuSectionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuMenuSectionUpdatePayload(form: MenuMenuSectionUpdateFormState): BaseMenuMenuSectionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.display_order !== undefined) {
    payload.display_order = form.display_order;
  }
  return payload as BaseMenuMenuSectionUpdate;
}
