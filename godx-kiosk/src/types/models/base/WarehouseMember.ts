/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { WarehouseMemberRole } from '../enum/WarehouseMemberRole';
import type { Warehouse } from './Warehouse';

/**
 * 倉庫メンバー
 */
export interface WarehouseMember {
  id: string;
  warehouse_id: string;
  warehouse?: Warehouse;
  user_id: string;
  role: WarehouseMemberRole;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for WarehouseMember
 */
export const warehouseMemberI18n = {
  label: {"ja":"倉庫メンバー","en":"Warehouse Member","vi":"Thành viên kho"},
  fields: {
    warehouse: {
      label: {"ja":"倉庫","en":"Warehouse","vi":"Kho hàng"},
      placeholder: {"ja":"倉庫を入力","en":"Enter Warehouse","vi":"Nhập Kho hàng"},
    },
    user_id: {
      label: {"ja":"ユーザーID","en":"User ID","vi":"ID người dùng"},
      placeholder: {"ja":"ユーザーIDを入力","en":"Enter User ID","vi":"Nhập ID người dùng"},
    },
    role: {
      label: {"ja":"役割","en":"Role","vi":"Vai trò"},
      placeholder: {"ja":"役割を入力","en":"Enter Role","vi":"Nhập Vai trò"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for WarehouseMember */
export const baseWarehouseMemberSchemas = {
  user_id: z.string(),
  role: z.nativeEnum(WarehouseMemberRole),
} as const;

/** Create schema for WarehouseMember */
export const baseWarehouseMemberCreateSchema = z.object({
  user_id: baseWarehouseMemberSchemas.user_id,
  role: baseWarehouseMemberSchemas.role,
});

/** Update schema for WarehouseMember */
export const baseWarehouseMemberUpdateSchema = baseWarehouseMemberCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseWarehouseMemberCreate = z.infer<typeof baseWarehouseMemberCreateSchema>;
export type BaseWarehouseMemberUpdate = z.infer<typeof baseWarehouseMemberUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getWarehouseMemberLabel(locale: string): string {
  return warehouseMemberI18n.label[locale as keyof typeof warehouseMemberI18n.label] ?? warehouseMemberI18n.label['en'] ?? 'WarehouseMember';
}

/** Get field label for a specific locale */
export function getWarehouseMemberFieldLabel(field: string, locale: string): string {
  const fieldI18n = warehouseMemberI18n.fields[field as keyof typeof warehouseMemberI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getWarehouseMemberFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = warehouseMemberI18n.fields[field as keyof typeof warehouseMemberI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for WarehouseMember.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const warehouseMemberMetadata = {
  modelName: "WarehouseMember",
  table: "warehouse_members",

  fields: {
    warehouse_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Warehouse",
        table: "warehouses",
        onDelete: "CASCADE",
      },
      label: {},
    },
    user_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ユーザーID",
        en: "User ID",
        vi: "ID người dùng",
      },
      placeholder: {
        ja: "ユーザーIDを入力",
        en: "Enter User ID",
        vi: "Nhập ID người dùng",
      },
      description: {
        ja: "user_caches.idを参照（SSOユーザー）。user_cachesはSSOキャッシュテーブルのためハードFKなし。",
        en: "References user_caches.id (SSO user). No hard FK because user_caches is an SSO cache table.",
        vi: "Tham chiếu user_caches.id (user SSO). Không dùng FK cứng vì user_caches là cache SSO.",
      },
    },
    role: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "WarehouseMemberRole",
      enumValues: ["manager", "staff"] as const,
      label: {
        ja: "役割",
        en: "Role",
        vi: "Vai trò",
      },
      placeholder: {
        ja: "役割を入力",
        en: "Enter Role",
        vi: "Nhập Vai trò",
      },
      description: {
        ja: "manager = 閲覧+作成+承認; staff = 閲覧+作成のみ（BR-06）",
        en: "manager = view + create + approve; staff = view + create only (BR-06)",
        vi: "manager = xem + tạo + duyệt; staff = chỉ xem + tạo (BR-06)",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["warehouse_id", "user_id", "role"] as const,
  enumFields: ["role"] as const,
  relationFields: ["warehouse_id"] as const,
} as const;

export type WarehouseMemberFieldName = keyof typeof warehouseMemberMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a WarehouseMember. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface WarehouseMemberCreateFormState {
  warehouse_id: string;
  user_id: string;
  role: WarehouseMemberRole;
}

/** Form-state shape for updating a WarehouseMember. Every field optional. */
export interface WarehouseMemberUpdateFormState {
  warehouse_id?: string;
  user_id?: string;
  role?: WarehouseMemberRole;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyWarehouseMemberCreateForm(): WarehouseMemberCreateFormState {
  return {
    warehouse_id: '',
    user_id: '',
    role: WarehouseMemberRole.Manager,
  };
}

/** Convert create form state into the payload that baseWarehouseMemberCreateSchema accepts. */
export function buildWarehouseMemberCreatePayload(form: WarehouseMemberCreateFormState): BaseWarehouseMemberCreate {
  const payload: Record<string, unknown> = {
    warehouse_id: form.warehouse_id.trim(),
    user_id: form.user_id.trim(),
    role: form.role,
  };

  return payload as BaseWarehouseMemberCreate;
}

/** Convert update form state into the payload that baseWarehouseMemberUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildWarehouseMemberUpdatePayload(form: WarehouseMemberUpdateFormState): BaseWarehouseMemberUpdate {
  const payload: Record<string, unknown> = {};
  if (form.warehouse_id !== undefined) {
    payload.warehouse_id = form.warehouse_id?.toString().trim() ?? null;
  }
  if (form.user_id !== undefined) {
    payload.user_id = form.user_id?.toString().trim() ?? null;
  }
  if (form.role !== undefined) {
    payload.role = form.role;
  }
  return payload as BaseWarehouseMemberUpdate;
}
