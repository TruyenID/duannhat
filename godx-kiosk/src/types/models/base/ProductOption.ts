/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Product } from './Product';
import type { ProductOptionValue } from './ProductOptionValue';

/**
 * 商品オプション
 */
export interface ProductOption {
  id: string;
  product_id: string;
  product?: Product;
  key: string;
  name: string;
  position: number;
  is_active: boolean;
  values?: ProductOptionValue[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ProductOption
 */
export const productOptionI18n = {
  label: {"ja":"商品オプション","en":"Product Option","vi":"Tùy chọn sản phẩm"},
  fields: {
    product: {
      label: {"ja":"商品","en":"Product","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Product","vi":"Nhập Sản phẩm"},
    },
    key: {
      label: {"ja":"オプションキー","en":"Option Key","vi":"Key tùy chọn"},
      placeholder: {"ja":"オプションキーを入力","en":"Enter Option Key","vi":"Nhập Key tùy chọn"},
    },
    name: {
      label: {"ja":"オプション名","en":"Option Name","vi":"Tên tùy chọn"},
      placeholder: {"ja":"オプション名を入力","en":"Enter Option Name","vi":"Nhập Tên tùy chọn"},
    },
    position: {
      label: {"ja":"順序","en":"Position","vi":"Thứ tự"},
      placeholder: {"ja":"順序を入力","en":"Enter Position","vi":"Nhập Thứ tự"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    values: {
      label: {"ja":"値","en":"Values","vi":"Giá trị"},
      placeholder: {"ja":"値を入力","en":"Enter Values","vi":"Nhập Giá trị"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ProductOption */
export const baseProductOptionSchemas = {
  key: z.string().min(1).max(50),
  name: z.string().min(1).max(100),
  position: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for ProductOption */
export const baseProductOptionCreateSchema = z.object({
  key: baseProductOptionSchemas.key,
  name: baseProductOptionSchemas.name,
  position: baseProductOptionSchemas.position,
  is_active: baseProductOptionSchemas.is_active,
  ja: z.object({
    name: z.string().max(100).optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(100).optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(100).optional().nullable(),
  }).optional(),
});

/** Update schema for ProductOption */
export const baseProductOptionUpdateSchema = baseProductOptionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseProductOptionCreate = z.infer<typeof baseProductOptionCreateSchema>;
export type BaseProductOptionUpdate = z.infer<typeof baseProductOptionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getProductOptionLabel(locale: string): string {
  return productOptionI18n.label[locale as keyof typeof productOptionI18n.label] ?? productOptionI18n.label['en'] ?? 'ProductOption';
}

/** Get field label for a specific locale */
export function getProductOptionFieldLabel(field: string, locale: string): string {
  const fieldI18n = productOptionI18n.fields[field as keyof typeof productOptionI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getProductOptionFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = productOptionI18n.fields[field as keyof typeof productOptionI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ProductOption.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const productOptionMetadata = {
  modelName: "ProductOption",
  table: "product_options",

  fields: {
    product_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Product",
        table: "products",
        onDelete: "CASCADE",
      },
      label: {},
    },
    key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "オプションキー",
        en: "Option Key",
        vi: "Key tùy chọn",
      },
      placeholder: {
        ja: "オプションキーを入力",
        en: "Enter Option Key",
        vi: "Nhập Key tùy chọn",
      },
      description: {
        ja: "内部スラッグ。SKUがこのオプションを参照している場合は変更不可。",
        en: "Internal slug. Immutable once any ProductSku references this option.",
        vi: "Slug nội bộ. Không đổi được khi đã có SKU tham chiếu.",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "オプション名",
        en: "Option Name",
        vi: "Tên tùy chọn",
      },
      placeholder: {
        ja: "オプション名を入力",
        en: "Enter Option Name",
        vi: "Nhập Tên tùy chọn",
      },
    },
    position: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "順序",
        en: "Position",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "順序を入力",
        en: "Enter Position",
        vi: "Nhập Thứ tự",
      },
      description: {
        ja: "1..3。ProductSku.optionValue{N}_idと対応。SKU存在時は再割当て禁止。",
        en: "1..3. Maps to ProductSku.optionValue{N}_id. NEVER reassign once SKUs exist.",
        vi: "1..3. Map tới ProductSku.optionValue{N}_id. KHÔNG được đổi khi đã có SKU.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
  },

  translatableFields: ["name"] as const,
  requiredFields: ["product_id", "key", "name", "position", "is_active"] as const,
  enumFields: [] as const,
  relationFields: ["product_id"] as const,
} as const;

export type ProductOptionFieldName = keyof typeof productOptionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ProductOption. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ProductOptionCreateFormState {
  product_id: string;
  key: string;
  name: Record<Locale, string>;
  position: number;
  is_active: boolean;
}

/** Form-state shape for updating a ProductOption. Every field optional. */
export interface ProductOptionUpdateFormState {
  product_id?: string;
  key?: string;
  name?: Record<Locale, string>;
  position?: number;
  is_active?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyProductOptionCreateForm(): ProductOptionCreateFormState {
  return {
    product_id: '',
    key: '',
    name: emptyLocaleMap(),
    position: 0,
    is_active: true,
  };
}

/** Convert create form state into the payload that baseProductOptionCreateSchema accepts. */
export function buildProductOptionCreatePayload(form: ProductOptionCreateFormState): BaseProductOptionCreate {
  const payload: Record<string, unknown> = {
    product_id: form.product_id.trim(),
    key: form.key.trim(),
    name: (form.name["ja"] ?? '').trim(),
    position: form.position,
    is_active: form.is_active,
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
  });
  Object.assign(payload, i18n);

  return payload as BaseProductOptionCreate;
}

/** Convert update form state into the payload that baseProductOptionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildProductOptionUpdatePayload(form: ProductOptionUpdateFormState): BaseProductOptionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.product_id !== undefined) {
    payload.product_id = form.product_id?.toString().trim() ?? null;
  }
  if (form.key !== undefined) {
    payload.key = form.key?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.position !== undefined) {
    payload.position = form.position;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseProductOptionUpdate;
}
