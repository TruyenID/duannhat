/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { ProductSku } from './ProductSku';
import type { Recipe } from './Recipe';

/**
 * 材料
 */
export interface Material {
  id: string;
  sku?: string;
  name: string;
  description?: string;
  components?: unknown;
  yield_quantity: number;
  yield_unit?: string;
  calculated_cost: number;
  is_active: boolean;
  outputSku_id?: string;
  outputSku?: ProductSku | null;
  recipes?: Recipe[];
  skus?: ProductSku[];
  organization_id: number;
  organization?: Organization;
  brand_id: number;
  brand?: Brand;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Material
 */
export const materialI18n = {
  label: {"ja":"材料","en":"Material","vi":"Nguyên liệu"},
  fields: {
    sku: {
      label: {"ja":"材料SKU","en":"Material SKU","vi":"Mã nguyên liệu"},
      placeholder: {"ja":"例：M-001","en":"e.g., M-001","vi":"VD: M-001"},
    },
    name: {
      label: {"ja":"材料名","en":"Material Name","vi":"Tên nguyên liệu"},
      placeholder: {"ja":"材料名を入力","en":"Enter Material Name","vi":"Nhập Tên nguyên liệu"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    components: {
      label: {"ja":"構成要素","en":"Components","vi":"Thành phần"},
      placeholder: {"ja":"構成要素を入力","en":"Enter Components","vi":"Nhập Thành phần"},
    },
    yield_quantity: {
      label: {"ja":"出来高","en":"Yield Quantity","vi":"Số lượng sản xuất"},
      placeholder: {"ja":"出来高を入力","en":"Enter Yield Quantity","vi":"Nhập Số lượng sản xuất"},
    },
    yield_unit: {
      label: {"ja":"出力単位","en":"Yield Unit","vi":"Đơn vị đầu ra"},
      placeholder: {"ja":"出力単位を入力","en":"Enter Yield Unit","vi":"Nhập Đơn vị đầu ra"},
    },
    calculated_cost: {
      label: {"ja":"計算原価","en":"Calculated Cost","vi":"Giá vốn tính toán"},
      placeholder: {"ja":"計算原価を入力","en":"Enter Calculated Cost","vi":"Nhập Giá vốn tính toán"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    output_sku: {
      label: {"ja":"出力SKU","en":"Output SKU","vi":"SKU đầu ra"},
      placeholder: {"ja":"出力SKUを入力","en":"Enter Output SKU","vi":"Nhập SKU đầu ra"},
    },
    recipes: {
      label: {"ja":"レシピ","en":"Recipes","vi":"Công thức"},
      placeholder: {"ja":"レシピを入力","en":"Enter Recipes","vi":"Nhập Công thức"},
    },
    skus: {
      label: {"ja":"リンクされたSKU","en":"Linked SKUs","vi":"Các SKU liên kết"},
      placeholder: {"ja":"リンクされたSKUを入力","en":"Enter Linked SKUs","vi":"Nhập Các SKU liên kết"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Material */
export const baseMaterialSchemas = {
  sku: z.string().max(50).optional().nullable(),
  name: z.string().min(1).max(255),
  description: z.string().optional().nullable(),
  components: z.unknown().optional().nullable(),
  yield_quantity: z.number(),
  yield_unit: z.string().max(50).optional().nullable(),
  calculated_cost: z.number(),
  is_active: z.boolean(),
} as const;

/** Create schema for Material */
export const baseMaterialCreateSchema = z.object({
  sku: baseMaterialSchemas.sku,
  name: baseMaterialSchemas.name,
  description: baseMaterialSchemas.description,
  components: baseMaterialSchemas.components,
  yield_quantity: baseMaterialSchemas.yield_quantity,
  yield_unit: baseMaterialSchemas.yield_unit,
  calculated_cost: baseMaterialSchemas.calculated_cost,
  is_active: baseMaterialSchemas.is_active,
});

/** Update schema for Material */
export const baseMaterialUpdateSchema = baseMaterialCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMaterialCreate = z.infer<typeof baseMaterialCreateSchema>;
export type BaseMaterialUpdate = z.infer<typeof baseMaterialUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMaterialLabel(locale: string): string {
  return materialI18n.label[locale as keyof typeof materialI18n.label] ?? materialI18n.label['en'] ?? 'Material';
}

/** Get field label for a specific locale */
export function getMaterialFieldLabel(field: string, locale: string): string {
  const fieldI18n = materialI18n.fields[field as keyof typeof materialI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMaterialFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = materialI18n.fields[field as keyof typeof materialI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Material.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const materialMetadata = {
  modelName: "Material",
  table: "materials",

  fields: {
    sku: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "材料SKU",
        en: "Material SKU",
        vi: "Mã nguyên liệu",
      },
      placeholder: {
        ja: "例：M-001",
        en: "e.g., M-001",
        vi: "VD: M-001",
      },
      description: {
        ja: "プレフィックスM-を持つ一意のSKU",
        en: "Unique SKU with prefix M-",
        vi: "SKU duy nhất với tiền tố M-",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "材料名",
        en: "Material Name",
        vi: "Tên nguyên liệu",
      },
      placeholder: {
        ja: "材料名を入力",
        en: "Enter Material Name",
        vi: "Nhập Tên nguyên liệu",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    components: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "構成要素",
        en: "Components",
        vi: "Thành phần",
      },
      placeholder: {
        ja: "構成要素を入力",
        en: "Enter Components",
        vi: "Nhập Thành phần",
      },
      description: {
        ja: "バリエーション/材料コンポーネントのJSON配列",
        en: "JSON array of variant/material components",
        vi: "Mảng JSON các thành phần variant/material",
      },
    },
    yield_quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "出来高",
        en: "Yield Quantity",
        vi: "Số lượng sản xuất",
      },
      placeholder: {
        ja: "出来高を入力",
        en: "Enter Yield Quantity",
        vi: "Nhập Số lượng sản xuất",
      },
      description: {
        ja: "1バッチあたりの出力量",
        en: "How much output per batch",
        vi: "1 lần pha ra bao nhiêu",
      },
    },
    yield_unit: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "出力単位",
        en: "Yield Unit",
        vi: "Đơn vị đầu ra",
      },
      placeholder: {
        ja: "出力単位を入力",
        en: "Enter Yield Unit",
        vi: "Nhập Đơn vị đầu ra",
      },
      description: {
        ja: "出力の単位（例：PORTION、LITER、GRAM）",
        en: "Unit of output (e.g., PORTION, LITER, GRAM)",
        vi: "Đơn vị của output (VD: PORTION, LITER, GRAM)",
      },
    },
    calculated_cost: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "計算原価",
        en: "Calculated Cost",
        vi: "Giá vốn tính toán",
      },
      placeholder: {
        ja: "計算原価を入力",
        en: "Enter Calculated Cost",
        vi: "Nhập Giá vốn tính toán",
      },
      description: {
        ja: "コンポーネントから事前計算された原価",
        en: "Pre-calculated cost from components",
        vi: "Giá vốn tính sẵn từ components",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    output_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "SET NULL",
      },
      label: {},
    },
    organization_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["name", "yield_quantity", "calculated_cost", "is_active", "organization_id", "brand_id"] as const,
  enumFields: [] as const,
  relationFields: ["output_sku_id", "organization_id", "brand_id"] as const,
} as const;

export type MaterialFieldName = keyof typeof materialMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Material. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MaterialCreateFormState {
  sku?: string | null;
  name: string;
  description?: string | null;
  components?: unknown | null;
  yield_quantity: number;
  yield_unit?: string | null;
  calculated_cost: number;
  is_active: boolean;
  output_sku_id?: string | null;
  organization_id: number;
  brand_id: number;
}

/** Form-state shape for updating a Material. Every field optional. */
export interface MaterialUpdateFormState {
  sku?: string | null;
  name?: string;
  description?: string | null;
  components?: unknown | null;
  yield_quantity?: number;
  yield_unit?: string | null;
  calculated_cost?: number;
  is_active?: boolean;
  output_sku_id?: string | null;
  organization_id?: number;
  brand_id?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMaterialCreateForm(): MaterialCreateFormState {
  return {
    sku: '',
    name: '',
    description: '',
    components: null,
    yield_quantity: 1,
    yield_unit: '',
    calculated_cost: 0,
    is_active: true,
    output_sku_id: '',
    organization_id: 0,
    brand_id: 0,
  };
}

/** Convert create form state into the payload that baseMaterialCreateSchema accepts. */
export function buildMaterialCreatePayload(form: MaterialCreateFormState): BaseMaterialCreate {
  const payload: Record<string, unknown> = {
    sku: form.sku?.toString().trim() || null,
    name: form.name.trim(),
    description: form.description?.toString().trim() || null,
    components: form.components,
    yield_quantity: form.yield_quantity,
    yield_unit: form.yield_unit?.toString().trim() || null,
    calculated_cost: form.calculated_cost,
    is_active: form.is_active,
    output_sku_id: form.output_sku_id?.toString().trim() || null,
    organization_id: form.organization_id,
    brand_id: form.brand_id,
  };

  return payload as BaseMaterialCreate;
}

/** Convert update form state into the payload that baseMaterialUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMaterialUpdatePayload(form: MaterialUpdateFormState): BaseMaterialUpdate {
  const payload: Record<string, unknown> = {};
  if (form.sku !== undefined) {
    payload.sku = form.sku?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.components !== undefined) {
    payload.components = form.components;
  }
  if (form.yield_quantity !== undefined) {
    payload.yield_quantity = form.yield_quantity;
  }
  if (form.yield_unit !== undefined) {
    payload.yield_unit = form.yield_unit?.toString().trim() ?? null;
  }
  if (form.calculated_cost !== undefined) {
    payload.calculated_cost = form.calculated_cost;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.output_sku_id !== undefined) {
    payload.output_sku_id = form.output_sku_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id;
  }
  return payload as BaseMaterialUpdate;
}
