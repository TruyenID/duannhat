/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { MenuProduct } from './MenuProduct';
import type { ProductSku } from './ProductSku';

/**
 * メニュー商品SKU
 */
export interface MenuProductSku {
  id: string;
  menuProduct_id: string;
  menuProduct?: MenuProduct;
  productSku_id: string;
  productSku?: ProductSku;
  selling_price: number;
  is_price_overridden: boolean;
  is_active: boolean;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuProductSku
 */
export const menuProductSkuI18n = {
  label: {"ja":"メニュー商品SKU","en":"Menu Product SKU","vi":"SKU sản phẩm trong Menu"},
  fields: {
    menu_product: {
      label: {"ja":"メニュー商品","en":"Menu Product","vi":"Sản phẩm trong Menu"},
      placeholder: {"ja":"メニュー商品を入力","en":"Enter Menu Product","vi":"Nhập Sản phẩm trong Menu"},
    },
    product_sku: {
      label: {"ja":"商品SKU","en":"Product SKU","vi":"SKU sản phẩm"},
      placeholder: {"ja":"商品SKUを入力","en":"Enter Product SKU","vi":"Nhập SKU sản phẩm"},
    },
    selling_price: {
      label: {"ja":"販売価格","en":"Selling Price","vi":"Giá bán"},
      placeholder: {"ja":"販売価格を入力","en":"Enter Selling Price","vi":"Nhập Giá bán"},
    },
    is_price_overridden: {
      label: {"ja":"価格上書き済み","en":"Price Overridden","vi":"Giá đã ghi đè"},
      placeholder: {"ja":"価格上書き済みを入力","en":"Enter Price Overridden","vi":"Nhập Giá đã ghi đè"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuProductSku */
export const baseMenuProductSkuSchemas = {
  selling_price: z.number(),
  is_price_overridden: z.boolean(),
  is_active: z.boolean(),
} as const;

/** Create schema for MenuProductSku */
export const baseMenuProductSkuCreateSchema = z.object({
  selling_price: baseMenuProductSkuSchemas.selling_price,
  is_price_overridden: baseMenuProductSkuSchemas.is_price_overridden,
  is_active: baseMenuProductSkuSchemas.is_active,
});

/** Update schema for MenuProductSku */
export const baseMenuProductSkuUpdateSchema = baseMenuProductSkuCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuProductSkuCreate = z.infer<typeof baseMenuProductSkuCreateSchema>;
export type BaseMenuProductSkuUpdate = z.infer<typeof baseMenuProductSkuUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuProductSkuLabel(locale: string): string {
  return menuProductSkuI18n.label[locale as keyof typeof menuProductSkuI18n.label] ?? menuProductSkuI18n.label['en'] ?? 'MenuProductSku';
}

/** Get field label for a specific locale */
export function getMenuProductSkuFieldLabel(field: string, locale: string): string {
  const fieldI18n = menuProductSkuI18n.fields[field as keyof typeof menuProductSkuI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuProductSkuFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = menuProductSkuI18n.fields[field as keyof typeof menuProductSkuI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuProductSku.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuProductSkuMetadata = {
  modelName: "MenuProductSku",
  table: "menu_product_skus",

  fields: {
    menu_product_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "MenuProduct",
        table: "menu_products",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "CASCADE",
      },
      label: {},
    },
    selling_price: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "販売価格",
        en: "Selling Price",
        vi: "Giá bán",
      },
      placeholder: {
        ja: "販売価格を入力",
        en: "Enter Selling Price",
        vi: "Nhập Giá bán",
      },
      description: {
        ja: "店舗販売価格。クローン時にproduct_skus.selling_priceからコピー。店舗でオーバーライド可能。",
        en: "Branch selling price. Initially copied from product_skus.selling_price during clone. Branch can override.",
        vi: "Giá bán tại branch. Ban đầu copy từ product_skus.selling_price khi clone. Branch có thể override.",
      },
    },
    is_price_overridden: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "価格上書き済み",
        en: "Price Overridden",
        vi: "Giá đã ghi đè",
      },
      placeholder: {
        ja: "価格上書き済みを入力",
        en: "Enter Price Overridden",
        vi: "Nhập Giá đã ghi đè",
      },
      description: {
        ja: "店舗がselling_priceをカスタマイズしたかどうか。店舗が明示的に価格を設定した場合True（デフォルトと同じ場合でも）。クローンまたはリセット後はFalse。",
        en: "Whether the branch has customized selling_price. True when branch explicitly sets a price (even if same as default). False after clone or reset.",
        vi: "Branch đã tùy chỉnh selling_price chưa. True khi branch tự set giá (kể cả nếu trùng giá gốc). False sau clone hoặc reset.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
      description: {
        ja: "SKUレベルのトグル。店舗は個別のSKUを無効にできます（例：特定のサイズが品切れ）。",
        en: "SKU-level toggle. Branch can deactivate individual SKUs (e.g. out of a specific size).",
        vi: "Toggle cấp SKU. Branch có thể tắt bán 1 SKU cụ thể (vd hết size lớn).",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["menu_product_id", "product_sku_id", "selling_price", "is_price_overridden", "is_active"] as const,
  enumFields: [] as const,
  relationFields: ["menu_product_id", "product_sku_id"] as const,
} as const;

export type MenuProductSkuFieldName = keyof typeof menuProductSkuMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuProductSku. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuProductSkuCreateFormState {
  menu_product_id: string;
  product_sku_id: string;
  selling_price: number;
  is_price_overridden: boolean;
  is_active: boolean;
}

/** Form-state shape for updating a MenuProductSku. Every field optional. */
export interface MenuProductSkuUpdateFormState {
  menu_product_id?: string;
  product_sku_id?: string;
  selling_price?: number;
  is_price_overridden?: boolean;
  is_active?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuProductSkuCreateForm(): MenuProductSkuCreateFormState {
  return {
    menu_product_id: '',
    product_sku_id: '',
    selling_price: 0,
    is_price_overridden: false,
    is_active: true,
  };
}

/** Convert create form state into the payload that baseMenuProductSkuCreateSchema accepts. */
export function buildMenuProductSkuCreatePayload(form: MenuProductSkuCreateFormState): BaseMenuProductSkuCreate {
  const payload: Record<string, unknown> = {
    menu_product_id: form.menu_product_id.trim(),
    product_sku_id: form.product_sku_id.trim(),
    selling_price: form.selling_price,
    is_price_overridden: form.is_price_overridden,
    is_active: form.is_active,
  };

  return payload as BaseMenuProductSkuCreate;
}

/** Convert update form state into the payload that baseMenuProductSkuUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuProductSkuUpdatePayload(form: MenuProductSkuUpdateFormState): BaseMenuProductSkuUpdate {
  const payload: Record<string, unknown> = {};
  if (form.menu_product_id !== undefined) {
    payload.menu_product_id = form.menu_product_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.selling_price !== undefined) {
    payload.selling_price = form.selling_price;
  }
  if (form.is_price_overridden !== undefined) {
    payload.is_price_overridden = form.is_price_overridden;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  return payload as BaseMenuProductSkuUpdate;
}
