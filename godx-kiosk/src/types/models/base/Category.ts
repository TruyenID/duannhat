/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Product } from './Product';

/**
 * カテゴリー
 */
export interface Category {
  id: string;
  sku?: string;
  name: string;
  slug?: string;
  description?: string;
  image_url?: string;
  is_active: boolean;
  parent_id?: string;
  parent?: Category | null;
  children?: Category[];
  products?: Product[];
  organization_id: number;
  organization?: Organization;
  brand_id: number;
  brand?: Brand;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Category
 */
export const categoryI18n = {
  label: {"ja":"カテゴリー","en":"Category","vi":"Danh mục"},
  fields: {
    sku: {
      label: {"ja":"SKU","en":"SKU","vi":"Mã danh mục"},
      placeholder: {"ja":"例：C-001","en":"e.g., C-001","vi":"VD: C-001"},
    },
    name: {
      label: {"ja":"名前","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名前を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    slug: {
      label: {"ja":"スラッグ","en":"Slug","vi":"Slug"},
      placeholder: {"ja":"スラッグを入力","en":"Enter Slug","vi":"Nhập Slug"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    image_url: {
      label: {"ja":"画像URL","en":"Image URL","vi":"URL hình ảnh"},
      placeholder: {"ja":"画像URLを入力","en":"Enter Image URL","vi":"Nhập URL hình ảnh"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    parent: {
      label: {"ja":"親カテゴリー","en":"Parent Category","vi":"Danh mục cha"},
      placeholder: {"ja":"親カテゴリーを入力","en":"Enter Parent Category","vi":"Nhập Danh mục cha"},
    },
    children: {
      label: {"ja":"子カテゴリー","en":"Child Categories","vi":"Danh mục con"},
      placeholder: {"ja":"子カテゴリーを入力","en":"Enter Child Categories","vi":"Nhập Danh mục con"},
    },
    products: {
      label: {"ja":"商品","en":"Products","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Products","vi":"Nhập Sản phẩm"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Category */
export const baseCategorySchemas = {
  sku: z.string().max(50).optional().nullable(),
  name: z.string().min(1).max(100),
  slug: z.string().max(100).optional().nullable(),
  description: z.string().optional().nullable(),
  image_url: z.string().max(500).optional().nullable(),
  is_active: z.boolean(),
} as const;

/** Create schema for Category */
export const baseCategoryCreateSchema = z.object({
  sku: baseCategorySchemas.sku,
  name: baseCategorySchemas.name,
  slug: baseCategorySchemas.slug,
  description: baseCategorySchemas.description,
  image_url: baseCategorySchemas.image_url,
  is_active: baseCategorySchemas.is_active,
});

/** Update schema for Category */
export const baseCategoryUpdateSchema = baseCategoryCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseCategoryCreate = z.infer<typeof baseCategoryCreateSchema>;
export type BaseCategoryUpdate = z.infer<typeof baseCategoryUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getCategoryLabel(locale: string): string {
  return categoryI18n.label[locale as keyof typeof categoryI18n.label] ?? categoryI18n.label['en'] ?? 'Category';
}

/** Get field label for a specific locale */
export function getCategoryFieldLabel(field: string, locale: string): string {
  const fieldI18n = categoryI18n.fields[field as keyof typeof categoryI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getCategoryFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = categoryI18n.fields[field as keyof typeof categoryI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Category.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const categoryMetadata = {
  modelName: "Category",
  table: "categories",

  fields: {
    sku: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "SKU",
        en: "SKU",
        vi: "Mã danh mục",
      },
      placeholder: {
        ja: "例：C-001",
        en: "e.g., C-001",
        vi: "VD: C-001",
      },
      description: {
        ja: "プレフィックスC-を持つ一意のSKU",
        en: "Unique SKU with prefix C-",
        vi: "SKU duy nhất với tiền tố C-",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "名前",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名前を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    slug: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "スラッグ",
        en: "Slug",
        vi: "Slug",
      },
      placeholder: {
        ja: "スラッグを入力",
        en: "Enter Slug",
        vi: "Nhập Slug",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    image_url: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 500,
      },
      label: {
        ja: "画像URL",
        en: "Image URL",
        vi: "URL hình ảnh",
      },
      placeholder: {
        ja: "画像URLを入力",
        en: "Enter Image URL",
        vi: "Nhập URL hình ảnh",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    parent_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Category",
        table: "categories",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["name", "is_active", "organization_id", "brand_id"] as const,
  enumFields: [] as const,
  relationFields: ["parent_id", "organization_id", "brand_id"] as const,
} as const;

export type CategoryFieldName = keyof typeof categoryMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Category. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface CategoryCreateFormState {
  sku?: string | null;
  name: string;
  slug?: string | null;
  description?: string | null;
  image_url?: string | null;
  is_active: boolean;
  parent_id?: string | null;
  organization_id: number;
  brand_id: number;
}

/** Form-state shape for updating a Category. Every field optional. */
export interface CategoryUpdateFormState {
  sku?: string | null;
  name?: string;
  slug?: string | null;
  description?: string | null;
  image_url?: string | null;
  is_active?: boolean;
  parent_id?: string | null;
  organization_id?: number;
  brand_id?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyCategoryCreateForm(): CategoryCreateFormState {
  return {
    sku: '',
    name: '',
    slug: '',
    description: '',
    image_url: '',
    is_active: true,
    parent_id: '',
    organization_id: 0,
    brand_id: 0,
  };
}

/** Convert create form state into the payload that baseCategoryCreateSchema accepts. */
export function buildCategoryCreatePayload(form: CategoryCreateFormState): BaseCategoryCreate {
  const payload: Record<string, unknown> = {
    sku: form.sku?.toString().trim() || null,
    name: form.name.trim(),
    slug: form.slug?.toString().trim() || null,
    description: form.description?.toString().trim() || null,
    image_url: form.image_url?.toString().trim() || null,
    is_active: form.is_active,
    parent_id: form.parent_id?.toString().trim() || null,
    organization_id: form.organization_id,
    brand_id: form.brand_id,
  };

  return payload as BaseCategoryCreate;
}

/** Convert update form state into the payload that baseCategoryUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildCategoryUpdatePayload(form: CategoryUpdateFormState): BaseCategoryUpdate {
  const payload: Record<string, unknown> = {};
  if (form.sku !== undefined) {
    payload.sku = form.sku?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.slug !== undefined) {
    payload.slug = form.slug?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.image_url !== undefined) {
    payload.image_url = form.image_url?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.parent_id !== undefined) {
    payload.parent_id = form.parent_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id;
  }
  return payload as BaseCategoryUpdate;
}
