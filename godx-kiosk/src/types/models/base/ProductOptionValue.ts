/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { ProductOption } from './ProductOption';

/**
 * 商品オプション値
 */
export interface ProductOptionValue {
  id: string;
  option_id: string;
  option?: ProductOption;
  value: string;
  label: string;
  position: number;
  is_active: boolean;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ProductOptionValue
 */
export const productOptionValueI18n = {
  label: {"ja":"商品オプション値","en":"Product Option Value","vi":"Giá trị tùy chọn"},
  fields: {
    option: {
      label: {"ja":"オプション","en":"Option","vi":"Tùy chọn"},
      placeholder: {"ja":"オプションを入力","en":"Enter Option","vi":"Nhập Tùy chọn"},
    },
    value: {
      label: {"ja":"値","en":"Value","vi":"Giá trị"},
      placeholder: {"ja":"値を入力","en":"Enter Value","vi":"Nhập Giá trị"},
    },
    label: {
      label: {"ja":"ラベル","en":"Label","vi":"Nhãn hiển thị"},
      placeholder: {"ja":"ラベルを入力","en":"Enter Label","vi":"Nhập Nhãn hiển thị"},
    },
    position: {
      label: {"ja":"順序","en":"Position","vi":"Thứ tự"},
      placeholder: {"ja":"順序を入力","en":"Enter Position","vi":"Nhập Thứ tự"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ProductOptionValue */
export const baseProductOptionValueSchemas = {
  value: z.string().min(1).max(100),
  label: z.string().min(1).max(150),
  position: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for ProductOptionValue */
export const baseProductOptionValueCreateSchema = z.object({
  value: baseProductOptionValueSchemas.value,
  label: baseProductOptionValueSchemas.label,
  position: baseProductOptionValueSchemas.position,
  is_active: baseProductOptionValueSchemas.is_active,
  ja: z.object({
    label: z.string().max(150).optional().nullable(),
  }).optional(),
  en: z.object({
    label: z.string().max(150).optional().nullable(),
  }).optional(),
  vi: z.object({
    label: z.string().max(150).optional().nullable(),
  }).optional(),
});

/** Update schema for ProductOptionValue */
export const baseProductOptionValueUpdateSchema = baseProductOptionValueCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseProductOptionValueCreate = z.infer<typeof baseProductOptionValueCreateSchema>;
export type BaseProductOptionValueUpdate = z.infer<typeof baseProductOptionValueUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getProductOptionValueLabel(locale: string): string {
  return productOptionValueI18n.label[locale as keyof typeof productOptionValueI18n.label] ?? productOptionValueI18n.label['en'] ?? 'ProductOptionValue';
}

/** Get field label for a specific locale */
export function getProductOptionValueFieldLabel(field: string, locale: string): string {
  const fieldI18n = productOptionValueI18n.fields[field as keyof typeof productOptionValueI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getProductOptionValueFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = productOptionValueI18n.fields[field as keyof typeof productOptionValueI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ProductOptionValue.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const productOptionValueMetadata = {
  modelName: "ProductOptionValue",
  table: "product_option_values",

  fields: {
    option_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductOption",
        table: "product_options",
        onDelete: "CASCADE",
      },
      label: {},
    },
    value: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "値",
        en: "Value",
        vi: "Giá trị",
      },
      placeholder: {
        ja: "値を入力",
        en: "Enter Value",
        vi: "Nhập Giá trị",
      },
      description: {
        ja: "Internal slug. Avoid changing once any ProductSku references this value.",
        en: "Internal slug. Avoid changing once any ProductSku references this value.",
        vi: "Slug nội bộ. Không nên đổi khi đã có SKU tham chiếu.",
      },
    },
    label: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 150,
      },
      label: {
        ja: "ラベル",
        en: "Label",
        vi: "Nhãn hiển thị",
      },
      placeholder: {
        ja: "ラベルを入力",
        en: "Enter Label",
        vi: "Nhập Nhãn hiển thị",
      },
    },
    position: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "順序",
        en: "Position",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "順序を入力",
        en: "Enter Position",
        vi: "Nhập Thứ tự",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
  },

  translatableFields: ["label"] as const,
  requiredFields: ["option_id", "value", "label", "position", "is_active"] as const,
  enumFields: [] as const,
  relationFields: ["option_id"] as const,
} as const;

export type ProductOptionValueFieldName = keyof typeof productOptionValueMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ProductOptionValue. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ProductOptionValueCreateFormState {
  option_id: string;
  value: string;
  label: Record<Locale, string>;
  position: number;
  is_active: boolean;
}

/** Form-state shape for updating a ProductOptionValue. Every field optional. */
export interface ProductOptionValueUpdateFormState {
  option_id?: string;
  value?: string;
  label?: Record<Locale, string>;
  position?: number;
  is_active?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyProductOptionValueCreateForm(): ProductOptionValueCreateFormState {
  return {
    option_id: '',
    value: '',
    label: emptyLocaleMap(),
    position: 0,
    is_active: true,
  };
}

/** Convert create form state into the payload that baseProductOptionValueCreateSchema accepts. */
export function buildProductOptionValueCreatePayload(form: ProductOptionValueCreateFormState): BaseProductOptionValueCreate {
  const payload: Record<string, unknown> = {
    option_id: form.option_id.trim(),
    value: form.value.trim(),
    label: (form.label["ja"] ?? '').trim(),
    position: form.position,
    is_active: form.is_active,
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    label: form.label,
  });
  Object.assign(payload, i18n);

  return payload as BaseProductOptionValueCreate;
}

/** Convert update form state into the payload that baseProductOptionValueUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildProductOptionValueUpdatePayload(form: ProductOptionValueUpdateFormState): BaseProductOptionValueUpdate {
  const payload: Record<string, unknown> = {};
  if (form.option_id !== undefined) {
    payload.option_id = form.option_id?.toString().trim() ?? null;
  }
  if (form.value !== undefined) {
    payload.value = form.value?.toString().trim() ?? null;
  }
  if (form.label !== undefined) {
    payload.label = (form.label["ja"] ?? '').trim();
  }
  if (form.position !== undefined) {
    payload.position = form.position;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.label !== undefined) localeInput.label = form.label;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseProductOptionValueUpdate;
}
