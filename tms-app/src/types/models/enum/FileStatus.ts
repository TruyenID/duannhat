/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * ファイルステータス
 */
export enum FileStatus {
  Temporary = 'temporary',
  Permanent = 'permanent',
}

/** All FileStatus values */
export const FileStatusValues = Object.values(FileStatus) as FileStatus[];

/** Type guard for FileStatus */
export function isFileStatus(value: unknown): value is FileStatus {
  return FileStatusValues.includes(value as FileStatus);
}

const fileStatusLabels: Partial<Record<FileStatus, Record<string, string>>> = {
  [FileStatus.Temporary]: { 'en': 'Temporary', 'ja': '一時', 'vi': 'Tạm thời' },
  [FileStatus.Permanent]: { 'en': 'Permanent', 'ja': '永続', 'vi': 'Vĩnh viễn' },
};

/** Get label for FileStatus value with locale support */
export function getFileStatusLabel(value: FileStatus, locale?: string): string {
  const labels = fileStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for FileStatus value */
export function getFileStatusExtra(_value: FileStatus): Record<string, unknown> | undefined {
  return undefined;
}
