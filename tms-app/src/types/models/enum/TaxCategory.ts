/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 消費税区分
 */
export enum TaxCategory {
  Standard = 'standard',
  Reduced = 'reduced',
  Exempt = 'exempt',
}

/** All TaxCategory values */
export const TaxCategoryValues = Object.values(TaxCategory) as TaxCategory[];

/** Type guard for TaxCategory */
export function isTaxCategory(value: unknown): value is TaxCategory {
  return TaxCategoryValues.includes(value as TaxCategory);
}

const taxCategoryLabels: Partial<Record<TaxCategory, Record<string, string>>> = {
  [TaxCategory.Standard]: { 'en': 'Standard (10%)', 'ja': '標準税率（10%）', 'vi': 'Thuế suất chuẩn (10%)' },
  [TaxCategory.Reduced]: { 'en': 'Reduced (8%)', 'ja': '軽減税率（8%）', 'vi': 'Thuế suất giảm (8%)' },
  [TaxCategory.Exempt]: { 'en': 'Tax-exempt', 'ja': '非課税', 'vi': 'Miễn thuế' },
};

/** Get label for TaxCategory value with locale support */
export function getTaxCategoryLabel(value: TaxCategory, locale?: string): string {
  const labels = taxCategoryLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for TaxCategory value */
export function getTaxCategoryExtra(_value: TaxCategory): Record<string, unknown> | undefined {
  return undefined;
}
