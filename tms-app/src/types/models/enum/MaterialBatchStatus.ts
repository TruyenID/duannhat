/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 製造バッチステータス
 */
export enum MaterialBatchStatus {
  Draft = 'draft',
  Pending = 'pending',
  Approved = 'approved',
  InProgress = 'in_progress',
  Completed = 'completed',
  Cancelled = 'cancelled',
}

/** All MaterialBatchStatus values */
export const MaterialBatchStatusValues = Object.values(MaterialBatchStatus) as MaterialBatchStatus[];

/** Type guard for MaterialBatchStatus */
export function isMaterialBatchStatus(value: unknown): value is MaterialBatchStatus {
  return MaterialBatchStatusValues.includes(value as MaterialBatchStatus);
}

const materialBatchStatusLabels: Partial<Record<MaterialBatchStatus, Record<string, string>>> = {
  [MaterialBatchStatus.Draft]: { 'en': 'Draft', 'ja': '下書き', 'vi': 'Nháp' },
  [MaterialBatchStatus.Pending]: { 'en': 'Pending', 'ja': '承認待ち', 'vi': 'Chờ duyệt' },
  [MaterialBatchStatus.Approved]: { 'en': 'Approved', 'ja': '承認済み', 'vi': 'Đã duyệt' },
  [MaterialBatchStatus.InProgress]: { 'en': 'In Progress', 'ja': '進行中', 'vi': 'Đang chế biến' },
  [MaterialBatchStatus.Completed]: { 'en': 'Completed', 'ja': '完了', 'vi': 'Hoàn thành' },
  [MaterialBatchStatus.Cancelled]: { 'en': 'Cancelled', 'ja': 'キャンセル', 'vi': 'Đã hủy' },
};

/** Get label for MaterialBatchStatus value with locale support */
export function getMaterialBatchStatusLabel(value: MaterialBatchStatus, locale?: string): string {
  const labels = materialBatchStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for MaterialBatchStatus value */
export function getMaterialBatchStatusExtra(_value: MaterialBatchStatus): Record<string, unknown> | undefined {
  return undefined;
}
