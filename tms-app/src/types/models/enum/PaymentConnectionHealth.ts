/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 決済接続ヘルス
 */
export enum PaymentConnectionHealth {
  PendingVerification = 'pending_verification',
  Ready = 'ready',
  Degraded = 'degraded',
  Unavailable = 'unavailable',
  Restricted = 'restricted',
  Revoked = 'revoked',
}

/** All PaymentConnectionHealth values */
export const PaymentConnectionHealthValues = Object.values(PaymentConnectionHealth) as PaymentConnectionHealth[];

/** Type guard for PaymentConnectionHealth */
export function isPaymentConnectionHealth(value: unknown): value is PaymentConnectionHealth {
  return PaymentConnectionHealthValues.includes(value as PaymentConnectionHealth);
}

const paymentConnectionHealthLabels: Partial<Record<PaymentConnectionHealth, Record<string, string>>> = {
  [PaymentConnectionHealth.PendingVerification]: { 'en': 'Pending Verification', 'ja': '確認待ち', 'vi': 'Chờ xác minh' },
  [PaymentConnectionHealth.Ready]: { 'en': 'Ready', 'ja': '利用可能', 'vi': 'Sẵn sàng' },
  [PaymentConnectionHealth.Degraded]: { 'en': 'Degraded', 'ja': '性能低下', 'vi': 'Suy giảm' },
  [PaymentConnectionHealth.Unavailable]: { 'en': 'Temporarily Unavailable', 'ja': '一時利用不可', 'vi': 'Tạm thời không khả dụng' },
  [PaymentConnectionHealth.Restricted]: { 'en': 'Restricted', 'ja': '制限あり', 'vi': 'Bị hạn chế' },
  [PaymentConnectionHealth.Revoked]: { 'en': 'Revoked', 'ja': '取消済み', 'vi': 'Đã thu hồi' },
};

/** Get label for PaymentConnectionHealth value with locale support */
export function getPaymentConnectionHealthLabel(value: PaymentConnectionHealth, locale?: string): string {
  const labels = paymentConnectionHealthLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for PaymentConnectionHealth value */
export function getPaymentConnectionHealthExtra(_value: PaymentConnectionHealth): Record<string, unknown> | undefined {
  return undefined;
}
