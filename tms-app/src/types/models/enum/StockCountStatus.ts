/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 棚卸ステータス
 */
export enum StockCountStatus {
  Draft = 'draft',
  InProgress = 'in_progress',
  PendingApproval = 'pending_approval',
  Approved = 'approved',
  Cancelled = 'cancelled',
}

/** All StockCountStatus values */
export const StockCountStatusValues = Object.values(StockCountStatus) as StockCountStatus[];

/** Type guard for StockCountStatus */
export function isStockCountStatus(value: unknown): value is StockCountStatus {
  return StockCountStatusValues.includes(value as StockCountStatus);
}

const stockCountStatusLabels: Partial<Record<StockCountStatus, Record<string, string>>> = {
  [StockCountStatus.Draft]: { 'en': 'Draft', 'ja': '下書き', 'vi': 'Nháp' },
  [StockCountStatus.InProgress]: { 'en': 'In Progress', 'ja': '実施中', 'vi': 'Đang kiểm kê' },
  [StockCountStatus.PendingApproval]: { 'en': 'Pending Approval', 'ja': '承認待ち', 'vi': 'Chờ duyệt' },
  [StockCountStatus.Approved]: { 'en': 'Approved', 'ja': '承認済み', 'vi': 'Đã duyệt' },
  [StockCountStatus.Cancelled]: { 'en': 'Cancelled', 'ja': 'キャンセル', 'vi': 'Đã hủy' },
};

/** Get label for StockCountStatus value with locale support */
export function getStockCountStatusLabel(value: StockCountStatus, locale?: string): string {
  const labels = stockCountStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for StockCountStatus value */
export function getStockCountStatusExtra(_value: StockCountStatus): Record<string, unknown> | undefined {
  return undefined;
}
