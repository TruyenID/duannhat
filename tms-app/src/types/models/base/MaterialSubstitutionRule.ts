/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Brand } from './Brand';
import type { Material } from './Material';
import type { Organization } from './Organization';

/**
 * 材料代替ルール
 */
export interface MaterialSubstitutionRule {
  id: string;
  primaryMaterial_id: string;
  primaryMaterial?: Material;
  substituteMaterial_id: string;
  substituteMaterial?: Material;
  conversion_factor: number;
  priority: number;
  is_active: boolean;
  auto_substitute: boolean;
  valid_from?: DateTimeString | null;
  valid_until?: DateTimeString | null;
  notes?: string | null;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MaterialSubstitutionRule
 */
export const materialSubstitutionRuleI18n = {
  label: {"ja":"材料代替ルール","en":"Material Substitution Rule","vi":"Quy tắc thay thế nguyên liệu"},
  fields: {
    primary_material: {
      label: {"ja":"主材料","en":"Primary Material","vi":"Nguyên liệu chính"},
      placeholder: {"ja":"主材料を入力","en":"Enter Primary Material","vi":"Nhập Nguyên liệu chính"},
    },
    substitute_material: {
      label: {"ja":"代替材料","en":"Substitute Material","vi":"Nguyên liệu thay thế"},
      placeholder: {"ja":"代替材料を入力","en":"Enter Substitute Material","vi":"Nhập Nguyên liệu thay thế"},
    },
    conversion_factor: {
      label: {"ja":"換算係数","en":"Conversion Factor","vi":"Hệ số quy đổi"},
      placeholder: {"ja":"換算係数を入力","en":"Enter Conversion Factor","vi":"Nhập Hệ số quy đổi"},
    },
    priority: {
      label: {"ja":"優先度","en":"Priority","vi":"Ưu tiên"},
      placeholder: {"ja":"優先度を入力","en":"Enter Priority","vi":"Nhập Ưu tiên"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    auto_substitute: {
      label: {"ja":"自動代替","en":"Auto Substitute","vi":"Tự động thay thế"},
      placeholder: {"ja":"自動代替を入力","en":"Enter Auto Substitute","vi":"Nhập Tự động thay thế"},
    },
    valid_from: {
      label: {"ja":"有効開始","en":"Valid From","vi":"Hiệu lực từ"},
      placeholder: {"ja":"有効開始を入力","en":"Enter Valid From","vi":"Nhập Hiệu lực từ"},
    },
    valid_until: {
      label: {"ja":"有効終了","en":"Valid Until","vi":"Hiệu lực đến"},
      placeholder: {"ja":"有効終了を入力","en":"Enter Valid Until","vi":"Nhập Hiệu lực đến"},
    },
    notes: {
      label: {"ja":"メモ","en":"Notes","vi":"Ghi chú"},
      placeholder: {"ja":"メモを入力","en":"Enter Notes","vi":"Nhập Ghi chú"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MaterialSubstitutionRule */
export const baseMaterialSubstitutionRuleSchemas = {
  conversion_factor: z.number(),
  priority: z.number().int(),
  is_active: z.boolean(),
  auto_substitute: z.boolean(),
  valid_from: z.string().datetime({ offset: true }).optional().nullable(),
  valid_until: z.string().datetime({ offset: true }).optional().nullable(),
  notes: z.string().optional().nullable(),
} as const;

/** Create schema for MaterialSubstitutionRule */
export const baseMaterialSubstitutionRuleCreateSchema = z.object({
  conversion_factor: baseMaterialSubstitutionRuleSchemas.conversion_factor,
  priority: baseMaterialSubstitutionRuleSchemas.priority,
  is_active: baseMaterialSubstitutionRuleSchemas.is_active,
  auto_substitute: baseMaterialSubstitutionRuleSchemas.auto_substitute,
  valid_from: baseMaterialSubstitutionRuleSchemas.valid_from,
  valid_until: baseMaterialSubstitutionRuleSchemas.valid_until,
  notes: baseMaterialSubstitutionRuleSchemas.notes,
});

/** Update schema for MaterialSubstitutionRule */
export const baseMaterialSubstitutionRuleUpdateSchema = baseMaterialSubstitutionRuleCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMaterialSubstitutionRuleCreate = z.infer<typeof baseMaterialSubstitutionRuleCreateSchema>;
export type BaseMaterialSubstitutionRuleUpdate = z.infer<typeof baseMaterialSubstitutionRuleUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMaterialSubstitutionRuleLabel(locale: string): string {
  return materialSubstitutionRuleI18n.label[locale as keyof typeof materialSubstitutionRuleI18n.label] ?? materialSubstitutionRuleI18n.label['en'] ?? 'MaterialSubstitutionRule';
}

/** Get field label for a specific locale */
export function getMaterialSubstitutionRuleFieldLabel(field: string, locale: string): string {
  const fields = materialSubstitutionRuleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMaterialSubstitutionRuleFieldPlaceholder(field: string, locale: string): string {
  const fields = materialSubstitutionRuleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MaterialSubstitutionRule.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const materialSubstitutionRuleMetadata = {
  modelName: "MaterialSubstitutionRule",
  table: "material_substitution_rules",

  fields: {
    primary_material_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Material",
        table: "materials",
        onDelete: "CASCADE",
      },
      label: {},
    },
    substitute_material_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Material",
        table: "materials",
        onDelete: "CASCADE",
      },
      label: {},
    },
    conversion_factor: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "換算係数",
        en: "Conversion Factor",
        vi: "Hệ số quy đổi",
      },
      placeholder: {
        ja: "換算係数を入力",
        en: "Enter Conversion Factor",
        vi: "Nhập Hệ số quy đổi",
      },
      description: {
        ja: "Qty multiplier. 1.0 = 1:1 replacement. 1.1 = need 10% more substitute.",
        en: "Qty multiplier. 1.0 = 1:1 replacement. 1.1 = need 10% more substitute.",
        vi: "Qty multiplier. 1.0 = 1:1 replacement. 1.1 = need 10% more substitute.",
      },
    },
    priority: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "優先度",
        en: "Priority",
        vi: "Ưu tiên",
      },
      placeholder: {
        ja: "優先度を入力",
        en: "Enter Priority",
        vi: "Nhập Ưu tiên",
      },
      description: {
        ja: "Lower number = higher priority. Used to order fallback candidates.",
        en: "Lower number = higher priority. Used to order fallback candidates.",
        vi: "Lower number = higher priority. Used to order fallback candidates.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    auto_substitute: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "自動代替",
        en: "Auto Substitute",
        vi: "Tự động thay thế",
      },
      placeholder: {
        ja: "自動代替を入力",
        en: "Enter Auto Substitute",
        vi: "Nhập Tự động thay thế",
      },
      description: {
        ja: "Off by default. When true, pickLotsForConsumption falls back to the substitute automatically once the primary is exhausted.",
        en: "Off by default. When true, pickLotsForConsumption falls back to the substitute automatically once the primary is exhausted.",
        vi: "Off by default. When true, pickLotsForConsumption falls back to the substitute automatically once the primary is exhausted.",
      },
    },
    valid_from: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "有効開始",
        en: "Valid From",
        vi: "Hiệu lực từ",
      },
      placeholder: {
        ja: "有効開始を入力",
        en: "Enter Valid From",
        vi: "Nhập Hiệu lực từ",
      },
    },
    valid_until: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "有効終了",
        en: "Valid Until",
        vi: "Hiệu lực đến",
      },
      placeholder: {
        ja: "有効終了を入力",
        en: "Enter Valid Until",
        vi: "Nhập Hiệu lực đến",
      },
    },
    notes: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "メモ",
        en: "Notes",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "メモを入力",
        en: "Enter Notes",
        vi: "Nhập Ghi chú",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["primary_material_id", "substitute_material_id", "conversion_factor", "priority", "is_active", "auto_substitute", "organization_id", "brand_id"] as const,
  enumFields: [] as const,
  relationFields: ["primary_material_id", "substitute_material_id", "organization_id", "brand_id"] as const,
} as const;

export type MaterialSubstitutionRuleFieldName = keyof typeof materialSubstitutionRuleMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MaterialSubstitutionRule. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MaterialSubstitutionRuleCreateFormState {
  primary_material_id: string;
  substitute_material_id: string;
  conversion_factor: number;
  priority: number;
  is_active: boolean;
  auto_substitute: boolean;
  valid_from?: string | null;
  valid_until?: string | null;
  notes?: string | null;
  organization_id: string;
  brand_id: string;
}

/** Form-state shape for updating a MaterialSubstitutionRule. Every field optional. */
export interface MaterialSubstitutionRuleUpdateFormState {
  primary_material_id?: string;
  substitute_material_id?: string;
  conversion_factor?: number;
  priority?: number;
  is_active?: boolean;
  auto_substitute?: boolean;
  valid_from?: string | null;
  valid_until?: string | null;
  notes?: string | null;
  organization_id?: string;
  brand_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMaterialSubstitutionRuleCreateForm(): MaterialSubstitutionRuleCreateFormState {
  return {
    primary_material_id: '',
    substitute_material_id: '',
    conversion_factor: 1,
    priority: 0,
    is_active: true,
    auto_substitute: false,
    valid_from: '',
    valid_until: '',
    notes: '',
    organization_id: '',
    brand_id: '',
  };
}

/** Convert create form state into the payload that baseMaterialSubstitutionRuleCreateSchema accepts. */
export function buildMaterialSubstitutionRuleCreatePayload(form: MaterialSubstitutionRuleCreateFormState): BaseMaterialSubstitutionRuleCreate {
  const payload: Record<string, unknown> = {
    primary_material_id: form.primary_material_id.trim(),
    substitute_material_id: form.substitute_material_id.trim(),
    conversion_factor: form.conversion_factor,
    priority: form.priority,
    is_active: form.is_active,
    auto_substitute: form.auto_substitute,
    valid_from: form.valid_from,
    valid_until: form.valid_until,
    notes: form.notes?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
  };

  return payload as BaseMaterialSubstitutionRuleCreate;
}

/** Convert update form state into the payload that baseMaterialSubstitutionRuleUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMaterialSubstitutionRuleUpdatePayload(form: MaterialSubstitutionRuleUpdateFormState): BaseMaterialSubstitutionRuleUpdate {
  const payload: Record<string, unknown> = {};
  if (form.primary_material_id !== undefined) {
    payload.primary_material_id = form.primary_material_id?.toString().trim() ?? null;
  }
  if (form.substitute_material_id !== undefined) {
    payload.substitute_material_id = form.substitute_material_id?.toString().trim() ?? null;
  }
  if (form.conversion_factor !== undefined) {
    payload.conversion_factor = form.conversion_factor;
  }
  if (form.priority !== undefined) {
    payload.priority = form.priority;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.auto_substitute !== undefined) {
    payload.auto_substitute = form.auto_substitute;
  }
  if (form.valid_from !== undefined) {
    payload.valid_from = form.valid_from;
  }
  if (form.valid_until !== undefined) {
    payload.valid_until = form.valid_until;
  }
  if (form.notes !== undefined) {
    payload.notes = form.notes?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  return payload as BaseMaterialSubstitutionRuleUpdate;
}
