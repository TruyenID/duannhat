/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Permission } from './Permission';
import type { Role } from './Role';

/**
 * ロール権限
 */
export interface RolePermission {
  role_id: string;
  role?: Role;
  permission_id: string;
  permission?: Permission;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for RolePermission
 */
export const rolePermissionI18n = {
  label: {"ja":"ロール権限","en":"Role Permission","vi":"Quyền vai trò"},
  fields: {
    role: {
      label: {"ja":"ロール","en":"Role","vi":"Vai trò"},
      placeholder: {"ja":"ロールを入力","en":"Enter Role","vi":"Nhập Vai trò"},
    },
    permission: {
      label: {"ja":"権限","en":"Permission","vi":"Quyền hạn"},
      placeholder: {"ja":"権限を入力","en":"Enter Permission","vi":"Nhập Quyền hạn"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for RolePermission */
export const baseRolePermissionSchemas = {
} as const;

/** Create schema for RolePermission */
export const baseRolePermissionCreateSchema = z.object({
});

/** Update schema for RolePermission */
export const baseRolePermissionUpdateSchema = baseRolePermissionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseRolePermissionCreate = z.infer<typeof baseRolePermissionCreateSchema>;
export type BaseRolePermissionUpdate = z.infer<typeof baseRolePermissionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getRolePermissionLabel(locale: string): string {
  return rolePermissionI18n.label[locale as keyof typeof rolePermissionI18n.label] ?? rolePermissionI18n.label['en'] ?? 'RolePermission';
}

/** Get field label for a specific locale */
export function getRolePermissionFieldLabel(field: string, locale: string): string {
  const fieldI18n = rolePermissionI18n.fields[field as keyof typeof rolePermissionI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getRolePermissionFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = rolePermissionI18n.fields[field as keyof typeof rolePermissionI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for RolePermission.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const rolePermissionMetadata = {
  modelName: "RolePermission",
  table: "role_permissions",

  fields: {
    role_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Role",
        table: "roles",
        onDelete: undefined,
      },
      label: {},
    },
    permission_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Permission",
        table: "permissions",
        onDelete: undefined,
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["role_id", "permission_id"] as const,
  enumFields: [] as const,
  relationFields: ["role_id", "permission_id"] as const,
} as const;

export type RolePermissionFieldName = keyof typeof rolePermissionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a RolePermission. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface RolePermissionCreateFormState {
  role_id: string;
  permission_id: string;
}

/** Form-state shape for updating a RolePermission. Every field optional. */
export interface RolePermissionUpdateFormState {
  role_id?: string;
  permission_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyRolePermissionCreateForm(): RolePermissionCreateFormState {
  return {
    role_id: '',
    permission_id: '',
  };
}

/** Convert create form state into the payload that baseRolePermissionCreateSchema accepts. */
export function buildRolePermissionCreatePayload(form: RolePermissionCreateFormState): BaseRolePermissionCreate {
  const payload: Record<string, unknown> = {
    role_id: form.role_id.trim(),
    permission_id: form.permission_id.trim(),
  };

  return payload as BaseRolePermissionCreate;
}

/** Convert update form state into the payload that baseRolePermissionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildRolePermissionUpdatePayload(form: RolePermissionUpdateFormState): BaseRolePermissionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.role_id !== undefined) {
    payload.role_id = form.role_id?.toString().trim() ?? null;
  }
  if (form.permission_id !== undefined) {
    payload.permission_id = form.permission_id?.toString().trim() ?? null;
  }
  return payload as BaseRolePermissionUpdate;
}
