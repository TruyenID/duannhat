/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Material } from './Material';

/**
 * 材料単位
 */
export interface MaterialUnit {
  id: string;
  material_id: string;
  material?: Material;
  unit: string;
  ratio: number;
  is_base: boolean;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MaterialUnit
 */
export const materialUnitI18n = {
  label: {"ja":"材料単位","en":"Material Unit","vi":"Đơn vị nguyên liệu"},
  fields: {
    material: {
      label: {"ja":"材料","en":"Material","vi":"Nguyên liệu"},
      placeholder: {"ja":"材料を入力","en":"Enter Material","vi":"Nhập Nguyên liệu"},
    },
    unit: {
      label: {"ja":"単位","en":"Unit","vi":"Đơn vị"},
      placeholder: {"ja":"単位を入力","en":"Enter Unit","vi":"Nhập Đơn vị"},
    },
    ratio: {
      label: {"ja":"比率","en":"Ratio","vi":"Tỷ lệ"},
      placeholder: {"ja":"比率を入力","en":"Enter Ratio","vi":"Nhập Tỷ lệ"},
    },
    is_base: {
      label: {"ja":"基本単位","en":"Is Base Unit","vi":"Đơn vị cơ bản"},
      placeholder: {"ja":"基本単位を入力","en":"Enter Is Base Unit","vi":"Nhập Đơn vị cơ bản"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MaterialUnit */
export const baseMaterialUnitSchemas = {
  unit: z.string().min(1).max(50),
  ratio: z.number(),
  is_base: z.boolean(),
} as const;

/** Create schema for MaterialUnit */
export const baseMaterialUnitCreateSchema = z.object({
  unit: baseMaterialUnitSchemas.unit,
  ratio: baseMaterialUnitSchemas.ratio,
  is_base: baseMaterialUnitSchemas.is_base,
});

/** Update schema for MaterialUnit */
export const baseMaterialUnitUpdateSchema = baseMaterialUnitCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMaterialUnitCreate = z.infer<typeof baseMaterialUnitCreateSchema>;
export type BaseMaterialUnitUpdate = z.infer<typeof baseMaterialUnitUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMaterialUnitLabel(locale: string): string {
  return materialUnitI18n.label[locale as keyof typeof materialUnitI18n.label] ?? materialUnitI18n.label['en'] ?? 'MaterialUnit';
}

/** Get field label for a specific locale */
export function getMaterialUnitFieldLabel(field: string, locale: string): string {
  const fields = materialUnitI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMaterialUnitFieldPlaceholder(field: string, locale: string): string {
  const fields = materialUnitI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MaterialUnit.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const materialUnitMetadata = {
  modelName: "MaterialUnit",
  table: "material_units",

  fields: {
    material_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Material",
        table: "materials",
        onDelete: "CASCADE",
      },
      label: {},
    },
    unit: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "単位",
        en: "Unit",
        vi: "Đơn vị",
      },
      placeholder: {
        ja: "単位を入力",
        en: "Enter Unit",
        vi: "Nhập Đơn vị",
      },
      description: {
        ja: "単位名（例：袋、箱、カートン）",
        en: "Unit name (e.g., bag, box, carton)",
        vi: "Tên đơn vị (VD: bao, thùng, hộp)",
      },
    },
    ratio: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "比率",
        en: "Ratio",
        vi: "Tỷ lệ",
      },
      placeholder: {
        ja: "比率を入力",
        en: "Enter Ratio",
        vi: "Nhập Tỷ lệ",
      },
      description: {
        ja: "換算比率: 1 [単位] = ratio × 基本単位",
        en: "Conversion ratio: 1 [unit] = ratio x base_unit",
        vi: "Tỷ lệ quy đổi: 1 [đơn vị] = ratio x đơn vị cơ bản",
      },
    },
    is_base: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "基本単位",
        en: "Is Base Unit",
        vi: "Đơn vị cơ bản",
      },
      placeholder: {
        ja: "基本単位を入力",
        en: "Enter Is Base Unit",
        vi: "Nhập Đơn vị cơ bản",
      },
      description: {
        ja: "材料ごとに1つの基本単位のみ（同期時に自動作成、BR-09）",
        en: "Only one base unit per material (auto-created during sync, BR-09)",
        vi: "Chỉ 1 đơn vị cơ bản mỗi NVL (tự tạo khi sync, BR-09)",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["material_id", "unit", "ratio", "is_base"] as const,
  enumFields: [] as const,
  relationFields: ["material_id"] as const,
} as const;

export type MaterialUnitFieldName = keyof typeof materialUnitMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MaterialUnit. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MaterialUnitCreateFormState {
  material_id: string;
  unit: string;
  ratio: number;
  is_base: boolean;
}

/** Form-state shape for updating a MaterialUnit. Every field optional. */
export interface MaterialUnitUpdateFormState {
  material_id?: string;
  unit?: string;
  ratio?: number;
  is_base?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMaterialUnitCreateForm(): MaterialUnitCreateFormState {
  return {
    material_id: '',
    unit: '',
    ratio: 1,
    is_base: false,
  };
}

/** Convert create form state into the payload that baseMaterialUnitCreateSchema accepts. */
export function buildMaterialUnitCreatePayload(form: MaterialUnitCreateFormState): BaseMaterialUnitCreate {
  const payload: Record<string, unknown> = {
    material_id: form.material_id.trim(),
    unit: form.unit.trim(),
    ratio: form.ratio,
    is_base: form.is_base,
  };

  return payload as BaseMaterialUnitCreate;
}

/** Convert update form state into the payload that baseMaterialUnitUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMaterialUnitUpdatePayload(form: MaterialUnitUpdateFormState): BaseMaterialUnitUpdate {
  const payload: Record<string, unknown> = {};
  if (form.material_id !== undefined) {
    payload.material_id = form.material_id?.toString().trim() ?? null;
  }
  if (form.unit !== undefined) {
    payload.unit = form.unit?.toString().trim() ?? null;
  }
  if (form.ratio !== undefined) {
    payload.ratio = form.ratio;
  }
  if (form.is_base !== undefined) {
    payload.is_base = form.is_base;
  }
  return payload as BaseMaterialUnitUpdate;
}
