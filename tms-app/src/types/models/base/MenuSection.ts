/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Brand } from './Brand';
import type { Menu } from './Menu';
import type { Organization } from './Organization';

/**
 * メニューセクション
 */
export interface MenuSection {
  id: string;
  name: string;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  menus?: Menu[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuSection
 */
export const menuSectionI18n = {
  label: {"ja":"メニューセクション","en":"Menu Section","vi":"Nhom danh muc Menu"},
  fields: {
    name: {
      label: {"ja":"セクション名","en":"Section Name","vi":"Ten nhom"},
      placeholder: {"ja":"セクション名を入力","en":"Enter Section Name","vi":"Nhập Ten nhom"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    menus: {
      label: {"ja":"メニュー","en":"Menus","vi":"Cac menu"},
      placeholder: {"ja":"メニューを入力","en":"Enter Menus","vi":"Nhập Cac menu"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuSection */
export const baseMenuSectionSchemas = {
  name: z.string().min(1).max(255),
} as const;

/** Create schema for MenuSection */
export const baseMenuSectionCreateSchema = z.object({
  name: baseMenuSectionSchemas.name,
  ja: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
});

/** Update schema for MenuSection */
export const baseMenuSectionUpdateSchema = baseMenuSectionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuSectionCreate = z.infer<typeof baseMenuSectionCreateSchema>;
export type BaseMenuSectionUpdate = z.infer<typeof baseMenuSectionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuSectionLabel(locale: string): string {
  return menuSectionI18n.label[locale as keyof typeof menuSectionI18n.label] ?? menuSectionI18n.label['en'] ?? 'MenuSection';
}

/** Get field label for a specific locale */
export function getMenuSectionFieldLabel(field: string, locale: string): string {
  const fields = menuSectionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuSectionFieldPlaceholder(field: string, locale: string): string {
  const fields = menuSectionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuSection.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuSectionMetadata = {
  modelName: "MenuSection",
  table: "menu_sections",

  fields: {
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "セクション名",
        en: "Section Name",
        vi: "Ten nhom",
      },
      placeholder: {
        ja: "セクション名を入力",
        en: "Enter Section Name",
        vi: "Nhập Ten nhom",
      },
      description: {
        ja: "メニューセクションの表示名",
        en: "Display name for this menu section",
        vi: "Ten hien thi cua nhom danh muc",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name"] as const,
  requiredFields: ["name", "organization_id", "brand_id"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id"] as const,
} as const;

export type MenuSectionFieldName = keyof typeof menuSectionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuSection. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuSectionCreateFormState {
  name: Record<Locale, string>;
  organization_id: string;
  brand_id: string;
}

/** Form-state shape for updating a MenuSection. Every field optional. */
export interface MenuSectionUpdateFormState {
  name?: Record<Locale, string>;
  organization_id?: string;
  brand_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuSectionCreateForm(): MenuSectionCreateFormState {
  return {
    name: emptyLocaleMap(),
    organization_id: '',
    brand_id: '',
  };
}

/** Convert create form state into the payload that baseMenuSectionCreateSchema accepts. */
export function buildMenuSectionCreatePayload(form: MenuSectionCreateFormState): BaseMenuSectionCreate {
  const payload: Record<string, unknown> = {
    name: (form.name["ja"] ?? '').trim(),
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
  });
  Object.assign(payload, i18n);

  return payload as BaseMenuSectionCreate;
}

/** Convert update form state into the payload that baseMenuSectionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuSectionUpdatePayload(form: MenuSectionUpdateFormState): BaseMenuSectionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseMenuSectionUpdate;
}
