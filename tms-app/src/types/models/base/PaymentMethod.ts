/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Organization } from './Organization';

/**
 * 支払方法
 */
export interface PaymentMethod {
  id: string;
  scope_key: string;
  code: string;
  name: string;
  type: 'cash' | 'card' | 'transfer' | 'qr' | 'voucher' | 'on_account' | 'other';
  is_auto_confirm: boolean;
  requires_tendered: boolean;
  is_active: boolean;
  sort_order: number;
  branch_id?: string | null;
  branch?: Branch | null;
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PaymentMethod
 */
export const paymentMethodI18n = {
  label: {"ja":"支払方法","en":"Payment Method","vi":"Phương thức thanh toán"},
  fields: {
    scope_key: {
      label: {"ja":"スコープキー","en":"Scope Key","vi":"Khoá phạm vi"},
      placeholder: {"ja":"スコープキーを入力","en":"Enter Scope Key","vi":"Nhập Khoá phạm vi"},
    },
    code: {
      label: {"ja":"コード","en":"Code","vi":"Mã"},
      placeholder: {"ja":"コードを入力","en":"Enter Code","vi":"Nhập Mã"},
    },
    name: {
      label: {"ja":"名前","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名前を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    type: {
      label: {"ja":"支払タイプ","en":"Payment Type","vi":"Loại thanh toán"},
      placeholder: {"ja":"支払タイプを入力","en":"Enter Payment Type","vi":"Nhập Loại thanh toán"},
    },
    is_auto_confirm: {
      label: {"ja":"自動確認","en":"Auto Confirm","vi":"Tự động xác nhận"},
      placeholder: {"ja":"自動確認を入力","en":"Enter Auto Confirm","vi":"Nhập Tự động xác nhận"},
    },
    requires_tendered: {
      label: {"ja":"受取金額必須","en":"Requires Tendered Amount","vi":"Yêu cầu tiền đưa"},
      placeholder: {"ja":"受取金額必須を入力","en":"Enter Requires Tendered Amount","vi":"Nhập Yêu cầu tiền đưa"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    sort_order: {
      label: {"ja":"表示順","en":"Sort Order","vi":"Thứ tự"},
      placeholder: {"ja":"表示順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PaymentMethod */
export const basePaymentMethodSchemas = {
  scope_key: z.string().min(1).max(36),
  code: z.string().min(1).max(50),
  name: z.string().min(1).max(255),
  type: z.enum(['cash', 'card', 'transfer', 'qr', 'voucher', 'on_account', 'other']),
  is_auto_confirm: z.boolean(),
  requires_tendered: z.boolean(),
  is_active: z.boolean(),
  sort_order: z.number().int(),
} as const;

/** Create schema for PaymentMethod */
export const basePaymentMethodCreateSchema = z.object({
  scope_key: basePaymentMethodSchemas.scope_key,
  code: basePaymentMethodSchemas.code,
  name: basePaymentMethodSchemas.name,
  type: basePaymentMethodSchemas.type,
  is_auto_confirm: basePaymentMethodSchemas.is_auto_confirm,
  requires_tendered: basePaymentMethodSchemas.requires_tendered,
  is_active: basePaymentMethodSchemas.is_active,
  sort_order: basePaymentMethodSchemas.sort_order,
  ja: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
});

/** Update schema for PaymentMethod */
export const basePaymentMethodUpdateSchema = basePaymentMethodCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePaymentMethodCreate = z.infer<typeof basePaymentMethodCreateSchema>;
export type BasePaymentMethodUpdate = z.infer<typeof basePaymentMethodUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPaymentMethodLabel(locale: string): string {
  return paymentMethodI18n.label[locale as keyof typeof paymentMethodI18n.label] ?? paymentMethodI18n.label['en'] ?? 'PaymentMethod';
}

/** Get field label for a specific locale */
export function getPaymentMethodFieldLabel(field: string, locale: string): string {
  const fields = paymentMethodI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPaymentMethodFieldPlaceholder(field: string, locale: string): string {
  const fields = paymentMethodI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PaymentMethod.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const paymentMethodMetadata = {
  modelName: "PaymentMethod",
  table: "payment_methods",

  fields: {
    scope_key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 36,
      },
      label: {
        ja: "スコープキー",
        en: "Scope Key",
        vi: "Khoá phạm vi",
      },
      placeholder: {
        ja: "スコープキーを入力",
        en: "Enter Scope Key",
        vi: "Nhập Khoá phạm vi",
      },
    },
    code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "コード",
        en: "Code",
        vi: "Mã",
      },
      placeholder: {
        ja: "コードを入力",
        en: "Enter Code",
        vi: "Nhập Mã",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "名前",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名前を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enumValues: ["cash", "card", "transfer", "qr", "voucher", "on_account", "other"] as const,
      label: {
        ja: "支払タイプ",
        en: "Payment Type",
        vi: "Loại thanh toán",
      },
      placeholder: {
        ja: "支払タイプを入力",
        en: "Enter Payment Type",
        vi: "Nhập Loại thanh toán",
      },
      description: {
        ja: "安定した動作分類。`on_account` は売掛を記録し、外部資金移動ではありません。",
        en: "Stable behavior category. `on_account` records debt and is not external money movement.",
        vi: "Phân loại hành vi ổn định. `on_account` ghi nhận công nợ, không chuyển tiền bên ngoài.",
      },
    },
    is_auto_confirm: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "自動確認",
        en: "Auto Confirm",
        vi: "Tự động xác nhận",
      },
      placeholder: {
        ja: "自動確認を入力",
        en: "Enter Auto Confirm",
        vi: "Nhập Tự động xác nhận",
      },
    },
    requires_tendered: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "受取金額必須",
        en: "Requires Tendered Amount",
        vi: "Yêu cầu tiền đưa",
      },
      placeholder: {
        ja: "受取金額必須を入力",
        en: "Enter Requires Tendered Amount",
        vi: "Nhập Yêu cầu tiền đưa",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Sort Order",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự",
      },
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name"] as const,
  requiredFields: ["scope_key", "code", "name", "type", "is_auto_confirm", "requires_tendered", "is_active", "sort_order", "organization_id"] as const,
  enumFields: ["type"] as const,
  relationFields: ["branch_id", "organization_id"] as const,
} as const;

export type PaymentMethodFieldName = keyof typeof paymentMethodMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PaymentMethod. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PaymentMethodCreateFormState {
  scope_key: string;
  code: string;
  name: Record<Locale, string>;
  type: 'cash' | 'card' | 'transfer' | 'qr' | 'voucher' | 'on_account' | 'other';
  is_auto_confirm: boolean;
  requires_tendered: boolean;
  is_active: boolean;
  sort_order: number;
  branch_id?: string | null;
  organization_id: string;
}

/** Form-state shape for updating a PaymentMethod. Every field optional. */
export interface PaymentMethodUpdateFormState {
  scope_key?: string;
  code?: string;
  name?: Record<Locale, string>;
  type?: 'cash' | 'card' | 'transfer' | 'qr' | 'voucher' | 'on_account' | 'other';
  is_auto_confirm?: boolean;
  requires_tendered?: boolean;
  is_active?: boolean;
  sort_order?: number;
  branch_id?: string | null;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPaymentMethodCreateForm(): PaymentMethodCreateFormState {
  return {
    scope_key: '',
    code: '',
    name: emptyLocaleMap(),
    type: "cash",
    is_auto_confirm: false,
    requires_tendered: false,
    is_active: true,
    sort_order: 0,
    branch_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that basePaymentMethodCreateSchema accepts. */
export function buildPaymentMethodCreatePayload(form: PaymentMethodCreateFormState): BasePaymentMethodCreate {
  const payload: Record<string, unknown> = {
    scope_key: form.scope_key.trim(),
    code: form.code.trim(),
    name: (form.name["ja"] ?? '').trim(),
    type: form.type,
    is_auto_confirm: form.is_auto_confirm,
    requires_tendered: form.requires_tendered,
    is_active: form.is_active,
    sort_order: form.sort_order,
    branch_id: form.branch_id?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
  });
  Object.assign(payload, i18n);

  return payload as BasePaymentMethodCreate;
}

/** Convert update form state into the payload that basePaymentMethodUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPaymentMethodUpdatePayload(form: PaymentMethodUpdateFormState): BasePaymentMethodUpdate {
  const payload: Record<string, unknown> = {};
  if (form.scope_key !== undefined) {
    payload.scope_key = form.scope_key?.toString().trim() ?? null;
  }
  if (form.code !== undefined) {
    payload.code = form.code?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.type !== undefined) {
    payload.type = form.type;
  }
  if (form.is_auto_confirm !== undefined) {
    payload.is_auto_confirm = form.is_auto_confirm;
  }
  if (form.requires_tendered !== undefined) {
    payload.requires_tendered = form.requires_tendered;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BasePaymentMethodUpdate;
}
