/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * AllergenMaterial
 */
export interface AllergenMaterial {
  id: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for AllergenMaterial
 */
export const allergenMaterialI18n = {
  label: {"ja":"AllergenMaterial","en":"AllergenMaterial","vi":"AllergenMaterial"},
  fields: {
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for AllergenMaterial */
export const baseAllergenMaterialSchemas = {
} as const;

/** Create schema for AllergenMaterial */
export const baseAllergenMaterialCreateSchema = z.object({
});

/** Update schema for AllergenMaterial */
export const baseAllergenMaterialUpdateSchema = baseAllergenMaterialCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseAllergenMaterialCreate = z.infer<typeof baseAllergenMaterialCreateSchema>;
export type BaseAllergenMaterialUpdate = z.infer<typeof baseAllergenMaterialUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getAllergenMaterialLabel(locale: string): string {
  return allergenMaterialI18n.label[locale as keyof typeof allergenMaterialI18n.label] ?? allergenMaterialI18n.label['en'] ?? 'AllergenMaterial';
}

/** Get field label for a specific locale */
export function getAllergenMaterialFieldLabel(field: string, locale: string): string {
  const fields = allergenMaterialI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getAllergenMaterialFieldPlaceholder(field: string, locale: string): string {
  const fields = allergenMaterialI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for AllergenMaterial.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const allergenMaterialMetadata = {
  modelName: "AllergenMaterial",
  table: "allergen_materials",

  fields: {
  },

  translatableFields: [] as const,
  requiredFields: [] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type AllergenMaterialFieldName = keyof typeof allergenMaterialMetadata.fields;

