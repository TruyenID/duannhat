/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Product } from './Product';
import type { ToppingGroup } from './ToppingGroup';
import type { ToppingGroupItemSku } from './ToppingGroupItemSku';

/**
 * トッピンググループ項目
 */
export interface ToppingGroupItem {
  id: string;
  toppingGroup_id: string;
  toppingGroup?: ToppingGroup;
  product_id: string;
  product?: Product;
  sort_order: number;
  is_default: boolean;
  skus?: ToppingGroupItemSku[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ToppingGroupItem
 */
export const toppingGroupItemI18n = {
  label: {"ja":"トッピンググループ項目","en":"Topping Group Item","vi":"Mục Topping trong Nhóm"},
  fields: {
    topping_group: {
      label: {"ja":"トッピンググループ","en":"Topping Group","vi":"Nhóm Topping"},
      placeholder: {"ja":"トッピンググループを入力","en":"Enter Topping Group","vi":"Nhập Nhóm Topping"},
    },
    product: {
      label: {"ja":"商品","en":"Product","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Product","vi":"Nhập Sản phẩm"},
    },
    sort_order: {
      label: {"ja":"表示順序","en":"Sort Order","vi":"Thứ tự hiển thị"},
      placeholder: {"ja":"表示順序を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự hiển thị"},
    },
    is_default: {
      label: {"ja":"デフォルト選択","en":"Default Included","vi":"Chọn sẵn"},
      placeholder: {"ja":"デフォルト選択を入力","en":"Enter Default Included","vi":"Nhập Chọn sẵn"},
    },
    skus: {
      label: {"ja":"SKU価格","en":"SKU Prices","vi":"Giá theo SKU"},
      placeholder: {"ja":"SKU価格を入力","en":"Enter SKU Prices","vi":"Nhập Giá theo SKU"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ToppingGroupItem */
export const baseToppingGroupItemSchemas = {
  sort_order: z.number().int(),
  is_default: z.boolean(),
} as const;

/** Create schema for ToppingGroupItem */
export const baseToppingGroupItemCreateSchema = z.object({
  sort_order: baseToppingGroupItemSchemas.sort_order,
  is_default: baseToppingGroupItemSchemas.is_default,
});

/** Update schema for ToppingGroupItem */
export const baseToppingGroupItemUpdateSchema = baseToppingGroupItemCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseToppingGroupItemCreate = z.infer<typeof baseToppingGroupItemCreateSchema>;
export type BaseToppingGroupItemUpdate = z.infer<typeof baseToppingGroupItemUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getToppingGroupItemLabel(locale: string): string {
  return toppingGroupItemI18n.label[locale as keyof typeof toppingGroupItemI18n.label] ?? toppingGroupItemI18n.label['en'] ?? 'ToppingGroupItem';
}

/** Get field label for a specific locale */
export function getToppingGroupItemFieldLabel(field: string, locale: string): string {
  const fields = toppingGroupItemI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getToppingGroupItemFieldPlaceholder(field: string, locale: string): string {
  const fields = toppingGroupItemI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ToppingGroupItem.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const toppingGroupItemMetadata = {
  modelName: "ToppingGroupItem",
  table: "topping_group_items",

  fields: {
    topping_group_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ToppingGroup",
        table: "topping_groups",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Product",
        table: "products",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順序",
        en: "Sort Order",
        vi: "Thứ tự hiển thị",
      },
      placeholder: {
        ja: "表示順序を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự hiển thị",
      },
    },
    is_default: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "デフォルト選択",
        en: "Default Included",
        vi: "Chọn sẵn",
      },
      placeholder: {
        ja: "デフォルト選択を入力",
        en: "Enter Default Included",
        vi: "Nhập Chọn sẵn",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["topping_group_id", "product_id", "sort_order", "is_default"] as const,
  enumFields: [] as const,
  relationFields: ["topping_group_id", "product_id"] as const,
} as const;

export type ToppingGroupItemFieldName = keyof typeof toppingGroupItemMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ToppingGroupItem. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ToppingGroupItemCreateFormState {
  topping_group_id: string;
  product_id: string;
  sort_order: number;
  is_default: boolean;
}

/** Form-state shape for updating a ToppingGroupItem. Every field optional. */
export interface ToppingGroupItemUpdateFormState {
  topping_group_id?: string;
  product_id?: string;
  sort_order?: number;
  is_default?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyToppingGroupItemCreateForm(): ToppingGroupItemCreateFormState {
  return {
    topping_group_id: '',
    product_id: '',
    sort_order: 0,
    is_default: false,
  };
}

/** Convert create form state into the payload that baseToppingGroupItemCreateSchema accepts. */
export function buildToppingGroupItemCreatePayload(form: ToppingGroupItemCreateFormState): BaseToppingGroupItemCreate {
  const payload: Record<string, unknown> = {
    topping_group_id: form.topping_group_id.trim(),
    product_id: form.product_id.trim(),
    sort_order: form.sort_order,
    is_default: form.is_default,
  };

  return payload as BaseToppingGroupItemCreate;
}

/** Convert update form state into the payload that baseToppingGroupItemUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildToppingGroupItemUpdatePayload(form: ToppingGroupItemUpdateFormState): BaseToppingGroupItemUpdate {
  const payload: Record<string, unknown> = {};
  if (form.topping_group_id !== undefined) {
    payload.topping_group_id = form.topping_group_id?.toString().trim() ?? null;
  }
  if (form.product_id !== undefined) {
    payload.product_id = form.product_id?.toString().trim() ?? null;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.is_default !== undefined) {
    payload.is_default = form.is_default;
  }
  return payload as BaseToppingGroupItemUpdate;
}
