/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { User } from './User';

/**
 * 通知オーディエンス
 */
export interface NotificationAudience {
  id: string;
  organization_id: string;
  organization?: Organization;
  brand_id?: string | null;
  brand?: Brand | null;
  branch_id?: string | null;
  branch?: Branch | null;
  name: string;
  description?: string | null;
  rule: unknown;
  is_system: boolean;
  createdBy_id?: string | null;
  createdBy?: User | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationAudience
 */
export const notificationAudienceI18n = {
  label: {"ja":"通知オーディエンス","en":"Notification Audience","vi":"Đối tượng thông báo"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"店舗","en":"Shop","vi":"Cửa hàng"},
      placeholder: {"ja":"店舗を入力","en":"Enter Shop","vi":"Nhập Cửa hàng"},
    },
    name: {
      label: {"ja":"名前","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名前を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    rule: {
      label: {"ja":"ルール","en":"Rule","vi":"Quy tắc"},
      placeholder: {"ja":"ルールを入力","en":"Enter Rule","vi":"Nhập Quy tắc"},
    },
    is_system: {
      label: {"ja":"システム","en":"Is System","vi":"Hệ thống"},
      placeholder: {"ja":"システムを入力","en":"Enter Is System","vi":"Nhập Hệ thống"},
    },
    created_by: {
      label: {"ja":"作成者","en":"Created By","vi":"Người tạo"},
      placeholder: {"ja":"作成者を入力","en":"Enter Created By","vi":"Nhập Người tạo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationAudience */
export const baseNotificationAudienceSchemas = {
  name: z.string().min(1).max(160),
  description: z.string().optional().nullable(),
  rule: z.unknown(),
  is_system: z.boolean(),
} as const;

/** Create schema for NotificationAudience */
export const baseNotificationAudienceCreateSchema = z.object({
  name: baseNotificationAudienceSchemas.name,
  description: baseNotificationAudienceSchemas.description,
  rule: baseNotificationAudienceSchemas.rule,
  is_system: baseNotificationAudienceSchemas.is_system,
});

/** Update schema for NotificationAudience */
export const baseNotificationAudienceUpdateSchema = baseNotificationAudienceCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationAudienceCreate = z.infer<typeof baseNotificationAudienceCreateSchema>;
export type BaseNotificationAudienceUpdate = z.infer<typeof baseNotificationAudienceUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationAudienceLabel(locale: string): string {
  return notificationAudienceI18n.label[locale as keyof typeof notificationAudienceI18n.label] ?? notificationAudienceI18n.label['en'] ?? 'NotificationAudience';
}

/** Get field label for a specific locale */
export function getNotificationAudienceFieldLabel(field: string, locale: string): string {
  const fields = notificationAudienceI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationAudienceFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationAudienceI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationAudience.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationAudienceMetadata = {
  modelName: "NotificationAudience",
  table: "notification_audiences",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 160,
      },
      label: {
        ja: "名前",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名前を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
      description: {
        ja: "コンポーザーのオーディエンスピッカーや管理リストに表示される人間可読なラベル。例：「全倉庫マネージャー」、「ブランドAの店舗スタッフ」、「オンコールエンジニア」。組織内で一意。",
        en: "Human-readable label shown in the compose audience picker and the admin list. Examples: 'All warehouse managers', 'Brand A shop staff', 'On-call engineers'. Unique per organization.",
        vi: "Nhãn human-readable hiển thị ở composer picker và list admin. VD: 'Tất cả quản lý kho', 'Nhân viên shop brand A', 'On-call engineers'. Unique trong phạm vi tổ chức.",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
      description: {
        ja: "オーディエンスの対象と目的についての自由記述メモ。ピッカーでは `name` の下の補助行として描画される。",
        en: "Free-form notes on what the audience targets and why. Rendered as a secondary line under `name` in the picker.",
        vi: "Ghi chú tự do về audience nhắm tới ai và vì sao. Render thành dòng phụ dưới `name` trong picker.",
      },
    },
    rule: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ルール",
        en: "Rule",
        vi: "Quy tắc",
      },
      placeholder: {
        ja: "ルールを入力",
        en: "Enter Rule",
        vi: "Nhập Quy tắc",
      },
      description: {
        ja: "`AudienceResolverService` が消費するルール JSON。形状：`{version, combinator: 'or'|'and', rules: [...], exclude?: [...]}`。各サブルールの `type` は `*Resolver` クラスにマップされる。完全なスキーマと例についてはヘッダーコメントを参照。",
        en: "Rule JSON consumed by `AudienceResolverService`. Shape: `{version, combinator: 'or'|'and', rules: [...], exclude?: [...]}`. Each sub-rule `type` maps to a `*Resolver` class. See header comment for full schema and examples.",
        vi: "Rule JSON mà `AudienceResolverService` consume. Shape: `{version, combinator: 'or'|'and', rules: [...], exclude?: [...]}`. Mỗi sub-rule `type` map tới một class `*Resolver`. Xem header comment để biết schema đầy đủ và ví dụ.",
      },
    },
    is_system: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "システム",
        en: "Is System",
        vi: "Hệ thống",
      },
      placeholder: {
        ja: "システムを入力",
        en: "Enter Is System",
        vi: "Nhập Hệ thống",
      },
      description: {
        ja: "`SystemNotificationAudienceSeeder` によりシードされた行の場合 `true`（例：「全倉庫マネージャー」）。これらの行への PATCH + DELETE は 403 を返す — プラットフォームのベースラインの一部。ユーザー作成行の場合 `false`；完全に編集可能。",
        en: "`true` when seeded by `SystemNotificationAudienceSeeder` (e.g. 'All warehouse managers'). PATCH + DELETE on these rows return 403 — they're part of the platform baseline. `false` for user-created rows; fully editable.",
        vi: "`true` khi được seed bởi `SystemNotificationAudienceSeeder` (VD: 'Tất cả quản lý kho'). PATCH + DELETE vào row này trả về 403 — chúng là một phần baseline của platform. `false` cho row do user tạo; sửa được hoàn toàn.",
      },
    },
    created_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "name", "rule", "is_system"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "branch_id", "created_by_id"] as const,
} as const;

export type NotificationAudienceFieldName = keyof typeof notificationAudienceMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationAudience. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationAudienceCreateFormState {
  organization_id: string;
  brand_id?: string | null;
  branch_id?: string | null;
  name: string;
  description?: string | null;
  rule: unknown;
  is_system: boolean;
  created_by_id?: string | null;
}

/** Form-state shape for updating a NotificationAudience. Every field optional. */
export interface NotificationAudienceUpdateFormState {
  organization_id?: string;
  brand_id?: string | null;
  branch_id?: string | null;
  name?: string;
  description?: string | null;
  rule?: unknown;
  is_system?: boolean;
  created_by_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationAudienceCreateForm(): NotificationAudienceCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    branch_id: '',
    name: '',
    description: '',
    rule: null,
    is_system: false,
    created_by_id: '',
  };
}

/** Convert create form state into the payload that baseNotificationAudienceCreateSchema accepts. */
export function buildNotificationAudienceCreatePayload(form: NotificationAudienceCreateFormState): BaseNotificationAudienceCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id?.toString().trim() || null,
    branch_id: form.branch_id?.toString().trim() || null,
    name: form.name.trim(),
    description: form.description?.toString().trim() || null,
    rule: form.rule,
    is_system: form.is_system,
    created_by_id: form.created_by_id?.toString().trim() || null,
  };

  return payload as BaseNotificationAudienceCreate;
}

/** Convert update form state into the payload that baseNotificationAudienceUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationAudienceUpdatePayload(form: NotificationAudienceUpdateFormState): BaseNotificationAudienceUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.rule !== undefined) {
    payload.rule = form.rule;
  }
  if (form.is_system !== undefined) {
    payload.is_system = form.is_system;
  }
  if (form.created_by_id !== undefined) {
    payload.created_by_id = form.created_by_id?.toString().trim() ?? null;
  }
  return payload as BaseNotificationAudienceUpdate;
}
