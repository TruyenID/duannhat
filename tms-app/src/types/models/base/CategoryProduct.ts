/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * CategoryProduct
 */
export interface CategoryProduct {
  id: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for CategoryProduct
 */
export const categoryProductI18n = {
  label: {"ja":"CategoryProduct","en":"CategoryProduct","vi":"CategoryProduct"},
  fields: {
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for CategoryProduct */
export const baseCategoryProductSchemas = {
} as const;

/** Create schema for CategoryProduct */
export const baseCategoryProductCreateSchema = z.object({
});

/** Update schema for CategoryProduct */
export const baseCategoryProductUpdateSchema = baseCategoryProductCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseCategoryProductCreate = z.infer<typeof baseCategoryProductCreateSchema>;
export type BaseCategoryProductUpdate = z.infer<typeof baseCategoryProductUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getCategoryProductLabel(locale: string): string {
  return categoryProductI18n.label[locale as keyof typeof categoryProductI18n.label] ?? categoryProductI18n.label['en'] ?? 'CategoryProduct';
}

/** Get field label for a specific locale */
export function getCategoryProductFieldLabel(field: string, locale: string): string {
  const fields = categoryProductI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getCategoryProductFieldPlaceholder(field: string, locale: string): string {
  const fields = categoryProductI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for CategoryProduct.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const categoryProductMetadata = {
  modelName: "CategoryProduct",
  table: "category_products",

  fields: {
  },

  translatableFields: [] as const,
  requiredFields: [] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type CategoryProductFieldName = keyof typeof categoryProductMetadata.fields;

