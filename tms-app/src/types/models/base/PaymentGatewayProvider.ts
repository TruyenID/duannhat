/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { PaymentGatewayProviderCode } from '../enum/PaymentGatewayProviderCode';

/**
 * 決済ゲートウェイプロバイダー
 */
export interface PaymentGatewayProvider {
  id: string;
  code: PaymentGatewayProviderCode;
  name: string;
  description?: string | null;
  is_active: boolean;
  sort_order: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PaymentGatewayProvider
 */
export const paymentGatewayProviderI18n = {
  label: {"ja":"決済ゲートウェイプロバイダー","en":"Payment Gateway Provider","vi":"Nhà cung cấp cổng thanh toán"},
  fields: {
    code: {
      label: {"ja":"プロバイダーコード","en":"Provider Code","vi":"Mã nhà cung cấp"},
      placeholder: {"ja":"プロバイダーコードを入力","en":"Enter Provider Code","vi":"Nhập Mã nhà cung cấp"},
    },
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    sort_order: {
      label: {"ja":"表示順","en":"Sort Order","vi":"Thứ tự"},
      placeholder: {"ja":"表示順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PaymentGatewayProvider */
export const basePaymentGatewayProviderSchemas = {
  code: z.nativeEnum(PaymentGatewayProviderCode),
  name: z.string().min(1).max(120),
  description: z.string().optional().nullable(),
  is_active: z.boolean(),
  sort_order: z.number().int(),
} as const;

/** Create schema for PaymentGatewayProvider */
export const basePaymentGatewayProviderCreateSchema = z.object({
  code: basePaymentGatewayProviderSchemas.code,
  name: basePaymentGatewayProviderSchemas.name,
  description: basePaymentGatewayProviderSchemas.description,
  is_active: basePaymentGatewayProviderSchemas.is_active,
  sort_order: basePaymentGatewayProviderSchemas.sort_order,
  ja: z.object({
    name: z.string().max(120).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(120).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(120).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
});

/** Update schema for PaymentGatewayProvider */
export const basePaymentGatewayProviderUpdateSchema = basePaymentGatewayProviderCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePaymentGatewayProviderCreate = z.infer<typeof basePaymentGatewayProviderCreateSchema>;
export type BasePaymentGatewayProviderUpdate = z.infer<typeof basePaymentGatewayProviderUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPaymentGatewayProviderLabel(locale: string): string {
  return paymentGatewayProviderI18n.label[locale as keyof typeof paymentGatewayProviderI18n.label] ?? paymentGatewayProviderI18n.label['en'] ?? 'PaymentGatewayProvider';
}

/** Get field label for a specific locale */
export function getPaymentGatewayProviderFieldLabel(field: string, locale: string): string {
  const fields = paymentGatewayProviderI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPaymentGatewayProviderFieldPlaceholder(field: string, locale: string): string {
  const fields = paymentGatewayProviderI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PaymentGatewayProvider.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const paymentGatewayProviderMetadata = {
  modelName: "PaymentGatewayProvider",
  table: "payment_gateway_providers",

  fields: {
    code: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "PaymentGatewayProviderCode",
      enumValues: ["internal", "stripe", "paypay", "sbps"] as const,
      label: {
        ja: "プロバイダーコード",
        en: "Provider Code",
        vi: "Mã nhà cung cấp",
      },
      placeholder: {
        ja: "プロバイダーコードを入力",
        en: "Enter Provider Code",
        vi: "Nhập Mã nhà cung cấp",
      },
      description: {
        ja: "安定したアダプターレジストリコード。変更は表示編集ではなく互換性移行です。",
        en: "Stable adapter registry code. Changing it is a compatibility migration, not a display edit.",
        vi: "Mã registry adapter ổn định. Đổi mã là migration tương thích, không phải sửa nhãn.",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 120,
      },
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: true,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Sort Order",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự",
      },
    },
  },

  translatableFields: ["name", "description"] as const,
  requiredFields: ["code", "name", "is_active", "sort_order"] as const,
  enumFields: ["code"] as const,
  relationFields: [] as const,
} as const;

export type PaymentGatewayProviderFieldName = keyof typeof paymentGatewayProviderMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PaymentGatewayProvider. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PaymentGatewayProviderCreateFormState {
  code: PaymentGatewayProviderCode;
  name: Record<Locale, string>;
  description: Record<Locale, string>;
  is_active: boolean;
  sort_order: number;
}

/** Form-state shape for updating a PaymentGatewayProvider. Every field optional. */
export interface PaymentGatewayProviderUpdateFormState {
  code?: PaymentGatewayProviderCode;
  name?: Record<Locale, string>;
  description?: Record<Locale, string>;
  is_active?: boolean;
  sort_order?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPaymentGatewayProviderCreateForm(): PaymentGatewayProviderCreateFormState {
  return {
    code: PaymentGatewayProviderCode.Internal,
    name: emptyLocaleMap(),
    description: emptyLocaleMap(),
    is_active: false,
    sort_order: 0,
  };
}

/** Convert create form state into the payload that basePaymentGatewayProviderCreateSchema accepts. */
export function buildPaymentGatewayProviderCreatePayload(form: PaymentGatewayProviderCreateFormState): BasePaymentGatewayProviderCreate {
  const payload: Record<string, unknown> = {
    code: form.code,
    name: (form.name["ja"] ?? '').trim(),
    description: (form.description["ja"] ?? '').trim(),
    is_active: form.is_active,
    sort_order: form.sort_order,
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
    description: form.description,
  });
  Object.assign(payload, i18n);

  return payload as BasePaymentGatewayProviderCreate;
}

/** Convert update form state into the payload that basePaymentGatewayProviderUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPaymentGatewayProviderUpdatePayload(form: PaymentGatewayProviderUpdateFormState): BasePaymentGatewayProviderUpdate {
  const payload: Record<string, unknown> = {};
  if (form.code !== undefined) {
    payload.code = form.code;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.description !== undefined) {
    payload.description = (form.description["ja"] ?? '').trim();
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  if (form.description !== undefined) localeInput.description = form.description;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BasePaymentGatewayProviderUpdate;
}
