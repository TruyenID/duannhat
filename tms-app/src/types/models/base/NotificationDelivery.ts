/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { NotificationChannel } from '../enum/NotificationChannel';
import { NotificationDeliveryStatus } from '../enum/NotificationDeliveryStatus';
import type { NotificationRecipient } from './NotificationRecipient';

/**
 * 通知配信記録
 */
export interface NotificationDelivery {
  id: string;
  notificationRecipient_id: string;
  notificationRecipient?: NotificationRecipient;
  channel: NotificationChannel;
  status: NotificationDeliveryStatus;
  attempts?: number | null;
  last_attempted_at?: DateTimeString | null;
  sent_at?: DateTimeString | null;
  delivered_at?: DateTimeString | null;
  failed_at?: DateTimeString | null;
  error?: string | null;
  provider_ref?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationDelivery
 */
export const notificationDeliveryI18n = {
  label: {"ja":"通知配信記録","en":"Notification Delivery","vi":"Bản ghi phân phối thông báo"},
  fields: {
    notification_recipient: {
      label: {"ja":"通知受信者","en":"Notification Recipient","vi":"Người nhận"},
      placeholder: {"ja":"通知受信者を入力","en":"Enter Notification Recipient","vi":"Nhập Người nhận"},
    },
    channel: {
      label: {"ja":"チャンネル","en":"Channel","vi":"Kênh"},
      placeholder: {"ja":"チャンネルを入力","en":"Enter Channel","vi":"Nhập Kênh"},
    },
    status: {
      label: {"ja":"ステータス","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"ステータスを入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    attempts: {
      label: {"ja":"試行回数","en":"Attempts","vi":"Số lần thử"},
      placeholder: {"ja":"試行回数を入力","en":"Enter Attempts","vi":"Nhập Số lần thử"},
    },
    last_attempted_at: {
      label: {"ja":"最終試行日時","en":"Last Attempted At","vi":"Thử lần cuối"},
      placeholder: {"ja":"最終試行日時を入力","en":"Enter Last Attempted At","vi":"Nhập Thử lần cuối"},
    },
    sent_at: {
      label: {"ja":"送信日時","en":"Sent At","vi":"Gửi lúc"},
      placeholder: {"ja":"送信日時を入力","en":"Enter Sent At","vi":"Nhập Gửi lúc"},
    },
    delivered_at: {
      label: {"ja":"配信日時","en":"Delivered At","vi":"Phân phối lúc"},
      placeholder: {"ja":"配信日時を入力","en":"Enter Delivered At","vi":"Nhập Phân phối lúc"},
    },
    failed_at: {
      label: {"ja":"失敗日時","en":"Failed At","vi":"Thất bại lúc"},
      placeholder: {"ja":"失敗日時を入力","en":"Enter Failed At","vi":"Nhập Thất bại lúc"},
    },
    error: {
      label: {"ja":"エラー","en":"Error","vi":"Lỗi"},
      placeholder: {"ja":"エラーを入力","en":"Enter Error","vi":"Nhập Lỗi"},
    },
    provider_ref: {
      label: {"ja":"プロバイダー参照","en":"Provider Ref","vi":"Ref provider"},
      placeholder: {"ja":"プロバイダー参照を入力","en":"Enter Provider Ref","vi":"Nhập Ref provider"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationDelivery */
export const baseNotificationDeliverySchemas = {
  channel: z.nativeEnum(NotificationChannel),
  status: z.nativeEnum(NotificationDeliveryStatus),
  attempts: z.number().int().optional().nullable(),
  last_attempted_at: z.string().datetime({ offset: true }).optional().nullable(),
  sent_at: z.string().datetime({ offset: true }).optional().nullable(),
  delivered_at: z.string().datetime({ offset: true }).optional().nullable(),
  failed_at: z.string().datetime({ offset: true }).optional().nullable(),
  error: z.string().optional().nullable(),
  provider_ref: z.string().max(200).optional().nullable(),
} as const;

/** Create schema for NotificationDelivery */
export const baseNotificationDeliveryCreateSchema = z.object({
  channel: baseNotificationDeliverySchemas.channel,
  status: baseNotificationDeliverySchemas.status,
  attempts: baseNotificationDeliverySchemas.attempts,
  last_attempted_at: baseNotificationDeliverySchemas.last_attempted_at,
  sent_at: baseNotificationDeliverySchemas.sent_at,
  delivered_at: baseNotificationDeliverySchemas.delivered_at,
  failed_at: baseNotificationDeliverySchemas.failed_at,
  error: baseNotificationDeliverySchemas.error,
  provider_ref: baseNotificationDeliverySchemas.provider_ref,
});

/** Update schema for NotificationDelivery */
export const baseNotificationDeliveryUpdateSchema = baseNotificationDeliveryCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationDeliveryCreate = z.infer<typeof baseNotificationDeliveryCreateSchema>;
export type BaseNotificationDeliveryUpdate = z.infer<typeof baseNotificationDeliveryUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationDeliveryLabel(locale: string): string {
  return notificationDeliveryI18n.label[locale as keyof typeof notificationDeliveryI18n.label] ?? notificationDeliveryI18n.label['en'] ?? 'NotificationDelivery';
}

/** Get field label for a specific locale */
export function getNotificationDeliveryFieldLabel(field: string, locale: string): string {
  const fields = notificationDeliveryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationDeliveryFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationDeliveryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationDelivery.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationDeliveryMetadata = {
  modelName: "NotificationDelivery",
  table: "notification_deliveries",

  fields: {
    notification_recipient_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "NotificationRecipient",
        table: "notification_recipients",
        onDelete: "CASCADE",
      },
      label: {},
    },
    channel: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "NotificationChannel",
      enumValues: ["in_app", "realtime", "email", "push"] as const,
      label: {
        ja: "チャンネル",
        en: "Channel",
        vi: "Kênh",
      },
      placeholder: {
        ja: "チャンネルを入力",
        en: "Enter Channel",
        vi: "Nhập Kênh",
      },
    },
    status: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "NotificationDeliveryStatus",
      enumValues: ["pending", "sent", "delivered", "failed", "bounced", "complained", "suppressed", "skipped"] as const,
      label: {
        ja: "ステータス",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "ステータスを入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    attempts: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "試行回数",
        en: "Attempts",
        vi: "Số lần thử",
      },
      placeholder: {
        ja: "試行回数を入力",
        en: "Enter Attempts",
        vi: "Nhập Số lần thử",
      },
      description: {
        ja: "再試行カウンタ。NotificationChannelJobが送信試行毎にインクリメント。null = 未試行（新規行）。ジョブ内で0に正規化してからインクリメント。",
        en: "Retry counter; incremented by NotificationChannelJob on each send attempt. Null = never attempted (freshly-written row); the job normalizes to 0 before incrementing.",
        vi: "Bộ đếm retry; NotificationChannelJob tăng mỗi lần send. Null = chưa thử (row mới tạo); job chuẩn hoá về 0 trước khi +1.",
      },
    },
    last_attempted_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最終試行日時",
        en: "Last Attempted At",
        vi: "Thử lần cuối",
      },
      placeholder: {
        ja: "最終試行日時を入力",
        en: "Enter Last Attempted At",
        vi: "Nhập Thử lần cuối",
      },
    },
    sent_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "送信日時",
        en: "Sent At",
        vi: "Gửi lúc",
      },
      placeholder: {
        ja: "送信日時を入力",
        en: "Enter Sent At",
        vi: "Nhập Gửi lúc",
      },
    },
    delivered_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "配信日時",
        en: "Delivered At",
        vi: "Phân phối lúc",
      },
      placeholder: {
        ja: "配信日時を入力",
        en: "Enter Delivered At",
        vi: "Nhập Phân phối lúc",
      },
    },
    failed_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "失敗日時",
        en: "Failed At",
        vi: "Thất bại lúc",
      },
      placeholder: {
        ja: "失敗日時を入力",
        en: "Enter Failed At",
        vi: "Nhập Thất bại lúc",
      },
    },
    error: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "エラー",
        en: "Error",
        vi: "Lỗi",
      },
      placeholder: {
        ja: "エラーを入力",
        en: "Enter Error",
        vi: "Nhập Lỗi",
      },
      description: {
        ja: "最後に失敗した試行の例外メッセージ。後続の試行が成功するとクリア。",
        en: "Exception message from the last failed attempt. Cleared when a subsequent attempt succeeds.",
        vi: "Message exception của lần thử thất bại gần nhất. Xoá khi attempt sau thành công.",
      },
    },
    provider_ref: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 200,
      },
      label: {
        ja: "プロバイダー参照",
        en: "Provider Ref",
        vi: "Ref provider",
      },
      placeholder: {
        ja: "プロバイダー参照を入力",
        en: "Enter Provider Ref",
        vi: "Nhập Ref provider",
      },
      description: {
        ja: "プロバイダー側の識別子（メール message id、プッシュ ticket、ブロードキャスト event id）。非同期配信 webhook との突合に使用。",
        en: "Provider-side identifier (email message id, push ticket, broadcast event id). Used to reconcile async delivery webhooks.",
        vi: "Mã ở phía provider (email message id, push ticket, broadcast event id). Đối chiếu với webhook phân phối async.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["notification_recipient_id", "channel", "status"] as const,
  enumFields: ["channel", "status"] as const,
  relationFields: ["notification_recipient_id"] as const,
} as const;

export type NotificationDeliveryFieldName = keyof typeof notificationDeliveryMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationDelivery. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationDeliveryCreateFormState {
  notification_recipient_id: string;
  channel: NotificationChannel;
  status: NotificationDeliveryStatus;
  attempts?: number | null;
  last_attempted_at?: string | null;
  sent_at?: string | null;
  delivered_at?: string | null;
  failed_at?: string | null;
  error?: string | null;
  provider_ref?: string | null;
}

/** Form-state shape for updating a NotificationDelivery. Every field optional. */
export interface NotificationDeliveryUpdateFormState {
  notification_recipient_id?: string;
  channel?: NotificationChannel;
  status?: NotificationDeliveryStatus;
  attempts?: number | null;
  last_attempted_at?: string | null;
  sent_at?: string | null;
  delivered_at?: string | null;
  failed_at?: string | null;
  error?: string | null;
  provider_ref?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationDeliveryCreateForm(): NotificationDeliveryCreateFormState {
  return {
    notification_recipient_id: '',
    channel: NotificationChannel.InApp,
    status: NotificationDeliveryStatus.Pending,
    attempts: 0,
    last_attempted_at: '',
    sent_at: '',
    delivered_at: '',
    failed_at: '',
    error: '',
    provider_ref: '',
  };
}

/** Convert create form state into the payload that baseNotificationDeliveryCreateSchema accepts. */
export function buildNotificationDeliveryCreatePayload(form: NotificationDeliveryCreateFormState): BaseNotificationDeliveryCreate {
  const payload: Record<string, unknown> = {
    notification_recipient_id: form.notification_recipient_id.trim(),
    channel: form.channel,
    status: form.status,
    attempts: form.attempts,
    last_attempted_at: form.last_attempted_at,
    sent_at: form.sent_at,
    delivered_at: form.delivered_at,
    failed_at: form.failed_at,
    error: form.error?.toString().trim() || null,
    provider_ref: form.provider_ref?.toString().trim() || null,
  };

  return payload as BaseNotificationDeliveryCreate;
}

/** Convert update form state into the payload that baseNotificationDeliveryUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationDeliveryUpdatePayload(form: NotificationDeliveryUpdateFormState): BaseNotificationDeliveryUpdate {
  const payload: Record<string, unknown> = {};
  if (form.notification_recipient_id !== undefined) {
    payload.notification_recipient_id = form.notification_recipient_id?.toString().trim() ?? null;
  }
  if (form.channel !== undefined) {
    payload.channel = form.channel;
  }
  if (form.status !== undefined) {
    payload.status = form.status;
  }
  if (form.attempts !== undefined) {
    payload.attempts = form.attempts;
  }
  if (form.last_attempted_at !== undefined) {
    payload.last_attempted_at = form.last_attempted_at;
  }
  if (form.sent_at !== undefined) {
    payload.sent_at = form.sent_at;
  }
  if (form.delivered_at !== undefined) {
    payload.delivered_at = form.delivered_at;
  }
  if (form.failed_at !== undefined) {
    payload.failed_at = form.failed_at;
  }
  if (form.error !== undefined) {
    payload.error = form.error?.toString().trim() ?? null;
  }
  if (form.provider_ref !== undefined) {
    payload.provider_ref = form.provider_ref?.toString().trim() ?? null;
  }
  return payload as BaseNotificationDeliveryUpdate;
}
