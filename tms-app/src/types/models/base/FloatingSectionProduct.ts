/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { FloatingSection } from './FloatingSection';
import type { FloatingSectionProductSku } from './FloatingSectionProductSku';
import type { Product } from './Product';

/**
 * フローティングセクション商品
 */
export interface FloatingSectionProduct {
  id: string;
  floatingSection_id: string;
  floatingSection?: FloatingSection;
  product_id: string;
  product?: Product;
  is_active: boolean;
  display_order: number;
  skus?: FloatingSectionProductSku[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for FloatingSectionProduct
 */
export const floatingSectionProductI18n = {
  label: {"ja":"フローティングセクション商品","en":"Floating Section Product","vi":"Sản phẩm trong khung giờ ưu đãi"},
  fields: {
    floating_section: {
      label: {"ja":"フローティングセクション","en":"Floating Section","vi":"Khung giờ ưu đãi"},
      placeholder: {"ja":"フローティングセクションを入力","en":"Enter Floating Section","vi":"Nhập Khung giờ ưu đãi"},
    },
    product: {
      label: {"ja":"商品","en":"Product","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Product","vi":"Nhập Sản phẩm"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    display_order: {
      label: {"ja":"表示順序","en":"Display Order","vi":"Thứ tự hiển thị"},
      placeholder: {"ja":"表示順序を入力","en":"Enter Display Order","vi":"Nhập Thứ tự hiển thị"},
    },
    skus: {
      label: {"ja":"商品SKU","en":"Product SKUs","vi":"SKU của sản phẩm"},
      placeholder: {"ja":"商品SKUを入力","en":"Enter Product SKUs","vi":"Nhập SKU của sản phẩm"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for FloatingSectionProduct */
export const baseFloatingSectionProductSchemas = {
  is_active: z.boolean(),
  display_order: z.number().int(),
} as const;

/** Create schema for FloatingSectionProduct */
export const baseFloatingSectionProductCreateSchema = z.object({
  is_active: baseFloatingSectionProductSchemas.is_active,
  display_order: baseFloatingSectionProductSchemas.display_order,
});

/** Update schema for FloatingSectionProduct */
export const baseFloatingSectionProductUpdateSchema = baseFloatingSectionProductCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseFloatingSectionProductCreate = z.infer<typeof baseFloatingSectionProductCreateSchema>;
export type BaseFloatingSectionProductUpdate = z.infer<typeof baseFloatingSectionProductUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getFloatingSectionProductLabel(locale: string): string {
  return floatingSectionProductI18n.label[locale as keyof typeof floatingSectionProductI18n.label] ?? floatingSectionProductI18n.label['en'] ?? 'FloatingSectionProduct';
}

/** Get field label for a specific locale */
export function getFloatingSectionProductFieldLabel(field: string, locale: string): string {
  const fields = floatingSectionProductI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getFloatingSectionProductFieldPlaceholder(field: string, locale: string): string {
  const fields = floatingSectionProductI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for FloatingSectionProduct.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const floatingSectionProductMetadata = {
  modelName: "FloatingSectionProduct",
  table: "floating_section_products",

  fields: {
    floating_section_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "FloatingSection",
        table: "floating_sections",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Product",
        table: "products",
        onDelete: "CASCADE",
      },
      label: {},
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    display_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順序",
        en: "Display Order",
        vi: "Thứ tự hiển thị",
      },
      placeholder: {
        ja: "表示順序を入力",
        en: "Enter Display Order",
        vi: "Nhập Thứ tự hiển thị",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["floating_section_id", "product_id", "is_active", "display_order"] as const,
  enumFields: [] as const,
  relationFields: ["floating_section_id", "product_id"] as const,
} as const;

export type FloatingSectionProductFieldName = keyof typeof floatingSectionProductMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a FloatingSectionProduct. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface FloatingSectionProductCreateFormState {
  floating_section_id: string;
  product_id: string;
  is_active: boolean;
  display_order: number;
}

/** Form-state shape for updating a FloatingSectionProduct. Every field optional. */
export interface FloatingSectionProductUpdateFormState {
  floating_section_id?: string;
  product_id?: string;
  is_active?: boolean;
  display_order?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyFloatingSectionProductCreateForm(): FloatingSectionProductCreateFormState {
  return {
    floating_section_id: '',
    product_id: '',
    is_active: true,
    display_order: 0,
  };
}

/** Convert create form state into the payload that baseFloatingSectionProductCreateSchema accepts. */
export function buildFloatingSectionProductCreatePayload(form: FloatingSectionProductCreateFormState): BaseFloatingSectionProductCreate {
  const payload: Record<string, unknown> = {
    floating_section_id: form.floating_section_id.trim(),
    product_id: form.product_id.trim(),
    is_active: form.is_active,
    display_order: form.display_order,
  };

  return payload as BaseFloatingSectionProductCreate;
}

/** Convert update form state into the payload that baseFloatingSectionProductUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildFloatingSectionProductUpdatePayload(form: FloatingSectionProductUpdateFormState): BaseFloatingSectionProductUpdate {
  const payload: Record<string, unknown> = {};
  if (form.floating_section_id !== undefined) {
    payload.floating_section_id = form.floating_section_id?.toString().trim() ?? null;
  }
  if (form.product_id !== undefined) {
    payload.product_id = form.product_id?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.display_order !== undefined) {
    payload.display_order = form.display_order;
  }
  return payload as BaseFloatingSectionProductUpdate;
}
