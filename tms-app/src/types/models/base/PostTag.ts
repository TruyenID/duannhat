/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Organization } from './Organization';
import type { Post } from './Post';

/**
 * 投稿タグ
 */
export interface PostTag {
  id: string;
  organization_id: string;
  organization?: Organization;
  name: string;
  slug: string;
  color?: string | null;
  posts?: Post[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PostTag
 */
export const postTagI18n = {
  label: {"ja":"投稿タグ","en":"Post Tag","vi":"Thẻ Bài viết"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    name: {
      label: {"ja":"名前","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名前を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    slug: {
      label: {"ja":"スラッグ","en":"Slug","vi":"Slug"},
      placeholder: {"ja":"スラッグを入力","en":"Enter Slug","vi":"Nhập Slug"},
    },
    color: {
      label: {"ja":"色","en":"Color","vi":"Màu"},
      placeholder: {"ja":"色を入力","en":"Enter Color","vi":"Nhập Màu"},
    },
    posts: {
      label: {"ja":"投稿","en":"Posts","vi":"Bài viết"},
      placeholder: {"ja":"投稿を入力","en":"Enter Posts","vi":"Nhập Bài viết"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PostTag */
export const basePostTagSchemas = {
  name: z.string().min(1).max(80),
  slug: z.string().min(1).max(100),
  color: z.string().max(20).optional().nullable(),
} as const;

/** Create schema for PostTag */
export const basePostTagCreateSchema = z.object({
  name: basePostTagSchemas.name,
  slug: basePostTagSchemas.slug,
  color: basePostTagSchemas.color,
  ja: z.object({
    name: z.string().max(80).optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(80).optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(80).optional().nullable(),
  }).optional(),
});

/** Update schema for PostTag */
export const basePostTagUpdateSchema = basePostTagCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePostTagCreate = z.infer<typeof basePostTagCreateSchema>;
export type BasePostTagUpdate = z.infer<typeof basePostTagUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPostTagLabel(locale: string): string {
  return postTagI18n.label[locale as keyof typeof postTagI18n.label] ?? postTagI18n.label['en'] ?? 'PostTag';
}

/** Get field label for a specific locale */
export function getPostTagFieldLabel(field: string, locale: string): string {
  const fields = postTagI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPostTagFieldPlaceholder(field: string, locale: string): string {
  const fields = postTagI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PostTag.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const postTagMetadata = {
  modelName: "PostTag",
  table: "post_tags",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 80,
      },
      label: {
        ja: "名前",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名前を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    slug: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "スラッグ",
        en: "Slug",
        vi: "Slug",
      },
      placeholder: {
        ja: "スラッグを入力",
        en: "Enter Slug",
        vi: "Nhập Slug",
      },
      description: {
        ja: "URLセーフな識別子。組織内で一意。",
        en: "URL-safe identifier. Unique per organization.",
        vi: "Mã định danh URL-safe. Duy nhất trong phạm vi tổ chức.",
      },
    },
    color: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "色",
        en: "Color",
        vi: "Màu",
      },
      placeholder: {
        ja: "色を入力",
        en: "Enter Color",
        vi: "Nhập Màu",
      },
      description: {
        ja: "タグチップ表示用のオプションのHEXカラー（例 \"#fff5d6\"）。",
        en: "Optional hex color for tag chip display (e.g. \"#fff5d6\").",
        vi: "Mã màu HEX tùy chọn cho chip hiển thị tag (VD \"#fff5d6\").",
      },
    },
  },

  translatableFields: ["name"] as const,
  requiredFields: ["organization_id", "name", "slug"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id"] as const,
} as const;

export type PostTagFieldName = keyof typeof postTagMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PostTag. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PostTagCreateFormState {
  organization_id: string;
  name: Record<Locale, string>;
  slug: string;
  color?: string | null;
}

/** Form-state shape for updating a PostTag. Every field optional. */
export interface PostTagUpdateFormState {
  organization_id?: string;
  name?: Record<Locale, string>;
  slug?: string;
  color?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPostTagCreateForm(): PostTagCreateFormState {
  return {
    organization_id: '',
    name: emptyLocaleMap(),
    slug: '',
    color: '',
  };
}

/** Convert create form state into the payload that basePostTagCreateSchema accepts. */
export function buildPostTagCreatePayload(form: PostTagCreateFormState): BasePostTagCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    name: (form.name["ja"] ?? '').trim(),
    slug: form.slug.trim(),
    color: form.color?.toString().trim() || null,
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
  });
  Object.assign(payload, i18n);

  return payload as BasePostTagCreate;
}

/** Convert update form state into the payload that basePostTagUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPostTagUpdatePayload(form: PostTagUpdateFormState): BasePostTagUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.slug !== undefined) {
    payload.slug = form.slug?.toString().trim() ?? null;
  }
  if (form.color !== undefined) {
    payload.color = form.color?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BasePostTagUpdate;
}
