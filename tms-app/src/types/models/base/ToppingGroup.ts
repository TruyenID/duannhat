/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { ToppingGroupModifierType } from '../enum/ToppingGroupModifierType';
import { ToppingGroupPriceStrategy } from '../enum/ToppingGroupPriceStrategy';
import { ToppingGroupSelectionType } from '../enum/ToppingGroupSelectionType';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { Product } from './Product';
import type { ToppingGroupItem } from './ToppingGroupItem';

/**
 * トッピンググループ
 */
export interface ToppingGroup {
  id: string;
  name: string;
  price_strategy: ToppingGroupPriceStrategy;
  free_quantity?: number | null;
  selection_type: ToppingGroupSelectionType;
  modifier_type: ToppingGroupModifierType;
  min_select: number;
  max_select?: number | null;
  max_qty_per_item: number;
  sort_order: number;
  is_active: boolean;
  brand_id: string;
  brand?: Brand;
  organization_id: string;
  organization?: Organization;
  items?: ToppingGroupItem[];
  products?: Product[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ToppingGroup
 */
export const toppingGroupI18n = {
  label: {"ja":"トッピンググループ","en":"Topping Group","vi":"Nhóm Topping"},
  fields: {
    name: {
      label: {"ja":"グループ名","en":"Name","vi":"Tên nhóm"},
      placeholder: {"ja":"グループ名を入力","en":"Enter Name","vi":"Nhập Tên nhóm"},
    },
    price_strategy: {
      label: {"ja":"価格戦略","en":"Price Strategy","vi":"Chiến lược giá"},
      placeholder: {"ja":"価格戦略を入力","en":"Enter Price Strategy","vi":"Nhập Chiến lược giá"},
    },
    free_quantity: {
      label: {"ja":"無料数量","en":"Free Quantity","vi":"Số lượng miễn phí"},
      placeholder: {"ja":"無料数量を入力","en":"Enter Free Quantity","vi":"Nhập Số lượng miễn phí"},
    },
    selection_type: {
      label: {"ja":"選択方式","en":"Selection Type","vi":"Cách chọn"},
      placeholder: {"ja":"選択方式を入力","en":"Enter Selection Type","vi":"Nhập Cách chọn"},
    },
    modifier_type: {
      label: {"ja":"修飾タイプ","en":"Modifier Type","vi":"Loại điều chỉnh"},
      placeholder: {"ja":"修飾タイプを入力","en":"Enter Modifier Type","vi":"Nhập Loại điều chỉnh"},
    },
    min_select: {
      label: {"ja":"最小選択数","en":"Min Select","vi":"Số lượng tối thiểu"},
      placeholder: {"ja":"最小選択数を入力","en":"Enter Min Select","vi":"Nhập Số lượng tối thiểu"},
    },
    max_select: {
      label: {"ja":"最大選択数","en":"Max Select","vi":"Số lượng tối đa"},
      placeholder: {"ja":"最大選択数を入力","en":"Enter Max Select","vi":"Nhập Số lượng tối đa"},
    },
    max_qty_per_item: {
      label: {"ja":"アイテムあたりの最大数量","en":"Max Qty Per Item","vi":"Số lượng tối đa mỗi loại"},
      placeholder: {"ja":"アイテムあたりの最大数量を入力","en":"Enter Max Qty Per Item","vi":"Nhập Số lượng tối đa mỗi loại"},
    },
    sort_order: {
      label: {"ja":"表示順序","en":"Sort Order","vi":"Thứ tự hiển thị"},
      placeholder: {"ja":"表示順序を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự hiển thị"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    items: {
      label: {"ja":"トッピング項目","en":"Items","vi":"Mục topping"},
      placeholder: {"ja":"トッピング項目を入力","en":"Enter Items","vi":"Nhập Mục topping"},
    },
    products: {
      label: {"ja":"商品","en":"Products","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Products","vi":"Nhập Sản phẩm"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ToppingGroup */
export const baseToppingGroupSchemas = {
  name: z.string().min(1).max(255),
  price_strategy: z.nativeEnum(ToppingGroupPriceStrategy),
  free_quantity: z.number().int().optional().nullable(),
  selection_type: z.nativeEnum(ToppingGroupSelectionType),
  modifier_type: z.nativeEnum(ToppingGroupModifierType),
  min_select: z.number().int(),
  max_select: z.number().int().optional().nullable(),
  max_qty_per_item: z.number().int(),
  sort_order: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for ToppingGroup */
export const baseToppingGroupCreateSchema = z.object({
  name: baseToppingGroupSchemas.name,
  price_strategy: baseToppingGroupSchemas.price_strategy,
  free_quantity: baseToppingGroupSchemas.free_quantity,
  selection_type: baseToppingGroupSchemas.selection_type,
  modifier_type: baseToppingGroupSchemas.modifier_type,
  min_select: baseToppingGroupSchemas.min_select,
  max_select: baseToppingGroupSchemas.max_select,
  max_qty_per_item: baseToppingGroupSchemas.max_qty_per_item,
  sort_order: baseToppingGroupSchemas.sort_order,
  is_active: baseToppingGroupSchemas.is_active,
  ja: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
});

/** Update schema for ToppingGroup */
export const baseToppingGroupUpdateSchema = baseToppingGroupCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseToppingGroupCreate = z.infer<typeof baseToppingGroupCreateSchema>;
export type BaseToppingGroupUpdate = z.infer<typeof baseToppingGroupUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getToppingGroupLabel(locale: string): string {
  return toppingGroupI18n.label[locale as keyof typeof toppingGroupI18n.label] ?? toppingGroupI18n.label['en'] ?? 'ToppingGroup';
}

/** Get field label for a specific locale */
export function getToppingGroupFieldLabel(field: string, locale: string): string {
  const fields = toppingGroupI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getToppingGroupFieldPlaceholder(field: string, locale: string): string {
  const fields = toppingGroupI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ToppingGroup.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const toppingGroupMetadata = {
  modelName: "ToppingGroup",
  table: "topping_groups",

  fields: {
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "グループ名",
        en: "Name",
        vi: "Tên nhóm",
      },
      placeholder: {
        ja: "グループ名を入力",
        en: "Enter Name",
        vi: "Nhập Tên nhóm",
      },
    },
    price_strategy: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "ToppingGroupPriceStrategy",
      enumValues: ["flat", "free_up_to_n"] as const,
      label: {
        ja: "価格戦略",
        en: "Price Strategy",
        vi: "Chiến lược giá",
      },
      placeholder: {
        ja: "価格戦略を入力",
        en: "Enter Price Strategy",
        vi: "Nhập Chiến lược giá",
      },
    },
    free_quantity: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "無料数量",
        en: "Free Quantity",
        vi: "Số lượng miễn phí",
      },
      placeholder: {
        ja: "無料数量を入力",
        en: "Enter Free Quantity",
        vi: "Nhập Số lượng miễn phí",
      },
    },
    selection_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "ToppingGroupSelectionType",
      enumValues: ["single", "multiple"] as const,
      label: {
        ja: "選択方式",
        en: "Selection Type",
        vi: "Cách chọn",
      },
      placeholder: {
        ja: "選択方式を入力",
        en: "Enter Selection Type",
        vi: "Nhập Cách chọn",
      },
    },
    modifier_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "ToppingGroupModifierType",
      enumValues: ["add", "remove"] as const,
      label: {
        ja: "修飾タイプ",
        en: "Modifier Type",
        vi: "Loại điều chỉnh",
      },
      placeholder: {
        ja: "修飾タイプを入力",
        en: "Enter Modifier Type",
        vi: "Nhập Loại điều chỉnh",
      },
    },
    min_select: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "最小選択数",
        en: "Min Select",
        vi: "Số lượng tối thiểu",
      },
      placeholder: {
        ja: "最小選択数を入力",
        en: "Enter Min Select",
        vi: "Nhập Số lượng tối thiểu",
      },
    },
    max_select: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最大選択数",
        en: "Max Select",
        vi: "Số lượng tối đa",
      },
      placeholder: {
        ja: "最大選択数を入力",
        en: "Enter Max Select",
        vi: "Nhập Số lượng tối đa",
      },
    },
    max_qty_per_item: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "アイテムあたりの最大数量",
        en: "Max Qty Per Item",
        vi: "Số lượng tối đa mỗi loại",
      },
      placeholder: {
        ja: "アイテムあたりの最大数量を入力",
        en: "Enter Max Qty Per Item",
        vi: "Nhập Số lượng tối đa mỗi loại",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順序",
        en: "Sort Order",
        vi: "Thứ tự hiển thị",
      },
      placeholder: {
        ja: "表示順序を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự hiển thị",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name"] as const,
  requiredFields: ["name", "price_strategy", "selection_type", "modifier_type", "min_select", "max_qty_per_item", "sort_order", "is_active", "brand_id", "organization_id"] as const,
  enumFields: ["price_strategy", "selection_type", "modifier_type"] as const,
  relationFields: ["brand_id", "organization_id"] as const,
} as const;

export type ToppingGroupFieldName = keyof typeof toppingGroupMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ToppingGroup. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ToppingGroupCreateFormState {
  name: Record<Locale, string>;
  price_strategy: ToppingGroupPriceStrategy;
  free_quantity?: number | null;
  selection_type: ToppingGroupSelectionType;
  modifier_type: ToppingGroupModifierType;
  min_select: number;
  max_select?: number | null;
  max_qty_per_item: number;
  sort_order: number;
  is_active: boolean;
  brand_id: string;
  organization_id: string;
}

/** Form-state shape for updating a ToppingGroup. Every field optional. */
export interface ToppingGroupUpdateFormState {
  name?: Record<Locale, string>;
  price_strategy?: ToppingGroupPriceStrategy;
  free_quantity?: number | null;
  selection_type?: ToppingGroupSelectionType;
  modifier_type?: ToppingGroupModifierType;
  min_select?: number;
  max_select?: number | null;
  max_qty_per_item?: number;
  sort_order?: number;
  is_active?: boolean;
  brand_id?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyToppingGroupCreateForm(): ToppingGroupCreateFormState {
  return {
    name: emptyLocaleMap(),
    price_strategy: ToppingGroupPriceStrategy.Flat,
    free_quantity: 0,
    selection_type: ToppingGroupSelectionType.Multiple,
    modifier_type: ToppingGroupModifierType.Add,
    min_select: 0,
    max_select: 0,
    max_qty_per_item: 1,
    sort_order: 0,
    is_active: true,
    brand_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseToppingGroupCreateSchema accepts. */
export function buildToppingGroupCreatePayload(form: ToppingGroupCreateFormState): BaseToppingGroupCreate {
  const payload: Record<string, unknown> = {
    name: (form.name["ja"] ?? '').trim(),
    price_strategy: form.price_strategy,
    free_quantity: form.free_quantity,
    selection_type: form.selection_type,
    modifier_type: form.modifier_type,
    min_select: form.min_select,
    max_select: form.max_select,
    max_qty_per_item: form.max_qty_per_item,
    sort_order: form.sort_order,
    is_active: form.is_active,
    brand_id: form.brand_id.trim(),
    organization_id: form.organization_id.trim(),
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
  });
  Object.assign(payload, i18n);

  return payload as BaseToppingGroupCreate;
}

/** Convert update form state into the payload that baseToppingGroupUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildToppingGroupUpdatePayload(form: ToppingGroupUpdateFormState): BaseToppingGroupUpdate {
  const payload: Record<string, unknown> = {};
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.price_strategy !== undefined) {
    payload.price_strategy = form.price_strategy;
  }
  if (form.free_quantity !== undefined) {
    payload.free_quantity = form.free_quantity;
  }
  if (form.selection_type !== undefined) {
    payload.selection_type = form.selection_type;
  }
  if (form.modifier_type !== undefined) {
    payload.modifier_type = form.modifier_type;
  }
  if (form.min_select !== undefined) {
    payload.min_select = form.min_select;
  }
  if (form.max_select !== undefined) {
    payload.max_select = form.max_select;
  }
  if (form.max_qty_per_item !== undefined) {
    payload.max_qty_per_item = form.max_qty_per_item;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseToppingGroupUpdate;
}
