/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Device } from './Device';
import type { Organization } from './Organization';
import type { TillSession } from './TillSession';

/**
 * レジ
 */
export interface Till {
  id: string;
  till_code: string;
  default_currency_code: string;
  variance_tolerance_amount: number;
  is_active: boolean;
  device_id?: string | null;
  device?: Device | null;
  currentSession_id?: string | null;
  currentSession?: TillSession | null;
  branch_id: string;
  branch?: Branch;
  brand_id: string;
  brand?: Brand;
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Till
 */
export const tillI18n = {
  label: {"ja":"レジ","en":"Till","vi":"Két tiền"},
  fields: {
    till_code: {
      label: {"ja":"レジコード","en":"Till Code","vi":"Mã két"},
      placeholder: {"ja":"レジコードを入力","en":"Enter Till Code","vi":"Nhập Mã két"},
    },
    default_currency_code: {
      label: {"ja":"既定通貨","en":"Default Currency","vi":"Tiền tệ mặc định"},
      placeholder: {"ja":"既定通貨を入力","en":"Enter Default Currency","vi":"Nhập Tiền tệ mặc định"},
    },
    variance_tolerance_amount: {
      label: {"ja":"許容差額","en":"Variance Tolerance","vi":"Sai số cho phép"},
      placeholder: {"ja":"許容差額を入力","en":"Enter Variance Tolerance","vi":"Nhập Sai số cho phép"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    device: {
      label: {"ja":"端末","en":"Device","vi":"Thiết bị"},
      placeholder: {"ja":"端末を入力","en":"Enter Device","vi":"Nhập Thiết bị"},
    },
    current_session: {
      label: {"ja":"現在のセッション","en":"Current Session","vi":"Ca hiện tại"},
      placeholder: {"ja":"現在のセッションを入力","en":"Enter Current Session","vi":"Nhập Ca hiện tại"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Till */
export const baseTillSchemas = {
  till_code: z.string().min(1).max(50),
  default_currency_code: z.string().min(1).max(3),
  variance_tolerance_amount: z.number(),
  is_active: z.boolean(),
} as const;

/** Create schema for Till */
export const baseTillCreateSchema = z.object({
  till_code: baseTillSchemas.till_code,
  default_currency_code: baseTillSchemas.default_currency_code,
  variance_tolerance_amount: baseTillSchemas.variance_tolerance_amount,
  is_active: baseTillSchemas.is_active,
});

/** Update schema for Till */
export const baseTillUpdateSchema = baseTillCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTillCreate = z.infer<typeof baseTillCreateSchema>;
export type BaseTillUpdate = z.infer<typeof baseTillUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTillLabel(locale: string): string {
  return tillI18n.label[locale as keyof typeof tillI18n.label] ?? tillI18n.label['en'] ?? 'Till';
}

/** Get field label for a specific locale */
export function getTillFieldLabel(field: string, locale: string): string {
  const fields = tillI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTillFieldPlaceholder(field: string, locale: string): string {
  const fields = tillI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Till.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tillMetadata = {
  modelName: "Till",
  table: "tills",

  fields: {
    till_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "レジコード",
        en: "Till Code",
        vi: "Mã két",
      },
      placeholder: {
        ja: "レジコードを入力",
        en: "Enter Till Code",
        vi: "Nhập Mã két",
      },
    },
    default_currency_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 3,
      },
      label: {
        ja: "既定通貨",
        en: "Default Currency",
        vi: "Tiền tệ mặc định",
      },
      placeholder: {
        ja: "既定通貨を入力",
        en: "Enter Default Currency",
        vi: "Nhập Tiền tệ mặc định",
      },
    },
    variance_tolerance_amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "許容差額",
        en: "Variance Tolerance",
        vi: "Sai số cho phép",
      },
      placeholder: {
        ja: "許容差額を入力",
        en: "Enter Variance Tolerance",
        vi: "Nhập Sai số cho phép",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    device_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Device",
        table: "devices",
        onDelete: "SET NULL",
      },
      label: {},
    },
    current_session_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "TillSession",
        table: "till_sessions",
        onDelete: "SET NULL",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["till_code", "default_currency_code", "variance_tolerance_amount", "is_active", "branch_id", "brand_id", "organization_id"] as const,
  enumFields: [] as const,
  relationFields: ["device_id", "current_session_id", "branch_id", "brand_id", "organization_id"] as const,
} as const;

export type TillFieldName = keyof typeof tillMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Till. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TillCreateFormState {
  till_code: string;
  default_currency_code: string;
  variance_tolerance_amount: number;
  is_active: boolean;
  device_id?: string | null;
  current_session_id?: string | null;
  branch_id: string;
  brand_id: string;
  organization_id: string;
}

/** Form-state shape for updating a Till. Every field optional. */
export interface TillUpdateFormState {
  till_code?: string;
  default_currency_code?: string;
  variance_tolerance_amount?: number;
  is_active?: boolean;
  device_id?: string | null;
  current_session_id?: string | null;
  branch_id?: string;
  brand_id?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTillCreateForm(): TillCreateFormState {
  return {
    till_code: '',
    default_currency_code: "JPY",
    variance_tolerance_amount: 0,
    is_active: true,
    device_id: '',
    current_session_id: '',
    branch_id: '',
    brand_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseTillCreateSchema accepts. */
export function buildTillCreatePayload(form: TillCreateFormState): BaseTillCreate {
  const payload: Record<string, unknown> = {
    till_code: form.till_code.trim(),
    default_currency_code: form.default_currency_code.trim(),
    variance_tolerance_amount: form.variance_tolerance_amount,
    is_active: form.is_active,
    device_id: form.device_id?.toString().trim() || null,
    current_session_id: form.current_session_id?.toString().trim() || null,
    branch_id: form.branch_id.trim(),
    brand_id: form.brand_id.trim(),
    organization_id: form.organization_id.trim(),
  };

  return payload as BaseTillCreate;
}

/** Convert update form state into the payload that baseTillUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTillUpdatePayload(form: TillUpdateFormState): BaseTillUpdate {
  const payload: Record<string, unknown> = {};
  if (form.till_code !== undefined) {
    payload.till_code = form.till_code?.toString().trim() ?? null;
  }
  if (form.default_currency_code !== undefined) {
    payload.default_currency_code = form.default_currency_code?.toString().trim() ?? null;
  }
  if (form.variance_tolerance_amount !== undefined) {
    payload.variance_tolerance_amount = form.variance_tolerance_amount;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.device_id !== undefined) {
    payload.device_id = form.device_id?.toString().trim() ?? null;
  }
  if (form.current_session_id !== undefined) {
    payload.current_session_id = form.current_session_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseTillUpdate;
}
