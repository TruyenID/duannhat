/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { TableStatus } from '../enum/TableStatus';
import type { Branch } from './Branch';
import type { CustomerOrder } from './CustomerOrder';
import type { Organization } from './Organization';
import type { TableStatusChange } from './TableStatusChange';
import type { TableTemplate } from './TableTemplate';
import type { Zone } from './Zone';

/**
 * テーブル
 */
export interface Table {
  id: string;
  code: string;
  name?: string | null;
  seat_count: number;
  status: TableStatus;
  paid_at?: DateTimeString | null;
  call_requested_at?: DateTimeString | null;
  qr_token: string;
  is_active: boolean;
  currentOrder_id?: string | null;
  currentOrder?: CustomerOrder | null;
  organization_id: string;
  organization?: Organization;
  branch_id: string;
  branch?: Branch;
  zone_id: string;
  zone?: Zone;
  tableTemplate_id?: string | null;
  tableTemplate?: TableTemplate | null;
  status_changes?: TableStatusChange[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Table
 */
export const tableI18n = {
  label: {"ja":"テーブル","en":"Table","vi":"Bàn"},
  fields: {
    code: {
      label: {"ja":"テーブルコード","en":"Table Code","vi":"Mã bàn"},
      placeholder: {"ja":"テーブルコードを入力","en":"Enter Table Code","vi":"Nhập Mã bàn"},
    },
    name: {
      label: {"ja":"テーブル名","en":"Table Name","vi":"Tên bàn"},
      placeholder: {"ja":"テーブル名を入力","en":"Enter Table Name","vi":"Nhập Tên bàn"},
    },
    seat_count: {
      label: {"ja":"座席数","en":"Seat Count","vi":"Số chỗ ngồi"},
      placeholder: {"ja":"座席数を入力","en":"Enter Seat Count","vi":"Nhập Số chỗ ngồi"},
    },
    status: {
      label: {"ja":"状態","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"状態を入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    paid_at: {
      label: {"ja":"最終会計日時","en":"Last Paid At","vi":"Thanh toán lúc"},
      placeholder: {"ja":"最終会計日時を入力","en":"Enter Last Paid At","vi":"Nhập Thanh toán lúc"},
    },
    call_requested_at: {
      label: {"ja":"スタッフ呼出日時","en":"Staff Call Requested At","vi":"Yêu cầu gọi NV lúc"},
      placeholder: {"ja":"スタッフ呼出日時を入力","en":"Enter Staff Call Requested At","vi":"Nhập Yêu cầu gọi NV lúc"},
    },
    qr_token: {
      label: {"ja":"QRトークン","en":"QR Token","vi":"Token QR"},
      placeholder: {"ja":"QRトークンを入力","en":"Enter QR Token","vi":"Nhập Token QR"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    current_order: {
      label: {"ja":"現在の注文","en":"Current Order","vi":"Order hiện tại"},
      placeholder: {"ja":"現在の注文を入力","en":"Enter Current Order","vi":"Nhập Order hiện tại"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    zone: {
      label: {"ja":"エリア","en":"Zone","vi":"Khu vực"},
      placeholder: {"ja":"エリアを入力","en":"Enter Zone","vi":"Nhập Khu vực"},
    },
    table_template: {
      label: {"ja":"HQテーブルテンプレート","en":"HQ Table Template","vi":"Mẫu bàn HQ"},
      placeholder: {"ja":"HQテーブルテンプレートを入力","en":"Enter HQ Table Template","vi":"Nhập Mẫu bàn HQ"},
    },
    status_changes: {
      label: {"ja":"状態変更履歴","en":"Status Changes","vi":"Lịch sử trạng thái"},
      placeholder: {"ja":"状態変更履歴を入力","en":"Enter Status Changes","vi":"Nhập Lịch sử trạng thái"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Table */
export const baseTableSchemas = {
  code: z.string().min(1).max(50),
  name: z.string().max(255).optional().nullable(),
  seat_count: z.number().int(),
  status: z.nativeEnum(TableStatus),
  paid_at: z.string().datetime({ offset: true }).optional().nullable(),
  call_requested_at: z.string().datetime({ offset: true }).optional().nullable(),
  qr_token: z.string().min(1).max(64),
  is_active: z.boolean(),
} as const;

/** Create schema for Table */
export const baseTableCreateSchema = z.object({
  code: baseTableSchemas.code,
  name: baseTableSchemas.name,
  seat_count: baseTableSchemas.seat_count,
  status: baseTableSchemas.status,
  paid_at: baseTableSchemas.paid_at,
  call_requested_at: baseTableSchemas.call_requested_at,
  qr_token: baseTableSchemas.qr_token,
  is_active: baseTableSchemas.is_active,
});

/** Update schema for Table */
export const baseTableUpdateSchema = baseTableCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTableCreate = z.infer<typeof baseTableCreateSchema>;
export type BaseTableUpdate = z.infer<typeof baseTableUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTableLabel(locale: string): string {
  return tableI18n.label[locale as keyof typeof tableI18n.label] ?? tableI18n.label['en'] ?? 'Table';
}

/** Get field label for a specific locale */
export function getTableFieldLabel(field: string, locale: string): string {
  const fields = tableI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTableFieldPlaceholder(field: string, locale: string): string {
  const fields = tableI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Table.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tableMetadata = {
  modelName: "Table",
  table: "tables",

  fields: {
    code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "テーブルコード",
        en: "Table Code",
        vi: "Mã bàn",
      },
      placeholder: {
        ja: "テーブルコードを入力",
        en: "Enter Table Code",
        vi: "Nhập Mã bàn",
      },
      description: {
        ja: "店舗内で一意のコード（BR-T01）。例：T-01。",
        en: "Unique code per branch (BR-T01). Example: T-01.",
        vi: "Mã unique trong shop (BR-T01). Ví dụ: T-01.",
      },
    },
    name: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "テーブル名",
        en: "Table Name",
        vi: "Tên bàn",
      },
      placeholder: {
        ja: "テーブル名を入力",
        en: "Enter Table Name",
        vi: "Nhập Tên bàn",
      },
      description: {
        ja: "任意の表示名。空のときはコードにフォールバック。",
        en: "Optional friendly name. UI falls back to code when null.",
        vi: "Tên hiển thị tuỳ chọn. Nếu trống, UI dùng code.",
      },
    },
    seat_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "座席数",
        en: "Seat Count",
        vi: "Số chỗ ngồi",
      },
      placeholder: {
        ja: "座席数を入力",
        en: "Enter Seat Count",
        vi: "Nhập Số chỗ ngồi",
      },
      description: {
        ja: "座席数。1以上必須（BR-T05）。",
        en: "Number of seats. Must be >= 1 (BR-T05).",
        vi: "Số chỗ ngồi. Tối thiểu 1 (BR-T05).",
      },
    },
    status: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "TableStatus",
      enumValues: ["free", "occupied", "reserved", "cleaning", "out_of_service"] as const,
      label: {
        ja: "状態",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "状態を入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
      description: {
        ja: "ランタイム状態。TableStatusService経由で変更し、table_status_changesに記録。",
        en: "Runtime status. Changes via TableStatusService and logged in table_status_changes.",
        vi: "Trạng thái runtime. Đổi qua TableStatusService, log vào table_status_changes.",
      },
    },
    paid_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最終会計日時",
        en: "Last Paid At",
        vi: "Thanh toán lúc",
      },
      placeholder: {
        ja: "最終会計日時を入力",
        en: "Enter Last Paid At",
        vi: "Nhập Thanh toán lúc",
      },
      description: {
        ja: "このテーブルの最終会計日時。TMSで「会計済」表示に使用。",
        en: "Timestamp of the last payment for this table. Used by TMS to show 'recently paid' state.",
        vi: "Thời điểm thanh toán cuối cùng. TMS dùng để hiển thị trạng thái 'vừa thanh toán'.",
      },
    },
    call_requested_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "スタッフ呼出日時",
        en: "Staff Call Requested At",
        vi: "Yêu cầu gọi NV lúc",
      },
      placeholder: {
        ja: "スタッフ呼出日時を入力",
        en: "Enter Staff Call Requested At",
        vi: "Nhập Yêu cầu gọi NV lúc",
      },
      description: {
        ja: "顧客がスタッフを呼び出した日時。nullは呼出なし。TMSで赤色＋ベルアイコン表示。",
        en: "Timestamp when a customer requested staff. Null means no active call. TMS shows red + bell icon.",
        vi: "Thời điểm khách gọi nhân viên. Null = không có yêu cầu. TMS hiển thị đỏ + icon chuông.",
      },
    },
    qr_token: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "QRトークン",
        en: "QR Token",
        vi: "Token QR",
      },
      placeholder: {
        ja: "QRトークンを入力",
        en: "Enter QR Token",
        vi: "Nhập Token QR",
      },
      description: {
        ja: "ランダムなURL-safe base62文字列（32文字）。グローバルに一意（BR-T02）。フロントエンドがこの値からQRを生成。",
        en: "Opaque random URL-safe base62 (32 chars). Unique globally (BR-T02). Frontend renders QR client-side from this string.",
        vi: "Chuỗi opaque random URL-safe base62 (32 ký tự). Unique toàn cục (BR-T02). Frontend render QR từ chuỗi này.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
      description: {
        ja: "無効なテーブルはランタイム状態を変更できません（BR-T03）。",
        en: "Inactive tables cannot have their runtime status changed (BR-T03).",
        vi: "Bàn vô hiệu không cho đổi runtime status (BR-T03).",
      },
    },
    current_order_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrder",
        table: "customer_orders",
        onDelete: "SET NULL",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    zone_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Zone",
        table: "zones",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    table_template_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "TableTemplate",
        table: "table_templates",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["code", "seat_count", "status", "qr_token", "is_active", "organization_id", "branch_id", "zone_id"] as const,
  enumFields: ["status"] as const,
  relationFields: ["current_order_id", "organization_id", "branch_id", "zone_id", "table_template_id"] as const,
} as const;

export type TableFieldName = keyof typeof tableMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Table. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TableCreateFormState {
  code: string;
  name?: string | null;
  seat_count: number;
  status: TableStatus;
  paid_at?: string | null;
  call_requested_at?: string | null;
  qr_token: string;
  is_active: boolean;
  current_order_id?: string | null;
  organization_id: string;
  branch_id: string;
  zone_id: string;
  table_template_id?: string | null;
}

/** Form-state shape for updating a Table. Every field optional. */
export interface TableUpdateFormState {
  code?: string;
  name?: string | null;
  seat_count?: number;
  status?: TableStatus;
  paid_at?: string | null;
  call_requested_at?: string | null;
  qr_token?: string;
  is_active?: boolean;
  current_order_id?: string | null;
  organization_id?: string;
  branch_id?: string;
  zone_id?: string;
  table_template_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTableCreateForm(): TableCreateFormState {
  return {
    code: '',
    name: '',
    seat_count: 2,
    status: TableStatus.Free,
    paid_at: '',
    call_requested_at: '',
    qr_token: '',
    is_active: true,
    current_order_id: '',
    organization_id: '',
    branch_id: '',
    zone_id: '',
    table_template_id: '',
  };
}

/** Convert create form state into the payload that baseTableCreateSchema accepts. */
export function buildTableCreatePayload(form: TableCreateFormState): BaseTableCreate {
  const payload: Record<string, unknown> = {
    code: form.code.trim(),
    name: form.name?.toString().trim() || null,
    seat_count: form.seat_count,
    status: form.status,
    paid_at: form.paid_at,
    call_requested_at: form.call_requested_at,
    qr_token: form.qr_token.trim(),
    is_active: form.is_active,
    current_order_id: form.current_order_id?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
    branch_id: form.branch_id.trim(),
    zone_id: form.zone_id.trim(),
    table_template_id: form.table_template_id?.toString().trim() || null,
  };

  return payload as BaseTableCreate;
}

/** Convert update form state into the payload that baseTableUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTableUpdatePayload(form: TableUpdateFormState): BaseTableUpdate {
  const payload: Record<string, unknown> = {};
  if (form.code !== undefined) {
    payload.code = form.code?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.seat_count !== undefined) {
    payload.seat_count = form.seat_count;
  }
  if (form.status !== undefined) {
    payload.status = form.status;
  }
  if (form.paid_at !== undefined) {
    payload.paid_at = form.paid_at;
  }
  if (form.call_requested_at !== undefined) {
    payload.call_requested_at = form.call_requested_at;
  }
  if (form.qr_token !== undefined) {
    payload.qr_token = form.qr_token?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.current_order_id !== undefined) {
    payload.current_order_id = form.current_order_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.zone_id !== undefined) {
    payload.zone_id = form.zone_id?.toString().trim() ?? null;
  }
  if (form.table_template_id !== undefined) {
    payload.table_template_id = form.table_template_id?.toString().trim() ?? null;
  }
  return payload as BaseTableUpdate;
}
