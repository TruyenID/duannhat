/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { ZoneTemplate } from './ZoneTemplate';

/**
 * テーブルテンプレート
 */
export interface TableTemplate {
  id: string;
  code: string;
  name?: string | null;
  seat_count: number;
  is_active: boolean;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  branch_id?: string | null;
  branch?: Branch | null;
  zoneTemplate_id: string;
  zoneTemplate?: ZoneTemplate;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TableTemplate
 */
export const tableTemplateI18n = {
  label: {"ja":"テーブルテンプレート","en":"Table Template","vi":"Mẫu bàn"},
  fields: {
    code: {
      label: {"ja":"テーブルコード","en":"Table Code","vi":"Mã bàn"},
      placeholder: {"ja":"テーブルコードを入力","en":"Enter Table Code","vi":"Nhập Mã bàn"},
    },
    name: {
      label: {"ja":"テーブル名","en":"Table Name","vi":"Tên bàn"},
      placeholder: {"ja":"テーブル名を入力","en":"Enter Table Name","vi":"Nhập Tên bàn"},
    },
    seat_count: {
      label: {"ja":"座席数","en":"Seat Count","vi":"Số chỗ ngồi"},
      placeholder: {"ja":"座席数を入力","en":"Enter Seat Count","vi":"Nhập Số chỗ ngồi"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"対象店舗","en":"Target Branch","vi":"Chi nhánh áp dụng"},
      placeholder: {"ja":"対象店舗を入力","en":"Enter Target Branch","vi":"Nhập Chi nhánh áp dụng"},
    },
    zone_template: {
      label: {"ja":"エリアテンプレート","en":"Zone Template","vi":"Mẫu khu vực"},
      placeholder: {"ja":"エリアテンプレートを入力","en":"Enter Zone Template","vi":"Nhập Mẫu khu vực"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TableTemplate */
export const baseTableTemplateSchemas = {
  code: z.string().min(1).max(50),
  name: z.string().max(255).optional().nullable(),
  seat_count: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for TableTemplate */
export const baseTableTemplateCreateSchema = z.object({
  code: baseTableTemplateSchemas.code,
  name: baseTableTemplateSchemas.name,
  seat_count: baseTableTemplateSchemas.seat_count,
  is_active: baseTableTemplateSchemas.is_active,
});

/** Update schema for TableTemplate */
export const baseTableTemplateUpdateSchema = baseTableTemplateCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTableTemplateCreate = z.infer<typeof baseTableTemplateCreateSchema>;
export type BaseTableTemplateUpdate = z.infer<typeof baseTableTemplateUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTableTemplateLabel(locale: string): string {
  return tableTemplateI18n.label[locale as keyof typeof tableTemplateI18n.label] ?? tableTemplateI18n.label['en'] ?? 'TableTemplate';
}

/** Get field label for a specific locale */
export function getTableTemplateFieldLabel(field: string, locale: string): string {
  const fields = tableTemplateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTableTemplateFieldPlaceholder(field: string, locale: string): string {
  const fields = tableTemplateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TableTemplate.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tableTemplateMetadata = {
  modelName: "TableTemplate",
  table: "table_templates",

  fields: {
    code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "テーブルコード",
        en: "Table Code",
        vi: "Mã bàn",
      },
      placeholder: {
        ja: "テーブルコードを入力",
        en: "Enter Table Code",
        vi: "Nhập Mã bàn",
      },
      description: {
        ja: "ブランド内で一意のコード（BR-TT01）。例：T-01。既定適用時に店舗テーブルへコピー。",
        en: "Unique code per brand (BR-TT01). Example: T-01. Copied to the shop table on apply-defaults.",
        vi: "Mã unique trong brand (BR-TT01). Ví dụ: T-01. Copy sang bàn của shop khi nhận mặc định.",
      },
    },
    name: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "テーブル名",
        en: "Table Name",
        vi: "Tên bàn",
      },
      placeholder: {
        ja: "テーブル名を入力",
        en: "Enter Table Name",
        vi: "Nhập Tên bàn",
      },
      description: {
        ja: "任意の表示名。空のときはコードにフォールバック。",
        en: "Optional friendly name. UI falls back to code when null.",
        vi: "Tên hiển thị tuỳ chọn. Nếu trống, UI dùng code.",
      },
    },
    seat_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "座席数",
        en: "Seat Count",
        vi: "Số chỗ ngồi",
      },
      placeholder: {
        ja: "座席数を入力",
        en: "Enter Seat Count",
        vi: "Nhập Số chỗ ngồi",
      },
      description: {
        ja: "座席数。1以上必須（BR-TT03）。",
        en: "Number of seats. Must be >= 1 (BR-TT03).",
        vi: "Số chỗ ngồi. Tối thiểu 1 (BR-TT03).",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
      description: {
        ja: "無効なテンプレートは既定適用時にスキップされます。",
        en: "Inactive templates are skipped when a shop applies brand defaults.",
        vi: "Template vô hiệu sẽ bị bỏ qua khi shop nhận mặc định.",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    zone_template_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ZoneTemplate",
        table: "zone_templates",
        onDelete: "RESTRICT",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["code", "seat_count", "is_active", "organization_id", "brand_id", "zone_template_id"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "branch_id", "zone_template_id"] as const,
} as const;

export type TableTemplateFieldName = keyof typeof tableTemplateMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TableTemplate. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TableTemplateCreateFormState {
  code: string;
  name?: string | null;
  seat_count: number;
  is_active: boolean;
  organization_id: string;
  brand_id: string;
  branch_id?: string | null;
  zone_template_id: string;
}

/** Form-state shape for updating a TableTemplate. Every field optional. */
export interface TableTemplateUpdateFormState {
  code?: string;
  name?: string | null;
  seat_count?: number;
  is_active?: boolean;
  organization_id?: string;
  brand_id?: string;
  branch_id?: string | null;
  zone_template_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTableTemplateCreateForm(): TableTemplateCreateFormState {
  return {
    code: '',
    name: '',
    seat_count: 2,
    is_active: true,
    organization_id: '',
    brand_id: '',
    branch_id: '',
    zone_template_id: '',
  };
}

/** Convert create form state into the payload that baseTableTemplateCreateSchema accepts. */
export function buildTableTemplateCreatePayload(form: TableTemplateCreateFormState): BaseTableTemplateCreate {
  const payload: Record<string, unknown> = {
    code: form.code.trim(),
    name: form.name?.toString().trim() || null,
    seat_count: form.seat_count,
    is_active: form.is_active,
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    branch_id: form.branch_id?.toString().trim() || null,
    zone_template_id: form.zone_template_id.trim(),
  };

  return payload as BaseTableTemplateCreate;
}

/** Convert update form state into the payload that baseTableTemplateUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTableTemplateUpdatePayload(form: TableTemplateUpdateFormState): BaseTableTemplateUpdate {
  const payload: Record<string, unknown> = {};
  if (form.code !== undefined) {
    payload.code = form.code?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.seat_count !== undefined) {
    payload.seat_count = form.seat_count;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.zone_template_id !== undefined) {
    payload.zone_template_id = form.zone_template_id?.toString().trim() ?? null;
  }
  return payload as BaseTableTemplateUpdate;
}
