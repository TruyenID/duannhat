/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { FileStatus } from '../enum/FileStatus';

/**
 * ファイル
 */
export interface File {
  id: string;
  organization_id?: string;
  fileable_type?: string;
  fileable_id?: string;
  collection: string;
  disk: string;
  path: string;
  original_name: string;
  mime_type: string;
  size: number;
  status: FileStatus;
  expires_at?: DateTimeString;
  sort_order: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for File
 */
export const fileI18n = {
  label: {"ja":"ファイル","en":"File","vi":"Tệp"},
  fields: {
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID Tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID Tổ chức"},
    },
    fileable_type: {
      label: {"ja":"関連モデル種別","en":"Fileable Type","vi":"Loại đối tượng"},
      placeholder: {"ja":"関連モデル種別を入力","en":"Enter Fileable Type","vi":"Nhập Loại đối tượng"},
    },
    fileable_id: {
      label: {"ja":"関連モデルID","en":"Fileable ID","vi":"ID đối tượng"},
      placeholder: {"ja":"関連モデルIDを入力","en":"Enter Fileable ID","vi":"Nhập ID đối tượng"},
    },
    collection: {
      label: {"ja":"コレクション","en":"Collection","vi":"Bộ sưu tập"},
      placeholder: {"ja":"コレクションを入力","en":"Enter Collection","vi":"Nhập Bộ sưu tập"},
    },
    disk: {
      label: {"ja":"ディスク","en":"Disk","vi":"Đĩa"},
      placeholder: {"ja":"ディスクを入力","en":"Enter Disk","vi":"Nhập Đĩa"},
    },
    path: {
      label: {"ja":"パス","en":"Path","vi":"Đường dẫn"},
      placeholder: {"ja":"パスを入力","en":"Enter Path","vi":"Nhập Đường dẫn"},
    },
    original_name: {
      label: {"ja":"ファイル名","en":"File Name","vi":"Tên tệp"},
      placeholder: {"ja":"ファイル名を入力","en":"Enter File Name","vi":"Nhập Tên tệp"},
    },
    mime_type: {
      label: {"ja":"MIMEタイプ","en":"MIME Type","vi":"Loại MIME"},
      placeholder: {"ja":"MIMEタイプを入力","en":"Enter MIME Type","vi":"Nhập Loại MIME"},
    },
    size: {
      label: {"ja":"サイズ","en":"Size","vi":"Kích thước"},
      placeholder: {"ja":"サイズを入力","en":"Enter Size","vi":"Nhập Kích thước"},
    },
    status: {
      label: {"ja":"ステータス","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"ステータスを入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    expires_at: {
      label: {"ja":"有効期限","en":"Expires At","vi":"Hết hạn"},
      placeholder: {"ja":"有効期限を入力","en":"Enter Expires At","vi":"Nhập Hết hạn"},
    },
    sort_order: {
      label: {"ja":"並び順","en":"Sort Order","vi":"Thứ tự sắp xếp"},
      placeholder: {"ja":"並び順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự sắp xếp"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for File */
export const baseFileSchemas = {
  organization_id: z.string().optional().nullable(),
  fileable_type: z.string().optional().nullable(),
  fileable_id: z.string().optional().nullable(),
  collection: z.string().min(1),
  disk: z.string().min(1),
  path: z.string().min(1),
  original_name: z.string().min(1),
  mime_type: z.string().min(1),
  size: z.number().int(),
  status: z.nativeEnum(FileStatus),
  expires_at: z.string().datetime({ offset: true }).optional().nullable(),
  sort_order: z.number().int(),
} as const;

/** Create schema for File */
export const baseFileCreateSchema = z.object({
  organization_id: baseFileSchemas.organization_id,
  fileable_type: baseFileSchemas.fileable_type,
  fileable_id: baseFileSchemas.fileable_id,
  collection: baseFileSchemas.collection,
  disk: baseFileSchemas.disk,
  path: baseFileSchemas.path,
  original_name: baseFileSchemas.original_name,
  mime_type: baseFileSchemas.mime_type,
  size: baseFileSchemas.size,
  status: baseFileSchemas.status,
  expires_at: baseFileSchemas.expires_at,
  sort_order: baseFileSchemas.sort_order,
});

/** Update schema for File */
export const baseFileUpdateSchema = baseFileCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseFileCreate = z.infer<typeof baseFileCreateSchema>;
export type BaseFileUpdate = z.infer<typeof baseFileUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getFileLabel(locale: string): string {
  return fileI18n.label[locale as keyof typeof fileI18n.label] ?? fileI18n.label['en'] ?? 'File';
}

/** Get field label for a specific locale */
export function getFileFieldLabel(field: string, locale: string): string {
  const fieldI18n = fileI18n.fields[field as keyof typeof fileI18n.fields];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale as keyof typeof fieldI18n.label] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getFileFieldPlaceholder(field: string, locale: string): string {
  const fieldI18n = fileI18n.fields[field as keyof typeof fileI18n.fields];
  if (!fieldI18n || !('placeholder' in fieldI18n)) return '';
  const placeholder = fieldI18n.placeholder as Record<string, string>;
  return placeholder[locale] ?? placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for File.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const fileMetadata = {
  modelName: "File",
  table: "files",

  fields: {
    organization_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID Tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID Tổ chức",
      },
    },
    fileable_type: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "関連モデル種別",
        en: "Fileable Type",
        vi: "Loại đối tượng",
      },
      placeholder: {
        ja: "関連モデル種別を入力",
        en: "Enter Fileable Type",
        vi: "Nhập Loại đối tượng",
      },
    },
    fileable_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "関連モデルID",
        en: "Fileable ID",
        vi: "ID đối tượng",
      },
      placeholder: {
        ja: "関連モデルIDを入力",
        en: "Enter Fileable ID",
        vi: "Nhập ID đối tượng",
      },
    },
    collection: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "コレクション",
        en: "Collection",
        vi: "Bộ sưu tập",
      },
      placeholder: {
        ja: "コレクションを入力",
        en: "Enter Collection",
        vi: "Nhập Bộ sưu tập",
      },
      description: {
        ja: "論理グループ（例：avatar, documents, gallery）。",
        en: "Logical grouping (e.g. 'avatar', 'documents', 'gallery').",
        vi: "Nhóm logic (vd: avatar, documents, gallery).",
      },
    },
    disk: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ディスク",
        en: "Disk",
        vi: "Đĩa",
      },
      placeholder: {
        ja: "ディスクを入力",
        en: "Enter Disk",
        vi: "Nhập Đĩa",
      },
      description: {
        ja: "ストレージディスク名（例：public, s3）。",
        en: "Storage disk name (e.g. 'public', 's3').",
        vi: "Tên disk lưu trữ (vd: public, s3).",
      },
    },
    path: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "パス",
        en: "Path",
        vi: "Đường dẫn",
      },
      placeholder: {
        ja: "パスを入力",
        en: "Enter Path",
        vi: "Nhập Đường dẫn",
      },
      description: {
        ja: "ディスク内の相対パス。",
        en: "Relative path within the disk.",
        vi: "Đường dẫn tương đối trong disk.",
      },
    },
    original_name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ファイル名",
        en: "File Name",
        vi: "Tên tệp",
      },
      placeholder: {
        ja: "ファイル名を入力",
        en: "Enter File Name",
        vi: "Nhập Tên tệp",
      },
    },
    mime_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "MIMEタイプ",
        en: "MIME Type",
        vi: "Loại MIME",
      },
      placeholder: {
        ja: "MIMEタイプを入力",
        en: "Enter MIME Type",
        vi: "Nhập Loại MIME",
      },
    },
    size: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "サイズ",
        en: "Size",
        vi: "Kích thước",
      },
      placeholder: {
        ja: "サイズを入力",
        en: "Enter Size",
        vi: "Nhập Kích thước",
      },
      description: {
        ja: "ファイルサイズ（バイト）。",
        en: "File size in bytes.",
        vi: "Kích thước tệp (byte).",
      },
    },
    status: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "FileStatus",
      enumValues: ["temporary", "permanent"] as const,
      label: {
        ja: "ステータス",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "ステータスを入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    expires_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "有効期限",
        en: "Expires At",
        vi: "Hết hạn",
      },
      placeholder: {
        ja: "有効期限を入力",
        en: "Enter Expires At",
        vi: "Nhập Hết hạn",
      },
      description: {
        ja: "一時ファイルの自動削除時刻。永続ファイルはnull。",
        en: "Auto-purge time for temporary files. Null for permanent files.",
        vi: "Thời gian tự xóa tệp tạm. Null cho tệp vĩnh viễn.",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "並び順",
        en: "Sort Order",
        vi: "Thứ tự sắp xếp",
      },
      placeholder: {
        ja: "並び順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự sắp xếp",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["collection", "disk", "path", "original_name", "mime_type", "size", "status", "sort_order"] as const,
  enumFields: ["status"] as const,
  relationFields: [] as const,
} as const;

export type FileFieldName = keyof typeof fileMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a File. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface FileCreateFormState {
  organization_id?: string | null;
  fileable_type?: string | null;
  fileable_id?: string | null;
  collection: string;
  disk: string;
  path: string;
  original_name: string;
  mime_type: string;
  size: number;
  status: FileStatus;
  expires_at?: string | null;
  sort_order: number;
}

/** Form-state shape for updating a File. Every field optional. */
export interface FileUpdateFormState {
  organization_id?: string | null;
  fileable_type?: string | null;
  fileable_id?: string | null;
  collection?: string;
  disk?: string;
  path?: string;
  original_name?: string;
  mime_type?: string;
  size?: number;
  status?: FileStatus;
  expires_at?: string | null;
  sort_order?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyFileCreateForm(): FileCreateFormState {
  return {
    organization_id: '',
    fileable_type: '',
    fileable_id: '',
    collection: "default",
    disk: '',
    path: '',
    original_name: '',
    mime_type: '',
    size: 0,
    status: FileStatus.Temporary,
    expires_at: '',
    sort_order: 0,
  };
}

/** Convert create form state into the payload that baseFileCreateSchema accepts. */
export function buildFileCreatePayload(form: FileCreateFormState): BaseFileCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id?.toString().trim() || null,
    fileable_type: form.fileable_type?.toString().trim() || null,
    fileable_id: form.fileable_id?.toString().trim() || null,
    collection: form.collection.trim(),
    disk: form.disk.trim(),
    path: form.path.trim(),
    original_name: form.original_name.trim(),
    mime_type: form.mime_type.trim(),
    size: form.size,
    status: form.status,
    expires_at: form.expires_at,
    sort_order: form.sort_order,
  };

  return payload as BaseFileCreate;
}

/** Convert update form state into the payload that baseFileUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildFileUpdatePayload(form: FileUpdateFormState): BaseFileUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.fileable_type !== undefined) {
    payload.fileable_type = form.fileable_type?.toString().trim() ?? null;
  }
  if (form.fileable_id !== undefined) {
    payload.fileable_id = form.fileable_id?.toString().trim() ?? null;
  }
  if (form.collection !== undefined) {
    payload.collection = form.collection?.toString().trim() ?? null;
  }
  if (form.disk !== undefined) {
    payload.disk = form.disk?.toString().trim() ?? null;
  }
  if (form.path !== undefined) {
    payload.path = form.path?.toString().trim() ?? null;
  }
  if (form.original_name !== undefined) {
    payload.original_name = form.original_name?.toString().trim() ?? null;
  }
  if (form.mime_type !== undefined) {
    payload.mime_type = form.mime_type?.toString().trim() ?? null;
  }
  if (form.size !== undefined) {
    payload.size = form.size;
  }
  if (form.status !== undefined) {
    payload.status = form.status;
  }
  if (form.expires_at !== undefined) {
    payload.expires_at = form.expires_at;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  return payload as BaseFileUpdate;
}
