/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Material } from './Material';
import type { ProductSku } from './ProductSku';
import type { StockCount } from './StockCount';

/**
 * 棚卸明細
 */
export interface StockCountItem {
  id: string;
  stockCount_id: string;
  stockCount?: StockCount;
  productSku_id?: string | null;
  productSku?: ProductSku | null;
  material_id?: string | null;
  material?: Material | null;
  system_quantity: number;
  counted_quantity?: number | null;
  difference?: number | null;
  unit?: string | null;
  note?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for StockCountItem
 */
export const stockCountItemI18n = {
  label: {"ja":"棚卸明細","en":"Stock Count Item","vi":"Chi tiết kiểm kê"},
  fields: {
    stock_count: {
      label: {"ja":"棚卸","en":"Stock Count","vi":"Kiểm kê"},
      placeholder: {"ja":"棚卸を入力","en":"Enter Stock Count","vi":"Nhập Kiểm kê"},
    },
    product_sku: {
      label: {"ja":"商品バリエーション","en":"Product Variant","vi":"Biến thể sản phẩm"},
      placeholder: {"ja":"商品バリエーションを入力","en":"Enter Product Variant","vi":"Nhập Biến thể sản phẩm"},
    },
    material: {
      label: {"ja":"材料","en":"Material","vi":"Nguyên liệu"},
      placeholder: {"ja":"材料を入力","en":"Enter Material","vi":"Nhập Nguyên liệu"},
    },
    system_quantity: {
      label: {"ja":"システム数量","en":"System Quantity","vi":"Số lượng hệ thống"},
      placeholder: {"ja":"システム数量を入力","en":"Enter System Quantity","vi":"Nhập Số lượng hệ thống"},
    },
    counted_quantity: {
      label: {"ja":"実数","en":"Counted Quantity","vi":"Số lượng thực tế"},
      placeholder: {"ja":"実数を入力","en":"Enter Counted Quantity","vi":"Nhập Số lượng thực tế"},
    },
    difference: {
      label: {"ja":"差異","en":"Difference","vi":"Chênh lệch"},
      placeholder: {"ja":"差異を入力","en":"Enter Difference","vi":"Nhập Chênh lệch"},
    },
    unit: {
      label: {"ja":"単位","en":"Unit","vi":"Đơn vị"},
      placeholder: {"ja":"単位を入力","en":"Enter Unit","vi":"Nhập Đơn vị"},
    },
    note: {
      label: {"ja":"備考","en":"Note","vi":"Ghi chú"},
      placeholder: {"ja":"備考を入力","en":"Enter Note","vi":"Nhập Ghi chú"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for StockCountItem */
export const baseStockCountItemSchemas = {
  system_quantity: z.number(),
  counted_quantity: z.number().optional().nullable(),
  difference: z.number().optional().nullable(),
  unit: z.string().max(50).optional().nullable(),
  note: z.string().optional().nullable(),
} as const;

/** Create schema for StockCountItem */
export const baseStockCountItemCreateSchema = z.object({
  system_quantity: baseStockCountItemSchemas.system_quantity,
  counted_quantity: baseStockCountItemSchemas.counted_quantity,
  difference: baseStockCountItemSchemas.difference,
  unit: baseStockCountItemSchemas.unit,
  note: baseStockCountItemSchemas.note,
});

/** Update schema for StockCountItem */
export const baseStockCountItemUpdateSchema = baseStockCountItemCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseStockCountItemCreate = z.infer<typeof baseStockCountItemCreateSchema>;
export type BaseStockCountItemUpdate = z.infer<typeof baseStockCountItemUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getStockCountItemLabel(locale: string): string {
  return stockCountItemI18n.label[locale as keyof typeof stockCountItemI18n.label] ?? stockCountItemI18n.label['en'] ?? 'StockCountItem';
}

/** Get field label for a specific locale */
export function getStockCountItemFieldLabel(field: string, locale: string): string {
  const fields = stockCountItemI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getStockCountItemFieldPlaceholder(field: string, locale: string): string {
  const fields = stockCountItemI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for StockCountItem.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const stockCountItemMetadata = {
  modelName: "StockCountItem",
  table: "stock_count_items",

  fields: {
    stock_count_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "StockCount",
        table: "stock_counts",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    material_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Material",
        table: "materials",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    system_quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "システム数量",
        en: "System Quantity",
        vi: "Số lượng hệ thống",
      },
      placeholder: {
        ja: "システム数量を入力",
        en: "Enter System Quantity",
        vi: "Nhập Số lượng hệ thống",
      },
      description: {
        ja: "棚卸作成時の在庫レベルのスナップショット（BR-02：スナップショット後は更新されない）",
        en: "Snapshot of stock level at count creation time (BR-02: never updated after snapshot)",
        vi: "Snapshot tồn kho tại thời điểm tạo phiên (BR-02: KHÔNG cập nhật sau snapshot)",
      },
    },
    counted_quantity: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "実数",
        en: "Counted Quantity",
        vi: "Số lượng thực tế",
      },
      placeholder: {
        ja: "実数を入力",
        en: "Enter Counted Quantity",
        vi: "Nhập Số lượng thực tế",
      },
      description: {
        ja: "スタッフが実際にカウントした数量。カウントされるまでNull。",
        en: "Actual quantity counted by staff. Null until counted.",
        vi: "Số lượng thực tế đếm được. Null cho đến khi đếm.",
      },
    },
    difference: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "差異",
        en: "Difference",
        vi: "Chênh lệch",
      },
      placeholder: {
        ja: "差異を入力",
        en: "Enter Difference",
        vi: "Nhập Chênh lệch",
      },
      description: {
        ja: "counted_quantity - system_quantity。正 = 過剰、負 = 不足。カウントされるまでNull。",
        en: "counted_quantity - system_quantity. Positive = surplus, negative = shortage. Null until counted.",
        vi: "counted_quantity - system_quantity. Dương = thừa, âm = thiếu. Null cho đến khi đếm.",
      },
    },
    unit: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "単位",
        en: "Unit",
        vi: "Đơn vị",
      },
      placeholder: {
        ja: "単位を入力",
        en: "Enter Unit",
        vi: "Nhập Đơn vị",
      },
      description: {
        ja: "この品目の単位（例：kg、個、L）",
        en: "Unit of measurement for this item (e.g., kg, pcs, L)",
        vi: "Đơn vị đo lường (VD: kg, cái, L)",
      },
    },
    note: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "備考",
        en: "Note",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "備考を入力",
        en: "Enter Note",
        vi: "Nhập Ghi chú",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["stock_count_id", "system_quantity"] as const,
  enumFields: [] as const,
  relationFields: ["stock_count_id", "product_sku_id", "material_id"] as const,
} as const;

export type StockCountItemFieldName = keyof typeof stockCountItemMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a StockCountItem. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface StockCountItemCreateFormState {
  stock_count_id: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  system_quantity: number;
  counted_quantity?: number | null;
  difference?: number | null;
  unit?: string | null;
  note?: string | null;
}

/** Form-state shape for updating a StockCountItem. Every field optional. */
export interface StockCountItemUpdateFormState {
  stock_count_id?: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  system_quantity?: number;
  counted_quantity?: number | null;
  difference?: number | null;
  unit?: string | null;
  note?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyStockCountItemCreateForm(): StockCountItemCreateFormState {
  return {
    stock_count_id: '',
    product_sku_id: '',
    material_id: '',
    system_quantity: 0,
    counted_quantity: 0,
    difference: 0,
    unit: '',
    note: '',
  };
}

/** Convert create form state into the payload that baseStockCountItemCreateSchema accepts. */
export function buildStockCountItemCreatePayload(form: StockCountItemCreateFormState): BaseStockCountItemCreate {
  const payload: Record<string, unknown> = {
    stock_count_id: form.stock_count_id.trim(),
    product_sku_id: form.product_sku_id?.toString().trim() || null,
    material_id: form.material_id?.toString().trim() || null,
    system_quantity: form.system_quantity,
    counted_quantity: form.counted_quantity,
    difference: form.difference,
    unit: form.unit?.toString().trim() || null,
    note: form.note?.toString().trim() || null,
  };

  return payload as BaseStockCountItemCreate;
}

/** Convert update form state into the payload that baseStockCountItemUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildStockCountItemUpdatePayload(form: StockCountItemUpdateFormState): BaseStockCountItemUpdate {
  const payload: Record<string, unknown> = {};
  if (form.stock_count_id !== undefined) {
    payload.stock_count_id = form.stock_count_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.material_id !== undefined) {
    payload.material_id = form.material_id?.toString().trim() ?? null;
  }
  if (form.system_quantity !== undefined) {
    payload.system_quantity = form.system_quantity;
  }
  if (form.counted_quantity !== undefined) {
    payload.counted_quantity = form.counted_quantity;
  }
  if (form.difference !== undefined) {
    payload.difference = form.difference;
  }
  if (form.unit !== undefined) {
    payload.unit = form.unit?.toString().trim() ?? null;
  }
  if (form.note !== undefined) {
    payload.note = form.note?.toString().trim() ?? null;
  }
  return payload as BaseStockCountItemUpdate;
}
