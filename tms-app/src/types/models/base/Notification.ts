/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { NotificationPriority } from '../enum/NotificationPriority';
import type { Organization } from './Organization';

/**
 * 通知
 */
export interface Notification {
  id: string;
  organization_id: string;
  organization?: Organization;
  actor_type?: string | null;
  actor_id?: string | null;
  type: string;
  template_key: string;
  params?: unknown | null;
  subject_type?: string | null;
  subject_id?: string | null;
  priority: NotificationPriority;
  aggregation_key?: string | null;
  idempotency_key?: string | null;
  audience_id?: string | null;
  scheduled_for?: DateTimeString | null;
  is_dispatched: boolean;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Notification
 */
export const notificationI18n = {
  label: {"ja":"通知","en":"Notification","vi":"Thông báo"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    actor_type: {
      label: {"ja":"アクタータイプ","en":"Actor Type","vi":"Loại chủ thể"},
      placeholder: {"ja":"アクタータイプを入力","en":"Enter Actor Type","vi":"Nhập Loại chủ thể"},
    },
    actor_id: {
      label: {"ja":"アクターID","en":"Actor ID","vi":"ID chủ thể"},
      placeholder: {"ja":"アクターIDを入力","en":"Enter Actor ID","vi":"Nhập ID chủ thể"},
    },
    type: {
      label: {"ja":"タイプ","en":"Type","vi":"Loại"},
      placeholder: {"ja":"タイプを入力","en":"Enter Type","vi":"Nhập Loại"},
    },
    template_key: {
      label: {"ja":"テンプレートキー","en":"Template Key","vi":"Khóa template"},
      placeholder: {"ja":"テンプレートキーを入力","en":"Enter Template Key","vi":"Nhập Khóa template"},
    },
    params: {
      label: {"ja":"パラメータ","en":"Params","vi":"Tham số"},
      placeholder: {"ja":"パラメータを入力","en":"Enter Params","vi":"Nhập Tham số"},
    },
    subject_type: {
      label: {"ja":"サブジェクトタイプ","en":"Subject Type","vi":"Loại đối tượng"},
      placeholder: {"ja":"サブジェクトタイプを入力","en":"Enter Subject Type","vi":"Nhập Loại đối tượng"},
    },
    subject_id: {
      label: {"ja":"サブジェクトID","en":"Subject ID","vi":"ID đối tượng"},
      placeholder: {"ja":"サブジェクトIDを入力","en":"Enter Subject ID","vi":"Nhập ID đối tượng"},
    },
    priority: {
      label: {"ja":"優先度","en":"Priority","vi":"Độ ưu tiên"},
      placeholder: {"ja":"優先度を入力","en":"Enter Priority","vi":"Nhập Độ ưu tiên"},
    },
    aggregation_key: {
      label: {"ja":"集約キー","en":"Aggregation Key","vi":"Khóa gộp"},
      placeholder: {"ja":"集約キーを入力","en":"Enter Aggregation Key","vi":"Nhập Khóa gộp"},
    },
    idempotency_key: {
      label: {"ja":"冪等性キー","en":"Idempotency Key","vi":"Khóa idempotency"},
      placeholder: {"ja":"冪等性キーを入力","en":"Enter Idempotency Key","vi":"Nhập Khóa idempotency"},
    },
    audience_id: {
      label: {"ja":"オーディエンスID","en":"Audience ID","vi":"ID audience"},
      placeholder: {"ja":"オーディエンスIDを入力","en":"Enter Audience ID","vi":"Nhập ID audience"},
    },
    scheduled_for: {
      label: {"ja":"予約送信日時","en":"Scheduled For","vi":"Thời điểm gửi"},
      placeholder: {"ja":"予約送信日時を入力","en":"Enter Scheduled For","vi":"Nhập Thời điểm gửi"},
    },
    is_dispatched: {
      label: {"ja":"送信済み","en":"Is Dispatched","vi":"Đã gửi"},
      placeholder: {"ja":"送信済みを入力","en":"Enter Is Dispatched","vi":"Nhập Đã gửi"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Notification */
export const baseNotificationSchemas = {
  actor_type: z.string().max(60).optional().nullable(),
  actor_id: z.string().optional().nullable(),
  type: z.string().min(1).max(100),
  template_key: z.string().min(1).max(100),
  params: z.unknown().optional().nullable(),
  subject_type: z.string().max(60).optional().nullable(),
  subject_id: z.string().optional().nullable(),
  priority: z.nativeEnum(NotificationPriority),
  aggregation_key: z.string().max(120).optional().nullable(),
  idempotency_key: z.string().max(120).optional().nullable(),
  audience_id: z.string().optional().nullable(),
  scheduled_for: z.string().datetime({ offset: true }).optional().nullable(),
  is_dispatched: z.boolean(),
} as const;

/** Create schema for Notification */
export const baseNotificationCreateSchema = z.object({
  actor_type: baseNotificationSchemas.actor_type,
  actor_id: baseNotificationSchemas.actor_id,
  type: baseNotificationSchemas.type,
  template_key: baseNotificationSchemas.template_key,
  params: baseNotificationSchemas.params,
  subject_type: baseNotificationSchemas.subject_type,
  subject_id: baseNotificationSchemas.subject_id,
  priority: baseNotificationSchemas.priority,
  aggregation_key: baseNotificationSchemas.aggregation_key,
  idempotency_key: baseNotificationSchemas.idempotency_key,
  audience_id: baseNotificationSchemas.audience_id,
  scheduled_for: baseNotificationSchemas.scheduled_for,
  is_dispatched: baseNotificationSchemas.is_dispatched,
});

/** Update schema for Notification */
export const baseNotificationUpdateSchema = baseNotificationCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationCreate = z.infer<typeof baseNotificationCreateSchema>;
export type BaseNotificationUpdate = z.infer<typeof baseNotificationUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationLabel(locale: string): string {
  return notificationI18n.label[locale as keyof typeof notificationI18n.label] ?? notificationI18n.label['en'] ?? 'Notification';
}

/** Get field label for a specific locale */
export function getNotificationFieldLabel(field: string, locale: string): string {
  const fields = notificationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Notification.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationMetadata = {
  modelName: "Notification",
  table: "notifications",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    actor_type: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 60,
      },
      label: {
        ja: "アクタータイプ",
        en: "Actor Type",
        vi: "Loại chủ thể",
      },
      placeholder: {
        ja: "アクタータイプを入力",
        en: "Enter Actor Type",
        vi: "Nhập Loại chủ thể",
      },
      description: {
        ja: "morph エイリアス（例：'user'、'device'）。クラスパスではない。システムイベントは null。",
        en: "Morph alias (e.g. 'user', 'device') — NOT a class path. Null for system events.",
        vi: "Morph alias (VD: 'user', 'device') — KHÔNG phải class path. Null cho sự kiện hệ thống.",
      },
    },
    actor_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "アクターID",
        en: "Actor ID",
        vi: "ID chủ thể",
      },
      placeholder: {
        ja: "アクターIDを入力",
        en: "Enter Actor ID",
        vi: "Nhập ID chủ thể",
      },
      description: {
        ja: "アクターレコードの主キー。`actor_type` が null の場合のみ null（サービスで検証）。",
        en: "PK of the actor record. Null iff `actor_type` is null (validated in service).",
        vi: "PK của record chủ thể. Null khi và chỉ khi `actor_type` null (validate trong service).",
      },
    },
    type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "タイプ",
        en: "Type",
        vi: "Loại",
      },
      placeholder: {
        ja: "タイプを入力",
        en: "Enter Type",
        vi: "Nhập Loại",
      },
      description: {
        ja: "ドット区切りのイベント名。例：`recipe.approved`、`stock.alert.low`、`system.broadcast`。null アクター + `system.*` タイプでシステムイベントを区別。",
        en: "Dot-namespaced event name. Examples: `recipe.approved`, `stock.alert.low`, `system.broadcast`. Null actor + `system.*` type distinguishes system events.",
        vi: "Tên sự kiện phân cách bằng dấu chấm. VD: `recipe.approved`, `stock.alert.low`, `system.broadcast`. Actor null + type `system.*` = sự kiện hệ thống.",
      },
    },
    template_key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "テンプレートキー",
        en: "Template Key",
        vi: "Khóa template",
      },
      placeholder: {
        ja: "テンプレートキーを入力",
        en: "Enter Template Key",
        vi: "Nhập Khóa template",
      },
      description: {
        ja: "テンプレート参照キー。フェーズAではFE i18n バンドルのキーと一致。フェーズBでは `notification_templates.key` へのソフトFKになる（DB FK なし — サービス経由で参照）。通常 `type` と同じだが異なっても許容。",
        en: "Template lookup key. In Phase A matches the FE i18n bundle key. In Phase B becomes a soft FK into `notification_templates.key` (no DB FK — lookup via service). Usually identical to `type` but allowed to diverge.",
        vi: "Khóa lookup template. Phase A trùng với key của FE i18n bundle. Phase B trở thành soft FK vào `notification_templates.key` (không FK DB — lookup qua service). Thường trùng `type` nhưng cho phép khác.",
      },
    },
    params: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "パラメータ",
        en: "Params",
        vi: "Tham số",
      },
      placeholder: {
        ja: "パラメータを入力",
        en: "Enter Params",
        vi: "Nhập Tham số",
      },
      description: {
        ja: "テンプレートパラメータの JSON。例：{\"recipe_name\":\"餃子\",\"approver\":\"田中\"}。応答時にテンプレートの `{{param}}` トークンへ埋め込む。",
        en: "Template parameters as JSON. E.g. {\"recipe_name\":\"Gyoza\",\"approver\":\"Tanaka\"}. Substituted into template `{{param}}` tokens at response time.",
        vi: "Tham số template dạng JSON. VD: {\"recipe_name\":\"Gyoza\",\"approver\":\"Tanaka\"}. Substitute vào token `{{param}}` của template lúc render.",
      },
    },
    subject_type: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 60,
      },
      label: {
        ja: "サブジェクトタイプ",
        en: "Subject Type",
        vi: "Loại đối tượng",
      },
      placeholder: {
        ja: "サブジェクトタイプを入力",
        en: "Enter Subject Type",
        vi: "Nhập Loại đối tượng",
      },
      description: {
        ja: "関連エンティティの morph エイリアス（例：'recipe'、'customer_order'、'stock_alert'）。`subject_id` が null の場合のみ null。",
        en: "Morph alias of the related entity (e.g. 'recipe', 'customer_order', 'stock_alert'). Null iff `subject_id` is null.",
        vi: "Morph alias của entity liên quan (VD: 'recipe', 'customer_order', 'stock_alert'). Null khi và chỉ khi `subject_id` null.",
      },
    },
    subject_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "サブジェクトID",
        en: "Subject ID",
        vi: "ID đối tượng",
      },
      placeholder: {
        ja: "サブジェクトIDを入力",
        en: "Enter Subject ID",
        vi: "Nhập ID đối tượng",
      },
      description: {
        ja: "サブジェクトレコードの主キー。`subject_type` と null を揃える（ディスパッチサービスでアプリレベル CHECK）。",
        en: "PK of the subject record. Null-together with `subject_type` (app-level CHECK in dispatch service).",
        vi: "PK record đối tượng. Null cùng với `subject_type` (CHECK app-level trong dispatch service).",
      },
    },
    priority: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "NotificationPriority",
      enumValues: ["low", "normal", "high", "urgent"] as const,
      label: {
        ja: "優先度",
        en: "Priority",
        vi: "Độ ưu tiên",
      },
      placeholder: {
        ja: "優先度を入力",
        en: "Enter Priority",
        vi: "Nhập Độ ưu tiên",
      },
      description: {
        ja: "`low` / `normal` / `high` / `urgent`。デフォルト `normal`。`urgent` はマスターミュート + クワイエットアワーをバイパス（決定10）だが、タイプ固有のオプトアウトは尊重。",
        en: "`low` / `normal` / `high` / `urgent`. Default `normal`. `urgent` bypasses master mute + quiet hours (Decision 10) but not type-specific opt-out.",
        vi: "`low` / `normal` / `high` / `urgent`. Mặc định `normal`. `urgent` bypass master mute + quiet hours (Decision 10) nhưng vẫn tôn trọng type-specific opt-out.",
      },
    },
    aggregation_key: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 120,
      },
      label: {
        ja: "集約キー",
        en: "Aggregation Key",
        vi: "Khóa gộp",
      },
      placeholder: {
        ja: "集約キーを入力",
        en: "Enter Aggregation Key",
        vi: "Nhập Khóa gộp",
      },
      description: {
        ja: "受信箱の折りたたみ用グルーピングキー（例：`order.new:{shop_id}`）。フェーズAから保存するが v1 ではレンダリングしない。前方互換性のため。",
        en: "Future-use grouping key for inbox collapse (e.g. `order.new:{shop_id}`). Persisted since Phase A but not rendered in v1; forward-compatible.",
        vi: "Khóa gộp cho collapse inbox sau này (VD: `order.new:{shop_id}`). Lưu từ Phase A nhưng v1 không render; forward-compatible.",
      },
    },
    idempotency_key: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 120,
      },
      label: {
        ja: "冪等性キー",
        en: "Idempotency Key",
        vi: "Khóa idempotency",
      },
      placeholder: {
        ja: "冪等性キーを入力",
        en: "Enter Idempotency Key",
        vi: "Nhập Khóa idempotency",
      },
      description: {
        ja: "キューリトライ時の重複ディスパッチ防止用の安定キー。非nullのとき一意（ユニークインデックスで強制）。リトライ安全なイベントではエミッターで必ず指定。",
        en: "Stable key to prevent duplicate dispatches on queue retry. Unique when non-null (enforced by unique index). Emitters MUST pass this for retry-safe events.",
        vi: "Khóa ổn định để tránh dispatch trùng khi queue retry. Unique khi non-null (unique index). Emitter PHẢI pass cho sự kiện retry-safe.",
      },
    },
    audience_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "オーディエンスID",
        en: "Audience ID",
        vi: "ID audience",
      },
      placeholder: {
        ja: "オーディエンスIDを入力",
        en: "Enter Audience ID",
        vi: "Nhập ID audience",
      },
      description: {
        ja: "保存されたオーディエンスからのディスパッチ時は `notification_audiences.id` への FK。インライン / コードルール使用時は null。FK 制約は T12.1 でオーディエンステーブル作成時に追加。",
        en: "FK to `notification_audiences.id` when dispatch came from a saved audience. Null when dispatch used an inline/code rule. FK constraint added when T12.1 creates the audiences table.",
        vi: "FK tới `notification_audiences.id` khi dispatch từ audience đã lưu. Null khi dispatch dùng inline/code rule. Constraint FK được thêm khi T12.1 tạo bảng audiences.",
      },
    },
    scheduled_for: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "予約送信日時",
        en: "Scheduled For",
        vi: "Thời điểm gửi",
      },
      placeholder: {
        ja: "予約送信日時を入力",
        en: "Enter Scheduled For",
        vi: "Nhập Thời điểm gửi",
      },
      description: {
        ja: "予約送信日時。null は「即送信」。設定時は `is_dispatched=false` とセット。cron が1分毎に期限到来の行をスキャンし `scheduled_for` でディスパッチ。",
        en: "Future dispatch time. Null means 'send now'. When set, paired with `is_dispatched=false`; cron scans due rows every minute and fires dispatch at `scheduled_for`.",
        vi: "Thời điểm gửi trong tương lai. Null = gửi ngay. Khi set, đi kèm `is_dispatched=false`; cron scan row due mỗi phút và fire dispatch tại `scheduled_for`.",
      },
    },
    is_dispatched: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "送信済み",
        en: "Is Dispatched",
        vi: "Đã gửi",
      },
      placeholder: {
        ja: "送信済みを入力",
        en: "Enter Is Dispatched",
        vi: "Nhập Đã gửi",
      },
      description: {
        ja: "即送信行はデフォルト `true`。フェーズDコンポーザーは `scheduled_for` と合わせて `false` を設定。cron は期限到来時のディスパッチ後に `true` へフリップ。",
        en: "Default `true` for now-send rows. Phase D composer sets `false` alongside `scheduled_for`; cron flips to `true` after dispatching at due time.",
        vi: "Default `true` cho row gửi ngay. Phase D composer set `false` kèm `scheduled_for`; cron flip thành `true` sau khi dispatch tại due time.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "type", "template_key", "priority", "is_dispatched"] as const,
  enumFields: ["priority"] as const,
  relationFields: ["organization_id"] as const,
} as const;

export type NotificationFieldName = keyof typeof notificationMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Notification. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationCreateFormState {
  organization_id: string;
  actor_type?: string | null;
  actor_id?: string | null;
  type: string;
  template_key: string;
  params?: unknown | null;
  subject_type?: string | null;
  subject_id?: string | null;
  priority: NotificationPriority;
  aggregation_key?: string | null;
  idempotency_key?: string | null;
  audience_id?: string | null;
  scheduled_for?: string | null;
  is_dispatched: boolean;
}

/** Form-state shape for updating a Notification. Every field optional. */
export interface NotificationUpdateFormState {
  organization_id?: string;
  actor_type?: string | null;
  actor_id?: string | null;
  type?: string;
  template_key?: string;
  params?: unknown | null;
  subject_type?: string | null;
  subject_id?: string | null;
  priority?: NotificationPriority;
  aggregation_key?: string | null;
  idempotency_key?: string | null;
  audience_id?: string | null;
  scheduled_for?: string | null;
  is_dispatched?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationCreateForm(): NotificationCreateFormState {
  return {
    organization_id: '',
    actor_type: '',
    actor_id: '',
    type: '',
    template_key: '',
    params: null,
    subject_type: '',
    subject_id: '',
    priority: NotificationPriority.Normal,
    aggregation_key: '',
    idempotency_key: '',
    audience_id: '',
    scheduled_for: '',
    is_dispatched: true,
  };
}

/** Convert create form state into the payload that baseNotificationCreateSchema accepts. */
export function buildNotificationCreatePayload(form: NotificationCreateFormState): BaseNotificationCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    actor_type: form.actor_type?.toString().trim() || null,
    actor_id: form.actor_id?.toString().trim() || null,
    type: form.type.trim(),
    template_key: form.template_key.trim(),
    params: form.params,
    subject_type: form.subject_type?.toString().trim() || null,
    subject_id: form.subject_id?.toString().trim() || null,
    priority: form.priority,
    aggregation_key: form.aggregation_key?.toString().trim() || null,
    idempotency_key: form.idempotency_key?.toString().trim() || null,
    audience_id: form.audience_id?.toString().trim() || null,
    scheduled_for: form.scheduled_for,
    is_dispatched: form.is_dispatched,
  };

  return payload as BaseNotificationCreate;
}

/** Convert update form state into the payload that baseNotificationUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationUpdatePayload(form: NotificationUpdateFormState): BaseNotificationUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.actor_type !== undefined) {
    payload.actor_type = form.actor_type?.toString().trim() ?? null;
  }
  if (form.actor_id !== undefined) {
    payload.actor_id = form.actor_id?.toString().trim() ?? null;
  }
  if (form.type !== undefined) {
    payload.type = form.type?.toString().trim() ?? null;
  }
  if (form.template_key !== undefined) {
    payload.template_key = form.template_key?.toString().trim() ?? null;
  }
  if (form.params !== undefined) {
    payload.params = form.params;
  }
  if (form.subject_type !== undefined) {
    payload.subject_type = form.subject_type?.toString().trim() ?? null;
  }
  if (form.subject_id !== undefined) {
    payload.subject_id = form.subject_id?.toString().trim() ?? null;
  }
  if (form.priority !== undefined) {
    payload.priority = form.priority;
  }
  if (form.aggregation_key !== undefined) {
    payload.aggregation_key = form.aggregation_key?.toString().trim() ?? null;
  }
  if (form.idempotency_key !== undefined) {
    payload.idempotency_key = form.idempotency_key?.toString().trim() ?? null;
  }
  if (form.audience_id !== undefined) {
    payload.audience_id = form.audience_id?.toString().trim() ?? null;
  }
  if (form.scheduled_for !== undefined) {
    payload.scheduled_for = form.scheduled_for;
  }
  if (form.is_dispatched !== undefined) {
    payload.is_dispatched = form.is_dispatched;
  }
  return payload as BaseNotificationUpdate;
}
