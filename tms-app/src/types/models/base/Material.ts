/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Allergen } from './Allergen';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { ProductSku } from './ProductSku';
import type { Recipe } from './Recipe';

/**
 * 材料
 */
export interface Material {
  id: string;
  sku?: string | null;
  name: string;
  description?: string | null;
  yield_quantity: number;
  yield_unit?: string | null;
  shelf_life_days?: number | null;
  calculated_cost: number;
  is_active: boolean;
  requires_temperature_check: boolean;
  temperature_min?: number | null;
  temperature_max?: number | null;
  expiry_alert_thresholds?: unknown | null;
  outputSku_id?: string | null;
  outputSku?: ProductSku | null;
  allergens?: Allergen[];
  recipes?: Recipe[];
  skus?: ProductSku[];
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Material
 */
export const materialI18n = {
  label: {"ja":"材料","en":"Material","vi":"Nguyên liệu"},
  fields: {
    sku: {
      label: {"ja":"材料SKU","en":"Material SKU","vi":"Mã nguyên liệu"},
      placeholder: {"ja":"例：M-001","en":"e.g., M-001","vi":"VD: M-001"},
    },
    name: {
      label: {"ja":"材料名","en":"Material Name","vi":"Tên nguyên liệu"},
      placeholder: {"ja":"材料名を入力","en":"Enter Material Name","vi":"Nhập Tên nguyên liệu"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    yield_quantity: {
      label: {"ja":"出来高","en":"Yield Quantity","vi":"Số lượng sản xuất"},
      placeholder: {"ja":"出来高を入力","en":"Enter Yield Quantity","vi":"Nhập Số lượng sản xuất"},
    },
    yield_unit: {
      label: {"ja":"出力単位","en":"Yield Unit","vi":"Đơn vị đầu ra"},
      placeholder: {"ja":"出力単位を入力","en":"Enter Yield Unit","vi":"Nhập Đơn vị đầu ra"},
    },
    shelf_life_days: {
      label: {"ja":"賞味期限（日数）","en":"Shelf Life (days)","vi":"Hạn sử dụng (ngày)"},
      placeholder: {"ja":"賞味期限（日数）を入力","en":"Enter Shelf Life (days)","vi":"Nhập Hạn sử dụng (ngày)"},
    },
    calculated_cost: {
      label: {"ja":"計算原価","en":"Calculated Cost","vi":"Giá vốn tính toán"},
      placeholder: {"ja":"計算原価を入力","en":"Enter Calculated Cost","vi":"Nhập Giá vốn tính toán"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    requires_temperature_check: {
      label: {"ja":"温度チェック必須","en":"Requires Temperature Check","vi":"Yêu cầu kiểm tra nhiệt độ"},
      placeholder: {"ja":"温度チェック必須を入力","en":"Enter Requires Temperature Check","vi":"Nhập Yêu cầu kiểm tra nhiệt độ"},
    },
    temperature_min: {
      label: {"ja":"温度下限（°C）","en":"Temperature Min (°C)","vi":"Nhiệt độ tối thiểu (°C)"},
      placeholder: {"ja":"温度下限（°C）を入力","en":"Enter Temperature Min (°C)","vi":"Nhập Nhiệt độ tối thiểu (°C)"},
    },
    temperature_max: {
      label: {"ja":"温度上限（°C）","en":"Temperature Max (°C)","vi":"Nhiệt độ tối đa (°C)"},
      placeholder: {"ja":"温度上限（°C）を入力","en":"Enter Temperature Max (°C)","vi":"Nhập Nhiệt độ tối đa (°C)"},
    },
    expiry_alert_thresholds: {
      label: {"ja":"期限アラート閾値（日）","en":"Expiry Alert Thresholds (days)","vi":"Ngưỡng cảnh báo hạn (ngày)"},
      placeholder: {"ja":"期限アラート閾値（日）を入力","en":"Enter Expiry Alert Thresholds (days)","vi":"Nhập Ngưỡng cảnh báo hạn (ngày)"},
    },
    output_sku: {
      label: {"ja":"出力SKU","en":"Output SKU","vi":"SKU đầu ra"},
      placeholder: {"ja":"出力SKUを入力","en":"Enter Output SKU","vi":"Nhập SKU đầu ra"},
    },
    allergens: {
      label: {"ja":"アレルゲン","en":"Allergens","vi":"Chất gây dị ứng"},
      placeholder: {"ja":"アレルゲンを入力","en":"Enter Allergens","vi":"Nhập Chất gây dị ứng"},
    },
    recipes: {
      label: {"ja":"レシピ","en":"Recipes","vi":"Công thức"},
      placeholder: {"ja":"レシピを入力","en":"Enter Recipes","vi":"Nhập Công thức"},
    },
    skus: {
      label: {"ja":"リンクされたSKU","en":"Linked SKUs","vi":"Các SKU liên kết"},
      placeholder: {"ja":"リンクされたSKUを入力","en":"Enter Linked SKUs","vi":"Nhập Các SKU liên kết"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Material */
export const baseMaterialSchemas = {
  sku: z.string().max(50).optional().nullable(),
  name: z.string().min(1).max(255),
  description: z.string().optional().nullable(),
  yield_quantity: z.number(),
  yield_unit: z.string().max(50).optional().nullable(),
  shelf_life_days: z.number().int().optional().nullable(),
  calculated_cost: z.number(),
  is_active: z.boolean(),
  requires_temperature_check: z.boolean(),
  temperature_min: z.number().optional().nullable(),
  temperature_max: z.number().optional().nullable(),
  expiry_alert_thresholds: z.unknown().optional().nullable(),
} as const;

/** Create schema for Material */
export const baseMaterialCreateSchema = z.object({
  sku: baseMaterialSchemas.sku,
  name: baseMaterialSchemas.name,
  description: baseMaterialSchemas.description,
  yield_quantity: baseMaterialSchemas.yield_quantity,
  yield_unit: baseMaterialSchemas.yield_unit,
  shelf_life_days: baseMaterialSchemas.shelf_life_days,
  calculated_cost: baseMaterialSchemas.calculated_cost,
  is_active: baseMaterialSchemas.is_active,
  requires_temperature_check: baseMaterialSchemas.requires_temperature_check,
  temperature_min: baseMaterialSchemas.temperature_min,
  temperature_max: baseMaterialSchemas.temperature_max,
  expiry_alert_thresholds: baseMaterialSchemas.expiry_alert_thresholds,
  ja: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
});

/** Update schema for Material */
export const baseMaterialUpdateSchema = baseMaterialCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMaterialCreate = z.infer<typeof baseMaterialCreateSchema>;
export type BaseMaterialUpdate = z.infer<typeof baseMaterialUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMaterialLabel(locale: string): string {
  return materialI18n.label[locale as keyof typeof materialI18n.label] ?? materialI18n.label['en'] ?? 'Material';
}

/** Get field label for a specific locale */
export function getMaterialFieldLabel(field: string, locale: string): string {
  const fields = materialI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMaterialFieldPlaceholder(field: string, locale: string): string {
  const fields = materialI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Material.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const materialMetadata = {
  modelName: "Material",
  table: "materials",

  fields: {
    sku: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "材料SKU",
        en: "Material SKU",
        vi: "Mã nguyên liệu",
      },
      placeholder: {
        ja: "例：M-001",
        en: "e.g., M-001",
        vi: "VD: M-001",
      },
      description: {
        ja: "プレフィックスM-を持つ一意のSKU",
        en: "Unique SKU with prefix M-",
        vi: "SKU duy nhất với tiền tố M-",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "材料名",
        en: "Material Name",
        vi: "Tên nguyên liệu",
      },
      placeholder: {
        ja: "材料名を入力",
        en: "Enter Material Name",
        vi: "Nhập Tên nguyên liệu",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: true,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    yield_quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "出来高",
        en: "Yield Quantity",
        vi: "Số lượng sản xuất",
      },
      placeholder: {
        ja: "出来高を入力",
        en: "Enter Yield Quantity",
        vi: "Nhập Số lượng sản xuất",
      },
      description: {
        ja: "1バッチあたりの出力量",
        en: "How much output per batch",
        vi: "1 lần pha ra bao nhiêu",
      },
    },
    yield_unit: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "出力単位",
        en: "Yield Unit",
        vi: "Đơn vị đầu ra",
      },
      placeholder: {
        ja: "出力単位を入力",
        en: "Enter Yield Unit",
        vi: "Nhập Đơn vị đầu ra",
      },
      description: {
        ja: "出力の単位（例：PORTION、LITER、GRAM）",
        en: "Unit of output (e.g., PORTION, LITER, GRAM)",
        vi: "Đơn vị của output (VD: PORTION, LITER, GRAM)",
      },
    },
    shelf_life_days: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "賞味期限（日数）",
        en: "Shelf Life (days)",
        vi: "Hạn sử dụng (ngày)",
      },
      placeholder: {
        ja: "賞味期限（日数）を入力",
        en: "Enter Shelf Life (days)",
        vi: "Nhập Hạn sử dụng (ngày)",
      },
      description: {
        ja: "生産から期限切れまでの日数。NULL=ポリシーなし。MaterialBatchService::completeで出力ロットの期限計算に使用。",
        en: "Days from production until expiry. NULL = no policy. Used by MaterialBatchService::complete to compute output lot expiry.",
        vi: "Số ngày từ khi sản xuất đến khi hết hạn. NULL = không quy định. Dùng tính expiry lô đầu ra trong MaterialBatchService::complete.",
      },
    },
    calculated_cost: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "計算原価",
        en: "Calculated Cost",
        vi: "Giá vốn tính toán",
      },
      placeholder: {
        ja: "計算原価を入力",
        en: "Enter Calculated Cost",
        vi: "Nhập Giá vốn tính toán",
      },
      description: {
        ja: "キャッシュ原価。HQ管理者が手動で設定するか、紐づくレシピから計算される。",
        en: "Cached cost — written by the HQ admin or computed via the linked Recipe.",
        vi: "Giá vốn cache — do quản trị HQ nhập tay hoặc tính từ Recipe liên kết.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    requires_temperature_check: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "温度チェック必須",
        en: "Requires Temperature Check",
        vi: "Yêu cầu kiểm tra nhiệt độ",
      },
      placeholder: {
        ja: "温度チェック必須を入力",
        en: "Enter Requires Temperature Check",
        vi: "Nhập Yêu cầu kiểm tra nhiệt độ",
      },
    },
    temperature_min: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "温度下限（°C）",
        en: "Temperature Min (°C)",
        vi: "Nhiệt độ tối thiểu (°C)",
      },
      placeholder: {
        ja: "温度下限（°C）を入力",
        en: "Enter Temperature Min (°C)",
        vi: "Nhập Nhiệt độ tối thiểu (°C)",
      },
    },
    temperature_max: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "温度上限（°C）",
        en: "Temperature Max (°C)",
        vi: "Nhiệt độ tối đa (°C)",
      },
      placeholder: {
        ja: "温度上限（°C）を入力",
        en: "Enter Temperature Max (°C)",
        vi: "Nhập Nhiệt độ tối đa (°C)",
      },
    },
    expiry_alert_thresholds: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "期限アラート閾値（日）",
        en: "Expiry Alert Thresholds (days)",
        vi: "Ngưỡng cảnh báo hạn (ngày)",
      },
      placeholder: {
        ja: "期限アラート閾値（日）を入力",
        en: "Enter Expiry Alert Thresholds (days)",
        vi: "Nhập Ngưỡng cảnh báo hạn (ngày)",
      },
      description: {
        ja: "日数のJSON配列。例：[7, 3, 1]。NULLの場合はシステム既定値を使用。",
        en: "JSON array of days, e.g. [7, 3, 1]. NULL = use system default.",
        vi: "Mảng JSON số ngày, VD: [7, 3, 1]. NULL = dùng giá trị mặc định hệ thống.",
      },
    },
    output_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "SET NULL",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name", "description"] as const,
  requiredFields: ["name", "yield_quantity", "calculated_cost", "is_active", "requires_temperature_check", "organization_id", "brand_id"] as const,
  enumFields: [] as const,
  relationFields: ["output_sku_id", "organization_id", "brand_id"] as const,
} as const;

export type MaterialFieldName = keyof typeof materialMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Material. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MaterialCreateFormState {
  sku?: string | null;
  name: Record<Locale, string>;
  description: Record<Locale, string>;
  yield_quantity: number;
  yield_unit?: string | null;
  shelf_life_days?: number | null;
  calculated_cost: number;
  is_active: boolean;
  requires_temperature_check: boolean;
  temperature_min?: number | null;
  temperature_max?: number | null;
  expiry_alert_thresholds?: unknown | null;
  output_sku_id?: string | null;
  organization_id: string;
  brand_id: string;
}

/** Form-state shape for updating a Material. Every field optional. */
export interface MaterialUpdateFormState {
  sku?: string | null;
  name?: Record<Locale, string>;
  description?: Record<Locale, string>;
  yield_quantity?: number;
  yield_unit?: string | null;
  shelf_life_days?: number | null;
  calculated_cost?: number;
  is_active?: boolean;
  requires_temperature_check?: boolean;
  temperature_min?: number | null;
  temperature_max?: number | null;
  expiry_alert_thresholds?: unknown | null;
  output_sku_id?: string | null;
  organization_id?: string;
  brand_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMaterialCreateForm(): MaterialCreateFormState {
  return {
    sku: '',
    name: emptyLocaleMap(),
    description: emptyLocaleMap(),
    yield_quantity: 1,
    yield_unit: '',
    shelf_life_days: 0,
    calculated_cost: 0,
    is_active: true,
    requires_temperature_check: false,
    temperature_min: 0,
    temperature_max: 0,
    expiry_alert_thresholds: null,
    output_sku_id: '',
    organization_id: '',
    brand_id: '',
  };
}

/** Convert create form state into the payload that baseMaterialCreateSchema accepts. */
export function buildMaterialCreatePayload(form: MaterialCreateFormState): BaseMaterialCreate {
  const payload: Record<string, unknown> = {
    sku: form.sku?.toString().trim() || null,
    name: (form.name["ja"] ?? '').trim(),
    description: (form.description["ja"] ?? '').trim(),
    yield_quantity: form.yield_quantity,
    yield_unit: form.yield_unit?.toString().trim() || null,
    shelf_life_days: form.shelf_life_days,
    calculated_cost: form.calculated_cost,
    is_active: form.is_active,
    requires_temperature_check: form.requires_temperature_check,
    temperature_min: form.temperature_min,
    temperature_max: form.temperature_max,
    expiry_alert_thresholds: form.expiry_alert_thresholds,
    output_sku_id: form.output_sku_id?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
    description: form.description,
  });
  Object.assign(payload, i18n);

  return payload as BaseMaterialCreate;
}

/** Convert update form state into the payload that baseMaterialUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMaterialUpdatePayload(form: MaterialUpdateFormState): BaseMaterialUpdate {
  const payload: Record<string, unknown> = {};
  if (form.sku !== undefined) {
    payload.sku = form.sku?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.description !== undefined) {
    payload.description = (form.description["ja"] ?? '').trim();
  }
  if (form.yield_quantity !== undefined) {
    payload.yield_quantity = form.yield_quantity;
  }
  if (form.yield_unit !== undefined) {
    payload.yield_unit = form.yield_unit?.toString().trim() ?? null;
  }
  if (form.shelf_life_days !== undefined) {
    payload.shelf_life_days = form.shelf_life_days;
  }
  if (form.calculated_cost !== undefined) {
    payload.calculated_cost = form.calculated_cost;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.requires_temperature_check !== undefined) {
    payload.requires_temperature_check = form.requires_temperature_check;
  }
  if (form.temperature_min !== undefined) {
    payload.temperature_min = form.temperature_min;
  }
  if (form.temperature_max !== undefined) {
    payload.temperature_max = form.temperature_max;
  }
  if (form.expiry_alert_thresholds !== undefined) {
    payload.expiry_alert_thresholds = form.expiry_alert_thresholds;
  }
  if (form.output_sku_id !== undefined) {
    payload.output_sku_id = form.output_sku_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  if (form.description !== undefined) localeInput.description = form.description;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseMaterialUpdate;
}
