/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Material } from './Material';
import type { ProductSku } from './ProductSku';
import type { StockTransfer } from './StockTransfer';

/**
 * 在庫移動明細
 */
export interface StockTransferItem {
  id: string;
  stockTransfer_id: string;
  stockTransfer?: StockTransfer;
  productSku_id?: string | null;
  productSku?: ProductSku | null;
  material_id?: string | null;
  material?: Material | null;
  sent_quantity: number;
  sent_base_quantity: number;
  received_quantity?: number | null;
  received_base_quantity?: number | null;
  unit?: string | null;
  note?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for StockTransferItem
 */
export const stockTransferItemI18n = {
  label: {"ja":"在庫移動明細","en":"Stock Transfer Item","vi":"Chi tiết chuyển kho"},
  fields: {
    stock_transfer: {
      label: {"ja":"在庫移動","en":"Stock Transfer","vi":"Chuyển kho"},
      placeholder: {"ja":"在庫移動を入力","en":"Enter Stock Transfer","vi":"Nhập Chuyển kho"},
    },
    product_sku: {
      label: {"ja":"商品バリエーション","en":"Product Variant","vi":"Biến thể sản phẩm"},
      placeholder: {"ja":"商品バリエーションを入力","en":"Enter Product Variant","vi":"Nhập Biến thể sản phẩm"},
    },
    material: {
      label: {"ja":"材料","en":"Material","vi":"Nguyên liệu"},
      placeholder: {"ja":"材料を入力","en":"Enter Material","vi":"Nhập Nguyên liệu"},
    },
    sent_quantity: {
      label: {"ja":"送付数量","en":"Sent Quantity","vi":"Số lượng gửi"},
      placeholder: {"ja":"送付数量を入力","en":"Enter Sent Quantity","vi":"Nhập Số lượng gửi"},
    },
    sent_base_quantity: {
      label: {"ja":"送付基本数量","en":"Sent Base Quantity","vi":"Số lượng gửi (đơn vị gốc)"},
      placeholder: {"ja":"送付基本数量を入力","en":"Enter Sent Base Quantity","vi":"Nhập Số lượng gửi (đơn vị gốc)"},
    },
    received_quantity: {
      label: {"ja":"受領数量","en":"Received Quantity","vi":"Số lượng nhận"},
      placeholder: {"ja":"受領数量を入力","en":"Enter Received Quantity","vi":"Nhập Số lượng nhận"},
    },
    received_base_quantity: {
      label: {"ja":"受領基本数量","en":"Received Base Quantity","vi":"Số lượng nhận (đơn vị gốc)"},
      placeholder: {"ja":"受領基本数量を入力","en":"Enter Received Base Quantity","vi":"Nhập Số lượng nhận (đơn vị gốc)"},
    },
    unit: {
      label: {"ja":"単位","en":"Unit","vi":"Đơn vị"},
      placeholder: {"ja":"単位を入力","en":"Enter Unit","vi":"Nhập Đơn vị"},
    },
    note: {
      label: {"ja":"備考","en":"Note","vi":"Ghi chú"},
      placeholder: {"ja":"備考を入力","en":"Enter Note","vi":"Nhập Ghi chú"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for StockTransferItem */
export const baseStockTransferItemSchemas = {
  sent_quantity: z.number(),
  sent_base_quantity: z.number(),
  received_quantity: z.number().optional().nullable(),
  received_base_quantity: z.number().optional().nullable(),
  unit: z.string().max(50).optional().nullable(),
  note: z.string().optional().nullable(),
} as const;

/** Create schema for StockTransferItem */
export const baseStockTransferItemCreateSchema = z.object({
  sent_quantity: baseStockTransferItemSchemas.sent_quantity,
  sent_base_quantity: baseStockTransferItemSchemas.sent_base_quantity,
  received_quantity: baseStockTransferItemSchemas.received_quantity,
  received_base_quantity: baseStockTransferItemSchemas.received_base_quantity,
  unit: baseStockTransferItemSchemas.unit,
  note: baseStockTransferItemSchemas.note,
});

/** Update schema for StockTransferItem */
export const baseStockTransferItemUpdateSchema = baseStockTransferItemCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseStockTransferItemCreate = z.infer<typeof baseStockTransferItemCreateSchema>;
export type BaseStockTransferItemUpdate = z.infer<typeof baseStockTransferItemUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getStockTransferItemLabel(locale: string): string {
  return stockTransferItemI18n.label[locale as keyof typeof stockTransferItemI18n.label] ?? stockTransferItemI18n.label['en'] ?? 'StockTransferItem';
}

/** Get field label for a specific locale */
export function getStockTransferItemFieldLabel(field: string, locale: string): string {
  const fields = stockTransferItemI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getStockTransferItemFieldPlaceholder(field: string, locale: string): string {
  const fields = stockTransferItemI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for StockTransferItem.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const stockTransferItemMetadata = {
  modelName: "StockTransferItem",
  table: "stock_transfer_items",

  fields: {
    stock_transfer_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "StockTransfer",
        table: "stock_transfers",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    material_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Material",
        table: "materials",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    sent_quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "送付数量",
        en: "Sent Quantity",
        vi: "Số lượng gửi",
      },
      placeholder: {
        ja: "送付数量を入力",
        en: "Enter Sent Quantity",
        vi: "Nhập Số lượng gửi",
      },
      description: {
        ja: "ユーザー選択単位での送付数量",
        en: "Quantity sent in user-selected unit",
        vi: "Số lượng gửi theo đơn vị user chọn",
      },
    },
    sent_base_quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "送付基本数量",
        en: "Sent Base Quantity",
        vi: "Số lượng gửi (đơn vị gốc)",
      },
      placeholder: {
        ja: "送付基本数量を入力",
        en: "Enter Sent Base Quantity",
        vi: "Nhập Số lượng gửi (đơn vị gốc)",
      },
      description: {
        ja: "基本単位に変換した送付数量",
        en: "Sent quantity converted to base unit",
        vi: "Số lượng gửi đã quy đổi về đơn vị gốc",
      },
    },
    received_quantity: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "受領数量",
        en: "Received Quantity",
        vi: "Số lượng nhận",
      },
      placeholder: {
        ja: "受領数量を入力",
        en: "Enter Received Quantity",
        vi: "Nhập Số lượng nhận",
      },
      description: {
        ja: "ユーザー選択単位での受領数量。送付と異なる場合あり（BR-04）。",
        en: "Quantity received in user-selected unit. May differ from sent (BR-04).",
        vi: "Số lượng nhận theo đơn vị user chọn. Có thể khác số gửi (BR-04).",
      },
    },
    received_base_quantity: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "受領基本数量",
        en: "Received Base Quantity",
        vi: "Số lượng nhận (đơn vị gốc)",
      },
      placeholder: {
        ja: "受領基本数量を入力",
        en: "Enter Received Base Quantity",
        vi: "Nhập Số lượng nhận (đơn vị gốc)",
      },
      description: {
        ja: "基本単位に変換した受領数量",
        en: "Received quantity converted to base unit",
        vi: "Số lượng nhận đã quy đổi về đơn vị gốc",
      },
    },
    unit: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "単位",
        en: "Unit",
        vi: "Đơn vị",
      },
      placeholder: {
        ja: "単位を入力",
        en: "Enter Unit",
        vi: "Nhập Đơn vị",
      },
      description: {
        ja: "表示用のユーザー選択単位",
        en: "User-selected unit for display",
        vi: "Đơn vị user chọn để hiển thị",
      },
    },
    note: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "備考",
        en: "Note",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "備考を入力",
        en: "Enter Note",
        vi: "Nhập Ghi chú",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["stock_transfer_id", "sent_quantity", "sent_base_quantity"] as const,
  enumFields: [] as const,
  relationFields: ["stock_transfer_id", "product_sku_id", "material_id"] as const,
} as const;

export type StockTransferItemFieldName = keyof typeof stockTransferItemMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a StockTransferItem. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface StockTransferItemCreateFormState {
  stock_transfer_id: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  sent_quantity: number;
  sent_base_quantity: number;
  received_quantity?: number | null;
  received_base_quantity?: number | null;
  unit?: string | null;
  note?: string | null;
}

/** Form-state shape for updating a StockTransferItem. Every field optional. */
export interface StockTransferItemUpdateFormState {
  stock_transfer_id?: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  sent_quantity?: number;
  sent_base_quantity?: number;
  received_quantity?: number | null;
  received_base_quantity?: number | null;
  unit?: string | null;
  note?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyStockTransferItemCreateForm(): StockTransferItemCreateFormState {
  return {
    stock_transfer_id: '',
    product_sku_id: '',
    material_id: '',
    sent_quantity: 0,
    sent_base_quantity: 0,
    received_quantity: 0,
    received_base_quantity: 0,
    unit: '',
    note: '',
  };
}

/** Convert create form state into the payload that baseStockTransferItemCreateSchema accepts. */
export function buildStockTransferItemCreatePayload(form: StockTransferItemCreateFormState): BaseStockTransferItemCreate {
  const payload: Record<string, unknown> = {
    stock_transfer_id: form.stock_transfer_id.trim(),
    product_sku_id: form.product_sku_id?.toString().trim() || null,
    material_id: form.material_id?.toString().trim() || null,
    sent_quantity: form.sent_quantity,
    sent_base_quantity: form.sent_base_quantity,
    received_quantity: form.received_quantity,
    received_base_quantity: form.received_base_quantity,
    unit: form.unit?.toString().trim() || null,
    note: form.note?.toString().trim() || null,
  };

  return payload as BaseStockTransferItemCreate;
}

/** Convert update form state into the payload that baseStockTransferItemUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildStockTransferItemUpdatePayload(form: StockTransferItemUpdateFormState): BaseStockTransferItemUpdate {
  const payload: Record<string, unknown> = {};
  if (form.stock_transfer_id !== undefined) {
    payload.stock_transfer_id = form.stock_transfer_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.material_id !== undefined) {
    payload.material_id = form.material_id?.toString().trim() ?? null;
  }
  if (form.sent_quantity !== undefined) {
    payload.sent_quantity = form.sent_quantity;
  }
  if (form.sent_base_quantity !== undefined) {
    payload.sent_base_quantity = form.sent_base_quantity;
  }
  if (form.received_quantity !== undefined) {
    payload.received_quantity = form.received_quantity;
  }
  if (form.received_base_quantity !== undefined) {
    payload.received_base_quantity = form.received_base_quantity;
  }
  if (form.unit !== undefined) {
    payload.unit = form.unit?.toString().trim() ?? null;
  }
  if (form.note !== undefined) {
    payload.note = form.note?.toString().trim() ?? null;
  }
  return payload as BaseStockTransferItemUpdate;
}
