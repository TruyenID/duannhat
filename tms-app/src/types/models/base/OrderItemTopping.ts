/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import { OrderItemStatus } from '../enum/OrderItemStatus';
import type { CustomerOrderItem } from './CustomerOrderItem';
import type { ProductSku } from './ProductSku';
import type { ToppingGroupItem } from './ToppingGroupItem';

/**
 * 注文アイテムトッピング
 */
export interface OrderItemTopping {
  id: string;
  customerOrderItem_id: string;
  customerOrderItem?: CustomerOrderItem;
  toppingGroupItem_id: string;
  toppingGroupItem?: ToppingGroupItem;
  productSku_id: string;
  productSku?: ProductSku;
  quantity: number;
  status: OrderItemStatus;
  unit_price: number;
  note?: string | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for OrderItemTopping
 */
export const orderItemToppingI18n = {
  label: {"ja":"注文アイテムトッピング","en":"Order Item Topping","vi":"Topping trong Order Item"},
  fields: {
    customer_order_item: {
      label: {"ja":"注文明細","en":"Customer Order Item","vi":"Chi tiết đơn hàng"},
      placeholder: {"ja":"注文明細を入力","en":"Enter Customer Order Item","vi":"Nhập Chi tiết đơn hàng"},
    },
    topping_group_item: {
      label: {"ja":"トッピング項目","en":"Topping Group Item","vi":"Mục Topping"},
      placeholder: {"ja":"トッピング項目を入力","en":"Enter Topping Group Item","vi":"Nhập Mục Topping"},
    },
    product_sku: {
      label: {"ja":"商品SKU","en":"Product SKU","vi":"SKU sản phẩm"},
      placeholder: {"ja":"商品SKUを入力","en":"Enter Product SKU","vi":"Nhập SKU sản phẩm"},
    },
    quantity: {
      label: {"ja":"数量","en":"Quantity","vi":"Số lượng"},
      placeholder: {"ja":"数量を入力","en":"Enter Quantity","vi":"Nhập Số lượng"},
    },
    status: {
      label: {"ja":"ステータス","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"ステータスを入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    unit_price: {
      label: {"ja":"単価","en":"Unit Price","vi":"Đơn giá"},
      placeholder: {"ja":"単価を入力","en":"Enter Unit Price","vi":"Nhập Đơn giá"},
    },
    note: {
      label: {"ja":"備考","en":"Note","vi":"Ghi chú"},
      placeholder: {"ja":"備考を入力","en":"Enter Note","vi":"Nhập Ghi chú"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for OrderItemTopping */
export const baseOrderItemToppingSchemas = {
  quantity: z.number().int(),
  status: z.nativeEnum(OrderItemStatus),
  unit_price: z.number(),
  note: z.string().max(500).optional().nullable(),
} as const;

/** Create schema for OrderItemTopping */
export const baseOrderItemToppingCreateSchema = z.object({
  quantity: baseOrderItemToppingSchemas.quantity,
  status: baseOrderItemToppingSchemas.status,
  unit_price: baseOrderItemToppingSchemas.unit_price,
  note: baseOrderItemToppingSchemas.note,
});

/** Update schema for OrderItemTopping */
export const baseOrderItemToppingUpdateSchema = baseOrderItemToppingCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseOrderItemToppingCreate = z.infer<typeof baseOrderItemToppingCreateSchema>;
export type BaseOrderItemToppingUpdate = z.infer<typeof baseOrderItemToppingUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getOrderItemToppingLabel(locale: string): string {
  return orderItemToppingI18n.label[locale as keyof typeof orderItemToppingI18n.label] ?? orderItemToppingI18n.label['en'] ?? 'OrderItemTopping';
}

/** Get field label for a specific locale */
export function getOrderItemToppingFieldLabel(field: string, locale: string): string {
  const fields = orderItemToppingI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getOrderItemToppingFieldPlaceholder(field: string, locale: string): string {
  const fields = orderItemToppingI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for OrderItemTopping.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const orderItemToppingMetadata = {
  modelName: "OrderItemTopping",
  table: "order_item_toppings",

  fields: {
    customer_order_item_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrderItem",
        table: "customer_order_items",
        onDelete: "CASCADE",
      },
      label: {},
    },
    topping_group_item_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ToppingGroupItem",
        table: "topping_group_items",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "数量",
        en: "Quantity",
        vi: "Số lượng",
      },
      placeholder: {
        ja: "数量を入力",
        en: "Enter Quantity",
        vi: "Nhập Số lượng",
      },
    },
    status: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "OrderItemStatus",
      enumValues: ["pending", "preparing", "ready", "served", "voided"] as const,
      label: {
        ja: "ステータス",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "ステータスを入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    unit_price: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "単価",
        en: "Unit Price",
        vi: "Đơn giá",
      },
      placeholder: {
        ja: "単価を入力",
        en: "Enter Unit Price",
        vi: "Nhập Đơn giá",
      },
    },
    note: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 500,
      },
      label: {
        ja: "備考",
        en: "Note",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "備考を入力",
        en: "Enter Note",
        vi: "Nhập Ghi chú",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["customer_order_item_id", "topping_group_item_id", "product_sku_id", "quantity", "status", "unit_price"] as const,
  enumFields: ["status"] as const,
  relationFields: ["customer_order_item_id", "topping_group_item_id", "product_sku_id"] as const,
} as const;

export type OrderItemToppingFieldName = keyof typeof orderItemToppingMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a OrderItemTopping. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface OrderItemToppingCreateFormState {
  customer_order_item_id: string;
  topping_group_item_id: string;
  product_sku_id: string;
  quantity: number;
  status: OrderItemStatus;
  unit_price: number;
  note?: string | null;
}

/** Form-state shape for updating a OrderItemTopping. Every field optional. */
export interface OrderItemToppingUpdateFormState {
  customer_order_item_id?: string;
  topping_group_item_id?: string;
  product_sku_id?: string;
  quantity?: number;
  status?: OrderItemStatus;
  unit_price?: number;
  note?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyOrderItemToppingCreateForm(): OrderItemToppingCreateFormState {
  return {
    customer_order_item_id: '',
    topping_group_item_id: '',
    product_sku_id: '',
    quantity: 1,
    status: OrderItemStatus.Pending,
    unit_price: 0,
    note: '',
  };
}

/** Convert create form state into the payload that baseOrderItemToppingCreateSchema accepts. */
export function buildOrderItemToppingCreatePayload(form: OrderItemToppingCreateFormState): BaseOrderItemToppingCreate {
  const payload: Record<string, unknown> = {
    customer_order_item_id: form.customer_order_item_id.trim(),
    topping_group_item_id: form.topping_group_item_id.trim(),
    product_sku_id: form.product_sku_id.trim(),
    quantity: form.quantity,
    status: form.status,
    unit_price: form.unit_price,
    note: form.note?.toString().trim() || null,
  };

  return payload as BaseOrderItemToppingCreate;
}

/** Convert update form state into the payload that baseOrderItemToppingUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildOrderItemToppingUpdatePayload(form: OrderItemToppingUpdateFormState): BaseOrderItemToppingUpdate {
  const payload: Record<string, unknown> = {};
  if (form.customer_order_item_id !== undefined) {
    payload.customer_order_item_id = form.customer_order_item_id?.toString().trim() ?? null;
  }
  if (form.topping_group_item_id !== undefined) {
    payload.topping_group_item_id = form.topping_group_item_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.quantity !== undefined) {
    payload.quantity = form.quantity;
  }
  if (form.status !== undefined) {
    payload.status = form.status;
  }
  if (form.unit_price !== undefined) {
    payload.unit_price = form.unit_price;
  }
  if (form.note !== undefined) {
    payload.note = form.note?.toString().trim() ?? null;
  }
  return payload as BaseOrderItemToppingUpdate;
}
