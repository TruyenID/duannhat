/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { User } from './User';

/**
 * 通知テンプレート
 */
export interface NotificationTemplate {
  id: string;
  organization_id?: string | null;
  organization?: Organization | null;
  brand_id?: string | null;
  brand?: Brand | null;
  branch_id?: string | null;
  branch?: Branch | null;
  key: string;
  content: unknown;
  default_channels?: unknown | null;
  params_schema?: unknown | null;
  is_system: boolean;
  createdBy_id?: string | null;
  createdBy?: User | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationTemplate
 */
export const notificationTemplateI18n = {
  label: {"ja":"通知テンプレート","en":"Notification Template","vi":"Mẫu thông báo"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"店舗","en":"Shop","vi":"Cửa hàng"},
      placeholder: {"ja":"店舗を入力","en":"Enter Shop","vi":"Nhập Cửa hàng"},
    },
    key: {
      label: {"ja":"キー","en":"Key","vi":"Khóa"},
      placeholder: {"ja":"キーを入力","en":"Enter Key","vi":"Nhập Khóa"},
    },
    content: {
      label: {"ja":"コンテンツ","en":"Content","vi":"Nội dung"},
      placeholder: {"ja":"コンテンツを入力","en":"Enter Content","vi":"Nhập Nội dung"},
    },
    default_channels: {
      label: {"ja":"デフォルトチャンネル","en":"Default Channels","vi":"Kênh mặc định"},
      placeholder: {"ja":"デフォルトチャンネルを入力","en":"Enter Default Channels","vi":"Nhập Kênh mặc định"},
    },
    params_schema: {
      label: {"ja":"パラメータスキーマ","en":"Params Schema","vi":"Schema tham số"},
      placeholder: {"ja":"パラメータスキーマを入力","en":"Enter Params Schema","vi":"Nhập Schema tham số"},
    },
    is_system: {
      label: {"ja":"システム","en":"Is System","vi":"Hệ thống"},
      placeholder: {"ja":"システムを入力","en":"Enter Is System","vi":"Nhập Hệ thống"},
    },
    created_by: {
      label: {"ja":"作成者","en":"Created By","vi":"Người tạo"},
      placeholder: {"ja":"作成者を入力","en":"Enter Created By","vi":"Nhập Người tạo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationTemplate */
export const baseNotificationTemplateSchemas = {
  key: z.string().min(1).max(100),
  content: z.unknown(),
  default_channels: z.unknown().optional().nullable(),
  params_schema: z.unknown().optional().nullable(),
  is_system: z.boolean(),
} as const;

/** Create schema for NotificationTemplate */
export const baseNotificationTemplateCreateSchema = z.object({
  key: baseNotificationTemplateSchemas.key,
  content: baseNotificationTemplateSchemas.content,
  default_channels: baseNotificationTemplateSchemas.default_channels,
  params_schema: baseNotificationTemplateSchemas.params_schema,
  is_system: baseNotificationTemplateSchemas.is_system,
});

/** Update schema for NotificationTemplate */
export const baseNotificationTemplateUpdateSchema = baseNotificationTemplateCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationTemplateCreate = z.infer<typeof baseNotificationTemplateCreateSchema>;
export type BaseNotificationTemplateUpdate = z.infer<typeof baseNotificationTemplateUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationTemplateLabel(locale: string): string {
  return notificationTemplateI18n.label[locale as keyof typeof notificationTemplateI18n.label] ?? notificationTemplateI18n.label['en'] ?? 'NotificationTemplate';
}

/** Get field label for a specific locale */
export function getNotificationTemplateFieldLabel(field: string, locale: string): string {
  const fields = notificationTemplateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationTemplateFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationTemplateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationTemplate.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationTemplateMetadata = {
  modelName: "NotificationTemplate",
  table: "notification_templates",

  fields: {
    organization_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "キー",
        en: "Key",
        vi: "Khóa",
      },
      placeholder: {
        ja: "キーを入力",
        en: "Enter Key",
        vi: "Nhập Khóa",
      },
      description: {
        ja: "ルックアップキー — notifications.template_key と結合。例：`stock.alert.low`、`recipe.approved`、`order.status_changed`。グローバルで一意。",
        en: "Lookup key — joined to notifications.template_key. Examples: `stock.alert.low`, `recipe.approved`, `order.status_changed`. Unique globally.",
        vi: "Khóa lookup — join vào notifications.template_key. VD: `stock.alert.low`, `recipe.approved`, `order.status_changed`. Unique global.",
      },
    },
    content: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "コンテンツ",
        en: "Content",
        vi: "Nội dung",
      },
      placeholder: {
        ja: "コンテンツを入力",
        en: "Enter Content",
        vi: "Nhập Nội dung",
      },
      description: {
        ja: "ロケール別コンテンツ：`{ja:{title,body}, en:{title,body}, vi:{title,body}}`。トークン `{{param}}` はレンダリング時に notifications.params から置換。欠落トークン → 空 + Log::warning（throw しない）。",
        en: "Locale-keyed content: `{ja:{title,body}, en:{title,body}, vi:{title,body}}`. Tokens `{{param}}` are substituted at render time from `notifications.params`. Missing token → empty + Log::warning (no throw).",
        vi: "Nội dung theo locale: `{ja:{title,body}, en:{title,body}, vi:{title,body}}`. Token `{{param}}` được substitute lúc render từ notifications.params. Token thiếu → rỗng + Log::warning (không throw).",
      },
    },
    default_channels: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "デフォルトチャンネル",
        en: "Default Channels",
        vi: "Kênh mặc định",
      },
      placeholder: {
        ja: "デフォルトチャンネルを入力",
        en: "Enter Default Channels",
        vi: "Nhập Kênh mặc định",
      },
      description: {
        ja: "NotificationChannel 値の JSON 配列（M3 enum）。Phase B 行は `['in_app']` で出荷。M3 T3.9 のルーティング層で、ブランド／組織のルート上書きがないときのフォールバックとして読み取る。null → `['in_app']` として扱う。",
        en: "JSON array of NotificationChannel cases (M3 enum). Phase B rows ship with `['in_app']`; M3 T3.9 channel-routing layer reads this as the fallback when no brand / org route override exists. Nullable → treat as `['in_app']`.",
        vi: "Mảng JSON các case NotificationChannel (enum M3). Row Phase B ship với `['in_app']`; tầng routing M3 T3.9 đọc làm fallback khi không có route override brand/org. Null → treat như `['in_app']`.",
      },
    },
    params_schema: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "パラメータスキーマ",
        en: "Params Schema",
        vi: "Schema tham số",
      },
      placeholder: {
        ja: "パラメータスキーマを入力",
        en: "Enter Params Schema",
        vi: "Nhập Schema tham số",
      },
      description: {
        ja: "ソフト契約：`{required: [key, ...], optional: [key, ...]}`。コンポーザー UI がパラメータヘルパーのドロップダウンを描画する際に参照。システムテンプレートはシーダー時に検証、管理者作成行は厳密な検証なし。",
        en: "Soft contract: `{required: [key, ...], optional: [key, ...]}`. Composer UI reads this to render the param-helper dropdown. Validated at seeder time for system templates; admin-created rows are not strictly validated.",
        vi: "Soft contract: `{required: [key, ...], optional: [key, ...]}`. UI composer đọc để render dropdown param helper. System template validate tại seeder; row admin tạo không validate chặt.",
      },
    },
    is_system: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "システム",
        en: "Is System",
        vi: "Hệ thống",
      },
      placeholder: {
        ja: "システムを入力",
        en: "Enter Is System",
        vi: "Nhập Hệ thống",
      },
      description: {
        ja: "`SystemNotificationTemplateSeeder` によるシード時 `true`。PATCH + DELETE は 403 — 管理者は同じ `key` のブランドスコープ行を作成して上書き。",
        en: "`true` when seeded by `SystemNotificationTemplateSeeder`. PATCH + DELETE return 403 on these rows — admins override by creating a brand-scoped row with the same `key`.",
        vi: "`true` khi seed bởi `SystemNotificationTemplateSeeder`. PATCH + DELETE trả 403 — admin override bằng cách tạo row brand-scoped cùng `key`.",
      },
    },
    created_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["key", "content", "is_system"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "branch_id", "created_by_id"] as const,
} as const;

export type NotificationTemplateFieldName = keyof typeof notificationTemplateMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationTemplate. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationTemplateCreateFormState {
  organization_id?: string | null;
  brand_id?: string | null;
  branch_id?: string | null;
  key: string;
  content: unknown;
  default_channels?: unknown | null;
  params_schema?: unknown | null;
  is_system: boolean;
  created_by_id?: string | null;
}

/** Form-state shape for updating a NotificationTemplate. Every field optional. */
export interface NotificationTemplateUpdateFormState {
  organization_id?: string | null;
  brand_id?: string | null;
  branch_id?: string | null;
  key?: string;
  content?: unknown;
  default_channels?: unknown | null;
  params_schema?: unknown | null;
  is_system?: boolean;
  created_by_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationTemplateCreateForm(): NotificationTemplateCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    branch_id: '',
    key: '',
    content: null,
    default_channels: null,
    params_schema: null,
    is_system: false,
    created_by_id: '',
  };
}

/** Convert create form state into the payload that baseNotificationTemplateCreateSchema accepts. */
export function buildNotificationTemplateCreatePayload(form: NotificationTemplateCreateFormState): BaseNotificationTemplateCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id?.toString().trim() || null,
    brand_id: form.brand_id?.toString().trim() || null,
    branch_id: form.branch_id?.toString().trim() || null,
    key: form.key.trim(),
    content: form.content,
    default_channels: form.default_channels,
    params_schema: form.params_schema,
    is_system: form.is_system,
    created_by_id: form.created_by_id?.toString().trim() || null,
  };

  return payload as BaseNotificationTemplateCreate;
}

/** Convert update form state into the payload that baseNotificationTemplateUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationTemplateUpdatePayload(form: NotificationTemplateUpdateFormState): BaseNotificationTemplateUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.key !== undefined) {
    payload.key = form.key?.toString().trim() ?? null;
  }
  if (form.content !== undefined) {
    payload.content = form.content;
  }
  if (form.default_channels !== undefined) {
    payload.default_channels = form.default_channels;
  }
  if (form.params_schema !== undefined) {
    payload.params_schema = form.params_schema;
  }
  if (form.is_system !== undefined) {
    payload.is_system = form.is_system;
  }
  if (form.created_by_id !== undefined) {
    payload.created_by_id = form.created_by_id?.toString().trim() ?? null;
  }
  return payload as BaseNotificationTemplateUpdate;
}
