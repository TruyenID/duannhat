/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { User } from './User';

/**
 * 通知ルール
 */
export interface NotificationRule {
  id: string;
  organization_id: string;
  organization?: Organization;
  brand_id?: string | null;
  brand?: Brand | null;
  branch_id?: string | null;
  branch?: Branch | null;
  name: string;
  description?: string | null;
  trigger_event: string;
  trigger_model_type?: string | null;
  conditions: unknown;
  action: unknown;
  cooldown_minutes: number;
  is_active: boolean;
  last_fired_at?: DateTimeString | null;
  fire_count: number;
  createdBy_id?: string | null;
  createdBy?: User | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationRule
 */
export const notificationRuleI18n = {
  label: {"ja":"通知ルール","en":"Notification Rule","vi":"Rule thông báo"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"店舗","en":"Shop","vi":"Cửa hàng"},
      placeholder: {"ja":"店舗を入力","en":"Enter Shop","vi":"Nhập Cửa hàng"},
    },
    name: {
      label: {"ja":"名前","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名前を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    trigger_event: {
      label: {"ja":"トリガーイベント","en":"Trigger Event","vi":"Trigger event"},
      placeholder: {"ja":"トリガーイベントを入力","en":"Enter Trigger Event","vi":"Nhập Trigger event"},
    },
    trigger_model_type: {
      label: {"ja":"トリガーモデル型","en":"Trigger Model Type","vi":"Trigger model type"},
      placeholder: {"ja":"トリガーモデル型を入力","en":"Enter Trigger Model Type","vi":"Nhập Trigger model type"},
    },
    conditions: {
      label: {"ja":"条件","en":"Conditions","vi":"Điều kiện"},
      placeholder: {"ja":"条件を入力","en":"Enter Conditions","vi":"Nhập Điều kiện"},
    },
    action: {
      label: {"ja":"アクション","en":"Action","vi":"Action"},
      placeholder: {"ja":"アクションを入力","en":"Enter Action","vi":"Nhập Action"},
    },
    cooldown_minutes: {
      label: {"ja":"クールダウン分","en":"Cooldown Minutes","vi":"Cooldown"},
      placeholder: {"ja":"クールダウン分を入力","en":"Enter Cooldown Minutes","vi":"Nhập Cooldown"},
    },
    is_active: {
      label: {"ja":"アクティブ","en":"Is Active","vi":"Đang hoạt động"},
      placeholder: {"ja":"アクティブを入力","en":"Enter Is Active","vi":"Nhập Đang hoạt động"},
    },
    last_fired_at: {
      label: {"ja":"最終発火日時","en":"Last Fired At","vi":"Lần fire cuối"},
      placeholder: {"ja":"最終発火日時を入力","en":"Enter Last Fired At","vi":"Nhập Lần fire cuối"},
    },
    fire_count: {
      label: {"ja":"発火回数","en":"Fire Count","vi":"Số lần fire"},
      placeholder: {"ja":"発火回数を入力","en":"Enter Fire Count","vi":"Nhập Số lần fire"},
    },
    created_by: {
      label: {"ja":"作成者","en":"Created By","vi":"Người tạo"},
      placeholder: {"ja":"作成者を入力","en":"Enter Created By","vi":"Nhập Người tạo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationRule */
export const baseNotificationRuleSchemas = {
  name: z.string().min(1).max(200),
  description: z.string().optional().nullable(),
  trigger_event: z.string().min(1).max(100),
  trigger_model_type: z.string().max(100).optional().nullable(),
  conditions: z.unknown(),
  action: z.unknown(),
  cooldown_minutes: z.number().int(),
  is_active: z.boolean(),
  last_fired_at: z.string().datetime({ offset: true }).optional().nullable(),
  fire_count: z.number().int(),
} as const;

/** Create schema for NotificationRule */
export const baseNotificationRuleCreateSchema = z.object({
  name: baseNotificationRuleSchemas.name,
  description: baseNotificationRuleSchemas.description,
  trigger_event: baseNotificationRuleSchemas.trigger_event,
  trigger_model_type: baseNotificationRuleSchemas.trigger_model_type,
  conditions: baseNotificationRuleSchemas.conditions,
  action: baseNotificationRuleSchemas.action,
  cooldown_minutes: baseNotificationRuleSchemas.cooldown_minutes,
  is_active: baseNotificationRuleSchemas.is_active,
  last_fired_at: baseNotificationRuleSchemas.last_fired_at,
  fire_count: baseNotificationRuleSchemas.fire_count,
});

/** Update schema for NotificationRule */
export const baseNotificationRuleUpdateSchema = baseNotificationRuleCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationRuleCreate = z.infer<typeof baseNotificationRuleCreateSchema>;
export type BaseNotificationRuleUpdate = z.infer<typeof baseNotificationRuleUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationRuleLabel(locale: string): string {
  return notificationRuleI18n.label[locale as keyof typeof notificationRuleI18n.label] ?? notificationRuleI18n.label['en'] ?? 'NotificationRule';
}

/** Get field label for a specific locale */
export function getNotificationRuleFieldLabel(field: string, locale: string): string {
  const fields = notificationRuleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationRuleFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationRuleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationRule.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationRuleMetadata = {
  modelName: "NotificationRule",
  table: "notification_rules",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 200,
      },
      label: {
        ja: "名前",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名前を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
      description: {
        ja: "管理リストでレンダリングされる可読ルールラベル。自由テキスト。",
        en: "Human-readable rule label rendered in admin lists. Free-text.",
        vi: "Label rule dạng readable, hiển thị trong admin list. Free-text.",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
      description: {
        ja: "管理者の自由記述メモ；受信者には表示されない。",
        en: "Free-form admin notes; not shown to recipients.",
        vi: "Ghi chú tự do của admin; không hiển thị tới recipient.",
      },
    },
    trigger_event: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "トリガーイベント",
        en: "Trigger Event",
        vi: "Trigger event",
      },
      placeholder: {
        ja: "トリガーイベントを入力",
        en: "Enter Trigger Event",
        vi: "Nhập Trigger event",
      },
      description: {
        ja: "Eloquent トリガー用の `model.created` / `model.updated` / `model.deleted`、または非 Eloquent トリガー（plan-023 M8 スケジュール検出器）用の `custom.<name>`。",
        en: "`model.created` / `model.updated` / `model.deleted` for Eloquent triggers, OR `custom.<name>` for non-Eloquent triggers (plan-023 M8 scheduled detectors).",
        vi: "`model.created` / `model.updated` / `model.deleted` cho Eloquent triggers, HOẶC `custom.<name>` cho trigger không phải Eloquent (plan-023 M8 scheduled detector).",
      },
    },
    trigger_model_type: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "トリガーモデル型",
        en: "Trigger Model Type",
        vi: "Trigger model type",
      },
      placeholder: {
        ja: "トリガーモデル型を入力",
        en: "Enter Trigger Model Type",
        vi: "Nhập Trigger model type",
      },
      description: {
        ja: "trigger_event が `model.*` の場合に監視する Eloquent モデルのモーフエイリアス（例：「Recipe」、「CustomerOrder」）。`custom.*` トリガーでは null。",
        en: "Morph alias of the watched Eloquent model when trigger_event is `model.*` (e.g. \"Recipe\", \"CustomerOrder\"). Null on `custom.*` triggers.",
        vi: "Morph alias của Eloquent model được watch khi trigger_event là `model.*` (VD: \"Recipe\", \"CustomerOrder\"). Null cho trigger `custom.*`.",
      },
    },
    conditions: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "条件",
        en: "Conditions",
        vi: "Điều kiện",
      },
      placeholder: {
        ja: "条件を入力",
        en: "Enter Conditions",
        vi: "Nhập Điều kiện",
      },
      description: {
        ja: "JSON DSL ツリー。ノード = `{combinator: and|or, children: [...]}`。リーフ = `{field, op, value?}`。13 のサポート op；ドット区切りフィールドパスは最大深さ 3（data_get）。`RuleDslValidator` で検証；最大深さ 5、最大 50 リーフ。",
        en: "JSON DSL tree. Node = `{combinator: and|or, children: [...]}`. Leaf = `{field, op, value?}`. 13 supported ops; dotted field paths up to depth 3 (data_get). Validated by `RuleDslValidator`; max depth 5, max 50 leaves.",
        vi: "JSON DSL tree. Node = `{combinator: and|or, children: [...]}`. Leaf = `{field, op, value?}`. 13 op hỗ trợ; dotted field path tối đa depth 3 (data_get). Validate bằng `RuleDslValidator`; max depth 5, max 50 leaves.",
      },
    },
    action: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "アクション",
        en: "Action",
        vi: "Action",
      },
      placeholder: {
        ja: "アクションを入力",
        en: "Enter Action",
        vi: "Nhập Action",
      },
      description: {
        ja: "`{template_key, audience_rule, priority, channels, param_template?}`。param_template により管理者は発火モデルのフィールドを参照可能（例：`{warehouse_name: \"$model.warehouse.name\"}`）。",
        en: "`{template_key, audience_rule, priority, channels, param_template?}`. param_template lets the admin reference fields off the firing model (e.g. `{warehouse_name: \"$model.warehouse.name\"}`).",
        vi: "`{template_key, audience_rule, priority, channels, param_template?}`. param_template cho phép admin reference field từ firing model (VD: `{warehouse_name: \"$model.warehouse.name\"}`).",
      },
    },
    cooldown_minutes: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "クールダウン分",
        en: "Cooldown Minutes",
        vi: "Cooldown",
      },
      placeholder: {
        ja: "クールダウン分を入力",
        en: "Enter Cooldown Minutes",
        vi: "Nhập Cooldown",
      },
      description: {
        ja: "同じ (rule, model_id) の連続発火間の最小分数。0 = 常に発火。EvaluateRuleJob は条件評価前に notification_rule_firings をクエリするため、クールダウンスキップは 1 firing 行を書き込むが通知は書き込まない。",
        en: "Minimum minutes between consecutive fires for the same (rule, model_id). 0 = always fire. The EvaluateRuleJob queries notification_rule_firings BEFORE evaluating conditions, so a cooldown skip writes one firing row but no notification.",
        vi: "Số phút tối thiểu giữa các lần fire liên tiếp cho cùng (rule, model_id). 0 = luôn fire. EvaluateRuleJob query notification_rule_firings TRƯỚC khi evaluate condition, nên cooldown skip ghi 1 row firing nhưng không có notification.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "アクティブ",
        en: "Is Active",
        vi: "Đang hoạt động",
      },
      placeholder: {
        ja: "アクティブを入力",
        en: "Enter Is Active",
        vi: "Nhập Đang hoạt động",
      },
      description: {
        ja: "ルールは非アクティブで出荷 — 管理者はドライランの後で明示的に有効化。システムルール（SystemNotificationRuleSeeder）も非アクティブで開始；パラレルシャドウがハードコード化されたエミッターとのパリティをフリップ前に検証。",
        en: "Rules ship inactive — admin enables explicitly after dry-run. System rules (SystemNotificationRuleSeeder) also start inactive; parallel-shadow validates parity with hardcoded emitters before flip.",
        vi: "Rule ship inactive — admin enable explicit sau dry-run. System rule (SystemNotificationRuleSeeder) cũng start inactive; parallel-shadow validate parity với hardcoded emitter trước khi flip.",
      },
    },
    last_fired_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最終発火日時",
        en: "Last Fired At",
        vi: "Lần fire cuối",
      },
      placeholder: {
        ja: "最終発火日時を入力",
        en: "Enter Last Fired At",
        vi: "Nhập Lần fire cuối",
      },
      description: {
        ja: "成功（outcome=matched）発火後に EvaluateRuleJob が更新。skipped_cooldown / skipped_condition の結果では更新されない。",
        en: "Updated by EvaluateRuleJob after a successful (outcome=matched) fire. NOT updated for skipped_cooldown / skipped_condition outcomes.",
        vi: "Update bởi EvaluateRuleJob sau khi fire thành công (outcome=matched). KHÔNG update cho skipped_cooldown / skipped_condition.",
      },
    },
    fire_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "発火回数",
        en: "Fire Count",
        vi: "Số lần fire",
      },
      placeholder: {
        ja: "発火回数を入力",
        en: "Enter Fire Count",
        vi: "Nhập Số lần fire",
      },
      description: {
        ja: "ルール作成以降の成功（outcome=matched）発火の合計。診断カウンタ；notification_rule_firings の監査証跡が権威。",
        en: "Total successful (outcome=matched) fires since rule creation. Diagnostics counter; the audit trail in notification_rule_firings is authoritative.",
        vi: "Tổng số fire thành công (outcome=matched) kể từ khi rule tạo. Counter diagnostic; audit trail trong notification_rule_firings là authoritative.",
      },
    },
    created_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "name", "trigger_event", "conditions", "action", "cooldown_minutes", "is_active", "fire_count"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "branch_id", "created_by_id"] as const,
} as const;

export type NotificationRuleFieldName = keyof typeof notificationRuleMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationRule. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationRuleCreateFormState {
  organization_id: string;
  brand_id?: string | null;
  branch_id?: string | null;
  name: string;
  description?: string | null;
  trigger_event: string;
  trigger_model_type?: string | null;
  conditions: unknown;
  action: unknown;
  cooldown_minutes: number;
  is_active: boolean;
  last_fired_at?: string | null;
  fire_count: number;
  created_by_id?: string | null;
}

/** Form-state shape for updating a NotificationRule. Every field optional. */
export interface NotificationRuleUpdateFormState {
  organization_id?: string;
  brand_id?: string | null;
  branch_id?: string | null;
  name?: string;
  description?: string | null;
  trigger_event?: string;
  trigger_model_type?: string | null;
  conditions?: unknown;
  action?: unknown;
  cooldown_minutes?: number;
  is_active?: boolean;
  last_fired_at?: string | null;
  fire_count?: number;
  created_by_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationRuleCreateForm(): NotificationRuleCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    branch_id: '',
    name: '',
    description: '',
    trigger_event: '',
    trigger_model_type: '',
    conditions: null,
    action: null,
    cooldown_minutes: 0,
    is_active: false,
    last_fired_at: '',
    fire_count: 0,
    created_by_id: '',
  };
}

/** Convert create form state into the payload that baseNotificationRuleCreateSchema accepts. */
export function buildNotificationRuleCreatePayload(form: NotificationRuleCreateFormState): BaseNotificationRuleCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id?.toString().trim() || null,
    branch_id: form.branch_id?.toString().trim() || null,
    name: form.name.trim(),
    description: form.description?.toString().trim() || null,
    trigger_event: form.trigger_event.trim(),
    trigger_model_type: form.trigger_model_type?.toString().trim() || null,
    conditions: form.conditions,
    action: form.action,
    cooldown_minutes: form.cooldown_minutes,
    is_active: form.is_active,
    last_fired_at: form.last_fired_at,
    fire_count: form.fire_count,
    created_by_id: form.created_by_id?.toString().trim() || null,
  };

  return payload as BaseNotificationRuleCreate;
}

/** Convert update form state into the payload that baseNotificationRuleUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationRuleUpdatePayload(form: NotificationRuleUpdateFormState): BaseNotificationRuleUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = form.description?.toString().trim() ?? null;
  }
  if (form.trigger_event !== undefined) {
    payload.trigger_event = form.trigger_event?.toString().trim() ?? null;
  }
  if (form.trigger_model_type !== undefined) {
    payload.trigger_model_type = form.trigger_model_type?.toString().trim() ?? null;
  }
  if (form.conditions !== undefined) {
    payload.conditions = form.conditions;
  }
  if (form.action !== undefined) {
    payload.action = form.action;
  }
  if (form.cooldown_minutes !== undefined) {
    payload.cooldown_minutes = form.cooldown_minutes;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.last_fired_at !== undefined) {
    payload.last_fired_at = form.last_fired_at;
  }
  if (form.fire_count !== undefined) {
    payload.fire_count = form.fire_count;
  }
  if (form.created_by_id !== undefined) {
    payload.created_by_id = form.created_by_id?.toString().trim() ?? null;
  }
  return payload as BaseNotificationRuleUpdate;
}
