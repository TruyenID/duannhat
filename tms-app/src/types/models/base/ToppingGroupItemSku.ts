/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { ProductSku } from './ProductSku';
import type { ToppingGroupItem } from './ToppingGroupItem';

/**
 * トッピンググループ項目SKU
 */
export interface ToppingGroupItemSku {
  id: string;
  toppingGroupItem_id: string;
  toppingGroupItem?: ToppingGroupItem;
  productSku_id?: string | null;
  productSku?: ProductSku | null;
  extra_price: number;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ToppingGroupItemSku
 */
export const toppingGroupItemSkuI18n = {
  label: {"ja":"トッピンググループ項目SKU","en":"Topping Group Item SKU","vi":"Giá SKU của Mục Topping"},
  fields: {
    topping_group_item: {
      label: {"ja":"トッピンググループ項目","en":"Topping Group Item","vi":"Mục Topping"},
      placeholder: {"ja":"トッピンググループ項目を入力","en":"Enter Topping Group Item","vi":"Nhập Mục Topping"},
    },
    product_sku: {
      label: {"ja":"商品SKU","en":"Product SKU","vi":"SKU sản phẩm"},
      placeholder: {"ja":"商品SKUを入力","en":"Enter Product SKU","vi":"Nhập SKU sản phẩm"},
    },
    extra_price: {
      label: {"ja":"追加料金","en":"Extra Price","vi":"Giá thêm"},
      placeholder: {"ja":"追加料金を入力","en":"Enter Extra Price","vi":"Nhập Giá thêm"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ToppingGroupItemSku */
export const baseToppingGroupItemSkuSchemas = {
  extra_price: z.number(),
} as const;

/** Create schema for ToppingGroupItemSku */
export const baseToppingGroupItemSkuCreateSchema = z.object({
  extra_price: baseToppingGroupItemSkuSchemas.extra_price,
});

/** Update schema for ToppingGroupItemSku */
export const baseToppingGroupItemSkuUpdateSchema = baseToppingGroupItemSkuCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseToppingGroupItemSkuCreate = z.infer<typeof baseToppingGroupItemSkuCreateSchema>;
export type BaseToppingGroupItemSkuUpdate = z.infer<typeof baseToppingGroupItemSkuUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getToppingGroupItemSkuLabel(locale: string): string {
  return toppingGroupItemSkuI18n.label[locale as keyof typeof toppingGroupItemSkuI18n.label] ?? toppingGroupItemSkuI18n.label['en'] ?? 'ToppingGroupItemSku';
}

/** Get field label for a specific locale */
export function getToppingGroupItemSkuFieldLabel(field: string, locale: string): string {
  const fields = toppingGroupItemSkuI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getToppingGroupItemSkuFieldPlaceholder(field: string, locale: string): string {
  const fields = toppingGroupItemSkuI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ToppingGroupItemSku.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const toppingGroupItemSkuMetadata = {
  modelName: "ToppingGroupItemSku",
  table: "topping_group_item_skus",

  fields: {
    topping_group_item_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ToppingGroupItem",
        table: "topping_group_items",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "CASCADE",
      },
      label: {},
    },
    extra_price: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "追加料金",
        en: "Extra Price",
        vi: "Giá thêm",
      },
      placeholder: {
        ja: "追加料金を入力",
        en: "Enter Extra Price",
        vi: "Nhập Giá thêm",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["topping_group_item_id", "extra_price"] as const,
  enumFields: [] as const,
  relationFields: ["topping_group_item_id", "product_sku_id"] as const,
} as const;

export type ToppingGroupItemSkuFieldName = keyof typeof toppingGroupItemSkuMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ToppingGroupItemSku. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ToppingGroupItemSkuCreateFormState {
  topping_group_item_id: string;
  product_sku_id?: string | null;
  extra_price: number;
}

/** Form-state shape for updating a ToppingGroupItemSku. Every field optional. */
export interface ToppingGroupItemSkuUpdateFormState {
  topping_group_item_id?: string;
  product_sku_id?: string | null;
  extra_price?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyToppingGroupItemSkuCreateForm(): ToppingGroupItemSkuCreateFormState {
  return {
    topping_group_item_id: '',
    product_sku_id: '',
    extra_price: 0,
  };
}

/** Convert create form state into the payload that baseToppingGroupItemSkuCreateSchema accepts. */
export function buildToppingGroupItemSkuCreatePayload(form: ToppingGroupItemSkuCreateFormState): BaseToppingGroupItemSkuCreate {
  const payload: Record<string, unknown> = {
    topping_group_item_id: form.topping_group_item_id.trim(),
    product_sku_id: form.product_sku_id?.toString().trim() || null,
    extra_price: form.extra_price,
  };

  return payload as BaseToppingGroupItemSkuCreate;
}

/** Convert update form state into the payload that baseToppingGroupItemSkuUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildToppingGroupItemSkuUpdatePayload(form: ToppingGroupItemSkuUpdateFormState): BaseToppingGroupItemSkuUpdate {
  const payload: Record<string, unknown> = {};
  if (form.topping_group_item_id !== undefined) {
    payload.topping_group_item_id = form.topping_group_item_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.extra_price !== undefined) {
    payload.extra_price = form.extra_price;
  }
  return payload as BaseToppingGroupItemSkuUpdate;
}
