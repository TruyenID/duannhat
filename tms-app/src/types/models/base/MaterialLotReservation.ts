/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { MaterialBatch } from './MaterialBatch';
import type { MaterialLot } from './MaterialLot';
import type { User } from './User';

/**
 * 材料ロット予約
 */
export interface MaterialLotReservation {
  id: string;
  materialLot_id: string;
  materialLot?: MaterialLot;
  qty_reserved: number;
  reservedBy_id: string;
  reservedBy?: User;
  expected_consume_at?: DateTimeString | null;
  materialBatch_id?: string | null;
  materialBatch?: MaterialBatch | null;
  status: string;
  reason?: string | null;
  organization_id: string;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MaterialLotReservation
 */
export const materialLotReservationI18n = {
  label: {"ja":"材料ロット予約","en":"Material Lot Reservation","vi":"Đặt trước lô nguyên liệu"},
  fields: {
    material_lot: {
      label: {"ja":"材料ロット","en":"Material Lot","vi":"Lô nguyên liệu"},
      placeholder: {"ja":"材料ロットを入力","en":"Enter Material Lot","vi":"Nhập Lô nguyên liệu"},
    },
    qty_reserved: {
      label: {"ja":"予約数量","en":"Quantity Reserved","vi":"Số lượng đặt trước"},
      placeholder: {"ja":"予約数量を入力","en":"Enter Quantity Reserved","vi":"Nhập Số lượng đặt trước"},
    },
    reserved_by: {
      label: {"ja":"予約者","en":"Reserved By","vi":"Người đặt trước"},
      placeholder: {"ja":"予約者を入力","en":"Enter Reserved By","vi":"Nhập Người đặt trước"},
    },
    expected_consume_at: {
      label: {"ja":"消費予定日時","en":"Expected Consume At","vi":"Thời điểm dự kiến tiêu thụ"},
      placeholder: {"ja":"消費予定日時を入力","en":"Enter Expected Consume At","vi":"Nhập Thời điểm dự kiến tiêu thụ"},
    },
    material_batch: {
      label: {"ja":"材料バッチ","en":"Material Batch","vi":"Lô sản xuất"},
      placeholder: {"ja":"材料バッチを入力","en":"Enter Material Batch","vi":"Nhập Lô sản xuất"},
    },
    status: {
      label: {"ja":"ステータス","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"ステータスを入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    reason: {
      label: {"ja":"理由","en":"Reason","vi":"Lý do"},
      placeholder: {"ja":"理由を入力","en":"Enter Reason","vi":"Nhập Lý do"},
    },
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MaterialLotReservation */
export const baseMaterialLotReservationSchemas = {
  qty_reserved: z.number(),
  expected_consume_at: z.string().datetime({ offset: true }).optional().nullable(),
  status: z.string().min(1).max(16),
  reason: z.string().optional().nullable(),
  organization_id: z.string(),
} as const;

/** Create schema for MaterialLotReservation */
export const baseMaterialLotReservationCreateSchema = z.object({
  qty_reserved: baseMaterialLotReservationSchemas.qty_reserved,
  expected_consume_at: baseMaterialLotReservationSchemas.expected_consume_at,
  status: baseMaterialLotReservationSchemas.status,
  reason: baseMaterialLotReservationSchemas.reason,
  organization_id: baseMaterialLotReservationSchemas.organization_id,
});

/** Update schema for MaterialLotReservation */
export const baseMaterialLotReservationUpdateSchema = baseMaterialLotReservationCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMaterialLotReservationCreate = z.infer<typeof baseMaterialLotReservationCreateSchema>;
export type BaseMaterialLotReservationUpdate = z.infer<typeof baseMaterialLotReservationUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMaterialLotReservationLabel(locale: string): string {
  return materialLotReservationI18n.label[locale as keyof typeof materialLotReservationI18n.label] ?? materialLotReservationI18n.label['en'] ?? 'MaterialLotReservation';
}

/** Get field label for a specific locale */
export function getMaterialLotReservationFieldLabel(field: string, locale: string): string {
  const fields = materialLotReservationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMaterialLotReservationFieldPlaceholder(field: string, locale: string): string {
  const fields = materialLotReservationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MaterialLotReservation.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const materialLotReservationMetadata = {
  modelName: "MaterialLotReservation",
  table: "material_lot_reservations",

  fields: {
    material_lot_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "MaterialLot",
        table: "material_lots",
        onDelete: "CASCADE",
      },
      label: {},
    },
    qty_reserved: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "予約数量",
        en: "Quantity Reserved",
        vi: "Số lượng đặt trước",
      },
      placeholder: {
        ja: "予約数量を入力",
        en: "Enter Quantity Reserved",
        vi: "Nhập Số lượng đặt trước",
      },
    },
    reserved_by_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    expected_consume_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "消費予定日時",
        en: "Expected Consume At",
        vi: "Thời điểm dự kiến tiêu thụ",
      },
      placeholder: {
        ja: "消費予定日時を入力",
        en: "Enter Expected Consume At",
        vi: "Nhập Thời điểm dự kiến tiêu thụ",
      },
    },
    material_batch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "MaterialBatch",
        table: "material_batches",
        onDelete: "SET NULL",
      },
      label: {},
    },
    status: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 16,
      },
      label: {
        ja: "ステータス",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "ステータスを入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    reason: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "理由",
        en: "Reason",
        vi: "Lý do",
      },
      placeholder: {
        ja: "理由を入力",
        en: "Enter Reason",
        vi: "Nhập Lý do",
      },
    },
    organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["material_lot_id", "qty_reserved", "reserved_by_id", "status", "organization_id"] as const,
  enumFields: [] as const,
  relationFields: ["material_lot_id", "reserved_by_id", "material_batch_id"] as const,
} as const;

export type MaterialLotReservationFieldName = keyof typeof materialLotReservationMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MaterialLotReservation. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MaterialLotReservationCreateFormState {
  material_lot_id: string;
  qty_reserved: number;
  reserved_by_id: string;
  expected_consume_at?: string | null;
  material_batch_id?: string | null;
  status: string;
  reason?: string | null;
  organization_id: string;
}

/** Form-state shape for updating a MaterialLotReservation. Every field optional. */
export interface MaterialLotReservationUpdateFormState {
  material_lot_id?: string;
  qty_reserved?: number;
  reserved_by_id?: string;
  expected_consume_at?: string | null;
  material_batch_id?: string | null;
  status?: string;
  reason?: string | null;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMaterialLotReservationCreateForm(): MaterialLotReservationCreateFormState {
  return {
    material_lot_id: '',
    qty_reserved: 0,
    reserved_by_id: '',
    expected_consume_at: '',
    material_batch_id: '',
    status: '',
    reason: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseMaterialLotReservationCreateSchema accepts. */
export function buildMaterialLotReservationCreatePayload(form: MaterialLotReservationCreateFormState): BaseMaterialLotReservationCreate {
  const payload: Record<string, unknown> = {
    material_lot_id: form.material_lot_id.trim(),
    qty_reserved: form.qty_reserved,
    reserved_by_id: form.reserved_by_id.trim(),
    expected_consume_at: form.expected_consume_at,
    material_batch_id: form.material_batch_id?.toString().trim() || null,
    status: form.status.trim(),
    reason: form.reason?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
  };

  return payload as BaseMaterialLotReservationCreate;
}

/** Convert update form state into the payload that baseMaterialLotReservationUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMaterialLotReservationUpdatePayload(form: MaterialLotReservationUpdateFormState): BaseMaterialLotReservationUpdate {
  const payload: Record<string, unknown> = {};
  if (form.material_lot_id !== undefined) {
    payload.material_lot_id = form.material_lot_id?.toString().trim() ?? null;
  }
  if (form.qty_reserved !== undefined) {
    payload.qty_reserved = form.qty_reserved;
  }
  if (form.reserved_by_id !== undefined) {
    payload.reserved_by_id = form.reserved_by_id?.toString().trim() ?? null;
  }
  if (form.expected_consume_at !== undefined) {
    payload.expected_consume_at = form.expected_consume_at;
  }
  if (form.material_batch_id !== undefined) {
    payload.material_batch_id = form.material_batch_id?.toString().trim() ?? null;
  }
  if (form.status !== undefined) {
    payload.status = form.status?.toString().trim() ?? null;
  }
  if (form.reason !== undefined) {
    payload.reason = form.reason?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseMaterialLotReservationUpdate;
}
