/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { TillTenderCategory } from '../enum/TillTenderCategory';
import type { TillSession } from './TillSession';
import type { TillTenderType } from './TillTenderType';

/**
 * テンダー精算明細
 */
export interface TillSettlementTenderDetail {
  id: string;
  tender_key: string;
  category: TillTenderCategory;
  currency_code: string;
  expected_amount?: number | null;
  declared_gross_amount: number;
  declared_cancel_amount: number;
  declared_amount: number;
  terminal_batch_total?: number | null;
  variance_amount?: number | null;
  variance_reason?: string | null;
  session_id: string;
  session?: TillSession;
  tenderType_id: string;
  tenderType?: TillTenderType;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TillSettlementTenderDetail
 */
export const tillSettlementTenderDetailI18n = {
  label: {"ja":"テンダー精算明細","en":"Till Settlement Tender Detail","vi":"Chi tiết đối soát tender"},
  fields: {
    tender_key: {
      label: {"ja":"テンダーキー","en":"Tender Key","vi":"Mã tender"},
      placeholder: {"ja":"テンダーキーを入力","en":"Enter Tender Key","vi":"Nhập Mã tender"},
    },
    category: {
      label: {"ja":"分類","en":"Category","vi":"Phân loại"},
      placeholder: {"ja":"分類を入力","en":"Enter Category","vi":"Nhập Phân loại"},
    },
    currency_code: {
      label: {"ja":"通貨","en":"Currency","vi":"Tiền tệ"},
      placeholder: {"ja":"通貨を入力","en":"Enter Currency","vi":"Nhập Tiền tệ"},
    },
    expected_amount: {
      label: {"ja":"予定額","en":"Expected Amount","vi":"Số dự kiến"},
      placeholder: {"ja":"予定額を入力","en":"Enter Expected Amount","vi":"Nhập Số dự kiến"},
    },
    declared_gross_amount: {
      label: {"ja":"売上","en":"Declared Gross (売上)","vi":"Doanh thu (gross)"},
      placeholder: {"ja":"売上を入力","en":"Enter Declared Gross (売上)","vi":"Nhập Doanh thu (gross)"},
    },
    declared_cancel_amount: {
      label: {"ja":"取消","en":"Declared Cancel (取消)","vi":"Huỷ (cancel)"},
      placeholder: {"ja":"取消を入力","en":"Enter Declared Cancel (取消)","vi":"Nhập Huỷ (cancel)"},
    },
    declared_amount: {
      label: {"ja":"申告額（純額）","en":"Declared Net Amount","vi":"Số thực khai"},
      placeholder: {"ja":"申告額（純額）を入力","en":"Enter Declared Net Amount","vi":"Nhập Số thực khai"},
    },
    terminal_batch_total: {
      label: {"ja":"端末合計","en":"Terminal Batch Total","vi":"Tổng máy"},
      placeholder: {"ja":"端末合計を入力","en":"Enter Terminal Batch Total","vi":"Nhập Tổng máy"},
    },
    variance_amount: {
      label: {"ja":"差額","en":"Variance","vi":"Sai số"},
      placeholder: {"ja":"差額を入力","en":"Enter Variance","vi":"Nhập Sai số"},
    },
    variance_reason: {
      label: {"ja":"差額理由","en":"Variance Reason","vi":"Lý do sai số"},
      placeholder: {"ja":"差額理由を入力","en":"Enter Variance Reason","vi":"Nhập Lý do sai số"},
    },
    session: {
      label: {"ja":"セッション","en":"Session","vi":"Ca"},
      placeholder: {"ja":"セッションを入力","en":"Enter Session","vi":"Nhập Ca"},
    },
    tender_type: {
      label: {"ja":"テンダー種別","en":"Tender Type","vi":"Loại tender"},
      placeholder: {"ja":"テンダー種別を入力","en":"Enter Tender Type","vi":"Nhập Loại tender"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TillSettlementTenderDetail */
export const baseTillSettlementTenderDetailSchemas = {
  tender_key: z.string().min(1).max(50),
  category: z.nativeEnum(TillTenderCategory),
  currency_code: z.string().min(1).max(3),
  expected_amount: z.number().optional().nullable(),
  declared_gross_amount: z.number(),
  declared_cancel_amount: z.number(),
  declared_amount: z.number(),
  terminal_batch_total: z.number().optional().nullable(),
  variance_amount: z.number().optional().nullable(),
  variance_reason: z.string().optional().nullable(),
} as const;

/** Create schema for TillSettlementTenderDetail */
export const baseTillSettlementTenderDetailCreateSchema = z.object({
  tender_key: baseTillSettlementTenderDetailSchemas.tender_key,
  category: baseTillSettlementTenderDetailSchemas.category,
  currency_code: baseTillSettlementTenderDetailSchemas.currency_code,
  expected_amount: baseTillSettlementTenderDetailSchemas.expected_amount,
  declared_gross_amount: baseTillSettlementTenderDetailSchemas.declared_gross_amount,
  declared_cancel_amount: baseTillSettlementTenderDetailSchemas.declared_cancel_amount,
  declared_amount: baseTillSettlementTenderDetailSchemas.declared_amount,
  terminal_batch_total: baseTillSettlementTenderDetailSchemas.terminal_batch_total,
  variance_amount: baseTillSettlementTenderDetailSchemas.variance_amount,
  variance_reason: baseTillSettlementTenderDetailSchemas.variance_reason,
});

/** Update schema for TillSettlementTenderDetail */
export const baseTillSettlementTenderDetailUpdateSchema = baseTillSettlementTenderDetailCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTillSettlementTenderDetailCreate = z.infer<typeof baseTillSettlementTenderDetailCreateSchema>;
export type BaseTillSettlementTenderDetailUpdate = z.infer<typeof baseTillSettlementTenderDetailUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTillSettlementTenderDetailLabel(locale: string): string {
  return tillSettlementTenderDetailI18n.label[locale as keyof typeof tillSettlementTenderDetailI18n.label] ?? tillSettlementTenderDetailI18n.label['en'] ?? 'TillSettlementTenderDetail';
}

/** Get field label for a specific locale */
export function getTillSettlementTenderDetailFieldLabel(field: string, locale: string): string {
  const fields = tillSettlementTenderDetailI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTillSettlementTenderDetailFieldPlaceholder(field: string, locale: string): string {
  const fields = tillSettlementTenderDetailI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TillSettlementTenderDetail.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tillSettlementTenderDetailMetadata = {
  modelName: "TillSettlementTenderDetail",
  table: "till_settlement_tender_details",

  fields: {
    tender_key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "テンダーキー",
        en: "Tender Key",
        vi: "Mã tender",
      },
      placeholder: {
        ja: "テンダーキーを入力",
        en: "Enter Tender Key",
        vi: "Nhập Mã tender",
      },
    },
    category: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "TillTenderCategory",
      enumValues: ["cash", "card", "qr", "emoney"] as const,
      label: {
        ja: "分類",
        en: "Category",
        vi: "Phân loại",
      },
      placeholder: {
        ja: "分類を入力",
        en: "Enter Category",
        vi: "Nhập Phân loại",
      },
    },
    currency_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 3,
      },
      label: {
        ja: "通貨",
        en: "Currency",
        vi: "Tiền tệ",
      },
      placeholder: {
        ja: "通貨を入力",
        en: "Enter Currency",
        vi: "Nhập Tiền tệ",
      },
    },
    expected_amount: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "予定額",
        en: "Expected Amount",
        vi: "Số dự kiến",
      },
      placeholder: {
        ja: "予定額を入力",
        en: "Enter Expected Amount",
        vi: "Nhập Số dự kiến",
      },
    },
    declared_gross_amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "売上",
        en: "Declared Gross (売上)",
        vi: "Doanh thu (gross)",
      },
      placeholder: {
        ja: "売上を入力",
        en: "Enter Declared Gross (売上)",
        vi: "Nhập Doanh thu (gross)",
      },
    },
    declared_cancel_amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "取消",
        en: "Declared Cancel (取消)",
        vi: "Huỷ (cancel)",
      },
      placeholder: {
        ja: "取消を入力",
        en: "Enter Declared Cancel (取消)",
        vi: "Nhập Huỷ (cancel)",
      },
    },
    declared_amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "申告額（純額）",
        en: "Declared Net Amount",
        vi: "Số thực khai",
      },
      placeholder: {
        ja: "申告額（純額）を入力",
        en: "Enter Declared Net Amount",
        vi: "Nhập Số thực khai",
      },
    },
    terminal_batch_total: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "端末合計",
        en: "Terminal Batch Total",
        vi: "Tổng máy",
      },
      placeholder: {
        ja: "端末合計を入力",
        en: "Enter Terminal Batch Total",
        vi: "Nhập Tổng máy",
      },
    },
    variance_amount: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "差額",
        en: "Variance",
        vi: "Sai số",
      },
      placeholder: {
        ja: "差額を入力",
        en: "Enter Variance",
        vi: "Nhập Sai số",
      },
    },
    variance_reason: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "差額理由",
        en: "Variance Reason",
        vi: "Lý do sai số",
      },
      placeholder: {
        ja: "差額理由を入力",
        en: "Enter Variance Reason",
        vi: "Nhập Lý do sai số",
      },
    },
    session_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "TillSession",
        table: "till_sessions",
        onDelete: "CASCADE",
      },
      label: {},
    },
    tender_type_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "TillTenderType",
        table: "till_tender_types",
        onDelete: "RESTRICT",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["tender_key", "category", "currency_code", "declared_gross_amount", "declared_cancel_amount", "declared_amount", "session_id", "tender_type_id"] as const,
  enumFields: ["category"] as const,
  relationFields: ["session_id", "tender_type_id"] as const,
} as const;

export type TillSettlementTenderDetailFieldName = keyof typeof tillSettlementTenderDetailMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TillSettlementTenderDetail. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TillSettlementTenderDetailCreateFormState {
  tender_key: string;
  category: TillTenderCategory;
  currency_code: string;
  expected_amount?: number | null;
  declared_gross_amount: number;
  declared_cancel_amount: number;
  declared_amount: number;
  terminal_batch_total?: number | null;
  variance_amount?: number | null;
  variance_reason?: string | null;
  session_id: string;
  tender_type_id: string;
}

/** Form-state shape for updating a TillSettlementTenderDetail. Every field optional. */
export interface TillSettlementTenderDetailUpdateFormState {
  tender_key?: string;
  category?: TillTenderCategory;
  currency_code?: string;
  expected_amount?: number | null;
  declared_gross_amount?: number;
  declared_cancel_amount?: number;
  declared_amount?: number;
  terminal_batch_total?: number | null;
  variance_amount?: number | null;
  variance_reason?: string | null;
  session_id?: string;
  tender_type_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTillSettlementTenderDetailCreateForm(): TillSettlementTenderDetailCreateFormState {
  return {
    tender_key: '',
    category: TillTenderCategory.Cash,
    currency_code: '',
    expected_amount: 0,
    declared_gross_amount: 0,
    declared_cancel_amount: 0,
    declared_amount: 0,
    terminal_batch_total: 0,
    variance_amount: 0,
    variance_reason: '',
    session_id: '',
    tender_type_id: '',
  };
}

/** Convert create form state into the payload that baseTillSettlementTenderDetailCreateSchema accepts. */
export function buildTillSettlementTenderDetailCreatePayload(form: TillSettlementTenderDetailCreateFormState): BaseTillSettlementTenderDetailCreate {
  const payload: Record<string, unknown> = {
    tender_key: form.tender_key.trim(),
    category: form.category,
    currency_code: form.currency_code.trim(),
    expected_amount: form.expected_amount,
    declared_gross_amount: form.declared_gross_amount,
    declared_cancel_amount: form.declared_cancel_amount,
    declared_amount: form.declared_amount,
    terminal_batch_total: form.terminal_batch_total,
    variance_amount: form.variance_amount,
    variance_reason: form.variance_reason?.toString().trim() || null,
    session_id: form.session_id.trim(),
    tender_type_id: form.tender_type_id.trim(),
  };

  return payload as BaseTillSettlementTenderDetailCreate;
}

/** Convert update form state into the payload that baseTillSettlementTenderDetailUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTillSettlementTenderDetailUpdatePayload(form: TillSettlementTenderDetailUpdateFormState): BaseTillSettlementTenderDetailUpdate {
  const payload: Record<string, unknown> = {};
  if (form.tender_key !== undefined) {
    payload.tender_key = form.tender_key?.toString().trim() ?? null;
  }
  if (form.category !== undefined) {
    payload.category = form.category;
  }
  if (form.currency_code !== undefined) {
    payload.currency_code = form.currency_code?.toString().trim() ?? null;
  }
  if (form.expected_amount !== undefined) {
    payload.expected_amount = form.expected_amount;
  }
  if (form.declared_gross_amount !== undefined) {
    payload.declared_gross_amount = form.declared_gross_amount;
  }
  if (form.declared_cancel_amount !== undefined) {
    payload.declared_cancel_amount = form.declared_cancel_amount;
  }
  if (form.declared_amount !== undefined) {
    payload.declared_amount = form.declared_amount;
  }
  if (form.terminal_batch_total !== undefined) {
    payload.terminal_batch_total = form.terminal_batch_total;
  }
  if (form.variance_amount !== undefined) {
    payload.variance_amount = form.variance_amount;
  }
  if (form.variance_reason !== undefined) {
    payload.variance_reason = form.variance_reason?.toString().trim() ?? null;
  }
  if (form.session_id !== undefined) {
    payload.session_id = form.session_id?.toString().trim() ?? null;
  }
  if (form.tender_type_id !== undefined) {
    payload.tender_type_id = form.tender_type_id?.toString().trim() ?? null;
  }
  return payload as BaseTillSettlementTenderDetailUpdate;
}
