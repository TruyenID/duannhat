/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { MovementType } from '../enum/MovementType';
import type { Material } from './Material';
import type { MaterialLot } from './MaterialLot';
import type { ProductSku } from './ProductSku';
import type { StockTransaction } from './StockTransaction';
import type { Warehouse } from './Warehouse';

/**
 * 在庫変動
 */
export interface StockMovement {
  id: string;
  warehouse_id: string;
  warehouse?: Warehouse;
  productSku_id?: string | null;
  productSku?: ProductSku | null;
  material_id?: string | null;
  material?: Material | null;
  materialLot_id?: string | null;
  materialLot?: MaterialLot | null;
  stockTransaction_id: string;
  stockTransaction?: StockTransaction;
  movement_type: MovementType;
  quantity: number;
  quantity_before: number;
  quantity_after: number;
  unit?: string | null;
  created_at: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for StockMovement
 */
export const stockMovementI18n = {
  label: {"ja":"在庫変動","en":"Stock Movement","vi":"Biến động kho"},
  fields: {
    warehouse: {
      label: {"ja":"倉庫","en":"Warehouse","vi":"Kho hàng"},
      placeholder: {"ja":"倉庫を入力","en":"Enter Warehouse","vi":"Nhập Kho hàng"},
    },
    product_sku: {
      label: {"ja":"商品バリエーション","en":"Product Variant","vi":"Biến thể sản phẩm"},
      placeholder: {"ja":"商品バリエーションを入力","en":"Enter Product Variant","vi":"Nhập Biến thể sản phẩm"},
    },
    material: {
      label: {"ja":"材料","en":"Material","vi":"Nguyên liệu"},
      placeholder: {"ja":"材料を入力","en":"Enter Material","vi":"Nhập Nguyên liệu"},
    },
    material_lot: {
      label: {"ja":"材料ロット","en":"Material Lot","vi":"Lô nguyên liệu"},
      placeholder: {"ja":"材料ロットを入力","en":"Enter Material Lot","vi":"Nhập Lô nguyên liệu"},
    },
    stock_transaction: {
      label: {"ja":"在庫取引","en":"Stock Transaction","vi":"Phiếu kho"},
      placeholder: {"ja":"在庫取引を入力","en":"Enter Stock Transaction","vi":"Nhập Phiếu kho"},
    },
    movement_type: {
      label: {"ja":"変動タイプ","en":"Movement Type","vi":"Loại biến động"},
      placeholder: {"ja":"変動タイプを入力","en":"Enter Movement Type","vi":"Nhập Loại biến động"},
    },
    quantity: {
      label: {"ja":"数量","en":"Quantity","vi":"Số lượng"},
      placeholder: {"ja":"数量を入力","en":"Enter Quantity","vi":"Nhập Số lượng"},
    },
    quantity_before: {
      label: {"ja":"変動前数量","en":"Quantity Before","vi":"Tồn trước"},
      placeholder: {"ja":"変動前数量を入力","en":"Enter Quantity Before","vi":"Nhập Tồn trước"},
    },
    quantity_after: {
      label: {"ja":"変動後数量","en":"Quantity After","vi":"Tồn sau"},
      placeholder: {"ja":"変動後数量を入力","en":"Enter Quantity After","vi":"Nhập Tồn sau"},
    },
    unit: {
      label: {"ja":"単位","en":"Unit","vi":"Đơn vị"},
      placeholder: {"ja":"単位を入力","en":"Enter Unit","vi":"Nhập Đơn vị"},
    },
    created_at: {
      label: {"ja":"作成日時","en":"Created At","vi":"Ngày tạo"},
      placeholder: {"ja":"作成日時を入力","en":"Enter Created At","vi":"Nhập Ngày tạo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for StockMovement */
export const baseStockMovementSchemas = {
  movement_type: z.nativeEnum(MovementType),
  quantity: z.number(),
  quantity_before: z.number(),
  quantity_after: z.number(),
  unit: z.string().max(50).optional().nullable(),
  created_at: z.string().datetime({ offset: true }),
} as const;

/** Create schema for StockMovement */
export const baseStockMovementCreateSchema = z.object({
  movement_type: baseStockMovementSchemas.movement_type,
  quantity: baseStockMovementSchemas.quantity,
  quantity_before: baseStockMovementSchemas.quantity_before,
  quantity_after: baseStockMovementSchemas.quantity_after,
  unit: baseStockMovementSchemas.unit,
  created_at: baseStockMovementSchemas.created_at,
});

/** Update schema for StockMovement */
export const baseStockMovementUpdateSchema = baseStockMovementCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseStockMovementCreate = z.infer<typeof baseStockMovementCreateSchema>;
export type BaseStockMovementUpdate = z.infer<typeof baseStockMovementUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getStockMovementLabel(locale: string): string {
  return stockMovementI18n.label[locale as keyof typeof stockMovementI18n.label] ?? stockMovementI18n.label['en'] ?? 'StockMovement';
}

/** Get field label for a specific locale */
export function getStockMovementFieldLabel(field: string, locale: string): string {
  const fields = stockMovementI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getStockMovementFieldPlaceholder(field: string, locale: string): string {
  const fields = stockMovementI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for StockMovement.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const stockMovementMetadata = {
  modelName: "StockMovement",
  table: "stock_movements",

  fields: {
    warehouse_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Warehouse",
        table: "warehouses",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    material_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Material",
        table: "materials",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    material_lot_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "MaterialLot",
        table: "material_lots",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    stock_transaction_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "StockTransaction",
        table: "stock_transactions",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    movement_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "MovementType",
      enumValues: ["in", "out"] as const,
      label: {
        ja: "変動タイプ",
        en: "Movement Type",
        vi: "Loại biến động",
      },
      placeholder: {
        ja: "変動タイプを入力",
        en: "Enter Movement Type",
        vi: "Nhập Loại biến động",
      },
      description: {
        ja: "in = 在庫増加、out = 在庫減少",
        en: "in = increase stock, out = decrease stock",
        vi: "in = tăng tồn, out = giảm tồn",
      },
    },
    quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "数量",
        en: "Quantity",
        vi: "Số lượng",
      },
      placeholder: {
        ja: "数量を入力",
        en: "Enter Quantity",
        vi: "Nhập Số lượng",
      },
      description: {
        ja: "基本単位での変動数量（常に正の値）",
        en: "Amount changed in base unit (always positive)",
        vi: "Số lượng thay đổi (đơn vị cơ bản, luôn dương)",
      },
    },
    quantity_before: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "変動前数量",
        en: "Quantity Before",
        vi: "Tồn trước",
      },
      placeholder: {
        ja: "変動前数量を入力",
        en: "Enter Quantity Before",
        vi: "Nhập Tồn trước",
      },
      description: {
        ja: "この変動前の在庫数量",
        en: "Stock quantity before this movement",
        vi: "Số lượng tồn kho trước biến động",
      },
    },
    quantity_after: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "変動後数量",
        en: "Quantity After",
        vi: "Tồn sau",
      },
      placeholder: {
        ja: "変動後数量を入力",
        en: "Enter Quantity After",
        vi: "Nhập Tồn sau",
      },
      description: {
        ja: "この変動後の在庫数量",
        en: "Stock quantity after this movement",
        vi: "Số lượng tồn kho sau biến động",
      },
    },
    unit: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "単位",
        en: "Unit",
        vi: "Đơn vị",
      },
      placeholder: {
        ja: "単位を入力",
        en: "Enter Unit",
        vi: "Nhập Đơn vị",
      },
      description: {
        ja: "基本計量単位",
        en: "Base unit of measurement",
        vi: "Đơn vị đo lường cơ bản",
      },
    },
    created_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "作成日時",
        en: "Created At",
        vi: "Ngày tạo",
      },
      placeholder: {
        ja: "作成日時を入力",
        en: "Enter Created At",
        vi: "Nhập Ngày tạo",
      },
      description: {
        ja: "不変のタイムスタンプ。設計上updated_atやdeleted_atなし（BR-05）",
        en: "Immutable timestamp. No updated_at or deleted_at by design (BR-05)",
        vi: "Timestamp bất biến. Không có updated_at, deleted_at theo thiết kế (BR-05)",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["warehouse_id", "stock_transaction_id", "movement_type", "quantity", "quantity_before", "quantity_after", "created_at"] as const,
  enumFields: ["movement_type"] as const,
  relationFields: ["warehouse_id", "product_sku_id", "material_id", "material_lot_id", "stock_transaction_id"] as const,
} as const;

export type StockMovementFieldName = keyof typeof stockMovementMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a StockMovement. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface StockMovementCreateFormState {
  warehouse_id: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  material_lot_id?: string | null;
  stock_transaction_id: string;
  movement_type: MovementType;
  quantity: number;
  quantity_before: number;
  quantity_after: number;
  unit?: string | null;
  created_at: string;
}

/** Form-state shape for updating a StockMovement. Every field optional. */
export interface StockMovementUpdateFormState {
  warehouse_id?: string;
  product_sku_id?: string | null;
  material_id?: string | null;
  material_lot_id?: string | null;
  stock_transaction_id?: string;
  movement_type?: MovementType;
  quantity?: number;
  quantity_before?: number;
  quantity_after?: number;
  unit?: string | null;
  created_at?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyStockMovementCreateForm(): StockMovementCreateFormState {
  return {
    warehouse_id: '',
    product_sku_id: '',
    material_id: '',
    material_lot_id: '',
    stock_transaction_id: '',
    movement_type: MovementType.In,
    quantity: 0,
    quantity_before: 0,
    quantity_after: 0,
    unit: '',
    created_at: '',
  };
}

/** Convert create form state into the payload that baseStockMovementCreateSchema accepts. */
export function buildStockMovementCreatePayload(form: StockMovementCreateFormState): BaseStockMovementCreate {
  const payload: Record<string, unknown> = {
    warehouse_id: form.warehouse_id.trim(),
    product_sku_id: form.product_sku_id?.toString().trim() || null,
    material_id: form.material_id?.toString().trim() || null,
    material_lot_id: form.material_lot_id?.toString().trim() || null,
    stock_transaction_id: form.stock_transaction_id.trim(),
    movement_type: form.movement_type,
    quantity: form.quantity,
    quantity_before: form.quantity_before,
    quantity_after: form.quantity_after,
    unit: form.unit?.toString().trim() || null,
    created_at: form.created_at,
  };

  return payload as BaseStockMovementCreate;
}

/** Convert update form state into the payload that baseStockMovementUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildStockMovementUpdatePayload(form: StockMovementUpdateFormState): BaseStockMovementUpdate {
  const payload: Record<string, unknown> = {};
  if (form.warehouse_id !== undefined) {
    payload.warehouse_id = form.warehouse_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.material_id !== undefined) {
    payload.material_id = form.material_id?.toString().trim() ?? null;
  }
  if (form.material_lot_id !== undefined) {
    payload.material_lot_id = form.material_lot_id?.toString().trim() ?? null;
  }
  if (form.stock_transaction_id !== undefined) {
    payload.stock_transaction_id = form.stock_transaction_id?.toString().trim() ?? null;
  }
  if (form.movement_type !== undefined) {
    payload.movement_type = form.movement_type;
  }
  if (form.quantity !== undefined) {
    payload.quantity = form.quantity;
  }
  if (form.quantity_before !== undefined) {
    payload.quantity_before = form.quantity_before;
  }
  if (form.quantity_after !== undefined) {
    payload.quantity_after = form.quantity_after;
  }
  if (form.unit !== undefined) {
    payload.unit = form.unit?.toString().trim() ?? null;
  }
  if (form.created_at !== undefined) {
    payload.created_at = form.created_at;
  }
  return payload as BaseStockMovementUpdate;
}
