/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString, DateString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { FloatingSectionProduct } from './FloatingSectionProduct';
import type { FloatingSectionSchedule } from './FloatingSectionSchedule';
import type { Organization } from './Organization';

/**
 * フローティングセクション
 */
export interface FloatingSection {
  id: string;
  name: string;
  is_active: boolean;
  priority: number;
  start_date?: DateString | null;
  end_date?: DateString | null;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  branch_id?: string | null;
  branch?: Branch | null;
  masterSection_id?: string | null;
  masterSection?: FloatingSection | null;
  clonedSections?: FloatingSection[];
  schedules?: FloatingSectionSchedule[];
  products?: FloatingSectionProduct[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for FloatingSection
 */
export const floatingSectionI18n = {
  label: {"ja":"フローティングセクション","en":"Floating Section","vi":"Khung giờ ưu đãi"},
  fields: {
    name: {
      label: {"ja":"名前","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名前を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    priority: {
      label: {"ja":"優先度","en":"Priority","vi":"Độ ưu tiên"},
      placeholder: {"ja":"優先度を入力","en":"Enter Priority","vi":"Nhập Độ ưu tiên"},
    },
    start_date: {
      label: {"ja":"開始日","en":"Start Date","vi":"Ngày bắt đầu"},
      placeholder: {"ja":"開始日を入力","en":"Enter Start Date","vi":"Nhập Ngày bắt đầu"},
    },
    end_date: {
      label: {"ja":"終了日","en":"End Date","vi":"Ngày kết thúc"},
      placeholder: {"ja":"終了日を入力","en":"Enter End Date","vi":"Nhập Ngày kết thúc"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    master_section: {
      label: {"ja":"マスターセクション","en":"Master Section","vi":"Khung giờ gốc (HQ)"},
      placeholder: {"ja":"マスターセクションを入力","en":"Enter Master Section","vi":"Nhập Khung giờ gốc (HQ)"},
    },
    cloned_sections: {
      label: {"ja":"クローンされたセクション","en":"Cloned Sections","vi":"Các bản sao đã clone"},
      placeholder: {"ja":"クローンされたセクションを入力","en":"Enter Cloned Sections","vi":"Nhập Các bản sao đã clone"},
    },
    schedules: {
      label: {"ja":"スケジュール","en":"Schedules","vi":"Lịch trình"},
      placeholder: {"ja":"スケジュールを入力","en":"Enter Schedules","vi":"Nhập Lịch trình"},
    },
    products: {
      label: {"ja":"商品","en":"Products","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Products","vi":"Nhập Sản phẩm"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for FloatingSection */
export const baseFloatingSectionSchemas = {
  name: z.string().min(1).max(255),
  is_active: z.boolean(),
  priority: z.number().int(),
  start_date: z.string().date().optional().nullable(),
  end_date: z.string().date().optional().nullable(),
} as const;

/** Create schema for FloatingSection */
export const baseFloatingSectionCreateSchema = z.object({
  name: baseFloatingSectionSchemas.name,
  is_active: baseFloatingSectionSchemas.is_active,
  priority: baseFloatingSectionSchemas.priority,
  start_date: baseFloatingSectionSchemas.start_date,
  end_date: baseFloatingSectionSchemas.end_date,
});

/** Update schema for FloatingSection */
export const baseFloatingSectionUpdateSchema = baseFloatingSectionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseFloatingSectionCreate = z.infer<typeof baseFloatingSectionCreateSchema>;
export type BaseFloatingSectionUpdate = z.infer<typeof baseFloatingSectionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getFloatingSectionLabel(locale: string): string {
  return floatingSectionI18n.label[locale as keyof typeof floatingSectionI18n.label] ?? floatingSectionI18n.label['en'] ?? 'FloatingSection';
}

/** Get field label for a specific locale */
export function getFloatingSectionFieldLabel(field: string, locale: string): string {
  const fields = floatingSectionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getFloatingSectionFieldPlaceholder(field: string, locale: string): string {
  const fields = floatingSectionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for FloatingSection.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const floatingSectionMetadata = {
  modelName: "FloatingSection",
  table: "floating_sections",

  fields: {
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "名前",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名前を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
      description: {
        ja: "フローティングセクションの表示名（例：「ハッピーアワー 17:00-19:00」）",
        en: "Display name for this floating section (e.g. \"Happy Hour 17:00-19:00\")",
        vi: "Tên hiển thị của khung giờ ưu đãi (vd \"Happy Hour 17:00-19:00\")",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
      description: {
        ja: "シンプルなオン/オフの切り替え。Menuのような承認ワークフローはありません。",
        en: "Simple on/off toggle — no approval workflow like Menu.",
        vi: "Bật/tắt đơn giản — không có quy trình duyệt như Menu.",
      },
    },
    priority: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "優先度",
        en: "Priority",
        vi: "Độ ưu tiên",
      },
      placeholder: {
        ja: "優先度を入力",
        en: "Enter Priority",
        vi: "Nhập Độ ưu tiên",
      },
      description: {
        ja: "複数のフローティングセクションが同時に有効な場合の表示順。",
        en: "Tiebreak ordering when multiple floating sections are active at once.",
        vi: "Thứ tự ưu tiên khi nhiều khung giờ ưu đãi cùng hoạt động.",
      },
    },
    start_date: {
      type: "date",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "開始日",
        en: "Start Date",
        vi: "Ngày bắt đầu",
      },
      placeholder: {
        ja: "開始日を入力",
        en: "Enter Start Date",
        vi: "Nhập Ngày bắt đầu",
      },
      description: {
        ja: "オプションのカレンダー開始日（季節限定プロモなど）。NULL = 下限なし。",
        en: "Optional calendar-date lower bound (e.g. a seasonal promo). NULL = no lower bound.",
        vi: "Ngày bắt đầu áp dụng (vd chương trình theo mùa). NULL = không giới hạn.",
      },
    },
    end_date: {
      type: "date",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "終了日",
        en: "End Date",
        vi: "Ngày kết thúc",
      },
      placeholder: {
        ja: "終了日を入力",
        en: "Enter End Date",
        vi: "Nhập Ngày kết thúc",
      },
      description: {
        ja: "オプションのカレンダー終了日。NULL = 上限なし。",
        en: "Optional calendar-date upper bound. NULL = no upper bound.",
        vi: "Ngày kết thúc áp dụng. NULL = không giới hạn.",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    master_section_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "FloatingSection",
        table: "floating_sections",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["name", "is_active", "priority", "organization_id", "brand_id"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "branch_id", "master_section_id"] as const,
} as const;

export type FloatingSectionFieldName = keyof typeof floatingSectionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a FloatingSection. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface FloatingSectionCreateFormState {
  name: string;
  is_active: boolean;
  priority: number;
  start_date?: string | null;
  end_date?: string | null;
  organization_id: string;
  brand_id: string;
  branch_id?: string | null;
  master_section_id?: string | null;
}

/** Form-state shape for updating a FloatingSection. Every field optional. */
export interface FloatingSectionUpdateFormState {
  name?: string;
  is_active?: boolean;
  priority?: number;
  start_date?: string | null;
  end_date?: string | null;
  organization_id?: string;
  brand_id?: string;
  branch_id?: string | null;
  master_section_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyFloatingSectionCreateForm(): FloatingSectionCreateFormState {
  return {
    name: '',
    is_active: true,
    priority: 0,
    start_date: '',
    end_date: '',
    organization_id: '',
    brand_id: '',
    branch_id: '',
    master_section_id: '',
  };
}

/** Convert create form state into the payload that baseFloatingSectionCreateSchema accepts. */
export function buildFloatingSectionCreatePayload(form: FloatingSectionCreateFormState): BaseFloatingSectionCreate {
  const payload: Record<string, unknown> = {
    name: form.name.trim(),
    is_active: form.is_active,
    priority: form.priority,
    start_date: form.start_date,
    end_date: form.end_date,
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    branch_id: form.branch_id?.toString().trim() || null,
    master_section_id: form.master_section_id?.toString().trim() || null,
  };

  return payload as BaseFloatingSectionCreate;
}

/** Convert update form state into the payload that baseFloatingSectionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildFloatingSectionUpdatePayload(form: FloatingSectionUpdateFormState): BaseFloatingSectionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.priority !== undefined) {
    payload.priority = form.priority;
  }
  if (form.start_date !== undefined) {
    payload.start_date = form.start_date;
  }
  if (form.end_date !== undefined) {
    payload.end_date = form.end_date;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.master_section_id !== undefined) {
    payload.master_section_id = form.master_section_id?.toString().trim() ?? null;
  }
  return payload as BaseFloatingSectionUpdate;
}
