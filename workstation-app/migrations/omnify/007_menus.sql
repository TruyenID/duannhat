-- Code generated by omnify. DO NOT EDIT.

CREATE TABLE IF NOT EXISTS menus (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT,
    valid_from TEXT,
    valid_to TEXT,
    priority INTEGER NOT NULL DEFAULT 10,
    status TEXT NOT NULL DEFAULT 'Draft',
    rejection_reason TEXT,
    created_by_id TEXT,
    approved_by_id TEXT,
    approved_at TEXT,
    rejected_by_id TEXT,
    rejected_at TEXT,
    is_master INTEGER NOT NULL DEFAULT 0,
    last_synced_at TEXT,
    master_menu_id TEXT,
    organization_id TEXT NOT NULL,
    brand_id TEXT NOT NULL,
    branch_id TEXT,
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at TEXT NOT NULL DEFAULT (datetime('now')),
    deleted_at TEXT
);

CREATE INDEX IF NOT EXISTS idx_menus_master_menu_id ON menus(master_menu_id);

CREATE INDEX IF NOT EXISTS idx_menus_organization_id ON menus(organization_id);

CREATE INDEX IF NOT EXISTS idx_menus_brand_id ON menus(brand_id);

CREATE INDEX IF NOT EXISTS idx_menus_branch_id ON menus(branch_id);
