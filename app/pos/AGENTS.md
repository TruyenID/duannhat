# POS shell agents

Read `CLAUDE.md` in this directory and the Expo SDK 57 docs at
https://docs.expo.dev/versions/v57.0.0/ before changing native code.

This app is a **WebView shell only**. Do not reimplement POS UI here — change
`web/pos` instead.
