/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 承認ステータス
 */
export enum ApprovalStatus {
  Draft = 'draft',
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
}

/** All ApprovalStatus values */
export const ApprovalStatusValues = Object.values(ApprovalStatus) as ApprovalStatus[];

/** Type guard for ApprovalStatus */
export function isApprovalStatus(value: unknown): value is ApprovalStatus {
  return ApprovalStatusValues.includes(value as ApprovalStatus);
}

const approvalStatusLabels: Partial<Record<ApprovalStatus, Record<string, string>>> = {
  [ApprovalStatus.Draft]: { 'en': 'Draft', 'ja': '下書き', 'vi': 'Bản nháp' },
  [ApprovalStatus.Pending]: { 'en': 'Pending Approval', 'ja': '承認待ち', 'vi': 'Chờ duyệt' },
  [ApprovalStatus.Approved]: { 'en': 'Approved', 'ja': '承認済み', 'vi': 'Đã duyệt' },
  [ApprovalStatus.Rejected]: { 'en': 'Rejected', 'ja': '却下', 'vi': 'Từ chối' },
};

/** Get label for ApprovalStatus value with locale support */
export function getApprovalStatusLabel(value: ApprovalStatus, locale?: string): string {
  const labels = approvalStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for ApprovalStatus value */
export function getApprovalStatusExtra(_value: ApprovalStatus): Record<string, unknown> | undefined {
  return undefined;
}
