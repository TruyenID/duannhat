/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * ポイント履歴種別
 */
export enum PointEntryKind {
  Earn = 'earn',
  Redeem = 'redeem',
  Revoke = 'revoke',
  Adjust = 'adjust',
  Expire = 'expire',
}

/** All PointEntryKind values */
export const PointEntryKindValues = Object.values(PointEntryKind) as PointEntryKind[];

/** Type guard for PointEntryKind */
export function isPointEntryKind(value: unknown): value is PointEntryKind {
  return PointEntryKindValues.includes(value as PointEntryKind);
}

const pointEntryKindLabels: Partial<Record<PointEntryKind, Record<string, string>>> = {
  [PointEntryKind.Earn]: { 'en': 'Earned', 'ja': '獲得', 'vi': 'Tích điểm' },
  [PointEntryKind.Redeem]: { 'en': 'Redeemed', 'ja': '交換', 'vi': 'Đổi điểm' },
  [PointEntryKind.Revoke]: { 'en': 'Revoked', 'ja': '取消', 'vi': 'Thu hồi' },
  [PointEntryKind.Adjust]: { 'en': 'Adjusted', 'ja': '調整', 'vi': 'Điều chỉnh' },
  [PointEntryKind.Expire]: { 'en': 'Expired', 'ja': '失効', 'vi': 'Hết hạn' },
};

/** Get label for PointEntryKind value with locale support */
export function getPointEntryKindLabel(value: PointEntryKind, locale?: string): string {
  const labels = pointEntryKindLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for PointEntryKind value */
export function getPointEntryKindExtra(_value: PointEntryKind): Record<string, unknown> | undefined {
  return undefined;
}
