/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * リコールステータス
 */
export enum RecallStatus {
  Draft = 'draft',
  Active = 'active',
  Completed = 'completed',
  Cancelled = 'cancelled',
}

/** All RecallStatus values */
export const RecallStatusValues = Object.values(RecallStatus) as RecallStatus[];

/** Type guard for RecallStatus */
export function isRecallStatus(value: unknown): value is RecallStatus {
  return RecallStatusValues.includes(value as RecallStatus);
}

const recallStatusLabels: Partial<Record<RecallStatus, Record<string, string>>> = {
  [RecallStatus.Draft]: { 'en': 'Draft', 'ja': '下書き', 'vi': 'Nháp' },
  [RecallStatus.Active]: { 'en': 'Active', 'ja': '実行中', 'vi': 'Đang thực hiện' },
  [RecallStatus.Completed]: { 'en': 'Completed', 'ja': '完了', 'vi': 'Hoàn thành' },
  [RecallStatus.Cancelled]: { 'en': 'Cancelled', 'ja': 'キャンセル', 'vi': 'Đã hủy' },
};

/** Get label for RecallStatus value with locale support */
export function getRecallStatusLabel(value: RecallStatus, locale?: string): string {
  const labels = recallStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for RecallStatus value */
export function getRecallStatusExtra(_value: RecallStatus): Record<string, unknown> | undefined {
  return undefined;
}
