/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 価格戦略
 */
export enum ToppingGroupPriceStrategy {
  Flat = 'flat',
  FreeUpToN = 'free_up_to_n',
}

/** All ToppingGroupPriceStrategy values */
export const ToppingGroupPriceStrategyValues = Object.values(ToppingGroupPriceStrategy) as ToppingGroupPriceStrategy[];

/** Type guard for ToppingGroupPriceStrategy */
export function isToppingGroupPriceStrategy(value: unknown): value is ToppingGroupPriceStrategy {
  return ToppingGroupPriceStrategyValues.includes(value as ToppingGroupPriceStrategy);
}

const toppingGroupPriceStrategyLabels: Partial<Record<ToppingGroupPriceStrategy, Record<string, string>>> = {
  [ToppingGroupPriceStrategy.Flat]: { 'en': 'Flat (per item)', 'ja': '個別課金', 'vi': 'Tính riêng từng món' },
  [ToppingGroupPriceStrategy.FreeUpToN]: { 'en': 'N most expensive free', 'ja': '高い順にN個無料', 'vi': 'N món đắt nhất miễn phí' },
};

/** Get label for ToppingGroupPriceStrategy value with locale support */
export function getToppingGroupPriceStrategyLabel(value: ToppingGroupPriceStrategy, locale?: string): string {
  const labels = toppingGroupPriceStrategyLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for ToppingGroupPriceStrategy value */
export function getToppingGroupPriceStrategyExtra(_value: ToppingGroupPriceStrategy): Record<string, unknown> | undefined {
  return undefined;
}
