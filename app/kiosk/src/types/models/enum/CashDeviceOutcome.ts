/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 釣銭機取引結果
 */
export enum CashDeviceOutcome {
  Finish = 'finish',
  Cancel = 'cancel',
  Abort = 'abort',
  Timeout = 'timeout',
  Failure = 'failure',
}

/** All CashDeviceOutcome values */
export const CashDeviceOutcomeValues = Object.values(CashDeviceOutcome) as CashDeviceOutcome[];

/** Type guard for CashDeviceOutcome */
export function isCashDeviceOutcome(value: unknown): value is CashDeviceOutcome {
  return CashDeviceOutcomeValues.includes(value as CashDeviceOutcome);
}

const cashDeviceOutcomeLabels: Partial<Record<CashDeviceOutcome, Record<string, string>>> = {
  [CashDeviceOutcome.Finish]: { 'en': 'Finished', 'ja': '取引完了', 'vi': 'Hoàn tất' },
  [CashDeviceOutcome.Cancel]: { 'en': 'Cancelled', 'ja': '取引キャンセル', 'vi': 'Đã huỷ' },
  [CashDeviceOutcome.Abort]: { 'en': 'Aborted', 'ja': '取引強制終了', 'vi': 'Đứt giữa chừng' },
  [CashDeviceOutcome.Timeout]: { 'en': 'Timed Out', 'ja': '取引タイムアウト', 'vi': 'Quá thời gian' },
  [CashDeviceOutcome.Failure]: { 'en': 'Failed', 'ja': '取引エラー終了', 'vi': 'Lỗi máy' },
};

/** Get label for CashDeviceOutcome value with locale support */
export function getCashDeviceOutcomeLabel(value: CashDeviceOutcome, locale?: string): string {
  const labels = cashDeviceOutcomeLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for CashDeviceOutcome value */
export function getCashDeviceOutcomeExtra(_value: CashDeviceOutcome): Record<string, unknown> | undefined {
  return undefined;
}
