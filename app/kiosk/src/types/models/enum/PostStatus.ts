/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 投稿ステータス
 */
export enum PostStatus {
  Draft = 'Draft',
  Published = 'Published',
  Archived = 'Archived',
}

/** All PostStatus values */
export const PostStatusValues = Object.values(PostStatus) as PostStatus[];

/** Type guard for PostStatus */
export function isPostStatus(value: unknown): value is PostStatus {
  return PostStatusValues.includes(value as PostStatus);
}

const postStatusLabels: Partial<Record<PostStatus, Record<string, string>>> = {
  [PostStatus.Draft]: { 'en': 'Draft', 'ja': '下書き', 'vi': 'Bản nháp' },
  [PostStatus.Published]: { 'en': 'Published', 'ja': '公開', 'vi': 'Đã xuất bản' },
  [PostStatus.Archived]: { 'en': 'Archived', 'ja': 'アーカイブ', 'vi': 'Lưu trữ' },
};

/** Get label for PostStatus value with locale support */
export function getPostStatusLabel(value: PostStatus, locale?: string): string {
  const labels = postStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for PostStatus value */
export function getPostStatusExtra(_value: PostStatus): Record<string, unknown> | undefined {
  return undefined;
}
