/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 支払いステータス
 */
export enum PaymentStatus {
  Pending = 'pending',
  Succeeded = 'succeeded',
  Failed = 'failed',
  Refunded = 'refunded',
}

/** All PaymentStatus values */
export const PaymentStatusValues = Object.values(PaymentStatus) as PaymentStatus[];

/** Type guard for PaymentStatus */
export function isPaymentStatus(value: unknown): value is PaymentStatus {
  return PaymentStatusValues.includes(value as PaymentStatus);
}

const paymentStatusLabels: Partial<Record<PaymentStatus, Record<string, string>>> = {
  [PaymentStatus.Pending]: { 'en': 'Pending', 'ja': '確認待ち', 'vi': 'Chờ xác nhận' },
  [PaymentStatus.Succeeded]: { 'en': 'Succeeded', 'ja': '成功', 'vi': 'Thành công' },
  [PaymentStatus.Failed]: { 'en': 'Failed', 'ja': '失敗', 'vi': 'Thất bại' },
  [PaymentStatus.Refunded]: { 'en': 'Refunded', 'ja': '返金済み', 'vi': 'Đã hoàn tiền' },
};

/** Get label for PaymentStatus value with locale support */
export function getPaymentStatusLabel(value: PaymentStatus, locale?: string): string {
  const labels = paymentStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for PaymentStatus value */
export function getPaymentStatusExtra(_value: PaymentStatus): Record<string, unknown> | undefined {
  return undefined;
}
