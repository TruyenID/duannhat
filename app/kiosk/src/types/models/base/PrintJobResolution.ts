/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Organization } from './Organization';
import type { PrintJob } from './PrintJob';
import type { User } from './User';

/**
 * PrintJobResolution
 */
export interface PrintJobResolution {
  id: string;
  print_job_id: string;
  printJob?: PrintJob;
  organization_id: string;
  organization?: Organization;
  branch_id: string;
  branch?: Branch;
  resolution: string;
  reason: string;
  resolved_by_id?: string | null;
  resolvedBy?: User | null;
  resolved_at: DateTimeString;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PrintJobResolution
 */
export const printJobResolutionI18n = {
  label: {"ja":"PrintJobResolution","en":"PrintJobResolution","vi":"PrintJobResolution"},
  fields: {
    print_job: {
      label: {"ja":"印刷ジョブ","en":"Print Job","vi":"Lệnh in"},
      placeholder: {"ja":"印刷ジョブを入力","en":"Enter Print Job","vi":"Nhập Lệnh in"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branch: {
      label: {"ja":"支店","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"支店を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    resolution: {
      label: {"ja":"処理","en":"Resolution","vi":"Cách xử lý"},
      placeholder: {"ja":"処理を入力","en":"Enter Resolution","vi":"Nhập Cách xử lý"},
    },
    reason: {
      label: {"ja":"理由","en":"Reason","vi":"Lý do"},
      placeholder: {"ja":"理由を入力","en":"Enter Reason","vi":"Nhập Lý do"},
    },
    resolved_by: {
      label: {"ja":"処理者","en":"Resolved By","vi":"Người xử lý"},
      placeholder: {"ja":"処理者を入力","en":"Enter Resolved By","vi":"Nhập Người xử lý"},
    },
    resolved_at: {
      label: {"ja":"処理日時","en":"Resolved At","vi":"Xử lý lúc"},
      placeholder: {"ja":"処理日時を入力","en":"Enter Resolved At","vi":"Nhập Xử lý lúc"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PrintJobResolution */
export const basePrintJobResolutionSchemas = {
  resolution: z.string().min(1).max(32),
  reason: z.string().min(1).max(255),
  resolved_at: z.string().datetime({ offset: true }),
} as const;

/** Create schema for PrintJobResolution */
export const basePrintJobResolutionCreateSchema = z.object({
  resolution: basePrintJobResolutionSchemas.resolution,
  reason: basePrintJobResolutionSchemas.reason,
  resolved_at: basePrintJobResolutionSchemas.resolved_at,
});

/** Update schema for PrintJobResolution */
export const basePrintJobResolutionUpdateSchema = basePrintJobResolutionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePrintJobResolutionCreate = z.infer<typeof basePrintJobResolutionCreateSchema>;
export type BasePrintJobResolutionUpdate = z.infer<typeof basePrintJobResolutionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPrintJobResolutionLabel(locale: string): string {
  return printJobResolutionI18n.label[locale as keyof typeof printJobResolutionI18n.label] ?? printJobResolutionI18n.label['en'] ?? 'PrintJobResolution';
}

/** Get field label for a specific locale */
export function getPrintJobResolutionFieldLabel(field: string, locale: string): string {
  const fields = printJobResolutionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPrintJobResolutionFieldPlaceholder(field: string, locale: string): string {
  const fields = printJobResolutionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PrintJobResolution.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const printJobResolutionMetadata = {
  modelName: "PrintJobResolution",
  table: "print_job_resolutions",

  fields: {
    print_job_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "PrintJob",
        table: "print_jobs",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    resolution: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "処理",
        en: "Resolution",
        vi: "Cách xử lý",
      },
      placeholder: {
        ja: "処理を入力",
        en: "Enter Resolution",
        vi: "Nhập Cách xử lý",
      },
    },
    reason: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "理由",
        en: "Reason",
        vi: "Lý do",
      },
      placeholder: {
        ja: "理由を入力",
        en: "Enter Reason",
        vi: "Nhập Lý do",
      },
    },
    resolved_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
    resolved_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "処理日時",
        en: "Resolved At",
        vi: "Xử lý lúc",
      },
      placeholder: {
        ja: "処理日時を入力",
        en: "Enter Resolved At",
        vi: "Nhập Xử lý lúc",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["print_job_id", "organization_id", "branch_id", "resolution", "reason", "resolved_at"] as const,
  enumFields: [] as const,
  relationFields: ["print_job_id", "organization_id", "branch_id", "resolved_by_id"] as const,
} as const;

export type PrintJobResolutionFieldName = keyof typeof printJobResolutionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PrintJobResolution. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PrintJobResolutionCreateFormState {
  print_job_id: string;
  organization_id: string;
  branch_id: string;
  resolution: string;
  reason: string;
  resolved_by_id?: string | null;
  resolved_at: string;
}

/** Form-state shape for updating a PrintJobResolution. Every field optional. */
export interface PrintJobResolutionUpdateFormState {
  print_job_id?: string;
  organization_id?: string;
  branch_id?: string;
  resolution?: string;
  reason?: string;
  resolved_by_id?: string | null;
  resolved_at?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPrintJobResolutionCreateForm(): PrintJobResolutionCreateFormState {
  return {
    print_job_id: '',
    organization_id: '',
    branch_id: '',
    resolution: '',
    reason: '',
    resolved_by_id: '',
    resolved_at: '',
  };
}

/** Convert create form state into the payload that basePrintJobResolutionCreateSchema accepts. */
export function buildPrintJobResolutionCreatePayload(form: PrintJobResolutionCreateFormState): BasePrintJobResolutionCreate {
  const payload: Record<string, unknown> = {
    print_job_id: form.print_job_id.trim(),
    organization_id: form.organization_id.trim(),
    branch_id: form.branch_id.trim(),
    resolution: form.resolution.trim(),
    reason: form.reason.trim(),
    resolved_by_id: form.resolved_by_id?.toString().trim() || null,
    resolved_at: form.resolved_at,
  };

  return payload as BasePrintJobResolutionCreate;
}

/** Convert update form state into the payload that basePrintJobResolutionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPrintJobResolutionUpdatePayload(form: PrintJobResolutionUpdateFormState): BasePrintJobResolutionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.print_job_id !== undefined) {
    payload.print_job_id = form.print_job_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.resolution !== undefined) {
    payload.resolution = form.resolution?.toString().trim() ?? null;
  }
  if (form.reason !== undefined) {
    payload.reason = form.reason?.toString().trim() ?? null;
  }
  if (form.resolved_by_id !== undefined) {
    payload.resolved_by_id = form.resolved_by_id?.toString().trim() ?? null;
  }
  if (form.resolved_at !== undefined) {
    payload.resolved_at = form.resolved_at;
  }
  return payload as BasePrintJobResolutionUpdate;
}
