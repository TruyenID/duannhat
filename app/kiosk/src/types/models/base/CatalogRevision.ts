/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Organization } from './Organization';

/**
 * カタログ版
 */
export interface CatalogRevision {
  id: string;
  revision: number;
  snapshot_hash: string;
  snapshot: unknown;
  branch_id: string;
  branch?: Branch;
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for CatalogRevision
 */
export const catalogRevisionI18n = {
  label: {"ja":"カタログ版","en":"Catalog Revision","vi":"Phiên bản catalog"},
  fields: {
    revision: {
      label: {"ja":"版番号","en":"Revision","vi":"Số phiên bản"},
      placeholder: {"ja":"版番号を入力","en":"Enter Revision","vi":"Nhập Số phiên bản"},
    },
    snapshot_hash: {
      label: {"ja":"スナップショットハッシュ","en":"Snapshot Hash","vi":"Hash ảnh chụp"},
      placeholder: {"ja":"スナップショットハッシュを入力","en":"Enter Snapshot Hash","vi":"Nhập Hash ảnh chụp"},
    },
    snapshot: {
      label: {"ja":"価格スナップショット","en":"Price Snapshot","vi":"Ảnh chụp giá"},
      placeholder: {"ja":"価格スナップショットを入力","en":"Enter Price Snapshot","vi":"Nhập Ảnh chụp giá"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for CatalogRevision */
export const baseCatalogRevisionSchemas = {
  revision: z.number().int(),
  snapshot_hash: z.string().min(1).max(64),
  snapshot: z.unknown(),
} as const;

/** Create schema for CatalogRevision */
export const baseCatalogRevisionCreateSchema = z.object({
  revision: baseCatalogRevisionSchemas.revision,
  snapshot_hash: baseCatalogRevisionSchemas.snapshot_hash,
  snapshot: baseCatalogRevisionSchemas.snapshot,
});

/** Update schema for CatalogRevision */
export const baseCatalogRevisionUpdateSchema = baseCatalogRevisionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseCatalogRevisionCreate = z.infer<typeof baseCatalogRevisionCreateSchema>;
export type BaseCatalogRevisionUpdate = z.infer<typeof baseCatalogRevisionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getCatalogRevisionLabel(locale: string): string {
  return catalogRevisionI18n.label[locale as keyof typeof catalogRevisionI18n.label] ?? catalogRevisionI18n.label['en'] ?? 'CatalogRevision';
}

/** Get field label for a specific locale */
export function getCatalogRevisionFieldLabel(field: string, locale: string): string {
  const fields = catalogRevisionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getCatalogRevisionFieldPlaceholder(field: string, locale: string): string {
  const fields = catalogRevisionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for CatalogRevision.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const catalogRevisionMetadata = {
  modelName: "CatalogRevision",
  table: "catalog_revisions",

  fields: {
    revision: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "版番号",
        en: "Revision",
        vi: "Số phiên bản",
      },
      placeholder: {
        ja: "版番号を入力",
        en: "Enter Revision",
        vi: "Nhập Số phiên bản",
      },
      description: {
        ja: "店舗ごとの単調増加カウンタ（1から、BR-CR01）。",
        en: "Per-branch monotonic counter starting at 1 (BR-CR01).",
        vi: "Bộ đếm tăng dần theo chi nhánh, bắt đầu từ 1 (BR-CR01).",
      },
    },
    snapshot_hash: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "スナップショットハッシュ",
        en: "Snapshot Hash",
        vi: "Hash ảnh chụp",
      },
      placeholder: {
        ja: "スナップショットハッシュを入力",
        en: "Enter Snapshot Hash",
        vi: "Nhập Hash ảnh chụp",
      },
      description: {
        ja: "価格マップのsha256。同一ハッシュなら新版を作らない（BR-CR02）。",
        en: "sha256 of the canonical price map. Equal hash = no new revision (BR-CR02).",
        vi: "sha256 của bảng giá canonical. Hash giống nhau = không tạo version mới (BR-CR02).",
      },
    },
    snapshot: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "価格スナップショット",
        en: "Price Snapshot",
        vi: "Ảnh chụp giá",
      },
      placeholder: {
        ja: "価格スナップショットを入力",
        en: "Enter Price Snapshot",
        vi: "Nhập Ảnh chụp giá",
      },
      description: {
        ja: "検証器が過去のオフライン注文を再計算するための価格・税マップ。",
        en: "Compact price/tax map the verifier re-prices historical offline orders against: menu_product_sku_id → [product_sku_id, price, tax_type_id].",
        vi: "Bảng giá/thuế gọn để verifier tính lại đơn offline lịch sử.",
      },
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["revision", "snapshot_hash", "snapshot", "branch_id", "organization_id"] as const,
  enumFields: [] as const,
  relationFields: ["branch_id", "organization_id"] as const,
} as const;

export type CatalogRevisionFieldName = keyof typeof catalogRevisionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a CatalogRevision. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface CatalogRevisionCreateFormState {
  revision: number;
  snapshot_hash: string;
  snapshot: unknown;
  branch_id: string;
  organization_id: string;
}

/** Form-state shape for updating a CatalogRevision. Every field optional. */
export interface CatalogRevisionUpdateFormState {
  revision?: number;
  snapshot_hash?: string;
  snapshot?: unknown;
  branch_id?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyCatalogRevisionCreateForm(): CatalogRevisionCreateFormState {
  return {
    revision: 0,
    snapshot_hash: '',
    snapshot: null,
    branch_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseCatalogRevisionCreateSchema accepts. */
export function buildCatalogRevisionCreatePayload(form: CatalogRevisionCreateFormState): BaseCatalogRevisionCreate {
  const payload: Record<string, unknown> = {
    revision: form.revision,
    snapshot_hash: form.snapshot_hash.trim(),
    snapshot: form.snapshot,
    branch_id: form.branch_id.trim(),
    organization_id: form.organization_id.trim(),
  };

  return payload as BaseCatalogRevisionCreate;
}

/** Convert update form state into the payload that baseCatalogRevisionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildCatalogRevisionUpdatePayload(form: CatalogRevisionUpdateFormState): BaseCatalogRevisionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.revision !== undefined) {
    payload.revision = form.revision;
  }
  if (form.snapshot_hash !== undefined) {
    payload.snapshot_hash = form.snapshot_hash?.toString().trim() ?? null;
  }
  if (form.snapshot !== undefined) {
    payload.snapshot = form.snapshot;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseCatalogRevisionUpdate;
}
