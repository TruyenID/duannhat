/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Notification } from './Notification';

/**
 * 通知受信者
 */
export interface NotificationRecipient {
  id: string;
  notification_id: string;
  notification?: Notification;
  recipient_type: string;
  recipient_id: string;
  seen_at?: DateTimeString | null;
  read_at?: DateTimeString | null;
  dismissed_at?: DateTimeString | null;
  resolved_via?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationRecipient
 */
export const notificationRecipientI18n = {
  label: {"ja":"通知受信者","en":"Notification Recipient","vi":"Người nhận thông báo"},
  fields: {
    notification: {
      label: {"ja":"通知","en":"Notification","vi":"Thông báo"},
      placeholder: {"ja":"通知を入力","en":"Enter Notification","vi":"Nhập Thông báo"},
    },
    recipient_type: {
      label: {"ja":"受信者タイプ","en":"Recipient Type","vi":"Loại người nhận"},
      placeholder: {"ja":"受信者タイプを入力","en":"Enter Recipient Type","vi":"Nhập Loại người nhận"},
    },
    recipient_id: {
      label: {"ja":"受信者ID","en":"Recipient ID","vi":"ID người nhận"},
      placeholder: {"ja":"受信者IDを入力","en":"Enter Recipient ID","vi":"Nhập ID người nhận"},
    },
    seen_at: {
      label: {"ja":"表示日時","en":"Seen At","vi":"Đã xem lúc"},
      placeholder: {"ja":"表示日時を入力","en":"Enter Seen At","vi":"Nhập Đã xem lúc"},
    },
    read_at: {
      label: {"ja":"既読日時","en":"Read At","vi":"Đã đọc lúc"},
      placeholder: {"ja":"既読日時を入力","en":"Enter Read At","vi":"Nhập Đã đọc lúc"},
    },
    dismissed_at: {
      label: {"ja":"消去日時","en":"Dismissed At","vi":"Đã bỏ qua lúc"},
      placeholder: {"ja":"消去日時を入力","en":"Enter Dismissed At","vi":"Nhập Đã bỏ qua lúc"},
    },
    resolved_via: {
      label: {"ja":"解決元","en":"Resolved Via","vi":"Nguồn resolve"},
      placeholder: {"ja":"解決元を入力","en":"Enter Resolved Via","vi":"Nhập Nguồn resolve"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationRecipient */
export const baseNotificationRecipientSchemas = {
  recipient_type: z.string().min(1).max(60),
  recipient_id: z.string(),
  seen_at: z.string().datetime({ offset: true }).optional().nullable(),
  read_at: z.string().datetime({ offset: true }).optional().nullable(),
  dismissed_at: z.string().datetime({ offset: true }).optional().nullable(),
  resolved_via: z.string().max(120).optional().nullable(),
} as const;

/** Create schema for NotificationRecipient */
export const baseNotificationRecipientCreateSchema = z.object({
  recipient_type: baseNotificationRecipientSchemas.recipient_type,
  recipient_id: baseNotificationRecipientSchemas.recipient_id,
  seen_at: baseNotificationRecipientSchemas.seen_at,
  read_at: baseNotificationRecipientSchemas.read_at,
  dismissed_at: baseNotificationRecipientSchemas.dismissed_at,
  resolved_via: baseNotificationRecipientSchemas.resolved_via,
});

/** Update schema for NotificationRecipient */
export const baseNotificationRecipientUpdateSchema = baseNotificationRecipientCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationRecipientCreate = z.infer<typeof baseNotificationRecipientCreateSchema>;
export type BaseNotificationRecipientUpdate = z.infer<typeof baseNotificationRecipientUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationRecipientLabel(locale: string): string {
  return notificationRecipientI18n.label[locale as keyof typeof notificationRecipientI18n.label] ?? notificationRecipientI18n.label['en'] ?? 'NotificationRecipient';
}

/** Get field label for a specific locale */
export function getNotificationRecipientFieldLabel(field: string, locale: string): string {
  const fields = notificationRecipientI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationRecipientFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationRecipientI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationRecipient.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationRecipientMetadata = {
  modelName: "NotificationRecipient",
  table: "notification_recipients",

  fields: {
    notification_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Notification",
        table: "notifications",
        onDelete: "CASCADE",
      },
      label: {},
    },
    recipient_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 60,
      },
      label: {
        ja: "受信者タイプ",
        en: "Recipient Type",
        vi: "Loại người nhận",
      },
      placeholder: {
        ja: "受信者タイプを入力",
        en: "Enter Recipient Type",
        vi: "Nhập Loại người nhận",
      },
      description: {
        ja: "morph エイリアス — `user` または `device`。null は不可；すべての受信者行は AppServiceProvider::boot の morph マップ経由で解決される必要がある。",
        en: "Morph alias — `user` or `device`. Never null; every recipient row must resolve through the morph map in AppServiceProvider::boot.",
        vi: "Morph alias — `user` hoặc `device`. Không bao giờ null; mọi row người nhận phải resolve qua morph map trong AppServiceProvider::boot.",
      },
    },
    recipient_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "受信者ID",
        en: "Recipient ID",
        vi: "ID người nhận",
      },
      placeholder: {
        ja: "受信者IDを入力",
        en: "Enter Recipient ID",
        vi: "Nhập ID người nhận",
      },
      description: {
        ja: "受信者レコードの主キー（User または Device の UUID）。null は不可。",
        en: "PK of the recipient record (User or Device UUID). Never null.",
        vi: "PK record người nhận (UUID của User hoặc Device). Không null.",
      },
    },
    seen_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "表示日時",
        en: "Seen At",
        vi: "Đã xem lúc",
      },
      placeholder: {
        ja: "表示日時を入力",
        en: "Enter Seen At",
        vi: "Nhập Đã xem lúc",
      },
      description: {
        ja: "通知がユーザーのビューポートに入った / 受信箱が開かれた日時。冪等 — 一度セットされると後続の seen 呼び出しで上書きされない。",
        en: "Timestamp when the notification entered the user's viewport / inbox was opened. Idempotent — once set, subsequent seen calls do not overwrite.",
        vi: "Thời điểm notification vào viewport / inbox được mở. Idempotent — set rồi thì call seen tiếp theo không ghi đè.",
      },
    },
    read_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "既読日時",
        en: "Read At",
        vi: "Đã đọc lúc",
      },
      placeholder: {
        ja: "既読日時を入力",
        en: "Enter Read At",
        vi: "Nhập Đã đọc lúc",
      },
      description: {
        ja: "ユーザーが明示的に操作した（行クリック / 詳細を開いた）日時。冪等。`read_at` のセットは `seen_at` も含意（コントローラが `seen_at` が null なら併せてセット）。",
        en: "Timestamp when the user explicitly interacted (clicked the row, opened detail). Idempotent. Setting `read_at` also implies `seen_at` (the controller sets both if `seen_at` is still null).",
        vi: "Thời điểm user tương tác rõ ràng (click row, mở detail). Idempotent. Set `read_at` cũng hàm ý `seen_at` (controller set cả 2 nếu `seen_at` còn null).",
      },
    },
    dismissed_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "消去日時",
        en: "Dismissed At",
        vi: "Đã bỏ qua lúc",
      },
      placeholder: {
        ja: "消去日時を入力",
        en: "Enter Dismissed At",
        vi: "Nhập Đã bỏ qua lúc",
      },
      description: {
        ja: "ユーザーが行をアーカイブ / クリアした日時。消去された行はデフォルトの `/me/notifications` 応答から消える。ソフトフラグ（ハード削除ではない）— リテンション剪定で最終的に削除される。",
        en: "Timestamp when the user archived/cleared the row. Dismissed rows disappear from default `/me/notifications` responses. Soft-flag (not a hard delete) — retention pruning eventually removes rows anyway.",
        vi: "Thời điểm user archive/clear row. Row đã dismiss biến mất khỏi response `/me/notifications` mặc định. Soft-flag (không hard delete) — retention pruning sẽ xoá sau.",
      },
    },
    resolved_via: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 120,
      },
      label: {
        ja: "解決元",
        en: "Resolved Via",
        vi: "Nguồn resolve",
      },
      placeholder: {
        ja: "解決元を入力",
        en: "Enter Resolved Via",
        vi: "Nhập Nguồn resolve",
      },
      description: {
        ja: "この受信者に到達した経路を示す `AudienceResolverService` のトレース文字列。例：`role:warehouse_manager:{warehouse_id}`、`user:direct`、`shop:{shop_id}:members`。明示的な受信者 Collection を渡してリゾルバを経由しないディスパッチ経路では null。",
        en: "Trace string from `AudienceResolverService` explaining how this recipient was reached. Examples: `role:warehouse_manager:{warehouse_id}`, `user:direct`, `shop:{shop_id}:members`. Null when the dispatch path passed an explicit recipient Collection without going through the resolver.",
        vi: "Chuỗi trace từ `AudienceResolverService` giải thích cách reach recipient này. VD: `role:warehouse_manager:{warehouse_id}`, `user:direct`, `shop:{shop_id}:members`. Null khi dispatch path pass recipient Collection trực tiếp mà không qua resolver.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["notification_id", "recipient_type", "recipient_id"] as const,
  enumFields: [] as const,
  relationFields: ["notification_id"] as const,
} as const;

export type NotificationRecipientFieldName = keyof typeof notificationRecipientMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationRecipient. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationRecipientCreateFormState {
  notification_id: string;
  recipient_type: string;
  recipient_id: string;
  seen_at?: string | null;
  read_at?: string | null;
  dismissed_at?: string | null;
  resolved_via?: string | null;
}

/** Form-state shape for updating a NotificationRecipient. Every field optional. */
export interface NotificationRecipientUpdateFormState {
  notification_id?: string;
  recipient_type?: string;
  recipient_id?: string;
  seen_at?: string | null;
  read_at?: string | null;
  dismissed_at?: string | null;
  resolved_via?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationRecipientCreateForm(): NotificationRecipientCreateFormState {
  return {
    notification_id: '',
    recipient_type: '',
    recipient_id: '',
    seen_at: '',
    read_at: '',
    dismissed_at: '',
    resolved_via: '',
  };
}

/** Convert create form state into the payload that baseNotificationRecipientCreateSchema accepts. */
export function buildNotificationRecipientCreatePayload(form: NotificationRecipientCreateFormState): BaseNotificationRecipientCreate {
  const payload: Record<string, unknown> = {
    notification_id: form.notification_id.trim(),
    recipient_type: form.recipient_type.trim(),
    recipient_id: form.recipient_id.trim(),
    seen_at: form.seen_at,
    read_at: form.read_at,
    dismissed_at: form.dismissed_at,
    resolved_via: form.resolved_via?.toString().trim() || null,
  };

  return payload as BaseNotificationRecipientCreate;
}

/** Convert update form state into the payload that baseNotificationRecipientUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationRecipientUpdatePayload(form: NotificationRecipientUpdateFormState): BaseNotificationRecipientUpdate {
  const payload: Record<string, unknown> = {};
  if (form.notification_id !== undefined) {
    payload.notification_id = form.notification_id?.toString().trim() ?? null;
  }
  if (form.recipient_type !== undefined) {
    payload.recipient_type = form.recipient_type?.toString().trim() ?? null;
  }
  if (form.recipient_id !== undefined) {
    payload.recipient_id = form.recipient_id?.toString().trim() ?? null;
  }
  if (form.seen_at !== undefined) {
    payload.seen_at = form.seen_at;
  }
  if (form.read_at !== undefined) {
    payload.read_at = form.read_at;
  }
  if (form.dismissed_at !== undefined) {
    payload.dismissed_at = form.dismissed_at;
  }
  if (form.resolved_via !== undefined) {
    payload.resolved_via = form.resolved_via?.toString().trim() ?? null;
  }
  return payload as BaseNotificationRecipientUpdate;
}
