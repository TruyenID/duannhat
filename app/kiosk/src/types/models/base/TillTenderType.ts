/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Organization } from './Organization';

/**
 * テンダー種別
 */
export interface TillTenderType {
  id: string;
  tender_key: string;
  name: string;
  category: string;
  parent_tender_key?: string | null;
  currency_code: string;
  payment_method_code?: string | null;
  is_expected_anchor: boolean;
  requires_terminal_total: boolean;
  sort_order: number;
  is_active: boolean;
  branch_id?: string | null;
  branch?: Branch | null;
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TillTenderType
 */
export const tillTenderTypeI18n = {
  label: {"ja":"テンダー種別","en":"Till Tender Type","vi":"Loại tender"},
  fields: {
    tender_key: {
      label: {"ja":"テンダーキー","en":"Tender Key","vi":"Mã tender"},
      placeholder: {"ja":"テンダーキーを入力","en":"Enter Tender Key","vi":"Nhập Mã tender"},
    },
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    category: {
      label: {"ja":"分類","en":"Category","vi":"Phân loại"},
      placeholder: {"ja":"分類を入力","en":"Enter Category","vi":"Nhập Phân loại"},
    },
    parent_tender_key: {
      label: {"ja":"親テンダーキー","en":"Parent Tender Key","vi":"Tender cha"},
      placeholder: {"ja":"親テンダーキーを入力","en":"Enter Parent Tender Key","vi":"Nhập Tender cha"},
    },
    currency_code: {
      label: {"ja":"通貨","en":"Currency","vi":"Tiền tệ"},
      placeholder: {"ja":"通貨を入力","en":"Enter Currency","vi":"Nhập Tiền tệ"},
    },
    payment_method_code: {
      label: {"ja":"支払方法コード","en":"Payment Method Code","vi":"Mã phương thức POS"},
      placeholder: {"ja":"支払方法コードを入力","en":"Enter Payment Method Code","vi":"Nhập Mã phương thức POS"},
    },
    is_expected_anchor: {
      label: {"ja":"期待値アンカー","en":"Expected Anchor","vi":"Là gốc expected"},
      placeholder: {"ja":"期待値アンカーを入力","en":"Enter Expected Anchor","vi":"Nhập Là gốc expected"},
    },
    requires_terminal_total: {
      label: {"ja":"端末合計必須","en":"Requires Terminal Total","vi":"Yêu cầu tổng máy"},
      placeholder: {"ja":"端末合計必須を入力","en":"Enter Requires Terminal Total","vi":"Nhập Yêu cầu tổng máy"},
    },
    sort_order: {
      label: {"ja":"表示順","en":"Sort Order","vi":"Thứ tự"},
      placeholder: {"ja":"表示順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TillTenderType */
export const baseTillTenderTypeSchemas = {
  tender_key: z.string().min(1).max(50),
  name: z.string().min(1).max(255),
  category: z.string().min(1).max(50),
  parent_tender_key: z.string().max(50).optional().nullable(),
  currency_code: z.string().min(1).max(3),
  payment_method_code: z.string().max(50).optional().nullable(),
  is_expected_anchor: z.boolean(),
  requires_terminal_total: z.boolean(),
  sort_order: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for TillTenderType */
export const baseTillTenderTypeCreateSchema = z.object({
  tender_key: baseTillTenderTypeSchemas.tender_key,
  name: baseTillTenderTypeSchemas.name,
  category: baseTillTenderTypeSchemas.category,
  parent_tender_key: baseTillTenderTypeSchemas.parent_tender_key,
  currency_code: baseTillTenderTypeSchemas.currency_code,
  payment_method_code: baseTillTenderTypeSchemas.payment_method_code,
  is_expected_anchor: baseTillTenderTypeSchemas.is_expected_anchor,
  requires_terminal_total: baseTillTenderTypeSchemas.requires_terminal_total,
  sort_order: baseTillTenderTypeSchemas.sort_order,
  is_active: baseTillTenderTypeSchemas.is_active,
  ja: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(255).optional().nullable(),
  }).optional(),
});

/** Update schema for TillTenderType */
export const baseTillTenderTypeUpdateSchema = baseTillTenderTypeCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTillTenderTypeCreate = z.infer<typeof baseTillTenderTypeCreateSchema>;
export type BaseTillTenderTypeUpdate = z.infer<typeof baseTillTenderTypeUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTillTenderTypeLabel(locale: string): string {
  return tillTenderTypeI18n.label[locale as keyof typeof tillTenderTypeI18n.label] ?? tillTenderTypeI18n.label['en'] ?? 'TillTenderType';
}

/** Get field label for a specific locale */
export function getTillTenderTypeFieldLabel(field: string, locale: string): string {
  const fields = tillTenderTypeI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTillTenderTypeFieldPlaceholder(field: string, locale: string): string {
  const fields = tillTenderTypeI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TillTenderType.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tillTenderTypeMetadata = {
  modelName: "TillTenderType",
  table: "till_tender_types",

  fields: {
    tender_key: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "テンダーキー",
        en: "Tender Key",
        vi: "Mã tender",
      },
      placeholder: {
        ja: "テンダーキーを入力",
        en: "Enter Tender Key",
        vi: "Nhập Mã tender",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    category: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "分類",
        en: "Category",
        vi: "Phân loại",
      },
      placeholder: {
        ja: "分類を入力",
        en: "Enter Category",
        vi: "Nhập Phân loại",
      },
    },
    parent_tender_key: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "親テンダーキー",
        en: "Parent Tender Key",
        vi: "Tender cha",
      },
      placeholder: {
        ja: "親テンダーキーを入力",
        en: "Enter Parent Tender Key",
        vi: "Nhập Tender cha",
      },
    },
    currency_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 3,
      },
      label: {
        ja: "通貨",
        en: "Currency",
        vi: "Tiền tệ",
      },
      placeholder: {
        ja: "通貨を入力",
        en: "Enter Currency",
        vi: "Nhập Tiền tệ",
      },
    },
    payment_method_code: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "支払方法コード",
        en: "Payment Method Code",
        vi: "Mã phương thức POS",
      },
      placeholder: {
        ja: "支払方法コードを入力",
        en: "Enter Payment Method Code",
        vi: "Nhập Mã phương thức POS",
      },
    },
    is_expected_anchor: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "期待値アンカー",
        en: "Expected Anchor",
        vi: "Là gốc expected",
      },
      placeholder: {
        ja: "期待値アンカーを入力",
        en: "Enter Expected Anchor",
        vi: "Nhập Là gốc expected",
      },
    },
    requires_terminal_total: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "端末合計必須",
        en: "Requires Terminal Total",
        vi: "Yêu cầu tổng máy",
      },
      placeholder: {
        ja: "端末合計必須を入力",
        en: "Enter Requires Terminal Total",
        vi: "Nhập Yêu cầu tổng máy",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Sort Order",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name"] as const,
  requiredFields: ["tender_key", "name", "category", "currency_code", "is_expected_anchor", "requires_terminal_total", "sort_order", "is_active", "organization_id"] as const,
  enumFields: [] as const,
  relationFields: ["branch_id", "organization_id"] as const,
} as const;

export type TillTenderTypeFieldName = keyof typeof tillTenderTypeMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TillTenderType. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TillTenderTypeCreateFormState {
  tender_key: string;
  name: Record<Locale, string>;
  category: string;
  parent_tender_key?: string | null;
  currency_code: string;
  payment_method_code?: string | null;
  is_expected_anchor: boolean;
  requires_terminal_total: boolean;
  sort_order: number;
  is_active: boolean;
  branch_id?: string | null;
  organization_id: string;
}

/** Form-state shape for updating a TillTenderType. Every field optional. */
export interface TillTenderTypeUpdateFormState {
  tender_key?: string;
  name?: Record<Locale, string>;
  category?: string;
  parent_tender_key?: string | null;
  currency_code?: string;
  payment_method_code?: string | null;
  is_expected_anchor?: boolean;
  requires_terminal_total?: boolean;
  sort_order?: number;
  is_active?: boolean;
  branch_id?: string | null;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTillTenderTypeCreateForm(): TillTenderTypeCreateFormState {
  return {
    tender_key: '',
    name: emptyLocaleMap(),
    category: '',
    parent_tender_key: '',
    currency_code: "JPY",
    payment_method_code: '',
    is_expected_anchor: false,
    requires_terminal_total: false,
    sort_order: 0,
    is_active: true,
    branch_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseTillTenderTypeCreateSchema accepts. */
export function buildTillTenderTypeCreatePayload(form: TillTenderTypeCreateFormState): BaseTillTenderTypeCreate {
  const payload: Record<string, unknown> = {
    tender_key: form.tender_key.trim(),
    name: (form.name["ja"] ?? '').trim(),
    category: form.category.trim(),
    parent_tender_key: form.parent_tender_key?.toString().trim() || null,
    currency_code: form.currency_code.trim(),
    payment_method_code: form.payment_method_code?.toString().trim() || null,
    is_expected_anchor: form.is_expected_anchor,
    requires_terminal_total: form.requires_terminal_total,
    sort_order: form.sort_order,
    is_active: form.is_active,
    branch_id: form.branch_id?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
  });
  Object.assign(payload, i18n);

  return payload as BaseTillTenderTypeCreate;
}

/** Convert update form state into the payload that baseTillTenderTypeUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTillTenderTypeUpdatePayload(form: TillTenderTypeUpdateFormState): BaseTillTenderTypeUpdate {
  const payload: Record<string, unknown> = {};
  if (form.tender_key !== undefined) {
    payload.tender_key = form.tender_key?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.category !== undefined) {
    payload.category = form.category?.toString().trim() ?? null;
  }
  if (form.parent_tender_key !== undefined) {
    payload.parent_tender_key = form.parent_tender_key?.toString().trim() ?? null;
  }
  if (form.currency_code !== undefined) {
    payload.currency_code = form.currency_code?.toString().trim() ?? null;
  }
  if (form.payment_method_code !== undefined) {
    payload.payment_method_code = form.payment_method_code?.toString().trim() ?? null;
  }
  if (form.is_expected_anchor !== undefined) {
    payload.is_expected_anchor = form.is_expected_anchor;
  }
  if (form.requires_terminal_total !== undefined) {
    payload.requires_terminal_total = form.requires_terminal_total;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseTillTenderTypeUpdate;
}
