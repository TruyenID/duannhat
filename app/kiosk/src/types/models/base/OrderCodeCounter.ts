/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * 注文コードカウンター
 */
export interface OrderCodeCounter {
  id: string;
  year: number;
  next_value: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for OrderCodeCounter
 */
export const orderCodeCounterI18n = {
  label: {"ja":"注文コードカウンター","en":"Order Code Counter","vi":"Bộ đếm mã đơn"},
  fields: {
    year: {
      label: {"ja":"年","en":"Year","vi":"Năm"},
      placeholder: {"ja":"年を入力","en":"Enter Year","vi":"Nhập Năm"},
    },
    next_value: {
      label: {"ja":"次の値","en":"Next Value","vi":"Giá trị kế tiếp"},
      placeholder: {"ja":"次の値を入力","en":"Enter Next Value","vi":"Nhập Giá trị kế tiếp"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for OrderCodeCounter */
export const baseOrderCodeCounterSchemas = {
  year: z.number().int(),
  next_value: z.number().int(),
} as const;

/** Create schema for OrderCodeCounter */
export const baseOrderCodeCounterCreateSchema = z.object({
  year: baseOrderCodeCounterSchemas.year,
  next_value: baseOrderCodeCounterSchemas.next_value,
});

/** Update schema for OrderCodeCounter */
export const baseOrderCodeCounterUpdateSchema = baseOrderCodeCounterCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseOrderCodeCounterCreate = z.infer<typeof baseOrderCodeCounterCreateSchema>;
export type BaseOrderCodeCounterUpdate = z.infer<typeof baseOrderCodeCounterUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getOrderCodeCounterLabel(locale: string): string {
  return orderCodeCounterI18n.label[locale as keyof typeof orderCodeCounterI18n.label] ?? orderCodeCounterI18n.label['en'] ?? 'OrderCodeCounter';
}

/** Get field label for a specific locale */
export function getOrderCodeCounterFieldLabel(field: string, locale: string): string {
  const fields = orderCodeCounterI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getOrderCodeCounterFieldPlaceholder(field: string, locale: string): string {
  const fields = orderCodeCounterI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for OrderCodeCounter.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const orderCodeCounterMetadata = {
  modelName: "OrderCodeCounter",
  table: "order_code_counters",

  fields: {
    year: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "年",
        en: "Year",
        vi: "Năm",
      },
      placeholder: {
        ja: "年を入力",
        en: "Enter Year",
        vi: "Nhập Năm",
      },
    },
    next_value: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "次の値",
        en: "Next Value",
        vi: "Giá trị kế tiếp",
      },
      placeholder: {
        ja: "次の値を入力",
        en: "Enter Next Value",
        vi: "Nhập Giá trị kế tiếp",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["year", "next_value"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type OrderCodeCounterFieldName = keyof typeof orderCodeCounterMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a OrderCodeCounter. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface OrderCodeCounterCreateFormState {
  year: number;
  next_value: number;
}

/** Form-state shape for updating a OrderCodeCounter. Every field optional. */
export interface OrderCodeCounterUpdateFormState {
  year?: number;
  next_value?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyOrderCodeCounterCreateForm(): OrderCodeCounterCreateFormState {
  return {
    year: 0,
    next_value: 1,
  };
}

/** Convert create form state into the payload that baseOrderCodeCounterCreateSchema accepts. */
export function buildOrderCodeCounterCreatePayload(form: OrderCodeCounterCreateFormState): BaseOrderCodeCounterCreate {
  const payload: Record<string, unknown> = {
    year: form.year,
    next_value: form.next_value,
  };

  return payload as BaseOrderCodeCounterCreate;
}

/** Convert update form state into the payload that baseOrderCodeCounterUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildOrderCodeCounterUpdatePayload(form: OrderCodeCounterUpdateFormState): BaseOrderCodeCounterUpdate {
  const payload: Record<string, unknown> = {};
  if (form.year !== undefined) {
    payload.year = form.year;
  }
  if (form.next_value !== undefined) {
    payload.next_value = form.next_value;
  }
  return payload as BaseOrderCodeCounterUpdate;
}
