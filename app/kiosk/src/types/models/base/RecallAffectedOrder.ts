/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Recall } from './Recall';

/**
 * リコール影響注文
 */
export interface RecallAffectedOrder {
  id: string;
  recall_id: string;
  recall?: Recall;
  customer_order_id: string;
  notification_channel?: string | null;
  notified_at?: DateTimeString | null;
  notification_id?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for RecallAffectedOrder
 */
export const recallAffectedOrderI18n = {
  label: {"ja":"リコール影響注文","en":"Recall Affected Order","vi":"Đơn ảnh hưởng thu hồi"},
  fields: {
    recall: {
      label: {"ja":"recall","en":"recall","vi":"recall"},
    },
    customer_order_id: {
      label: {"ja":"顧客注文ID","en":"Customer Order ID","vi":"ID đơn khách"},
      placeholder: {"ja":"顧客注文IDを入力","en":"Enter Customer Order ID","vi":"Nhập ID đơn khách"},
    },
    notification_channel: {
      label: {"ja":"通知チャネル","en":"Notification Channel","vi":"Kênh thông báo"},
      placeholder: {"ja":"通知チャネルを入力","en":"Enter Notification Channel","vi":"Nhập Kênh thông báo"},
    },
    notified_at: {
      label: {"ja":"通知日時","en":"Notified At","vi":"Thời điểm thông báo"},
      placeholder: {"ja":"通知日時を入力","en":"Enter Notified At","vi":"Nhập Thời điểm thông báo"},
    },
    notification_id: {
      label: {"ja":"通知ID","en":"Notification ID","vi":"ID thông báo"},
      placeholder: {"ja":"通知IDを入力","en":"Enter Notification ID","vi":"Nhập ID thông báo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for RecallAffectedOrder */
export const baseRecallAffectedOrderSchemas = {
  customer_order_id: z.string(),
  notification_channel: z.string().max(20).optional().nullable(),
  notified_at: z.string().datetime({ offset: true }).optional().nullable(),
  notification_id: z.string().optional().nullable(),
} as const;

/** Create schema for RecallAffectedOrder */
export const baseRecallAffectedOrderCreateSchema = z.object({
  customer_order_id: baseRecallAffectedOrderSchemas.customer_order_id,
  notification_channel: baseRecallAffectedOrderSchemas.notification_channel,
  notified_at: baseRecallAffectedOrderSchemas.notified_at,
  notification_id: baseRecallAffectedOrderSchemas.notification_id,
});

/** Update schema for RecallAffectedOrder */
export const baseRecallAffectedOrderUpdateSchema = baseRecallAffectedOrderCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseRecallAffectedOrderCreate = z.infer<typeof baseRecallAffectedOrderCreateSchema>;
export type BaseRecallAffectedOrderUpdate = z.infer<typeof baseRecallAffectedOrderUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getRecallAffectedOrderLabel(locale: string): string {
  return recallAffectedOrderI18n.label[locale as keyof typeof recallAffectedOrderI18n.label] ?? recallAffectedOrderI18n.label['en'] ?? 'RecallAffectedOrder';
}

/** Get field label for a specific locale */
export function getRecallAffectedOrderFieldLabel(field: string, locale: string): string {
  const fields = recallAffectedOrderI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getRecallAffectedOrderFieldPlaceholder(field: string, locale: string): string {
  const fields = recallAffectedOrderI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for RecallAffectedOrder.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const recallAffectedOrderMetadata = {
  modelName: "RecallAffectedOrder",
  table: "recall_affected_orders",

  fields: {
    recall_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Recall",
        table: "recalls",
        onDelete: "CASCADE",
      },
      label: {},
    },
    customer_order_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "顧客注文ID",
        en: "Customer Order ID",
        vi: "ID đơn khách",
      },
      placeholder: {
        ja: "顧客注文IDを入力",
        en: "Enter Customer Order ID",
        vi: "Nhập ID đơn khách",
      },
    },
    notification_channel: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "通知チャネル",
        en: "Notification Channel",
        vi: "Kênh thông báo",
      },
      placeholder: {
        ja: "通知チャネルを入力",
        en: "Enter Notification Channel",
        vi: "Nhập Kênh thông báo",
      },
    },
    notified_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "通知日時",
        en: "Notified At",
        vi: "Thời điểm thông báo",
      },
      placeholder: {
        ja: "通知日時を入力",
        en: "Enter Notified At",
        vi: "Nhập Thời điểm thông báo",
      },
    },
    notification_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "通知ID",
        en: "Notification ID",
        vi: "ID thông báo",
      },
      placeholder: {
        ja: "通知IDを入力",
        en: "Enter Notification ID",
        vi: "Nhập ID thông báo",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["recall_id", "customer_order_id"] as const,
  enumFields: [] as const,
  relationFields: ["recall_id"] as const,
} as const;

export type RecallAffectedOrderFieldName = keyof typeof recallAffectedOrderMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a RecallAffectedOrder. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface RecallAffectedOrderCreateFormState {
  recall_id: string;
  customer_order_id: string;
  notification_channel?: string | null;
  notified_at?: string | null;
  notification_id?: string | null;
}

/** Form-state shape for updating a RecallAffectedOrder. Every field optional. */
export interface RecallAffectedOrderUpdateFormState {
  recall_id?: string;
  customer_order_id?: string;
  notification_channel?: string | null;
  notified_at?: string | null;
  notification_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyRecallAffectedOrderCreateForm(): RecallAffectedOrderCreateFormState {
  return {
    recall_id: '',
    customer_order_id: '',
    notification_channel: '',
    notified_at: '',
    notification_id: '',
  };
}

/** Convert create form state into the payload that baseRecallAffectedOrderCreateSchema accepts. */
export function buildRecallAffectedOrderCreatePayload(form: RecallAffectedOrderCreateFormState): BaseRecallAffectedOrderCreate {
  const payload: Record<string, unknown> = {
    recall_id: form.recall_id.trim(),
    customer_order_id: form.customer_order_id.trim(),
    notification_channel: form.notification_channel?.toString().trim() || null,
    notified_at: form.notified_at,
    notification_id: form.notification_id?.toString().trim() || null,
  };

  return payload as BaseRecallAffectedOrderCreate;
}

/** Convert update form state into the payload that baseRecallAffectedOrderUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildRecallAffectedOrderUpdatePayload(form: RecallAffectedOrderUpdateFormState): BaseRecallAffectedOrderUpdate {
  const payload: Record<string, unknown> = {};
  if (form.recall_id !== undefined) {
    payload.recall_id = form.recall_id?.toString().trim() ?? null;
  }
  if (form.customer_order_id !== undefined) {
    payload.customer_order_id = form.customer_order_id?.toString().trim() ?? null;
  }
  if (form.notification_channel !== undefined) {
    payload.notification_channel = form.notification_channel?.toString().trim() ?? null;
  }
  if (form.notified_at !== undefined) {
    payload.notified_at = form.notified_at;
  }
  if (form.notification_id !== undefined) {
    payload.notification_id = form.notification_id?.toString().trim() ?? null;
  }
  return payload as BaseRecallAffectedOrderUpdate;
}
