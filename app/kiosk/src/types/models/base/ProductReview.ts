/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { ReviewSentiment } from '../enum/ReviewSentiment';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Customer } from './Customer';
import type { CustomerOrder } from './CustomerOrder';
import type { CustomerOrderItem } from './CustomerOrderItem';
import type { Organization } from './Organization';
import type { Product } from './Product';

/**
 * 商品レビュー
 */
export interface ProductReview {
  id: string;
  product_id: string;
  product?: Product;
  customer_order_id: string;
  customerOrder?: CustomerOrder;
  customer_order_item_id: string;
  customerOrderItem?: CustomerOrderItem;
  customer_id?: string | null;
  customer?: Customer | null;
  sentiment?: ReviewSentiment | null;
  rating?: number | null;
  tags?: unknown | null;
  comment?: string | null;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  branch_id: string;
  branch?: Branch;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ProductReview
 */
export const productReviewI18n = {
  label: {"ja":"商品レビュー","en":"Product Review","vi":"Đánh giá sản phẩm"},
  fields: {
    product: {
      label: {"ja":"商品","en":"Product","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Product","vi":"Nhập Sản phẩm"},
    },
    customer_order: {
      label: {"ja":"注文","en":"Order","vi":"Đơn hàng"},
      placeholder: {"ja":"注文を入力","en":"Enter Order","vi":"Nhập Đơn hàng"},
    },
    customer_order_item: {
      label: {"ja":"注文明細","en":"Order Item","vi":"Chi tiết đơn hàng"},
      placeholder: {"ja":"注文明細を入力","en":"Enter Order Item","vi":"Nhập Chi tiết đơn hàng"},
    },
    customer: {
      label: {"ja":"顧客","en":"Customer","vi":"Khách hàng"},
      placeholder: {"ja":"顧客を入力","en":"Enter Customer","vi":"Nhập Khách hàng"},
    },
    sentiment: {
      label: {"ja":"評価","en":"Sentiment","vi":"Đánh giá"},
      placeholder: {"ja":"評価を入力","en":"Enter Sentiment","vi":"Nhập Đánh giá"},
    },
    rating: {
      label: {"ja":"評価（星）","en":"Rating","vi":"Số sao"},
      placeholder: {"ja":"評価（星）を入力","en":"Enter Rating","vi":"Nhập Số sao"},
    },
    tags: {
      label: {"ja":"タグ","en":"Tags","vi":"Thẻ nhanh"},
      placeholder: {"ja":"タグを入力","en":"Enter Tags","vi":"Nhập Thẻ nhanh"},
    },
    comment: {
      label: {"ja":"コメント","en":"Comment","vi":"Bình luận"},
      placeholder: {"ja":"コメントを入力","en":"Enter Comment","vi":"Nhập Bình luận"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ProductReview */
export const baseProductReviewSchemas = {
  sentiment: z.nativeEnum(ReviewSentiment).optional().nullable(),
  rating: z.number().int().optional().nullable(),
  tags: z.unknown().optional().nullable(),
  comment: z.string().optional().nullable(),
} as const;

/** Create schema for ProductReview */
export const baseProductReviewCreateSchema = z.object({
  sentiment: baseProductReviewSchemas.sentiment,
  rating: baseProductReviewSchemas.rating,
  tags: baseProductReviewSchemas.tags,
  comment: baseProductReviewSchemas.comment,
});

/** Update schema for ProductReview */
export const baseProductReviewUpdateSchema = baseProductReviewCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseProductReviewCreate = z.infer<typeof baseProductReviewCreateSchema>;
export type BaseProductReviewUpdate = z.infer<typeof baseProductReviewUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getProductReviewLabel(locale: string): string {
  return productReviewI18n.label[locale as keyof typeof productReviewI18n.label] ?? productReviewI18n.label['en'] ?? 'ProductReview';
}

/** Get field label for a specific locale */
export function getProductReviewFieldLabel(field: string, locale: string): string {
  const fields = productReviewI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getProductReviewFieldPlaceholder(field: string, locale: string): string {
  const fields = productReviewI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ProductReview.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const productReviewMetadata = {
  modelName: "ProductReview",
  table: "product_reviews",

  fields: {
    product_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Product",
        table: "products",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    customer_order_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrder",
        table: "customer_orders",
        onDelete: "CASCADE",
      },
      label: {},
    },
    customer_order_item_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrderItem",
        table: "customer_order_items",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    customer_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Customer",
        table: "customers",
        onDelete: "SET NULL",
      },
      label: {},
    },
    sentiment: {
      type: "enum",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      enum: "ReviewSentiment",
      enumValues: ["up", "down"] as const,
      label: {
        ja: "評価",
        en: "Sentiment",
        vi: "Đánh giá",
      },
      placeholder: {
        ja: "評価を入力",
        en: "Enter Sentiment",
        vi: "Nhập Đánh giá",
      },
    },
    rating: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "評価（星）",
        en: "Rating",
        vi: "Số sao",
      },
      placeholder: {
        ja: "評価（星）を入力",
        en: "Enter Rating",
        vi: "Nhập Số sao",
      },
    },
    tags: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "タグ",
        en: "Tags",
        vi: "Thẻ nhanh",
      },
      placeholder: {
        ja: "タグを入力",
        en: "Enter Tags",
        vi: "Nhập Thẻ nhanh",
      },
    },
    comment: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "コメント",
        en: "Comment",
        vi: "Bình luận",
      },
      placeholder: {
        ja: "コメントを入力",
        en: "Enter Comment",
        vi: "Nhập Bình luận",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["product_id", "customer_order_id", "customer_order_item_id", "organization_id", "brand_id", "branch_id"] as const,
  enumFields: ["sentiment"] as const,
  relationFields: ["product_id", "customer_order_id", "customer_order_item_id", "customer_id", "organization_id", "brand_id", "branch_id"] as const,
} as const;

export type ProductReviewFieldName = keyof typeof productReviewMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ProductReview. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ProductReviewCreateFormState {
  product_id: string;
  customer_order_id: string;
  customer_order_item_id: string;
  customer_id?: string | null;
  sentiment?: ReviewSentiment | null;
  rating?: number | null;
  tags?: unknown | null;
  comment?: string | null;
  organization_id: string;
  brand_id: string;
  branch_id: string;
}

/** Form-state shape for updating a ProductReview. Every field optional. */
export interface ProductReviewUpdateFormState {
  product_id?: string;
  customer_order_id?: string;
  customer_order_item_id?: string;
  customer_id?: string | null;
  sentiment?: ReviewSentiment | null;
  rating?: number | null;
  tags?: unknown | null;
  comment?: string | null;
  organization_id?: string;
  brand_id?: string;
  branch_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyProductReviewCreateForm(): ProductReviewCreateFormState {
  return {
    product_id: '',
    customer_order_id: '',
    customer_order_item_id: '',
    customer_id: '',
    rating: 0,
    tags: null,
    comment: '',
    organization_id: '',
    brand_id: '',
    branch_id: '',
  };
}

/** Convert create form state into the payload that baseProductReviewCreateSchema accepts. */
export function buildProductReviewCreatePayload(form: ProductReviewCreateFormState): BaseProductReviewCreate {
  const payload: Record<string, unknown> = {
    product_id: form.product_id.trim(),
    customer_order_id: form.customer_order_id.trim(),
    customer_order_item_id: form.customer_order_item_id.trim(),
    customer_id: form.customer_id?.toString().trim() || null,
    sentiment: form.sentiment,
    rating: form.rating,
    tags: form.tags,
    comment: form.comment?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    branch_id: form.branch_id.trim(),
  };

  return payload as BaseProductReviewCreate;
}

/** Convert update form state into the payload that baseProductReviewUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildProductReviewUpdatePayload(form: ProductReviewUpdateFormState): BaseProductReviewUpdate {
  const payload: Record<string, unknown> = {};
  if (form.product_id !== undefined) {
    payload.product_id = form.product_id?.toString().trim() ?? null;
  }
  if (form.customer_order_id !== undefined) {
    payload.customer_order_id = form.customer_order_id?.toString().trim() ?? null;
  }
  if (form.customer_order_item_id !== undefined) {
    payload.customer_order_item_id = form.customer_order_item_id?.toString().trim() ?? null;
  }
  if (form.customer_id !== undefined) {
    payload.customer_id = form.customer_id?.toString().trim() ?? null;
  }
  if (form.sentiment !== undefined) {
    payload.sentiment = form.sentiment;
  }
  if (form.rating !== undefined) {
    payload.rating = form.rating;
  }
  if (form.tags !== undefined) {
    payload.tags = form.tags;
  }
  if (form.comment !== undefined) {
    payload.comment = form.comment?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  return payload as BaseProductReviewUpdate;
}
