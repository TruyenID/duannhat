/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Device } from './Device';
import type { Organization } from './Organization';

/**
 * PeripheralDevice
 */
export interface PeripheralDevice {
  id: string;
  organization_id: string;
  organization?: Organization;
  branch_id: string;
  branch?: Branch;
  name: string;
  type: string;
  is_active: boolean;
  metadata?: unknown | null;
  secret?: string | null;
  registered_by_device_id?: string | null;
  registeredByDevice?: Device | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PeripheralDevice
 */
export const peripheralDeviceI18n = {
  label: {"ja":"PeripheralDevice","en":"PeripheralDevice","vi":"PeripheralDevice"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branch: {
      label: {"ja":"支店","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"支店を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    type: {
      label: {"ja":"種別","en":"Type","vi":"Loại"},
      placeholder: {"ja":"種別を入力","en":"Enter Type","vi":"Nhập Loại"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    metadata: {
      label: {"ja":"メタデータ","en":"Metadata","vi":"Dữ liệu kèm"},
      placeholder: {"ja":"メタデータを入力","en":"Enter Metadata","vi":"Nhập Dữ liệu kèm"},
    },
    secret: {
      label: {"ja":"シークレット","en":"Secret","vi":"Khoá bí mật"},
      placeholder: {"ja":"シークレットを入力","en":"Enter Secret","vi":"Nhập Khoá bí mật"},
    },
    registered_by_device: {
      label: {"ja":"登録した端末","en":"Registered By Device","vi":"Thiết bị đã đăng ký"},
      placeholder: {"ja":"登録した端末を入力","en":"Enter Registered By Device","vi":"Nhập Thiết bị đã đăng ký"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PeripheralDevice */
export const basePeripheralDeviceSchemas = {
  name: z.string().min(1).max(255),
  type: z.string().min(1).max(50),
  is_active: z.boolean(),
  metadata: z.unknown().optional().nullable(),
  secret: z.string().optional().nullable(),
} as const;

/** Create schema for PeripheralDevice */
export const basePeripheralDeviceCreateSchema = z.object({
  name: basePeripheralDeviceSchemas.name,
  type: basePeripheralDeviceSchemas.type,
  is_active: basePeripheralDeviceSchemas.is_active,
  metadata: basePeripheralDeviceSchemas.metadata,
  secret: basePeripheralDeviceSchemas.secret,
});

/** Update schema for PeripheralDevice */
export const basePeripheralDeviceUpdateSchema = basePeripheralDeviceCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePeripheralDeviceCreate = z.infer<typeof basePeripheralDeviceCreateSchema>;
export type BasePeripheralDeviceUpdate = z.infer<typeof basePeripheralDeviceUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPeripheralDeviceLabel(locale: string): string {
  return peripheralDeviceI18n.label[locale as keyof typeof peripheralDeviceI18n.label] ?? peripheralDeviceI18n.label['en'] ?? 'PeripheralDevice';
}

/** Get field label for a specific locale */
export function getPeripheralDeviceFieldLabel(field: string, locale: string): string {
  const fields = peripheralDeviceI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPeripheralDeviceFieldPlaceholder(field: string, locale: string): string {
  const fields = peripheralDeviceI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PeripheralDevice.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const peripheralDeviceMetadata = {
  modelName: "PeripheralDevice",
  table: "peripheral_devices",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "種別",
        en: "Type",
        vi: "Loại",
      },
      placeholder: {
        ja: "種別を入力",
        en: "Enter Type",
        vi: "Nhập Loại",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    metadata: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "メタデータ",
        en: "Metadata",
        vi: "Dữ liệu kèm",
      },
      placeholder: {
        ja: "メタデータを入力",
        en: "Enter Metadata",
        vi: "Nhập Dữ liệu kèm",
      },
    },
    secret: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "シークレット",
        en: "Secret",
        vi: "Khoá bí mật",
      },
      placeholder: {
        ja: "シークレットを入力",
        en: "Enter Secret",
        vi: "Nhập Khoá bí mật",
      },
    },
    registered_by_device_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Device",
        table: "devices",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "branch_id", "name", "type", "is_active"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "branch_id", "registered_by_device_id"] as const,
} as const;

export type PeripheralDeviceFieldName = keyof typeof peripheralDeviceMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PeripheralDevice. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PeripheralDeviceCreateFormState {
  organization_id: string;
  branch_id: string;
  name: string;
  type: string;
  is_active: boolean;
  metadata?: unknown | null;
  secret?: string | null;
  registered_by_device_id?: string | null;
}

/** Form-state shape for updating a PeripheralDevice. Every field optional. */
export interface PeripheralDeviceUpdateFormState {
  organization_id?: string;
  branch_id?: string;
  name?: string;
  type?: string;
  is_active?: boolean;
  metadata?: unknown | null;
  secret?: string | null;
  registered_by_device_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPeripheralDeviceCreateForm(): PeripheralDeviceCreateFormState {
  return {
    organization_id: '',
    branch_id: '',
    name: '',
    type: '',
    is_active: true,
    metadata: null,
    secret: '',
    registered_by_device_id: '',
  };
}

/** Convert create form state into the payload that basePeripheralDeviceCreateSchema accepts. */
export function buildPeripheralDeviceCreatePayload(form: PeripheralDeviceCreateFormState): BasePeripheralDeviceCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    branch_id: form.branch_id.trim(),
    name: form.name.trim(),
    type: form.type.trim(),
    is_active: form.is_active,
    metadata: form.metadata,
    secret: form.secret?.toString().trim() || null,
    registered_by_device_id: form.registered_by_device_id?.toString().trim() || null,
  };

  return payload as BasePeripheralDeviceCreate;
}

/** Convert update form state into the payload that basePeripheralDeviceUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPeripheralDeviceUpdatePayload(form: PeripheralDeviceUpdateFormState): BasePeripheralDeviceUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.type !== undefined) {
    payload.type = form.type?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.metadata !== undefined) {
    payload.metadata = form.metadata;
  }
  if (form.secret !== undefined) {
    payload.secret = form.secret?.toString().trim() ?? null;
  }
  if (form.registered_by_device_id !== undefined) {
    payload.registered_by_device_id = form.registered_by_device_id?.toString().trim() ?? null;
  }
  return payload as BasePeripheralDeviceUpdate;
}
