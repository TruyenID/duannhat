/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { CouponDiscountType } from '../enum/CouponDiscountType';
import { CouponStatus } from '../enum/CouponStatus';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { CouponRedemption } from './CouponRedemption';
import type { Customer } from './Customer';
import type { Organization } from './Organization';
import type { PointReward } from './PointReward';

/**
 * クーポン
 */
export interface Coupon {
  id: string;
  code: string;
  name: string;
  description?: string | null;
  discount_type: CouponDiscountType;
  discount_value: number;
  max_discount_cap?: number | null;
  min_order_subtotal: number;
  usage_limit_total?: number | null;
  usage_limit_per_customer: number;
  times_used: number;
  valid_from: DateTimeString;
  valid_until: DateTimeString;
  status: CouponStatus;
  brand_id: string;
  brand?: Brand;
  organization_id: string;
  organization?: Organization;
  branches?: Branch[];
  redemptions?: CouponRedemption[];
  customer_id?: string | null;
  customer?: Customer | null;
  point_reward_id?: string | null;
  pointReward?: PointReward | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Coupon
 */
export const couponI18n = {
  label: {"ja":"クーポン","en":"Coupon","vi":"Mã giảm giá"},
  fields: {
    code: {
      label: {"ja":"コード","en":"Code","vi":"Mã"},
      placeholder: {"ja":"コードを入力","en":"Enter Code","vi":"Nhập Mã"},
    },
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    discount_type: {
      label: {"ja":"割引種別","en":"Discount Type","vi":"Loại giảm giá"},
      placeholder: {"ja":"割引種別を入力","en":"Enter Discount Type","vi":"Nhập Loại giảm giá"},
    },
    discount_value: {
      label: {"ja":"割引額","en":"Discount Value","vi":"Giá trị giảm"},
      placeholder: {"ja":"割引額を入力","en":"Enter Discount Value","vi":"Nhập Giá trị giảm"},
    },
    max_discount_cap: {
      label: {"ja":"最大割引額","en":"Max Discount Cap","vi":"Giảm tối đa"},
      placeholder: {"ja":"最大割引額を入力","en":"Enter Max Discount Cap","vi":"Nhập Giảm tối đa"},
    },
    min_order_subtotal: {
      label: {"ja":"最低注文金額","en":"Minimum Order Subtotal","vi":"Hóa đơn tối thiểu"},
      placeholder: {"ja":"最低注文金額を入力","en":"Enter Minimum Order Subtotal","vi":"Nhập Hóa đơn tối thiểu"},
    },
    usage_limit_total: {
      label: {"ja":"使用上限（全体）","en":"Usage Limit (Total)","vi":"Lượt dùng tối đa"},
      placeholder: {"ja":"使用上限（全体）を入力","en":"Enter Usage Limit (Total)","vi":"Nhập Lượt dùng tối đa"},
    },
    usage_limit_per_customer: {
      label: {"ja":"顧客あたり上限","en":"Usage Limit per Customer","vi":"Lượt mỗi khách"},
      placeholder: {"ja":"顧客あたり上限を入力","en":"Enter Usage Limit per Customer","vi":"Nhập Lượt mỗi khách"},
    },
    times_used: {
      label: {"ja":"使用回数","en":"Times Used","vi":"Số lượt đã dùng"},
      placeholder: {"ja":"使用回数を入力","en":"Enter Times Used","vi":"Nhập Số lượt đã dùng"},
    },
    valid_from: {
      label: {"ja":"有効開始","en":"Valid From","vi":"Bắt đầu"},
      placeholder: {"ja":"有効開始を入力","en":"Enter Valid From","vi":"Nhập Bắt đầu"},
    },
    valid_until: {
      label: {"ja":"有効終了","en":"Valid Until","vi":"Kết thúc"},
      placeholder: {"ja":"有効終了を入力","en":"Enter Valid Until","vi":"Nhập Kết thúc"},
    },
    status: {
      label: {"ja":"ステータス","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"ステータスを入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branches: {
      label: {"ja":"対象店舗","en":"Applicable Branches","vi":"Chi nhánh áp dụng"},
      placeholder: {"ja":"対象店舗を入力","en":"Enter Applicable Branches","vi":"Nhập Chi nhánh áp dụng"},
    },
    redemptions: {
      label: {"ja":"利用履歴","en":"Redemptions","vi":"Lịch sử dùng"},
      placeholder: {"ja":"利用履歴を入力","en":"Enter Redemptions","vi":"Nhập Lịch sử dùng"},
    },
    customer: {
      label: {"ja":"所有者","en":"Owner","vi":"Khách sở hữu"},
      placeholder: {"ja":"所有者を入力","en":"Enter Owner","vi":"Nhập Khách sở hữu"},
    },
    point_reward: {
      label: {"ja":"交換商品","en":"Point Reward","vi":"Phần thưởng đổi điểm"},
      placeholder: {"ja":"交換商品を入力","en":"Enter Point Reward","vi":"Nhập Phần thưởng đổi điểm"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Coupon */
export const baseCouponSchemas = {
  code: z.string().min(1).max(50),
  name: z.string().min(1).max(255),
  description: z.string().optional().nullable(),
  discount_type: z.nativeEnum(CouponDiscountType),
  discount_value: z.number(),
  max_discount_cap: z.number().optional().nullable(),
  min_order_subtotal: z.number(),
  usage_limit_total: z.number().int().optional().nullable(),
  usage_limit_per_customer: z.number().int(),
  times_used: z.number().int(),
  valid_from: z.string().datetime({ offset: true }),
  valid_until: z.string().datetime({ offset: true }),
  status: z.nativeEnum(CouponStatus),
} as const;

/** Create schema for Coupon */
export const baseCouponCreateSchema = z.object({
  code: baseCouponSchemas.code,
  name: baseCouponSchemas.name,
  description: baseCouponSchemas.description,
  discount_type: baseCouponSchemas.discount_type,
  discount_value: baseCouponSchemas.discount_value,
  max_discount_cap: baseCouponSchemas.max_discount_cap,
  min_order_subtotal: baseCouponSchemas.min_order_subtotal,
  usage_limit_total: baseCouponSchemas.usage_limit_total,
  usage_limit_per_customer: baseCouponSchemas.usage_limit_per_customer,
  times_used: baseCouponSchemas.times_used,
  valid_from: baseCouponSchemas.valid_from,
  valid_until: baseCouponSchemas.valid_until,
  status: baseCouponSchemas.status,
  ja: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
});

/** Update schema for Coupon */
export const baseCouponUpdateSchema = baseCouponCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseCouponCreate = z.infer<typeof baseCouponCreateSchema>;
export type BaseCouponUpdate = z.infer<typeof baseCouponUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getCouponLabel(locale: string): string {
  return couponI18n.label[locale as keyof typeof couponI18n.label] ?? couponI18n.label['en'] ?? 'Coupon';
}

/** Get field label for a specific locale */
export function getCouponFieldLabel(field: string, locale: string): string {
  const fields = couponI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getCouponFieldPlaceholder(field: string, locale: string): string {
  const fields = couponI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Coupon.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const couponMetadata = {
  modelName: "Coupon",
  table: "coupons",

  fields: {
    code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "コード",
        en: "Code",
        vi: "Mã",
      },
      placeholder: {
        ja: "コードを入力",
        en: "Enter Code",
        vi: "Nhập Mã",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: true,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    discount_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "CouponDiscountType",
      enumValues: ["fixed", "percent"] as const,
      label: {
        ja: "割引種別",
        en: "Discount Type",
        vi: "Loại giảm giá",
      },
      placeholder: {
        ja: "割引種別を入力",
        en: "Enter Discount Type",
        vi: "Nhập Loại giảm giá",
      },
    },
    discount_value: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "割引額",
        en: "Discount Value",
        vi: "Giá trị giảm",
      },
      placeholder: {
        ja: "割引額を入力",
        en: "Enter Discount Value",
        vi: "Nhập Giá trị giảm",
      },
    },
    max_discount_cap: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最大割引額",
        en: "Max Discount Cap",
        vi: "Giảm tối đa",
      },
      placeholder: {
        ja: "最大割引額を入力",
        en: "Enter Max Discount Cap",
        vi: "Nhập Giảm tối đa",
      },
    },
    min_order_subtotal: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "最低注文金額",
        en: "Minimum Order Subtotal",
        vi: "Hóa đơn tối thiểu",
      },
      placeholder: {
        ja: "最低注文金額を入力",
        en: "Enter Minimum Order Subtotal",
        vi: "Nhập Hóa đơn tối thiểu",
      },
    },
    usage_limit_total: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "使用上限（全体）",
        en: "Usage Limit (Total)",
        vi: "Lượt dùng tối đa",
      },
      placeholder: {
        ja: "使用上限（全体）を入力",
        en: "Enter Usage Limit (Total)",
        vi: "Nhập Lượt dùng tối đa",
      },
    },
    usage_limit_per_customer: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "顧客あたり上限",
        en: "Usage Limit per Customer",
        vi: "Lượt mỗi khách",
      },
      placeholder: {
        ja: "顧客あたり上限を入力",
        en: "Enter Usage Limit per Customer",
        vi: "Nhập Lượt mỗi khách",
      },
    },
    times_used: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "使用回数",
        en: "Times Used",
        vi: "Số lượt đã dùng",
      },
      placeholder: {
        ja: "使用回数を入力",
        en: "Enter Times Used",
        vi: "Nhập Số lượt đã dùng",
      },
    },
    valid_from: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効開始",
        en: "Valid From",
        vi: "Bắt đầu",
      },
      placeholder: {
        ja: "有効開始を入力",
        en: "Enter Valid From",
        vi: "Nhập Bắt đầu",
      },
    },
    valid_until: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効終了",
        en: "Valid Until",
        vi: "Kết thúc",
      },
      placeholder: {
        ja: "有効終了を入力",
        en: "Enter Valid Until",
        vi: "Nhập Kết thúc",
      },
    },
    status: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "CouponStatus",
      enumValues: ["draft", "paused"] as const,
      label: {
        ja: "ステータス",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "ステータスを入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    customer_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Customer",
        table: "customers",
        onDelete: "CASCADE",
      },
      label: {},
    },
    point_reward_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "PointReward",
        table: "point_rewards",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: ["name", "description"] as const,
  requiredFields: ["code", "name", "discount_type", "discount_value", "min_order_subtotal", "usage_limit_per_customer", "times_used", "valid_from", "valid_until", "status", "brand_id", "organization_id"] as const,
  enumFields: ["discount_type", "status"] as const,
  relationFields: ["brand_id", "organization_id", "customer_id", "point_reward_id"] as const,
} as const;

export type CouponFieldName = keyof typeof couponMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Coupon. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface CouponCreateFormState {
  code: string;
  name: Record<Locale, string>;
  description: Record<Locale, string>;
  discount_type: CouponDiscountType;
  discount_value: number;
  max_discount_cap?: number | null;
  min_order_subtotal: number;
  usage_limit_total?: number | null;
  usage_limit_per_customer: number;
  times_used: number;
  valid_from: string;
  valid_until: string;
  status: CouponStatus;
  brand_id: string;
  organization_id: string;
  customer_id?: string | null;
  point_reward_id?: string | null;
}

/** Form-state shape for updating a Coupon. Every field optional. */
export interface CouponUpdateFormState {
  code?: string;
  name?: Record<Locale, string>;
  description?: Record<Locale, string>;
  discount_type?: CouponDiscountType;
  discount_value?: number;
  max_discount_cap?: number | null;
  min_order_subtotal?: number;
  usage_limit_total?: number | null;
  usage_limit_per_customer?: number;
  times_used?: number;
  valid_from?: string;
  valid_until?: string;
  status?: CouponStatus;
  brand_id?: string;
  organization_id?: string;
  customer_id?: string | null;
  point_reward_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyCouponCreateForm(): CouponCreateFormState {
  return {
    code: '',
    name: emptyLocaleMap(),
    description: emptyLocaleMap(),
    discount_type: CouponDiscountType.Fixed,
    discount_value: 0,
    max_discount_cap: 0,
    min_order_subtotal: 0,
    usage_limit_total: 0,
    usage_limit_per_customer: 0,
    times_used: 0,
    valid_from: '',
    valid_until: '',
    status: CouponStatus.Draft,
    brand_id: '',
    organization_id: '',
    customer_id: '',
    point_reward_id: '',
  };
}

/** Convert create form state into the payload that baseCouponCreateSchema accepts. */
export function buildCouponCreatePayload(form: CouponCreateFormState): BaseCouponCreate {
  const payload: Record<string, unknown> = {
    code: form.code.trim(),
    name: (form.name["ja"] ?? '').trim(),
    description: (form.description["ja"] ?? '').trim(),
    discount_type: form.discount_type,
    discount_value: form.discount_value,
    max_discount_cap: form.max_discount_cap,
    min_order_subtotal: form.min_order_subtotal,
    usage_limit_total: form.usage_limit_total,
    usage_limit_per_customer: form.usage_limit_per_customer,
    times_used: form.times_used,
    valid_from: form.valid_from,
    valid_until: form.valid_until,
    status: form.status,
    brand_id: form.brand_id.trim(),
    organization_id: form.organization_id.trim(),
    customer_id: form.customer_id?.toString().trim() || null,
    point_reward_id: form.point_reward_id?.toString().trim() || null,
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
    description: form.description,
  });
  Object.assign(payload, i18n);

  return payload as BaseCouponCreate;
}

/** Convert update form state into the payload that baseCouponUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildCouponUpdatePayload(form: CouponUpdateFormState): BaseCouponUpdate {
  const payload: Record<string, unknown> = {};
  if (form.code !== undefined) {
    payload.code = form.code?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.description !== undefined) {
    payload.description = (form.description["ja"] ?? '').trim();
  }
  if (form.discount_type !== undefined) {
    payload.discount_type = form.discount_type;
  }
  if (form.discount_value !== undefined) {
    payload.discount_value = form.discount_value;
  }
  if (form.max_discount_cap !== undefined) {
    payload.max_discount_cap = form.max_discount_cap;
  }
  if (form.min_order_subtotal !== undefined) {
    payload.min_order_subtotal = form.min_order_subtotal;
  }
  if (form.usage_limit_total !== undefined) {
    payload.usage_limit_total = form.usage_limit_total;
  }
  if (form.usage_limit_per_customer !== undefined) {
    payload.usage_limit_per_customer = form.usage_limit_per_customer;
  }
  if (form.times_used !== undefined) {
    payload.times_used = form.times_used;
  }
  if (form.valid_from !== undefined) {
    payload.valid_from = form.valid_from;
  }
  if (form.valid_until !== undefined) {
    payload.valid_until = form.valid_until;
  }
  if (form.status !== undefined) {
    payload.status = form.status;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.customer_id !== undefined) {
    payload.customer_id = form.customer_id?.toString().trim() ?? null;
  }
  if (form.point_reward_id !== undefined) {
    payload.point_reward_id = form.point_reward_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  if (form.description !== undefined) localeInput.description = form.description;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseCouponUpdate;
}
