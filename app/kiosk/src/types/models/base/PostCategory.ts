/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Organization } from './Organization';
import type { Post } from './Post';

/**
 * 投稿カテゴリー
 */
export interface PostCategory {
  id: string;
  organization_id: string;
  organization?: Organization;
  name: string;
  slug: string;
  description?: string | null;
  image_url?: string | null;
  sort_order: number;
  is_active: boolean;
  parent_id?: string | null;
  parent?: PostCategory | null;
  children?: PostCategory[];
  posts?: Post[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PostCategory
 */
export const postCategoryI18n = {
  label: {"ja":"投稿カテゴリー","en":"Post Category","vi":"Danh mục Bài viết"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    name: {
      label: {"ja":"名前","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名前を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    slug: {
      label: {"ja":"スラッグ","en":"Slug","vi":"Slug"},
      placeholder: {"ja":"スラッグを入力","en":"Enter Slug","vi":"Nhập Slug"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    image_url: {
      label: {"ja":"画像URL","en":"Image URL","vi":"URL hình ảnh"},
      placeholder: {"ja":"画像URLを入力","en":"Enter Image URL","vi":"Nhập URL hình ảnh"},
    },
    sort_order: {
      label: {"ja":"並び順","en":"Sort Order","vi":"Thứ tự sắp xếp"},
      placeholder: {"ja":"並び順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự sắp xếp"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    parent: {
      label: {"ja":"親カテゴリー","en":"Parent Category","vi":"Danh mục cha"},
      placeholder: {"ja":"親カテゴリーを入力","en":"Enter Parent Category","vi":"Nhập Danh mục cha"},
    },
    children: {
      label: {"ja":"子カテゴリー","en":"Child Categories","vi":"Danh mục con"},
      placeholder: {"ja":"子カテゴリーを入力","en":"Enter Child Categories","vi":"Nhập Danh mục con"},
    },
    posts: {
      label: {"ja":"投稿","en":"Posts","vi":"Bài viết"},
      placeholder: {"ja":"投稿を入力","en":"Enter Posts","vi":"Nhập Bài viết"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PostCategory */
export const basePostCategorySchemas = {
  name: z.string().min(1).max(120),
  slug: z.string().min(1).max(140),
  description: z.string().optional().nullable(),
  image_url: z.string().max(500).optional().nullable(),
  sort_order: z.number().int(),
  is_active: z.boolean(),
} as const;

/** Create schema for PostCategory */
export const basePostCategoryCreateSchema = z.object({
  name: basePostCategorySchemas.name,
  slug: basePostCategorySchemas.slug,
  description: basePostCategorySchemas.description,
  image_url: basePostCategorySchemas.image_url,
  sort_order: basePostCategorySchemas.sort_order,
  is_active: basePostCategorySchemas.is_active,
  ja: z.object({
    name: z.string().max(120).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(120).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(120).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
});

/** Update schema for PostCategory */
export const basePostCategoryUpdateSchema = basePostCategoryCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePostCategoryCreate = z.infer<typeof basePostCategoryCreateSchema>;
export type BasePostCategoryUpdate = z.infer<typeof basePostCategoryUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPostCategoryLabel(locale: string): string {
  return postCategoryI18n.label[locale as keyof typeof postCategoryI18n.label] ?? postCategoryI18n.label['en'] ?? 'PostCategory';
}

/** Get field label for a specific locale */
export function getPostCategoryFieldLabel(field: string, locale: string): string {
  const fields = postCategoryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPostCategoryFieldPlaceholder(field: string, locale: string): string {
  const fields = postCategoryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PostCategory.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const postCategoryMetadata = {
  modelName: "PostCategory",
  table: "post_categories",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 120,
      },
      label: {
        ja: "名前",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名前を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    slug: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 140,
      },
      label: {
        ja: "スラッグ",
        en: "Slug",
        vi: "Slug",
      },
      placeholder: {
        ja: "スラッグを入力",
        en: "Enter Slug",
        vi: "Nhập Slug",
      },
      description: {
        ja: "URLセーフな識別子。組織内で一意。",
        en: "URL-safe identifier. Unique per organization.",
        vi: "Mã định danh URL-safe. Duy nhất trong phạm vi tổ chức.",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: true,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    image_url: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 500,
      },
      label: {
        ja: "画像URL",
        en: "Image URL",
        vi: "URL hình ảnh",
      },
      placeholder: {
        ja: "画像URLを入力",
        en: "Enter Image URL",
        vi: "Nhập URL hình ảnh",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "並び順",
        en: "Sort Order",
        vi: "Thứ tự sắp xếp",
      },
      placeholder: {
        ja: "並び順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự sắp xếp",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    parent_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "PostCategory",
        table: "post_categories",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name", "description"] as const,
  requiredFields: ["organization_id", "name", "slug", "sort_order", "is_active"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "parent_id"] as const,
} as const;

export type PostCategoryFieldName = keyof typeof postCategoryMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PostCategory. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PostCategoryCreateFormState {
  organization_id: string;
  name: Record<Locale, string>;
  slug: string;
  description: Record<Locale, string>;
  image_url?: string | null;
  sort_order: number;
  is_active: boolean;
  parent_id?: string | null;
}

/** Form-state shape for updating a PostCategory. Every field optional. */
export interface PostCategoryUpdateFormState {
  organization_id?: string;
  name?: Record<Locale, string>;
  slug?: string;
  description?: Record<Locale, string>;
  image_url?: string | null;
  sort_order?: number;
  is_active?: boolean;
  parent_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPostCategoryCreateForm(): PostCategoryCreateFormState {
  return {
    organization_id: '',
    name: emptyLocaleMap(),
    slug: '',
    description: emptyLocaleMap(),
    image_url: '',
    sort_order: 0,
    is_active: true,
    parent_id: '',
  };
}

/** Convert create form state into the payload that basePostCategoryCreateSchema accepts. */
export function buildPostCategoryCreatePayload(form: PostCategoryCreateFormState): BasePostCategoryCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    name: (form.name["ja"] ?? '').trim(),
    slug: form.slug.trim(),
    description: (form.description["ja"] ?? '').trim(),
    image_url: form.image_url?.toString().trim() || null,
    sort_order: form.sort_order,
    is_active: form.is_active,
    parent_id: form.parent_id?.toString().trim() || null,
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
    description: form.description,
  });
  Object.assign(payload, i18n);

  return payload as BasePostCategoryCreate;
}

/** Convert update form state into the payload that basePostCategoryUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPostCategoryUpdatePayload(form: PostCategoryUpdateFormState): BasePostCategoryUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.slug !== undefined) {
    payload.slug = form.slug?.toString().trim() ?? null;
  }
  if (form.description !== undefined) {
    payload.description = (form.description["ja"] ?? '').trim();
  }
  if (form.image_url !== undefined) {
    payload.image_url = form.image_url?.toString().trim() ?? null;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.parent_id !== undefined) {
    payload.parent_id = form.parent_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  if (form.description !== undefined) localeInput.description = form.description;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BasePostCategoryUpdate;
}
