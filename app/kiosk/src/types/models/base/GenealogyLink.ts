/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { MaterialLot } from './MaterialLot';

/**
 * 系譜リンク
 */
export interface GenealogyLink {
  id: string;
  parent_lot_id: string;
  parent_lot?: MaterialLot;
  child_lot_id?: string | null;
  child_lot?: MaterialLot | null;
  customer_order_id?: string | null;
  qty_consumed: number;
  unit: string;
  consumed_at: DateTimeString;
  source_event_type: string;
  source_event_id: string;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for GenealogyLink
 */
export const genealogyLinkI18n = {
  label: {"ja":"系譜リンク","en":"Genealogy Link","vi":"Liên kết phả hệ"},
  fields: {
    parent_lot: {
      label: {"ja":"親ロット","en":"Parent Lot","vi":"Lô cha"},
      placeholder: {"ja":"親ロットを入力","en":"Enter Parent Lot","vi":"Nhập Lô cha"},
    },
    child_lot: {
      label: {"ja":"子ロット","en":"Child Lot","vi":"Lô con"},
      placeholder: {"ja":"子ロットを入力","en":"Enter Child Lot","vi":"Nhập Lô con"},
    },
    customer_order_id: {
      label: {"ja":"顧客注文ID","en":"Customer Order ID","vi":"ID đơn khách hàng"},
      placeholder: {"ja":"顧客注文IDを入力","en":"Enter Customer Order ID","vi":"Nhập ID đơn khách hàng"},
    },
    qty_consumed: {
      label: {"ja":"消費数量","en":"Quantity Consumed","vi":"Số lượng tiêu thụ"},
      placeholder: {"ja":"消費数量を入力","en":"Enter Quantity Consumed","vi":"Nhập Số lượng tiêu thụ"},
    },
    unit: {
      label: {"ja":"単位","en":"Unit","vi":"Đơn vị"},
      placeholder: {"ja":"単位を入力","en":"Enter Unit","vi":"Nhập Đơn vị"},
    },
    consumed_at: {
      label: {"ja":"消費日時","en":"Consumed At","vi":"Thời điểm tiêu thụ"},
      placeholder: {"ja":"消費日時を入力","en":"Enter Consumed At","vi":"Nhập Thời điểm tiêu thụ"},
    },
    source_event_type: {
      label: {"ja":"ソースイベント種別","en":"Source Event Type","vi":"Loại sự kiện gốc"},
      placeholder: {"ja":"ソースイベント種別を入力","en":"Enter Source Event Type","vi":"Nhập Loại sự kiện gốc"},
    },
    source_event_id: {
      label: {"ja":"ソースイベントID","en":"Source Event ID","vi":"ID sự kiện gốc"},
      placeholder: {"ja":"ソースイベントIDを入力","en":"Enter Source Event ID","vi":"Nhập ID sự kiện gốc"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for GenealogyLink */
export const baseGenealogyLinkSchemas = {
  customer_order_id: z.string().optional().nullable(),
  qty_consumed: z.number(),
  unit: z.string().min(1).max(50),
  consumed_at: z.string().datetime({ offset: true }),
  source_event_type: z.string().min(1).max(50),
  source_event_id: z.string(),
} as const;

/** Create schema for GenealogyLink */
export const baseGenealogyLinkCreateSchema = z.object({
  customer_order_id: baseGenealogyLinkSchemas.customer_order_id,
  qty_consumed: baseGenealogyLinkSchemas.qty_consumed,
  unit: baseGenealogyLinkSchemas.unit,
  consumed_at: baseGenealogyLinkSchemas.consumed_at,
  source_event_type: baseGenealogyLinkSchemas.source_event_type,
  source_event_id: baseGenealogyLinkSchemas.source_event_id,
});

/** Update schema for GenealogyLink */
export const baseGenealogyLinkUpdateSchema = baseGenealogyLinkCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseGenealogyLinkCreate = z.infer<typeof baseGenealogyLinkCreateSchema>;
export type BaseGenealogyLinkUpdate = z.infer<typeof baseGenealogyLinkUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getGenealogyLinkLabel(locale: string): string {
  return genealogyLinkI18n.label[locale as keyof typeof genealogyLinkI18n.label] ?? genealogyLinkI18n.label['en'] ?? 'GenealogyLink';
}

/** Get field label for a specific locale */
export function getGenealogyLinkFieldLabel(field: string, locale: string): string {
  const fields = genealogyLinkI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getGenealogyLinkFieldPlaceholder(field: string, locale: string): string {
  const fields = genealogyLinkI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for GenealogyLink.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const genealogyLinkMetadata = {
  modelName: "GenealogyLink",
  table: "genealogy_links",

  fields: {
    parent_lot_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "MaterialLot",
        table: "material_lots",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    child_lot_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "MaterialLot",
        table: "material_lots",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    customer_order_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "顧客注文ID",
        en: "Customer Order ID",
        vi: "ID đơn khách hàng",
      },
      placeholder: {
        ja: "顧客注文IDを入力",
        en: "Enter Customer Order ID",
        vi: "Nhập ID đơn khách hàng",
      },
      description: {
        ja: "source_event_type=customer_order時のcustomer_orders.idへのソフトFK。生産／廃棄エッジではNULL。",
        en: "Soft FK to customer_orders.id when source_event_type=customer_order. NULL on production / disposal edges.",
        vi: "Soft FK đến customer_orders.id khi source_event_type=customer_order. NULL với cạnh sản xuất/hủy.",
      },
    },
    qty_consumed: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "消費数量",
        en: "Quantity Consumed",
        vi: "Số lượng tiêu thụ",
      },
      placeholder: {
        ja: "消費数量を入力",
        en: "Enter Quantity Consumed",
        vi: "Nhập Số lượng tiêu thụ",
      },
      description: {
        ja: "このイベントでparent_lotから引き出された正の数量。リコールレポートで配分比率の計算に使用。",
        en: "Positive qty drawn from parent_lot in this event. Recall reports use this to compute distribution percentages.",
        vi: "Số lượng dương rút từ parent_lot trong sự kiện này. Báo cáo recall dùng để tính tỷ lệ phân phối.",
      },
    },
    unit: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "単位",
        en: "Unit",
        vi: "Đơn vị",
      },
      placeholder: {
        ja: "単位を入力",
        en: "Enter Unit",
        vi: "Nhập Đơn vị",
      },
      description: {
        ja: "qty_consumedの単位。parent_lot.unitと一致必須（レポート用に非正規化）。",
        en: "Unit of qty_consumed. Must match parent_lot.unit (denormalized for reporting).",
        vi: "Đơn vị của qty_consumed. Phải khớp parent_lot.unit (denormalize cho reporting).",
      },
    },
    consumed_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "消費日時",
        en: "Consumed At",
        vi: "Thời điểm tiêu thụ",
      },
      placeholder: {
        ja: "消費日時を入力",
        en: "Enter Consumed At",
        vi: "Nhập Thời điểm tiêu thụ",
      },
      description: {
        ja: "消費が物理的に発生した日時（バッチ開始／注文クローズ）。created_atとは別 — FSMA-204タイムラインレポートで使用。",
        en: "When consumption physically happened (batch start / order close). Distinct from created_at — used in FSMA-204 timeline reports.",
        vi: "Thời điểm tiêu thụ thực tế (start batch / close đơn). Khác created_at — dùng trong báo cáo timeline FSMA-204.",
      },
    },
    source_event_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "ソースイベント種別",
        en: "Source Event Type",
        vi: "Loại sự kiện gốc",
      },
      placeholder: {
        ja: "ソースイベント種別を入力",
        en: "Enter Source Event Type",
        vi: "Nhập Loại sự kiện gốc",
      },
      description: {
        ja: "source_event_idの判別子。既知の値：material_batch | customer_order | disposal | manual_adjustment。enumではない — 将来のイベント拡張可能性を維持。",
        en: "Discriminator for source_event_id. Known values: material_batch | customer_order | disposal | manual_adjustment. NOT an enum — keeps future events extensible.",
        vi: "Discriminator cho source_event_id. Giá trị đã biết: material_batch | customer_order | disposal | manual_adjustment. KHÔNG dùng enum — giữ khả năng mở rộng.",
      },
    },
    source_event_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ソースイベントID",
        en: "Source Event ID",
        vi: "ID sự kiện gốc",
      },
      placeholder: {
        ja: "ソースイベントIDを入力",
        en: "Enter Source Event ID",
        vi: "Nhập ID sự kiện gốc",
      },
      description: {
        ja: "source_event_typeで識別される行へのソフトFK。例：source_event_type=material_batch時のmaterial_batches.id。",
        en: "Soft FK to the row identified by source_event_type. e.g. material_batches.id when source_event_type=material_batch.",
        vi: "Soft FK đến row xác định bởi source_event_type. VD: material_batches.id khi source_event_type=material_batch.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["parent_lot_id", "qty_consumed", "unit", "consumed_at", "source_event_type", "source_event_id"] as const,
  enumFields: [] as const,
  relationFields: ["parent_lot_id", "child_lot_id"] as const,
} as const;

export type GenealogyLinkFieldName = keyof typeof genealogyLinkMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a GenealogyLink. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface GenealogyLinkCreateFormState {
  parent_lot_id: string;
  child_lot_id?: string | null;
  customer_order_id?: string | null;
  qty_consumed: number;
  unit: string;
  consumed_at: string;
  source_event_type: string;
  source_event_id: string;
}

/** Form-state shape for updating a GenealogyLink. Every field optional. */
export interface GenealogyLinkUpdateFormState {
  parent_lot_id?: string;
  child_lot_id?: string | null;
  customer_order_id?: string | null;
  qty_consumed?: number;
  unit?: string;
  consumed_at?: string;
  source_event_type?: string;
  source_event_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyGenealogyLinkCreateForm(): GenealogyLinkCreateFormState {
  return {
    parent_lot_id: '',
    child_lot_id: '',
    customer_order_id: '',
    qty_consumed: 0,
    unit: '',
    consumed_at: '',
    source_event_type: '',
    source_event_id: '',
  };
}

/** Convert create form state into the payload that baseGenealogyLinkCreateSchema accepts. */
export function buildGenealogyLinkCreatePayload(form: GenealogyLinkCreateFormState): BaseGenealogyLinkCreate {
  const payload: Record<string, unknown> = {
    parent_lot_id: form.parent_lot_id.trim(),
    child_lot_id: form.child_lot_id?.toString().trim() || null,
    customer_order_id: form.customer_order_id?.toString().trim() || null,
    qty_consumed: form.qty_consumed,
    unit: form.unit.trim(),
    consumed_at: form.consumed_at,
    source_event_type: form.source_event_type.trim(),
    source_event_id: form.source_event_id.trim(),
  };

  return payload as BaseGenealogyLinkCreate;
}

/** Convert update form state into the payload that baseGenealogyLinkUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildGenealogyLinkUpdatePayload(form: GenealogyLinkUpdateFormState): BaseGenealogyLinkUpdate {
  const payload: Record<string, unknown> = {};
  if (form.parent_lot_id !== undefined) {
    payload.parent_lot_id = form.parent_lot_id?.toString().trim() ?? null;
  }
  if (form.child_lot_id !== undefined) {
    payload.child_lot_id = form.child_lot_id?.toString().trim() ?? null;
  }
  if (form.customer_order_id !== undefined) {
    payload.customer_order_id = form.customer_order_id?.toString().trim() ?? null;
  }
  if (form.qty_consumed !== undefined) {
    payload.qty_consumed = form.qty_consumed;
  }
  if (form.unit !== undefined) {
    payload.unit = form.unit?.toString().trim() ?? null;
  }
  if (form.consumed_at !== undefined) {
    payload.consumed_at = form.consumed_at;
  }
  if (form.source_event_type !== undefined) {
    payload.source_event_type = form.source_event_type?.toString().trim() ?? null;
  }
  if (form.source_event_id !== undefined) {
    payload.source_event_id = form.source_event_id?.toString().trim() ?? null;
  }
  return payload as BaseGenealogyLinkUpdate;
}
