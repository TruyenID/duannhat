/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { User } from './User';

/**
 * PrintTemplate
 */
export interface PrintTemplate {
  id: string;
  organization_id?: string | null;
  organization?: Organization | null;
  brand_id?: string | null;
  brand?: Brand | null;
  branch_id?: string | null;
  branch?: Branch | null;
  kind: string;
  scope: string;
  version: number;
  status: string;
  definition: unknown;
  shop_editable?: unknown | null;
  effective_from?: DateTimeString | null;
  parent_version_id?: string | null;
  parentVersion?: PrintTemplate | null;
  notes?: string | null;
  created_by_id?: string | null;
  createdBy?: User | null;
  published_by_id?: string | null;
  publishedBy?: User | null;
  published_at?: DateTimeString | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PrintTemplate
 */
export const printTemplateI18n = {
  label: {"ja":"PrintTemplate","en":"PrintTemplate","vi":"PrintTemplate"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"支店","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"支店を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    kind: {
      label: {"ja":"種別","en":"Kind","vi":"Loại phiếu"},
      placeholder: {"ja":"種別を入力","en":"Enter Kind","vi":"Nhập Loại phiếu"},
    },
    scope: {
      label: {"ja":"スコープ","en":"Scope","vi":"Phạm vi"},
      placeholder: {"ja":"スコープを入力","en":"Enter Scope","vi":"Nhập Phạm vi"},
    },
    version: {
      label: {"ja":"バージョン","en":"Version","vi":"Phiên bản"},
      placeholder: {"ja":"バージョンを入力","en":"Enter Version","vi":"Nhập Phiên bản"},
    },
    status: {
      label: {"ja":"状態","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"状態を入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    definition: {
      label: {"ja":"定義","en":"Definition","vi":"Định nghĩa"},
      placeholder: {"ja":"定義を入力","en":"Enter Definition","vi":"Nhập Định nghĩa"},
    },
    shop_editable: {
      label: {"ja":"店舗編集可","en":"Shop Editable","vi":"Shop sửa được"},
      placeholder: {"ja":"店舗編集可を入力","en":"Enter Shop Editable","vi":"Nhập Shop sửa được"},
    },
    effective_from: {
      label: {"ja":"適用開始","en":"Effective From","vi":"Hiệu lực từ"},
      placeholder: {"ja":"適用開始を入力","en":"Enter Effective From","vi":"Nhập Hiệu lực từ"},
    },
    parent_version: {
      label: {"ja":"親バージョン","en":"Parent Version","vi":"Bản gốc"},
      placeholder: {"ja":"親バージョンを入力","en":"Enter Parent Version","vi":"Nhập Bản gốc"},
    },
    notes: {
      label: {"ja":"変更メモ","en":"Notes","vi":"Ghi chú"},
      placeholder: {"ja":"変更メモを入力","en":"Enter Notes","vi":"Nhập Ghi chú"},
    },
    created_by: {
      label: {"ja":"作成者","en":"Created By","vi":"Người tạo"},
      placeholder: {"ja":"作成者を入力","en":"Enter Created By","vi":"Nhập Người tạo"},
    },
    published_by: {
      label: {"ja":"公開者","en":"Published By","vi":"Người phát hành"},
      placeholder: {"ja":"公開者を入力","en":"Enter Published By","vi":"Nhập Người phát hành"},
    },
    published_at: {
      label: {"ja":"公開日時","en":"Published At","vi":"Phát hành lúc"},
      placeholder: {"ja":"公開日時を入力","en":"Enter Published At","vi":"Nhập Phát hành lúc"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PrintTemplate */
export const basePrintTemplateSchemas = {
  kind: z.string().min(1).max(32),
  scope: z.string().min(1).max(16),
  version: z.number().int(),
  status: z.string().min(1).max(16),
  definition: z.unknown(),
  shop_editable: z.unknown().optional().nullable(),
  effective_from: z.string().datetime({ offset: true }).optional().nullable(),
  notes: z.string().max(500).optional().nullable(),
  published_at: z.string().datetime({ offset: true }).optional().nullable(),
} as const;

/** Create schema for PrintTemplate */
export const basePrintTemplateCreateSchema = z.object({
  kind: basePrintTemplateSchemas.kind,
  scope: basePrintTemplateSchemas.scope,
  version: basePrintTemplateSchemas.version,
  status: basePrintTemplateSchemas.status,
  definition: basePrintTemplateSchemas.definition,
  shop_editable: basePrintTemplateSchemas.shop_editable,
  effective_from: basePrintTemplateSchemas.effective_from,
  notes: basePrintTemplateSchemas.notes,
  published_at: basePrintTemplateSchemas.published_at,
});

/** Update schema for PrintTemplate */
export const basePrintTemplateUpdateSchema = basePrintTemplateCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePrintTemplateCreate = z.infer<typeof basePrintTemplateCreateSchema>;
export type BasePrintTemplateUpdate = z.infer<typeof basePrintTemplateUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPrintTemplateLabel(locale: string): string {
  return printTemplateI18n.label[locale as keyof typeof printTemplateI18n.label] ?? printTemplateI18n.label['en'] ?? 'PrintTemplate';
}

/** Get field label for a specific locale */
export function getPrintTemplateFieldLabel(field: string, locale: string): string {
  const fields = printTemplateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPrintTemplateFieldPlaceholder(field: string, locale: string): string {
  const fields = printTemplateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PrintTemplate.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const printTemplateMetadata = {
  modelName: "PrintTemplate",
  table: "print_templates",

  fields: {
    organization_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    kind: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "種別",
        en: "Kind",
        vi: "Loại phiếu",
      },
      placeholder: {
        ja: "種別を入力",
        en: "Enter Kind",
        vi: "Nhập Loại phiếu",
      },
    },
    scope: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 16,
      },
      label: {
        ja: "スコープ",
        en: "Scope",
        vi: "Phạm vi",
      },
      placeholder: {
        ja: "スコープを入力",
        en: "Enter Scope",
        vi: "Nhập Phạm vi",
      },
    },
    version: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "バージョン",
        en: "Version",
        vi: "Phiên bản",
      },
      placeholder: {
        ja: "バージョンを入力",
        en: "Enter Version",
        vi: "Nhập Phiên bản",
      },
    },
    status: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 16,
      },
      label: {
        ja: "状態",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "状態を入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    definition: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "定義",
        en: "Definition",
        vi: "Định nghĩa",
      },
      placeholder: {
        ja: "定義を入力",
        en: "Enter Definition",
        vi: "Nhập Định nghĩa",
      },
    },
    shop_editable: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "店舗編集可",
        en: "Shop Editable",
        vi: "Shop sửa được",
      },
      placeholder: {
        ja: "店舗編集可を入力",
        en: "Enter Shop Editable",
        vi: "Nhập Shop sửa được",
      },
    },
    effective_from: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "適用開始",
        en: "Effective From",
        vi: "Hiệu lực từ",
      },
      placeholder: {
        ja: "適用開始を入力",
        en: "Enter Effective From",
        vi: "Nhập Hiệu lực từ",
      },
    },
    parent_version_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "PrintTemplate",
        table: "print_templates",
        onDelete: "SET NULL",
      },
      label: {},
    },
    notes: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 500,
      },
      label: {
        ja: "変更メモ",
        en: "Notes",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "変更メモを入力",
        en: "Enter Notes",
        vi: "Nhập Ghi chú",
      },
    },
    created_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
    published_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
    published_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "公開日時",
        en: "Published At",
        vi: "Phát hành lúc",
      },
      placeholder: {
        ja: "公開日時を入力",
        en: "Enter Published At",
        vi: "Nhập Phát hành lúc",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["kind", "scope", "version", "status", "definition"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "brand_id", "branch_id", "parent_version_id", "created_by_id", "published_by_id"] as const,
} as const;

export type PrintTemplateFieldName = keyof typeof printTemplateMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PrintTemplate. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PrintTemplateCreateFormState {
  organization_id?: string | null;
  brand_id?: string | null;
  branch_id?: string | null;
  kind: string;
  scope: string;
  version: number;
  status: string;
  definition: unknown;
  shop_editable?: unknown | null;
  effective_from?: string | null;
  parent_version_id?: string | null;
  notes?: string | null;
  created_by_id?: string | null;
  published_by_id?: string | null;
  published_at?: string | null;
}

/** Form-state shape for updating a PrintTemplate. Every field optional. */
export interface PrintTemplateUpdateFormState {
  organization_id?: string | null;
  brand_id?: string | null;
  branch_id?: string | null;
  kind?: string;
  scope?: string;
  version?: number;
  status?: string;
  definition?: unknown;
  shop_editable?: unknown | null;
  effective_from?: string | null;
  parent_version_id?: string | null;
  notes?: string | null;
  created_by_id?: string | null;
  published_by_id?: string | null;
  published_at?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPrintTemplateCreateForm(): PrintTemplateCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    branch_id: '',
    kind: '',
    scope: '',
    version: 0,
    status: "draft",
    definition: null,
    shop_editable: null,
    effective_from: '',
    parent_version_id: '',
    notes: '',
    created_by_id: '',
    published_by_id: '',
    published_at: '',
  };
}

/** Convert create form state into the payload that basePrintTemplateCreateSchema accepts. */
export function buildPrintTemplateCreatePayload(form: PrintTemplateCreateFormState): BasePrintTemplateCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id?.toString().trim() || null,
    brand_id: form.brand_id?.toString().trim() || null,
    branch_id: form.branch_id?.toString().trim() || null,
    kind: form.kind.trim(),
    scope: form.scope.trim(),
    version: form.version,
    status: form.status.trim(),
    definition: form.definition,
    shop_editable: form.shop_editable,
    effective_from: form.effective_from,
    parent_version_id: form.parent_version_id?.toString().trim() || null,
    notes: form.notes?.toString().trim() || null,
    created_by_id: form.created_by_id?.toString().trim() || null,
    published_by_id: form.published_by_id?.toString().trim() || null,
    published_at: form.published_at,
  };

  return payload as BasePrintTemplateCreate;
}

/** Convert update form state into the payload that basePrintTemplateUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPrintTemplateUpdatePayload(form: PrintTemplateUpdateFormState): BasePrintTemplateUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.kind !== undefined) {
    payload.kind = form.kind?.toString().trim() ?? null;
  }
  if (form.scope !== undefined) {
    payload.scope = form.scope?.toString().trim() ?? null;
  }
  if (form.version !== undefined) {
    payload.version = form.version;
  }
  if (form.status !== undefined) {
    payload.status = form.status?.toString().trim() ?? null;
  }
  if (form.definition !== undefined) {
    payload.definition = form.definition;
  }
  if (form.shop_editable !== undefined) {
    payload.shop_editable = form.shop_editable;
  }
  if (form.effective_from !== undefined) {
    payload.effective_from = form.effective_from;
  }
  if (form.parent_version_id !== undefined) {
    payload.parent_version_id = form.parent_version_id?.toString().trim() ?? null;
  }
  if (form.notes !== undefined) {
    payload.notes = form.notes?.toString().trim() ?? null;
  }
  if (form.created_by_id !== undefined) {
    payload.created_by_id = form.created_by_id?.toString().trim() ?? null;
  }
  if (form.published_by_id !== undefined) {
    payload.published_by_id = form.published_by_id?.toString().trim() ?? null;
  }
  if (form.published_at !== undefined) {
    payload.published_at = form.published_at;
  }
  return payload as BasePrintTemplateUpdate;
}
