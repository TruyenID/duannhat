/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Table } from './Table';

/**
 * TableSession
 */
export interface TableSession {
  id: string;
  organization_id: string;
  branch_id: string;
  table_id: string;
  table?: Table;
  status: string;
  opened_at: DateTimeString;
  closed_at?: DateTimeString | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TableSession
 */
export const tableSessionI18n = {
  label: {"ja":"TableSession","en":"TableSession","vi":"TableSession"},
  fields: {
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
    branch_id: {
      label: {"ja":"支店ID","en":"Branch ID","vi":"ID chi nhánh"},
      placeholder: {"ja":"支店IDを入力","en":"Enter Branch ID","vi":"Nhập ID chi nhánh"},
    },
    table: {
      label: {"ja":"テーブル","en":"Table","vi":"Bàn"},
      placeholder: {"ja":"テーブルを入力","en":"Enter Table","vi":"Nhập Bàn"},
    },
    status: {
      label: {"ja":"状態","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"状態を入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    opened_at: {
      label: {"ja":"開始日時","en":"Opened At","vi":"Mở lúc"},
      placeholder: {"ja":"開始日時を入力","en":"Enter Opened At","vi":"Nhập Mở lúc"},
    },
    closed_at: {
      label: {"ja":"終了日時","en":"Closed At","vi":"Đóng lúc"},
      placeholder: {"ja":"終了日時を入力","en":"Enter Closed At","vi":"Nhập Đóng lúc"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TableSession */
export const baseTableSessionSchemas = {
  organization_id: z.string(),
  branch_id: z.string(),
  status: z.string().min(1).max(16),
  opened_at: z.string().datetime({ offset: true }),
  closed_at: z.string().datetime({ offset: true }).optional().nullable(),
} as const;

/** Create schema for TableSession */
export const baseTableSessionCreateSchema = z.object({
  organization_id: baseTableSessionSchemas.organization_id,
  branch_id: baseTableSessionSchemas.branch_id,
  status: baseTableSessionSchemas.status,
  opened_at: baseTableSessionSchemas.opened_at,
  closed_at: baseTableSessionSchemas.closed_at,
});

/** Update schema for TableSession */
export const baseTableSessionUpdateSchema = baseTableSessionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTableSessionCreate = z.infer<typeof baseTableSessionCreateSchema>;
export type BaseTableSessionUpdate = z.infer<typeof baseTableSessionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTableSessionLabel(locale: string): string {
  return tableSessionI18n.label[locale as keyof typeof tableSessionI18n.label] ?? tableSessionI18n.label['en'] ?? 'TableSession';
}

/** Get field label for a specific locale */
export function getTableSessionFieldLabel(field: string, locale: string): string {
  const fields = tableSessionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTableSessionFieldPlaceholder(field: string, locale: string): string {
  const fields = tableSessionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TableSession.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tableSessionMetadata = {
  modelName: "TableSession",
  table: "table_sessions",

  fields: {
    organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
    },
    branch_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "支店ID",
        en: "Branch ID",
        vi: "ID chi nhánh",
      },
      placeholder: {
        ja: "支店IDを入力",
        en: "Enter Branch ID",
        vi: "Nhập ID chi nhánh",
      },
    },
    table_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Table",
        table: "tables",
        onDelete: "CASCADE",
      },
      label: {},
    },
    status: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 16,
      },
      label: {
        ja: "状態",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "状態を入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    opened_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "開始日時",
        en: "Opened At",
        vi: "Mở lúc",
      },
      placeholder: {
        ja: "開始日時を入力",
        en: "Enter Opened At",
        vi: "Nhập Mở lúc",
      },
    },
    closed_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "終了日時",
        en: "Closed At",
        vi: "Đóng lúc",
      },
      placeholder: {
        ja: "終了日時を入力",
        en: "Enter Closed At",
        vi: "Nhập Đóng lúc",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "branch_id", "table_id", "status", "opened_at"] as const,
  enumFields: [] as const,
  relationFields: ["table_id"] as const,
} as const;

export type TableSessionFieldName = keyof typeof tableSessionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TableSession. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TableSessionCreateFormState {
  organization_id: string;
  branch_id: string;
  table_id: string;
  status: string;
  opened_at: string;
  closed_at?: string | null;
}

/** Form-state shape for updating a TableSession. Every field optional. */
export interface TableSessionUpdateFormState {
  organization_id?: string;
  branch_id?: string;
  table_id?: string;
  status?: string;
  opened_at?: string;
  closed_at?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTableSessionCreateForm(): TableSessionCreateFormState {
  return {
    organization_id: '',
    branch_id: '',
    table_id: '',
    status: "open",
    opened_at: '',
    closed_at: '',
  };
}

/** Convert create form state into the payload that baseTableSessionCreateSchema accepts. */
export function buildTableSessionCreatePayload(form: TableSessionCreateFormState): BaseTableSessionCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    branch_id: form.branch_id.trim(),
    table_id: form.table_id.trim(),
    status: form.status.trim(),
    opened_at: form.opened_at,
    closed_at: form.closed_at,
  };

  return payload as BaseTableSessionCreate;
}

/** Convert update form state into the payload that baseTableSessionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTableSessionUpdatePayload(form: TableSessionUpdateFormState): BaseTableSessionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.table_id !== undefined) {
    payload.table_id = form.table_id?.toString().trim() ?? null;
  }
  if (form.status !== undefined) {
    payload.status = form.status?.toString().trim() ?? null;
  }
  if (form.opened_at !== undefined) {
    payload.opened_at = form.opened_at;
  }
  if (form.closed_at !== undefined) {
    payload.closed_at = form.closed_at;
  }
  return payload as BaseTableSessionUpdate;
}
