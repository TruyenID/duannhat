/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Menu } from './Menu';
import type { MenuProductSku } from './MenuProductSku';
import type { MenuSection } from './MenuSection';
import type { Product } from './Product';
import type { TaxType } from './TaxType';

/**
 * メニュー商品
 */
export interface MenuProduct {
  id: string;
  menu_id: string;
  menu?: Menu;
  product_id: string;
  product?: Product;
  tax_type_id?: string | null;
  taxType?: TaxType | null;
  menu_section_id?: string | null;
  menuSection?: MenuSection | null;
  is_active: boolean;
  disabled_reason?: string | null;
  disabled_at?: DateTimeString | null;
  disabled_by_name?: string | null;
  display_order: number;
  master_menu_product_id?: string | null;
  masterMenuProduct?: MenuProduct | null;
  menuProductSkus?: MenuProductSku[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuProduct
 */
export const menuProductI18n = {
  label: {"ja":"メニュー商品","en":"Menu Product","vi":"Sản phẩm trong Menu"},
  fields: {
    menu: {
      label: {"ja":"メニュー","en":"Menu","vi":"Menu"},
      placeholder: {"ja":"メニューを入力","en":"Enter Menu","vi":"Nhập Menu"},
    },
    product: {
      label: {"ja":"商品","en":"Product","vi":"Sản phẩm"},
      placeholder: {"ja":"商品を入力","en":"Enter Product","vi":"Nhập Sản phẩm"},
    },
    tax_type: {
      label: {"ja":"税区分オーバーライド","en":"Tax Type Override","vi":"Loại thuế (override)"},
      placeholder: {"ja":"税区分オーバーライドを入力","en":"Enter Tax Type Override","vi":"Nhập Loại thuế (override)"},
    },
    menu_section: {
      label: {"ja":"メニューセクション","en":"Menu Section","vi":"Nhóm danh mục"},
      placeholder: {"ja":"メニューセクションを入力","en":"Enter Menu Section","vi":"Nhập Nhóm danh mục"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    disabled_reason: {
      label: {"ja":"停止理由","en":"Disabled Reason","vi":"Lý do tắt"},
      placeholder: {"ja":"停止理由を入力","en":"Enter Disabled Reason","vi":"Nhập Lý do tắt"},
    },
    disabled_at: {
      label: {"ja":"停止日時","en":"Disabled At","vi":"Thời điểm tắt"},
      placeholder: {"ja":"停止日時を入力","en":"Enter Disabled At","vi":"Nhập Thời điểm tắt"},
    },
    disabled_by_name: {
      label: {"ja":"停止操作者（スナップショット）","en":"Disabled By (snapshot)","vi":"Người tắt (ảnh chụp)"},
      placeholder: {"ja":"停止操作者（スナップショット）を入力","en":"Enter Disabled By (snapshot)","vi":"Nhập Người tắt (ảnh chụp)"},
    },
    display_order: {
      label: {"ja":"表示順序","en":"Display Order","vi":"Thứ tự hiển thị"},
      placeholder: {"ja":"表示順序を入力","en":"Enter Display Order","vi":"Nhập Thứ tự hiển thị"},
    },
    master_menu_product: {
      label: {"ja":"マスターメニュー商品","en":"Master Menu Product","vi":"Sản phẩm trong Master Menu"},
      placeholder: {"ja":"マスターメニュー商品を入力","en":"Enter Master Menu Product","vi":"Nhập Sản phẩm trong Master Menu"},
    },
    menu_product_skus: {
      label: {"ja":"メニュー商品SKU","en":"Menu Product SKUs","vi":"SKU của sản phẩm trong menu"},
      placeholder: {"ja":"メニュー商品SKUを入力","en":"Enter Menu Product SKUs","vi":"Nhập SKU của sản phẩm trong menu"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuProduct */
export const baseMenuProductSchemas = {
  is_active: z.boolean(),
  disabled_reason: z.string().max(255).optional().nullable(),
  disabled_at: z.string().datetime({ offset: true }).optional().nullable(),
  disabled_by_name: z.string().max(100).optional().nullable(),
  display_order: z.number().int(),
} as const;

/** Create schema for MenuProduct */
export const baseMenuProductCreateSchema = z.object({
  is_active: baseMenuProductSchemas.is_active,
  disabled_reason: baseMenuProductSchemas.disabled_reason,
  disabled_at: baseMenuProductSchemas.disabled_at,
  disabled_by_name: baseMenuProductSchemas.disabled_by_name,
  display_order: baseMenuProductSchemas.display_order,
});

/** Update schema for MenuProduct */
export const baseMenuProductUpdateSchema = baseMenuProductCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuProductCreate = z.infer<typeof baseMenuProductCreateSchema>;
export type BaseMenuProductUpdate = z.infer<typeof baseMenuProductUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuProductLabel(locale: string): string {
  return menuProductI18n.label[locale as keyof typeof menuProductI18n.label] ?? menuProductI18n.label['en'] ?? 'MenuProduct';
}

/** Get field label for a specific locale */
export function getMenuProductFieldLabel(field: string, locale: string): string {
  const fields = menuProductI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuProductFieldPlaceholder(field: string, locale: string): string {
  const fields = menuProductI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuProduct.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuProductMetadata = {
  modelName: "MenuProduct",
  table: "menu_products",

  fields: {
    menu_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Menu",
        table: "menus",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Product",
        table: "products",
        onDelete: "CASCADE",
      },
      label: {},
    },
    tax_type_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "TaxType",
        table: "tax_types",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    menu_section_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "MenuSection",
        table: "menu_sections",
        onDelete: "SET NULL",
      },
      label: {},
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
      description: {
        ja: "商品レベルのトグル。falseの場合、この商品のすべてのSKUは個別のis_activeに関わらず非表示になります。",
        en: "Product-level toggle. When false, all SKUs under this product are hidden regardless of their individual is_active.",
        vi: "Toggle cấp product. Khi false, tất cả SKU của product này bị ẩn bất kể is_active riêng của từng SKU.",
      },
    },
    disabled_reason: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "停止理由",
        en: "Disabled Reason",
        vi: "Lý do tắt",
      },
      placeholder: {
        ja: "停止理由を入力",
        en: "Enter Disabled Reason",
        vi: "Nhập Lý do tắt",
      },
      description: {
        ja: "現在停止している理由。自由記述で、最小文字数の制約はどの層にもない。",
        en: "Why this dish is currently off. Free text; NO minimum length is enforced at any layer.",
        vi: "Vì sao món đang tắt. Chữ tự do; KHÔNG tầng nào ràng buộc độ dài tối thiểu.",
      },
    },
    disabled_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "停止日時",
        en: "Disabled At",
        vi: "Thời điểm tắt",
      },
      placeholder: {
        ja: "停止日時を入力",
        en: "Enter Disabled At",
        vi: "Nhập Thời điểm tắt",
      },
      description: {
        ja: "店舗が停止した時刻。クラウド受信時刻ではなく操作者がタップした時刻（オフライン店舗は数時間後に同期）。",
        en: "When the shop turned it off — the moment the operator tapped, not the moment Cloud received it (an offline shop syncs hours later).",
        vi: "Lúc quán tắt món — thời điểm NGƯỜI BẤM bấm, không phải lúc Cloud nhận (quán offline có thể sync sau hàng giờ).",
      },
    },
    disabled_by_name: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "停止操作者（スナップショット）",
        en: "Disabled By (snapshot)",
        vi: "Người tắt (ảnh chụp)",
      },
      placeholder: {
        ja: "停止操作者（スナップショット）を入力",
        en: "Enter Disabled By (snapshot)",
        vi: "Nhập Người tắt (ảnh chụp)",
      },
    },
    display_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順序",
        en: "Display Order",
        vi: "Thứ tự hiển thị",
      },
      placeholder: {
        ja: "表示順序を入力",
        en: "Enter Display Order",
        vi: "Nhập Thứ tự hiển thị",
      },
      description: {
        ja: "メニュー内の表示順序（小さいほど先）",
        en: "Order for display within the menu (lower = first)",
        vi: "Thứ tự hiển thị trong menu (nhỏ hơn = trước)",
      },
    },
    master_menu_product_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "MenuProduct",
        table: "menu_products",
        onDelete: undefined,
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["menu_id", "product_id", "is_active", "display_order"] as const,
  enumFields: [] as const,
  relationFields: ["menu_id", "product_id", "tax_type_id", "menu_section_id", "master_menu_product_id"] as const,
} as const;

export type MenuProductFieldName = keyof typeof menuProductMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuProduct. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuProductCreateFormState {
  menu_id: string;
  product_id: string;
  tax_type_id?: string | null;
  menu_section_id?: string | null;
  is_active: boolean;
  disabled_reason?: string | null;
  disabled_at?: string | null;
  disabled_by_name?: string | null;
  display_order: number;
  master_menu_product_id?: string | null;
}

/** Form-state shape for updating a MenuProduct. Every field optional. */
export interface MenuProductUpdateFormState {
  menu_id?: string;
  product_id?: string;
  tax_type_id?: string | null;
  menu_section_id?: string | null;
  is_active?: boolean;
  disabled_reason?: string | null;
  disabled_at?: string | null;
  disabled_by_name?: string | null;
  display_order?: number;
  master_menu_product_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuProductCreateForm(): MenuProductCreateFormState {
  return {
    menu_id: '',
    product_id: '',
    tax_type_id: '',
    menu_section_id: '',
    is_active: true,
    disabled_reason: '',
    disabled_at: '',
    disabled_by_name: '',
    display_order: 0,
    master_menu_product_id: '',
  };
}

/** Convert create form state into the payload that baseMenuProductCreateSchema accepts. */
export function buildMenuProductCreatePayload(form: MenuProductCreateFormState): BaseMenuProductCreate {
  const payload: Record<string, unknown> = {
    menu_id: form.menu_id.trim(),
    product_id: form.product_id.trim(),
    tax_type_id: form.tax_type_id?.toString().trim() || null,
    menu_section_id: form.menu_section_id?.toString().trim() || null,
    is_active: form.is_active,
    disabled_reason: form.disabled_reason?.toString().trim() || null,
    disabled_at: form.disabled_at,
    disabled_by_name: form.disabled_by_name?.toString().trim() || null,
    display_order: form.display_order,
    master_menu_product_id: form.master_menu_product_id?.toString().trim() || null,
  };

  return payload as BaseMenuProductCreate;
}

/** Convert update form state into the payload that baseMenuProductUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuProductUpdatePayload(form: MenuProductUpdateFormState): BaseMenuProductUpdate {
  const payload: Record<string, unknown> = {};
  if (form.menu_id !== undefined) {
    payload.menu_id = form.menu_id?.toString().trim() ?? null;
  }
  if (form.product_id !== undefined) {
    payload.product_id = form.product_id?.toString().trim() ?? null;
  }
  if (form.tax_type_id !== undefined) {
    payload.tax_type_id = form.tax_type_id?.toString().trim() ?? null;
  }
  if (form.menu_section_id !== undefined) {
    payload.menu_section_id = form.menu_section_id?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.disabled_reason !== undefined) {
    payload.disabled_reason = form.disabled_reason?.toString().trim() ?? null;
  }
  if (form.disabled_at !== undefined) {
    payload.disabled_at = form.disabled_at;
  }
  if (form.disabled_by_name !== undefined) {
    payload.disabled_by_name = form.disabled_by_name?.toString().trim() ?? null;
  }
  if (form.display_order !== undefined) {
    payload.display_order = form.display_order;
  }
  if (form.master_menu_product_id !== undefined) {
    payload.master_menu_product_id = form.master_menu_product_id?.toString().trim() ?? null;
  }
  return payload as BaseMenuProductUpdate;
}
