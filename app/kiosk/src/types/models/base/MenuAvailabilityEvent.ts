/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { MenuAvailabilityEntityType } from '../enum/MenuAvailabilityEntityType';
import { MenuAvailabilitySource } from '../enum/MenuAvailabilitySource';
import type { Branch } from './Branch';
import type { MenuProduct } from './MenuProduct';
import type { User } from './User';

/**
 * メニュー在庫切れ操作履歴
 */
export interface MenuAvailabilityEvent {
  id: string;
  branch_id: string;
  branch?: Branch;
  menu_product_id?: string | null;
  menuProduct?: MenuProduct | null;
  entity_type: MenuAvailabilityEntityType;
  entity_id: string;
  is_active: boolean;
  reason?: string | null;
  source: MenuAvailabilitySource;
  occurred_at: DateTimeString;
  acted_by_user_id?: string | null;
  actedByUser?: User | null;
  actor_name?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuAvailabilityEvent
 */
export const menuAvailabilityEventI18n = {
  label: {"ja":"メニュー在庫切れ操作履歴","en":"Menu Availability Event","vi":"Nhật ký bật/tắt món"},
  fields: {
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    menu_product: {
      label: {"ja":"メニュー商品","en":"Menu Product","vi":"Món trong menu"},
      placeholder: {"ja":"メニュー商品を入力","en":"Enter Menu Product","vi":"Nhập Món trong menu"},
    },
    entity_type: {
      label: {"ja":"対象種別","en":"Entity Type","vi":"Loại đối tượng"},
      placeholder: {"ja":"対象種別を入力","en":"Enter Entity Type","vi":"Nhập Loại đối tượng"},
    },
    entity_id: {
      label: {"ja":"対象ID","en":"Entity ID","vi":"ID đối tượng"},
      placeholder: {"ja":"対象IDを入力","en":"Enter Entity ID","vi":"Nhập ID đối tượng"},
    },
    is_active: {
      label: {"ja":"変更後の状態","en":"New Active State","vi":"Trạng thái sau thao tác"},
      placeholder: {"ja":"変更後の状態を入力","en":"Enter New Active State","vi":"Nhập Trạng thái sau thao tác"},
    },
    reason: {
      label: {"ja":"理由","en":"Reason","vi":"Lý do"},
      placeholder: {"ja":"理由を入力","en":"Enter Reason","vi":"Nhập Lý do"},
    },
    source: {
      label: {"ja":"出所","en":"Source","vi":"Nguồn"},
      placeholder: {"ja":"出所を入力","en":"Enter Source","vi":"Nhập Nguồn"},
    },
    occurred_at: {
      label: {"ja":"操作日時","en":"Occurred At","vi":"Thời điểm thao tác"},
      placeholder: {"ja":"操作日時を入力","en":"Enter Occurred At","vi":"Nhập Thời điểm thao tác"},
    },
    acted_by_user: {
      label: {"ja":"操作者","en":"Acted By","vi":"Người thao tác"},
      placeholder: {"ja":"操作者を入力","en":"Enter Acted By","vi":"Nhập Người thao tác"},
    },
    actor_name: {
      label: {"ja":"操作者名（スナップショット）","en":"Actor Name (snapshot)","vi":"Tên người thao tác (ảnh chụp)"},
      placeholder: {"ja":"操作者名（スナップショット）を入力","en":"Enter Actor Name (snapshot)","vi":"Nhập Tên người thao tác (ảnh chụp)"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuAvailabilityEvent */
export const baseMenuAvailabilityEventSchemas = {
  entity_type: z.nativeEnum(MenuAvailabilityEntityType),
  entity_id: z.string().min(1).max(36),
  is_active: z.boolean(),
  reason: z.string().max(255).optional().nullable(),
  source: z.nativeEnum(MenuAvailabilitySource),
  occurred_at: z.string().datetime({ offset: true }),
  actor_name: z.string().max(100).optional().nullable(),
} as const;

/** Create schema for MenuAvailabilityEvent */
export const baseMenuAvailabilityEventCreateSchema = z.object({
  entity_type: baseMenuAvailabilityEventSchemas.entity_type,
  entity_id: baseMenuAvailabilityEventSchemas.entity_id,
  is_active: baseMenuAvailabilityEventSchemas.is_active,
  reason: baseMenuAvailabilityEventSchemas.reason,
  source: baseMenuAvailabilityEventSchemas.source,
  occurred_at: baseMenuAvailabilityEventSchemas.occurred_at,
  actor_name: baseMenuAvailabilityEventSchemas.actor_name,
});

/** Update schema for MenuAvailabilityEvent */
export const baseMenuAvailabilityEventUpdateSchema = baseMenuAvailabilityEventCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuAvailabilityEventCreate = z.infer<typeof baseMenuAvailabilityEventCreateSchema>;
export type BaseMenuAvailabilityEventUpdate = z.infer<typeof baseMenuAvailabilityEventUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuAvailabilityEventLabel(locale: string): string {
  return menuAvailabilityEventI18n.label[locale as keyof typeof menuAvailabilityEventI18n.label] ?? menuAvailabilityEventI18n.label['en'] ?? 'MenuAvailabilityEvent';
}

/** Get field label for a specific locale */
export function getMenuAvailabilityEventFieldLabel(field: string, locale: string): string {
  const fields = menuAvailabilityEventI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuAvailabilityEventFieldPlaceholder(field: string, locale: string): string {
  const fields = menuAvailabilityEventI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuAvailabilityEvent.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuAvailabilityEventMetadata = {
  modelName: "MenuAvailabilityEvent",
  table: "menu_availability_events",

  fields: {
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    menu_product_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "MenuProduct",
        table: "menu_products",
        onDelete: "SET NULL",
      },
      label: {},
    },
    entity_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "MenuAvailabilityEntityType",
      enumValues: ["menu_product", "menu_product_sku", "topping_item"] as const,
      label: {
        ja: "対象種別",
        en: "Entity Type",
        vi: "Loại đối tượng",
      },
      placeholder: {
        ja: "対象種別を入力",
        en: "Enter Entity Type",
        vi: "Nhập Loại đối tượng",
      },
    },
    entity_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 36,
      },
      label: {
        ja: "対象ID",
        en: "Entity ID",
        vi: "ID đối tượng",
      },
      placeholder: {
        ja: "対象IDを入力",
        en: "Enter Entity ID",
        vi: "Nhập ID đối tượng",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "変更後の状態",
        en: "New Active State",
        vi: "Trạng thái sau thao tác",
      },
      placeholder: {
        ja: "変更後の状態を入力",
        en: "Enter New Active State",
        vi: "Nhập Trạng thái sau thao tác",
      },
      description: {
        ja: "操作後の値（BR-MAE02）。トッピングは is_hidden の反転。",
        en: "The value AFTER the change (BR-MAE02). For toppings this is the inverse of is_hidden.",
        vi: "Giá trị SAU thao tác (BR-MAE02). Với topping đây là giá trị đảo của is_hidden.",
      },
    },
    reason: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "理由",
        en: "Reason",
        vi: "Lý do",
      },
      placeholder: {
        ja: "理由を入力",
        en: "Enter Reason",
        vi: "Nhập Lý do",
      },
      description: {
        ja: "自由記述。is_active が false のときのみ意味を持つ。最小文字数の制約は一切なし。",
        en: "Free text, only meaningful when is_active is false. No minimum length is enforced anywhere.",
        vi: "Chữ tự do, chỉ có nghĩa khi is_active = false. KHÔNG có ràng buộc độ dài tối thiểu ở bất kỳ tầng nào.",
      },
    },
    source: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "MenuAvailabilitySource",
      enumValues: ["pos", "workstation", "admin"] as const,
      label: {
        ja: "出所",
        en: "Source",
        vi: "Nguồn",
      },
      placeholder: {
        ja: "出所を入力",
        en: "Enter Source",
        vi: "Nhập Nguồn",
      },
    },
    occurred_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "操作日時",
        en: "Occurred At",
        vi: "Thời điểm thao tác",
      },
      placeholder: {
        ja: "操作日時を入力",
        en: "Enter Occurred At",
        vi: "Nhập Thời điểm thao tác",
      },
    },
    acted_by_user_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
    actor_name: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "操作者名（スナップショット）",
        en: "Actor Name (snapshot)",
        vi: "Tên người thao tác (ảnh chụp)",
      },
      placeholder: {
        ja: "操作者名（スナップショット）を入力",
        en: "Enter Actor Name (snapshot)",
        vi: "Nhập Tên người thao tác (ảnh chụp)",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["branch_id", "entity_type", "entity_id", "is_active", "source", "occurred_at"] as const,
  enumFields: ["entity_type", "source"] as const,
  relationFields: ["branch_id", "menu_product_id", "acted_by_user_id"] as const,
} as const;

export type MenuAvailabilityEventFieldName = keyof typeof menuAvailabilityEventMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuAvailabilityEvent. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuAvailabilityEventCreateFormState {
  branch_id: string;
  menu_product_id?: string | null;
  entity_type: MenuAvailabilityEntityType;
  entity_id: string;
  is_active: boolean;
  reason?: string | null;
  source: MenuAvailabilitySource;
  occurred_at: string;
  acted_by_user_id?: string | null;
  actor_name?: string | null;
}

/** Form-state shape for updating a MenuAvailabilityEvent. Every field optional. */
export interface MenuAvailabilityEventUpdateFormState {
  branch_id?: string;
  menu_product_id?: string | null;
  entity_type?: MenuAvailabilityEntityType;
  entity_id?: string;
  is_active?: boolean;
  reason?: string | null;
  source?: MenuAvailabilitySource;
  occurred_at?: string;
  acted_by_user_id?: string | null;
  actor_name?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuAvailabilityEventCreateForm(): MenuAvailabilityEventCreateFormState {
  return {
    branch_id: '',
    menu_product_id: '',
    entity_type: MenuAvailabilityEntityType.MenuProduct,
    entity_id: '',
    is_active: false,
    reason: '',
    source: MenuAvailabilitySource.Pos,
    occurred_at: '',
    acted_by_user_id: '',
    actor_name: '',
  };
}

/** Convert create form state into the payload that baseMenuAvailabilityEventCreateSchema accepts. */
export function buildMenuAvailabilityEventCreatePayload(form: MenuAvailabilityEventCreateFormState): BaseMenuAvailabilityEventCreate {
  const payload: Record<string, unknown> = {
    branch_id: form.branch_id.trim(),
    menu_product_id: form.menu_product_id?.toString().trim() || null,
    entity_type: form.entity_type,
    entity_id: form.entity_id.trim(),
    is_active: form.is_active,
    reason: form.reason?.toString().trim() || null,
    source: form.source,
    occurred_at: form.occurred_at,
    acted_by_user_id: form.acted_by_user_id?.toString().trim() || null,
    actor_name: form.actor_name?.toString().trim() || null,
  };

  return payload as BaseMenuAvailabilityEventCreate;
}

/** Convert update form state into the payload that baseMenuAvailabilityEventUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuAvailabilityEventUpdatePayload(form: MenuAvailabilityEventUpdateFormState): BaseMenuAvailabilityEventUpdate {
  const payload: Record<string, unknown> = {};
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.menu_product_id !== undefined) {
    payload.menu_product_id = form.menu_product_id?.toString().trim() ?? null;
  }
  if (form.entity_type !== undefined) {
    payload.entity_type = form.entity_type;
  }
  if (form.entity_id !== undefined) {
    payload.entity_id = form.entity_id?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.reason !== undefined) {
    payload.reason = form.reason?.toString().trim() ?? null;
  }
  if (form.source !== undefined) {
    payload.source = form.source;
  }
  if (form.occurred_at !== undefined) {
    payload.occurred_at = form.occurred_at;
  }
  if (form.acted_by_user_id !== undefined) {
    payload.acted_by_user_id = form.acted_by_user_id?.toString().trim() ?? null;
  }
  if (form.actor_name !== undefined) {
    payload.actor_name = form.actor_name?.toString().trim() ?? null;
  }
  return payload as BaseMenuAvailabilityEventUpdate;
}
