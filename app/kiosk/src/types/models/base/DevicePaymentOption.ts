/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Device } from './Device';
import type { Organization } from './Organization';
import type { ShopPaymentOption } from './ShopPaymentOption';

/**
 * デバイス決済オプション
 */
export interface DevicePaymentOption {
  id: string;
  organization_id: string;
  organization?: Organization;
  branch_id: string;
  branch?: Branch;
  device_id: string;
  device?: Device;
  shop_payment_option_id: string;
  shop_payment_option?: ShopPaymentOption;
  preference: 'inherit' | 'disabled';
  change_reason?: string | null;
  version: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for DevicePaymentOption
 */
export const devicePaymentOptionI18n = {
  label: {"ja":"デバイス決済オプション","en":"Device Payment Option","vi":"Tuỳ chọn thanh toán thiết bị"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branch: {
      label: {"ja":"店舗","en":"Shop","vi":"Cửa hàng"},
      placeholder: {"ja":"店舗を入力","en":"Enter Shop","vi":"Nhập Cửa hàng"},
    },
    device: {
      label: {"ja":"デバイス","en":"Device","vi":"Thiết bị"},
      placeholder: {"ja":"デバイスを入力","en":"Enter Device","vi":"Nhập Thiết bị"},
    },
    shop_payment_option: {
      label: {"ja":"店舗決済オプション","en":"Shop Payment Option","vi":"Tuỳ chọn cửa hàng"},
      placeholder: {"ja":"店舗決済オプションを入力","en":"Enter Shop Payment Option","vi":"Nhập Tuỳ chọn cửa hàng"},
    },
    preference: {
      label: {"ja":"制限","en":"Restriction","vi":"Hạn chế"},
      placeholder: {"ja":"制限を入力","en":"Enter Restriction","vi":"Nhập Hạn chế"},
    },
    change_reason: {
      label: {"ja":"変更理由","en":"Change Reason","vi":"Lý do thay đổi"},
      placeholder: {"ja":"変更理由を入力","en":"Enter Change Reason","vi":"Nhập Lý do thay đổi"},
    },
    version: {
      label: {"ja":"バージョン","en":"Version","vi":"Phiên bản"},
      placeholder: {"ja":"バージョンを入力","en":"Enter Version","vi":"Nhập Phiên bản"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for DevicePaymentOption */
export const baseDevicePaymentOptionSchemas = {
  preference: z.enum(['inherit', 'disabled']),
  change_reason: z.string().max(500).optional().nullable(),
  version: z.number().int().gte(1),
} as const;

/** Create schema for DevicePaymentOption */
export const baseDevicePaymentOptionCreateSchema = z.object({
  preference: baseDevicePaymentOptionSchemas.preference,
  change_reason: baseDevicePaymentOptionSchemas.change_reason,
  version: baseDevicePaymentOptionSchemas.version,
});

/** Update schema for DevicePaymentOption */
export const baseDevicePaymentOptionUpdateSchema = baseDevicePaymentOptionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseDevicePaymentOptionCreate = z.infer<typeof baseDevicePaymentOptionCreateSchema>;
export type BaseDevicePaymentOptionUpdate = z.infer<typeof baseDevicePaymentOptionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getDevicePaymentOptionLabel(locale: string): string {
  return devicePaymentOptionI18n.label[locale as keyof typeof devicePaymentOptionI18n.label] ?? devicePaymentOptionI18n.label['en'] ?? 'DevicePaymentOption';
}

/** Get field label for a specific locale */
export function getDevicePaymentOptionFieldLabel(field: string, locale: string): string {
  const fields = devicePaymentOptionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getDevicePaymentOptionFieldPlaceholder(field: string, locale: string): string {
  const fields = devicePaymentOptionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for DevicePaymentOption.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const devicePaymentOptionMetadata = {
  modelName: "DevicePaymentOption",
  table: "device_payment_options",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    device_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Device",
        table: "devices",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    shop_payment_option_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ShopPaymentOption",
        table: "shop_payment_options",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    preference: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enumValues: ["inherit", "disabled"] as const,
      label: {
        ja: "制限",
        en: "Restriction",
        vi: "Hạn chế",
      },
      placeholder: {
        ja: "制限を入力",
        en: "Enter Restriction",
        vi: "Nhập Hạn chế",
      },
    },
    change_reason: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 500,
      },
      label: {
        ja: "変更理由",
        en: "Change Reason",
        vi: "Lý do thay đổi",
      },
      placeholder: {
        ja: "変更理由を入力",
        en: "Enter Change Reason",
        vi: "Nhập Lý do thay đổi",
      },
    },
    version: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        min: 1,
      },
      label: {
        ja: "バージョン",
        en: "Version",
        vi: "Phiên bản",
      },
      placeholder: {
        ja: "バージョンを入力",
        en: "Enter Version",
        vi: "Nhập Phiên bản",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "branch_id", "device_id", "shop_payment_option_id", "preference", "version"] as const,
  enumFields: ["preference"] as const,
  relationFields: ["organization_id", "branch_id", "device_id", "shop_payment_option_id"] as const,
} as const;

export type DevicePaymentOptionFieldName = keyof typeof devicePaymentOptionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a DevicePaymentOption. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface DevicePaymentOptionCreateFormState {
  organization_id: string;
  branch_id: string;
  device_id: string;
  shop_payment_option_id: string;
  preference: 'inherit' | 'disabled';
  change_reason?: string | null;
  version: number;
}

/** Form-state shape for updating a DevicePaymentOption. Every field optional. */
export interface DevicePaymentOptionUpdateFormState {
  organization_id?: string;
  branch_id?: string;
  device_id?: string;
  shop_payment_option_id?: string;
  preference?: 'inherit' | 'disabled';
  change_reason?: string | null;
  version?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyDevicePaymentOptionCreateForm(): DevicePaymentOptionCreateFormState {
  return {
    organization_id: '',
    branch_id: '',
    device_id: '',
    shop_payment_option_id: '',
    preference: "inherit",
    change_reason: '',
    version: 1,
  };
}

/** Convert create form state into the payload that baseDevicePaymentOptionCreateSchema accepts. */
export function buildDevicePaymentOptionCreatePayload(form: DevicePaymentOptionCreateFormState): BaseDevicePaymentOptionCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    branch_id: form.branch_id.trim(),
    device_id: form.device_id.trim(),
    shop_payment_option_id: form.shop_payment_option_id.trim(),
    preference: form.preference,
    change_reason: form.change_reason?.toString().trim() || null,
    version: form.version,
  };

  return payload as BaseDevicePaymentOptionCreate;
}

/** Convert update form state into the payload that baseDevicePaymentOptionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildDevicePaymentOptionUpdatePayload(form: DevicePaymentOptionUpdateFormState): BaseDevicePaymentOptionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.device_id !== undefined) {
    payload.device_id = form.device_id?.toString().trim() ?? null;
  }
  if (form.shop_payment_option_id !== undefined) {
    payload.shop_payment_option_id = form.shop_payment_option_id?.toString().trim() ?? null;
  }
  if (form.preference !== undefined) {
    payload.preference = form.preference;
  }
  if (form.change_reason !== undefined) {
    payload.change_reason = form.change_reason?.toString().trim() ?? null;
  }
  if (form.version !== undefined) {
    payload.version = form.version;
  }
  return payload as BaseDevicePaymentOptionUpdate;
}
