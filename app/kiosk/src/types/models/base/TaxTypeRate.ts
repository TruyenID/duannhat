/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString, DateString } from '../common';
import type { TaxType } from './TaxType';

/**
 * 税率適用期間
 */
export interface TaxTypeRate {
  id: string;
  rate: number;
  effective_from: DateString;
  effective_to?: DateString | null;
  tax_type_id: string;
  taxType?: TaxType;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TaxTypeRate
 */
export const taxTypeRateI18n = {
  label: {"ja":"税率適用期間","en":"Tax Rate Period","vi":"Kỳ hiệu lực thuế suất"},
  fields: {
    rate: {
      label: {"ja":"税率 (%)","en":"Rate (%)","vi":"Thuế suất (%)"},
      placeholder: {"ja":"税率 (%)を入力","en":"Enter Rate (%)","vi":"Nhập Thuế suất (%)"},
    },
    effective_from: {
      label: {"ja":"適用開始日","en":"Effective From","vi":"Hiệu lực từ"},
      placeholder: {"ja":"適用開始日を入力","en":"Enter Effective From","vi":"Nhập Hiệu lực từ"},
    },
    effective_to: {
      label: {"ja":"適用終了日","en":"Effective To","vi":"Hiệu lực đến"},
      placeholder: {"ja":"適用終了日を入力","en":"Enter Effective To","vi":"Nhập Hiệu lực đến"},
    },
    tax_type: {
      label: {"ja":"税区分","en":"Tax Type","vi":"Loại thuế"},
      placeholder: {"ja":"税区分を入力","en":"Enter Tax Type","vi":"Nhập Loại thuế"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TaxTypeRate */
export const baseTaxTypeRateSchemas = {
  rate: z.number(),
  effective_from: z.string().date(),
  effective_to: z.string().date().optional().nullable(),
} as const;

/** Create schema for TaxTypeRate */
export const baseTaxTypeRateCreateSchema = z.object({
  rate: baseTaxTypeRateSchemas.rate,
  effective_from: baseTaxTypeRateSchemas.effective_from,
  effective_to: baseTaxTypeRateSchemas.effective_to,
});

/** Update schema for TaxTypeRate */
export const baseTaxTypeRateUpdateSchema = baseTaxTypeRateCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTaxTypeRateCreate = z.infer<typeof baseTaxTypeRateCreateSchema>;
export type BaseTaxTypeRateUpdate = z.infer<typeof baseTaxTypeRateUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTaxTypeRateLabel(locale: string): string {
  return taxTypeRateI18n.label[locale as keyof typeof taxTypeRateI18n.label] ?? taxTypeRateI18n.label['en'] ?? 'TaxTypeRate';
}

/** Get field label for a specific locale */
export function getTaxTypeRateFieldLabel(field: string, locale: string): string {
  const fields = taxTypeRateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTaxTypeRateFieldPlaceholder(field: string, locale: string): string {
  const fields = taxTypeRateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TaxTypeRate.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const taxTypeRateMetadata = {
  modelName: "TaxTypeRate",
  table: "tax_type_rates",

  fields: {
    rate: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "税率 (%)",
        en: "Rate (%)",
        vi: "Thuế suất (%)",
      },
      placeholder: {
        ja: "税率 (%)を入力",
        en: "Enter Rate (%)",
        vi: "Nhập Thuế suất (%)",
      },
    },
    effective_from: {
      type: "date",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "適用開始日",
        en: "Effective From",
        vi: "Hiệu lực từ",
      },
      placeholder: {
        ja: "適用開始日を入力",
        en: "Enter Effective From",
        vi: "Nhập Hiệu lực từ",
      },
    },
    effective_to: {
      type: "date",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "適用終了日",
        en: "Effective To",
        vi: "Hiệu lực đến",
      },
      placeholder: {
        ja: "適用終了日を入力",
        en: "Enter Effective To",
        vi: "Nhập Hiệu lực đến",
      },
    },
    tax_type_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "TaxType",
        table: "tax_types",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["rate", "effective_from", "tax_type_id"] as const,
  enumFields: [] as const,
  relationFields: ["tax_type_id"] as const,
} as const;

export type TaxTypeRateFieldName = keyof typeof taxTypeRateMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TaxTypeRate. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TaxTypeRateCreateFormState {
  rate: number;
  effective_from: string;
  effective_to?: string | null;
  tax_type_id: string;
}

/** Form-state shape for updating a TaxTypeRate. Every field optional. */
export interface TaxTypeRateUpdateFormState {
  rate?: number;
  effective_from?: string;
  effective_to?: string | null;
  tax_type_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTaxTypeRateCreateForm(): TaxTypeRateCreateFormState {
  return {
    rate: 0,
    effective_from: '',
    effective_to: '',
    tax_type_id: '',
  };
}

/** Convert create form state into the payload that baseTaxTypeRateCreateSchema accepts. */
export function buildTaxTypeRateCreatePayload(form: TaxTypeRateCreateFormState): BaseTaxTypeRateCreate {
  const payload: Record<string, unknown> = {
    rate: form.rate,
    effective_from: form.effective_from,
    effective_to: form.effective_to,
    tax_type_id: form.tax_type_id.trim(),
  };

  return payload as BaseTaxTypeRateCreate;
}

/** Convert update form state into the payload that baseTaxTypeRateUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTaxTypeRateUpdatePayload(form: TaxTypeRateUpdateFormState): BaseTaxTypeRateUpdate {
  const payload: Record<string, unknown> = {};
  if (form.rate !== undefined) {
    payload.rate = form.rate;
  }
  if (form.effective_from !== undefined) {
    payload.effective_from = form.effective_from;
  }
  if (form.effective_to !== undefined) {
    payload.effective_to = form.effective_to;
  }
  if (form.tax_type_id !== undefined) {
    payload.tax_type_id = form.tax_type_id?.toString().trim() ?? null;
  }
  return payload as BaseTaxTypeRateUpdate;
}
