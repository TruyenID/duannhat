/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { PointEntryKind } from '../enum/PointEntryKind';
import type { Coupon } from './Coupon';
import type { Customer } from './Customer';
import type { CustomerOrder } from './CustomerOrder';
import type { Organization } from './Organization';
import type { PointReward } from './PointReward';

/**
 * ポイント履歴
 */
export interface CustomerPointEntry {
  id: string;
  points: number;
  kind: PointEntryKind;
  note?: string | null;
  expires_at?: DateTimeString | null;
  customer_id: string;
  customer?: Customer;
  organization_id?: string | null;
  organization?: Organization | null;
  customer_order_id?: string | null;
  customerOrder?: CustomerOrder | null;
  coupon_id?: string | null;
  coupon?: Coupon | null;
  point_reward_id?: string | null;
  pointReward?: PointReward | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for CustomerPointEntry
 */
export const customerPointEntryI18n = {
  label: {"ja":"ポイント履歴","en":"Customer Point Entry","vi":"Lịch sử điểm"},
  fields: {
    points: {
      label: {"ja":"ポイント","en":"Points","vi":"Điểm"},
      placeholder: {"ja":"ポイントを入力","en":"Enter Points","vi":"Nhập Điểm"},
    },
    kind: {
      label: {"ja":"種別","en":"Kind","vi":"Loại"},
      placeholder: {"ja":"種別を入力","en":"Enter Kind","vi":"Nhập Loại"},
    },
    note: {
      label: {"ja":"備考","en":"Note","vi":"Ghi chú"},
      placeholder: {"ja":"備考を入力","en":"Enter Note","vi":"Nhập Ghi chú"},
    },
    expires_at: {
      label: {"ja":"有効期限","en":"Expires At","vi":"Hạn dùng"},
      placeholder: {"ja":"有効期限を入力","en":"Enter Expires At","vi":"Nhập Hạn dùng"},
    },
    customer: {
      label: {"ja":"顧客","en":"Customer","vi":"Khách hàng"},
      placeholder: {"ja":"顧客を入力","en":"Enter Customer","vi":"Nhập Khách hàng"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    customer_order: {
      label: {"ja":"注文","en":"Customer Order","vi":"Đơn hàng"},
      placeholder: {"ja":"注文を入力","en":"Enter Customer Order","vi":"Nhập Đơn hàng"},
    },
    coupon: {
      label: {"ja":"クーポン","en":"Coupon","vi":"Coupon"},
      placeholder: {"ja":"クーポンを入力","en":"Enter Coupon","vi":"Nhập Coupon"},
    },
    point_reward: {
      label: {"ja":"交換商品","en":"Point Reward","vi":"Phần thưởng"},
      placeholder: {"ja":"交換商品を入力","en":"Enter Point Reward","vi":"Nhập Phần thưởng"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for CustomerPointEntry */
export const baseCustomerPointEntrySchemas = {
  points: z.number().int(),
  kind: z.nativeEnum(PointEntryKind),
  note: z.string().max(255).optional().nullable(),
  expires_at: z.string().datetime({ offset: true }).optional().nullable(),
} as const;

/** Create schema for CustomerPointEntry */
export const baseCustomerPointEntryCreateSchema = z.object({
  points: baseCustomerPointEntrySchemas.points,
  kind: baseCustomerPointEntrySchemas.kind,
  note: baseCustomerPointEntrySchemas.note,
  expires_at: baseCustomerPointEntrySchemas.expires_at,
});

/** Update schema for CustomerPointEntry */
export const baseCustomerPointEntryUpdateSchema = baseCustomerPointEntryCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseCustomerPointEntryCreate = z.infer<typeof baseCustomerPointEntryCreateSchema>;
export type BaseCustomerPointEntryUpdate = z.infer<typeof baseCustomerPointEntryUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getCustomerPointEntryLabel(locale: string): string {
  return customerPointEntryI18n.label[locale as keyof typeof customerPointEntryI18n.label] ?? customerPointEntryI18n.label['en'] ?? 'CustomerPointEntry';
}

/** Get field label for a specific locale */
export function getCustomerPointEntryFieldLabel(field: string, locale: string): string {
  const fields = customerPointEntryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getCustomerPointEntryFieldPlaceholder(field: string, locale: string): string {
  const fields = customerPointEntryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for CustomerPointEntry.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const customerPointEntryMetadata = {
  modelName: "CustomerPointEntry",
  table: "customer_point_entries",

  fields: {
    points: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ポイント",
        en: "Points",
        vi: "Điểm",
      },
      placeholder: {
        ja: "ポイントを入力",
        en: "Enter Points",
        vi: "Nhập Điểm",
      },
    },
    kind: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "PointEntryKind",
      enumValues: ["earn", "redeem", "revoke", "adjust", "expire"] as const,
      label: {
        ja: "種別",
        en: "Kind",
        vi: "Loại",
      },
      placeholder: {
        ja: "種別を入力",
        en: "Enter Kind",
        vi: "Nhập Loại",
      },
    },
    note: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "備考",
        en: "Note",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "備考を入力",
        en: "Enter Note",
        vi: "Nhập Ghi chú",
      },
    },
    expires_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "有効期限",
        en: "Expires At",
        vi: "Hạn dùng",
      },
      placeholder: {
        ja: "有効期限を入力",
        en: "Enter Expires At",
        vi: "Nhập Hạn dùng",
      },
    },
    customer_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Customer",
        table: "customers",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "SET NULL",
      },
      label: {},
    },
    customer_order_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrder",
        table: "customer_orders",
        onDelete: "SET NULL",
      },
      label: {},
    },
    coupon_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Coupon",
        table: "coupons",
        onDelete: "SET NULL",
      },
      label: {},
    },
    point_reward_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "PointReward",
        table: "point_rewards",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["points", "kind", "customer_id"] as const,
  enumFields: ["kind"] as const,
  relationFields: ["customer_id", "organization_id", "customer_order_id", "coupon_id", "point_reward_id"] as const,
} as const;

export type CustomerPointEntryFieldName = keyof typeof customerPointEntryMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a CustomerPointEntry. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface CustomerPointEntryCreateFormState {
  points: number;
  kind: PointEntryKind;
  note?: string | null;
  expires_at?: string | null;
  customer_id: string;
  organization_id?: string | null;
  customer_order_id?: string | null;
  coupon_id?: string | null;
  point_reward_id?: string | null;
}

/** Form-state shape for updating a CustomerPointEntry. Every field optional. */
export interface CustomerPointEntryUpdateFormState {
  points?: number;
  kind?: PointEntryKind;
  note?: string | null;
  expires_at?: string | null;
  customer_id?: string;
  organization_id?: string | null;
  customer_order_id?: string | null;
  coupon_id?: string | null;
  point_reward_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyCustomerPointEntryCreateForm(): CustomerPointEntryCreateFormState {
  return {
    points: 0,
    kind: PointEntryKind.Earn,
    note: '',
    expires_at: '',
    customer_id: '',
    organization_id: '',
    customer_order_id: '',
    coupon_id: '',
    point_reward_id: '',
  };
}

/** Convert create form state into the payload that baseCustomerPointEntryCreateSchema accepts. */
export function buildCustomerPointEntryCreatePayload(form: CustomerPointEntryCreateFormState): BaseCustomerPointEntryCreate {
  const payload: Record<string, unknown> = {
    points: form.points,
    kind: form.kind,
    note: form.note?.toString().trim() || null,
    expires_at: form.expires_at,
    customer_id: form.customer_id.trim(),
    organization_id: form.organization_id?.toString().trim() || null,
    customer_order_id: form.customer_order_id?.toString().trim() || null,
    coupon_id: form.coupon_id?.toString().trim() || null,
    point_reward_id: form.point_reward_id?.toString().trim() || null,
  };

  return payload as BaseCustomerPointEntryCreate;
}

/** Convert update form state into the payload that baseCustomerPointEntryUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildCustomerPointEntryUpdatePayload(form: CustomerPointEntryUpdateFormState): BaseCustomerPointEntryUpdate {
  const payload: Record<string, unknown> = {};
  if (form.points !== undefined) {
    payload.points = form.points;
  }
  if (form.kind !== undefined) {
    payload.kind = form.kind;
  }
  if (form.note !== undefined) {
    payload.note = form.note?.toString().trim() ?? null;
  }
  if (form.expires_at !== undefined) {
    payload.expires_at = form.expires_at;
  }
  if (form.customer_id !== undefined) {
    payload.customer_id = form.customer_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.customer_order_id !== undefined) {
    payload.customer_order_id = form.customer_order_id?.toString().trim() ?? null;
  }
  if (form.coupon_id !== undefined) {
    payload.coupon_id = form.coupon_id?.toString().trim() ?? null;
  }
  if (form.point_reward_id !== undefined) {
    payload.point_reward_id = form.point_reward_id?.toString().trim() ?? null;
  }
  return payload as BaseCustomerPointEntryUpdate;
}
