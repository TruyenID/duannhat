/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * MenuPromotionProduct
 */
export interface MenuPromotionProduct {
  id: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuPromotionProduct
 */
export const menuPromotionProductI18n = {
  label: {"ja":"MenuPromotionProduct","en":"MenuPromotionProduct","vi":"MenuPromotionProduct"},
  fields: {
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuPromotionProduct */
export const baseMenuPromotionProductSchemas = {
} as const;

/** Create schema for MenuPromotionProduct */
export const baseMenuPromotionProductCreateSchema = z.object({
});

/** Update schema for MenuPromotionProduct */
export const baseMenuPromotionProductUpdateSchema = baseMenuPromotionProductCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuPromotionProductCreate = z.infer<typeof baseMenuPromotionProductCreateSchema>;
export type BaseMenuPromotionProductUpdate = z.infer<typeof baseMenuPromotionProductUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuPromotionProductLabel(locale: string): string {
  return menuPromotionProductI18n.label[locale as keyof typeof menuPromotionProductI18n.label] ?? menuPromotionProductI18n.label['en'] ?? 'MenuPromotionProduct';
}

/** Get field label for a specific locale */
export function getMenuPromotionProductFieldLabel(field: string, locale: string): string {
  const fields = menuPromotionProductI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuPromotionProductFieldPlaceholder(field: string, locale: string): string {
  const fields = menuPromotionProductI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuPromotionProduct.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuPromotionProductMetadata = {
  modelName: "MenuPromotionProduct",
  table: "menu_promotion_product",

  fields: {
  },

  translatableFields: [] as const,
  requiredFields: [] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type MenuPromotionProductFieldName = keyof typeof menuPromotionProductMetadata.fields;

