/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { AllergenJurisdiction } from '../enum/AllergenJurisdiction';
import { AllergenSeverity } from '../enum/AllergenSeverity';
import type { Material } from './Material';
import type { Organization } from './Organization';

/**
 * アレルゲン
 */
export interface Allergen {
  id: string;
  code: string;
  name: string;
  jurisdiction: AllergenJurisdiction;
  severity: AllergenSeverity;
  is_active: boolean;
  materials?: Material[];
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Allergen
 */
export const allergenI18n = {
  label: {"ja":"アレルゲン","en":"Allergen","vi":"Chất gây dị ứng"},
  fields: {
    code: {
      label: {"ja":"コード","en":"Code","vi":"Mã"},
      placeholder: {"ja":"コードを入力","en":"Enter Code","vi":"Nhập Mã"},
    },
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    jurisdiction: {
      label: {"ja":"管轄","en":"Jurisdiction","vi":"Thẩm quyền"},
      placeholder: {"ja":"管轄を入力","en":"Enter Jurisdiction","vi":"Nhập Thẩm quyền"},
    },
    severity: {
      label: {"ja":"重要度","en":"Severity","vi":"Mức độ"},
      placeholder: {"ja":"重要度を入力","en":"Enter Severity","vi":"Nhập Mức độ"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    materials: {
      label: {"ja":"関連材料","en":"Materials","vi":"Nguyên liệu liên quan"},
      placeholder: {"ja":"関連材料を入力","en":"Enter Materials","vi":"Nhập Nguyên liệu liên quan"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Allergen */
export const baseAllergenSchemas = {
  code: z.string().min(1).max(60),
  name: z.string().min(1).max(120),
  jurisdiction: z.nativeEnum(AllergenJurisdiction),
  severity: z.nativeEnum(AllergenSeverity),
  is_active: z.boolean(),
} as const;

/** Create schema for Allergen */
export const baseAllergenCreateSchema = z.object({
  code: baseAllergenSchemas.code,
  name: baseAllergenSchemas.name,
  jurisdiction: baseAllergenSchemas.jurisdiction,
  severity: baseAllergenSchemas.severity,
  is_active: baseAllergenSchemas.is_active,
  ja: z.object({
    name: z.string().max(120).optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(120).optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(120).optional().nullable(),
  }).optional(),
});

/** Update schema for Allergen */
export const baseAllergenUpdateSchema = baseAllergenCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseAllergenCreate = z.infer<typeof baseAllergenCreateSchema>;
export type BaseAllergenUpdate = z.infer<typeof baseAllergenUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getAllergenLabel(locale: string): string {
  return allergenI18n.label[locale as keyof typeof allergenI18n.label] ?? allergenI18n.label['en'] ?? 'Allergen';
}

/** Get field label for a specific locale */
export function getAllergenFieldLabel(field: string, locale: string): string {
  const fields = allergenI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getAllergenFieldPlaceholder(field: string, locale: string): string {
  const fields = allergenI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Allergen.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const allergenMetadata = {
  modelName: "Allergen",
  table: "allergens",

  fields: {
    code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 60,
      },
      label: {
        ja: "コード",
        en: "Code",
        vi: "Mã",
      },
      placeholder: {
        ja: "コードを入力",
        en: "Enter Code",
        vi: "Nhập Mã",
      },
      description: {
        ja: "管轄内で固定される snake_case 識別子 (例 \"milk\")。",
        en: "Stable snake_case identifier within a jurisdiction (e.g. \"milk\").",
        vi: "Định danh snake_case cố định trong thẩm quyền (vd \"milk\").",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 120,
      },
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    jurisdiction: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "AllergenJurisdiction",
      enumValues: ["jp", "eu", "us"] as const,
      label: {
        ja: "管轄",
        en: "Jurisdiction",
        vi: "Thẩm quyền",
      },
      placeholder: {
        ja: "管轄を入力",
        en: "Enter Jurisdiction",
        vi: "Nhập Thẩm quyền",
      },
    },
    severity: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "AllergenSeverity",
      enumValues: ["mandatory", "recommended"] as const,
      label: {
        ja: "重要度",
        en: "Severity",
        vi: "Mức độ",
      },
      placeholder: {
        ja: "重要度を入力",
        en: "Enter Severity",
        vi: "Nhập Mức độ",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name"] as const,
  requiredFields: ["code", "name", "jurisdiction", "severity", "is_active", "organization_id"] as const,
  enumFields: ["jurisdiction", "severity"] as const,
  relationFields: ["organization_id"] as const,
} as const;

export type AllergenFieldName = keyof typeof allergenMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Allergen. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface AllergenCreateFormState {
  code: string;
  name: Record<Locale, string>;
  jurisdiction: AllergenJurisdiction;
  severity: AllergenSeverity;
  is_active: boolean;
  organization_id: string;
}

/** Form-state shape for updating a Allergen. Every field optional. */
export interface AllergenUpdateFormState {
  code?: string;
  name?: Record<Locale, string>;
  jurisdiction?: AllergenJurisdiction;
  severity?: AllergenSeverity;
  is_active?: boolean;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyAllergenCreateForm(): AllergenCreateFormState {
  return {
    code: '',
    name: emptyLocaleMap(),
    jurisdiction: AllergenJurisdiction.Jp,
    severity: AllergenSeverity.Mandatory,
    is_active: true,
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseAllergenCreateSchema accepts. */
export function buildAllergenCreatePayload(form: AllergenCreateFormState): BaseAllergenCreate {
  const payload: Record<string, unknown> = {
    code: form.code.trim(),
    name: (form.name["ja"] ?? '').trim(),
    jurisdiction: form.jurisdiction,
    severity: form.severity,
    is_active: form.is_active,
    organization_id: form.organization_id.trim(),
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
  });
  Object.assign(payload, i18n);

  return payload as BaseAllergenCreate;
}

/** Convert update form state into the payload that baseAllergenUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildAllergenUpdatePayload(form: AllergenUpdateFormState): BaseAllergenUpdate {
  const payload: Record<string, unknown> = {};
  if (form.code !== undefined) {
    payload.code = form.code?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.jurisdiction !== undefined) {
    payload.jurisdiction = form.jurisdiction;
  }
  if (form.severity !== undefined) {
    payload.severity = form.severity;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseAllergenUpdate;
}
