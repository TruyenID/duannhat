/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { DenominationKind } from '../enum/DenominationKind';
import { TillCountPhase } from '../enum/TillCountPhase';
import type { Denomination } from './Denomination';
import type { TillSession } from './TillSession';

/**
 * 額面カウント
 */
export interface TillCashDenominationCount {
  id: string;
  count_phase: TillCountPhase;
  quantity: number;
  subtotal_amount: number;
  currency_code: string;
  denomination_value: number;
  denomination_kind: DenominationKind;
  session_id: string;
  session?: TillSession;
  denomination_id: string;
  denomination?: Denomination;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for TillCashDenominationCount
 */
export const tillCashDenominationCountI18n = {
  label: {"ja":"額面カウント","en":"Till Cash Denomination Count","vi":"Đếm theo mệnh giá"},
  fields: {
    count_phase: {
      label: {"ja":"カウント段階","en":"Count Phase","vi":"Giai đoạn đếm"},
      placeholder: {"ja":"カウント段階を入力","en":"Enter Count Phase","vi":"Nhập Giai đoạn đếm"},
    },
    quantity: {
      label: {"ja":"数量","en":"Quantity","vi":"Số lượng"},
      placeholder: {"ja":"数量を入力","en":"Enter Quantity","vi":"Nhập Số lượng"},
    },
    subtotal_amount: {
      label: {"ja":"小計","en":"Subtotal","vi":"Tạm tính"},
      placeholder: {"ja":"小計を入力","en":"Enter Subtotal","vi":"Nhập Tạm tính"},
    },
    currency_code: {
      label: {"ja":"通貨","en":"Currency","vi":"Tiền tệ"},
      placeholder: {"ja":"通貨を入力","en":"Enter Currency","vi":"Nhập Tiền tệ"},
    },
    denomination_value: {
      label: {"ja":"額面金額","en":"Denomination Value","vi":"Giá trị mệnh giá"},
      placeholder: {"ja":"額面金額を入力","en":"Enter Denomination Value","vi":"Nhập Giá trị mệnh giá"},
    },
    denomination_kind: {
      label: {"ja":"額面種別","en":"Denomination Kind","vi":"Loại mệnh giá"},
      placeholder: {"ja":"額面種別を入力","en":"Enter Denomination Kind","vi":"Nhập Loại mệnh giá"},
    },
    session: {
      label: {"ja":"セッション","en":"Session","vi":"Ca"},
      placeholder: {"ja":"セッションを入力","en":"Enter Session","vi":"Nhập Ca"},
    },
    denomination: {
      label: {"ja":"額面","en":"Denomination","vi":"Mệnh giá"},
      placeholder: {"ja":"額面を入力","en":"Enter Denomination","vi":"Nhập Mệnh giá"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for TillCashDenominationCount */
export const baseTillCashDenominationCountSchemas = {
  count_phase: z.nativeEnum(TillCountPhase),
  quantity: z.number().int(),
  subtotal_amount: z.number(),
  currency_code: z.string().min(1).max(3),
  denomination_value: z.number(),
  denomination_kind: z.nativeEnum(DenominationKind),
} as const;

/** Create schema for TillCashDenominationCount */
export const baseTillCashDenominationCountCreateSchema = z.object({
  count_phase: baseTillCashDenominationCountSchemas.count_phase,
  quantity: baseTillCashDenominationCountSchemas.quantity,
  subtotal_amount: baseTillCashDenominationCountSchemas.subtotal_amount,
  currency_code: baseTillCashDenominationCountSchemas.currency_code,
  denomination_value: baseTillCashDenominationCountSchemas.denomination_value,
  denomination_kind: baseTillCashDenominationCountSchemas.denomination_kind,
});

/** Update schema for TillCashDenominationCount */
export const baseTillCashDenominationCountUpdateSchema = baseTillCashDenominationCountCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseTillCashDenominationCountCreate = z.infer<typeof baseTillCashDenominationCountCreateSchema>;
export type BaseTillCashDenominationCountUpdate = z.infer<typeof baseTillCashDenominationCountUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getTillCashDenominationCountLabel(locale: string): string {
  return tillCashDenominationCountI18n.label[locale as keyof typeof tillCashDenominationCountI18n.label] ?? tillCashDenominationCountI18n.label['en'] ?? 'TillCashDenominationCount';
}

/** Get field label for a specific locale */
export function getTillCashDenominationCountFieldLabel(field: string, locale: string): string {
  const fields = tillCashDenominationCountI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getTillCashDenominationCountFieldPlaceholder(field: string, locale: string): string {
  const fields = tillCashDenominationCountI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for TillCashDenominationCount.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const tillCashDenominationCountMetadata = {
  modelName: "TillCashDenominationCount",
  table: "till_cash_denomination_counts",

  fields: {
    count_phase: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "TillCountPhase",
      enumValues: ["opening", "closing"] as const,
      label: {
        ja: "カウント段階",
        en: "Count Phase",
        vi: "Giai đoạn đếm",
      },
      placeholder: {
        ja: "カウント段階を入力",
        en: "Enter Count Phase",
        vi: "Nhập Giai đoạn đếm",
      },
    },
    quantity: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "数量",
        en: "Quantity",
        vi: "Số lượng",
      },
      placeholder: {
        ja: "数量を入力",
        en: "Enter Quantity",
        vi: "Nhập Số lượng",
      },
    },
    subtotal_amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "小計",
        en: "Subtotal",
        vi: "Tạm tính",
      },
      placeholder: {
        ja: "小計を入力",
        en: "Enter Subtotal",
        vi: "Nhập Tạm tính",
      },
    },
    currency_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 3,
      },
      label: {
        ja: "通貨",
        en: "Currency",
        vi: "Tiền tệ",
      },
      placeholder: {
        ja: "通貨を入力",
        en: "Enter Currency",
        vi: "Nhập Tiền tệ",
      },
    },
    denomination_value: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "額面金額",
        en: "Denomination Value",
        vi: "Giá trị mệnh giá",
      },
      placeholder: {
        ja: "額面金額を入力",
        en: "Enter Denomination Value",
        vi: "Nhập Giá trị mệnh giá",
      },
    },
    denomination_kind: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "DenominationKind",
      enumValues: ["note", "coin"] as const,
      label: {
        ja: "額面種別",
        en: "Denomination Kind",
        vi: "Loại mệnh giá",
      },
      placeholder: {
        ja: "額面種別を入力",
        en: "Enter Denomination Kind",
        vi: "Nhập Loại mệnh giá",
      },
    },
    session_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "TillSession",
        table: "till_sessions",
        onDelete: "CASCADE",
      },
      label: {},
    },
    denomination_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Denomination",
        table: "denominations",
        onDelete: "RESTRICT",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["count_phase", "quantity", "subtotal_amount", "currency_code", "denomination_value", "denomination_kind", "session_id", "denomination_id"] as const,
  enumFields: ["count_phase", "denomination_kind"] as const,
  relationFields: ["session_id", "denomination_id"] as const,
} as const;

export type TillCashDenominationCountFieldName = keyof typeof tillCashDenominationCountMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a TillCashDenominationCount. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface TillCashDenominationCountCreateFormState {
  count_phase: TillCountPhase;
  quantity: number;
  subtotal_amount: number;
  currency_code: string;
  denomination_value: number;
  denomination_kind: DenominationKind;
  session_id: string;
  denomination_id: string;
}

/** Form-state shape for updating a TillCashDenominationCount. Every field optional. */
export interface TillCashDenominationCountUpdateFormState {
  count_phase?: TillCountPhase;
  quantity?: number;
  subtotal_amount?: number;
  currency_code?: string;
  denomination_value?: number;
  denomination_kind?: DenominationKind;
  session_id?: string;
  denomination_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyTillCashDenominationCountCreateForm(): TillCashDenominationCountCreateFormState {
  return {
    count_phase: TillCountPhase.Opening,
    quantity: 0,
    subtotal_amount: 0,
    currency_code: '',
    denomination_value: 0,
    denomination_kind: DenominationKind.Note,
    session_id: '',
    denomination_id: '',
  };
}

/** Convert create form state into the payload that baseTillCashDenominationCountCreateSchema accepts. */
export function buildTillCashDenominationCountCreatePayload(form: TillCashDenominationCountCreateFormState): BaseTillCashDenominationCountCreate {
  const payload: Record<string, unknown> = {
    count_phase: form.count_phase,
    quantity: form.quantity,
    subtotal_amount: form.subtotal_amount,
    currency_code: form.currency_code.trim(),
    denomination_value: form.denomination_value,
    denomination_kind: form.denomination_kind,
    session_id: form.session_id.trim(),
    denomination_id: form.denomination_id.trim(),
  };

  return payload as BaseTillCashDenominationCountCreate;
}

/** Convert update form state into the payload that baseTillCashDenominationCountUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildTillCashDenominationCountUpdatePayload(form: TillCashDenominationCountUpdateFormState): BaseTillCashDenominationCountUpdate {
  const payload: Record<string, unknown> = {};
  if (form.count_phase !== undefined) {
    payload.count_phase = form.count_phase;
  }
  if (form.quantity !== undefined) {
    payload.quantity = form.quantity;
  }
  if (form.subtotal_amount !== undefined) {
    payload.subtotal_amount = form.subtotal_amount;
  }
  if (form.currency_code !== undefined) {
    payload.currency_code = form.currency_code?.toString().trim() ?? null;
  }
  if (form.denomination_value !== undefined) {
    payload.denomination_value = form.denomination_value;
  }
  if (form.denomination_kind !== undefined) {
    payload.denomination_kind = form.denomination_kind;
  }
  if (form.session_id !== undefined) {
    payload.session_id = form.session_id?.toString().trim() ?? null;
  }
  if (form.denomination_id !== undefined) {
    payload.denomination_id = form.denomination_id?.toString().trim() ?? null;
  }
  return payload as BaseTillCashDenominationCountUpdate;
}
