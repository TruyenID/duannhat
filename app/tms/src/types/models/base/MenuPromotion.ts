/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { MenuPromotionAppliesTo } from '../enum/MenuPromotionAppliesTo';
import { MenuPromotionStackingMode } from '../enum/MenuPromotionStackingMode';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Category } from './Category';
import type { CustomerOrderItem } from './CustomerOrderItem';
import type { Organization } from './Organization';
import type { Product } from './Product';

/**
 * メニュープロモーション
 */
export interface MenuPromotion {
  id: string;
  name: string;
  description?: string | null;
  discount_percent: number;
  applies_to: MenuPromotionAppliesTo;
  daily_time_from?: string | null;
  daily_time_to?: string | null;
  weekdays?: unknown | null;
  valid_from: DateTimeString;
  valid_until: DateTimeString;
  stacking_mode: MenuPromotionStackingMode;
  is_active: boolean;
  branch_id: string;
  branch?: Branch;
  brand_id: string;
  brand?: Brand;
  organization_id: string;
  organization?: Organization;
  categories?: Category[];
  products?: Product[];
  customerOrderItems?: CustomerOrderItem[];
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuPromotion
 */
export const menuPromotionI18n = {
  label: {"ja":"メニュープロモーション","en":"Menu Promotion","vi":"Khuyến mãi menu"},
  fields: {
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    discount_percent: {
      label: {"ja":"割引率","en":"Discount Percent","vi":"Phần trăm giảm"},
      placeholder: {"ja":"割引率を入力","en":"Enter Discount Percent","vi":"Nhập Phần trăm giảm"},
    },
    applies_to: {
      label: {"ja":"適用範囲","en":"Applies To","vi":"Phạm vi áp dụng"},
      placeholder: {"ja":"適用範囲を入力","en":"Enter Applies To","vi":"Nhập Phạm vi áp dụng"},
    },
    daily_time_from: {
      label: {"ja":"日次開始時刻","en":"Daily Time From","vi":"Giờ bắt đầu trong ngày"},
      placeholder: {"ja":"日次開始時刻を入力","en":"Enter Daily Time From","vi":"Nhập Giờ bắt đầu trong ngày"},
    },
    daily_time_to: {
      label: {"ja":"日次終了時刻","en":"Daily Time To","vi":"Giờ kết thúc trong ngày"},
      placeholder: {"ja":"日次終了時刻を入力","en":"Enter Daily Time To","vi":"Nhập Giờ kết thúc trong ngày"},
    },
    weekdays: {
      label: {"ja":"曜日","en":"Weekdays","vi":"Ngày trong tuần"},
      placeholder: {"ja":"曜日を入力","en":"Enter Weekdays","vi":"Nhập Ngày trong tuần"},
    },
    valid_from: {
      label: {"ja":"有効開始","en":"Valid From","vi":"Bắt đầu"},
      placeholder: {"ja":"有効開始を入力","en":"Enter Valid From","vi":"Nhập Bắt đầu"},
    },
    valid_until: {
      label: {"ja":"有効終了","en":"Valid Until","vi":"Kết thúc"},
      placeholder: {"ja":"有効終了を入力","en":"Enter Valid Until","vi":"Nhập Kết thúc"},
    },
    stacking_mode: {
      label: {"ja":"スタッキングモード","en":"Stacking Mode","vi":"Chế độ cộng dồn"},
      placeholder: {"ja":"スタッキングモードを入力","en":"Enter Stacking Mode","vi":"Nhập Chế độ cộng dồn"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    categories: {
      label: {"ja":"対象カテゴリ","en":"Applicable Categories","vi":"Danh mục áp dụng"},
      placeholder: {"ja":"対象カテゴリを入力","en":"Enter Applicable Categories","vi":"Nhập Danh mục áp dụng"},
    },
    products: {
      label: {"ja":"対象商品","en":"Applicable Products","vi":"Món áp dụng"},
      placeholder: {"ja":"対象商品を入力","en":"Enter Applicable Products","vi":"Nhập Món áp dụng"},
    },
    customer_order_items: {
      label: {"ja":"適用注文項目","en":"Applied Order Items","vi":"Item đã áp"},
      placeholder: {"ja":"適用注文項目を入力","en":"Enter Applied Order Items","vi":"Nhập Item đã áp"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuPromotion */
export const baseMenuPromotionSchemas = {
  name: z.string().min(1).max(255),
  description: z.string().optional().nullable(),
  discount_percent: z.number(),
  applies_to: z.nativeEnum(MenuPromotionAppliesTo),
  daily_time_from: z.string().time().optional().nullable(),
  daily_time_to: z.string().time().optional().nullable(),
  weekdays: z.unknown().optional().nullable(),
  valid_from: z.string().datetime({ offset: true }),
  valid_until: z.string().datetime({ offset: true }),
  stacking_mode: z.nativeEnum(MenuPromotionStackingMode),
  is_active: z.boolean(),
} as const;

/** Create schema for MenuPromotion */
export const baseMenuPromotionCreateSchema = z.object({
  name: baseMenuPromotionSchemas.name,
  description: baseMenuPromotionSchemas.description,
  discount_percent: baseMenuPromotionSchemas.discount_percent,
  applies_to: baseMenuPromotionSchemas.applies_to,
  daily_time_from: baseMenuPromotionSchemas.daily_time_from,
  daily_time_to: baseMenuPromotionSchemas.daily_time_to,
  weekdays: baseMenuPromotionSchemas.weekdays,
  valid_from: baseMenuPromotionSchemas.valid_from,
  valid_until: baseMenuPromotionSchemas.valid_until,
  stacking_mode: baseMenuPromotionSchemas.stacking_mode,
  is_active: baseMenuPromotionSchemas.is_active,
  ja: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
});

/** Update schema for MenuPromotion */
export const baseMenuPromotionUpdateSchema = baseMenuPromotionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuPromotionCreate = z.infer<typeof baseMenuPromotionCreateSchema>;
export type BaseMenuPromotionUpdate = z.infer<typeof baseMenuPromotionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuPromotionLabel(locale: string): string {
  return menuPromotionI18n.label[locale as keyof typeof menuPromotionI18n.label] ?? menuPromotionI18n.label['en'] ?? 'MenuPromotion';
}

/** Get field label for a specific locale */
export function getMenuPromotionFieldLabel(field: string, locale: string): string {
  const fields = menuPromotionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuPromotionFieldPlaceholder(field: string, locale: string): string {
  const fields = menuPromotionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuPromotion.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuPromotionMetadata = {
  modelName: "MenuPromotion",
  table: "menu_promotions",

  fields: {
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: true,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    discount_percent: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "割引率",
        en: "Discount Percent",
        vi: "Phần trăm giảm",
      },
      placeholder: {
        ja: "割引率を入力",
        en: "Enter Discount Percent",
        vi: "Nhập Phần trăm giảm",
      },
    },
    applies_to: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "MenuPromotionAppliesTo",
      enumValues: ["all_items", "categories", "products", "mixed"] as const,
      label: {
        ja: "適用範囲",
        en: "Applies To",
        vi: "Phạm vi áp dụng",
      },
      placeholder: {
        ja: "適用範囲を入力",
        en: "Enter Applies To",
        vi: "Nhập Phạm vi áp dụng",
      },
    },
    daily_time_from: {
      type: "time",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "日次開始時刻",
        en: "Daily Time From",
        vi: "Giờ bắt đầu trong ngày",
      },
      placeholder: {
        ja: "日次開始時刻を入力",
        en: "Enter Daily Time From",
        vi: "Nhập Giờ bắt đầu trong ngày",
      },
    },
    daily_time_to: {
      type: "time",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "日次終了時刻",
        en: "Daily Time To",
        vi: "Giờ kết thúc trong ngày",
      },
      placeholder: {
        ja: "日次終了時刻を入力",
        en: "Enter Daily Time To",
        vi: "Nhập Giờ kết thúc trong ngày",
      },
    },
    weekdays: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "曜日",
        en: "Weekdays",
        vi: "Ngày trong tuần",
      },
      placeholder: {
        ja: "曜日を入力",
        en: "Enter Weekdays",
        vi: "Nhập Ngày trong tuần",
      },
    },
    valid_from: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効開始",
        en: "Valid From",
        vi: "Bắt đầu",
      },
      placeholder: {
        ja: "有効開始を入力",
        en: "Enter Valid From",
        vi: "Nhập Bắt đầu",
      },
    },
    valid_until: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効終了",
        en: "Valid Until",
        vi: "Kết thúc",
      },
      placeholder: {
        ja: "有効終了を入力",
        en: "Enter Valid Until",
        vi: "Nhập Kết thúc",
      },
    },
    stacking_mode: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "MenuPromotionStackingMode",
      enumValues: ["exclusive_with_coupons", "stackable_with_coupons"] as const,
      label: {
        ja: "スタッキングモード",
        en: "Stacking Mode",
        vi: "Chế độ cộng dồn",
      },
      placeholder: {
        ja: "スタッキングモードを入力",
        en: "Enter Stacking Mode",
        vi: "Nhập Chế độ cộng dồn",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name", "description"] as const,
  requiredFields: ["name", "discount_percent", "applies_to", "valid_from", "valid_until", "stacking_mode", "is_active", "branch_id", "brand_id", "organization_id"] as const,
  enumFields: ["applies_to", "stacking_mode"] as const,
  relationFields: ["branch_id", "brand_id", "organization_id"] as const,
} as const;

export type MenuPromotionFieldName = keyof typeof menuPromotionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuPromotion. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuPromotionCreateFormState {
  name: Record<Locale, string>;
  description: Record<Locale, string>;
  discount_percent: number;
  applies_to: MenuPromotionAppliesTo;
  daily_time_from?: string | null;
  daily_time_to?: string | null;
  weekdays?: unknown | null;
  valid_from: string;
  valid_until: string;
  stacking_mode: MenuPromotionStackingMode;
  is_active: boolean;
  branch_id: string;
  brand_id: string;
  organization_id: string;
}

/** Form-state shape for updating a MenuPromotion. Every field optional. */
export interface MenuPromotionUpdateFormState {
  name?: Record<Locale, string>;
  description?: Record<Locale, string>;
  discount_percent?: number;
  applies_to?: MenuPromotionAppliesTo;
  daily_time_from?: string | null;
  daily_time_to?: string | null;
  weekdays?: unknown | null;
  valid_from?: string;
  valid_until?: string;
  stacking_mode?: MenuPromotionStackingMode;
  is_active?: boolean;
  branch_id?: string;
  brand_id?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuPromotionCreateForm(): MenuPromotionCreateFormState {
  return {
    name: emptyLocaleMap(),
    description: emptyLocaleMap(),
    discount_percent: 0,
    applies_to: MenuPromotionAppliesTo.AllItems,
    daily_time_from: '',
    daily_time_to: '',
    weekdays: null,
    valid_from: '',
    valid_until: '',
    stacking_mode: MenuPromotionStackingMode.StackableWithCoupons,
    is_active: true,
    branch_id: '',
    brand_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseMenuPromotionCreateSchema accepts. */
export function buildMenuPromotionCreatePayload(form: MenuPromotionCreateFormState): BaseMenuPromotionCreate {
  const payload: Record<string, unknown> = {
    name: (form.name["ja"] ?? '').trim(),
    description: (form.description["ja"] ?? '').trim(),
    discount_percent: form.discount_percent,
    applies_to: form.applies_to,
    daily_time_from: form.daily_time_from,
    daily_time_to: form.daily_time_to,
    weekdays: form.weekdays,
    valid_from: form.valid_from,
    valid_until: form.valid_until,
    stacking_mode: form.stacking_mode,
    is_active: form.is_active,
    branch_id: form.branch_id.trim(),
    brand_id: form.brand_id.trim(),
    organization_id: form.organization_id.trim(),
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
    description: form.description,
  });
  Object.assign(payload, i18n);

  return payload as BaseMenuPromotionCreate;
}

/** Convert update form state into the payload that baseMenuPromotionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuPromotionUpdatePayload(form: MenuPromotionUpdateFormState): BaseMenuPromotionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.description !== undefined) {
    payload.description = (form.description["ja"] ?? '').trim();
  }
  if (form.discount_percent !== undefined) {
    payload.discount_percent = form.discount_percent;
  }
  if (form.applies_to !== undefined) {
    payload.applies_to = form.applies_to;
  }
  if (form.daily_time_from !== undefined) {
    payload.daily_time_from = form.daily_time_from;
  }
  if (form.daily_time_to !== undefined) {
    payload.daily_time_to = form.daily_time_to;
  }
  if (form.weekdays !== undefined) {
    payload.weekdays = form.weekdays;
  }
  if (form.valid_from !== undefined) {
    payload.valid_from = form.valid_from;
  }
  if (form.valid_until !== undefined) {
    payload.valid_until = form.valid_until;
  }
  if (form.stacking_mode !== undefined) {
    payload.stacking_mode = form.stacking_mode;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  if (form.description !== undefined) localeInput.description = form.description;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseMenuPromotionUpdate;
}
