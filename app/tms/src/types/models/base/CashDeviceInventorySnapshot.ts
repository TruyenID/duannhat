/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { TillCountPhase } from '../enum/TillCountPhase';
import type { Branch } from './Branch';
import type { Organization } from './Organization';
import type { PeripheralDevice } from './PeripheralDevice';
import type { TillSession } from './TillSession';

/**
 * 釣銭機在高スナップショット
 */
export interface CashDeviceInventorySnapshot {
  id: string;
  peripheral_device_id: string;
  peripheralDevice?: PeripheralDevice;
  till_session_id: string;
  tillSession?: TillSession;
  count_phase: TillCountPhase;
  denominations: unknown;
  uncertain_denominations?: unknown | null;
  bill_reject_count: number;
  total_minor: number;
  machine_seq_no?: number | null;
  captured_at: DateTimeString;
  branch_id: string;
  branch?: Branch;
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for CashDeviceInventorySnapshot
 */
export const cashDeviceInventorySnapshotI18n = {
  label: {"ja":"釣銭機在高スナップショット","en":"Cash Device Inventory Snapshot","vi":"Ảnh chụp tiền trong máy"},
  fields: {
    peripheral_device: {
      label: {"ja":"釣銭機","en":"Cash Device","vi":"Máy thu tiền"},
      placeholder: {"ja":"釣銭機を入力","en":"Enter Cash Device","vi":"Nhập Máy thu tiền"},
    },
    till_session: {
      label: {"ja":"レジ精算","en":"Till Session","vi":"Ca thu ngân"},
      placeholder: {"ja":"レジ精算を入力","en":"Enter Till Session","vi":"Nhập Ca thu ngân"},
    },
    count_phase: {
      label: {"ja":"計数段階","en":"Count Phase","vi":"Giai đoạn"},
      placeholder: {"ja":"計数段階を入力","en":"Enter Count Phase","vi":"Nhập Giai đoạn"},
    },
    denominations: {
      label: {"ja":"金種内訳","en":"Denominations","vi":"Theo mệnh giá"},
      placeholder: {"ja":"金種内訳を入力","en":"Enter Denominations","vi":"Nhập Theo mệnh giá"},
    },
    uncertain_denominations: {
      label: {"ja":"在高不確定金種","en":"Uncertain Denominations","vi":"Mệnh giá không chắc"},
      placeholder: {"ja":"在高不確定金種を入力","en":"Enter Uncertain Denominations","vi":"Nhập Mệnh giá không chắc"},
    },
    bill_reject_count: {
      label: {"ja":"リジェクト枚数","en":"Bill Reject Count","vi":"Tiền khay từ chối"},
      placeholder: {"ja":"リジェクト枚数を入力","en":"Enter Bill Reject Count","vi":"Nhập Tiền khay từ chối"},
    },
    total_minor: {
      label: {"ja":"在高合計","en":"Total (minor)","vi":"Tổng chắc chắn"},
      placeholder: {"ja":"在高合計を入力","en":"Enter Total (minor)","vi":"Nhập Tổng chắc chắn"},
    },
    machine_seq_no: {
      label: {"ja":"機器連番","en":"Machine Seq No","vi":"Số thứ tự máy"},
      placeholder: {"ja":"機器連番を入力","en":"Enter Machine Seq No","vi":"Nhập Số thứ tự máy"},
    },
    captured_at: {
      label: {"ja":"取得日時","en":"Captured At","vi":"Thời điểm chụp"},
      placeholder: {"ja":"取得日時を入力","en":"Enter Captured At","vi":"Nhập Thời điểm chụp"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for CashDeviceInventorySnapshot */
export const baseCashDeviceInventorySnapshotSchemas = {
  count_phase: z.nativeEnum(TillCountPhase),
  denominations: z.unknown(),
  uncertain_denominations: z.unknown().optional().nullable(),
  bill_reject_count: z.number().int(),
  total_minor: z.number().int(),
  machine_seq_no: z.number().int().optional().nullable(),
  captured_at: z.string().datetime({ offset: true }),
} as const;

/** Create schema for CashDeviceInventorySnapshot */
export const baseCashDeviceInventorySnapshotCreateSchema = z.object({
  count_phase: baseCashDeviceInventorySnapshotSchemas.count_phase,
  denominations: baseCashDeviceInventorySnapshotSchemas.denominations,
  uncertain_denominations: baseCashDeviceInventorySnapshotSchemas.uncertain_denominations,
  bill_reject_count: baseCashDeviceInventorySnapshotSchemas.bill_reject_count,
  total_minor: baseCashDeviceInventorySnapshotSchemas.total_minor,
  machine_seq_no: baseCashDeviceInventorySnapshotSchemas.machine_seq_no,
  captured_at: baseCashDeviceInventorySnapshotSchemas.captured_at,
});

/** Update schema for CashDeviceInventorySnapshot */
export const baseCashDeviceInventorySnapshotUpdateSchema = baseCashDeviceInventorySnapshotCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseCashDeviceInventorySnapshotCreate = z.infer<typeof baseCashDeviceInventorySnapshotCreateSchema>;
export type BaseCashDeviceInventorySnapshotUpdate = z.infer<typeof baseCashDeviceInventorySnapshotUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getCashDeviceInventorySnapshotLabel(locale: string): string {
  return cashDeviceInventorySnapshotI18n.label[locale as keyof typeof cashDeviceInventorySnapshotI18n.label] ?? cashDeviceInventorySnapshotI18n.label['en'] ?? 'CashDeviceInventorySnapshot';
}

/** Get field label for a specific locale */
export function getCashDeviceInventorySnapshotFieldLabel(field: string, locale: string): string {
  const fields = cashDeviceInventorySnapshotI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getCashDeviceInventorySnapshotFieldPlaceholder(field: string, locale: string): string {
  const fields = cashDeviceInventorySnapshotI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for CashDeviceInventorySnapshot.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const cashDeviceInventorySnapshotMetadata = {
  modelName: "CashDeviceInventorySnapshot",
  table: "cash_device_inventory_snapshots",

  fields: {
    peripheral_device_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "PeripheralDevice",
        table: "peripheral_devices",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    till_session_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "TillSession",
        table: "till_sessions",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    count_phase: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "TillCountPhase",
      enumValues: ["opening", "closing"] as const,
      label: {
        ja: "計数段階",
        en: "Count Phase",
        vi: "Giai đoạn",
      },
      placeholder: {
        ja: "計数段階を入力",
        en: "Enter Count Phase",
        vi: "Nhập Giai đoạn",
      },
    },
    denominations: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "金種内訳",
        en: "Denominations",
        vi: "Theo mệnh giá",
      },
      placeholder: {
        ja: "金種内訳を入力",
        en: "Enter Denominations",
        vi: "Nhập Theo mệnh giá",
      },
    },
    uncertain_denominations: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "在高不確定金種",
        en: "Uncertain Denominations",
        vi: "Mệnh giá không chắc",
      },
      placeholder: {
        ja: "在高不確定金種を入力",
        en: "Enter Uncertain Denominations",
        vi: "Nhập Mệnh giá không chắc",
      },
    },
    bill_reject_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "リジェクト枚数",
        en: "Bill Reject Count",
        vi: "Tiền khay từ chối",
      },
      placeholder: {
        ja: "リジェクト枚数を入力",
        en: "Enter Bill Reject Count",
        vi: "Nhập Tiền khay từ chối",
      },
    },
    total_minor: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "在高合計",
        en: "Total (minor)",
        vi: "Tổng chắc chắn",
      },
      placeholder: {
        ja: "在高合計を入力",
        en: "Enter Total (minor)",
        vi: "Nhập Tổng chắc chắn",
      },
    },
    machine_seq_no: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "機器連番",
        en: "Machine Seq No",
        vi: "Số thứ tự máy",
      },
      placeholder: {
        ja: "機器連番を入力",
        en: "Enter Machine Seq No",
        vi: "Nhập Số thứ tự máy",
      },
    },
    captured_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "取得日時",
        en: "Captured At",
        vi: "Thời điểm chụp",
      },
      placeholder: {
        ja: "取得日時を入力",
        en: "Enter Captured At",
        vi: "Nhập Thời điểm chụp",
      },
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "RESTRICT",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["peripheral_device_id", "till_session_id", "count_phase", "denominations", "bill_reject_count", "total_minor", "captured_at", "branch_id", "organization_id"] as const,
  enumFields: ["count_phase"] as const,
  relationFields: ["peripheral_device_id", "till_session_id", "branch_id", "organization_id"] as const,
} as const;

export type CashDeviceInventorySnapshotFieldName = keyof typeof cashDeviceInventorySnapshotMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a CashDeviceInventorySnapshot. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface CashDeviceInventorySnapshotCreateFormState {
  peripheral_device_id: string;
  till_session_id: string;
  count_phase: TillCountPhase;
  denominations: unknown;
  uncertain_denominations?: unknown | null;
  bill_reject_count: number;
  total_minor: number;
  machine_seq_no?: number | null;
  captured_at: string;
  branch_id: string;
  organization_id: string;
}

/** Form-state shape for updating a CashDeviceInventorySnapshot. Every field optional. */
export interface CashDeviceInventorySnapshotUpdateFormState {
  peripheral_device_id?: string;
  till_session_id?: string;
  count_phase?: TillCountPhase;
  denominations?: unknown;
  uncertain_denominations?: unknown | null;
  bill_reject_count?: number;
  total_minor?: number;
  machine_seq_no?: number | null;
  captured_at?: string;
  branch_id?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyCashDeviceInventorySnapshotCreateForm(): CashDeviceInventorySnapshotCreateFormState {
  return {
    peripheral_device_id: '',
    till_session_id: '',
    count_phase: TillCountPhase.Opening,
    denominations: null,
    uncertain_denominations: null,
    bill_reject_count: 0,
    total_minor: 0,
    machine_seq_no: 0,
    captured_at: '',
    branch_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseCashDeviceInventorySnapshotCreateSchema accepts. */
export function buildCashDeviceInventorySnapshotCreatePayload(form: CashDeviceInventorySnapshotCreateFormState): BaseCashDeviceInventorySnapshotCreate {
  const payload: Record<string, unknown> = {
    peripheral_device_id: form.peripheral_device_id.trim(),
    till_session_id: form.till_session_id.trim(),
    count_phase: form.count_phase,
    denominations: form.denominations,
    uncertain_denominations: form.uncertain_denominations,
    bill_reject_count: form.bill_reject_count,
    total_minor: form.total_minor,
    machine_seq_no: form.machine_seq_no,
    captured_at: form.captured_at,
    branch_id: form.branch_id.trim(),
    organization_id: form.organization_id.trim(),
  };

  return payload as BaseCashDeviceInventorySnapshotCreate;
}

/** Convert update form state into the payload that baseCashDeviceInventorySnapshotUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildCashDeviceInventorySnapshotUpdatePayload(form: CashDeviceInventorySnapshotUpdateFormState): BaseCashDeviceInventorySnapshotUpdate {
  const payload: Record<string, unknown> = {};
  if (form.peripheral_device_id !== undefined) {
    payload.peripheral_device_id = form.peripheral_device_id?.toString().trim() ?? null;
  }
  if (form.till_session_id !== undefined) {
    payload.till_session_id = form.till_session_id?.toString().trim() ?? null;
  }
  if (form.count_phase !== undefined) {
    payload.count_phase = form.count_phase;
  }
  if (form.denominations !== undefined) {
    payload.denominations = form.denominations;
  }
  if (form.uncertain_denominations !== undefined) {
    payload.uncertain_denominations = form.uncertain_denominations;
  }
  if (form.bill_reject_count !== undefined) {
    payload.bill_reject_count = form.bill_reject_count;
  }
  if (form.total_minor !== undefined) {
    payload.total_minor = form.total_minor;
  }
  if (form.machine_seq_no !== undefined) {
    payload.machine_seq_no = form.machine_seq_no;
  }
  if (form.captured_at !== undefined) {
    payload.captured_at = form.captured_at;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseCashDeviceInventorySnapshotUpdate;
}
