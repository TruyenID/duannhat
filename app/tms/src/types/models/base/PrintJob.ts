/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { Organization } from './Organization';
import type { Printer } from './Printer';
import type { User } from './User';

/**
 * PrintJob
 */
export interface PrintJob {
  id: string;
  organization_id: string;
  organization?: Organization;
  branch_id: string;
  branch?: Branch;
  printer_id?: string | null;
  printer?: Printer | null;
  transport: string;
  kind: string;
  order_id?: string | null;
  payment_id?: string | null;
  payload?: unknown | null;
  reprint_no: number;
  requested_by_id?: string | null;
  requestedBy?: User | null;
  requested_via?: string | null;
  reprint_reason?: string | null;
  template_version?: string | null;
  status: string;
  confidence: string;
  attempts: number;
  last_error?: string | null;
  acked_at?: DateTimeString | null;
  printed_reported_at?: DateTimeString | null;
  expires_at?: DateTimeString | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PrintJob
 */
export const printJobI18n = {
  label: {"ja":"PrintJob","en":"PrintJob","vi":"PrintJob"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branch: {
      label: {"ja":"支店","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"支店を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    printer: {
      label: {"ja":"プリンタ","en":"Printer","vi":"Máy in"},
      placeholder: {"ja":"プリンタを入力","en":"Enter Printer","vi":"Nhập Máy in"},
    },
    transport: {
      label: {"ja":"経路","en":"Transport","vi":"Kênh truyền"},
      placeholder: {"ja":"経路を入力","en":"Enter Transport","vi":"Nhập Kênh truyền"},
    },
    kind: {
      label: {"ja":"種別","en":"Kind","vi":"Loại phiếu"},
      placeholder: {"ja":"種別を入力","en":"Enter Kind","vi":"Nhập Loại phiếu"},
    },
    order_id: {
      label: {"ja":"注文ID","en":"Order ID","vi":"ID đơn"},
      placeholder: {"ja":"注文IDを入力","en":"Enter Order ID","vi":"Nhập ID đơn"},
    },
    payment_id: {
      label: {"ja":"支払ID","en":"Payment ID","vi":"ID thanh toán"},
      placeholder: {"ja":"支払IDを入力","en":"Enter Payment ID","vi":"Nhập ID thanh toán"},
    },
    payload: {
      label: {"ja":"ペイロード","en":"Payload","vi":"Nội dung"},
      placeholder: {"ja":"ペイロードを入力","en":"Enter Payload","vi":"Nhập Nội dung"},
    },
    reprint_no: {
      label: {"ja":"印刷回数","en":"Reprint No","vi":"Bản in số"},
      placeholder: {"ja":"印刷回数を入力","en":"Enter Reprint No","vi":"Nhập Bản in số"},
    },
    requested_by: {
      label: {"ja":"依頼者","en":"Requested By","vi":"Người yêu cầu"},
      placeholder: {"ja":"依頼者を入力","en":"Enter Requested By","vi":"Nhập Người yêu cầu"},
    },
    requested_via: {
      label: {"ja":"依頼元","en":"Requested Via","vi":"Yêu cầu từ"},
      placeholder: {"ja":"依頼元を入力","en":"Enter Requested Via","vi":"Nhập Yêu cầu từ"},
    },
    reprint_reason: {
      label: {"ja":"再印刷理由","en":"Reprint Reason","vi":"Lý do in lại"},
      placeholder: {"ja":"再印刷理由を入力","en":"Enter Reprint Reason","vi":"Nhập Lý do in lại"},
    },
    template_version: {
      label: {"ja":"帳票バージョン","en":"Template Version","vi":"Phiên bản mẫu"},
      placeholder: {"ja":"帳票バージョンを入力","en":"Enter Template Version","vi":"Nhập Phiên bản mẫu"},
    },
    status: {
      label: {"ja":"状態","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"状態を入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
    confidence: {
      label: {"ja":"確度","en":"Confidence","vi":"Độ chắc chắn"},
      placeholder: {"ja":"確度を入力","en":"Enter Confidence","vi":"Nhập Độ chắc chắn"},
    },
    attempts: {
      label: {"ja":"試行回数","en":"Attempts","vi":"Số lần thử"},
      placeholder: {"ja":"試行回数を入力","en":"Enter Attempts","vi":"Nhập Số lần thử"},
    },
    last_error: {
      label: {"ja":"最終エラー","en":"Last Error","vi":"Lỗi gần nhất"},
      placeholder: {"ja":"最終エラーを入力","en":"Enter Last Error","vi":"Nhập Lỗi gần nhất"},
    },
    acked_at: {
      label: {"ja":"受領日時","en":"Acked At","vi":"Nhận lúc"},
      placeholder: {"ja":"受領日時を入力","en":"Enter Acked At","vi":"Nhập Nhận lúc"},
    },
    printed_reported_at: {
      label: {"ja":"実印刷日時","en":"Printed Reported At","vi":"In thật lúc"},
      placeholder: {"ja":"実印刷日時を入力","en":"Enter Printed Reported At","vi":"Nhập In thật lúc"},
    },
    expires_at: {
      label: {"ja":"有効期限","en":"Expires At","vi":"Hết hạn"},
      placeholder: {"ja":"有効期限を入力","en":"Enter Expires At","vi":"Nhập Hết hạn"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PrintJob */
export const basePrintJobSchemas = {
  transport: z.string().min(1).max(32),
  kind: z.string().min(1).max(32),
  order_id: z.string().optional().nullable(),
  payment_id: z.string().optional().nullable(),
  payload: z.unknown().optional().nullable(),
  reprint_no: z.number().int(),
  requested_via: z.string().max(32).optional().nullable(),
  reprint_reason: z.string().max(255).optional().nullable(),
  template_version: z.string().max(32).optional().nullable(),
  status: z.string().min(1).max(32),
  confidence: z.string().min(1).max(16),
  attempts: z.number().int(),
  last_error: z.string().max(255).optional().nullable(),
  acked_at: z.string().datetime({ offset: true }).optional().nullable(),
  printed_reported_at: z.string().datetime({ offset: true }).optional().nullable(),
  expires_at: z.string().datetime({ offset: true }).optional().nullable(),
} as const;

/** Create schema for PrintJob */
export const basePrintJobCreateSchema = z.object({
  transport: basePrintJobSchemas.transport,
  kind: basePrintJobSchemas.kind,
  order_id: basePrintJobSchemas.order_id,
  payment_id: basePrintJobSchemas.payment_id,
  payload: basePrintJobSchemas.payload,
  reprint_no: basePrintJobSchemas.reprint_no,
  requested_via: basePrintJobSchemas.requested_via,
  reprint_reason: basePrintJobSchemas.reprint_reason,
  template_version: basePrintJobSchemas.template_version,
  status: basePrintJobSchemas.status,
  confidence: basePrintJobSchemas.confidence,
  attempts: basePrintJobSchemas.attempts,
  last_error: basePrintJobSchemas.last_error,
  acked_at: basePrintJobSchemas.acked_at,
  printed_reported_at: basePrintJobSchemas.printed_reported_at,
  expires_at: basePrintJobSchemas.expires_at,
});

/** Update schema for PrintJob */
export const basePrintJobUpdateSchema = basePrintJobCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePrintJobCreate = z.infer<typeof basePrintJobCreateSchema>;
export type BasePrintJobUpdate = z.infer<typeof basePrintJobUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPrintJobLabel(locale: string): string {
  return printJobI18n.label[locale as keyof typeof printJobI18n.label] ?? printJobI18n.label['en'] ?? 'PrintJob';
}

/** Get field label for a specific locale */
export function getPrintJobFieldLabel(field: string, locale: string): string {
  const fields = printJobI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPrintJobFieldPlaceholder(field: string, locale: string): string {
  const fields = printJobI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PrintJob.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const printJobMetadata = {
  modelName: "PrintJob",
  table: "print_jobs",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    printer_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Printer",
        table: "printers",
        onDelete: "SET NULL",
      },
      label: {},
    },
    transport: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "経路",
        en: "Transport",
        vi: "Kênh truyền",
      },
      placeholder: {
        ja: "経路を入力",
        en: "Enter Transport",
        vi: "Nhập Kênh truyền",
      },
    },
    kind: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "種別",
        en: "Kind",
        vi: "Loại phiếu",
      },
      placeholder: {
        ja: "種別を入力",
        en: "Enter Kind",
        vi: "Nhập Loại phiếu",
      },
    },
    order_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "注文ID",
        en: "Order ID",
        vi: "ID đơn",
      },
      placeholder: {
        ja: "注文IDを入力",
        en: "Enter Order ID",
        vi: "Nhập ID đơn",
      },
    },
    payment_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "支払ID",
        en: "Payment ID",
        vi: "ID thanh toán",
      },
      placeholder: {
        ja: "支払IDを入力",
        en: "Enter Payment ID",
        vi: "Nhập ID thanh toán",
      },
    },
    payload: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "ペイロード",
        en: "Payload",
        vi: "Nội dung",
      },
      placeholder: {
        ja: "ペイロードを入力",
        en: "Enter Payload",
        vi: "Nhập Nội dung",
      },
    },
    reprint_no: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "印刷回数",
        en: "Reprint No",
        vi: "Bản in số",
      },
      placeholder: {
        ja: "印刷回数を入力",
        en: "Enter Reprint No",
        vi: "Nhập Bản in số",
      },
    },
    requested_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
    requested_via: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "依頼元",
        en: "Requested Via",
        vi: "Yêu cầu từ",
      },
      placeholder: {
        ja: "依頼元を入力",
        en: "Enter Requested Via",
        vi: "Nhập Yêu cầu từ",
      },
    },
    reprint_reason: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "再印刷理由",
        en: "Reprint Reason",
        vi: "Lý do in lại",
      },
      placeholder: {
        ja: "再印刷理由を入力",
        en: "Enter Reprint Reason",
        vi: "Nhập Lý do in lại",
      },
    },
    template_version: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "帳票バージョン",
        en: "Template Version",
        vi: "Phiên bản mẫu",
      },
      placeholder: {
        ja: "帳票バージョンを入力",
        en: "Enter Template Version",
        vi: "Nhập Phiên bản mẫu",
      },
    },
    status: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "状態",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "状態を入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
    confidence: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 16,
      },
      label: {
        ja: "確度",
        en: "Confidence",
        vi: "Độ chắc chắn",
      },
      placeholder: {
        ja: "確度を入力",
        en: "Enter Confidence",
        vi: "Nhập Độ chắc chắn",
      },
    },
    attempts: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "試行回数",
        en: "Attempts",
        vi: "Số lần thử",
      },
      placeholder: {
        ja: "試行回数を入力",
        en: "Enter Attempts",
        vi: "Nhập Số lần thử",
      },
    },
    last_error: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "最終エラー",
        en: "Last Error",
        vi: "Lỗi gần nhất",
      },
      placeholder: {
        ja: "最終エラーを入力",
        en: "Enter Last Error",
        vi: "Nhập Lỗi gần nhất",
      },
    },
    acked_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "受領日時",
        en: "Acked At",
        vi: "Nhận lúc",
      },
      placeholder: {
        ja: "受領日時を入力",
        en: "Enter Acked At",
        vi: "Nhập Nhận lúc",
      },
    },
    printed_reported_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "実印刷日時",
        en: "Printed Reported At",
        vi: "In thật lúc",
      },
      placeholder: {
        ja: "実印刷日時を入力",
        en: "Enter Printed Reported At",
        vi: "Nhập In thật lúc",
      },
    },
    expires_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "有効期限",
        en: "Expires At",
        vi: "Hết hạn",
      },
      placeholder: {
        ja: "有効期限を入力",
        en: "Enter Expires At",
        vi: "Nhập Hết hạn",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "branch_id", "transport", "kind", "reprint_no", "status", "confidence", "attempts"] as const,
  enumFields: [] as const,
  relationFields: ["organization_id", "branch_id", "printer_id", "requested_by_id"] as const,
} as const;

export type PrintJobFieldName = keyof typeof printJobMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PrintJob. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PrintJobCreateFormState {
  organization_id: string;
  branch_id: string;
  printer_id?: string | null;
  transport: string;
  kind: string;
  order_id?: string | null;
  payment_id?: string | null;
  payload?: unknown | null;
  reprint_no: number;
  requested_by_id?: string | null;
  requested_via?: string | null;
  reprint_reason?: string | null;
  template_version?: string | null;
  status: string;
  confidence: string;
  attempts: number;
  last_error?: string | null;
  acked_at?: string | null;
  printed_reported_at?: string | null;
  expires_at?: string | null;
}

/** Form-state shape for updating a PrintJob. Every field optional. */
export interface PrintJobUpdateFormState {
  organization_id?: string;
  branch_id?: string;
  printer_id?: string | null;
  transport?: string;
  kind?: string;
  order_id?: string | null;
  payment_id?: string | null;
  payload?: unknown | null;
  reprint_no?: number;
  requested_by_id?: string | null;
  requested_via?: string | null;
  reprint_reason?: string | null;
  template_version?: string | null;
  status?: string;
  confidence?: string;
  attempts?: number;
  last_error?: string | null;
  acked_at?: string | null;
  printed_reported_at?: string | null;
  expires_at?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPrintJobCreateForm(): PrintJobCreateFormState {
  return {
    organization_id: '',
    branch_id: '',
    printer_id: '',
    transport: "ws_lan",
    kind: '',
    order_id: '',
    payment_id: '',
    payload: null,
    reprint_no: 1,
    requested_by_id: '',
    requested_via: '',
    reprint_reason: '',
    template_version: '',
    status: "queued",
    confidence: "sent_only",
    attempts: 0,
    last_error: '',
    acked_at: '',
    printed_reported_at: '',
    expires_at: '',
  };
}

/** Convert create form state into the payload that basePrintJobCreateSchema accepts. */
export function buildPrintJobCreatePayload(form: PrintJobCreateFormState): BasePrintJobCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    branch_id: form.branch_id.trim(),
    printer_id: form.printer_id?.toString().trim() || null,
    transport: form.transport.trim(),
    kind: form.kind.trim(),
    order_id: form.order_id?.toString().trim() || null,
    payment_id: form.payment_id?.toString().trim() || null,
    payload: form.payload,
    reprint_no: form.reprint_no,
    requested_by_id: form.requested_by_id?.toString().trim() || null,
    requested_via: form.requested_via?.toString().trim() || null,
    reprint_reason: form.reprint_reason?.toString().trim() || null,
    template_version: form.template_version?.toString().trim() || null,
    status: form.status.trim(),
    confidence: form.confidence.trim(),
    attempts: form.attempts,
    last_error: form.last_error?.toString().trim() || null,
    acked_at: form.acked_at,
    printed_reported_at: form.printed_reported_at,
    expires_at: form.expires_at,
  };

  return payload as BasePrintJobCreate;
}

/** Convert update form state into the payload that basePrintJobUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPrintJobUpdatePayload(form: PrintJobUpdateFormState): BasePrintJobUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.printer_id !== undefined) {
    payload.printer_id = form.printer_id?.toString().trim() ?? null;
  }
  if (form.transport !== undefined) {
    payload.transport = form.transport?.toString().trim() ?? null;
  }
  if (form.kind !== undefined) {
    payload.kind = form.kind?.toString().trim() ?? null;
  }
  if (form.order_id !== undefined) {
    payload.order_id = form.order_id?.toString().trim() ?? null;
  }
  if (form.payment_id !== undefined) {
    payload.payment_id = form.payment_id?.toString().trim() ?? null;
  }
  if (form.payload !== undefined) {
    payload.payload = form.payload;
  }
  if (form.reprint_no !== undefined) {
    payload.reprint_no = form.reprint_no;
  }
  if (form.requested_by_id !== undefined) {
    payload.requested_by_id = form.requested_by_id?.toString().trim() ?? null;
  }
  if (form.requested_via !== undefined) {
    payload.requested_via = form.requested_via?.toString().trim() ?? null;
  }
  if (form.reprint_reason !== undefined) {
    payload.reprint_reason = form.reprint_reason?.toString().trim() ?? null;
  }
  if (form.template_version !== undefined) {
    payload.template_version = form.template_version?.toString().trim() ?? null;
  }
  if (form.status !== undefined) {
    payload.status = form.status?.toString().trim() ?? null;
  }
  if (form.confidence !== undefined) {
    payload.confidence = form.confidence?.toString().trim() ?? null;
  }
  if (form.attempts !== undefined) {
    payload.attempts = form.attempts;
  }
  if (form.last_error !== undefined) {
    payload.last_error = form.last_error?.toString().trim() ?? null;
  }
  if (form.acked_at !== undefined) {
    payload.acked_at = form.acked_at;
  }
  if (form.printed_reported_at !== undefined) {
    payload.printed_reported_at = form.printed_reported_at;
  }
  if (form.expires_at !== undefined) {
    payload.expires_at = form.expires_at;
  }
  return payload as BasePrintJobUpdate;
}
