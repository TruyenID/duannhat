/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { PrinterConnectionType } from '../enum/PrinterConnectionType';
import { PrinterCutType } from '../enum/PrinterCutType';
import type { Branch } from './Branch';
import type { Organization } from './Organization';

/**
 * プリンター
 */
export interface Printer {
  id: string;
  name: string;
  roles: unknown;
  connection_type: PrinterConnectionType;
  address?: string | null;
  paper_width: number;
  cut_type: PrinterCutType;
  encoding: string;
  is_active: boolean;
  last_seen_at?: DateTimeString | null;
  notes?: string | null;
  organization_id: string;
  organization?: Organization;
  branch_id: string;
  branch?: Branch;
  transport: string;
  print_token?: string | null;
  model_profile?: unknown | null;
  last_status?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Printer
 */
export const printerI18n = {
  label: {"ja":"プリンター","en":"Printer","vi":"Máy in"},
  fields: {
    name: {
      label: {"ja":"プリンター名","en":"Printer Name","vi":"Tên máy in"},
      placeholder: {"ja":"プリンター名を入力","en":"Enter Printer Name","vi":"Nhập Tên máy in"},
    },
    roles: {
      label: {"ja":"役割","en":"Roles","vi":"Vai trò"},
      placeholder: {"ja":"役割を入力","en":"Enter Roles","vi":"Nhập Vai trò"},
    },
    connection_type: {
      label: {"ja":"接続方式","en":"Connection Type","vi":"Kiểu kết nối"},
      placeholder: {"ja":"接続方式を入力","en":"Enter Connection Type","vi":"Nhập Kiểu kết nối"},
    },
    address: {
      label: {"ja":"アドレス","en":"Address","vi":"Địa chỉ"},
      placeholder: {"ja":"アドレスを入力","en":"Enter Address","vi":"Nhập Địa chỉ"},
    },
    paper_width: {
      label: {"ja":"用紙幅（mm）","en":"Paper Width (mm)","vi":"Khổ giấy (mm)"},
      placeholder: {"ja":"用紙幅（mm）を入力","en":"Enter Paper Width (mm)","vi":"Nhập Khổ giấy (mm)"},
    },
    cut_type: {
      label: {"ja":"カット方式","en":"Cut Type","vi":"Kiểu cắt giấy"},
      placeholder: {"ja":"カット方式を入力","en":"Enter Cut Type","vi":"Nhập Kiểu cắt giấy"},
    },
    encoding: {
      label: {"ja":"文字コード","en":"Encoding","vi":"Bảng mã"},
      placeholder: {"ja":"文字コードを入力","en":"Enter Encoding","vi":"Nhập Bảng mã"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Đang dùng"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Đang dùng"},
    },
    last_seen_at: {
      label: {"ja":"最終通信日時","en":"Last Seen At","vi":"Lần hoạt động cuối"},
      placeholder: {"ja":"最終通信日時を入力","en":"Enter Last Seen At","vi":"Nhập Lần hoạt động cuối"},
    },
    notes: {
      label: {"ja":"メモ","en":"Notes","vi":"Ghi chú"},
      placeholder: {"ja":"メモを入力","en":"Enter Notes","vi":"Nhập Ghi chú"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    transport: {
      label: {"ja":"トランスポート","en":"Transport","vi":"Kênh truyền"},
      placeholder: {"ja":"トランスポートを入力","en":"Enter Transport","vi":"Nhập Kênh truyền"},
    },
    print_token: {
      label: {"ja":"印刷トークン","en":"Print Token","vi":"Token in"},
      placeholder: {"ja":"印刷トークンを入力","en":"Enter Print Token","vi":"Nhập Token in"},
    },
    model_profile: {
      label: {"ja":"モデルプロファイル","en":"Model Profile","vi":"Hồ sơ thiết bị"},
      placeholder: {"ja":"モデルプロファイルを入力","en":"Enter Model Profile","vi":"Nhập Hồ sơ thiết bị"},
    },
    last_status: {
      label: {"ja":"最終ステータス","en":"Last Status","vi":"Trạng thái gần nhất"},
      placeholder: {"ja":"最終ステータスを入力","en":"Enter Last Status","vi":"Nhập Trạng thái gần nhất"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Printer */
export const basePrinterSchemas = {
  name: z.string().min(1).max(255),
  roles: z.unknown(),
  connection_type: z.nativeEnum(PrinterConnectionType),
  address: z.string().max(255).optional().nullable(),
  paper_width: z.number().int(),
  cut_type: z.nativeEnum(PrinterCutType),
  encoding: z.string().min(1).max(32),
  is_active: z.boolean(),
  last_seen_at: z.string().datetime({ offset: true }).optional().nullable(),
  notes: z.string().optional().nullable(),
  transport: z.string().min(1).max(32),
  print_token: z.string().max(128).optional().nullable(),
  model_profile: z.unknown().optional().nullable(),
  last_status: z.string().max(32).optional().nullable(),
} as const;

/** Create schema for Printer */
export const basePrinterCreateSchema = z.object({
  name: basePrinterSchemas.name,
  roles: basePrinterSchemas.roles,
  connection_type: basePrinterSchemas.connection_type,
  address: basePrinterSchemas.address,
  paper_width: basePrinterSchemas.paper_width,
  cut_type: basePrinterSchemas.cut_type,
  encoding: basePrinterSchemas.encoding,
  is_active: basePrinterSchemas.is_active,
  last_seen_at: basePrinterSchemas.last_seen_at,
  notes: basePrinterSchemas.notes,
  transport: basePrinterSchemas.transport,
  print_token: basePrinterSchemas.print_token,
  model_profile: basePrinterSchemas.model_profile,
  last_status: basePrinterSchemas.last_status,
});

/** Update schema for Printer */
export const basePrinterUpdateSchema = basePrinterCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePrinterCreate = z.infer<typeof basePrinterCreateSchema>;
export type BasePrinterUpdate = z.infer<typeof basePrinterUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPrinterLabel(locale: string): string {
  return printerI18n.label[locale as keyof typeof printerI18n.label] ?? printerI18n.label['en'] ?? 'Printer';
}

/** Get field label for a specific locale */
export function getPrinterFieldLabel(field: string, locale: string): string {
  const fields = printerI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPrinterFieldPlaceholder(field: string, locale: string): string {
  const fields = printerI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Printer.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const printerMetadata = {
  modelName: "Printer",
  table: "printers",

  fields: {
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "プリンター名",
        en: "Printer Name",
        vi: "Tên máy in",
      },
      placeholder: {
        ja: "プリンター名を入力",
        en: "Enter Printer Name",
        vi: "Nhập Tên máy in",
      },
      description: {
        ja: "表示名。店舗内で一意（BR-P01）。例：キッチンプリンター、バープリンター。",
        en: "Friendly name, unique per branch (BR-P01). Example: Kitchen Printer, Bar Printer.",
        vi: "Tên hiển thị, unique trong quán (BR-P01). Ví dụ: Máy in bếp, Máy in bar.",
      },
    },
    roles: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "役割",
        en: "Roles",
        vi: "Vai trò",
      },
      placeholder: {
        ja: "役割を入力",
        en: "Enter Roles",
        vi: "Nhập Vai trò",
      },
      description: {
        ja: "PrinterRole値の配列。1台が複数役割を兼任可能（BR-P02）。例：[\"kitchen_printer\",\"receipt_printer\"]。ルーティングはこの項目で行う。",
        en: "ARRAY of PrinterRole values — a printer can hold several roles at once (BR-P02). Example: [\"kitchen_printer\",\"receipt_printer\"]. This is the routing key, not `type`.",
        vi: "MẢNG các giá trị PrinterRole — một máy giữ được nhiều vai trò (BR-P02). Ví dụ: [\"kitchen_printer\",\"receipt_printer\"]. Đây là khoá định tuyến.",
      },
    },
    connection_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "PrinterConnectionType",
      enumValues: ["network", "usb"] as const,
      label: {
        ja: "接続方式",
        en: "Connection Type",
        vi: "Kiểu kết nối",
      },
      placeholder: {
        ja: "接続方式を入力",
        en: "Enter Connection Type",
        vi: "Nhập Kiểu kết nối",
      },
      description: {
        ja: "network＝LAN経由TCP（Cloudから設定可）。usb＝デバイスパス、PC依存（BR-P04）。",
        en: "network = TCP over LAN (configurable from Cloud). usb = device path, machine-specific (BR-P04).",
        vi: "network = TCP qua LAN (cấu hình được từ Cloud). usb = đường dẫn thiết bị, phụ thuộc máy tính (BR-P04).",
      },
    },
    address: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "アドレス",
        en: "Address",
        vi: "Địa chỉ",
      },
      placeholder: {
        ja: "アドレスを入力",
        en: "Enter Address",
        vi: "Nhập Địa chỉ",
      },
      description: {
        ja: "network の場合：\"IP:ポート\"、ESC/POS既定ポートは9100（例：192.168.1.100:9100）。プライベートIPまたは.localのみ（BR-P03）。usb の場合：デバイスパス（/dev/usb/lp0、COM3）。",
        en: "For network: \"IP:port\", default ESC/POS port 9100 (e.g. 192.168.1.100:9100). Must be a private LAN IP or .local mDNS name (BR-P03). For usb: device path (/dev/usb/lp0, COM3).",
        vi: "Với network: \"IP:port\", cổng ESC/POS mặc định 9100 (vd 192.168.1.100:9100). Phải là IP LAN riêng tư hoặc tên .local (BR-P03). Với usb: đường dẫn thiết bị (/dev/usb/lp0, COM3).",
      },
    },
    paper_width: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "用紙幅（mm）",
        en: "Paper Width (mm)",
        vi: "Khổ giấy (mm)",
      },
      placeholder: {
        ja: "用紙幅（mm）を入力",
        en: "Enter Paper Width (mm)",
        vi: "Nhập Khổ giấy (mm)",
      },
      description: {
        ja: "感熱紙の幅（mm）：58 または 80。印字桁数を決める（80mm＝48桁）。",
        en: "Thermal paper width in mm: 58 or 80. Drives the print column count (80mm = 48 cols).",
        vi: "Khổ giấy nhiệt tính bằng mm: 58 hoặc 80. Quyết định số cột in (80mm = 48 cột).",
      },
    },
    cut_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "PrinterCutType",
      enumValues: ["full", "partial", "none"] as const,
      label: {
        ja: "カット方式",
        en: "Cut Type",
        vi: "Kiểu cắt giấy",
      },
      placeholder: {
        ja: "カット方式を入力",
        en: "Enter Cut Type",
        vi: "Nhập Kiểu cắt giấy",
      },
    },
    encoding: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "文字コード",
        en: "Encoding",
        vi: "Bảng mã",
      },
      placeholder: {
        ja: "文字コードを入力",
        en: "Enter Encoding",
        vi: "Nhập Bảng mã",
      },
      description: {
        ja: "プリンターへ送る文字コード。既定はshift_jis（日本語感熱プリンター）。ベトナム語はASCII折り畳み後に送信。",
        en: "Character encoding sent to the printer. Default shift_jis (Japanese thermal printers). Vietnamese text is ASCII-folded before encoding.",
        vi: "Bảng mã gửi tới máy in. Mặc định shift_jis (máy in nhiệt Nhật). Tiếng Việt được ASCII-fold trước khi encode.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Đang dùng",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Đang dùng",
      },
      description: {
        ja: "無効なプリンターはworkstationのLoadFromDB()で読み込まれず、ジョブを受け取らない。",
        en: "Inactive printers are skipped by the workstation's LoadFromDB() and never receive jobs.",
        vi: "Máy in tắt sẽ bị workstation bỏ qua khi nạp cấu hình, không nhận lệnh in.",
      },
    },
    last_seen_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最終通信日時",
        en: "Last Seen At",
        vi: "Lần hoạt động cuối",
      },
      placeholder: {
        ja: "最終通信日時を入力",
        en: "Enter Last Seen At",
        vi: "Nhập Lần hoạt động cuối",
      },
      description: {
        ja: "接続成功時にworkstationが報告。admin-webで到達可能なプリンターを表示するために使用。",
        en: "Reported by the workstation after a successful connection. Lets admin-web show which printers are actually reachable in the shop.",
        vi: "Do workstation báo lên sau khi kết nối thành công. Giúp admin-web hiển thị máy nào thực sự đang kết nối được.",
      },
    },
    notes: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "メモ",
        en: "Notes",
        vi: "Ghi chú",
      },
      placeholder: {
        ja: "メモを入力",
        en: "Enter Notes",
        vi: "Nhập Ghi chú",
      },
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    transport: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "トランスポート",
        en: "Transport",
        vi: "Kênh truyền",
      },
      placeholder: {
        ja: "トランスポートを入力",
        en: "Enter Transport",
        vi: "Nhập Kênh truyền",
      },
    },
    print_token: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 128,
      },
      label: {
        ja: "印刷トークン",
        en: "Print Token",
        vi: "Token in",
      },
      placeholder: {
        ja: "印刷トークンを入力",
        en: "Enter Print Token",
        vi: "Nhập Token in",
      },
    },
    model_profile: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "モデルプロファイル",
        en: "Model Profile",
        vi: "Hồ sơ thiết bị",
      },
      placeholder: {
        ja: "モデルプロファイルを入力",
        en: "Enter Model Profile",
        vi: "Nhập Hồ sơ thiết bị",
      },
    },
    last_status: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 32,
      },
      label: {
        ja: "最終ステータス",
        en: "Last Status",
        vi: "Trạng thái gần nhất",
      },
      placeholder: {
        ja: "最終ステータスを入力",
        en: "Enter Last Status",
        vi: "Nhập Trạng thái gần nhất",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["name", "roles", "connection_type", "paper_width", "cut_type", "encoding", "is_active", "organization_id", "branch_id", "transport"] as const,
  enumFields: ["connection_type", "cut_type"] as const,
  relationFields: ["organization_id", "branch_id"] as const,
} as const;

export type PrinterFieldName = keyof typeof printerMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Printer. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PrinterCreateFormState {
  name: string;
  roles: unknown;
  connection_type: PrinterConnectionType;
  address?: string | null;
  paper_width: number;
  cut_type: PrinterCutType;
  encoding: string;
  is_active: boolean;
  last_seen_at?: string | null;
  notes?: string | null;
  organization_id: string;
  branch_id: string;
  transport: string;
  print_token?: string | null;
  model_profile?: unknown | null;
  last_status?: string | null;
}

/** Form-state shape for updating a Printer. Every field optional. */
export interface PrinterUpdateFormState {
  name?: string;
  roles?: unknown;
  connection_type?: PrinterConnectionType;
  address?: string | null;
  paper_width?: number;
  cut_type?: PrinterCutType;
  encoding?: string;
  is_active?: boolean;
  last_seen_at?: string | null;
  notes?: string | null;
  organization_id?: string;
  branch_id?: string;
  transport?: string;
  print_token?: string | null;
  model_profile?: unknown | null;
  last_status?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPrinterCreateForm(): PrinterCreateFormState {
  return {
    name: '',
    roles: null,
    connection_type: PrinterConnectionType.Network,
    address: '',
    paper_width: 80,
    cut_type: PrinterCutType.Full,
    encoding: "shift_jis",
    is_active: true,
    last_seen_at: '',
    notes: '',
    organization_id: '',
    branch_id: '',
    transport: "ws_lan",
    print_token: '',
    model_profile: null,
    last_status: '',
  };
}

/** Convert create form state into the payload that basePrinterCreateSchema accepts. */
export function buildPrinterCreatePayload(form: PrinterCreateFormState): BasePrinterCreate {
  const payload: Record<string, unknown> = {
    name: form.name.trim(),
    roles: form.roles,
    connection_type: form.connection_type,
    address: form.address?.toString().trim() || null,
    paper_width: form.paper_width,
    cut_type: form.cut_type,
    encoding: form.encoding.trim(),
    is_active: form.is_active,
    last_seen_at: form.last_seen_at,
    notes: form.notes?.toString().trim() || null,
    organization_id: form.organization_id.trim(),
    branch_id: form.branch_id.trim(),
    transport: form.transport.trim(),
    print_token: form.print_token?.toString().trim() || null,
    model_profile: form.model_profile,
    last_status: form.last_status?.toString().trim() || null,
  };

  return payload as BasePrinterCreate;
}

/** Convert update form state into the payload that basePrinterUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPrinterUpdatePayload(form: PrinterUpdateFormState): BasePrinterUpdate {
  const payload: Record<string, unknown> = {};
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.roles !== undefined) {
    payload.roles = form.roles;
  }
  if (form.connection_type !== undefined) {
    payload.connection_type = form.connection_type;
  }
  if (form.address !== undefined) {
    payload.address = form.address?.toString().trim() ?? null;
  }
  if (form.paper_width !== undefined) {
    payload.paper_width = form.paper_width;
  }
  if (form.cut_type !== undefined) {
    payload.cut_type = form.cut_type;
  }
  if (form.encoding !== undefined) {
    payload.encoding = form.encoding?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.last_seen_at !== undefined) {
    payload.last_seen_at = form.last_seen_at;
  }
  if (form.notes !== undefined) {
    payload.notes = form.notes?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.transport !== undefined) {
    payload.transport = form.transport?.toString().trim() ?? null;
  }
  if (form.print_token !== undefined) {
    payload.print_token = form.print_token?.toString().trim() ?? null;
  }
  if (form.model_profile !== undefined) {
    payload.model_profile = form.model_profile;
  }
  if (form.last_status !== undefined) {
    payload.last_status = form.last_status?.toString().trim() ?? null;
  }
  return payload as BasePrinterUpdate;
}
