/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * 監査ログ
 */
export interface AuditLog {
  id: string;
  auditable_type: string;
  auditable_id: string;
  action: string;
  user_id?: string | null;
  metadata?: unknown | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for AuditLog
 */
export const auditLogI18n = {
  label: {"ja":"監査ログ","en":"Audit Log","vi":"Nhật ký kiểm tra"},
  fields: {
    auditable_type: {
      label: {"ja":"監査対象タイプ","en":"Auditable Type","vi":"Loại đối tượng"},
      placeholder: {"ja":"監査対象タイプを入力","en":"Enter Auditable Type","vi":"Nhập Loại đối tượng"},
    },
    auditable_id: {
      label: {"ja":"監査対象ID","en":"Auditable ID","vi":"ID đối tượng"},
      placeholder: {"ja":"監査対象IDを入力","en":"Enter Auditable ID","vi":"Nhập ID đối tượng"},
    },
    action: {
      label: {"ja":"アクション","en":"Action","vi":"Hành động"},
      placeholder: {"ja":"アクションを入力","en":"Enter Action","vi":"Nhập Hành động"},
    },
    user_id: {
      label: {"ja":"ユーザーID","en":"User ID","vi":"ID người dùng"},
      placeholder: {"ja":"ユーザーIDを入力","en":"Enter User ID","vi":"Nhập ID người dùng"},
    },
    metadata: {
      label: {"ja":"メタデータ","en":"Metadata","vi":"Dữ liệu bổ sung"},
      placeholder: {"ja":"メタデータを入力","en":"Enter Metadata","vi":"Nhập Dữ liệu bổ sung"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for AuditLog */
export const baseAuditLogSchemas = {
  auditable_type: z.string().min(1).max(255),
  auditable_id: z.string().min(1).max(36),
  action: z.string().min(1).max(100),
  user_id: z.string().optional().nullable(),
  metadata: z.unknown().optional().nullable(),
} as const;

/** Create schema for AuditLog */
export const baseAuditLogCreateSchema = z.object({
  auditable_type: baseAuditLogSchemas.auditable_type,
  auditable_id: baseAuditLogSchemas.auditable_id,
  action: baseAuditLogSchemas.action,
  user_id: baseAuditLogSchemas.user_id,
  metadata: baseAuditLogSchemas.metadata,
});

/** Update schema for AuditLog */
export const baseAuditLogUpdateSchema = baseAuditLogCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseAuditLogCreate = z.infer<typeof baseAuditLogCreateSchema>;
export type BaseAuditLogUpdate = z.infer<typeof baseAuditLogUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getAuditLogLabel(locale: string): string {
  return auditLogI18n.label[locale as keyof typeof auditLogI18n.label] ?? auditLogI18n.label['en'] ?? 'AuditLog';
}

/** Get field label for a specific locale */
export function getAuditLogFieldLabel(field: string, locale: string): string {
  const fields = auditLogI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getAuditLogFieldPlaceholder(field: string, locale: string): string {
  const fields = auditLogI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for AuditLog.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const auditLogMetadata = {
  modelName: "AuditLog",
  table: "audit_logs",

  fields: {
    auditable_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "監査対象タイプ",
        en: "Auditable Type",
        vi: "Loại đối tượng",
      },
      placeholder: {
        ja: "監査対象タイプを入力",
        en: "Enter Auditable Type",
        vi: "Nhập Loại đối tượng",
      },
      description: {
        ja: "Eloquentモデルの短縮クラス名（例：\"Recipe\"、\"Product\"）",
        en: "Eloquent model class short name (e.g. \"Recipe\", \"Product\").",
        vi: "Tên ngắn class Eloquent (vd. \"Recipe\", \"Product\").",
      },
    },
    auditable_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 36,
      },
      label: {
        ja: "監査対象ID",
        en: "Auditable ID",
        vi: "ID đối tượng",
      },
      placeholder: {
        ja: "監査対象IDを入力",
        en: "Enter Auditable ID",
        vi: "Nhập ID đối tượng",
      },
      description: {
        ja: "監査対象レコードの主キー（UUID文字列）。",
        en: "Primary key of the audited record (UUID string).",
        vi: "Khóa chính của bản ghi được kiểm tra (UUID string).",
      },
    },
    action: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "アクション",
        en: "Action",
        vi: "Hành động",
      },
      placeholder: {
        ja: "アクションを入力",
        en: "Enter Action",
        vi: "Nhập Hành động",
      },
      description: {
        ja: "ドット区切りのイベント名。例：recipe.approved、product.created。",
        en: "Dot-namespaced event name. Examples: recipe.approved, product.created.",
        vi: "Tên sự kiện phân cách bằng dấu chấm. VD: recipe.approved, product.created.",
      },
    },
    user_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "ユーザーID",
        en: "User ID",
        vi: "ID người dùng",
      },
      placeholder: {
        ja: "ユーザーIDを入力",
        en: "Enter User ID",
        vi: "Nhập ID người dùng",
      },
      description: {
        ja: "操作ユーザーのUUID。システム/バックグラウンドジョブは null。",
        en: "UUID of the acting user. Null for system/background job events.",
        vi: "UUID người thực hiện. Null với sự kiện hệ thống / background job.",
      },
    },
    metadata: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "メタデータ",
        en: "Metadata",
        vi: "Dữ liệu bổ sung",
      },
      placeholder: {
        ja: "メタデータを入力",
        en: "Enter Metadata",
        vi: "Nhập Dữ liệu bổ sung",
      },
      description: {
        ja: "アクション固有のコンテキスト（例：{approver_id、rejection_reason}）。",
        en: "Action-specific context (e.g. {approver_id, rejection_reason}).",
        vi: "Ngữ cảnh theo từng hành động (VD: {approver_id, rejection_reason}).",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["auditable_type", "auditable_id", "action"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type AuditLogFieldName = keyof typeof auditLogMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a AuditLog. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface AuditLogCreateFormState {
  auditable_type: string;
  auditable_id: string;
  action: string;
  user_id?: string | null;
  metadata?: unknown | null;
}

/** Form-state shape for updating a AuditLog. Every field optional. */
export interface AuditLogUpdateFormState {
  auditable_type?: string;
  auditable_id?: string;
  action?: string;
  user_id?: string | null;
  metadata?: unknown | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyAuditLogCreateForm(): AuditLogCreateFormState {
  return {
    auditable_type: '',
    auditable_id: '',
    action: '',
    user_id: '',
    metadata: null,
  };
}

/** Convert create form state into the payload that baseAuditLogCreateSchema accepts. */
export function buildAuditLogCreatePayload(form: AuditLogCreateFormState): BaseAuditLogCreate {
  const payload: Record<string, unknown> = {
    auditable_type: form.auditable_type.trim(),
    auditable_id: form.auditable_id.trim(),
    action: form.action.trim(),
    user_id: form.user_id?.toString().trim() || null,
    metadata: form.metadata,
  };

  return payload as BaseAuditLogCreate;
}

/** Convert update form state into the payload that baseAuditLogUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildAuditLogUpdatePayload(form: AuditLogUpdateFormState): BaseAuditLogUpdate {
  const payload: Record<string, unknown> = {};
  if (form.auditable_type !== undefined) {
    payload.auditable_type = form.auditable_type?.toString().trim() ?? null;
  }
  if (form.auditable_id !== undefined) {
    payload.auditable_id = form.auditable_id?.toString().trim() ?? null;
  }
  if (form.action !== undefined) {
    payload.action = form.action?.toString().trim() ?? null;
  }
  if (form.user_id !== undefined) {
    payload.user_id = form.user_id?.toString().trim() ?? null;
  }
  if (form.metadata !== undefined) {
    payload.metadata = form.metadata;
  }
  return payload as BaseAuditLogUpdate;
}
