/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { User } from './User';

/**
 * 投稿店舗
 */
export interface PostBranch {
  id: number;
  toggled_by_id?: string | null;
  toggledBy?: User | null;
  is_visible: boolean;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PostBranch
 */
export const postBranchI18n = {
  label: {"ja":"投稿店舗","en":"Post Branch","vi":"Câu hỏi theo chi nhánh"},
  fields: {
    toggled_by: {
      label: {"ja":"切替者","en":"Toggled By","vi":"Người bấm"},
      placeholder: {"ja":"切替者を入力","en":"Enter Toggled By","vi":"Nhập Người bấm"},
    },
    is_visible: {
      label: {"ja":"表示中","en":"Visible","vi":"Đang hiện"},
      placeholder: {"ja":"表示中を入力","en":"Enter Visible","vi":"Nhập Đang hiện"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PostBranch */
export const basePostBranchSchemas = {
  is_visible: z.boolean(),
} as const;

/** Create schema for PostBranch */
export const basePostBranchCreateSchema = z.object({
  is_visible: basePostBranchSchemas.is_visible,
});

/** Update schema for PostBranch */
export const basePostBranchUpdateSchema = basePostBranchCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePostBranchCreate = z.infer<typeof basePostBranchCreateSchema>;
export type BasePostBranchUpdate = z.infer<typeof basePostBranchUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPostBranchLabel(locale: string): string {
  return postBranchI18n.label[locale as keyof typeof postBranchI18n.label] ?? postBranchI18n.label['en'] ?? 'PostBranch';
}

/** Get field label for a specific locale */
export function getPostBranchFieldLabel(field: string, locale: string): string {
  const fields = postBranchI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPostBranchFieldPlaceholder(field: string, locale: string): string {
  const fields = postBranchI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PostBranch.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const postBranchMetadata = {
  modelName: "PostBranch",
  table: "post_branches",

  fields: {
    toggled_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "SET NULL",
      },
      label: {},
    },
    is_visible: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示中",
        en: "Visible",
        vi: "Đang hiện",
      },
      placeholder: {
        ja: "表示中を入力",
        en: "Enter Visible",
        vi: "Nhập Đang hiện",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["is_visible"] as const,
  enumFields: [] as const,
  relationFields: ["toggled_by_id"] as const,
} as const;

export type PostBranchFieldName = keyof typeof postBranchMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PostBranch. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PostBranchCreateFormState {
  toggled_by_id?: string | null;
  is_visible: boolean;
}

/** Form-state shape for updating a PostBranch. Every field optional. */
export interface PostBranchUpdateFormState {
  toggled_by_id?: string | null;
  is_visible?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPostBranchCreateForm(): PostBranchCreateFormState {
  return {
    toggled_by_id: '',
    is_visible: true,
  };
}

/** Convert create form state into the payload that basePostBranchCreateSchema accepts. */
export function buildPostBranchCreatePayload(form: PostBranchCreateFormState): BasePostBranchCreate {
  const payload: Record<string, unknown> = {
    toggled_by_id: form.toggled_by_id?.toString().trim() || null,
    is_visible: form.is_visible,
  };

  return payload as BasePostBranchCreate;
}

/** Convert update form state into the payload that basePostBranchUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPostBranchUpdatePayload(form: PostBranchUpdateFormState): BasePostBranchUpdate {
  const payload: Record<string, unknown> = {};
  if (form.toggled_by_id !== undefined) {
    payload.toggled_by_id = form.toggled_by_id?.toString().trim() ?? null;
  }
  if (form.is_visible !== undefined) {
    payload.is_visible = form.is_visible;
  }
  return payload as BasePostBranchUpdate;
}
