/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import { PaymentPolicyPreference } from '../enum/PaymentPolicyPreference';
import type { Branch } from './Branch';
import type { Brand } from './Brand';
import type { Organization } from './Organization';
import type { PaymentGatewayConnection } from './PaymentGatewayConnection';
import type { PaymentGatewayOption } from './PaymentGatewayOption';

/**
 * 店舗決済オプション
 */
export interface ShopPaymentOption {
  id: string;
  organization_id: string;
  organization?: Organization;
  brand_id: string;
  brand?: Brand;
  branch_id: string;
  branch?: Branch;
  option_id: string;
  option?: PaymentGatewayOption;
  connection_id?: string | null;
  connection?: PaymentGatewayConnection | null;
  preference: PaymentPolicyPreference;
  change_reason?: string | null;
  version: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ShopPaymentOption
 */
export const shopPaymentOptionI18n = {
  label: {"ja":"店舗決済オプション","en":"Shop Payment Option","vi":"Tuỳ chọn thanh toán cửa hàng"},
  fields: {
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    branch: {
      label: {"ja":"店舗","en":"Shop","vi":"Cửa hàng"},
      placeholder: {"ja":"店舗を入力","en":"Enter Shop","vi":"Nhập Cửa hàng"},
    },
    option: {
      label: {"ja":"ゲートウェイオプション","en":"Gateway Option","vi":"Tuỳ chọn cổng"},
      placeholder: {"ja":"ゲートウェイオプションを入力","en":"Enter Gateway Option","vi":"Nhập Tuỳ chọn cổng"},
    },
    connection: {
      label: {"ja":"選択接続","en":"Selected Connection","vi":"Kết nối được chọn"},
      placeholder: {"ja":"選択接続を入力","en":"Enter Selected Connection","vi":"Nhập Kết nối được chọn"},
    },
    preference: {
      label: {"ja":"設定","en":"Preference","vi":"Tuỳ chọn"},
      placeholder: {"ja":"設定を入力","en":"Enter Preference","vi":"Nhập Tuỳ chọn"},
    },
    change_reason: {
      label: {"ja":"変更理由","en":"Change Reason","vi":"Lý do thay đổi"},
      placeholder: {"ja":"変更理由を入力","en":"Enter Change Reason","vi":"Nhập Lý do thay đổi"},
    },
    version: {
      label: {"ja":"バージョン","en":"Version","vi":"Phiên bản"},
      placeholder: {"ja":"バージョンを入力","en":"Enter Version","vi":"Nhập Phiên bản"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ShopPaymentOption */
export const baseShopPaymentOptionSchemas = {
  preference: z.nativeEnum(PaymentPolicyPreference),
  change_reason: z.string().max(500).optional().nullable(),
  version: z.number().int().gte(1),
} as const;

/** Create schema for ShopPaymentOption */
export const baseShopPaymentOptionCreateSchema = z.object({
  preference: baseShopPaymentOptionSchemas.preference,
  change_reason: baseShopPaymentOptionSchemas.change_reason,
  version: baseShopPaymentOptionSchemas.version,
});

/** Update schema for ShopPaymentOption */
export const baseShopPaymentOptionUpdateSchema = baseShopPaymentOptionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseShopPaymentOptionCreate = z.infer<typeof baseShopPaymentOptionCreateSchema>;
export type BaseShopPaymentOptionUpdate = z.infer<typeof baseShopPaymentOptionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getShopPaymentOptionLabel(locale: string): string {
  return shopPaymentOptionI18n.label[locale as keyof typeof shopPaymentOptionI18n.label] ?? shopPaymentOptionI18n.label['en'] ?? 'ShopPaymentOption';
}

/** Get field label for a specific locale */
export function getShopPaymentOptionFieldLabel(field: string, locale: string): string {
  const fields = shopPaymentOptionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getShopPaymentOptionFieldPlaceholder(field: string, locale: string): string {
  const fields = shopPaymentOptionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ShopPaymentOption.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const shopPaymentOptionMetadata = {
  modelName: "ShopPaymentOption",
  table: "shop_payment_options",

  fields: {
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    option_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "PaymentGatewayOption",
        table: "payment_gateway_options",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    connection_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "PaymentGatewayConnection",
        table: "payment_gateway_connections",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    preference: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "PaymentPolicyPreference",
      enumValues: ["inherit", "enabled", "disabled", "blocked"] as const,
      label: {
        ja: "設定",
        en: "Preference",
        vi: "Tuỳ chọn",
      },
      placeholder: {
        ja: "設定を入力",
        en: "Enter Preference",
        vi: "Nhập Tuỳ chọn",
      },
    },
    change_reason: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 500,
      },
      label: {
        ja: "変更理由",
        en: "Change Reason",
        vi: "Lý do thay đổi",
      },
      placeholder: {
        ja: "変更理由を入力",
        en: "Enter Change Reason",
        vi: "Nhập Lý do thay đổi",
      },
    },
    version: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        min: 1,
      },
      label: {
        ja: "バージョン",
        en: "Version",
        vi: "Phiên bản",
      },
      placeholder: {
        ja: "バージョンを入力",
        en: "Enter Version",
        vi: "Nhập Phiên bản",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "brand_id", "branch_id", "option_id", "preference", "version"] as const,
  enumFields: ["preference"] as const,
  relationFields: ["organization_id", "brand_id", "branch_id", "option_id", "connection_id"] as const,
} as const;

export type ShopPaymentOptionFieldName = keyof typeof shopPaymentOptionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ShopPaymentOption. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ShopPaymentOptionCreateFormState {
  organization_id: string;
  brand_id: string;
  branch_id: string;
  option_id: string;
  connection_id?: string | null;
  preference: PaymentPolicyPreference;
  change_reason?: string | null;
  version: number;
}

/** Form-state shape for updating a ShopPaymentOption. Every field optional. */
export interface ShopPaymentOptionUpdateFormState {
  organization_id?: string;
  brand_id?: string;
  branch_id?: string;
  option_id?: string;
  connection_id?: string | null;
  preference?: PaymentPolicyPreference;
  change_reason?: string | null;
  version?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyShopPaymentOptionCreateForm(): ShopPaymentOptionCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    branch_id: '',
    option_id: '',
    connection_id: '',
    preference: PaymentPolicyPreference.Inherit,
    change_reason: '',
    version: 1,
  };
}

/** Convert create form state into the payload that baseShopPaymentOptionCreateSchema accepts. */
export function buildShopPaymentOptionCreatePayload(form: ShopPaymentOptionCreateFormState): BaseShopPaymentOptionCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    branch_id: form.branch_id.trim(),
    option_id: form.option_id.trim(),
    connection_id: form.connection_id?.toString().trim() || null,
    preference: form.preference,
    change_reason: form.change_reason?.toString().trim() || null,
    version: form.version,
  };

  return payload as BaseShopPaymentOptionCreate;
}

/** Convert update form state into the payload that baseShopPaymentOptionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildShopPaymentOptionUpdatePayload(form: ShopPaymentOptionUpdateFormState): BaseShopPaymentOptionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.option_id !== undefined) {
    payload.option_id = form.option_id?.toString().trim() ?? null;
  }
  if (form.connection_id !== undefined) {
    payload.connection_id = form.connection_id?.toString().trim() ?? null;
  }
  if (form.preference !== undefined) {
    payload.preference = form.preference;
  }
  if (form.change_reason !== undefined) {
    payload.change_reason = form.change_reason?.toString().trim() ?? null;
  }
  if (form.version !== undefined) {
    payload.version = form.version;
  }
  return payload as BaseShopPaymentOptionUpdate;
}
