/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString, OmnifyFile } from '../common';
import { OmnifyFileSchema } from '../common';
import { CouponDiscountType } from '../enum/CouponDiscountType';
import { PointRewardServiceCondition } from '../enum/PointRewardServiceCondition';
import type { Brand } from './Brand';
import type { Organization } from './Organization';

/**
 * ポイント交換商品
 */
export interface PointReward {
  id: string;
  name: string;
  description?: string | null;
  cost_points: number;
  discount_type: CouponDiscountType;
  discount_value: number;
  max_discount_cap?: number | null;
  min_order_subtotal: number;
  valid_days: number;
  stock_quantity?: number | null;
  redeemed_count: number;
  service_condition: PointRewardServiceCondition;
  image: OmnifyFile | null;
  is_active: boolean;
  sort_order: number;
  brand_id: string;
  brand?: Brand;
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PointReward
 */
export const pointRewardI18n = {
  label: {"ja":"ポイント交換商品","en":"Point Reward","vi":"Phần thưởng đổi điểm"},
  fields: {
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    description: {
      label: {"ja":"説明","en":"Description","vi":"Mô tả"},
      placeholder: {"ja":"説明を入力","en":"Enter Description","vi":"Nhập Mô tả"},
    },
    cost_points: {
      label: {"ja":"必要ポイント","en":"Cost (points)","vi":"Điểm cần"},
      placeholder: {"ja":"必要ポイントを入力","en":"Enter Cost (points)","vi":"Nhập Điểm cần"},
    },
    discount_type: {
      label: {"ja":"割引種別","en":"Discount Type","vi":"Loại giảm giá"},
      placeholder: {"ja":"割引種別を入力","en":"Enter Discount Type","vi":"Nhập Loại giảm giá"},
    },
    discount_value: {
      label: {"ja":"割引額","en":"Discount Value","vi":"Giá trị giảm"},
      placeholder: {"ja":"割引額を入力","en":"Enter Discount Value","vi":"Nhập Giá trị giảm"},
    },
    max_discount_cap: {
      label: {"ja":"最大割引額","en":"Max Discount Cap","vi":"Giảm tối đa"},
      placeholder: {"ja":"最大割引額を入力","en":"Enter Max Discount Cap","vi":"Nhập Giảm tối đa"},
    },
    min_order_subtotal: {
      label: {"ja":"最低注文金額","en":"Minimum Order Subtotal","vi":"Hóa đơn tối thiểu"},
      placeholder: {"ja":"最低注文金額を入力","en":"Enter Minimum Order Subtotal","vi":"Nhập Hóa đơn tối thiểu"},
    },
    valid_days: {
      label: {"ja":"クーポン有効日数","en":"Coupon Valid Days","vi":"Số ngày hiệu lực"},
      placeholder: {"ja":"クーポン有効日数を入力","en":"Enter Coupon Valid Days","vi":"Nhập Số ngày hiệu lực"},
    },
    stock_quantity: {
      label: {"ja":"在庫数","en":"Stock Quantity","vi":"Số lượng"},
      placeholder: {"ja":"在庫数を入力","en":"Enter Stock Quantity","vi":"Nhập Số lượng"},
    },
    redeemed_count: {
      label: {"ja":"交換済み数","en":"Redeemed Count","vi":"Đã đổi"},
      placeholder: {"ja":"交換済み数を入力","en":"Enter Redeemed Count","vi":"Nhập Đã đổi"},
    },
    service_condition: {
      label: {"ja":"提供条件","en":"Service Condition","vi":"Điều kiện phục vụ"},
      placeholder: {"ja":"提供条件を入力","en":"Enter Service Condition","vi":"Nhập Điều kiện phục vụ"},
    },
    image: {
      label: {"ja":"画像","en":"Image","vi":"Ảnh"},
      placeholder: {"ja":"画像を入力","en":"Enter Image","vi":"Nhập Ảnh"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Đang bật"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Đang bật"},
    },
    sort_order: {
      label: {"ja":"表示順","en":"Sort Order","vi":"Thứ tự"},
      placeholder: {"ja":"表示順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự"},
    },
    brand: {
      label: {"ja":"ブランド","en":"Brand","vi":"Thương hiệu"},
      placeholder: {"ja":"ブランドを入力","en":"Enter Brand","vi":"Nhập Thương hiệu"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PointReward */
export const basePointRewardSchemas = {
  name: z.string().min(1).max(255),
  description: z.string().optional().nullable(),
  cost_points: z.number().int(),
  discount_type: z.nativeEnum(CouponDiscountType),
  discount_value: z.number(),
  max_discount_cap: z.number().optional().nullable(),
  min_order_subtotal: z.number(),
  valid_days: z.number().int(),
  stock_quantity: z.number().int().optional().nullable(),
  redeemed_count: z.number().int(),
  service_condition: z.nativeEnum(PointRewardServiceCondition),
  image: OmnifyFileSchema.nullable(),
  is_active: z.boolean(),
  sort_order: z.number().int(),
} as const;

/** Create schema for PointReward */
export const basePointRewardCreateSchema = z.object({
  name: basePointRewardSchemas.name,
  description: basePointRewardSchemas.description,
  cost_points: basePointRewardSchemas.cost_points,
  discount_type: basePointRewardSchemas.discount_type,
  discount_value: basePointRewardSchemas.discount_value,
  max_discount_cap: basePointRewardSchemas.max_discount_cap,
  min_order_subtotal: basePointRewardSchemas.min_order_subtotal,
  valid_days: basePointRewardSchemas.valid_days,
  stock_quantity: basePointRewardSchemas.stock_quantity,
  redeemed_count: basePointRewardSchemas.redeemed_count,
  service_condition: basePointRewardSchemas.service_condition,
  image: basePointRewardSchemas.image,
  is_active: basePointRewardSchemas.is_active,
  sort_order: basePointRewardSchemas.sort_order,
  ja: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  en: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
  vi: z.object({
    name: z.string().max(255).optional().nullable(),
    description: z.string().optional().nullable(),
  }).optional(),
});

/** Update schema for PointReward */
export const basePointRewardUpdateSchema = basePointRewardCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePointRewardCreate = z.infer<typeof basePointRewardCreateSchema>;
export type BasePointRewardUpdate = z.infer<typeof basePointRewardUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPointRewardLabel(locale: string): string {
  return pointRewardI18n.label[locale as keyof typeof pointRewardI18n.label] ?? pointRewardI18n.label['en'] ?? 'PointReward';
}

/** Get field label for a specific locale */
export function getPointRewardFieldLabel(field: string, locale: string): string {
  const fields = pointRewardI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPointRewardFieldPlaceholder(field: string, locale: string): string {
  const fields = pointRewardI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PointReward.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const pointRewardMetadata = {
  modelName: "PointReward",
  table: "point_rewards",

  fields: {
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    description: {
      type: "text",
      required: false,
      nullable: true,
      translatable: true,
      validation: {},
      label: {
        ja: "説明",
        en: "Description",
        vi: "Mô tả",
      },
      placeholder: {
        ja: "説明を入力",
        en: "Enter Description",
        vi: "Nhập Mô tả",
      },
    },
    cost_points: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "必要ポイント",
        en: "Cost (points)",
        vi: "Điểm cần",
      },
      placeholder: {
        ja: "必要ポイントを入力",
        en: "Enter Cost (points)",
        vi: "Nhập Điểm cần",
      },
    },
    discount_type: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "CouponDiscountType",
      enumValues: ["fixed", "percent"] as const,
      label: {
        ja: "割引種別",
        en: "Discount Type",
        vi: "Loại giảm giá",
      },
      placeholder: {
        ja: "割引種別を入力",
        en: "Enter Discount Type",
        vi: "Nhập Loại giảm giá",
      },
    },
    discount_value: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "割引額",
        en: "Discount Value",
        vi: "Giá trị giảm",
      },
      placeholder: {
        ja: "割引額を入力",
        en: "Enter Discount Value",
        vi: "Nhập Giá trị giảm",
      },
    },
    max_discount_cap: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最大割引額",
        en: "Max Discount Cap",
        vi: "Giảm tối đa",
      },
      placeholder: {
        ja: "最大割引額を入力",
        en: "Enter Max Discount Cap",
        vi: "Nhập Giảm tối đa",
      },
    },
    min_order_subtotal: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "最低注文金額",
        en: "Minimum Order Subtotal",
        vi: "Hóa đơn tối thiểu",
      },
      placeholder: {
        ja: "最低注文金額を入力",
        en: "Enter Minimum Order Subtotal",
        vi: "Nhập Hóa đơn tối thiểu",
      },
    },
    valid_days: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "クーポン有効日数",
        en: "Coupon Valid Days",
        vi: "Số ngày hiệu lực",
      },
      placeholder: {
        ja: "クーポン有効日数を入力",
        en: "Enter Coupon Valid Days",
        vi: "Nhập Số ngày hiệu lực",
      },
    },
    stock_quantity: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "在庫数",
        en: "Stock Quantity",
        vi: "Số lượng",
      },
      placeholder: {
        ja: "在庫数を入力",
        en: "Enter Stock Quantity",
        vi: "Nhập Số lượng",
      },
    },
    redeemed_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "交換済み数",
        en: "Redeemed Count",
        vi: "Đã đổi",
      },
      placeholder: {
        ja: "交換済み数を入力",
        en: "Enter Redeemed Count",
        vi: "Nhập Đã đổi",
      },
    },
    service_condition: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "PointRewardServiceCondition",
      enumValues: ["dine_in", "takeaway", "both"] as const,
      label: {
        ja: "提供条件",
        en: "Service Condition",
        vi: "Điều kiện phục vụ",
      },
      placeholder: {
        ja: "提供条件を入力",
        en: "Enter Service Condition",
        vi: "Nhập Điều kiện phục vụ",
      },
    },
    image: {
      type: "file",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "画像",
        en: "Image",
        vi: "Ảnh",
      },
      placeholder: {
        ja: "画像を入力",
        en: "Enter Image",
        vi: "Nhập Ảnh",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Đang bật",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Đang bật",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Sort Order",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự",
      },
    },
    brand_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Brand",
        table: "brands",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: ["name", "description"] as const,
  requiredFields: ["name", "cost_points", "discount_type", "discount_value", "min_order_subtotal", "valid_days", "redeemed_count", "service_condition", "image", "is_active", "sort_order", "brand_id", "organization_id"] as const,
  enumFields: ["discount_type", "service_condition"] as const,
  relationFields: ["brand_id", "organization_id"] as const,
} as const;

export type PointRewardFieldName = keyof typeof pointRewardMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PointReward. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PointRewardCreateFormState {
  name: Record<Locale, string>;
  description: Record<Locale, string>;
  cost_points: number;
  discount_type: CouponDiscountType;
  discount_value: number;
  max_discount_cap?: number | null;
  min_order_subtotal: number;
  valid_days: number;
  stock_quantity?: number | null;
  redeemed_count: number;
  service_condition: PointRewardServiceCondition;
  is_active: boolean;
  sort_order: number;
  brand_id: string;
  organization_id: string;
}

/** Form-state shape for updating a PointReward. Every field optional. */
export interface PointRewardUpdateFormState {
  name?: Record<Locale, string>;
  description?: Record<Locale, string>;
  cost_points?: number;
  discount_type?: CouponDiscountType;
  discount_value?: number;
  max_discount_cap?: number | null;
  min_order_subtotal?: number;
  valid_days?: number;
  stock_quantity?: number | null;
  redeemed_count?: number;
  service_condition?: PointRewardServiceCondition;
  is_active?: boolean;
  sort_order?: number;
  brand_id?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPointRewardCreateForm(): PointRewardCreateFormState {
  return {
    name: emptyLocaleMap(),
    description: emptyLocaleMap(),
    cost_points: 0,
    discount_type: CouponDiscountType.Fixed,
    discount_value: 0,
    max_discount_cap: 0,
    min_order_subtotal: 0,
    valid_days: 30,
    stock_quantity: 0,
    redeemed_count: 0,
    service_condition: PointRewardServiceCondition.Both,
    is_active: true,
    sort_order: 0,
    brand_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that basePointRewardCreateSchema accepts. */
export function buildPointRewardCreatePayload(form: PointRewardCreateFormState): BasePointRewardCreate {
  const payload: Record<string, unknown> = {
    name: (form.name["ja"] ?? '').trim(),
    description: (form.description["ja"] ?? '').trim(),
    cost_points: form.cost_points,
    discount_type: form.discount_type,
    discount_value: form.discount_value,
    max_discount_cap: form.max_discount_cap,
    min_order_subtotal: form.min_order_subtotal,
    valid_days: form.valid_days,
    stock_quantity: form.stock_quantity,
    redeemed_count: form.redeemed_count,
    service_condition: form.service_condition,
    is_active: form.is_active,
    sort_order: form.sort_order,
    brand_id: form.brand_id.trim(),
    organization_id: form.organization_id.trim(),
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    name: form.name,
    description: form.description,
  });
  Object.assign(payload, i18n);

  return payload as BasePointRewardCreate;
}

/** Convert update form state into the payload that basePointRewardUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPointRewardUpdatePayload(form: PointRewardUpdateFormState): BasePointRewardUpdate {
  const payload: Record<string, unknown> = {};
  if (form.name !== undefined) {
    payload.name = (form.name["ja"] ?? '').trim();
  }
  if (form.description !== undefined) {
    payload.description = (form.description["ja"] ?? '').trim();
  }
  if (form.cost_points !== undefined) {
    payload.cost_points = form.cost_points;
  }
  if (form.discount_type !== undefined) {
    payload.discount_type = form.discount_type;
  }
  if (form.discount_value !== undefined) {
    payload.discount_value = form.discount_value;
  }
  if (form.max_discount_cap !== undefined) {
    payload.max_discount_cap = form.max_discount_cap;
  }
  if (form.min_order_subtotal !== undefined) {
    payload.min_order_subtotal = form.min_order_subtotal;
  }
  if (form.valid_days !== undefined) {
    payload.valid_days = form.valid_days;
  }
  if (form.stock_quantity !== undefined) {
    payload.stock_quantity = form.stock_quantity;
  }
  if (form.redeemed_count !== undefined) {
    payload.redeemed_count = form.redeemed_count;
  }
  if (form.service_condition !== undefined) {
    payload.service_condition = form.service_condition;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.name !== undefined) localeInput.name = form.name;
  if (form.description !== undefined) localeInput.description = form.description;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BasePointRewardUpdate;
}
