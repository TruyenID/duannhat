/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Notification } from './Notification';
import type { NotificationRule } from './NotificationRule';

/**
 * ルール発火履歴
 */
export interface NotificationRuleFiring {
  id: string;
  rule_id: string;
  rule?: NotificationRule;
  notification_id?: string | null;
  notification?: Notification | null;
  model_type?: string | null;
  model_id?: string | null;
  fired_at: DateTimeString;
  outcome: string;
  evaluation_trace?: unknown | null;
  error_message?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationRuleFiring
 */
export const notificationRuleFiringI18n = {
  label: {"ja":"ルール発火履歴","en":"Notification Rule Firing","vi":"Lịch sử fire rule"},
  fields: {
    rule: {
      label: {"ja":"ルール","en":"Rule","vi":"Rule"},
      placeholder: {"ja":"ルールを入力","en":"Enter Rule","vi":"Nhập Rule"},
    },
    notification: {
      label: {"ja":"通知","en":"Notification","vi":"Notification"},
      placeholder: {"ja":"通知を入力","en":"Enter Notification","vi":"Nhập Notification"},
    },
    model_type: {
      label: {"ja":"モデル型","en":"Model Type","vi":"Model type"},
      placeholder: {"ja":"モデル型を入力","en":"Enter Model Type","vi":"Nhập Model type"},
    },
    model_id: {
      label: {"ja":"モデル ID","en":"Model ID","vi":"Model ID"},
      placeholder: {"ja":"モデル IDを入力","en":"Enter Model ID","vi":"Nhập Model ID"},
    },
    fired_at: {
      label: {"ja":"発火日時","en":"Fired At","vi":"Thời điểm fire"},
      placeholder: {"ja":"発火日時を入力","en":"Enter Fired At","vi":"Nhập Thời điểm fire"},
    },
    outcome: {
      label: {"ja":"結果","en":"Outcome","vi":"Kết quả"},
      placeholder: {"ja":"結果を入力","en":"Enter Outcome","vi":"Nhập Kết quả"},
    },
    evaluation_trace: {
      label: {"ja":"評価トレース","en":"Evaluation Trace","vi":"Evaluation trace"},
      placeholder: {"ja":"評価トレースを入力","en":"Enter Evaluation Trace","vi":"Nhập Evaluation trace"},
    },
    error_message: {
      label: {"ja":"エラーメッセージ","en":"Error Message","vi":"Error message"},
      placeholder: {"ja":"エラーメッセージを入力","en":"Enter Error Message","vi":"Nhập Error message"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationRuleFiring */
export const baseNotificationRuleFiringSchemas = {
  model_type: z.string().max(100).optional().nullable(),
  model_id: z.string().max(64).optional().nullable(),
  fired_at: z.string().datetime({ offset: true }),
  outcome: z.string().min(1).max(30),
  evaluation_trace: z.unknown().optional().nullable(),
  error_message: z.string().optional().nullable(),
} as const;

/** Create schema for NotificationRuleFiring */
export const baseNotificationRuleFiringCreateSchema = z.object({
  model_type: baseNotificationRuleFiringSchemas.model_type,
  model_id: baseNotificationRuleFiringSchemas.model_id,
  fired_at: baseNotificationRuleFiringSchemas.fired_at,
  outcome: baseNotificationRuleFiringSchemas.outcome,
  evaluation_trace: baseNotificationRuleFiringSchemas.evaluation_trace,
  error_message: baseNotificationRuleFiringSchemas.error_message,
});

/** Update schema for NotificationRuleFiring */
export const baseNotificationRuleFiringUpdateSchema = baseNotificationRuleFiringCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationRuleFiringCreate = z.infer<typeof baseNotificationRuleFiringCreateSchema>;
export type BaseNotificationRuleFiringUpdate = z.infer<typeof baseNotificationRuleFiringUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationRuleFiringLabel(locale: string): string {
  return notificationRuleFiringI18n.label[locale as keyof typeof notificationRuleFiringI18n.label] ?? notificationRuleFiringI18n.label['en'] ?? 'NotificationRuleFiring';
}

/** Get field label for a specific locale */
export function getNotificationRuleFiringFieldLabel(field: string, locale: string): string {
  const fields = notificationRuleFiringI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationRuleFiringFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationRuleFiringI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationRuleFiring.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationRuleFiringMetadata = {
  modelName: "NotificationRuleFiring",
  table: "notification_rule_firings",

  fields: {
    rule_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "NotificationRule",
        table: "notification_rules",
        onDelete: "CASCADE",
      },
      label: {},
    },
    notification_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "Notification",
        table: "notifications",
        onDelete: "SET NULL",
      },
      label: {},
    },
    model_type: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "モデル型",
        en: "Model Type",
        vi: "Model type",
      },
      placeholder: {
        ja: "モデル型を入力",
        en: "Enter Model Type",
        vi: "Nhập Model type",
      },
      description: {
        ja: "発火 Eloquent モデルのモーフエイリアス。custom.* イベントトリガーでは null。",
        en: "Morph alias of the firing Eloquent model. Null on custom.* event triggers.",
        vi: "Morph alias của Eloquent model fire. Null cho trigger custom.*.",
      },
    },
    model_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "モデル ID",
        en: "Model ID",
        vi: "Model ID",
      },
      placeholder: {
        ja: "モデル IDを入力",
        en: "Enter Model ID",
        vi: "Nhập Model ID",
      },
      description: {
        ja: "発火モデル行の主キー。UUID と bigInt PK を吸収するため文字列型。custom.* トリガーでは null。",
        en: "Primary key of the firing model row. String to absorb UUID and bigInt PKs alike. Null on custom.* triggers.",
        vi: "PK của row model fire. Dạng String để hấp thụ cả UUID và bigInt PK. Null cho trigger custom.*.",
      },
    },
    fired_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "発火日時",
        en: "Fired At",
        vi: "Thời điểm fire",
      },
      placeholder: {
        ja: "発火日時を入力",
        en: "Enter Fired At",
        vi: "Nhập Thời điểm fire",
      },
      description: {
        ja: "EvaluateRuleJob がトリガーを処理した日時。ルール毎の発火ページ用にインデックス化。",
        en: "When the EvaluateRuleJob processed the trigger. Indexed for the per-rule firings page.",
        vi: "Khi EvaluateRuleJob xử lý trigger. Indexed cho trang firings per-rule.",
      },
    },
    outcome: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 30,
      },
      label: {
        ja: "結果",
        en: "Outcome",
        vi: "Kết quả",
      },
      placeholder: {
        ja: "結果を入力",
        en: "Enter Outcome",
        vi: "Nhập Kết quả",
      },
      description: {
        ja: "`matched` / `skipped_cooldown` / `skipped_condition` / `shadow` / `error`。意味論についてはスキーマヘッダを参照。",
        en: "`matched` / `skipped_cooldown` / `skipped_condition` / `shadow` / `error`. See schema header for semantics.",
        vi: "`matched` / `skipped_cooldown` / `skipped_condition` / `shadow` / `error`. Semantic xem header schema.",
      },
    },
    evaluation_trace: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "評価トレース",
        en: "Evaluation Trace",
        vi: "Evaluation trace",
      },
      placeholder: {
        ja: "評価トレースを入力",
        en: "Enter Evaluation Trace",
        vi: "Nhập Evaluation trace",
      },
      description: {
        ja: "RuleEvaluatorService::evaluate により生成されたリーフ毎の合格／不合格内訳。管理者「なぜ発火しなかったか？」デバッグビューを駆動。形状：`[{field, op, expected, actual, pass}, …]`。",
        en: "Per-leaf pass/fail breakdown produced by RuleEvaluatorService::evaluate. Powers the admin \"why didn't this fire?\" debug view. Shape: `[{field, op, expected, actual, pass}, …]`.",
        vi: "Breakdown pass/fail từng leaf do RuleEvaluatorService::evaluate sinh ra. Driver cho admin debug view \"tại sao không fire?\". Shape: `[{field, op, expected, actual, pass}, …]`.",
      },
    },
    error_message: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "エラーメッセージ",
        en: "Error Message",
        vi: "Error message",
      },
      placeholder: {
        ja: "エラーメッセージを入力",
        en: "Enter Error Message",
        vi: "Nhập Error message",
      },
      description: {
        ja: "outcome=error のときにキャプチャされた throwable メッセージ。他の結果では null。",
        en: "Captured throwable message when outcome=error. Null for every other outcome.",
        vi: "Throwable message capture khi outcome=error. Null cho mọi outcome khác.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["rule_id", "fired_at", "outcome"] as const,
  enumFields: [] as const,
  relationFields: ["rule_id", "notification_id"] as const,
} as const;

export type NotificationRuleFiringFieldName = keyof typeof notificationRuleFiringMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationRuleFiring. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationRuleFiringCreateFormState {
  rule_id: string;
  notification_id?: string | null;
  model_type?: string | null;
  model_id?: string | null;
  fired_at: string;
  outcome: string;
  evaluation_trace?: unknown | null;
  error_message?: string | null;
}

/** Form-state shape for updating a NotificationRuleFiring. Every field optional. */
export interface NotificationRuleFiringUpdateFormState {
  rule_id?: string;
  notification_id?: string | null;
  model_type?: string | null;
  model_id?: string | null;
  fired_at?: string;
  outcome?: string;
  evaluation_trace?: unknown | null;
  error_message?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationRuleFiringCreateForm(): NotificationRuleFiringCreateFormState {
  return {
    rule_id: '',
    notification_id: '',
    model_type: '',
    model_id: '',
    fired_at: '',
    outcome: '',
    evaluation_trace: null,
    error_message: '',
  };
}

/** Convert create form state into the payload that baseNotificationRuleFiringCreateSchema accepts. */
export function buildNotificationRuleFiringCreatePayload(form: NotificationRuleFiringCreateFormState): BaseNotificationRuleFiringCreate {
  const payload: Record<string, unknown> = {
    rule_id: form.rule_id.trim(),
    notification_id: form.notification_id?.toString().trim() || null,
    model_type: form.model_type?.toString().trim() || null,
    model_id: form.model_id?.toString().trim() || null,
    fired_at: form.fired_at,
    outcome: form.outcome.trim(),
    evaluation_trace: form.evaluation_trace,
    error_message: form.error_message?.toString().trim() || null,
  };

  return payload as BaseNotificationRuleFiringCreate;
}

/** Convert update form state into the payload that baseNotificationRuleFiringUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationRuleFiringUpdatePayload(form: NotificationRuleFiringUpdateFormState): BaseNotificationRuleFiringUpdate {
  const payload: Record<string, unknown> = {};
  if (form.rule_id !== undefined) {
    payload.rule_id = form.rule_id?.toString().trim() ?? null;
  }
  if (form.notification_id !== undefined) {
    payload.notification_id = form.notification_id?.toString().trim() ?? null;
  }
  if (form.model_type !== undefined) {
    payload.model_type = form.model_type?.toString().trim() ?? null;
  }
  if (form.model_id !== undefined) {
    payload.model_id = form.model_id?.toString().trim() ?? null;
  }
  if (form.fired_at !== undefined) {
    payload.fired_at = form.fired_at;
  }
  if (form.outcome !== undefined) {
    payload.outcome = form.outcome?.toString().trim() ?? null;
  }
  if (form.evaluation_trace !== undefined) {
    payload.evaluation_trace = form.evaluation_trace;
  }
  if (form.error_message !== undefined) {
    payload.error_message = form.error_message?.toString().trim() ?? null;
  }
  return payload as BaseNotificationRuleFiringUpdate;
}
