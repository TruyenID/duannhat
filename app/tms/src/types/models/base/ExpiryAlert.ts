/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { MaterialLot } from './MaterialLot';

/**
 * 期限アラート
 */
export interface ExpiryAlert {
  id: string;
  material_lot_id: string;
  materialLot?: MaterialLot;
  threshold_days: number;
  fired_at: DateTimeString;
  notification_id?: string | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ExpiryAlert
 */
export const expiryAlertI18n = {
  label: {"ja":"期限アラート","en":"Expiry Alert","vi":"Cảnh báo hạn"},
  fields: {
    material_lot: {
      label: {"ja":"材料ロット","en":"Material Lot","vi":"Lô nguyên liệu"},
      placeholder: {"ja":"材料ロットを入力","en":"Enter Material Lot","vi":"Nhập Lô nguyên liệu"},
    },
    threshold_days: {
      label: {"ja":"閾値（日）","en":"Threshold (days)","vi":"Ngưỡng (ngày)"},
      placeholder: {"ja":"閾値（日）を入力","en":"Enter Threshold (days)","vi":"Nhập Ngưỡng (ngày)"},
    },
    fired_at: {
      label: {"ja":"発火日時","en":"Fired At","vi":"Thời điểm kích hoạt"},
      placeholder: {"ja":"発火日時を入力","en":"Enter Fired At","vi":"Nhập Thời điểm kích hoạt"},
    },
    notification_id: {
      label: {"ja":"通知ID","en":"Notification ID","vi":"ID thông báo"},
      placeholder: {"ja":"通知IDを入力","en":"Enter Notification ID","vi":"Nhập ID thông báo"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ExpiryAlert */
export const baseExpiryAlertSchemas = {
  threshold_days: z.number().int(),
  fired_at: z.string().datetime({ offset: true }),
  notification_id: z.string().optional().nullable(),
} as const;

/** Create schema for ExpiryAlert */
export const baseExpiryAlertCreateSchema = z.object({
  threshold_days: baseExpiryAlertSchemas.threshold_days,
  fired_at: baseExpiryAlertSchemas.fired_at,
  notification_id: baseExpiryAlertSchemas.notification_id,
});

/** Update schema for ExpiryAlert */
export const baseExpiryAlertUpdateSchema = baseExpiryAlertCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseExpiryAlertCreate = z.infer<typeof baseExpiryAlertCreateSchema>;
export type BaseExpiryAlertUpdate = z.infer<typeof baseExpiryAlertUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getExpiryAlertLabel(locale: string): string {
  return expiryAlertI18n.label[locale as keyof typeof expiryAlertI18n.label] ?? expiryAlertI18n.label['en'] ?? 'ExpiryAlert';
}

/** Get field label for a specific locale */
export function getExpiryAlertFieldLabel(field: string, locale: string): string {
  const fields = expiryAlertI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getExpiryAlertFieldPlaceholder(field: string, locale: string): string {
  const fields = expiryAlertI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ExpiryAlert.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const expiryAlertMetadata = {
  modelName: "ExpiryAlert",
  table: "expiry_alerts",

  fields: {
    material_lot_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "MaterialLot",
        table: "material_lots",
        onDelete: "CASCADE",
      },
      label: {},
    },
    threshold_days: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "閾値（日）",
        en: "Threshold (days)",
        vi: "Ngưỡng (ngày)",
      },
      placeholder: {
        ja: "閾値（日）を入力",
        en: "Enter Threshold (days)",
        vi: "Nhập Ngưỡng (ngày)",
      },
      description: {
        ja: "このアラートをトリガーした日数ウィンドウ（例：7、3、1）。",
        en: "Days-out window that fired this alert (e.g. 7, 3, 1).",
        vi: "Ngưỡng số ngày kích hoạt cảnh báo (VD: 7, 3, 1).",
      },
    },
    fired_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "発火日時",
        en: "Fired At",
        vi: "Thời điểm kích hoạt",
      },
      placeholder: {
        ja: "発火日時を入力",
        en: "Enter Fired At",
        vi: "Nhập Thời điểm kích hoạt",
      },
      description: {
        ja: "ExpiryAlertService::scan()がこの行を作成した日時。DB DEFAULT で挿入時に設定。",
        en: "When ExpiryAlertService::scan() created this row. Set on insert via DB DEFAULT.",
        vi: "Thời điểm ExpiryAlertService::scan() tạo row. Set khi insert qua DB DEFAULT.",
      },
    },
    notification_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "通知ID",
        en: "Notification ID",
        vi: "ID thông báo",
      },
      placeholder: {
        ja: "通知IDを入力",
        en: "Enter Notification ID",
        vi: "Nhập ID thông báo",
      },
      description: {
        ja: "通知が正常にディスパッチされた場合のnotifications.idへのソフトFK。NULL = 作成のみのパス（通知は将来のTier 1.C仕上げに延期）。",
        en: "Soft FK to notifications.id when a notification was successfully dispatched. NULL = create-only path (notification deferred to a future Tier 1.C polish).",
        vi: "Soft FK đến notifications.id khi thông báo được dispatch thành công. NULL = chỉ tạo row (thông báo để sau).",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["material_lot_id", "threshold_days", "fired_at"] as const,
  enumFields: [] as const,
  relationFields: ["material_lot_id"] as const,
} as const;

export type ExpiryAlertFieldName = keyof typeof expiryAlertMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ExpiryAlert. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ExpiryAlertCreateFormState {
  material_lot_id: string;
  threshold_days: number;
  fired_at: string;
  notification_id?: string | null;
}

/** Form-state shape for updating a ExpiryAlert. Every field optional. */
export interface ExpiryAlertUpdateFormState {
  material_lot_id?: string;
  threshold_days?: number;
  fired_at?: string;
  notification_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyExpiryAlertCreateForm(): ExpiryAlertCreateFormState {
  return {
    material_lot_id: '',
    threshold_days: 0,
    fired_at: '',
    notification_id: '',
  };
}

/** Convert create form state into the payload that baseExpiryAlertCreateSchema accepts. */
export function buildExpiryAlertCreatePayload(form: ExpiryAlertCreateFormState): BaseExpiryAlertCreate {
  const payload: Record<string, unknown> = {
    material_lot_id: form.material_lot_id.trim(),
    threshold_days: form.threshold_days,
    fired_at: form.fired_at,
    notification_id: form.notification_id?.toString().trim() || null,
  };

  return payload as BaseExpiryAlertCreate;
}

/** Convert update form state into the payload that baseExpiryAlertUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildExpiryAlertUpdatePayload(form: ExpiryAlertUpdateFormState): BaseExpiryAlertUpdate {
  const payload: Record<string, unknown> = {};
  if (form.material_lot_id !== undefined) {
    payload.material_lot_id = form.material_lot_id?.toString().trim() ?? null;
  }
  if (form.threshold_days !== undefined) {
    payload.threshold_days = form.threshold_days;
  }
  if (form.fired_at !== undefined) {
    payload.fired_at = form.fired_at;
  }
  if (form.notification_id !== undefined) {
    payload.notification_id = form.notification_id?.toString().trim() ?? null;
  }
  return payload as BaseExpiryAlertUpdate;
}
