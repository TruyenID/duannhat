/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString, DateString } from '../common';
import type { MenuSchedule } from './MenuSchedule';
import type { Organization } from './Organization';

/**
 * メニュースケジュール特定日
 */
export interface MenuScheduleDate {
  id: string;
  date: DateString;
  menu_schedule_id: string;
  menuSchedule?: MenuSchedule;
  organization_id: string;
  organization?: Organization;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuScheduleDate
 */
export const menuScheduleDateI18n = {
  label: {"ja":"メニュースケジュール特定日","en":"Menu Schedule Date","vi":"Ngày cụ thể của lịch menu"},
  fields: {
    date: {
      label: {"ja":"日付","en":"Date","vi":"Ngày"},
      placeholder: {"ja":"日付を入力","en":"Enter Date","vi":"Nhập Ngày"},
    },
    menu_schedule: {
      label: {"ja":"メニュースケジュール","en":"Menu Schedule","vi":"Lịch trình menu"},
      placeholder: {"ja":"メニュースケジュールを入力","en":"Enter Menu Schedule","vi":"Nhập Lịch trình menu"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuScheduleDate */
export const baseMenuScheduleDateSchemas = {
  date: z.string().date(),
} as const;

/** Create schema for MenuScheduleDate */
export const baseMenuScheduleDateCreateSchema = z.object({
  date: baseMenuScheduleDateSchemas.date,
});

/** Update schema for MenuScheduleDate */
export const baseMenuScheduleDateUpdateSchema = baseMenuScheduleDateCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuScheduleDateCreate = z.infer<typeof baseMenuScheduleDateCreateSchema>;
export type BaseMenuScheduleDateUpdate = z.infer<typeof baseMenuScheduleDateUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuScheduleDateLabel(locale: string): string {
  return menuScheduleDateI18n.label[locale as keyof typeof menuScheduleDateI18n.label] ?? menuScheduleDateI18n.label['en'] ?? 'MenuScheduleDate';
}

/** Get field label for a specific locale */
export function getMenuScheduleDateFieldLabel(field: string, locale: string): string {
  const fields = menuScheduleDateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuScheduleDateFieldPlaceholder(field: string, locale: string): string {
  const fields = menuScheduleDateI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuScheduleDate.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuScheduleDateMetadata = {
  modelName: "MenuScheduleDate",
  table: "menu_schedule_dates",

  fields: {
    date: {
      type: "date",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "日付",
        en: "Date",
        vi: "Ngày",
      },
      placeholder: {
        ja: "日付を入力",
        en: "Enter Date",
        vi: "Nhập Ngày",
      },
    },
    menu_schedule_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "MenuSchedule",
        table: "menu_schedules",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: "CASCADE",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["date", "menu_schedule_id", "organization_id"] as const,
  enumFields: [] as const,
  relationFields: ["menu_schedule_id", "organization_id"] as const,
} as const;

export type MenuScheduleDateFieldName = keyof typeof menuScheduleDateMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuScheduleDate. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuScheduleDateCreateFormState {
  date: string;
  menu_schedule_id: string;
  organization_id: string;
}

/** Form-state shape for updating a MenuScheduleDate. Every field optional. */
export interface MenuScheduleDateUpdateFormState {
  date?: string;
  menu_schedule_id?: string;
  organization_id?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuScheduleDateCreateForm(): MenuScheduleDateCreateFormState {
  return {
    date: '',
    menu_schedule_id: '',
    organization_id: '',
  };
}

/** Convert create form state into the payload that baseMenuScheduleDateCreateSchema accepts. */
export function buildMenuScheduleDateCreatePayload(form: MenuScheduleDateCreateFormState): BaseMenuScheduleDateCreate {
  const payload: Record<string, unknown> = {
    date: form.date,
    menu_schedule_id: form.menu_schedule_id.trim(),
    organization_id: form.organization_id.trim(),
  };

  return payload as BaseMenuScheduleDateCreate;
}

/** Convert update form state into the payload that baseMenuScheduleDateUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuScheduleDateUpdatePayload(form: MenuScheduleDateUpdateFormState): BaseMenuScheduleDateUpdate {
  const payload: Record<string, unknown> = {};
  if (form.date !== undefined) {
    payload.date = form.date;
  }
  if (form.menu_schedule_id !== undefined) {
    payload.menu_schedule_id = form.menu_schedule_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  return payload as BaseMenuScheduleDateUpdate;
}
