/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { PaymentGatewayConnection } from './PaymentGatewayConnection';
import type { User } from './User';

/**
 * SettlementReportBatch
 */
export interface SettlementReportBatch {
  id: string;
  connection_id: string;
  connection?: PaymentGatewayConnection;
  provider: string;
  cycle_label: string;
  file_hash: string;
  row_count: number;
  matched_count: number;
  orphan_count: number;
  imported_by_id?: string | null;
  importedBy?: User | null;
  imported_at: DateTimeString;
  status: string;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for SettlementReportBatch
 */
export const settlementReportBatchI18n = {
  label: {"ja":"SettlementReportBatch","en":"SettlementReportBatch","vi":"SettlementReportBatch"},
  fields: {
    connection: {
      label: {"ja":"接続","en":"Connection","vi":"Kết nối"},
      placeholder: {"ja":"接続を入力","en":"Enter Connection","vi":"Nhập Kết nối"},
    },
    provider: {
      label: {"ja":"プロバイダ","en":"Provider","vi":"Nhà cung cấp"},
      placeholder: {"ja":"プロバイダを入力","en":"Enter Provider","vi":"Nhập Nhà cung cấp"},
    },
    cycle_label: {
      label: {"ja":"精算サイクル","en":"Cycle Label","vi":"Kỳ đối soát"},
      placeholder: {"ja":"精算サイクルを入力","en":"Enter Cycle Label","vi":"Nhập Kỳ đối soát"},
    },
    file_hash: {
      label: {"ja":"ファイルハッシュ","en":"File Hash","vi":"Hash tệp"},
      placeholder: {"ja":"ファイルハッシュを入力","en":"Enter File Hash","vi":"Nhập Hash tệp"},
    },
    row_count: {
      label: {"ja":"行数","en":"Row Count","vi":"Số dòng"},
      placeholder: {"ja":"行数を入力","en":"Enter Row Count","vi":"Nhập Số dòng"},
    },
    matched_count: {
      label: {"ja":"突合済","en":"Matched Count","vi":"Số dòng khớp"},
      placeholder: {"ja":"突合済を入力","en":"Enter Matched Count","vi":"Nhập Số dòng khớp"},
    },
    orphan_count: {
      label: {"ja":"孤立行","en":"Orphan Count","vi":"Số dòng mồ côi"},
      placeholder: {"ja":"孤立行を入力","en":"Enter Orphan Count","vi":"Nhập Số dòng mồ côi"},
    },
    imported_by: {
      label: {"ja":"取込者","en":"Imported By","vi":"Người nhập"},
      placeholder: {"ja":"取込者を入力","en":"Enter Imported By","vi":"Nhập Người nhập"},
    },
    imported_at: {
      label: {"ja":"取込日時","en":"Imported At","vi":"Nhập lúc"},
      placeholder: {"ja":"取込日時を入力","en":"Enter Imported At","vi":"Nhập Nhập lúc"},
    },
    status: {
      label: {"ja":"状態","en":"Status","vi":"Trạng thái"},
      placeholder: {"ja":"状態を入力","en":"Enter Status","vi":"Nhập Trạng thái"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for SettlementReportBatch */
export const baseSettlementReportBatchSchemas = {
  provider: z.string().min(1).max(50),
  cycle_label: z.string().min(1).max(100),
  file_hash: z.string().min(1).max(64),
  row_count: z.number().int(),
  matched_count: z.number().int(),
  orphan_count: z.number().int(),
  imported_at: z.string().datetime({ offset: true }),
  status: z.string().min(1).max(50),
} as const;

/** Create schema for SettlementReportBatch */
export const baseSettlementReportBatchCreateSchema = z.object({
  provider: baseSettlementReportBatchSchemas.provider,
  cycle_label: baseSettlementReportBatchSchemas.cycle_label,
  file_hash: baseSettlementReportBatchSchemas.file_hash,
  row_count: baseSettlementReportBatchSchemas.row_count,
  matched_count: baseSettlementReportBatchSchemas.matched_count,
  orphan_count: baseSettlementReportBatchSchemas.orphan_count,
  imported_at: baseSettlementReportBatchSchemas.imported_at,
  status: baseSettlementReportBatchSchemas.status,
});

/** Update schema for SettlementReportBatch */
export const baseSettlementReportBatchUpdateSchema = baseSettlementReportBatchCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseSettlementReportBatchCreate = z.infer<typeof baseSettlementReportBatchCreateSchema>;
export type BaseSettlementReportBatchUpdate = z.infer<typeof baseSettlementReportBatchUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getSettlementReportBatchLabel(locale: string): string {
  return settlementReportBatchI18n.label[locale as keyof typeof settlementReportBatchI18n.label] ?? settlementReportBatchI18n.label['en'] ?? 'SettlementReportBatch';
}

/** Get field label for a specific locale */
export function getSettlementReportBatchFieldLabel(field: string, locale: string): string {
  const fields = settlementReportBatchI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getSettlementReportBatchFieldPlaceholder(field: string, locale: string): string {
  const fields = settlementReportBatchI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for SettlementReportBatch.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const settlementReportBatchMetadata = {
  modelName: "SettlementReportBatch",
  table: "settlement_report_batches",

  fields: {
    connection_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "PaymentGatewayConnection",
        table: "payment_gateway_connections",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    provider: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "プロバイダ",
        en: "Provider",
        vi: "Nhà cung cấp",
      },
      placeholder: {
        ja: "プロバイダを入力",
        en: "Enter Provider",
        vi: "Nhập Nhà cung cấp",
      },
    },
    cycle_label: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "精算サイクル",
        en: "Cycle Label",
        vi: "Kỳ đối soát",
      },
      placeholder: {
        ja: "精算サイクルを入力",
        en: "Enter Cycle Label",
        vi: "Nhập Kỳ đối soát",
      },
    },
    file_hash: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "ファイルハッシュ",
        en: "File Hash",
        vi: "Hash tệp",
      },
      placeholder: {
        ja: "ファイルハッシュを入力",
        en: "Enter File Hash",
        vi: "Nhập Hash tệp",
      },
    },
    row_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "行数",
        en: "Row Count",
        vi: "Số dòng",
      },
      placeholder: {
        ja: "行数を入力",
        en: "Enter Row Count",
        vi: "Nhập Số dòng",
      },
    },
    matched_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "突合済",
        en: "Matched Count",
        vi: "Số dòng khớp",
      },
      placeholder: {
        ja: "突合済を入力",
        en: "Enter Matched Count",
        vi: "Nhập Số dòng khớp",
      },
    },
    orphan_count: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "孤立行",
        en: "Orphan Count",
        vi: "Số dòng mồ côi",
      },
      placeholder: {
        ja: "孤立行を入力",
        en: "Enter Orphan Count",
        vi: "Nhập Số dòng mồ côi",
      },
    },
    imported_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "RESTRICT",
      },
      label: {},
    },
    imported_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "取込日時",
        en: "Imported At",
        vi: "Nhập lúc",
      },
      placeholder: {
        ja: "取込日時を入力",
        en: "Enter Imported At",
        vi: "Nhập Nhập lúc",
      },
    },
    status: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "状態",
        en: "Status",
        vi: "Trạng thái",
      },
      placeholder: {
        ja: "状態を入力",
        en: "Enter Status",
        vi: "Nhập Trạng thái",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["connection_id", "provider", "cycle_label", "file_hash", "row_count", "matched_count", "orphan_count", "imported_at", "status"] as const,
  enumFields: [] as const,
  relationFields: ["connection_id", "imported_by_id"] as const,
} as const;

export type SettlementReportBatchFieldName = keyof typeof settlementReportBatchMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a SettlementReportBatch. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface SettlementReportBatchCreateFormState {
  connection_id: string;
  provider: string;
  cycle_label: string;
  file_hash: string;
  row_count: number;
  matched_count: number;
  orphan_count: number;
  imported_by_id?: string | null;
  imported_at: string;
  status: string;
}

/** Form-state shape for updating a SettlementReportBatch. Every field optional. */
export interface SettlementReportBatchUpdateFormState {
  connection_id?: string;
  provider?: string;
  cycle_label?: string;
  file_hash?: string;
  row_count?: number;
  matched_count?: number;
  orphan_count?: number;
  imported_by_id?: string | null;
  imported_at?: string;
  status?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptySettlementReportBatchCreateForm(): SettlementReportBatchCreateFormState {
  return {
    connection_id: '',
    provider: '',
    cycle_label: '',
    file_hash: '',
    row_count: 0,
    matched_count: 0,
    orphan_count: 0,
    imported_by_id: '',
    imported_at: '',
    status: "imported",
  };
}

/** Convert create form state into the payload that baseSettlementReportBatchCreateSchema accepts. */
export function buildSettlementReportBatchCreatePayload(form: SettlementReportBatchCreateFormState): BaseSettlementReportBatchCreate {
  const payload: Record<string, unknown> = {
    connection_id: form.connection_id.trim(),
    provider: form.provider.trim(),
    cycle_label: form.cycle_label.trim(),
    file_hash: form.file_hash.trim(),
    row_count: form.row_count,
    matched_count: form.matched_count,
    orphan_count: form.orphan_count,
    imported_by_id: form.imported_by_id?.toString().trim() || null,
    imported_at: form.imported_at,
    status: form.status.trim(),
  };

  return payload as BaseSettlementReportBatchCreate;
}

/** Convert update form state into the payload that baseSettlementReportBatchUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildSettlementReportBatchUpdatePayload(form: SettlementReportBatchUpdateFormState): BaseSettlementReportBatchUpdate {
  const payload: Record<string, unknown> = {};
  if (form.connection_id !== undefined) {
    payload.connection_id = form.connection_id?.toString().trim() ?? null;
  }
  if (form.provider !== undefined) {
    payload.provider = form.provider?.toString().trim() ?? null;
  }
  if (form.cycle_label !== undefined) {
    payload.cycle_label = form.cycle_label?.toString().trim() ?? null;
  }
  if (form.file_hash !== undefined) {
    payload.file_hash = form.file_hash?.toString().trim() ?? null;
  }
  if (form.row_count !== undefined) {
    payload.row_count = form.row_count;
  }
  if (form.matched_count !== undefined) {
    payload.matched_count = form.matched_count;
  }
  if (form.orphan_count !== undefined) {
    payload.orphan_count = form.orphan_count;
  }
  if (form.imported_by_id !== undefined) {
    payload.imported_by_id = form.imported_by_id?.toString().trim() ?? null;
  }
  if (form.imported_at !== undefined) {
    payload.imported_at = form.imported_at;
  }
  if (form.status !== undefined) {
    payload.status = form.status?.toString().trim() ?? null;
  }
  return payload as BaseSettlementReportBatchUpdate;
}
