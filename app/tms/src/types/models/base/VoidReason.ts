/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import type { Locale } from '../common';
import { buildI18nPayload, emptyLocaleMap } from '../payload-helpers';
import { z } from 'zod';
import type { DateTimeString } from '../common';
import { VoidStockEffect } from '../enum/VoidStockEffect';

/**
 * 取消理由
 */
export interface VoidReason {
  id: string;
  organization_id: string;
  brand_id: string;
  label: string;
  stock_effect: VoidStockEffect;
  requires_note: boolean;
  is_active: boolean;
  sort_order: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for VoidReason
 */
export const voidReasonI18n = {
  label: {"ja":"取消理由","en":"Void Reason","vi":"Lý do hủy món"},
  fields: {
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
    brand_id: {
      label: {"ja":"ブランドID","en":"Brand ID","vi":"ID thương hiệu"},
      placeholder: {"ja":"ブランドIDを入力","en":"Enter Brand ID","vi":"Nhập ID thương hiệu"},
    },
    label: {
      label: {"ja":"名称","en":"Label","vi":"Nhãn"},
      placeholder: {"ja":"名称を入力","en":"Enter Label","vi":"Nhập Nhãn"},
    },
    stock_effect: {
      label: {"ja":"在庫処理","en":"Stock Effect","vi":"Hiệu ứng kho"},
      placeholder: {"ja":"在庫処理を入力","en":"Enter Stock Effect","vi":"Nhập Hiệu ứng kho"},
    },
    requires_note: {
      label: {"ja":"備考必須","en":"Requires Note","vi":"Bắt buộc ghi chú"},
      placeholder: {"ja":"備考必須を入力","en":"Enter Requires Note","vi":"Nhập Bắt buộc ghi chú"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    sort_order: {
      label: {"ja":"表示順","en":"Sort Order","vi":"Thứ tự"},
      placeholder: {"ja":"表示順を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for VoidReason */
export const baseVoidReasonSchemas = {
  organization_id: z.string(),
  brand_id: z.string(),
  label: z.string().min(1).max(100),
  stock_effect: z.nativeEnum(VoidStockEffect),
  requires_note: z.boolean(),
  is_active: z.boolean(),
  sort_order: z.number().int(),
} as const;

/** Create schema for VoidReason */
export const baseVoidReasonCreateSchema = z.object({
  organization_id: baseVoidReasonSchemas.organization_id,
  brand_id: baseVoidReasonSchemas.brand_id,
  label: baseVoidReasonSchemas.label,
  stock_effect: baseVoidReasonSchemas.stock_effect,
  requires_note: baseVoidReasonSchemas.requires_note,
  is_active: baseVoidReasonSchemas.is_active,
  sort_order: baseVoidReasonSchemas.sort_order,
  ja: z.object({
    label: z.string().max(100).optional().nullable(),
  }).optional(),
  en: z.object({
    label: z.string().max(100).optional().nullable(),
  }).optional(),
  vi: z.object({
    label: z.string().max(100).optional().nullable(),
  }).optional(),
});

/** Update schema for VoidReason */
export const baseVoidReasonUpdateSchema = baseVoidReasonCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseVoidReasonCreate = z.infer<typeof baseVoidReasonCreateSchema>;
export type BaseVoidReasonUpdate = z.infer<typeof baseVoidReasonUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getVoidReasonLabel(locale: string): string {
  return voidReasonI18n.label[locale as keyof typeof voidReasonI18n.label] ?? voidReasonI18n.label['en'] ?? 'VoidReason';
}

/** Get field label for a specific locale */
export function getVoidReasonFieldLabel(field: string, locale: string): string {
  const fields = voidReasonI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getVoidReasonFieldPlaceholder(field: string, locale: string): string {
  const fields = voidReasonI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for VoidReason.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const voidReasonMetadata = {
  modelName: "VoidReason",
  table: "void_reasons",

  fields: {
    organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
    },
    brand_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ブランドID",
        en: "Brand ID",
        vi: "ID thương hiệu",
      },
      placeholder: {
        ja: "ブランドIDを入力",
        en: "Enter Brand ID",
        vi: "Nhập ID thương hiệu",
      },
    },
    label: {
      type: "string",
      required: true,
      nullable: false,
      translatable: true,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "名称",
        en: "Label",
        vi: "Nhãn",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Label",
        vi: "Nhập Nhãn",
      },
    },
    stock_effect: {
      type: "enum",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      enum: "VoidStockEffect",
      enumValues: ["waste", "restock", "none"] as const,
      label: {
        ja: "在庫処理",
        en: "Stock Effect",
        vi: "Hiệu ứng kho",
      },
      placeholder: {
        ja: "在庫処理を入力",
        en: "Enter Stock Effect",
        vi: "Nhập Hiệu ứng kho",
      },
    },
    requires_note: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "備考必須",
        en: "Requires Note",
        vi: "Bắt buộc ghi chú",
      },
      placeholder: {
        ja: "備考必須を入力",
        en: "Enter Requires Note",
        vi: "Nhập Bắt buộc ghi chú",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順",
        en: "Sort Order",
        vi: "Thứ tự",
      },
      placeholder: {
        ja: "表示順を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự",
      },
    },
  },

  translatableFields: ["label"] as const,
  requiredFields: ["organization_id", "brand_id", "label", "stock_effect", "requires_note", "is_active", "sort_order"] as const,
  enumFields: ["stock_effect"] as const,
  relationFields: [] as const,
} as const;

export type VoidReasonFieldName = keyof typeof voidReasonMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a VoidReason. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface VoidReasonCreateFormState {
  organization_id: string;
  brand_id: string;
  label: Record<Locale, string>;
  stock_effect: VoidStockEffect;
  requires_note: boolean;
  is_active: boolean;
  sort_order: number;
}

/** Form-state shape for updating a VoidReason. Every field optional. */
export interface VoidReasonUpdateFormState {
  organization_id?: string;
  brand_id?: string;
  label?: Record<Locale, string>;
  stock_effect?: VoidStockEffect;
  requires_note?: boolean;
  is_active?: boolean;
  sort_order?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyVoidReasonCreateForm(): VoidReasonCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    label: emptyLocaleMap(),
    stock_effect: VoidStockEffect.Waste,
    requires_note: false,
    is_active: true,
    sort_order: 0,
  };
}

/** Convert create form state into the payload that baseVoidReasonCreateSchema accepts. */
export function buildVoidReasonCreatePayload(form: VoidReasonCreateFormState): BaseVoidReasonCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    label: (form.label["ja"] ?? '').trim(),
    stock_effect: form.stock_effect,
    requires_note: form.requires_note,
    is_active: form.is_active,
    sort_order: form.sort_order,
  };

  // Per-locale sub-objects (matches issue #53 wire shape).
  const i18n = buildI18nPayload({
    label: form.label,
  });
  Object.assign(payload, i18n);

  return payload as BaseVoidReasonCreate;
}

/** Convert update form state into the payload that baseVoidReasonUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildVoidReasonUpdatePayload(form: VoidReasonUpdateFormState): BaseVoidReasonUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.label !== undefined) {
    payload.label = (form.label["ja"] ?? '').trim();
  }
  if (form.stock_effect !== undefined) {
    payload.stock_effect = form.stock_effect;
  }
  if (form.requires_note !== undefined) {
    payload.requires_note = form.requires_note;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }

  // Per-locale sub-objects only for fields the user touched.
  const localeInput: Record<string, Record<string, string> | undefined> = {};
  if (form.label !== undefined) localeInput.label = form.label;
  Object.assign(payload, buildI18nPayload(localeInput));
  return payload as BaseVoidReasonUpdate;
}
