/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * 注文金額上書き記録
 */
export interface OrderMoneyOverwrite {
  id: string;
  device_id: string;
  local_id: number;
  branch_id: string;
  organization_id: string;
  order_id: string;
  occurred_at: DateTimeString;
  paid_locally: number;
  total_amount_local: number;
  total_amount_cloud: number;
  subtotal_local: number;
  subtotal_cloud: number;
  tax_amount_local: number;
  tax_amount_cloud: number;
  service_charge_local: number;
  service_charge_cloud: number;
  discount_amount_local: number;
  discount_amount_cloud: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for OrderMoneyOverwrite
 */
export const orderMoneyOverwriteI18n = {
  label: {"ja":"注文金額上書き記録","en":"Order Money Overwrite","vi":"Bằng chứng ghi đè tiền của đơn"},
  fields: {
    device_id: {
      label: {"ja":"デバイスID","en":"Device ID","vi":"ID thiết bị"},
      placeholder: {"ja":"デバイスIDを入力","en":"Enter Device ID","vi":"Nhập ID thiết bị"},
    },
    local_id: {
      label: {"ja":"ローカル行ID","en":"Local Row ID","vi":"ID dòng cục bộ"},
      placeholder: {"ja":"ローカル行IDを入力","en":"Enter Local Row ID","vi":"Nhập ID dòng cục bộ"},
    },
    branch_id: {
      label: {"ja":"店舗ID","en":"Branch ID","vi":"ID chi nhánh"},
      placeholder: {"ja":"店舗IDを入力","en":"Enter Branch ID","vi":"Nhập ID chi nhánh"},
    },
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
    order_id: {
      label: {"ja":"注文ID","en":"Order ID","vi":"ID đơn hàng"},
      placeholder: {"ja":"注文IDを入力","en":"Enter Order ID","vi":"Nhập ID đơn hàng"},
    },
    occurred_at: {
      label: {"ja":"発生日時","en":"Occurred At","vi":"Thời điểm xảy ra"},
      placeholder: {"ja":"発生日時を入力","en":"Enter Occurred At","vi":"Nhập Thời điểm xảy ra"},
    },
    paid_locally: {
      label: {"ja":"現地収受額","en":"Paid Locally","vi":"Đã thu tại quán"},
      placeholder: {"ja":"現地収受額を入力","en":"Enter Paid Locally","vi":"Nhập Đã thu tại quán"},
    },
    total_amount_local: {
      label: {"ja":"合計（端末）","en":"Total (Local)","vi":"Tổng (máy trạm)"},
      placeholder: {"ja":"合計（端末）を入力","en":"Enter Total (Local)","vi":"Nhập Tổng (máy trạm)"},
    },
    total_amount_cloud: {
      label: {"ja":"合計（クラウド）","en":"Total (Cloud)","vi":"Tổng (Cloud)"},
      placeholder: {"ja":"合計（クラウド）を入力","en":"Enter Total (Cloud)","vi":"Nhập Tổng (Cloud)"},
    },
    subtotal_local: {
      label: {"ja":"小計（端末）","en":"Subtotal (Local)","vi":"Tạm tính (máy trạm)"},
      placeholder: {"ja":"小計（端末）を入力","en":"Enter Subtotal (Local)","vi":"Nhập Tạm tính (máy trạm)"},
    },
    subtotal_cloud: {
      label: {"ja":"小計（クラウド）","en":"Subtotal (Cloud)","vi":"Tạm tính (Cloud)"},
      placeholder: {"ja":"小計（クラウド）を入力","en":"Enter Subtotal (Cloud)","vi":"Nhập Tạm tính (Cloud)"},
    },
    tax_amount_local: {
      label: {"ja":"消費税（端末）","en":"Tax (Local)","vi":"Thuế (máy trạm)"},
      placeholder: {"ja":"消費税（端末）を入力","en":"Enter Tax (Local)","vi":"Nhập Thuế (máy trạm)"},
    },
    tax_amount_cloud: {
      label: {"ja":"消費税（クラウド）","en":"Tax (Cloud)","vi":"Thuế (Cloud)"},
      placeholder: {"ja":"消費税（クラウド）を入力","en":"Enter Tax (Cloud)","vi":"Nhập Thuế (Cloud)"},
    },
    service_charge_local: {
      label: {"ja":"サービス料（端末）","en":"Service Charge (Local)","vi":"Phí dịch vụ (máy trạm)"},
      placeholder: {"ja":"サービス料（端末）を入力","en":"Enter Service Charge (Local)","vi":"Nhập Phí dịch vụ (máy trạm)"},
    },
    service_charge_cloud: {
      label: {"ja":"サービス料（クラウド）","en":"Service Charge (Cloud)","vi":"Phí dịch vụ (Cloud)"},
      placeholder: {"ja":"サービス料（クラウド）を入力","en":"Enter Service Charge (Cloud)","vi":"Nhập Phí dịch vụ (Cloud)"},
    },
    discount_amount_local: {
      label: {"ja":"割引（端末）","en":"Discount (Local)","vi":"Giảm giá (máy trạm)"},
      placeholder: {"ja":"割引（端末）を入力","en":"Enter Discount (Local)","vi":"Nhập Giảm giá (máy trạm)"},
    },
    discount_amount_cloud: {
      label: {"ja":"割引（クラウド）","en":"Discount (Cloud)","vi":"Giảm giá (Cloud)"},
      placeholder: {"ja":"割引（クラウド）を入力","en":"Enter Discount (Cloud)","vi":"Nhập Giảm giá (Cloud)"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for OrderMoneyOverwrite */
export const baseOrderMoneyOverwriteSchemas = {
  device_id: z.string(),
  local_id: z.number().int(),
  branch_id: z.string(),
  organization_id: z.string(),
  order_id: z.string(),
  occurred_at: z.string().datetime({ offset: true }),
  paid_locally: z.number().int(),
  total_amount_local: z.number().int(),
  total_amount_cloud: z.number().int(),
  subtotal_local: z.number().int(),
  subtotal_cloud: z.number().int(),
  tax_amount_local: z.number().int(),
  tax_amount_cloud: z.number().int(),
  service_charge_local: z.number().int(),
  service_charge_cloud: z.number().int(),
  discount_amount_local: z.number().int(),
  discount_amount_cloud: z.number().int(),
} as const;

/** Create schema for OrderMoneyOverwrite */
export const baseOrderMoneyOverwriteCreateSchema = z.object({
  device_id: baseOrderMoneyOverwriteSchemas.device_id,
  local_id: baseOrderMoneyOverwriteSchemas.local_id,
  branch_id: baseOrderMoneyOverwriteSchemas.branch_id,
  organization_id: baseOrderMoneyOverwriteSchemas.organization_id,
  order_id: baseOrderMoneyOverwriteSchemas.order_id,
  occurred_at: baseOrderMoneyOverwriteSchemas.occurred_at,
  paid_locally: baseOrderMoneyOverwriteSchemas.paid_locally,
  total_amount_local: baseOrderMoneyOverwriteSchemas.total_amount_local,
  total_amount_cloud: baseOrderMoneyOverwriteSchemas.total_amount_cloud,
  subtotal_local: baseOrderMoneyOverwriteSchemas.subtotal_local,
  subtotal_cloud: baseOrderMoneyOverwriteSchemas.subtotal_cloud,
  tax_amount_local: baseOrderMoneyOverwriteSchemas.tax_amount_local,
  tax_amount_cloud: baseOrderMoneyOverwriteSchemas.tax_amount_cloud,
  service_charge_local: baseOrderMoneyOverwriteSchemas.service_charge_local,
  service_charge_cloud: baseOrderMoneyOverwriteSchemas.service_charge_cloud,
  discount_amount_local: baseOrderMoneyOverwriteSchemas.discount_amount_local,
  discount_amount_cloud: baseOrderMoneyOverwriteSchemas.discount_amount_cloud,
});

/** Update schema for OrderMoneyOverwrite */
export const baseOrderMoneyOverwriteUpdateSchema = baseOrderMoneyOverwriteCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseOrderMoneyOverwriteCreate = z.infer<typeof baseOrderMoneyOverwriteCreateSchema>;
export type BaseOrderMoneyOverwriteUpdate = z.infer<typeof baseOrderMoneyOverwriteUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getOrderMoneyOverwriteLabel(locale: string): string {
  return orderMoneyOverwriteI18n.label[locale as keyof typeof orderMoneyOverwriteI18n.label] ?? orderMoneyOverwriteI18n.label['en'] ?? 'OrderMoneyOverwrite';
}

/** Get field label for a specific locale */
export function getOrderMoneyOverwriteFieldLabel(field: string, locale: string): string {
  const fields = orderMoneyOverwriteI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getOrderMoneyOverwriteFieldPlaceholder(field: string, locale: string): string {
  const fields = orderMoneyOverwriteI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for OrderMoneyOverwrite.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const orderMoneyOverwriteMetadata = {
  modelName: "OrderMoneyOverwrite",
  table: "order_money_overwrites",

  fields: {
    device_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "デバイスID",
        en: "Device ID",
        vi: "ID thiết bị",
      },
      placeholder: {
        ja: "デバイスIDを入力",
        en: "Enter Device ID",
        vi: "Nhập ID thiết bị",
      },
      description: {
        ja: "上書きを報告したワークステーション。冪等キーの片方。FKなし（証跡はデバイスより長生きする）。",
        en: "Workstation device that reported the overwrite. Half of the idempotency key. Plain UUID, no FK — evidence outlives the device.",
        vi: "Máy trạm đã báo cáo lần ghi đè. Một nửa khoá idempotency. Uuid trần, không FK — bằng chứng sống lâu hơn thiết bị.",
      },
    },
    local_id: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ローカル行ID",
        en: "Local Row ID",
        vi: "ID dòng cục bộ",
      },
      placeholder: {
        ja: "ローカル行IDを入力",
        en: "Enter Local Row ID",
        vi: "Nhập ID dòng cục bộ",
      },
      description: {
        ja: "デバイス側SQLiteの自動採番ID。同一デバイス内で再利用されないため冪等キーに使う。",
        en: "Autoincrement id of the row in the device's own SQLite table. Stable and never reused on one device, which is why it is the idempotency key instead of (order_id, occurred_at).",
        vi: "Id autoincrement của dòng trong SQLite của chính thiết bị. Ổn định, không tái sinh trên cùng thiết bị — vì thế nó là khoá idempotency thay cho (order_id, occurred_at).",
      },
    },
    branch_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "店舗ID",
        en: "Branch ID",
        vi: "ID chi nhánh",
      },
      placeholder: {
        ja: "店舗IDを入力",
        en: "Enter Branch ID",
        vi: "Nhập ID chi nhánh",
      },
      description: {
        ja: "デバイストークンから解決した店舗。ペイロードからは取らない。",
        en: "Branch resolved from the device token, never from the payload — a device cannot write evidence into another branch.",
        vi: "Chi nhánh suy ra từ device token, KHÔNG lấy từ payload — thiết bị không ghi được bằng chứng sang chi nhánh khác.",
      },
    },
    organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
      description: {
        ja: "店舗の console_organization_id からミラーした組織（#2847）。",
        en: "Local organization mirrored from console_organization_id of the branch (#2847 — branches has no organization_id column).",
        vi: "Tổ chức local suy qua mirror console của chi nhánh (#2847 — bảng branches KHÔNG có cột organization_id).",
      },
    },
    order_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "注文ID",
        en: "Order ID",
        vi: "ID đơn hàng",
      },
      placeholder: {
        ja: "注文IDを入力",
        en: "Enter Order ID",
        vi: "Nhập ID đơn hàng",
      },
      description: {
        ja: "上書きされた注文。FKなし。",
        en: "The overwritten order. Plain UUID, no FK — evidence must survive order archival, and the order may not exist in Cloud yet.",
        vi: "Đơn bị ghi đè. Uuid trần, không FK — bằng chứng phải sống sót khi đơn được lưu trữ.",
      },
    },
    occurred_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "発生日時",
        en: "Occurred At",
        vi: "Thời điểm xảy ra",
      },
      placeholder: {
        ja: "発生日時を入力",
        en: "Enter Occurred At",
        vi: "Nhập Thời điểm xảy ra",
      },
      description: {
        ja: "デバイス上で上書きが起きた時刻（RFC3339 UTC）。",
        en: "When the overwrite happened on the device (RFC3339 UTC). Differs from created_at by however long the shop was offline.",
        vi: "Lúc ghi đè xảy ra TRÊN THIẾT BỊ (RFC3339 UTC). Lệch với created_at đúng bằng thời gian quán mất mạng.",
      },
    },
    paid_locally: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "現地収受額",
        en: "Paid Locally",
        vi: "Đã thu tại quán",
      },
      placeholder: {
        ja: "現地収受額を入力",
        en: "Enter Paid Locally",
        vi: "Nhập Đã thu tại quán",
      },
      description: {
        ja: "上書き時点で既に収受済みの金額。スナップショットであり、後から payments を読み直さない。",
        en: "Cash already taken at overwrite time. Decides whether the gap is a real 過不足 or just a slip about to print wrong. Snapshot — never re-read from payments later.",
        vi: "Tiền đã vào két TẠI THỜI ĐIỂM ghi đè — quyết định khoảng lệch là 過不足 thật hay mới chỉ là tờ phiếu sắp in sai. Snapshot, không đọc lại từ payments.",
      },
    },
    total_amount_local: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "合計（端末）",
        en: "Total (Local)",
        vi: "Tổng (máy trạm)",
      },
      placeholder: {
        ja: "合計（端末）を入力",
        en: "Enter Total (Local)",
        vi: "Nhập Tổng (máy trạm)",
      },
      description: {
        ja: "端末が保持していた合計金額（最小通貨単位、符号あり）。",
        en: "Order total the workstation was holding, in minor units. Signed.",
        vi: "Tổng tiền máy trạm đang giữ, đơn vị nhỏ nhất, có dấu.",
      },
    },
    total_amount_cloud: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "合計（クラウド）",
        en: "Total (Cloud)",
        vi: "Tổng (Cloud)",
      },
      placeholder: {
        ja: "合計（クラウド）を入力",
        en: "Enter Total (Cloud)",
        vi: "Nhập Tổng (Cloud)",
      },
      description: {
        ja: "クラウドが再計算して保存した合計金額。",
        en: "Order total Cloud re-priced and persisted, in minor units. Signed.",
        vi: "Tổng tiền Cloud tính lại và đã lưu, đơn vị nhỏ nhất, có dấu.",
      },
    },
    subtotal_local: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "小計（端末）",
        en: "Subtotal (Local)",
        vi: "Tạm tính (máy trạm)",
      },
      placeholder: {
        ja: "小計（端末）を入力",
        en: "Enter Subtotal (Local)",
        vi: "Nhập Tạm tính (máy trạm)",
      },
      description: {
        ja: "端末が保持していた小計。",
        en: "Subtotal the workstation was holding, in minor units. Signed.",
        vi: "Tạm tính máy trạm đang giữ, đơn vị nhỏ nhất, có dấu.",
      },
    },
    subtotal_cloud: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "小計（クラウド）",
        en: "Subtotal (Cloud)",
        vi: "Tạm tính (Cloud)",
      },
      placeholder: {
        ja: "小計（クラウド）を入力",
        en: "Enter Subtotal (Cloud)",
        vi: "Nhập Tạm tính (Cloud)",
      },
      description: {
        ja: "クラウドが再計算して保存した小計。",
        en: "Subtotal Cloud re-priced and persisted, in minor units. Signed.",
        vi: "Tạm tính Cloud tính lại và đã lưu, đơn vị nhỏ nhất, có dấu.",
      },
    },
    tax_amount_local: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "消費税（端末）",
        en: "Tax (Local)",
        vi: "Thuế (máy trạm)",
      },
      placeholder: {
        ja: "消費税（端末）を入力",
        en: "Enter Tax (Local)",
        vi: "Nhập Thuế (máy trạm)",
      },
      description: {
        ja: "端末が保持していた消費税額。",
        en: "Tax the workstation was holding, in minor units. Signed.",
        vi: "Thuế máy trạm đang giữ, đơn vị nhỏ nhất, có dấu.",
      },
    },
    tax_amount_cloud: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "消費税（クラウド）",
        en: "Tax (Cloud)",
        vi: "Thuế (Cloud)",
      },
      placeholder: {
        ja: "消費税（クラウド）を入力",
        en: "Enter Tax (Cloud)",
        vi: "Nhập Thuế (Cloud)",
      },
      description: {
        ja: "クラウドが再計算して保存した消費税額。",
        en: "Tax Cloud re-priced and persisted, in minor units. Signed.",
        vi: "Thuế Cloud tính lại và đã lưu, đơn vị nhỏ nhất, có dấu.",
      },
    },
    service_charge_local: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "サービス料（端末）",
        en: "Service Charge (Local)",
        vi: "Phí dịch vụ (máy trạm)",
      },
      placeholder: {
        ja: "サービス料（端末）を入力",
        en: "Enter Service Charge (Local)",
        vi: "Nhập Phí dịch vụ (máy trạm)",
      },
      description: {
        ja: "端末が保持していたサービス料。",
        en: "Service charge the workstation was holding, in minor units. Signed.",
        vi: "Phí dịch vụ máy trạm đang giữ, đơn vị nhỏ nhất, có dấu.",
      },
    },
    service_charge_cloud: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "サービス料（クラウド）",
        en: "Service Charge (Cloud)",
        vi: "Phí dịch vụ (Cloud)",
      },
      placeholder: {
        ja: "サービス料（クラウド）を入力",
        en: "Enter Service Charge (Cloud)",
        vi: "Nhập Phí dịch vụ (Cloud)",
      },
      description: {
        ja: "クラウドが再計算して保存したサービス料。",
        en: "Service charge Cloud re-priced and persisted, in minor units. Signed.",
        vi: "Phí dịch vụ Cloud tính lại và đã lưu, đơn vị nhỏ nhất, có dấu.",
      },
    },
    discount_amount_local: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "割引（端末）",
        en: "Discount (Local)",
        vi: "Giảm giá (máy trạm)",
      },
      placeholder: {
        ja: "割引（端末）を入力",
        en: "Enter Discount (Local)",
        vi: "Nhập Giảm giá (máy trạm)",
      },
      description: {
        ja: "端末が保持していた割引額（符号あり）。",
        en: "Discount the workstation was holding, in minor units. Signed — a discount can legitimately be negative here.",
        vi: "Giảm giá máy trạm đang giữ, đơn vị nhỏ nhất, có dấu — số âm là hợp lệ.",
      },
    },
    discount_amount_cloud: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "割引（クラウド）",
        en: "Discount (Cloud)",
        vi: "Giảm giá (Cloud)",
      },
      placeholder: {
        ja: "割引（クラウド）を入力",
        en: "Enter Discount (Cloud)",
        vi: "Nhập Giảm giá (Cloud)",
      },
      description: {
        ja: "クラウドが再計算して保存した割引額。",
        en: "Discount Cloud re-priced and persisted, in minor units. Signed.",
        vi: "Giảm giá Cloud tính lại và đã lưu, đơn vị nhỏ nhất, có dấu.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["device_id", "local_id", "branch_id", "organization_id", "order_id", "occurred_at", "paid_locally", "total_amount_local", "total_amount_cloud", "subtotal_local", "subtotal_cloud", "tax_amount_local", "tax_amount_cloud", "service_charge_local", "service_charge_cloud", "discount_amount_local", "discount_amount_cloud"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type OrderMoneyOverwriteFieldName = keyof typeof orderMoneyOverwriteMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a OrderMoneyOverwrite. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface OrderMoneyOverwriteCreateFormState {
  device_id: string;
  local_id: number;
  branch_id: string;
  organization_id: string;
  order_id: string;
  occurred_at: string;
  paid_locally: number;
  total_amount_local: number;
  total_amount_cloud: number;
  subtotal_local: number;
  subtotal_cloud: number;
  tax_amount_local: number;
  tax_amount_cloud: number;
  service_charge_local: number;
  service_charge_cloud: number;
  discount_amount_local: number;
  discount_amount_cloud: number;
}

/** Form-state shape for updating a OrderMoneyOverwrite. Every field optional. */
export interface OrderMoneyOverwriteUpdateFormState {
  device_id?: string;
  local_id?: number;
  branch_id?: string;
  organization_id?: string;
  order_id?: string;
  occurred_at?: string;
  paid_locally?: number;
  total_amount_local?: number;
  total_amount_cloud?: number;
  subtotal_local?: number;
  subtotal_cloud?: number;
  tax_amount_local?: number;
  tax_amount_cloud?: number;
  service_charge_local?: number;
  service_charge_cloud?: number;
  discount_amount_local?: number;
  discount_amount_cloud?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyOrderMoneyOverwriteCreateForm(): OrderMoneyOverwriteCreateFormState {
  return {
    device_id: '',
    local_id: 0,
    branch_id: '',
    organization_id: '',
    order_id: '',
    occurred_at: '',
    paid_locally: 0,
    total_amount_local: 0,
    total_amount_cloud: 0,
    subtotal_local: 0,
    subtotal_cloud: 0,
    tax_amount_local: 0,
    tax_amount_cloud: 0,
    service_charge_local: 0,
    service_charge_cloud: 0,
    discount_amount_local: 0,
    discount_amount_cloud: 0,
  };
}

/** Convert create form state into the payload that baseOrderMoneyOverwriteCreateSchema accepts. */
export function buildOrderMoneyOverwriteCreatePayload(form: OrderMoneyOverwriteCreateFormState): BaseOrderMoneyOverwriteCreate {
  const payload: Record<string, unknown> = {
    device_id: form.device_id.trim(),
    local_id: form.local_id,
    branch_id: form.branch_id.trim(),
    organization_id: form.organization_id.trim(),
    order_id: form.order_id.trim(),
    occurred_at: form.occurred_at,
    paid_locally: form.paid_locally,
    total_amount_local: form.total_amount_local,
    total_amount_cloud: form.total_amount_cloud,
    subtotal_local: form.subtotal_local,
    subtotal_cloud: form.subtotal_cloud,
    tax_amount_local: form.tax_amount_local,
    tax_amount_cloud: form.tax_amount_cloud,
    service_charge_local: form.service_charge_local,
    service_charge_cloud: form.service_charge_cloud,
    discount_amount_local: form.discount_amount_local,
    discount_amount_cloud: form.discount_amount_cloud,
  };

  return payload as BaseOrderMoneyOverwriteCreate;
}

/** Convert update form state into the payload that baseOrderMoneyOverwriteUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildOrderMoneyOverwriteUpdatePayload(form: OrderMoneyOverwriteUpdateFormState): BaseOrderMoneyOverwriteUpdate {
  const payload: Record<string, unknown> = {};
  if (form.device_id !== undefined) {
    payload.device_id = form.device_id?.toString().trim() ?? null;
  }
  if (form.local_id !== undefined) {
    payload.local_id = form.local_id;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.order_id !== undefined) {
    payload.order_id = form.order_id?.toString().trim() ?? null;
  }
  if (form.occurred_at !== undefined) {
    payload.occurred_at = form.occurred_at;
  }
  if (form.paid_locally !== undefined) {
    payload.paid_locally = form.paid_locally;
  }
  if (form.total_amount_local !== undefined) {
    payload.total_amount_local = form.total_amount_local;
  }
  if (form.total_amount_cloud !== undefined) {
    payload.total_amount_cloud = form.total_amount_cloud;
  }
  if (form.subtotal_local !== undefined) {
    payload.subtotal_local = form.subtotal_local;
  }
  if (form.subtotal_cloud !== undefined) {
    payload.subtotal_cloud = form.subtotal_cloud;
  }
  if (form.tax_amount_local !== undefined) {
    payload.tax_amount_local = form.tax_amount_local;
  }
  if (form.tax_amount_cloud !== undefined) {
    payload.tax_amount_cloud = form.tax_amount_cloud;
  }
  if (form.service_charge_local !== undefined) {
    payload.service_charge_local = form.service_charge_local;
  }
  if (form.service_charge_cloud !== undefined) {
    payload.service_charge_cloud = form.service_charge_cloud;
  }
  if (form.discount_amount_local !== undefined) {
    payload.discount_amount_local = form.discount_amount_local;
  }
  if (form.discount_amount_cloud !== undefined) {
    payload.discount_amount_cloud = form.discount_amount_cloud;
  }
  return payload as BaseOrderMoneyOverwriteUpdate;
}
