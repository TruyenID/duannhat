/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Role } from './Role';
import type { User } from './User';

/**
 * RoleUserPivot
 */
export interface RoleUserPivot {
  id: number;
  role_id: string;
  role?: Role;
  user_id: string;
  user?: User;
  organization_id?: string | null;
  branch_id?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for RoleUserPivot
 */
export const roleUserPivotI18n = {
  label: {"ja":"RoleUserPivot","en":"RoleUserPivot","vi":"RoleUserPivot"},
  fields: {
    role: {
      label: {"ja":"ロール","en":"Role","vi":"Vai trò"},
      placeholder: {"ja":"ロールを入力","en":"Enter Role","vi":"Nhập Vai trò"},
    },
    user: {
      label: {"ja":"ユーザー","en":"User","vi":"Người dùng"},
      placeholder: {"ja":"ユーザーを入力","en":"Enter User","vi":"Nhập Người dùng"},
    },
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
    branch_id: {
      label: {"ja":"支店ID","en":"Branch ID","vi":"ID chi nhánh"},
      placeholder: {"ja":"支店IDを入力","en":"Enter Branch ID","vi":"Nhập ID chi nhánh"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for RoleUserPivot */
export const baseRoleUserPivotSchemas = {
  organization_id: z.string().optional().nullable(),
  branch_id: z.string().optional().nullable(),
} as const;

/** Create schema for RoleUserPivot */
export const baseRoleUserPivotCreateSchema = z.object({
  organization_id: baseRoleUserPivotSchemas.organization_id,
  branch_id: baseRoleUserPivotSchemas.branch_id,
});

/** Update schema for RoleUserPivot */
export const baseRoleUserPivotUpdateSchema = baseRoleUserPivotCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseRoleUserPivotCreate = z.infer<typeof baseRoleUserPivotCreateSchema>;
export type BaseRoleUserPivotUpdate = z.infer<typeof baseRoleUserPivotUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getRoleUserPivotLabel(locale: string): string {
  return roleUserPivotI18n.label[locale as keyof typeof roleUserPivotI18n.label] ?? roleUserPivotI18n.label['en'] ?? 'RoleUserPivot';
}

/** Get field label for a specific locale */
export function getRoleUserPivotFieldLabel(field: string, locale: string): string {
  const fields = roleUserPivotI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getRoleUserPivotFieldPlaceholder(field: string, locale: string): string {
  const fields = roleUserPivotI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for RoleUserPivot.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const roleUserPivotMetadata = {
  modelName: "RoleUserPivot",
  table: "role_user_pivots",

  fields: {
    role_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Role",
        table: "roles",
        onDelete: "CASCADE",
      },
      label: {},
    },
    user_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
    },
    branch_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "支店ID",
        en: "Branch ID",
        vi: "ID chi nhánh",
      },
      placeholder: {
        ja: "支店IDを入力",
        en: "Enter Branch ID",
        vi: "Nhập ID chi nhánh",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["role_id", "user_id"] as const,
  enumFields: [] as const,
  relationFields: ["role_id", "user_id"] as const,
} as const;

export type RoleUserPivotFieldName = keyof typeof roleUserPivotMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a RoleUserPivot. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface RoleUserPivotCreateFormState {
  role_id: string;
  user_id: string;
  organization_id?: string | null;
  branch_id?: string | null;
}

/** Form-state shape for updating a RoleUserPivot. Every field optional. */
export interface RoleUserPivotUpdateFormState {
  role_id?: string;
  user_id?: string;
  organization_id?: string | null;
  branch_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyRoleUserPivotCreateForm(): RoleUserPivotCreateFormState {
  return {
    role_id: '',
    user_id: '',
    organization_id: '',
    branch_id: '',
  };
}

/** Convert create form state into the payload that baseRoleUserPivotCreateSchema accepts. */
export function buildRoleUserPivotCreatePayload(form: RoleUserPivotCreateFormState): BaseRoleUserPivotCreate {
  const payload: Record<string, unknown> = {
    role_id: form.role_id.trim(),
    user_id: form.user_id.trim(),
    organization_id: form.organization_id?.toString().trim() || null,
    branch_id: form.branch_id?.toString().trim() || null,
  };

  return payload as BaseRoleUserPivotCreate;
}

/** Convert update form state into the payload that baseRoleUserPivotUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildRoleUserPivotUpdatePayload(form: RoleUserPivotUpdateFormState): BaseRoleUserPivotUpdate {
  const payload: Record<string, unknown> = {};
  if (form.role_id !== undefined) {
    payload.role_id = form.role_id?.toString().trim() ?? null;
  }
  if (form.user_id !== undefined) {
    payload.user_id = form.user_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  return payload as BaseRoleUserPivotUpdate;
}
