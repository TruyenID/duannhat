/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * MenuPromotionCategory
 */
export interface MenuPromotionCategory {
  id: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuPromotionCategory
 */
export const menuPromotionCategoryI18n = {
  label: {"ja":"MenuPromotionCategory","en":"MenuPromotionCategory","vi":"MenuPromotionCategory"},
  fields: {
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuPromotionCategory */
export const baseMenuPromotionCategorySchemas = {
} as const;

/** Create schema for MenuPromotionCategory */
export const baseMenuPromotionCategoryCreateSchema = z.object({
});

/** Update schema for MenuPromotionCategory */
export const baseMenuPromotionCategoryUpdateSchema = baseMenuPromotionCategoryCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuPromotionCategoryCreate = z.infer<typeof baseMenuPromotionCategoryCreateSchema>;
export type BaseMenuPromotionCategoryUpdate = z.infer<typeof baseMenuPromotionCategoryUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuPromotionCategoryLabel(locale: string): string {
  return menuPromotionCategoryI18n.label[locale as keyof typeof menuPromotionCategoryI18n.label] ?? menuPromotionCategoryI18n.label['en'] ?? 'MenuPromotionCategory';
}

/** Get field label for a specific locale */
export function getMenuPromotionCategoryFieldLabel(field: string, locale: string): string {
  const fields = menuPromotionCategoryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuPromotionCategoryFieldPlaceholder(field: string, locale: string): string {
  const fields = menuPromotionCategoryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuPromotionCategory.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuPromotionCategoryMetadata = {
  modelName: "MenuPromotionCategory",
  table: "menu_promotion_category",

  fields: {
  },

  translatableFields: [] as const,
  requiredFields: [] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type MenuPromotionCategoryFieldName = keyof typeof menuPromotionCategoryMetadata.fields;

