/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * Organization
 */
export interface Organization {
  id: string;
  console_organization_id: string;
  name: string;
  slug: string;
  is_active: boolean;
  settings?: unknown | null;
  operating_country: string;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Organization
 */
export const organizationI18n = {
  label: {"ja":"Organization","en":"Organization","vi":"Organization"},
  fields: {
    console_organization_id: {
      label: {"ja":"コンソール組織ID","en":"Console Organization ID","vi":"ID tổ chức trên Console"},
      placeholder: {"ja":"コンソール組織IDを入力","en":"Enter Console Organization ID","vi":"Nhập ID tổ chức trên Console"},
    },
    name: {
      label: {"ja":"組織名","en":"Organization Name","vi":"Tên tổ chức"},
      placeholder: {"ja":"組織名を入力","en":"Enter Organization Name","vi":"Nhập Tên tổ chức"},
    },
    slug: {
      label: {"ja":"スラッグ","en":"Slug","vi":"Slug"},
      placeholder: {"ja":"スラッグを入力","en":"Enter Slug","vi":"Nhập Slug"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    settings: {
      label: {"ja":"設定","en":"Settings","vi":"Cấu hình"},
      placeholder: {"ja":"設定を入力","en":"Enter Settings","vi":"Nhập Cấu hình"},
    },
    operating_country: {
      label: {"ja":"事業国","en":"Operating Country","vi":"Quốc gia hoạt động"},
      placeholder: {"ja":"事業国を入力","en":"Enter Operating Country","vi":"Nhập Quốc gia hoạt động"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Organization */
export const baseOrganizationSchemas = {
  console_organization_id: z.string(),
  name: z.string().min(1).max(100),
  slug: z.string().min(1).max(100),
  is_active: z.boolean(),
  settings: z.unknown().optional().nullable(),
  operating_country: z.string().min(1).max(2),
} as const;

/** Create schema for Organization */
export const baseOrganizationCreateSchema = z.object({
  console_organization_id: baseOrganizationSchemas.console_organization_id,
  name: baseOrganizationSchemas.name,
  slug: baseOrganizationSchemas.slug,
  is_active: baseOrganizationSchemas.is_active,
  settings: baseOrganizationSchemas.settings,
  operating_country: baseOrganizationSchemas.operating_country,
});

/** Update schema for Organization */
export const baseOrganizationUpdateSchema = baseOrganizationCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseOrganizationCreate = z.infer<typeof baseOrganizationCreateSchema>;
export type BaseOrganizationUpdate = z.infer<typeof baseOrganizationUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getOrganizationLabel(locale: string): string {
  return organizationI18n.label[locale as keyof typeof organizationI18n.label] ?? organizationI18n.label['en'] ?? 'Organization';
}

/** Get field label for a specific locale */
export function getOrganizationFieldLabel(field: string, locale: string): string {
  const fields = organizationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getOrganizationFieldPlaceholder(field: string, locale: string): string {
  const fields = organizationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Organization.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const organizationMetadata = {
  modelName: "Organization",
  table: "organizations",

  fields: {
    console_organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "コンソール組織ID",
        en: "Console Organization ID",
        vi: "ID tổ chức trên Console",
      },
      placeholder: {
        ja: "コンソール組織IDを入力",
        en: "Enter Console Organization ID",
        vi: "Nhập ID tổ chức trên Console",
      },
    },
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "組織名",
        en: "Organization Name",
        vi: "Tên tổ chức",
      },
      placeholder: {
        ja: "組織名を入力",
        en: "Enter Organization Name",
        vi: "Nhập Tên tổ chức",
      },
    },
    slug: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "スラッグ",
        en: "Slug",
        vi: "Slug",
      },
      placeholder: {
        ja: "スラッグを入力",
        en: "Enter Slug",
        vi: "Nhập Slug",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    settings: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "設定",
        en: "Settings",
        vi: "Cấu hình",
      },
      placeholder: {
        ja: "設定を入力",
        en: "Enter Settings",
        vi: "Nhập Cấu hình",
      },
    },
    operating_country: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 2,
      },
      label: {
        ja: "事業国",
        en: "Operating Country",
        vi: "Quốc gia hoạt động",
      },
      placeholder: {
        ja: "事業国を入力",
        en: "Enter Operating Country",
        vi: "Nhập Quốc gia hoạt động",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["console_organization_id", "name", "slug", "is_active", "operating_country"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type OrganizationFieldName = keyof typeof organizationMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Organization. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface OrganizationCreateFormState {
  console_organization_id: string;
  name: string;
  slug: string;
  is_active: boolean;
  settings?: unknown | null;
  operating_country: string;
}

/** Form-state shape for updating a Organization. Every field optional. */
export interface OrganizationUpdateFormState {
  console_organization_id?: string;
  name?: string;
  slug?: string;
  is_active?: boolean;
  settings?: unknown | null;
  operating_country?: string;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyOrganizationCreateForm(): OrganizationCreateFormState {
  return {
    console_organization_id: '',
    name: '',
    slug: '',
    is_active: true,
    settings: null,
    operating_country: "JP",
  };
}

/** Convert create form state into the payload that baseOrganizationCreateSchema accepts. */
export function buildOrganizationCreatePayload(form: OrganizationCreateFormState): BaseOrganizationCreate {
  const payload: Record<string, unknown> = {
    console_organization_id: form.console_organization_id.trim(),
    name: form.name.trim(),
    slug: form.slug.trim(),
    is_active: form.is_active,
    settings: form.settings,
    operating_country: form.operating_country.trim(),
  };

  return payload as BaseOrganizationCreate;
}

/** Convert update form state into the payload that baseOrganizationUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildOrganizationUpdatePayload(form: OrganizationUpdateFormState): BaseOrganizationUpdate {
  const payload: Record<string, unknown> = {};
  if (form.console_organization_id !== undefined) {
    payload.console_organization_id = form.console_organization_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.slug !== undefined) {
    payload.slug = form.slug?.toString().trim() ?? null;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.settings !== undefined) {
    payload.settings = form.settings;
  }
  if (form.operating_country !== undefined) {
    payload.operating_country = form.operating_country?.toString().trim() ?? null;
  }
  return payload as BaseOrganizationUpdate;
}
