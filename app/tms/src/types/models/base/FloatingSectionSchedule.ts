/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString, DateString } from '../common';
import type { FloatingSection } from './FloatingSection';

/**
 * フローティングセクションスケジュール
 */
export interface FloatingSectionSchedule {
  id: string;
  start_time: string;
  end_time: string;
  start_date?: DateString | null;
  end_date?: DateString | null;
  days_of_week: number;
  is_active: boolean;
  is_time_overridden: boolean;
  priority: number;
  created_by_id?: string | null;
  floating_section_id: string;
  floatingSection?: FloatingSection;
  master_schedule_id?: string | null;
  masterSchedule?: FloatingSectionSchedule | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for FloatingSectionSchedule
 */
export const floatingSectionScheduleI18n = {
  label: {"ja":"フローティングセクションスケジュール","en":"Floating Section Schedule","vi":"Lịch trình khung giờ ưu đãi"},
  fields: {
    start_time: {
      label: {"ja":"開始時刻","en":"Start Time","vi":"Giờ bắt đầu"},
      placeholder: {"ja":"開始時刻を入力","en":"Enter Start Time","vi":"Nhập Giờ bắt đầu"},
    },
    end_time: {
      label: {"ja":"終了時刻","en":"End Time","vi":"Giờ kết thúc"},
      placeholder: {"ja":"終了時刻を入力","en":"Enter End Time","vi":"Nhập Giờ kết thúc"},
    },
    start_date: {
      label: {"ja":"開始日","en":"Start Date","vi":"Ngày bắt đầu"},
      placeholder: {"ja":"開始日を入力","en":"Enter Start Date","vi":"Nhập Ngày bắt đầu"},
    },
    end_date: {
      label: {"ja":"終了日","en":"End Date","vi":"Ngày kết thúc"},
      placeholder: {"ja":"終了日を入力","en":"Enter End Date","vi":"Nhập Ngày kết thúc"},
    },
    days_of_week: {
      label: {"ja":"曜日","en":"Days of Week","vi":"Ngày trong tuần"},
      placeholder: {"ja":"曜日を入力","en":"Enter Days of Week","vi":"Nhập Ngày trong tuần"},
    },
    is_active: {
      label: {"ja":"有効","en":"Active","vi":"Hoạt động"},
      placeholder: {"ja":"有効を入力","en":"Enter Active","vi":"Nhập Hoạt động"},
    },
    is_time_overridden: {
      label: {"ja":"時間上書き済み","en":"Time Overridden","vi":"Đã ghi đè giờ"},
      placeholder: {"ja":"時間上書き済みを入力","en":"Enter Time Overridden","vi":"Nhập Đã ghi đè giờ"},
    },
    priority: {
      label: {"ja":"優先度","en":"Priority","vi":"Độ ưu tiên"},
      placeholder: {"ja":"優先度を入力","en":"Enter Priority","vi":"Nhập Độ ưu tiên"},
    },
    created_by_id: {
      label: {"ja":"作成者","en":"Created By","vi":"Người tạo"},
      placeholder: {"ja":"作成者を入力","en":"Enter Created By","vi":"Nhập Người tạo"},
    },
    floating_section: {
      label: {"ja":"フローティングセクション","en":"Floating Section","vi":"Khung giờ ưu đãi"},
      placeholder: {"ja":"フローティングセクションを入力","en":"Enter Floating Section","vi":"Nhập Khung giờ ưu đãi"},
    },
    master_schedule: {
      label: {"ja":"マスタースケジュール","en":"Master Schedule","vi":"Lịch gốc (HQ)"},
      placeholder: {"ja":"マスタースケジュールを入力","en":"Enter Master Schedule","vi":"Nhập Lịch gốc (HQ)"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for FloatingSectionSchedule */
export const baseFloatingSectionScheduleSchemas = {
  start_time: z.string().time(),
  end_time: z.string().time(),
  start_date: z.string().date().optional().nullable(),
  end_date: z.string().date().optional().nullable(),
  days_of_week: z.number().int(),
  is_active: z.boolean(),
  is_time_overridden: z.boolean(),
  priority: z.number().int(),
  created_by_id: z.string().optional().nullable(),
} as const;

/** Create schema for FloatingSectionSchedule */
export const baseFloatingSectionScheduleCreateSchema = z.object({
  start_time: baseFloatingSectionScheduleSchemas.start_time,
  end_time: baseFloatingSectionScheduleSchemas.end_time,
  start_date: baseFloatingSectionScheduleSchemas.start_date,
  end_date: baseFloatingSectionScheduleSchemas.end_date,
  days_of_week: baseFloatingSectionScheduleSchemas.days_of_week,
  is_active: baseFloatingSectionScheduleSchemas.is_active,
  is_time_overridden: baseFloatingSectionScheduleSchemas.is_time_overridden,
  priority: baseFloatingSectionScheduleSchemas.priority,
  created_by_id: baseFloatingSectionScheduleSchemas.created_by_id,
});

/** Update schema for FloatingSectionSchedule */
export const baseFloatingSectionScheduleUpdateSchema = baseFloatingSectionScheduleCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseFloatingSectionScheduleCreate = z.infer<typeof baseFloatingSectionScheduleCreateSchema>;
export type BaseFloatingSectionScheduleUpdate = z.infer<typeof baseFloatingSectionScheduleUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getFloatingSectionScheduleLabel(locale: string): string {
  return floatingSectionScheduleI18n.label[locale as keyof typeof floatingSectionScheduleI18n.label] ?? floatingSectionScheduleI18n.label['en'] ?? 'FloatingSectionSchedule';
}

/** Get field label for a specific locale */
export function getFloatingSectionScheduleFieldLabel(field: string, locale: string): string {
  const fields = floatingSectionScheduleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getFloatingSectionScheduleFieldPlaceholder(field: string, locale: string): string {
  const fields = floatingSectionScheduleI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for FloatingSectionSchedule.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const floatingSectionScheduleMetadata = {
  modelName: "FloatingSectionSchedule",
  table: "floating_section_schedules",

  fields: {
    start_time: {
      type: "time",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "開始時刻",
        en: "Start Time",
        vi: "Giờ bắt đầu",
      },
      placeholder: {
        ja: "開始時刻を入力",
        en: "Enter Start Time",
        vi: "Nhập Giờ bắt đầu",
      },
    },
    end_time: {
      type: "time",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "終了時刻",
        en: "End Time",
        vi: "Giờ kết thúc",
      },
      placeholder: {
        ja: "終了時刻を入力",
        en: "Enter End Time",
        vi: "Nhập Giờ kết thúc",
      },
    },
    start_date: {
      type: "date",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "開始日",
        en: "Start Date",
        vi: "Ngày bắt đầu",
      },
      placeholder: {
        ja: "開始日を入力",
        en: "Enter Start Date",
        vi: "Nhập Ngày bắt đầu",
      },
    },
    end_date: {
      type: "date",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "終了日",
        en: "End Date",
        vi: "Ngày kết thúc",
      },
      placeholder: {
        ja: "終了日を入力",
        en: "Enter End Date",
        vi: "Nhập Ngày kết thúc",
      },
    },
    days_of_week: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "曜日",
        en: "Days of Week",
        vi: "Ngày trong tuần",
      },
      placeholder: {
        ja: "曜日を入力",
        en: "Enter Days of Week",
        vi: "Nhập Ngày trong tuần",
      },
      description: {
        ja: "ビットマスク、bit0=日...bit6=土。menu_schedules.days_of_weekと同じエンコーディング。",
        en: "Bitmask, bit0=Sun...bit6=Sat. Same encoding as menu_schedules.days_of_week.",
        vi: "Bitmask, bit0=CN...bit6=T7. Cùng encoding với menu_schedules.days_of_week.",
      },
    },
    is_active: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Active",
        vi: "Hoạt động",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Active",
        vi: "Nhập Hoạt động",
      },
    },
    is_time_overridden: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "時間上書き済み",
        en: "Time Overridden",
        vi: "Đã ghi đè giờ",
      },
      placeholder: {
        ja: "時間上書き済みを入力",
        en: "Enter Time Overridden",
        vi: "Nhập Đã ghi đè giờ",
      },
    },
    priority: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "優先度",
        en: "Priority",
        vi: "Độ ưu tiên",
      },
      placeholder: {
        ja: "優先度を入力",
        en: "Enter Priority",
        vi: "Nhập Độ ưu tiên",
      },
    },
    created_by_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "作成者",
        en: "Created By",
        vi: "Người tạo",
      },
      placeholder: {
        ja: "作成者を入力",
        en: "Enter Created By",
        vi: "Nhập Người tạo",
      },
    },
    floating_section_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "FloatingSection",
        table: "floating_sections",
        onDelete: "CASCADE",
      },
      label: {},
    },
    master_schedule_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "FloatingSectionSchedule",
        table: "floating_section_schedules",
        onDelete: "SET NULL",
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["start_time", "end_time", "days_of_week", "is_active", "is_time_overridden", "priority", "floating_section_id"] as const,
  enumFields: [] as const,
  relationFields: ["floating_section_id", "master_schedule_id"] as const,
} as const;

export type FloatingSectionScheduleFieldName = keyof typeof floatingSectionScheduleMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a FloatingSectionSchedule. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface FloatingSectionScheduleCreateFormState {
  start_time: string;
  end_time: string;
  start_date?: string | null;
  end_date?: string | null;
  days_of_week: number;
  is_active: boolean;
  is_time_overridden: boolean;
  priority: number;
  created_by_id?: string | null;
  floating_section_id: string;
  master_schedule_id?: string | null;
}

/** Form-state shape for updating a FloatingSectionSchedule. Every field optional. */
export interface FloatingSectionScheduleUpdateFormState {
  start_time?: string;
  end_time?: string;
  start_date?: string | null;
  end_date?: string | null;
  days_of_week?: number;
  is_active?: boolean;
  is_time_overridden?: boolean;
  priority?: number;
  created_by_id?: string | null;
  floating_section_id?: string;
  master_schedule_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyFloatingSectionScheduleCreateForm(): FloatingSectionScheduleCreateFormState {
  return {
    start_time: '',
    end_time: '',
    start_date: '',
    end_date: '',
    days_of_week: 0,
    is_active: true,
    is_time_overridden: false,
    priority: 0,
    created_by_id: '',
    floating_section_id: '',
    master_schedule_id: '',
  };
}

/** Convert create form state into the payload that baseFloatingSectionScheduleCreateSchema accepts. */
export function buildFloatingSectionScheduleCreatePayload(form: FloatingSectionScheduleCreateFormState): BaseFloatingSectionScheduleCreate {
  const payload: Record<string, unknown> = {
    start_time: form.start_time,
    end_time: form.end_time,
    start_date: form.start_date,
    end_date: form.end_date,
    days_of_week: form.days_of_week,
    is_active: form.is_active,
    is_time_overridden: form.is_time_overridden,
    priority: form.priority,
    created_by_id: form.created_by_id?.toString().trim() || null,
    floating_section_id: form.floating_section_id.trim(),
    master_schedule_id: form.master_schedule_id?.toString().trim() || null,
  };

  return payload as BaseFloatingSectionScheduleCreate;
}

/** Convert update form state into the payload that baseFloatingSectionScheduleUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildFloatingSectionScheduleUpdatePayload(form: FloatingSectionScheduleUpdateFormState): BaseFloatingSectionScheduleUpdate {
  const payload: Record<string, unknown> = {};
  if (form.start_time !== undefined) {
    payload.start_time = form.start_time;
  }
  if (form.end_time !== undefined) {
    payload.end_time = form.end_time;
  }
  if (form.start_date !== undefined) {
    payload.start_date = form.start_date;
  }
  if (form.end_date !== undefined) {
    payload.end_date = form.end_date;
  }
  if (form.days_of_week !== undefined) {
    payload.days_of_week = form.days_of_week;
  }
  if (form.is_active !== undefined) {
    payload.is_active = form.is_active;
  }
  if (form.is_time_overridden !== undefined) {
    payload.is_time_overridden = form.is_time_overridden;
  }
  if (form.priority !== undefined) {
    payload.priority = form.priority;
  }
  if (form.created_by_id !== undefined) {
    payload.created_by_id = form.created_by_id?.toString().trim() ?? null;
  }
  if (form.floating_section_id !== undefined) {
    payload.floating_section_id = form.floating_section_id?.toString().trim() ?? null;
  }
  if (form.master_schedule_id !== undefined) {
    payload.master_schedule_id = form.master_schedule_id?.toString().trim() ?? null;
  }
  return payload as BaseFloatingSectionScheduleUpdate;
}
