/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { PrintImageAsset } from './PrintImageAsset';

/**
 * PrintImageRaster
 */
export interface PrintImageRaster {
  id: string;
  asset_id: string;
  asset?: PrintImageAsset;
  max_width_dots: number;
  width_dots: number;
  height_dots: number;
  data: string;
  content_hash: string;
  byte_length: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PrintImageRaster
 */
export const printImageRasterI18n = {
  label: {"ja":"PrintImageRaster","en":"PrintImageRaster","vi":"PrintImageRaster"},
  fields: {
    asset: {
      label: {"ja":"元画像","en":"Asset","vi":"Ảnh gốc"},
      placeholder: {"ja":"元画像を入力","en":"Enter Asset","vi":"Nhập Ảnh gốc"},
    },
    max_width_dots: {
      label: {"ja":"最大幅ドット","en":"Max Width Dots","vi":"Bề rộng trần (dots)"},
      placeholder: {"ja":"最大幅ドットを入力","en":"Enter Max Width Dots","vi":"Nhập Bề rộng trần (dots)"},
    },
    width_dots: {
      label: {"ja":"幅ドット","en":"Width Dots","vi":"Bề rộng (dots)"},
      placeholder: {"ja":"幅ドットを入力","en":"Enter Width Dots","vi":"Nhập Bề rộng (dots)"},
    },
    height_dots: {
      label: {"ja":"高さドット","en":"Height Dots","vi":"Bề cao (dots)"},
      placeholder: {"ja":"高さドットを入力","en":"Enter Height Dots","vi":"Nhập Bề cao (dots)"},
    },
    data: {
      label: {"ja":"ビットマップ","en":"Bitmap Data","vi":"Dữ liệu bitmap"},
      placeholder: {"ja":"ビットマップを入力","en":"Enter Bitmap Data","vi":"Nhập Dữ liệu bitmap"},
    },
    content_hash: {
      label: {"ja":"ハッシュ","en":"Content Hash","vi":"Hash nội dung"},
      placeholder: {"ja":"ハッシュを入力","en":"Enter Content Hash","vi":"Nhập Hash nội dung"},
    },
    byte_length: {
      label: {"ja":"バイト長","en":"Byte Length","vi":"Số byte"},
      placeholder: {"ja":"バイト長を入力","en":"Enter Byte Length","vi":"Nhập Số byte"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PrintImageRaster */
export const basePrintImageRasterSchemas = {
  max_width_dots: z.number().int(),
  width_dots: z.number().int(),
  height_dots: z.number().int(),
  data: z.string().min(1),
  content_hash: z.string().min(1).max(64),
  byte_length: z.number().int(),
} as const;

/** Create schema for PrintImageRaster */
export const basePrintImageRasterCreateSchema = z.object({
  max_width_dots: basePrintImageRasterSchemas.max_width_dots,
  width_dots: basePrintImageRasterSchemas.width_dots,
  height_dots: basePrintImageRasterSchemas.height_dots,
  data: basePrintImageRasterSchemas.data,
  content_hash: basePrintImageRasterSchemas.content_hash,
  byte_length: basePrintImageRasterSchemas.byte_length,
});

/** Update schema for PrintImageRaster */
export const basePrintImageRasterUpdateSchema = basePrintImageRasterCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePrintImageRasterCreate = z.infer<typeof basePrintImageRasterCreateSchema>;
export type BasePrintImageRasterUpdate = z.infer<typeof basePrintImageRasterUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPrintImageRasterLabel(locale: string): string {
  return printImageRasterI18n.label[locale as keyof typeof printImageRasterI18n.label] ?? printImageRasterI18n.label['en'] ?? 'PrintImageRaster';
}

/** Get field label for a specific locale */
export function getPrintImageRasterFieldLabel(field: string, locale: string): string {
  const fields = printImageRasterI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPrintImageRasterFieldPlaceholder(field: string, locale: string): string {
  const fields = printImageRasterI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PrintImageRaster.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const printImageRasterMetadata = {
  modelName: "PrintImageRaster",
  table: "print_image_rasters",

  fields: {
    asset_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "PrintImageAsset",
        table: "print_image_assets",
        onDelete: "CASCADE",
      },
      label: {},
    },
    max_width_dots: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "最大幅ドット",
        en: "Max Width Dots",
        vi: "Bề rộng trần (dots)",
      },
      placeholder: {
        ja: "最大幅ドットを入力",
        en: "Enter Max Width Dots",
        vi: "Nhập Bề rộng trần (dots)",
      },
    },
    width_dots: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "幅ドット",
        en: "Width Dots",
        vi: "Bề rộng (dots)",
      },
      placeholder: {
        ja: "幅ドットを入力",
        en: "Enter Width Dots",
        vi: "Nhập Bề rộng (dots)",
      },
    },
    height_dots: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "高さドット",
        en: "Height Dots",
        vi: "Bề cao (dots)",
      },
      placeholder: {
        ja: "高さドットを入力",
        en: "Enter Height Dots",
        vi: "Nhập Bề cao (dots)",
      },
    },
    data: {
      type: "text",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ビットマップ",
        en: "Bitmap Data",
        vi: "Dữ liệu bitmap",
      },
      placeholder: {
        ja: "ビットマップを入力",
        en: "Enter Bitmap Data",
        vi: "Nhập Dữ liệu bitmap",
      },
    },
    content_hash: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "ハッシュ",
        en: "Content Hash",
        vi: "Hash nội dung",
      },
      placeholder: {
        ja: "ハッシュを入力",
        en: "Enter Content Hash",
        vi: "Nhập Hash nội dung",
      },
    },
    byte_length: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "バイト長",
        en: "Byte Length",
        vi: "Số byte",
      },
      placeholder: {
        ja: "バイト長を入力",
        en: "Enter Byte Length",
        vi: "Nhập Số byte",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["asset_id", "max_width_dots", "width_dots", "height_dots", "data", "content_hash", "byte_length"] as const,
  enumFields: [] as const,
  relationFields: ["asset_id"] as const,
} as const;

export type PrintImageRasterFieldName = keyof typeof printImageRasterMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PrintImageRaster. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PrintImageRasterCreateFormState {
  asset_id: string;
  max_width_dots: number;
  width_dots: number;
  height_dots: number;
  data: string;
  content_hash: string;
  byte_length: number;
}

/** Form-state shape for updating a PrintImageRaster. Every field optional. */
export interface PrintImageRasterUpdateFormState {
  asset_id?: string;
  max_width_dots?: number;
  width_dots?: number;
  height_dots?: number;
  data?: string;
  content_hash?: string;
  byte_length?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPrintImageRasterCreateForm(): PrintImageRasterCreateFormState {
  return {
    asset_id: '',
    max_width_dots: 0,
    width_dots: 0,
    height_dots: 0,
    data: '',
    content_hash: '',
    byte_length: 0,
  };
}

/** Convert create form state into the payload that basePrintImageRasterCreateSchema accepts. */
export function buildPrintImageRasterCreatePayload(form: PrintImageRasterCreateFormState): BasePrintImageRasterCreate {
  const payload: Record<string, unknown> = {
    asset_id: form.asset_id.trim(),
    max_width_dots: form.max_width_dots,
    width_dots: form.width_dots,
    height_dots: form.height_dots,
    data: form.data.trim(),
    content_hash: form.content_hash.trim(),
    byte_length: form.byte_length,
  };

  return payload as BasePrintImageRasterCreate;
}

/** Convert update form state into the payload that basePrintImageRasterUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPrintImageRasterUpdatePayload(form: PrintImageRasterUpdateFormState): BasePrintImageRasterUpdate {
  const payload: Record<string, unknown> = {};
  if (form.asset_id !== undefined) {
    payload.asset_id = form.asset_id?.toString().trim() ?? null;
  }
  if (form.max_width_dots !== undefined) {
    payload.max_width_dots = form.max_width_dots;
  }
  if (form.width_dots !== undefined) {
    payload.width_dots = form.width_dots;
  }
  if (form.height_dots !== undefined) {
    payload.height_dots = form.height_dots;
  }
  if (form.data !== undefined) {
    payload.data = form.data?.toString().trim() ?? null;
  }
  if (form.content_hash !== undefined) {
    payload.content_hash = form.content_hash?.toString().trim() ?? null;
  }
  if (form.byte_length !== undefined) {
    payload.byte_length = form.byte_length;
  }
  return payload as BasePrintImageRasterUpdate;
}
