/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * PersonalAccessToken
 */
export interface PersonalAccessToken {
  id: number;
  tokenable_type: string;
  tokenable_id: string;
  name: string;
  token: string;
  abilities?: string | null;
  last_used_at?: DateTimeString | null;
  expires_at?: DateTimeString | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for PersonalAccessToken
 */
export const personalAccessTokenI18n = {
  label: {"ja":"PersonalAccessToken","en":"PersonalAccessToken","vi":"PersonalAccessToken"},
  fields: {
    tokenable_type: {
      label: {"ja":"対象タイプ","en":"Tokenable Type","vi":"Loại đối tượng"},
      placeholder: {"ja":"対象タイプを入力","en":"Enter Tokenable Type","vi":"Nhập Loại đối tượng"},
    },
    tokenable_id: {
      label: {"ja":"対象ID","en":"Tokenable ID","vi":"ID đối tượng"},
      placeholder: {"ja":"対象IDを入力","en":"Enter Tokenable ID","vi":"Nhập ID đối tượng"},
    },
    name: {
      label: {"ja":"名称","en":"Name","vi":"Tên"},
      placeholder: {"ja":"名称を入力","en":"Enter Name","vi":"Nhập Tên"},
    },
    token: {
      label: {"ja":"トークンハッシュ","en":"Token Hash","vi":"Hash token"},
      placeholder: {"ja":"トークンハッシュを入力","en":"Enter Token Hash","vi":"Nhập Hash token"},
    },
    abilities: {
      label: {"ja":"権限","en":"Abilities","vi":"Quyền"},
      placeholder: {"ja":"権限を入力","en":"Enter Abilities","vi":"Nhập Quyền"},
    },
    last_used_at: {
      label: {"ja":"最終使用日時","en":"Last Used At","vi":"Lần dùng cuối"},
      placeholder: {"ja":"最終使用日時を入力","en":"Enter Last Used At","vi":"Nhập Lần dùng cuối"},
    },
    expires_at: {
      label: {"ja":"有効期限","en":"Expires At","vi":"Hết hạn"},
      placeholder: {"ja":"有効期限を入力","en":"Enter Expires At","vi":"Nhập Hết hạn"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for PersonalAccessToken */
export const basePersonalAccessTokenSchemas = {
  tokenable_type: z.string().min(1).max(255),
  tokenable_id: z.string().min(1).max(36),
  name: z.string().min(1),
  token: z.string().min(1).max(64),
  abilities: z.string().optional().nullable(),
  last_used_at: z.string().datetime({ offset: true }).optional().nullable(),
  expires_at: z.string().datetime({ offset: true }).optional().nullable(),
} as const;

/** Create schema for PersonalAccessToken */
export const basePersonalAccessTokenCreateSchema = z.object({
  tokenable_type: basePersonalAccessTokenSchemas.tokenable_type,
  tokenable_id: basePersonalAccessTokenSchemas.tokenable_id,
  name: basePersonalAccessTokenSchemas.name,
  token: basePersonalAccessTokenSchemas.token,
  abilities: basePersonalAccessTokenSchemas.abilities,
  last_used_at: basePersonalAccessTokenSchemas.last_used_at,
  expires_at: basePersonalAccessTokenSchemas.expires_at,
});

/** Update schema for PersonalAccessToken */
export const basePersonalAccessTokenUpdateSchema = basePersonalAccessTokenCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePersonalAccessTokenCreate = z.infer<typeof basePersonalAccessTokenCreateSchema>;
export type BasePersonalAccessTokenUpdate = z.infer<typeof basePersonalAccessTokenUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPersonalAccessTokenLabel(locale: string): string {
  return personalAccessTokenI18n.label[locale as keyof typeof personalAccessTokenI18n.label] ?? personalAccessTokenI18n.label['en'] ?? 'PersonalAccessToken';
}

/** Get field label for a specific locale */
export function getPersonalAccessTokenFieldLabel(field: string, locale: string): string {
  const fields = personalAccessTokenI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPersonalAccessTokenFieldPlaceholder(field: string, locale: string): string {
  const fields = personalAccessTokenI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for PersonalAccessToken.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const personalAccessTokenMetadata = {
  modelName: "PersonalAccessToken",
  table: "personal_access_tokens",

  fields: {
    tokenable_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 255,
      },
      label: {
        ja: "対象タイプ",
        en: "Tokenable Type",
        vi: "Loại đối tượng",
      },
      placeholder: {
        ja: "対象タイプを入力",
        en: "Enter Tokenable Type",
        vi: "Nhập Loại đối tượng",
      },
    },
    tokenable_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 36,
      },
      label: {
        ja: "対象ID",
        en: "Tokenable ID",
        vi: "ID đối tượng",
      },
      placeholder: {
        ja: "対象IDを入力",
        en: "Enter Tokenable ID",
        vi: "Nhập ID đối tượng",
      },
    },
    name: {
      type: "text",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "名称",
        en: "Name",
        vi: "Tên",
      },
      placeholder: {
        ja: "名称を入力",
        en: "Enter Name",
        vi: "Nhập Tên",
      },
    },
    token: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "トークンハッシュ",
        en: "Token Hash",
        vi: "Hash token",
      },
      placeholder: {
        ja: "トークンハッシュを入力",
        en: "Enter Token Hash",
        vi: "Nhập Hash token",
      },
    },
    abilities: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "権限",
        en: "Abilities",
        vi: "Quyền",
      },
      placeholder: {
        ja: "権限を入力",
        en: "Enter Abilities",
        vi: "Nhập Quyền",
      },
    },
    last_used_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最終使用日時",
        en: "Last Used At",
        vi: "Lần dùng cuối",
      },
      placeholder: {
        ja: "最終使用日時を入力",
        en: "Enter Last Used At",
        vi: "Nhập Lần dùng cuối",
      },
    },
    expires_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "有効期限",
        en: "Expires At",
        vi: "Hết hạn",
      },
      placeholder: {
        ja: "有効期限を入力",
        en: "Enter Expires At",
        vi: "Nhập Hết hạn",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["tokenable_type", "tokenable_id", "name", "token"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type PersonalAccessTokenFieldName = keyof typeof personalAccessTokenMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a PersonalAccessToken. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PersonalAccessTokenCreateFormState {
  tokenable_type: string;
  tokenable_id: string;
  name: string;
  token: string;
  abilities?: string | null;
  last_used_at?: string | null;
  expires_at?: string | null;
}

/** Form-state shape for updating a PersonalAccessToken. Every field optional. */
export interface PersonalAccessTokenUpdateFormState {
  tokenable_type?: string;
  tokenable_id?: string;
  name?: string;
  token?: string;
  abilities?: string | null;
  last_used_at?: string | null;
  expires_at?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPersonalAccessTokenCreateForm(): PersonalAccessTokenCreateFormState {
  return {
    tokenable_type: '',
    tokenable_id: '',
    name: '',
    token: '',
    abilities: '',
    last_used_at: '',
    expires_at: '',
  };
}

/** Convert create form state into the payload that basePersonalAccessTokenCreateSchema accepts. */
export function buildPersonalAccessTokenCreatePayload(form: PersonalAccessTokenCreateFormState): BasePersonalAccessTokenCreate {
  const payload: Record<string, unknown> = {
    tokenable_type: form.tokenable_type.trim(),
    tokenable_id: form.tokenable_id.trim(),
    name: form.name.trim(),
    token: form.token.trim(),
    abilities: form.abilities?.toString().trim() || null,
    last_used_at: form.last_used_at,
    expires_at: form.expires_at,
  };

  return payload as BasePersonalAccessTokenCreate;
}

/** Convert update form state into the payload that basePersonalAccessTokenUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPersonalAccessTokenUpdatePayload(form: PersonalAccessTokenUpdateFormState): BasePersonalAccessTokenUpdate {
  const payload: Record<string, unknown> = {};
  if (form.tokenable_type !== undefined) {
    payload.tokenable_type = form.tokenable_type?.toString().trim() ?? null;
  }
  if (form.tokenable_id !== undefined) {
    payload.tokenable_id = form.tokenable_id?.toString().trim() ?? null;
  }
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.token !== undefined) {
    payload.token = form.token?.toString().trim() ?? null;
  }
  if (form.abilities !== undefined) {
    payload.abilities = form.abilities?.toString().trim() ?? null;
  }
  if (form.last_used_at !== undefined) {
    payload.last_used_at = form.last_used_at;
  }
  if (form.expires_at !== undefined) {
    payload.expires_at = form.expires_at;
  }
  return payload as BasePersonalAccessTokenUpdate;
}
