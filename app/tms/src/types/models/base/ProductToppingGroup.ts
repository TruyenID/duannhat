/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * 商品トッピンググループ
 */
export interface ProductToppingGroup {
  id: number;
  sort_order: number;
  min_select_override?: number | null;
  max_select_override?: number | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for ProductToppingGroup
 */
export const productToppingGroupI18n = {
  label: {"ja":"商品トッピンググループ","en":"Product Topping Group","vi":"Phân công Topping cho Sản phẩm"},
  fields: {
    sort_order: {
      label: {"ja":"表示順序","en":"Sort Order","vi":"Thứ tự hiển thị"},
      placeholder: {"ja":"表示順序を入力","en":"Enter Sort Order","vi":"Nhập Thứ tự hiển thị"},
    },
    min_select_override: {
      label: {"ja":"最小選択数（上書き）","en":"Min Select Override","vi":"Số tối thiểu (ghi đè)"},
      placeholder: {"ja":"最小選択数（上書き）を入力","en":"Enter Min Select Override","vi":"Nhập Số tối thiểu (ghi đè)"},
    },
    max_select_override: {
      label: {"ja":"最大選択数（上書き）","en":"Max Select Override","vi":"Số tối đa (ghi đè)"},
      placeholder: {"ja":"最大選択数（上書き）を入力","en":"Enter Max Select Override","vi":"Nhập Số tối đa (ghi đè)"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for ProductToppingGroup */
export const baseProductToppingGroupSchemas = {
  sort_order: z.number().int(),
  min_select_override: z.number().int().optional().nullable(),
  max_select_override: z.number().int().optional().nullable(),
} as const;

/** Create schema for ProductToppingGroup */
export const baseProductToppingGroupCreateSchema = z.object({
  sort_order: baseProductToppingGroupSchemas.sort_order,
  min_select_override: baseProductToppingGroupSchemas.min_select_override,
  max_select_override: baseProductToppingGroupSchemas.max_select_override,
});

/** Update schema for ProductToppingGroup */
export const baseProductToppingGroupUpdateSchema = baseProductToppingGroupCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseProductToppingGroupCreate = z.infer<typeof baseProductToppingGroupCreateSchema>;
export type BaseProductToppingGroupUpdate = z.infer<typeof baseProductToppingGroupUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getProductToppingGroupLabel(locale: string): string {
  return productToppingGroupI18n.label[locale as keyof typeof productToppingGroupI18n.label] ?? productToppingGroupI18n.label['en'] ?? 'ProductToppingGroup';
}

/** Get field label for a specific locale */
export function getProductToppingGroupFieldLabel(field: string, locale: string): string {
  const fields = productToppingGroupI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getProductToppingGroupFieldPlaceholder(field: string, locale: string): string {
  const fields = productToppingGroupI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for ProductToppingGroup.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const productToppingGroupMetadata = {
  modelName: "ProductToppingGroup",
  table: "product_topping_groups",

  fields: {
    sort_order: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "表示順序",
        en: "Sort Order",
        vi: "Thứ tự hiển thị",
      },
      placeholder: {
        ja: "表示順序を入力",
        en: "Enter Sort Order",
        vi: "Nhập Thứ tự hiển thị",
      },
    },
    min_select_override: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最小選択数（上書き）",
        en: "Min Select Override",
        vi: "Số tối thiểu (ghi đè)",
      },
      placeholder: {
        ja: "最小選択数（上書き）を入力",
        en: "Enter Min Select Override",
        vi: "Nhập Số tối thiểu (ghi đè)",
      },
    },
    max_select_override: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最大選択数（上書き）",
        en: "Max Select Override",
        vi: "Số tối đa (ghi đè)",
      },
      placeholder: {
        ja: "最大選択数（上書き）を入力",
        en: "Enter Max Select Override",
        vi: "Nhập Số tối đa (ghi đè)",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["sort_order"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type ProductToppingGroupFieldName = keyof typeof productToppingGroupMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a ProductToppingGroup. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface ProductToppingGroupCreateFormState {
  sort_order: number;
  min_select_override?: number | null;
  max_select_override?: number | null;
}

/** Form-state shape for updating a ProductToppingGroup. Every field optional. */
export interface ProductToppingGroupUpdateFormState {
  sort_order?: number;
  min_select_override?: number | null;
  max_select_override?: number | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyProductToppingGroupCreateForm(): ProductToppingGroupCreateFormState {
  return {
    sort_order: 0,
    min_select_override: 0,
    max_select_override: 0,
  };
}

/** Convert create form state into the payload that baseProductToppingGroupCreateSchema accepts. */
export function buildProductToppingGroupCreatePayload(form: ProductToppingGroupCreateFormState): BaseProductToppingGroupCreate {
  const payload: Record<string, unknown> = {
    sort_order: form.sort_order,
    min_select_override: form.min_select_override,
    max_select_override: form.max_select_override,
  };

  return payload as BaseProductToppingGroupCreate;
}

/** Convert update form state into the payload that baseProductToppingGroupUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildProductToppingGroupUpdatePayload(form: ProductToppingGroupUpdateFormState): BaseProductToppingGroupUpdate {
  const payload: Record<string, unknown> = {};
  if (form.sort_order !== undefined) {
    payload.sort_order = form.sort_order;
  }
  if (form.min_select_override !== undefined) {
    payload.min_select_override = form.min_select_override;
  }
  if (form.max_select_override !== undefined) {
    payload.max_select_override = form.max_select_override;
  }
  return payload as BaseProductToppingGroupUpdate;
}
