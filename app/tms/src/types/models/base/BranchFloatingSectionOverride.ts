/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { Branch } from './Branch';
import type { FloatingSectionSchedule } from './FloatingSectionSchedule';
import type { Organization } from './Organization';
import type { User } from './User';

/**
 * 店舗フローティングセクション上書き
 */
export interface BranchFloatingSectionOverride {
  id: string;
  start_time?: string | null;
  end_time?: string | null;
  days_of_week?: number | null;
  floating_section_schedule_id: string;
  floatingSectionSchedule?: FloatingSectionSchedule;
  branch_id: string;
  branch?: Branch;
  organization_id: string;
  organization?: Organization;
  updated_by_id?: string | null;
  updatedBy?: User | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for BranchFloatingSectionOverride
 */
export const branchFloatingSectionOverrideI18n = {
  label: {"ja":"店舗フローティングセクション上書き","en":"Branch Floating Section Override","vi":"Ghi đè khung giờ ưu đãi chi nhánh"},
  fields: {
    start_time: {
      label: {"ja":"開始時刻（上書き）","en":"Start Time (Override)","vi":"Giờ bắt đầu (ghi đè)"},
      placeholder: {"ja":"開始時刻（上書き）を入力","en":"Enter Start Time (Override)","vi":"Nhập Giờ bắt đầu (ghi đè)"},
    },
    end_time: {
      label: {"ja":"終了時刻（上書き）","en":"End Time (Override)","vi":"Giờ kết thúc (ghi đè)"},
      placeholder: {"ja":"終了時刻（上書き）を入力","en":"Enter End Time (Override)","vi":"Nhập Giờ kết thúc (ghi đè)"},
    },
    days_of_week: {
      label: {"ja":"曜日（上書き）","en":"Days of Week (Override)","vi":"Ngày trong tuần (ghi đè)"},
      placeholder: {"ja":"曜日（上書き）を入力","en":"Enter Days of Week (Override)","vi":"Nhập Ngày trong tuần (ghi đè)"},
    },
    floating_section_schedule: {
      label: {"ja":"フローティングセクションスケジュール","en":"Floating Section Schedule","vi":"Lịch trình khung giờ ưu đãi"},
      placeholder: {"ja":"フローティングセクションスケジュールを入力","en":"Enter Floating Section Schedule","vi":"Nhập Lịch trình khung giờ ưu đãi"},
    },
    branch: {
      label: {"ja":"店舗","en":"Branch","vi":"Chi nhánh"},
      placeholder: {"ja":"店舗を入力","en":"Enter Branch","vi":"Nhập Chi nhánh"},
    },
    organization: {
      label: {"ja":"組織","en":"Organization","vi":"Tổ chức"},
      placeholder: {"ja":"組織を入力","en":"Enter Organization","vi":"Nhập Tổ chức"},
    },
    updated_by: {
      label: {"ja":"更新者","en":"Updated By","vi":"Người cập nhật"},
      placeholder: {"ja":"更新者を入力","en":"Enter Updated By","vi":"Nhập Người cập nhật"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for BranchFloatingSectionOverride */
export const baseBranchFloatingSectionOverrideSchemas = {
  start_time: z.string().time().optional().nullable(),
  end_time: z.string().time().optional().nullable(),
  days_of_week: z.number().int().optional().nullable(),
} as const;

/** Create schema for BranchFloatingSectionOverride */
export const baseBranchFloatingSectionOverrideCreateSchema = z.object({
  start_time: baseBranchFloatingSectionOverrideSchemas.start_time,
  end_time: baseBranchFloatingSectionOverrideSchemas.end_time,
  days_of_week: baseBranchFloatingSectionOverrideSchemas.days_of_week,
});

/** Update schema for BranchFloatingSectionOverride */
export const baseBranchFloatingSectionOverrideUpdateSchema = baseBranchFloatingSectionOverrideCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseBranchFloatingSectionOverrideCreate = z.infer<typeof baseBranchFloatingSectionOverrideCreateSchema>;
export type BaseBranchFloatingSectionOverrideUpdate = z.infer<typeof baseBranchFloatingSectionOverrideUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getBranchFloatingSectionOverrideLabel(locale: string): string {
  return branchFloatingSectionOverrideI18n.label[locale as keyof typeof branchFloatingSectionOverrideI18n.label] ?? branchFloatingSectionOverrideI18n.label['en'] ?? 'BranchFloatingSectionOverride';
}

/** Get field label for a specific locale */
export function getBranchFloatingSectionOverrideFieldLabel(field: string, locale: string): string {
  const fields = branchFloatingSectionOverrideI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getBranchFloatingSectionOverrideFieldPlaceholder(field: string, locale: string): string {
  const fields = branchFloatingSectionOverrideI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for BranchFloatingSectionOverride.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const branchFloatingSectionOverrideMetadata = {
  modelName: "BranchFloatingSectionOverride",
  table: "branch_floating_section_overrides",

  fields: {
    start_time: {
      type: "time",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "開始時刻（上書き）",
        en: "Start Time (Override)",
        vi: "Giờ bắt đầu (ghi đè)",
      },
      placeholder: {
        ja: "開始時刻（上書き）を入力",
        en: "Enter Start Time (Override)",
        vi: "Nhập Giờ bắt đầu (ghi đè)",
      },
    },
    end_time: {
      type: "time",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "終了時刻（上書き）",
        en: "End Time (Override)",
        vi: "Giờ kết thúc (ghi đè)",
      },
      placeholder: {
        ja: "終了時刻（上書き）を入力",
        en: "Enter End Time (Override)",
        vi: "Nhập Giờ kết thúc (ghi đè)",
      },
    },
    days_of_week: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "曜日（上書き）",
        en: "Days of Week (Override)",
        vi: "Ngày trong tuần (ghi đè)",
      },
      placeholder: {
        ja: "曜日（上書き）を入力",
        en: "Enter Days of Week (Override)",
        vi: "Nhập Ngày trong tuần (ghi đè)",
      },
    },
    floating_section_schedule_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "FloatingSectionSchedule",
        table: "floating_section_schedules",
        onDelete: "CASCADE",
      },
      label: {},
    },
    branch_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Branch",
        table: "branches",
        onDelete: "CASCADE",
      },
      label: {},
    },
    organization_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "Organization",
        table: "organizations",
        onDelete: undefined,
      },
      label: {},
    },
    updated_by_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: undefined,
      },
      label: {},
    },
  },

  translatableFields: [] as const,
  requiredFields: ["floating_section_schedule_id", "branch_id", "organization_id"] as const,
  enumFields: [] as const,
  relationFields: ["floating_section_schedule_id", "branch_id", "organization_id", "updated_by_id"] as const,
} as const;

export type BranchFloatingSectionOverrideFieldName = keyof typeof branchFloatingSectionOverrideMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a BranchFloatingSectionOverride. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface BranchFloatingSectionOverrideCreateFormState {
  start_time?: string | null;
  end_time?: string | null;
  days_of_week?: number | null;
  floating_section_schedule_id: string;
  branch_id: string;
  organization_id: string;
  updated_by_id?: string | null;
}

/** Form-state shape for updating a BranchFloatingSectionOverride. Every field optional. */
export interface BranchFloatingSectionOverrideUpdateFormState {
  start_time?: string | null;
  end_time?: string | null;
  days_of_week?: number | null;
  floating_section_schedule_id?: string;
  branch_id?: string;
  organization_id?: string;
  updated_by_id?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyBranchFloatingSectionOverrideCreateForm(): BranchFloatingSectionOverrideCreateFormState {
  return {
    start_time: '',
    end_time: '',
    days_of_week: 0,
    floating_section_schedule_id: '',
    branch_id: '',
    organization_id: '',
    updated_by_id: '',
  };
}

/** Convert create form state into the payload that baseBranchFloatingSectionOverrideCreateSchema accepts. */
export function buildBranchFloatingSectionOverrideCreatePayload(form: BranchFloatingSectionOverrideCreateFormState): BaseBranchFloatingSectionOverrideCreate {
  const payload: Record<string, unknown> = {
    start_time: form.start_time,
    end_time: form.end_time,
    days_of_week: form.days_of_week,
    floating_section_schedule_id: form.floating_section_schedule_id.trim(),
    branch_id: form.branch_id.trim(),
    organization_id: form.organization_id.trim(),
    updated_by_id: form.updated_by_id?.toString().trim() || null,
  };

  return payload as BaseBranchFloatingSectionOverrideCreate;
}

/** Convert update form state into the payload that baseBranchFloatingSectionOverrideUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildBranchFloatingSectionOverrideUpdatePayload(form: BranchFloatingSectionOverrideUpdateFormState): BaseBranchFloatingSectionOverrideUpdate {
  const payload: Record<string, unknown> = {};
  if (form.start_time !== undefined) {
    payload.start_time = form.start_time;
  }
  if (form.end_time !== undefined) {
    payload.end_time = form.end_time;
  }
  if (form.days_of_week !== undefined) {
    payload.days_of_week = form.days_of_week;
  }
  if (form.floating_section_schedule_id !== undefined) {
    payload.floating_section_schedule_id = form.floating_section_schedule_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.updated_by_id !== undefined) {
    payload.updated_by_id = form.updated_by_id?.toString().trim() ?? null;
  }
  return payload as BaseBranchFloatingSectionOverrideUpdate;
}
