/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * VN電子インボイス設定
 */
export interface VnEinvoiceSetting {
  id: string;
  organization_id: string;
  brand_id: string;
  branch_id?: string | null;
  enabled: boolean;
  provider?: string | null;
  template_code?: string | null;
  invoice_series?: string | null;
  credentials?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
  deleted_at?: DateTimeString | null;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for VnEinvoiceSetting
 */
export const vnEinvoiceSettingI18n = {
  label: {"ja":"VN電子インボイス設定","en":"VN E-Invoice Setting","vi":"Cấu hình HĐĐT"},
  fields: {
    organization_id: {
      label: {"ja":"組織ID","en":"Organization ID","vi":"ID tổ chức"},
      placeholder: {"ja":"組織IDを入力","en":"Enter Organization ID","vi":"Nhập ID tổ chức"},
    },
    brand_id: {
      label: {"ja":"ブランドID","en":"Brand ID","vi":"ID thương hiệu"},
      placeholder: {"ja":"ブランドIDを入力","en":"Enter Brand ID","vi":"Nhập ID thương hiệu"},
    },
    branch_id: {
      label: {"ja":"店舗ID","en":"Branch ID","vi":"ID chi nhánh"},
      placeholder: {"ja":"店舗IDを入力","en":"Enter Branch ID","vi":"Nhập ID chi nhánh"},
    },
    enabled: {
      label: {"ja":"有効","en":"Enabled","vi":"Bật truyền HĐĐT"},
      placeholder: {"ja":"有効を入力","en":"Enter Enabled","vi":"Nhập Bật truyền HĐĐT"},
    },
    provider: {
      label: {"ja":"プロバイダー","en":"Provider","vi":"Nhà cung cấp"},
      placeholder: {"ja":"プロバイダーを入力","en":"Enter Provider","vi":"Nhập Nhà cung cấp"},
    },
    template_code: {
      label: {"ja":"様式番号","en":"Template Code","vi":"Mẫu số"},
      placeholder: {"ja":"様式番号を入力","en":"Enter Template Code","vi":"Nhập Mẫu số"},
    },
    invoice_series: {
      label: {"ja":"記号","en":"Invoice Series","vi":"Ký hiệu"},
      placeholder: {"ja":"記号を入力","en":"Enter Invoice Series","vi":"Nhập Ký hiệu"},
    },
    credentials: {
      label: {"ja":"認証情報","en":"Credentials","vi":"Thông tin xác thực"},
      placeholder: {"ja":"認証情報を入力","en":"Enter Credentials","vi":"Nhập Thông tin xác thực"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for VnEinvoiceSetting */
export const baseVnEinvoiceSettingSchemas = {
  organization_id: z.string(),
  brand_id: z.string(),
  branch_id: z.string().optional().nullable(),
  enabled: z.boolean(),
  provider: z.string().max(30).optional().nullable(),
  template_code: z.string().max(20).optional().nullable(),
  invoice_series: z.string().max(20).optional().nullable(),
  credentials: z.string().optional().nullable(),
} as const;

/** Create schema for VnEinvoiceSetting */
export const baseVnEinvoiceSettingCreateSchema = z.object({
  organization_id: baseVnEinvoiceSettingSchemas.organization_id,
  brand_id: baseVnEinvoiceSettingSchemas.brand_id,
  branch_id: baseVnEinvoiceSettingSchemas.branch_id,
  enabled: baseVnEinvoiceSettingSchemas.enabled,
  provider: baseVnEinvoiceSettingSchemas.provider,
  template_code: baseVnEinvoiceSettingSchemas.template_code,
  invoice_series: baseVnEinvoiceSettingSchemas.invoice_series,
  credentials: baseVnEinvoiceSettingSchemas.credentials,
});

/** Update schema for VnEinvoiceSetting */
export const baseVnEinvoiceSettingUpdateSchema = baseVnEinvoiceSettingCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseVnEinvoiceSettingCreate = z.infer<typeof baseVnEinvoiceSettingCreateSchema>;
export type BaseVnEinvoiceSettingUpdate = z.infer<typeof baseVnEinvoiceSettingUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getVnEinvoiceSettingLabel(locale: string): string {
  return vnEinvoiceSettingI18n.label[locale as keyof typeof vnEinvoiceSettingI18n.label] ?? vnEinvoiceSettingI18n.label['en'] ?? 'VnEinvoiceSetting';
}

/** Get field label for a specific locale */
export function getVnEinvoiceSettingFieldLabel(field: string, locale: string): string {
  const fields = vnEinvoiceSettingI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getVnEinvoiceSettingFieldPlaceholder(field: string, locale: string): string {
  const fields = vnEinvoiceSettingI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for VnEinvoiceSetting.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const vnEinvoiceSettingMetadata = {
  modelName: "VnEinvoiceSetting",
  table: "vn_einvoice_settings",

  fields: {
    organization_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "組織ID",
        en: "Organization ID",
        vi: "ID tổ chức",
      },
      placeholder: {
        ja: "組織IDを入力",
        en: "Enter Organization ID",
        vi: "Nhập ID tổ chức",
      },
    },
    brand_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ブランドID",
        en: "Brand ID",
        vi: "ID thương hiệu",
      },
      placeholder: {
        ja: "ブランドIDを入力",
        en: "Enter Brand ID",
        vi: "Nhập ID thương hiệu",
      },
    },
    branch_id: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "店舗ID",
        en: "Branch ID",
        vi: "ID chi nhánh",
      },
      placeholder: {
        ja: "店舗IDを入力",
        en: "Enter Branch ID",
        vi: "Nhập ID chi nhánh",
      },
    },
    enabled: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "有効",
        en: "Enabled",
        vi: "Bật truyền HĐĐT",
      },
      placeholder: {
        ja: "有効を入力",
        en: "Enter Enabled",
        vi: "Nhập Bật truyền HĐĐT",
      },
    },
    provider: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 30,
      },
      label: {
        ja: "プロバイダー",
        en: "Provider",
        vi: "Nhà cung cấp",
      },
      placeholder: {
        ja: "プロバイダーを入力",
        en: "Enter Provider",
        vi: "Nhập Nhà cung cấp",
      },
    },
    template_code: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "様式番号",
        en: "Template Code",
        vi: "Mẫu số",
      },
      placeholder: {
        ja: "様式番号を入力",
        en: "Enter Template Code",
        vi: "Nhập Mẫu số",
      },
    },
    invoice_series: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "記号",
        en: "Invoice Series",
        vi: "Ký hiệu",
      },
      placeholder: {
        ja: "記号を入力",
        en: "Enter Invoice Series",
        vi: "Nhập Ký hiệu",
      },
    },
    credentials: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "認証情報",
        en: "Credentials",
        vi: "Thông tin xác thực",
      },
      placeholder: {
        ja: "認証情報を入力",
        en: "Enter Credentials",
        vi: "Nhập Thông tin xác thực",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["organization_id", "brand_id", "enabled"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type VnEinvoiceSettingFieldName = keyof typeof vnEinvoiceSettingMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a VnEinvoiceSetting. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface VnEinvoiceSettingCreateFormState {
  organization_id: string;
  brand_id: string;
  branch_id?: string | null;
  enabled: boolean;
  provider?: string | null;
  template_code?: string | null;
  invoice_series?: string | null;
  credentials?: string | null;
}

/** Form-state shape for updating a VnEinvoiceSetting. Every field optional. */
export interface VnEinvoiceSettingUpdateFormState {
  organization_id?: string;
  brand_id?: string;
  branch_id?: string | null;
  enabled?: boolean;
  provider?: string | null;
  template_code?: string | null;
  invoice_series?: string | null;
  credentials?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyVnEinvoiceSettingCreateForm(): VnEinvoiceSettingCreateFormState {
  return {
    organization_id: '',
    brand_id: '',
    branch_id: '',
    enabled: false,
    provider: '',
    template_code: '',
    invoice_series: '',
    credentials: '',
  };
}

/** Convert create form state into the payload that baseVnEinvoiceSettingCreateSchema accepts. */
export function buildVnEinvoiceSettingCreatePayload(form: VnEinvoiceSettingCreateFormState): BaseVnEinvoiceSettingCreate {
  const payload: Record<string, unknown> = {
    organization_id: form.organization_id.trim(),
    brand_id: form.brand_id.trim(),
    branch_id: form.branch_id?.toString().trim() || null,
    enabled: form.enabled,
    provider: form.provider?.toString().trim() || null,
    template_code: form.template_code?.toString().trim() || null,
    invoice_series: form.invoice_series?.toString().trim() || null,
    credentials: form.credentials?.toString().trim() || null,
  };

  return payload as BaseVnEinvoiceSettingCreate;
}

/** Convert update form state into the payload that baseVnEinvoiceSettingUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildVnEinvoiceSettingUpdatePayload(form: VnEinvoiceSettingUpdateFormState): BaseVnEinvoiceSettingUpdate {
  const payload: Record<string, unknown> = {};
  if (form.organization_id !== undefined) {
    payload.organization_id = form.organization_id?.toString().trim() ?? null;
  }
  if (form.brand_id !== undefined) {
    payload.brand_id = form.brand_id?.toString().trim() ?? null;
  }
  if (form.branch_id !== undefined) {
    payload.branch_id = form.branch_id?.toString().trim() ?? null;
  }
  if (form.enabled !== undefined) {
    payload.enabled = form.enabled;
  }
  if (form.provider !== undefined) {
    payload.provider = form.provider?.toString().trim() ?? null;
  }
  if (form.template_code !== undefined) {
    payload.template_code = form.template_code?.toString().trim() ?? null;
  }
  if (form.invoice_series !== undefined) {
    payload.invoice_series = form.invoice_series?.toString().trim() ?? null;
  }
  if (form.credentials !== undefined) {
    payload.credentials = form.credentials?.toString().trim() ?? null;
  }
  return payload as BaseVnEinvoiceSettingUpdate;
}
