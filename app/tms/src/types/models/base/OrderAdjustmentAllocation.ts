/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { CustomerOrder } from './CustomerOrder';
import type { CustomerOrderItem } from './CustomerOrderItem';
import type { OrderAdjustment } from './OrderAdjustment';

/**
 * OrderAdjustmentAllocation
 */
export interface OrderAdjustmentAllocation {
  id: string;
  orderAdjustment_id: string;
  orderAdjustment?: OrderAdjustment;
  customerOrder_id: string;
  customerOrder?: CustomerOrder;
  orderItem_id?: string | null;
  orderItem?: CustomerOrderItem | null;
  amount: number;
  tax_rate: number;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for OrderAdjustmentAllocation
 */
export const orderAdjustmentAllocationI18n = {
  label: {"ja":"OrderAdjustmentAllocation","en":"OrderAdjustmentAllocation","vi":"OrderAdjustmentAllocation"},
  fields: {
    order_adjustment: {
      label: {"ja":"調整","en":"Order Adjustment","vi":"Khoản điều chỉnh"},
      placeholder: {"ja":"調整を入力","en":"Enter Order Adjustment","vi":"Nhập Khoản điều chỉnh"},
    },
    customer_order: {
      label: {"ja":"注文","en":"Customer Order","vi":"Đơn hàng"},
      placeholder: {"ja":"注文を入力","en":"Enter Customer Order","vi":"Nhập Đơn hàng"},
    },
    order_item: {
      label: {"ja":"明細","en":"Order Item","vi":"Dòng hàng"},
      placeholder: {"ja":"明細を入力","en":"Enter Order Item","vi":"Nhập Dòng hàng"},
    },
    amount: {
      label: {"ja":"金額","en":"Amount","vi":"Số tiền"},
      placeholder: {"ja":"金額を入力","en":"Enter Amount","vi":"Nhập Số tiền"},
    },
    tax_rate: {
      label: {"ja":"税率","en":"Tax Rate","vi":"Thuế suất"},
      placeholder: {"ja":"税率を入力","en":"Enter Tax Rate","vi":"Nhập Thuế suất"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for OrderAdjustmentAllocation */
export const baseOrderAdjustmentAllocationSchemas = {
  amount: z.number(),
  tax_rate: z.number(),
} as const;

/** Create schema for OrderAdjustmentAllocation */
export const baseOrderAdjustmentAllocationCreateSchema = z.object({
  amount: baseOrderAdjustmentAllocationSchemas.amount,
  tax_rate: baseOrderAdjustmentAllocationSchemas.tax_rate,
});

/** Update schema for OrderAdjustmentAllocation */
export const baseOrderAdjustmentAllocationUpdateSchema = baseOrderAdjustmentAllocationCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseOrderAdjustmentAllocationCreate = z.infer<typeof baseOrderAdjustmentAllocationCreateSchema>;
export type BaseOrderAdjustmentAllocationUpdate = z.infer<typeof baseOrderAdjustmentAllocationUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getOrderAdjustmentAllocationLabel(locale: string): string {
  return orderAdjustmentAllocationI18n.label[locale as keyof typeof orderAdjustmentAllocationI18n.label] ?? orderAdjustmentAllocationI18n.label['en'] ?? 'OrderAdjustmentAllocation';
}

/** Get field label for a specific locale */
export function getOrderAdjustmentAllocationFieldLabel(field: string, locale: string): string {
  const fields = orderAdjustmentAllocationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getOrderAdjustmentAllocationFieldPlaceholder(field: string, locale: string): string {
  const fields = orderAdjustmentAllocationI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for OrderAdjustmentAllocation.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const orderAdjustmentAllocationMetadata = {
  modelName: "OrderAdjustmentAllocation",
  table: "order_adjustment_allocations",

  fields: {
    order_adjustment_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "OrderAdjustment",
        table: "order_adjustments",
        onDelete: "CASCADE",
      },
      label: {},
    },
    customer_order_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrder",
        table: "customer_orders",
        onDelete: "CASCADE",
      },
      label: {},
    },
    order_item_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "CustomerOrderItem",
        table: "customer_order_items",
        onDelete: "CASCADE",
      },
      label: {},
    },
    amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "金額",
        en: "Amount",
        vi: "Số tiền",
      },
      placeholder: {
        ja: "金額を入力",
        en: "Enter Amount",
        vi: "Nhập Số tiền",
      },
    },
    tax_rate: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "税率",
        en: "Tax Rate",
        vi: "Thuế suất",
      },
      placeholder: {
        ja: "税率を入力",
        en: "Enter Tax Rate",
        vi: "Nhập Thuế suất",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["order_adjustment_id", "customer_order_id", "amount", "tax_rate"] as const,
  enumFields: [] as const,
  relationFields: ["order_adjustment_id", "customer_order_id", "order_item_id"] as const,
} as const;

export type OrderAdjustmentAllocationFieldName = keyof typeof orderAdjustmentAllocationMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a OrderAdjustmentAllocation. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface OrderAdjustmentAllocationCreateFormState {
  order_adjustment_id: string;
  customer_order_id: string;
  order_item_id?: string | null;
  amount: number;
  tax_rate: number;
}

/** Form-state shape for updating a OrderAdjustmentAllocation. Every field optional. */
export interface OrderAdjustmentAllocationUpdateFormState {
  order_adjustment_id?: string;
  customer_order_id?: string;
  order_item_id?: string | null;
  amount?: number;
  tax_rate?: number;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyOrderAdjustmentAllocationCreateForm(): OrderAdjustmentAllocationCreateFormState {
  return {
    order_adjustment_id: '',
    customer_order_id: '',
    order_item_id: '',
    amount: 0,
    tax_rate: 0,
  };
}

/** Convert create form state into the payload that baseOrderAdjustmentAllocationCreateSchema accepts. */
export function buildOrderAdjustmentAllocationCreatePayload(form: OrderAdjustmentAllocationCreateFormState): BaseOrderAdjustmentAllocationCreate {
  const payload: Record<string, unknown> = {
    order_adjustment_id: form.order_adjustment_id.trim(),
    customer_order_id: form.customer_order_id.trim(),
    order_item_id: form.order_item_id?.toString().trim() || null,
    amount: form.amount,
    tax_rate: form.tax_rate,
  };

  return payload as BaseOrderAdjustmentAllocationCreate;
}

/** Convert update form state into the payload that baseOrderAdjustmentAllocationUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildOrderAdjustmentAllocationUpdatePayload(form: OrderAdjustmentAllocationUpdateFormState): BaseOrderAdjustmentAllocationUpdate {
  const payload: Record<string, unknown> = {};
  if (form.order_adjustment_id !== undefined) {
    payload.order_adjustment_id = form.order_adjustment_id?.toString().trim() ?? null;
  }
  if (form.customer_order_id !== undefined) {
    payload.customer_order_id = form.customer_order_id?.toString().trim() ?? null;
  }
  if (form.order_item_id !== undefined) {
    payload.order_item_id = form.order_item_id?.toString().trim() ?? null;
  }
  if (form.amount !== undefined) {
    payload.amount = form.amount;
  }
  if (form.tax_rate !== undefined) {
    payload.tax_rate = form.tax_rate;
  }
  return payload as BaseOrderAdjustmentAllocationUpdate;
}
