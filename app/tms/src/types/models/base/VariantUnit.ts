/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { ProductSku } from './ProductSku';

/**
 * バリエーション単位
 */
export interface VariantUnit {
  id: string;
  product_sku_id: string;
  productSku?: ProductSku;
  unit: string;
  ratio: number;
  sku: string;
  barcode?: string | null;
  price: number;
  is_base: boolean;
  is_sellable: boolean;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for VariantUnit
 */
export const variantUnitI18n = {
  label: {"ja":"バリエーション単位","en":"Variant Unit","vi":"Đơn vị biến thể"},
  fields: {
    product_sku: {
      label: {"ja":"SKU","en":"SKU","vi":"SKU"},
      placeholder: {"ja":"SKUを入力","en":"Enter SKU","vi":"Nhập SKU"},
    },
    unit: {
      label: {"ja":"単位名","en":"Unit Name","vi":"Tên đơn vị"},
      placeholder: {"ja":"単位名を入力","en":"Enter Unit Name","vi":"Nhập Tên đơn vị"},
    },
    ratio: {
      label: {"ja":"換算率","en":"Ratio","vi":"Tỷ lệ quy đổi"},
      placeholder: {"ja":"換算率を入力","en":"Enter Ratio","vi":"Nhập Tỷ lệ quy đổi"},
    },
    sku: {
      label: {"ja":"SKU","en":"SKU","vi":"SKU"},
      placeholder: {"ja":"SKUを入力","en":"Enter SKU","vi":"Nhập SKU"},
    },
    barcode: {
      label: {"ja":"バーコード","en":"Barcode","vi":"Mã vạch"},
      placeholder: {"ja":"バーコードを入力","en":"Enter Barcode","vi":"Nhập Mã vạch"},
    },
    price: {
      label: {"ja":"定価","en":"List Price","vi":"Giá niêm yết"},
      placeholder: {"ja":"定価を入力","en":"Enter List Price","vi":"Nhập Giá niêm yết"},
    },
    is_base: {
      label: {"ja":"基本単位","en":"Base Unit","vi":"Đơn vị gốc"},
      placeholder: {"ja":"基本単位を入力","en":"Enter Base Unit","vi":"Nhập Đơn vị gốc"},
    },
    is_sellable: {
      label: {"ja":"販売可能","en":"Sellable","vi":"Có thể bán"},
      placeholder: {"ja":"販売可能を入力","en":"Enter Sellable","vi":"Nhập Có thể bán"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for VariantUnit */
export const baseVariantUnitSchemas = {
  unit: z.string().min(1).max(50),
  ratio: z.number(),
  sku: z.string().min(1).max(50),
  barcode: z.string().max(100).optional().nullable(),
  price: z.number(),
  is_base: z.boolean(),
  is_sellable: z.boolean(),
} as const;

/** Create schema for VariantUnit */
export const baseVariantUnitCreateSchema = z.object({
  unit: baseVariantUnitSchemas.unit,
  ratio: baseVariantUnitSchemas.ratio,
  sku: baseVariantUnitSchemas.sku,
  barcode: baseVariantUnitSchemas.barcode,
  price: baseVariantUnitSchemas.price,
  is_base: baseVariantUnitSchemas.is_base,
  is_sellable: baseVariantUnitSchemas.is_sellable,
});

/** Update schema for VariantUnit */
export const baseVariantUnitUpdateSchema = baseVariantUnitCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseVariantUnitCreate = z.infer<typeof baseVariantUnitCreateSchema>;
export type BaseVariantUnitUpdate = z.infer<typeof baseVariantUnitUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getVariantUnitLabel(locale: string): string {
  return variantUnitI18n.label[locale as keyof typeof variantUnitI18n.label] ?? variantUnitI18n.label['en'] ?? 'VariantUnit';
}

/** Get field label for a specific locale */
export function getVariantUnitFieldLabel(field: string, locale: string): string {
  const fields = variantUnitI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getVariantUnitFieldPlaceholder(field: string, locale: string): string {
  const fields = variantUnitI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for VariantUnit.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const variantUnitMetadata = {
  modelName: "VariantUnit",
  table: "variant_units",

  fields: {
    product_sku_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "CASCADE",
      },
      label: {},
    },
    unit: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "単位名",
        en: "Unit Name",
        vi: "Tên đơn vị",
      },
      placeholder: {
        ja: "単位名を入力",
        en: "Enter Unit Name",
        vi: "Nhập Tên đơn vị",
      },
      description: {
        ja: "例：個、缶、パック、箱",
        en: "e.g., Piece, Can, Pack, Box",
        vi: "VD: Cái, Lon, Lốc, Thùng",
      },
    },
    ratio: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "換算率",
        en: "Ratio",
        vi: "Tỷ lệ quy đổi",
      },
      placeholder: {
        ja: "換算率を入力",
        en: "Enter Ratio",
        vi: "Nhập Tỷ lệ quy đổi",
      },
      description: {
        ja: "換算率（基本単位 = 1.0）",
        en: "Conversion ratio (base unit = 1.0)",
        vi: "Tỷ lệ quy đổi (đơn vị gốc = 1.0)",
      },
    },
    sku: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "SKU",
        en: "SKU",
        vi: "SKU",
      },
      placeholder: {
        ja: "SKUを入力",
        en: "Enter SKU",
        vi: "Nhập SKU",
      },
      description: {
        ja: "この単位の一意のSKU",
        en: "Unique SKU for this unit",
        vi: "SKU riêng cho đơn vị này",
      },
    },
    barcode: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "バーコード",
        en: "Barcode",
        vi: "Mã vạch",
      },
      placeholder: {
        ja: "バーコードを入力",
        en: "Enter Barcode",
        vi: "Nhập Mã vạch",
      },
    },
    price: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "定価",
        en: "List Price",
        vi: "Giá niêm yết",
      },
      placeholder: {
        ja: "定価を入力",
        en: "Enter List Price",
        vi: "Nhập Giá niêm yết",
      },
      description: {
        ja: "この単位の定価",
        en: "List price for this unit",
        vi: "Giá niêm yết theo đơn vị này",
      },
    },
    is_base: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "基本単位",
        en: "Base Unit",
        vi: "Đơn vị gốc",
      },
      placeholder: {
        ja: "基本単位を入力",
        en: "Enter Base Unit",
        vi: "Nhập Đơn vị gốc",
      },
      description: {
        ja: "計算の基本単位の場合はtrue",
        en: "True if this is the base unit for calculation",
        vi: "True nếu đây là đơn vị gốc để tính toán",
      },
    },
    is_sellable: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "販売可能",
        en: "Sellable",
        vi: "Có thể bán",
      },
      placeholder: {
        ja: "販売可能を入力",
        en: "Enter Sellable",
        vi: "Nhập Có thể bán",
      },
      description: {
        ja: "この単位での販売を許可",
        en: "Allow selling by this unit",
        vi: "Cho phép bán theo đơn vị này",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["product_sku_id", "unit", "ratio", "sku", "price", "is_base", "is_sellable"] as const,
  enumFields: [] as const,
  relationFields: ["product_sku_id"] as const,
} as const;

export type VariantUnitFieldName = keyof typeof variantUnitMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a VariantUnit. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface VariantUnitCreateFormState {
  product_sku_id: string;
  unit: string;
  ratio: number;
  sku: string;
  barcode?: string | null;
  price: number;
  is_base: boolean;
  is_sellable: boolean;
}

/** Form-state shape for updating a VariantUnit. Every field optional. */
export interface VariantUnitUpdateFormState {
  product_sku_id?: string;
  unit?: string;
  ratio?: number;
  sku?: string;
  barcode?: string | null;
  price?: number;
  is_base?: boolean;
  is_sellable?: boolean;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyVariantUnitCreateForm(): VariantUnitCreateFormState {
  return {
    product_sku_id: '',
    unit: '',
    ratio: 1,
    sku: '',
    barcode: '',
    price: 0,
    is_base: false,
    is_sellable: true,
  };
}

/** Convert create form state into the payload that baseVariantUnitCreateSchema accepts. */
export function buildVariantUnitCreatePayload(form: VariantUnitCreateFormState): BaseVariantUnitCreate {
  const payload: Record<string, unknown> = {
    product_sku_id: form.product_sku_id.trim(),
    unit: form.unit.trim(),
    ratio: form.ratio,
    sku: form.sku.trim(),
    barcode: form.barcode?.toString().trim() || null,
    price: form.price,
    is_base: form.is_base,
    is_sellable: form.is_sellable,
  };

  return payload as BaseVariantUnitCreate;
}

/** Convert update form state into the payload that baseVariantUnitUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildVariantUnitUpdatePayload(form: VariantUnitUpdateFormState): BaseVariantUnitUpdate {
  const payload: Record<string, unknown> = {};
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.unit !== undefined) {
    payload.unit = form.unit?.toString().trim() ?? null;
  }
  if (form.ratio !== undefined) {
    payload.ratio = form.ratio;
  }
  if (form.sku !== undefined) {
    payload.sku = form.sku?.toString().trim() ?? null;
  }
  if (form.barcode !== undefined) {
    payload.barcode = form.barcode?.toString().trim() ?? null;
  }
  if (form.price !== undefined) {
    payload.price = form.price;
  }
  if (form.is_base !== undefined) {
    payload.is_base = form.is_base;
  }
  if (form.is_sellable !== undefined) {
    payload.is_sellable = form.is_sellable;
  }
  return payload as BaseVariantUnitUpdate;
}
