/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * Permission
 */
export interface Permission {
  id: string;
  name: string;
  slug: string;
  group?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for Permission
 */
export const permissionI18n = {
  label: {"ja":"Permission","en":"Permission","vi":"Permission"},
  fields: {
    name: {
      label: {"ja":"権限名","en":"Permission Name","vi":"Tên quyền"},
      placeholder: {"ja":"権限名を入力","en":"Enter Permission Name","vi":"Nhập Tên quyền"},
    },
    slug: {
      label: {"ja":"スラッグ","en":"Slug","vi":"Slug"},
      placeholder: {"ja":"スラッグを入力","en":"Enter Slug","vi":"Nhập Slug"},
    },
    group: {
      label: {"ja":"グループ","en":"Group","vi":"Nhóm"},
      placeholder: {"ja":"グループを入力","en":"Enter Group","vi":"Nhập Nhóm"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for Permission */
export const basePermissionSchemas = {
  name: z.string().min(1).max(100),
  slug: z.string().min(1).max(100),
  group: z.string().max(50).optional().nullable(),
} as const;

/** Create schema for Permission */
export const basePermissionCreateSchema = z.object({
  name: basePermissionSchemas.name,
  slug: basePermissionSchemas.slug,
  group: basePermissionSchemas.group,
});

/** Update schema for Permission */
export const basePermissionUpdateSchema = basePermissionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BasePermissionCreate = z.infer<typeof basePermissionCreateSchema>;
export type BasePermissionUpdate = z.infer<typeof basePermissionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getPermissionLabel(locale: string): string {
  return permissionI18n.label[locale as keyof typeof permissionI18n.label] ?? permissionI18n.label['en'] ?? 'Permission';
}

/** Get field label for a specific locale */
export function getPermissionFieldLabel(field: string, locale: string): string {
  const fields = permissionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getPermissionFieldPlaceholder(field: string, locale: string): string {
  const fields = permissionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for Permission.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const permissionMetadata = {
  modelName: "Permission",
  table: "permissions",

  fields: {
    name: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "権限名",
        en: "Permission Name",
        vi: "Tên quyền",
      },
      placeholder: {
        ja: "権限名を入力",
        en: "Enter Permission Name",
        vi: "Nhập Tên quyền",
      },
    },
    slug: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "スラッグ",
        en: "Slug",
        vi: "Slug",
      },
      placeholder: {
        ja: "スラッグを入力",
        en: "Enter Slug",
        vi: "Nhập Slug",
      },
    },
    group: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 50,
      },
      label: {
        ja: "グループ",
        en: "Group",
        vi: "Nhóm",
      },
      placeholder: {
        ja: "グループを入力",
        en: "Enter Group",
        vi: "Nhập Nhóm",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["name", "slug"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type PermissionFieldName = keyof typeof permissionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a Permission. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface PermissionCreateFormState {
  name: string;
  slug: string;
  group?: string | null;
}

/** Form-state shape for updating a Permission. Every field optional. */
export interface PermissionUpdateFormState {
  name?: string;
  slug?: string;
  group?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyPermissionCreateForm(): PermissionCreateFormState {
  return {
    name: '',
    slug: '',
    group: '',
  };
}

/** Convert create form state into the payload that basePermissionCreateSchema accepts. */
export function buildPermissionCreatePayload(form: PermissionCreateFormState): BasePermissionCreate {
  const payload: Record<string, unknown> = {
    name: form.name.trim(),
    slug: form.slug.trim(),
    group: form.group?.toString().trim() || null,
  };

  return payload as BasePermissionCreate;
}

/** Convert update form state into the payload that basePermissionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildPermissionUpdatePayload(form: PermissionUpdateFormState): BasePermissionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.name !== undefined) {
    payload.name = form.name?.toString().trim() ?? null;
  }
  if (form.slug !== undefined) {
    payload.slug = form.slug?.toString().trim() ?? null;
  }
  if (form.group !== undefined) {
    payload.group = form.group?.toString().trim() ?? null;
  }
  return payload as BasePermissionUpdate;
}
