/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * 注文条件
 */
export interface OrderCondition {
  id: string;
  conditionable_type: string;
  conditionable_id: string;
  type: string;
  source?: string | null;
  label: string;
  rate?: number | null;
  amount: number;
  taxable_base?: number | null;
  currency_code: string;
  meta?: unknown | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for OrderCondition
 */
export const orderConditionI18n = {
  label: {"ja":"注文条件","en":"Order Condition","vi":"Điều kiện đơn hàng"},
  fields: {
    conditionable_type: {
      label: {"ja":"対象タイプ","en":"Conditionable Type","vi":"Loại đối tượng"},
      placeholder: {"ja":"対象タイプを入力","en":"Enter Conditionable Type","vi":"Nhập Loại đối tượng"},
    },
    conditionable_id: {
      label: {"ja":"対象ID","en":"Conditionable ID","vi":"ID đối tượng"},
      placeholder: {"ja":"対象IDを入力","en":"Enter Conditionable ID","vi":"Nhập ID đối tượng"},
    },
    type: {
      label: {"ja":"種別","en":"Type","vi":"Loại"},
      placeholder: {"ja":"種別を入力","en":"Enter Type","vi":"Nhập Loại"},
    },
    source: {
      label: {"ja":"発生源","en":"Source","vi":"Nguồn"},
      placeholder: {"ja":"発生源を入力","en":"Enter Source","vi":"Nhập Nguồn"},
    },
    label: {
      label: {"ja":"ラベル","en":"Label","vi":"Nhãn"},
      placeholder: {"ja":"ラベルを入力","en":"Enter Label","vi":"Nhập Nhãn"},
    },
    rate: {
      label: {"ja":"率 (%)","en":"Rate (%)","vi":"Tỷ lệ (%)"},
      placeholder: {"ja":"率 (%)を入力","en":"Enter Rate (%)","vi":"Nhập Tỷ lệ (%)"},
    },
    amount: {
      label: {"ja":"金額","en":"Amount","vi":"Số tiền"},
      placeholder: {"ja":"金額を入力","en":"Enter Amount","vi":"Nhập Số tiền"},
    },
    taxable_base: {
      label: {"ja":"課税対象額","en":"Taxable Base","vi":"Nền chịu thuế"},
      placeholder: {"ja":"課税対象額を入力","en":"Enter Taxable Base","vi":"Nhập Nền chịu thuế"},
    },
    currency_code: {
      label: {"ja":"通貨","en":"Currency","vi":"Tiền tệ"},
      placeholder: {"ja":"通貨を入力","en":"Enter Currency","vi":"Nhập Tiền tệ"},
    },
    meta: {
      label: {"ja":"メタ情報","en":"Meta","vi":"Thông tin thêm"},
      placeholder: {"ja":"メタ情報を入力","en":"Enter Meta","vi":"Nhập Thông tin thêm"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for OrderCondition */
export const baseOrderConditionSchemas = {
  conditionable_type: z.string().min(1).max(20),
  conditionable_id: z.string(),
  type: z.string().min(1).max(20),
  source: z.string().max(30).optional().nullable(),
  label: z.string().min(1).max(100),
  rate: z.number().optional().nullable(),
  amount: z.number(),
  taxable_base: z.number().optional().nullable(),
  currency_code: z.string().min(1).max(8),
  meta: z.unknown().optional().nullable(),
} as const;

/** Create schema for OrderCondition */
export const baseOrderConditionCreateSchema = z.object({
  conditionable_type: baseOrderConditionSchemas.conditionable_type,
  conditionable_id: baseOrderConditionSchemas.conditionable_id,
  type: baseOrderConditionSchemas.type,
  source: baseOrderConditionSchemas.source,
  label: baseOrderConditionSchemas.label,
  rate: baseOrderConditionSchemas.rate,
  amount: baseOrderConditionSchemas.amount,
  taxable_base: baseOrderConditionSchemas.taxable_base,
  currency_code: baseOrderConditionSchemas.currency_code,
  meta: baseOrderConditionSchemas.meta,
});

/** Update schema for OrderCondition */
export const baseOrderConditionUpdateSchema = baseOrderConditionCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseOrderConditionCreate = z.infer<typeof baseOrderConditionCreateSchema>;
export type BaseOrderConditionUpdate = z.infer<typeof baseOrderConditionUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getOrderConditionLabel(locale: string): string {
  return orderConditionI18n.label[locale as keyof typeof orderConditionI18n.label] ?? orderConditionI18n.label['en'] ?? 'OrderCondition';
}

/** Get field label for a specific locale */
export function getOrderConditionFieldLabel(field: string, locale: string): string {
  const fields = orderConditionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getOrderConditionFieldPlaceholder(field: string, locale: string): string {
  const fields = orderConditionI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for OrderCondition.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const orderConditionMetadata = {
  modelName: "OrderCondition",
  table: "order_conditions",

  fields: {
    conditionable_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "対象タイプ",
        en: "Conditionable Type",
        vi: "Loại đối tượng",
      },
      placeholder: {
        ja: "対象タイプを入力",
        en: "Enter Conditionable Type",
        vi: "Nhập Loại đối tượng",
      },
    },
    conditionable_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "対象ID",
        en: "Conditionable ID",
        vi: "ID đối tượng",
      },
      placeholder: {
        ja: "対象IDを入力",
        en: "Enter Conditionable ID",
        vi: "Nhập ID đối tượng",
      },
    },
    type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 20,
      },
      label: {
        ja: "種別",
        en: "Type",
        vi: "Loại",
      },
      placeholder: {
        ja: "種別を入力",
        en: "Enter Type",
        vi: "Nhập Loại",
      },
    },
    source: {
      type: "string",
      required: false,
      nullable: true,
      translatable: false,
      validation: {
        maxLength: 30,
      },
      label: {
        ja: "発生源",
        en: "Source",
        vi: "Nguồn",
      },
      placeholder: {
        ja: "発生源を入力",
        en: "Enter Source",
        vi: "Nhập Nguồn",
      },
    },
    label: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 100,
      },
      label: {
        ja: "ラベル",
        en: "Label",
        vi: "Nhãn",
      },
      placeholder: {
        ja: "ラベルを入力",
        en: "Enter Label",
        vi: "Nhập Nhãn",
      },
    },
    rate: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "率 (%)",
        en: "Rate (%)",
        vi: "Tỷ lệ (%)",
      },
      placeholder: {
        ja: "率 (%)を入力",
        en: "Enter Rate (%)",
        vi: "Nhập Tỷ lệ (%)",
      },
    },
    amount: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "金額",
        en: "Amount",
        vi: "Số tiền",
      },
      placeholder: {
        ja: "金額を入力",
        en: "Enter Amount",
        vi: "Nhập Số tiền",
      },
    },
    taxable_base: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "課税対象額",
        en: "Taxable Base",
        vi: "Nền chịu thuế",
      },
      placeholder: {
        ja: "課税対象額を入力",
        en: "Enter Taxable Base",
        vi: "Nhập Nền chịu thuế",
      },
    },
    currency_code: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 8,
      },
      label: {
        ja: "通貨",
        en: "Currency",
        vi: "Tiền tệ",
      },
      placeholder: {
        ja: "通貨を入力",
        en: "Enter Currency",
        vi: "Nhập Tiền tệ",
      },
    },
    meta: {
      type: "json",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "メタ情報",
        en: "Meta",
        vi: "Thông tin thêm",
      },
      placeholder: {
        ja: "メタ情報を入力",
        en: "Enter Meta",
        vi: "Nhập Thông tin thêm",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["conditionable_type", "conditionable_id", "type", "label", "amount", "currency_code"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type OrderConditionFieldName = keyof typeof orderConditionMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a OrderCondition. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface OrderConditionCreateFormState {
  conditionable_type: string;
  conditionable_id: string;
  type: string;
  source?: string | null;
  label: string;
  rate?: number | null;
  amount: number;
  taxable_base?: number | null;
  currency_code: string;
  meta?: unknown | null;
}

/** Form-state shape for updating a OrderCondition. Every field optional. */
export interface OrderConditionUpdateFormState {
  conditionable_type?: string;
  conditionable_id?: string;
  type?: string;
  source?: string | null;
  label?: string;
  rate?: number | null;
  amount?: number;
  taxable_base?: number | null;
  currency_code?: string;
  meta?: unknown | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyOrderConditionCreateForm(): OrderConditionCreateFormState {
  return {
    conditionable_type: '',
    conditionable_id: '',
    type: '',
    source: '',
    label: '',
    rate: 0,
    amount: 0,
    taxable_base: 0,
    currency_code: '',
    meta: null,
  };
}

/** Convert create form state into the payload that baseOrderConditionCreateSchema accepts. */
export function buildOrderConditionCreatePayload(form: OrderConditionCreateFormState): BaseOrderConditionCreate {
  const payload: Record<string, unknown> = {
    conditionable_type: form.conditionable_type.trim(),
    conditionable_id: form.conditionable_id.trim(),
    type: form.type.trim(),
    source: form.source?.toString().trim() || null,
    label: form.label.trim(),
    rate: form.rate,
    amount: form.amount,
    taxable_base: form.taxable_base,
    currency_code: form.currency_code.trim(),
    meta: form.meta,
  };

  return payload as BaseOrderConditionCreate;
}

/** Convert update form state into the payload that baseOrderConditionUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildOrderConditionUpdatePayload(form: OrderConditionUpdateFormState): BaseOrderConditionUpdate {
  const payload: Record<string, unknown> = {};
  if (form.conditionable_type !== undefined) {
    payload.conditionable_type = form.conditionable_type?.toString().trim() ?? null;
  }
  if (form.conditionable_id !== undefined) {
    payload.conditionable_id = form.conditionable_id?.toString().trim() ?? null;
  }
  if (form.type !== undefined) {
    payload.type = form.type?.toString().trim() ?? null;
  }
  if (form.source !== undefined) {
    payload.source = form.source?.toString().trim() ?? null;
  }
  if (form.label !== undefined) {
    payload.label = form.label?.toString().trim() ?? null;
  }
  if (form.rate !== undefined) {
    payload.rate = form.rate;
  }
  if (form.amount !== undefined) {
    payload.amount = form.amount;
  }
  if (form.taxable_base !== undefined) {
    payload.taxable_base = form.taxable_base;
  }
  if (form.currency_code !== undefined) {
    payload.currency_code = form.currency_code?.toString().trim() ?? null;
  }
  if (form.meta !== undefined) {
    payload.meta = form.meta;
  }
  return payload as BaseOrderConditionUpdate;
}
