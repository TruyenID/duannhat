/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';

/**
 * アイデンティティ受信箱
 */
export interface IdentityInboxEntry {
  id: string;
  event_id: string;
  resource_type: string;
  resource_id: string;
  event_type: string;
  sequence: number;
  payload: unknown;
  occurred_at?: DateTimeString | null;
  received_at: DateTimeString;
  applied_at?: DateTimeString | null;
  apply_error?: string | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for IdentityInboxEntry
 */
export const identityInboxEntryI18n = {
  label: {"ja":"アイデンティティ受信箱","en":"Identity Inbox Entry","vi":"Sổ nhận danh tính"},
  fields: {
    event_id: {
      label: {"ja":"イベントID","en":"Event ID","vi":"ID sự kiện"},
      placeholder: {"ja":"イベントIDを入力","en":"Enter Event ID","vi":"Nhập ID sự kiện"},
    },
    resource_type: {
      label: {"ja":"リソース種別","en":"Resource Type","vi":"Loại tài nguyên"},
      placeholder: {"ja":"リソース種別を入力","en":"Enter Resource Type","vi":"Nhập Loại tài nguyên"},
    },
    resource_id: {
      label: {"ja":"リソースID","en":"Resource ID","vi":"ID tài nguyên"},
      placeholder: {"ja":"リソースIDを入力","en":"Enter Resource ID","vi":"Nhập ID tài nguyên"},
    },
    event_type: {
      label: {"ja":"イベント種別","en":"Event Type","vi":"Loại sự kiện"},
      placeholder: {"ja":"イベント種別を入力","en":"Enter Event Type","vi":"Nhập Loại sự kiện"},
    },
    sequence: {
      label: {"ja":"シーケンス","en":"Sequence","vi":"Số thứ tự"},
      placeholder: {"ja":"シーケンスを入力","en":"Enter Sequence","vi":"Nhập Số thứ tự"},
    },
    payload: {
      label: {"ja":"ペイロード","en":"Payload","vi":"Nội dung"},
      placeholder: {"ja":"ペイロードを入力","en":"Enter Payload","vi":"Nhập Nội dung"},
    },
    occurred_at: {
      label: {"ja":"発生日時","en":"Occurred At","vi":"Thời điểm phát sinh"},
      placeholder: {"ja":"発生日時を入力","en":"Enter Occurred At","vi":"Nhập Thời điểm phát sinh"},
    },
    received_at: {
      label: {"ja":"受信日時","en":"Received At","vi":"Thời điểm nhận"},
      placeholder: {"ja":"受信日時を入力","en":"Enter Received At","vi":"Nhập Thời điểm nhận"},
    },
    applied_at: {
      label: {"ja":"適用日時","en":"Applied At","vi":"Thời điểm áp dụng"},
      placeholder: {"ja":"適用日時を入力","en":"Enter Applied At","vi":"Nhập Thời điểm áp dụng"},
    },
    apply_error: {
      label: {"ja":"適用エラー","en":"Apply Error","vi":"Lỗi khi áp dụng"},
      placeholder: {"ja":"適用エラーを入力","en":"Enter Apply Error","vi":"Nhập Lỗi khi áp dụng"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for IdentityInboxEntry */
export const baseIdentityInboxEntrySchemas = {
  event_id: z.string(),
  resource_type: z.string().min(1).max(64),
  resource_id: z.string().min(1).max(64),
  event_type: z.string().min(1).max(128),
  sequence: z.number().int(),
  payload: z.unknown(),
  occurred_at: z.string().datetime({ offset: true }).optional().nullable(),
  received_at: z.string().datetime({ offset: true }),
  applied_at: z.string().datetime({ offset: true }).optional().nullable(),
  apply_error: z.string().optional().nullable(),
} as const;

/** Create schema for IdentityInboxEntry */
export const baseIdentityInboxEntryCreateSchema = z.object({
  event_id: baseIdentityInboxEntrySchemas.event_id,
  resource_type: baseIdentityInboxEntrySchemas.resource_type,
  resource_id: baseIdentityInboxEntrySchemas.resource_id,
  event_type: baseIdentityInboxEntrySchemas.event_type,
  sequence: baseIdentityInboxEntrySchemas.sequence,
  payload: baseIdentityInboxEntrySchemas.payload,
  occurred_at: baseIdentityInboxEntrySchemas.occurred_at,
  received_at: baseIdentityInboxEntrySchemas.received_at,
  applied_at: baseIdentityInboxEntrySchemas.applied_at,
  apply_error: baseIdentityInboxEntrySchemas.apply_error,
});

/** Update schema for IdentityInboxEntry */
export const baseIdentityInboxEntryUpdateSchema = baseIdentityInboxEntryCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseIdentityInboxEntryCreate = z.infer<typeof baseIdentityInboxEntryCreateSchema>;
export type BaseIdentityInboxEntryUpdate = z.infer<typeof baseIdentityInboxEntryUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getIdentityInboxEntryLabel(locale: string): string {
  return identityInboxEntryI18n.label[locale as keyof typeof identityInboxEntryI18n.label] ?? identityInboxEntryI18n.label['en'] ?? 'IdentityInboxEntry';
}

/** Get field label for a specific locale */
export function getIdentityInboxEntryFieldLabel(field: string, locale: string): string {
  const fields = identityInboxEntryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getIdentityInboxEntryFieldPlaceholder(field: string, locale: string): string {
  const fields = identityInboxEntryI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for IdentityInboxEntry.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const identityInboxEntryMetadata = {
  modelName: "IdentityInboxEntry",
  table: "identity_inbox",

  fields: {
    event_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "イベントID",
        en: "Event ID",
        vi: "ID sự kiện",
      },
      placeholder: {
        ja: "イベントIDを入力",
        en: "Enter Event ID",
        vi: "Nhập ID sự kiện",
      },
    },
    resource_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "リソース種別",
        en: "Resource Type",
        vi: "Loại tài nguyên",
      },
      placeholder: {
        ja: "リソース種別を入力",
        en: "Enter Resource Type",
        vi: "Nhập Loại tài nguyên",
      },
    },
    resource_id: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "リソースID",
        en: "Resource ID",
        vi: "ID tài nguyên",
      },
      placeholder: {
        ja: "リソースIDを入力",
        en: "Enter Resource ID",
        vi: "Nhập ID tài nguyên",
      },
    },
    event_type: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 128,
      },
      label: {
        ja: "イベント種別",
        en: "Event Type",
        vi: "Loại sự kiện",
      },
      placeholder: {
        ja: "イベント種別を入力",
        en: "Enter Event Type",
        vi: "Nhập Loại sự kiện",
      },
    },
    sequence: {
      type: "number",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "シーケンス",
        en: "Sequence",
        vi: "Số thứ tự",
      },
      placeholder: {
        ja: "シーケンスを入力",
        en: "Enter Sequence",
        vi: "Nhập Số thứ tự",
      },
    },
    payload: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "ペイロード",
        en: "Payload",
        vi: "Nội dung",
      },
      placeholder: {
        ja: "ペイロードを入力",
        en: "Enter Payload",
        vi: "Nhập Nội dung",
      },
    },
    occurred_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "発生日時",
        en: "Occurred At",
        vi: "Thời điểm phát sinh",
      },
      placeholder: {
        ja: "発生日時を入力",
        en: "Enter Occurred At",
        vi: "Nhập Thời điểm phát sinh",
      },
    },
    received_at: {
      type: "datetime",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "受信日時",
        en: "Received At",
        vi: "Thời điểm nhận",
      },
      placeholder: {
        ja: "受信日時を入力",
        en: "Enter Received At",
        vi: "Nhập Thời điểm nhận",
      },
    },
    applied_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "適用日時",
        en: "Applied At",
        vi: "Thời điểm áp dụng",
      },
      placeholder: {
        ja: "適用日時を入力",
        en: "Enter Applied At",
        vi: "Nhập Thời điểm áp dụng",
      },
    },
    apply_error: {
      type: "text",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "適用エラー",
        en: "Apply Error",
        vi: "Lỗi khi áp dụng",
      },
      placeholder: {
        ja: "適用エラーを入力",
        en: "Enter Apply Error",
        vi: "Nhập Lỗi khi áp dụng",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["event_id", "resource_type", "resource_id", "event_type", "sequence", "payload", "received_at"] as const,
  enumFields: [] as const,
  relationFields: [] as const,
} as const;

export type IdentityInboxEntryFieldName = keyof typeof identityInboxEntryMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a IdentityInboxEntry. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface IdentityInboxEntryCreateFormState {
  event_id: string;
  resource_type: string;
  resource_id: string;
  event_type: string;
  sequence: number;
  payload: unknown;
  occurred_at?: string | null;
  received_at: string;
  applied_at?: string | null;
  apply_error?: string | null;
}

/** Form-state shape for updating a IdentityInboxEntry. Every field optional. */
export interface IdentityInboxEntryUpdateFormState {
  event_id?: string;
  resource_type?: string;
  resource_id?: string;
  event_type?: string;
  sequence?: number;
  payload?: unknown;
  occurred_at?: string | null;
  received_at?: string;
  applied_at?: string | null;
  apply_error?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyIdentityInboxEntryCreateForm(): IdentityInboxEntryCreateFormState {
  return {
    event_id: '',
    resource_type: '',
    resource_id: '',
    event_type: '',
    sequence: 0,
    payload: null,
    occurred_at: '',
    received_at: '',
    applied_at: '',
    apply_error: '',
  };
}

/** Convert create form state into the payload that baseIdentityInboxEntryCreateSchema accepts. */
export function buildIdentityInboxEntryCreatePayload(form: IdentityInboxEntryCreateFormState): BaseIdentityInboxEntryCreate {
  const payload: Record<string, unknown> = {
    event_id: form.event_id.trim(),
    resource_type: form.resource_type.trim(),
    resource_id: form.resource_id.trim(),
    event_type: form.event_type.trim(),
    sequence: form.sequence,
    payload: form.payload,
    occurred_at: form.occurred_at,
    received_at: form.received_at,
    applied_at: form.applied_at,
    apply_error: form.apply_error?.toString().trim() || null,
  };

  return payload as BaseIdentityInboxEntryCreate;
}

/** Convert update form state into the payload that baseIdentityInboxEntryUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildIdentityInboxEntryUpdatePayload(form: IdentityInboxEntryUpdateFormState): BaseIdentityInboxEntryUpdate {
  const payload: Record<string, unknown> = {};
  if (form.event_id !== undefined) {
    payload.event_id = form.event_id?.toString().trim() ?? null;
  }
  if (form.resource_type !== undefined) {
    payload.resource_type = form.resource_type?.toString().trim() ?? null;
  }
  if (form.resource_id !== undefined) {
    payload.resource_id = form.resource_id?.toString().trim() ?? null;
  }
  if (form.event_type !== undefined) {
    payload.event_type = form.event_type?.toString().trim() ?? null;
  }
  if (form.sequence !== undefined) {
    payload.sequence = form.sequence;
  }
  if (form.payload !== undefined) {
    payload.payload = form.payload;
  }
  if (form.occurred_at !== undefined) {
    payload.occurred_at = form.occurred_at;
  }
  if (form.received_at !== undefined) {
    payload.received_at = form.received_at;
  }
  if (form.applied_at !== undefined) {
    payload.applied_at = form.applied_at;
  }
  if (form.apply_error !== undefined) {
    payload.apply_error = form.apply_error?.toString().trim() ?? null;
  }
  return payload as BaseIdentityInboxEntryUpdate;
}
