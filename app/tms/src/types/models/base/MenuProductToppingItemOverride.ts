/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { MenuProduct } from './MenuProduct';
import type { ProductSku } from './ProductSku';
import type { ToppingGroup } from './ToppingGroup';
import type { ToppingGroupItem } from './ToppingGroupItem';

/**
 * メニュー商品トッピング項目上書き
 */
export interface MenuProductToppingItemOverride {
  id: string;
  menu_product_id: string;
  menuProduct?: MenuProduct;
  topping_group_id: string;
  toppingGroup?: ToppingGroup;
  topping_group_item_id: string;
  toppingGroupItem?: ToppingGroupItem;
  product_sku_id?: string | null;
  productSku?: ProductSku | null;
  is_hidden: boolean;
  override_price?: number | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for MenuProductToppingItemOverride
 */
export const menuProductToppingItemOverrideI18n = {
  label: {"ja":"メニュー商品トッピング項目上書き","en":"Menu Product Topping Item Override","vi":"Ghi đè Topping cấp Branch"},
  fields: {
    menu_product: {
      label: {"ja":"メニュー商品","en":"Menu Product","vi":"Sản phẩm trong Menu"},
      placeholder: {"ja":"メニュー商品を入力","en":"Enter Menu Product","vi":"Nhập Sản phẩm trong Menu"},
    },
    topping_group: {
      label: {"ja":"トッピンググループ","en":"Topping Group","vi":"Nhóm Topping"},
      placeholder: {"ja":"トッピンググループを入力","en":"Enter Topping Group","vi":"Nhập Nhóm Topping"},
    },
    topping_group_item: {
      label: {"ja":"トッピンググループ項目","en":"Topping Group Item","vi":"Mục Topping"},
      placeholder: {"ja":"トッピンググループ項目を入力","en":"Enter Topping Group Item","vi":"Nhập Mục Topping"},
    },
    product_sku: {
      label: {"ja":"商品SKU","en":"Product SKU","vi":"SKU sản phẩm"},
      placeholder: {"ja":"商品SKUを入力","en":"Enter Product SKU","vi":"Nhập SKU sản phẩm"},
    },
    is_hidden: {
      label: {"ja":"非表示","en":"Is Hidden","vi":"Ẩn"},
      placeholder: {"ja":"非表示を入力","en":"Enter Is Hidden","vi":"Nhập Ẩn"},
    },
    override_price: {
      label: {"ja":"上書き価格","en":"Override Price","vi":"Giá ghi đè"},
      placeholder: {"ja":"上書き価格を入力","en":"Enter Override Price","vi":"Nhập Giá ghi đè"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for MenuProductToppingItemOverride */
export const baseMenuProductToppingItemOverrideSchemas = {
  is_hidden: z.boolean(),
  override_price: z.number().optional().nullable(),
} as const;

/** Create schema for MenuProductToppingItemOverride */
export const baseMenuProductToppingItemOverrideCreateSchema = z.object({
  is_hidden: baseMenuProductToppingItemOverrideSchemas.is_hidden,
  override_price: baseMenuProductToppingItemOverrideSchemas.override_price,
});

/** Update schema for MenuProductToppingItemOverride */
export const baseMenuProductToppingItemOverrideUpdateSchema = baseMenuProductToppingItemOverrideCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseMenuProductToppingItemOverrideCreate = z.infer<typeof baseMenuProductToppingItemOverrideCreateSchema>;
export type BaseMenuProductToppingItemOverrideUpdate = z.infer<typeof baseMenuProductToppingItemOverrideUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getMenuProductToppingItemOverrideLabel(locale: string): string {
  return menuProductToppingItemOverrideI18n.label[locale as keyof typeof menuProductToppingItemOverrideI18n.label] ?? menuProductToppingItemOverrideI18n.label['en'] ?? 'MenuProductToppingItemOverride';
}

/** Get field label for a specific locale */
export function getMenuProductToppingItemOverrideFieldLabel(field: string, locale: string): string {
  const fields = menuProductToppingItemOverrideI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getMenuProductToppingItemOverrideFieldPlaceholder(field: string, locale: string): string {
  const fields = menuProductToppingItemOverrideI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for MenuProductToppingItemOverride.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const menuProductToppingItemOverrideMetadata = {
  modelName: "MenuProductToppingItemOverride",
  table: "menu_product_topping_item_overrides",

  fields: {
    menu_product_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "MenuProduct",
        table: "menu_products",
        onDelete: "CASCADE",
      },
      label: {},
    },
    topping_group_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ToppingGroup",
        table: "topping_groups",
        onDelete: "CASCADE",
      },
      label: {},
    },
    topping_group_item_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "ToppingGroupItem",
        table: "topping_group_items",
        onDelete: "CASCADE",
      },
      label: {},
    },
    product_sku_id: {
      type: "uuid",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      relation: {
        model: "ProductSku",
        table: "product_skus",
        onDelete: "CASCADE",
      },
      label: {},
    },
    is_hidden: {
      type: "boolean",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "非表示",
        en: "Is Hidden",
        vi: "Ẩn",
      },
      placeholder: {
        ja: "非表示を入力",
        en: "Enter Is Hidden",
        vi: "Nhập Ẩn",
      },
      description: {
        ja: "ブランチレベルでこのメニュー商品のトッピング項目を非表示にする",
        en: "Hide this topping item for this menu product at branch level",
        vi: "Ẩn topping item này với menu product này ở cấp branch",
      },
    },
    override_price: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "上書き価格",
        en: "Override Price",
        vi: "Giá ghi đè",
      },
      placeholder: {
        ja: "上書き価格を入力",
        en: "Enter Override Price",
        vi: "Nhập Giá ghi đè",
      },
      description: {
        ja: "ブランチの上書き追加価格。Null = HQ上書きまたはtopping_group_item_skusから継承",
        en: "Branch override extra price. Null = inherit from HQ override or topping_group_item_skus",
        vi: "Giá extra ghi đè cấp branch. Null = kế thừa từ HQ override hoặc topping_group_item_skus",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["menu_product_id", "topping_group_id", "topping_group_item_id", "is_hidden"] as const,
  enumFields: [] as const,
  relationFields: ["menu_product_id", "topping_group_id", "topping_group_item_id", "product_sku_id"] as const,
} as const;

export type MenuProductToppingItemOverrideFieldName = keyof typeof menuProductToppingItemOverrideMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a MenuProductToppingItemOverride. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface MenuProductToppingItemOverrideCreateFormState {
  menu_product_id: string;
  topping_group_id: string;
  topping_group_item_id: string;
  product_sku_id?: string | null;
  is_hidden: boolean;
  override_price?: number | null;
}

/** Form-state shape for updating a MenuProductToppingItemOverride. Every field optional. */
export interface MenuProductToppingItemOverrideUpdateFormState {
  menu_product_id?: string;
  topping_group_id?: string;
  topping_group_item_id?: string;
  product_sku_id?: string | null;
  is_hidden?: boolean;
  override_price?: number | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyMenuProductToppingItemOverrideCreateForm(): MenuProductToppingItemOverrideCreateFormState {
  return {
    menu_product_id: '',
    topping_group_id: '',
    topping_group_item_id: '',
    product_sku_id: '',
    is_hidden: false,
    override_price: 0,
  };
}

/** Convert create form state into the payload that baseMenuProductToppingItemOverrideCreateSchema accepts. */
export function buildMenuProductToppingItemOverrideCreatePayload(form: MenuProductToppingItemOverrideCreateFormState): BaseMenuProductToppingItemOverrideCreate {
  const payload: Record<string, unknown> = {
    menu_product_id: form.menu_product_id.trim(),
    topping_group_id: form.topping_group_id.trim(),
    topping_group_item_id: form.topping_group_item_id.trim(),
    product_sku_id: form.product_sku_id?.toString().trim() || null,
    is_hidden: form.is_hidden,
    override_price: form.override_price,
  };

  return payload as BaseMenuProductToppingItemOverrideCreate;
}

/** Convert update form state into the payload that baseMenuProductToppingItemOverrideUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildMenuProductToppingItemOverrideUpdatePayload(form: MenuProductToppingItemOverrideUpdateFormState): BaseMenuProductToppingItemOverrideUpdate {
  const payload: Record<string, unknown> = {};
  if (form.menu_product_id !== undefined) {
    payload.menu_product_id = form.menu_product_id?.toString().trim() ?? null;
  }
  if (form.topping_group_id !== undefined) {
    payload.topping_group_id = form.topping_group_id?.toString().trim() ?? null;
  }
  if (form.topping_group_item_id !== undefined) {
    payload.topping_group_item_id = form.topping_group_item_id?.toString().trim() ?? null;
  }
  if (form.product_sku_id !== undefined) {
    payload.product_sku_id = form.product_sku_id?.toString().trim() ?? null;
  }
  if (form.is_hidden !== undefined) {
    payload.is_hidden = form.is_hidden;
  }
  if (form.override_price !== undefined) {
    payload.override_price = form.override_price;
  }
  return payload as BaseMenuProductToppingItemOverrideUpdate;
}
