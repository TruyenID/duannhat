/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

import { z } from 'zod';
import type { DateTimeString } from '../common';
import type { User } from './User';

/**
 * 通知ダイジェスト設定
 */
export interface NotificationDigestPreference {
  id: string;
  user_id: string;
  user?: User;
  cadence: string;
  delivery_time: string;
  timezone: string;
  weekday?: number | null;
  include_priorities: unknown;
  last_sent_at?: DateTimeString | null;
  created_at?: DateTimeString;
  updated_at?: DateTimeString;
}

// ============================================================================
// I18n (Internationalization)
// ============================================================================

/**
 * Unified i18n object for NotificationDigestPreference
 */
export const notificationDigestPreferenceI18n = {
  label: {"ja":"通知ダイジェスト設定","en":"Notification Digest Preference","vi":"Tuỳ chọn digest thông báo"},
  fields: {
    user: {
      label: {"ja":"ユーザー","en":"User","vi":"Người dùng"},
      placeholder: {"ja":"ユーザーを入力","en":"Enter User","vi":"Nhập Người dùng"},
    },
    cadence: {
      label: {"ja":"頻度","en":"Cadence","vi":"Tần suất"},
      placeholder: {"ja":"頻度を入力","en":"Enter Cadence","vi":"Nhập Tần suất"},
    },
    delivery_time: {
      label: {"ja":"配信時刻","en":"Delivery Time","vi":"Thời điểm gửi"},
      placeholder: {"ja":"配信時刻を入力","en":"Enter Delivery Time","vi":"Nhập Thời điểm gửi"},
    },
    timezone: {
      label: {"ja":"タイムゾーン","en":"Timezone","vi":"Múi giờ"},
      placeholder: {"ja":"タイムゾーンを入力","en":"Enter Timezone","vi":"Nhập Múi giờ"},
    },
    weekday: {
      label: {"ja":"曜日","en":"Weekday","vi":"Thứ trong tuần"},
      placeholder: {"ja":"曜日を入力","en":"Enter Weekday","vi":"Nhập Thứ trong tuần"},
    },
    include_priorities: {
      label: {"ja":"含める優先度","en":"Include Priorities","vi":"Bao gồm priority"},
      placeholder: {"ja":"含める優先度を入力","en":"Enter Include Priorities","vi":"Nhập Bao gồm priority"},
    },
    last_sent_at: {
      label: {"ja":"最終送信日時","en":"Last Sent At","vi":"Lần gửi cuối"},
      placeholder: {"ja":"最終送信日時を入力","en":"Enter Last Sent At","vi":"Nhập Lần gửi cuối"},
    },
  },
} as const;

// ============================================================================
// Zod Schemas
// ============================================================================

/** Field schemas for NotificationDigestPreference */
export const baseNotificationDigestPreferenceSchemas = {
  cadence: z.string().min(1).max(10),
  delivery_time: z.string().min(1).max(5),
  timezone: z.string().min(1).max(64),
  weekday: z.number().int().optional().nullable(),
  include_priorities: z.unknown(),
  last_sent_at: z.string().datetime({ offset: true }).optional().nullable(),
} as const;

/** Create schema for NotificationDigestPreference */
export const baseNotificationDigestPreferenceCreateSchema = z.object({
  cadence: baseNotificationDigestPreferenceSchemas.cadence,
  delivery_time: baseNotificationDigestPreferenceSchemas.delivery_time,
  timezone: baseNotificationDigestPreferenceSchemas.timezone,
  weekday: baseNotificationDigestPreferenceSchemas.weekday,
  include_priorities: baseNotificationDigestPreferenceSchemas.include_priorities,
  last_sent_at: baseNotificationDigestPreferenceSchemas.last_sent_at,
});

/** Update schema for NotificationDigestPreference */
export const baseNotificationDigestPreferenceUpdateSchema = baseNotificationDigestPreferenceCreateSchema.partial();

// ============================================================================
// Inferred Types
// ============================================================================

export type BaseNotificationDigestPreferenceCreate = z.infer<typeof baseNotificationDigestPreferenceCreateSchema>;
export type BaseNotificationDigestPreferenceUpdate = z.infer<typeof baseNotificationDigestPreferenceUpdateSchema>;

// ============================================================================
// I18n Helper Functions
// ============================================================================

/** Get model label for a specific locale */
export function getNotificationDigestPreferenceLabel(locale: string): string {
  return notificationDigestPreferenceI18n.label[locale as keyof typeof notificationDigestPreferenceI18n.label] ?? notificationDigestPreferenceI18n.label['en'] ?? 'NotificationDigestPreference';
}

/** Get field label for a specific locale */
export function getNotificationDigestPreferenceFieldLabel(field: string, locale: string): string {
  const fields = notificationDigestPreferenceI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n) return field;
  return fieldI18n.label[locale] ?? fieldI18n.label['en'] ?? field;
}

/** Get field placeholder for a specific locale */
export function getNotificationDigestPreferenceFieldPlaceholder(field: string, locale: string): string {
  const fields = notificationDigestPreferenceI18n.fields as Record<string, { label: Record<string, string>; placeholder?: Record<string, string> }>;
  const fieldI18n = fields[field];
  if (!fieldI18n?.placeholder) return '';
  return fieldI18n.placeholder[locale] ?? fieldI18n.placeholder['en'] ?? '';
}
// ============================================================================
// Form Field Metadata (issue #54)
// ============================================================================

/**
 * Form field metadata for NotificationDigestPreference.
 *
 * Single source of truth for downstream tools (form runners, stories,
 * smoke tests, headless validation). Includes per-field type taxonomy,
 * validation rules, translatable / required / nullable flags, enum value
 * lists, relation pointers, and i18n labels — all flat, all in one const.
 */
export const notificationDigestPreferenceMetadata = {
  modelName: "NotificationDigestPreference",
  table: "notification_digest_preferences",

  fields: {
    user_id: {
      type: "uuid",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      relation: {
        model: "User",
        table: "users",
        onDelete: "CASCADE",
      },
      label: {},
    },
    cadence: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 10,
      },
      label: {
        ja: "頻度",
        en: "Cadence",
        vi: "Tần suất",
      },
      placeholder: {
        ja: "頻度を入力",
        en: "Enter Cadence",
        vi: "Nhập Tần suất",
      },
      description: {
        ja: "`off`（既定 — 配信しない）、`daily`、または `weekly`。`off` の行は適格性チェックで完全にスキップされるため、配信スループットに影響を与えずにオプトアウト行と並べて保存できる。",
        en: "`off` (default — no digest), `daily`, or `weekly`. Eligibility check skips `off` rows entirely so the digest table can hold opt-out rows alongside opt-ins without affecting send throughput.",
        vi: "`off` (mặc định — không digest), `daily`, hoặc `weekly`. Eligibility check skip toàn bộ row `off` để có thể giữ opt-out row cùng với opt-in mà không ảnh hưởng throughput gửi.",
      },
    },
    delivery_time: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 5,
      },
      label: {
        ja: "配信時刻",
        en: "Delivery Time",
        vi: "Thời điểm gửi",
      },
      placeholder: {
        ja: "配信時刻を入力",
        en: "Enter Delivery Time",
        vi: "Nhập Thời điểm gửi",
      },
      description: {
        ja: "`HH:MM`（ユーザーの `timezone` 基準）。MySQL サーバー間のタイムゾーン変換の予期せぬ動作を避けるため、Time ではなく String として保存。スケジューラは substr/intval で解析し、09:00 の行はユーザーのタイムゾーンで 09:00–09:59 のウィンドウにのみ一致。",
        en: "`HH:MM` in the user's `timezone`. Stored as String (not Time) to dodge DB-time-zone-conversion surprises across MySQL servers; the hourly scheduler parses with substr/intval so a 09:00 row only matches the 09:00–09:59 window in user.timezone.",
        vi: "`HH:MM` (theo `timezone` của user). Lưu dạng String (không Time) để tránh DB-timezone-conversion bất ngờ giữa các MySQL server; scheduler parse bằng substr/intval nên row 09:00 chỉ match window 09:00–09:59 trong user.timezone.",
      },
    },
    timezone: {
      type: "string",
      required: true,
      nullable: false,
      translatable: false,
      validation: {
        maxLength: 64,
      },
      label: {
        ja: "タイムゾーン",
        en: "Timezone",
        vi: "Múi giờ",
      },
      placeholder: {
        ja: "タイムゾーンを入力",
        en: "Enter Timezone",
        vi: "Nhập Múi giờ",
      },
      description: {
        ja: "IANA タイムゾーン名（`Asia/Tokyo` / `America/Los_Angeles`）。PATCH 時に `DateTimeZone::listIdentifiers()` で検証。",
        en: "IANA tz (`Asia/Tokyo` / `America/Los_Angeles`). Validated against `DateTimeZone::listIdentifiers()` on PATCH.",
        vi: "Tên tz IANA (`Asia/Tokyo` / `America/Los_Angeles`). Validate `DateTimeZone::listIdentifiers()` lúc PATCH.",
      },
    },
    weekday: {
      type: "number",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "曜日",
        en: "Weekday",
        vi: "Thứ trong tuần",
      },
      placeholder: {
        ja: "曜日を入力",
        en: "Enter Weekday",
        vi: "Nhập Thứ trong tuần",
      },
      description: {
        ja: "`cadence='weekly'` 時に必須：0=日曜 … 6=土曜。それ以外では Nullable。Validator が cadence×weekday の関係を強制；スキーマは寛容。",
        en: "Required when `cadence='weekly'`: 0=Sunday … 6=Saturday. Nullable otherwise. Validator enforces the cadence×weekday relationship; the schema is permissive.",
        vi: "Bắt buộc khi `cadence='weekly'`: 0=Chủ nhật … 6=Thứ bảy. Nullable trong trường hợp khác. Validator enforce quan hệ cadence×weekday; schema permissive.",
      },
    },
    include_priorities: {
      type: "json",
      required: true,
      nullable: false,
      translatable: false,
      validation: {},
      label: {
        ja: "含める優先度",
        en: "Include Priorities",
        vi: "Bao gồm priority",
      },
      placeholder: {
        ja: "含める優先度を入力",
        en: "Enter Include Priorities",
        vi: "Nhập Bao gồm priority",
      },
      description: {
        ja: "NotificationPriority 値の JSON 配列（既定 `[\"urgent\",\"high\",\"normal\",\"low\"]`）。DigestBuilderService はこの配列でフィルタリングし、ユーザーは urgent の可視性を失わずに低優先度のノイズをオプトアウトできる。",
        en: "JSON array of NotificationPriority values (default `[\"urgent\",\"high\",\"normal\",\"low\"]`). DigestBuilderService filters by this array so users can opt out of low-priority noise without losing urgent visibility.",
        vi: "Mảng JSON các giá trị NotificationPriority (mặc định `[\"urgent\",\"high\",\"normal\",\"low\"]`). DigestBuilderService filter theo mảng này để user có thể opt-out priority thấp mà không mất visibility urgent.",
      },
    },
    last_sent_at: {
      type: "datetime",
      required: false,
      nullable: true,
      translatable: false,
      validation: {},
      label: {
        ja: "最終送信日時",
        en: "Last Sent At",
        vi: "Lần gửi cuối",
      },
      placeholder: {
        ja: "最終送信日時を入力",
        en: "Enter Last Sent At",
        vi: "Nhập Lần gửi cuối",
      },
      description: {
        ja: "`SendDigestJob` が Mailable をキューに入れた後、または空ウィンドウのスキップ後に更新。\"last_sent_at < startOfDay(now())\" ガードが時間ごとの再起動時の二重送信を防ぎ、DST 繰り返しを吸収する。",
        en: "Updated by `SendDigestJob` after the Mailable is queued OR after an empty-window skip. The \"last_sent_at < startOfDay(now())\" guard prevents double-send on hourly re-fires and absorbs DST repeats.",
        vi: "Cập nhật bởi `SendDigestJob` sau khi queue Mailable HOẶC sau khi skip empty-window. Guard \"last_sent_at < startOfDay(now())\" chặn double-send khi hourly re-fire và hấp thụ DST repeats.",
      },
    },
  },

  translatableFields: [] as const,
  requiredFields: ["user_id", "cadence", "delivery_time", "timezone", "include_priorities"] as const,
  enumFields: [] as const,
  relationFields: ["user_id"] as const,
} as const;

export type NotificationDigestPreferenceFieldName = keyof typeof notificationDigestPreferenceMetadata.fields;

// ============================================================================
// Form State + Payload Builders (issue #55)
// ============================================================================

/** Form-state shape for creating a NotificationDigestPreference. Translatable fields are
 *  flat per-locale maps; the builder collapses them into the wire shape. */
export interface NotificationDigestPreferenceCreateFormState {
  user_id: string;
  cadence: string;
  delivery_time: string;
  timezone: string;
  weekday?: number | null;
  include_priorities: unknown;
  last_sent_at?: string | null;
}

/** Form-state shape for updating a NotificationDigestPreference. Every field optional. */
export interface NotificationDigestPreferenceUpdateFormState {
  user_id?: string;
  cadence?: string;
  delivery_time?: string;
  timezone?: string;
  weekday?: number | null;
  include_priorities?: unknown;
  last_sent_at?: string | null;
}

/** Default-state factory for the create form. Pre-fills every locale key. */
export function emptyNotificationDigestPreferenceCreateForm(): NotificationDigestPreferenceCreateFormState {
  return {
    user_id: '',
    cadence: "off",
    delivery_time: "08:00",
    timezone: "Asia/Tokyo",
    weekday: 0,
    include_priorities: null,
    last_sent_at: '',
  };
}

/** Convert create form state into the payload that baseNotificationDigestPreferenceCreateSchema accepts. */
export function buildNotificationDigestPreferenceCreatePayload(form: NotificationDigestPreferenceCreateFormState): BaseNotificationDigestPreferenceCreate {
  const payload: Record<string, unknown> = {
    user_id: form.user_id.trim(),
    cadence: form.cadence.trim(),
    delivery_time: form.delivery_time.trim(),
    timezone: form.timezone.trim(),
    weekday: form.weekday,
    include_priorities: form.include_priorities,
    last_sent_at: form.last_sent_at,
  };

  return payload as BaseNotificationDigestPreferenceCreate;
}

/** Convert update form state into the payload that baseNotificationDigestPreferenceUpdateSchema accepts. Only fields present on `form` are emitted. */
export function buildNotificationDigestPreferenceUpdatePayload(form: NotificationDigestPreferenceUpdateFormState): BaseNotificationDigestPreferenceUpdate {
  const payload: Record<string, unknown> = {};
  if (form.user_id !== undefined) {
    payload.user_id = form.user_id?.toString().trim() ?? null;
  }
  if (form.cadence !== undefined) {
    payload.cadence = form.cadence?.toString().trim() ?? null;
  }
  if (form.delivery_time !== undefined) {
    payload.delivery_time = form.delivery_time?.toString().trim() ?? null;
  }
  if (form.timezone !== undefined) {
    payload.timezone = form.timezone?.toString().trim() ?? null;
  }
  if (form.weekday !== undefined) {
    payload.weekday = form.weekday;
  }
  if (form.include_priorities !== undefined) {
    payload.include_priorities = form.include_priorities;
  }
  if (form.last_sent_at !== undefined) {
    payload.last_sent_at = form.last_sent_at;
  }
  return payload as BaseNotificationDigestPreferenceUpdate;
}
