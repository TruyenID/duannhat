/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 商品ステータス
 */
export enum ProductStatus {
  Draft = 'draft',
  Pending = 'pending',
  Approved = 'approved',
  Active = 'active',
  Inactive = 'inactive',
  Rejected = 'rejected',
}

/** All ProductStatus values */
export const ProductStatusValues = Object.values(ProductStatus) as ProductStatus[];

/** Type guard for ProductStatus */
export function isProductStatus(value: unknown): value is ProductStatus {
  return ProductStatusValues.includes(value as ProductStatus);
}

const productStatusLabels: Partial<Record<ProductStatus, Record<string, string>>> = {
  [ProductStatus.Draft]: { 'en': 'Draft', 'ja': '下書き', 'vi': 'Bản nháp' },
  [ProductStatus.Pending]: { 'en': 'Pending Approval', 'ja': '承認待ち', 'vi': 'Chờ duyệt' },
  [ProductStatus.Approved]: { 'en': 'Approved', 'ja': '承認済み', 'vi': 'Đã duyệt' },
  [ProductStatus.Active]: { 'en': 'Active', 'ja': '有効', 'vi': 'Đang hoạt động' },
  [ProductStatus.Inactive]: { 'en': 'Inactive', 'ja': '無効', 'vi': 'Tạm dừng' },
  [ProductStatus.Rejected]: { 'en': 'Rejected', 'ja': '却下', 'vi': 'Từ chối' },
};

/** Get label for ProductStatus value with locale support */
export function getProductStatusLabel(value: ProductStatus, locale?: string): string {
  const labels = productStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for ProductStatus value */
export function getProductStatusExtra(_value: ProductStatus): Record<string, unknown> | undefined {
  return undefined;
}
