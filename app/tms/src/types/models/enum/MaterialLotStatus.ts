/**
 * ⚠️ DO NOT EDIT THIS FILE! ⚠️
 * このファイルを編集しないでください！
 *
 * Auto-generated by omnify-ts from schemas.json.
 * Any manual changes will be OVERWRITTEN on next generation.
 */

/**
 * 材料ロットステータス
 */
export enum MaterialLotStatus {
  Active = 'active',
  Quarantined = 'quarantined',
  Depleted = 'depleted',
  Expired = 'expired',
  Disposed = 'disposed',
}

/** All MaterialLotStatus values */
export const MaterialLotStatusValues = Object.values(MaterialLotStatus) as MaterialLotStatus[];

/** Type guard for MaterialLotStatus */
export function isMaterialLotStatus(value: unknown): value is MaterialLotStatus {
  return MaterialLotStatusValues.includes(value as MaterialLotStatus);
}

const materialLotStatusLabels: Partial<Record<MaterialLotStatus, Record<string, string>>> = {
  [MaterialLotStatus.Active]: { 'en': 'Active', 'ja': '有効', 'vi': 'Còn dùng' },
  [MaterialLotStatus.Quarantined]: { 'en': 'Quarantined', 'ja': '隔離', 'vi': 'Cách ly' },
  [MaterialLotStatus.Depleted]: { 'en': 'Depleted', 'ja': '払底', 'vi': 'Đã hết' },
  [MaterialLotStatus.Expired]: { 'en': 'Expired', 'ja': '期限切れ', 'vi': 'Hết hạn' },
  [MaterialLotStatus.Disposed]: { 'en': 'Disposed', 'ja': '廃棄済み', 'vi': 'Đã hủy' },
};

/** Get label for MaterialLotStatus value with locale support */
export function getMaterialLotStatusLabel(value: MaterialLotStatus, locale?: string): string {
  const labels = materialLotStatusLabels[value];
  if (!labels) return value;
  if (locale && labels[locale]) return labels[locale];
  return labels['ja'] ?? labels['en'] ?? Object.values(labels)[0] ?? value;
}

/** Get extra metadata for MaterialLotStatus value */
export function getMaterialLotStatusExtra(_value: MaterialLotStatus): Record<string, unknown> | undefined {
  return undefined;
}
